Function Flush(String color, Integer time, String cgFile)
	If (IsLoad() == -1)
		Return 0
	Endfif
	Integer fFlush
	fFlush := {PUSH_OBJ  10}.isKeyUpdateFlush()
	If (fFlush == -1)
		Return 0
	EndIf
	{PUSH_OBJ  10}.effectFlush(color, time, cgFile)
EndFunc

