Function CheckPlayVoice(String trueName)
	Integer cnfId
	If ({PUSH_OBJ  5}.voiceDetails[0] == -1)
        SetBit(cnfId, 4)
    EndIf
	If ({PUSH_OBJ  5}.voiceDetails[1] == -1)
        SetBit(cnfId, 2)
    EndIf
	If ({PUSH_OBJ  5}.voiceDetails[2] == -1)
        SetBit(cnfId, 3)
    EndIf
	If ({PUSH_OBJ  5}.voiceDetails[3] == -1)
        SetBit(cnfId, 5)
    EndIf
	If ({PUSH_OBJ  5}.voiceDetails[4] == -1)
        SetBit(cnfId, 6)
    EndIf
	If ({PUSH_OBJ  5}.voiceDetails[5] == -1)
        SetBit(cnfId, 9)
    EndIf
	If ({PUSH_OBJ  5}.voiceDetails[6] == -1)
        SetBit(cnfId, 8)
    EndIf
	If ({PUSH_OBJ  5}.voiceDetails[7] == -1)
        SetBit(cnfId, 7)
    EndIf
	If ({PUSH_OBJ  5}.voiceDetails[8] == -1)
        SetBit(cnfId, 24)
    EndIf
	trueName := trueName.Replace("悠", "")
	trueName := trueName.Replace("＆", "")
	Integer fPlayVoice
	fPlayVoice := -1
	If ((trueName.Find("穹") != -1) && (fPlayVoice == -1))
        JUMP_OK _l_135
        fPlayVoice := (cnfId.TestBit(4) == -1)
    EndIf
	If ((trueName.Find("瑛") != -1) && (fPlayVoice == -1))
        JUMP_OK _l_135
        fPlayVoice := (cnfId.TestBit(2) == -1)
    EndIf
	If ((trueName.Find("奈緒") != -1) && (fPlayVoice == -1))
        JUMP_OK _l_135
        fPlayVoice := (cnfId.TestBit(3) == -1)
    EndIf
	If ((trueName.Find("一葉") != -1) && (fPlayVoice == -1))
        JUMP_OK _l_135
        fPlayVoice := (cnfId.TestBit(5) == -1)
    EndIf
	If ((trueName.Find("初佳") != -1) && (fPlayVoice == -1))
        JUMP_OK _l_135
        fPlayVoice := (cnfId.TestBit(6) == -1)
    EndIf
	If ((trueName.Find("委員長") != -1) && (fPlayVoice == -1))
        JUMP_OK _l_135
        fPlayVoice := (cnfId.TestBit(9) == -1)
    EndIf
	If ((trueName.Find("やひろ") != -1) && (fPlayVoice == -1))
        JUMP_OK _l_135
        fPlayVoice := (cnfId.TestBit(8) == -1)
    EndIf
	If ((trueName.Find("亮平") != -1) && (fPlayVoice == -1))
        JUMP_OK _l_135
        fPlayVoice := (cnfId.TestBit(7) == -1)
    EndIf
	trueName := trueName.Replace("穹", "")
	trueName := trueName.Replace("瑛", "")
	trueName := trueName.Replace("奈緒", "")
	trueName := trueName.Replace("一葉", "")
	trueName := trueName.Replace("初佳", "")
	trueName := trueName.Replace("委員長", "")
	trueName := trueName.Replace("やひろ", "")
	trueName := trueName.Replace("亮平", "")
	If ((trueName != "") && (fPlayVoice == -1)
        fPlayVoice := cnfId.TestBit(24)
    EndIf
@_l_333:
	Return fPlayVoice
EndFunc

