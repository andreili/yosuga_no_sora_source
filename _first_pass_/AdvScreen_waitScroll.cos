Function AdvScreen::waitScroll()
	If (this.sprCg.IsActivation() == -1)
		If (isSkip() || {PUSH_UNK}.IsJoyButtonPushing(6))
			this.sprCg.FlushActivation()
		Else
			this.sprCg.WaitUntilSpriteActive(-1)
		EndIf
	EndIf
EndFunc

