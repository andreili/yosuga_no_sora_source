Function BustupManager::create(Integer max)
	this.max := max
	this.info.Remove(0, -1)
	this.spr.Remove(0, -1)
	Integer i
	i := 0
	While (i <= max)
        this.info[i] = BustupInfo
        this.spr[i] = Sprite
        i += 1
    EndWhile
EndFunc

