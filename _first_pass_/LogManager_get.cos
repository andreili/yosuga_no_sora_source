Function LogManager::get(Integer index)
	if (index <= this.max)
		If (this.index - 1 <= index)
			Return this.log[this - 1 - index - this.num]
		EndIf
		Return this.log[this.index - 1 - index]
	EndIf
	Return ""
EndFunc

