Function BustupManager::sortRelation()
	Integer i
	Integer j
	i := 0
@_l_6:
    While (i < 5)
        JUMP_OK _l_44
        j := i + 1
    @_l_16:
        While (j <= 5)
            JUMP_OK _l_39
            If (this.info[i].relation > this.info[j].relation)
                swap(i, j)
            EndIf
            j += 1
        EndWhile
        i += 1
    EndWhile
EndFunc

