Include"yosuga.ch"

Function sc00_A014()
	ScenarioEnter()
	ScPlayBgm("BGM05", 0)
	SetCg("EZ06a", 0, 0)
	Talk("Ryouhei", "RH010152")
	Mess("By the way, Haruka, did you see it yesterday?")
	hitretResult := Hitret(4325)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm, see what?")
	hitretResult := Hitret(4326)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010153")
	Mess("Miku and Takami's Jungle Survival.")
	hitretResult := Hitret(4327)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...That TV show sounds really familiar.")
	hitretResult := Hitret(4328)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010112")
	Mess("Oh, I saw it! They got separated from their college'sÅ^excavation team, and then they got lost in the jungleÅ^with a bunch of poachers.")
	hitretResult := Hitret(4329)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06d", 0, 0)
	Talk("Ryouhei", "RH010154")
	Mess("Yeah, and then they found the ivory! Ah, this isÅ^definitely going to be a hit.")
	hitretResult := Hitret(4330)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Yeah, I saw that. I remember the ending was reallyÅ^good.")
	hitretResult := Hitret(4331)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But it's still on TV? Is there a second season?")
	hitretResult := Hitret(4332)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06a", 0, 0)
	Talk("Ryouhei", "RH010155")
	Mess("Huh? What are you talking about? It's only on the sixthÅ^episode.")
	hitretResult := Hitret(4333)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010113")
	Mess("That's right.")
	hitretResult := Hitret(4334)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...No wait, that show's already over.")
	hitretResult := Hitret(4335)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010156")
	Mess("...Are you confusing it with something else?")
	hitretResult := Hitret(4336)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("N-No... In the city where I used to live, that showÅ^aired last year...")
	hitretResult := Hitret(4337)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010157")
	Mess("Wha-!? D-Don't tell me how it ends! I'm really excitedÅ^about it!")
	hitretResult := Hitret(4338)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010114")
	Mess("You already saw the ending? You're so lucky.")
	hitretResult := Hitret(4339)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... No... So that means stuff gets played on TV laterÅ^out here in the country.")
	hitretResult := Hitret(4340)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010158")
	Mess("Tch, this is why no one likes you cityslickers...")
	hitretResult := Hitret(4341)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see... So the broadcasting stations are different...")
	hitretResult := Hitret(4342)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010159")
	Mess("It's not like they always play fillers! It's Okay ToÅ^Laugh plays at 4 o'clock!")
	hitretResult := Hitret(4343)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then what do they play at noon?")
	hitretResult := Hitret(4344)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010160")
	Mess("Our TV station is doing its best... Don't make fun ofÅ^it.")
	hitretResult := Hitret(4345)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("O-Oh, okay...")
	hitretResult := Hitret(4346)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I didn't notice at all, though I haven't watched much TVÅ^since I got here...")
	hitretResult := Hitret(4347)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I remember there weren't many channels, and I don'tÅ^think the usual programs were playing when I flippedÅ^through the channels.")
	hitretResult := Hitret(4348)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, there were too many channels in the city anyway. IÅ^could never watch that much TV...")
	hitretResult := Hitret(4349)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Maybe I don't have as many things to do for recreationÅ^here, but there isn't such an information overload outÅ^here either.")
	hitretResult := Hitret(4350)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...But I guess that means digital TV still isn'tÅ^broadcasted out here.")
	hitretResult := Hitret(4351)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06c", 0, 0)
	Talk("Kazuha", "KA010041")
	Mess("Could you please focus on the class...")
	hitretResult := Hitret(4352)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06f", 0, 0)
	Talk("Ryouhei", "RH010161")
	Mess("What? Do you want to join in?")
	hitretResult := Hitret(4353)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06e", 0, 0)
	Talk("Kazuha", "KA010042")
	Mess("Th-That's ridiculous!")
	hitretResult := Hitret(4354)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Maths Teacher", "NP120001")
	Mess("Oh, would you all like to answer a question? Nakazato,Å^Kasugano, Amatsume, one each.")
	hitretResult := Hitret(4355)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06c", 0, 0)
	Talk("Ryouhei", "RH010162")
	Mess("Whoa... N-No, don't mind us.")
	hitretResult := Hitret(4356)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010115")
	Mess("Aha~, that might be kind of hard.")
	hitretResult := Hitret(4357)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? A-A question!?")
	hitretResult := Hitret(4358)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Maths Teacher", "NP120002")
	Mess("Oh, Kasugano, you look like you're up for it. All right,Å^come to the front of the class.")
	hitretResult := Hitret(4359)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, o-okay...")
	hitretResult := Hitret(4360)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06f", 0, 0)
	Talk("Ryouhei", "RH010163")
	Mess("You have to get it right, okay?")
	hitretResult := Hitret(4361)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010116")
	Mess("Do your best!")
	hitretResult := Hitret(4362)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Kazuha", "KA010043")
	Mess("I told you...")
	hitretResult := Hitret(4363)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B19a", 0, 0)
	ScPlayBgm("BGM13", 0)
	Talk("Haruka", "")
	Mess("Ugh... What's this...")
	hitretResult := Hitret(4364)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There was a long, complicated formula written on theÅ^board. I didn't know which part to substitute at all.")
	hitretResult := Hitret(4365)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Maths Teacher", "NP120003")
	Mess("Do you need a hint?")
	hitretResult := Hitret(4366)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I'll do my best...")
	hitretResult := Hitret(4367)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Some people behind me started giggling.")
	hitretResult := Hitret(4368)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ugh... Crap, Ryouhei and Amatsume managed to play itÅ^off, too...")
	hitretResult := Hitret(4369)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("U-Um...")
	hitretResult := Hitret(4370)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I expected someone behind me to give me a hint, but...")
	hitretResult := Hitret(4371)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010164")
	Mess("...!")
	hitretResult := Hitret(4372)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010117")
	Mess("...!")
	hitretResult := Hitret(4373)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("They both smiled and ignored me.")
	hitretResult := Hitret(4374)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Damn it.")
	hitretResult := Hitret(4375)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1000, 0)
	SetCg("B19a", 0, 0)
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR010430")
	Mess("...What are you down about?")
	hitretResult := Hitret(4376)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH010165")
	Mess("Oh, just leave him be.")
	hitretResult := Hitret(4377)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK010118")
	Mess("When he was called on earlier, he couldn't solve theÅ^problem. He got extra homework as a punishment.")
	hitretResult := Hitret(4378)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR010431")
	Mess("...Dummy...")
	hitretResult := Hitret(4379)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ugh... That's why I couldn't solve the problem, becauseÅ^I'm a big dummy...")
	hitretResult := Hitret(4380)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH010166")
	Mess("Cheer up. This happens to everyone.")
	hitretResult := Hitret(4381)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK010119")
	Mess("Yeah.")
	hitretResult := Hitret(4382)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	SetBustup("CD01_04M", 0, 0)
	Talk("Kazuha", "KA010044")
	Mess("...Including the two of you, right?")
	hitretResult := Hitret(4383)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH010167")
	Mess("Heh, there was plenty to go around...")
	hitretResult := Hitret(4384)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After I went back to my seat, Ryouhei and Amatsume bothÅ^got called up.")
	hitretResult := Hitret(4385)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_05M", 0, 0)
	Talk("Akira", "AK010120")
	Mess("Kazu-chan, teach me~!")
	hitretResult := Hitret(4386)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_07M", 0, 0)
	Talk("Kazuha", "KA010045")
	Mess("No. Do it yourself.")
	hitretResult := Hitret(4387)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And then they got the same punishment as me...")
	hitretResult := Hitret(4388)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA01_06M", 0, 0)
	Talk("Sora", "SR010432")
	Mess("Jeez.. What are you doing...")
	hitretResult := Hitret(4389)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_04M", 0, 0)
	Talk("Kazuha", "KA010046")
	Mess("If you don't pay attention in class, you're going to endÅ^up with too much extra homework to have fun during theÅ^summer.")
	hitretResult := Hitret(4390)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_09M", 0, 0)
	Talk("Akira", "AK010121")
	Mess("Ehh, that would be awful...")
	hitretResult := Hitret(4391)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH010168")
	Mess("If you'd like, I could be your teacher.")
	hitretResult := Hitret(4392)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPauseBgm()
	BustupClear()
	SetBustup("CA01_04M", 0, 0)
	SetBustup("CC01_01M", 0, 0)
	SetBustup("CD01_12M", 0, 0)
	Talk("Sora & Akira & Kazuha", "SYN00001")
	Mess("Eh!?")
	hitretResult := Hitret(4393)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...")
	hitretResult := Hitret(4394)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScRestartBgm()
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK010123")
	Mess("Really? You're great, Ryou-niichan!")
	hitretResult := Hitret(4395)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA010048")
	Mess("No! If that's what it comes down to, I'll tutor you.")
	hitretResult := Hitret(4396)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Jump(2, 0)
	Talk("Akira", "AK010124")
	Mess("Eh, really? Thank you!")
	hitretResult := Hitret(4397)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH010169")
	Mess("Oh, sorry about that, princess.")
	hitretResult := Hitret(4398)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_13M", 0, 0)
	Jump(5, 0)
	Talk("Kazuha", "KA010049")
	Mess("Eh... Wh-What do you mean?")
	hitretResult := Hitret(4399)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010170")
	Mess("Haruka, you'll join in too, right? We're having aÅ^special study session.")
	hitretResult := Hitret(4400)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Why would we do that?")
	hitretResult := Hitret(4401)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH010171")
	Mess("Okay! We'll do it at Akira's house!")
	hitretResult := Hitret(4402)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010125")
	Mess("That would be great.")
	hitretResult := Hitret(4403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA010050")
	Mess("Ehh!? W-Wait, Akira!")
	hitretResult := Hitret(4404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_14M", 0, 0)
	Talk("Akira", "AK010126")
	Mess("Hehe~, I bet it'll be fun.")
	hitretResult := Hitret(4405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR010434")
	Mess("...If Haru goes... so will I.")
	hitretResult := Hitret(4406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Eh?")
	hitretResult := Hitret(4407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("We all agreed to meet at Amatsume's house tomorrow toÅ^study.")
	hitretResult := Hitret(4408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("DATE", 0)
	ScPlayBgm("BGM05", 0)
	SetCg("B08a", 0, 0)
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK010127")
	Mess("It's here! Have some tea!")
	hitretResult := Hitret(4409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_02M", 0, 0)
	Talk("Ryouhei", "RH010172")
	Mess("Oh, thank you! Are there any snacks?")
	hitretResult := Hitret(4410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_04M", 0, 0)
	Talk("Kazuha", "KA010051")
	Mess("Nakazato-senpai, we didn't come here to play around.")
	hitretResult := Hitret(4411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010128")
	Mess("Ah~, should I go to Hiro-neechan's store and buy some?")
	hitretResult := Hitret(4412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA010052")
	Mess("Like I said, we don't need them. We came to study.")
	hitretResult := Hitret(4413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_05M", 0, 0)
	Talk("Ryouhei", "RH010173")
	Mess("But you need to keep your blood sugar up to thinkÅ^properly. Even at school, our grades would go up if weÅ^could have snacks during class.")
	hitretResult := Hitret(4414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA010053")
	Mess("Please don't try to fool me with that stuff you saw onÅ^TV! Akira will actually believe you!")
	hitretResult := Hitret(4415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_14M", 0, 0)
	Talk("Akira", "AK010129")
	Mess("Snacks, huh~! I would love to have snacks during class.")
	hitretResult := Hitret(4416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_04M", 0, 0)
	Talk("Kazuha", "KA010054")
	Mess("Oh jeez, and I just said it...")
	hitretResult := Hitret(4417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK010130")
	Mess("Oh, but, Kazu-chan brought some snacks. We can eat thoseÅ^afterwards, okay?")
	hitretResult := Hitret(4418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_12M", 0, 0)
	Talk("Kazuha", "KA010055")
	Mess("Wait a sec-... A-Akira, you didn't need to tell themÅ^that.")
	hitretResult := Hitret(4419)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH010174")
	Mess("Oh, so the princess thought ahead!")
	hitretResult := Hitret(4420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_06M", 0, 0)
	Talk("Kazuha", "KA010056")
	Mess("I-It's not like I bought them for you!")
	hitretResult := Hitret(4421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA02_04M", 0, 0)
	Talk("Sora", "SR010435")
	Mess("...Shut up.")
	hitretResult := Hitret(4422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hey, Sora...")
	hitretResult := Hitret(4423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("That's how our study session got started.")
	hitretResult := Hitret(4424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We met at Amatsume's house, a small residence behind theÅ^shrine.")
	hitretResult := Hitret(4425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I had been to the shrine several times before, but neverÅ^inside her house.")
	hitretResult := Hitret(4426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was a very nice house. The inside looked just asÅ^high-class and formal as the outside, but...")
	hitretResult := Hitret(4427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH010175")
	Mess("Akira lives here alone.")
	hitretResult := Hitret(4428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh... Ehh!?")
	hitretResult := Hitret(4429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("According to Ryouhei, the chief priest that manages thisÅ^shrine said he would let her live here as long as sheÅ^wants.")
	hitretResult := Hitret(4431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And the reason she was living alone wasn't because sheÅ^ran away from her family, but rather because she has noÅ^family... apparently.")
	hitretResult := Hitret(4432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...In other words, like me and Sora.")
	hitretResult := Hitret(4433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I vaguely remember her having a grandmother when I was aÅ^child, but I never would have imagined that she had noÅ^family other than that one grandmother.")
	hitretResult := Hitret(4434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She never gave any indication of it. Sora and I wereÅ^shocked that Amatsume could be so cheerful despite herÅ^circumstances.")
	hitretResult := Hitret(4435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Apparently, there are a few elderly people in theÅ^neighborhood who keep an eye on her, as well as a womanÅ^who acts as her guardian.")
	hitretResult := Hitret(4436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The woman Amatsume calls \"Hiro-neechan\" is the owner ofÅ^the candy store here in town. Apparently she's a ratherÅ^eccentric woman.")
	hitretResult := Hitret(4437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I only met her for a moment when I went to that candyÅ^store, but... she doesn't seem like the kind of personÅ^who would be anyone's guardian.")
	hitretResult := Hitret(4438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even if her situation isn't ideal, she's surrounded byÅ^good people, and she's doing her best to live a happyÅ^life.")
	hitretResult := Hitret(4439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...We need to follow her example.")
	hitretResult := Hitret(4440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK010131")
	Mess("Okay then, let's start this study session!")
	hitretResult := Hitret(4441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_02M", 0, 0)
	Talk("Ryouhei", "RH010176")
	Mess("Sora-chan! If there's anything you need help with, I canÅ^help you, okay?")
	hitretResult := Hitret(4442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04M", 0, 0)
	Talk("Sora", "SR010436")
	Mess("...No thanks.")
	hitretResult := Hitret(4443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_04M", 0, 0)
	Talk("Ryouhei", "RH010177")
	Mess("Now, now, don't say that. I am a year ahead of you,Å^after all!")
	hitretResult := Hitret(4444)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	SetBustup("CA02_05M", 0, 0)
	SetBustup("CD02_04M", 0, 0)
	Talk("Kazuha", "KA010057")
	Mess("You better make sure you don't give up your lead.")
	hitretResult := Hitret(4445)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_03M", 0, 0)
	Talk("Ryouhei", "RH010178")
	Mess("Hehe, just wait till you see my test results. This isÅ^just review for me...")
	hitretResult := Hitret(4446)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se010", 0)
	Flush("RED", 500, "")
	SetBustup("CF03_06M", 0, 0)
	Shake(7)
	Talk("Ryouhei", "RH010179")
	Mess("...Ow!?")
	hitretResult := Hitret(4447)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Are you all right?")
	hitretResult := Hitret(4448)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's pen hit him right in the forehead.")
	hitretResult := Hitret(4449)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_02M", 0, 0)
	Talk("Ryouhei", "RH010180")
	Mess("Okay, okay, ask me whatever you want.")
	hitretResult := Hitret(4450)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	BustupClear(5)
	ScStopBgm(1)
	Talk("êSÇÃê∫", "")
	Mess("...")
	hitretResult := Hitret(4451)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_06M", 0, 0)
	Talk("Ryouhei", "RH010181")
	Mess("Huh...?")
	hitretResult := Hitret(4452)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM13", 0)
	Talk("êSÇÃê∫", "")
	Mess("Everyone had already started flipping through theirÅ^textbooks.")
	hitretResult := Hitret(4453)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Shouldn't you get started too?")
	hitretResult := Hitret(4454)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_03M", 0, 0)
	Talk("Ryouhei", "RH010182")
	Mess("I guess so... Haruka, quiz me.")
	hitretResult := Hitret(4455)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What subject?")
	hitretResult := Hitret(4456)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010183")
	Mess("Whatever you want... Come on, give me your best shot.")
	hitretResult := Hitret(4457)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm... Okay, give me the common ratio of the geometricÅ^progression of -3, 2, 6.")
	hitretResult := Hitret(4458)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_06M", 0, 0)
	Jump(7, 0)
	Talk("Ryouhei", "RH010184")
	Mess("Whaaat!? D-Did we learn that?")
	hitretResult := Hitret(4459)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's one of the problems in here.")
	hitretResult := Hitret(4460)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_03M", 0, 0)
	Talk("Ryouhei", "RH010185")
	Mess("I-I was good at... the historical date quizzes. LikeÅ^when was the Kamakura shogunate, and that stuff.")
	hitretResult := Hitret(4461)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That was in 1185, wasn't it?")
	hitretResult := Hitret(4462)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_06M", 0, 0)
	Jump(7, 0)
	Talk("Ryouhei", "RH010186")
	Mess("Wh-What!? I never heard about that!")
	hitretResult := Hitret(4463)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So then, what was the answer to the last one?")
	hitretResult := Hitret(4464)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010187")
	Mess("Eh... Ah, well... Jeez, acting like you're so smart.")
	hitretResult := Hitret(4465)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei just scratched his head. He didn't even look atÅ^his notes.")
	hitretResult := Hitret(4466)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR010437")
	Mess("...-189.")
	hitretResult := Hitret(4467)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_02M", 0, 0)
	Talk("Ryouhei", "RH010188")
	Mess("Yeah, that! I was just about to say it.")
	hitretResult := Hitret(4468)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm, you're correct.")
	hitretResult := Hitret(4469)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010189")
	Mess("Yeah, yeah! Me next, okay? Where was the capital in theÅ^Heian period?")
	hitretResult := Hitret(4470)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka & Sora", "SR010438")
	Mess("Kyoto.")
	hitretResult := Hitret(4471)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then, next... If you throw a ball directly upward at 5Å^meters/second, how fast will it be moving when itÅ^reaches its highest point?")
	hitretResult := Hitret(4472)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_06M", 0, 0)
	Talk("Ryouhei", "RH010190")
	Mess("Wh-What!?")
	hitretResult := Hitret(4473)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010439")
	Mess("Zero meters/second.")
	hitretResult := Hitret(4474)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_04M", 0, 0)
	Talk("Ryouhei", "RH010191")
	Mess("Y-Yeah! That's right! That's what I was thinking.")
	hitretResult := Hitret(4475)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_05M", 0, 0)
	Talk("Ryouhei", "RH010192")
	Mess("Th-This next one is really hard. The capital during theÅ^Heisei period was...")
	hitretResult := Hitret(4476)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nara.")
	hitretResult := Hitret(4477)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_04M", 0, 0)
	Talk("Ryouhei", "RH010193")
	Mess("But! Where was the Fujiwara capital that came before it?")
	hitretResult := Hitret(4478)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010440")
	Mess("Nara.")
	hitretResult := Hitret(4479)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay, next.")
	hitretResult := Hitret(4480)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Can you list the conjugations of irregular sa row verbsÅ^in the mizen, renyou, shuushi, rentai, izen, and meireiÅ^forms?")
	hitretResult := Hitret(4481)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_05M", 0, 0)
	Talk("Ryouhei", "RH010194")
	Mess("Hehe, you picked the wrong subject. This is my strikeÅ^zone!")
	hitretResult := Hitret(4482)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010195")
	Mess("Are, ore, haberi, right there!")
	hitretResult := Hitret(4483)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010441")
	Mess("Se, shi, su, suru, sure, seyo.")
	hitretResult := Hitret(4484)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_06M", 0, 0)
	Talk("Ryouhei", "RH010196")
	Mess("Wha-!? What did you say!?")
	hitretResult := Hitret(4485)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_05M", 0, 0)
	Talk("Sora", "SR010442")
	Mess("Haru... These questions are too easy...")
	hitretResult := Hitret(4486)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK010132")
	Mess("Hehe, Sora-chan just struck you out, Ryou-niichan.")
	hitretResult := Hitret(4487)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_10M", 0, 0)
	Talk("Ryouhei", "RH010197")
	Mess("Damn! Now even you are making fun of me!")
	hitretResult := Hitret(4488)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK010133")
	Mess("But Sora-chan is really amazing. She got them all right.")
	hitretResult := Hitret(4489)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah. I don't know when she's studying, but if nothingÅ^else, her grades are good.")
	hitretResult := Hitret(4490)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04M", 0, 0)
	Talk("Sora", "SR010443")
	Mess("...Don't talk about me like I'm some weirdo.")
	hitretResult := Hitret(4491)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetBustup("CF03_03M", 0, 0)
	Talk("Ryouhei", "RH010198")
	Mess("Sorry!")
	hitretResult := Hitret(4492)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(7)
	Talk("Sora", "SR010444")
	Mess("I had a lot of free time... in the hospital.")
	hitretResult := Hitret(4493)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM16", 0)
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK010134")
	Mess("...The hospital?")
	hitretResult := Hitret(4494)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, Sora... was hospitalized for a while.")
	hitretResult := Hitret(4495)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But she attended just enough days to pass, and she tookÅ^all the tests.")
	hitretResult := Hitret(4496)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("She was at the top of the class even though she wasÅ^never there. Everyone asked who she was.")
	hitretResult := Hitret(4497)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR010445")
	Mess("...")
	hitretResult := Hitret(4498)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK010135")
	Mess("Oh, that's amazing! What do you have to do to becomeÅ^smart?")
	hitretResult := Hitret(4499)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_11M", 0, 0)
	Talk("Sora", "SR010446")
	Mess("I-I haven't done anything amazing...")
	hitretResult := Hitret(4500)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	SetBustup("CD02_04M", 0, 0)
	Talk("Kazuha", "KA010058")
	Mess("You need to learn to focus first, instead of drawing onÅ^your notes.")
	hitretResult := Hitret(4501)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_10M", 0, 0)
	Shake(2)
	Talk("Akira", "AK010136")
	Mess("Ah! Kazu-chan, stop!")
	hitretResult := Hitret(4502)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san started erasing the drawings in Amatsume'sÅ^notebook.")
	hitretResult := Hitret(4503)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_09M", 0, 0)
	Talk("Akira", "AK010137")
	Mess("Ah, it took me forever to draw the teacher as a cat...")
	hitretResult := Hitret(4504)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA010059")
	Mess("I'll help you. Let's work on these practice problems,Å^okay?")
	hitretResult := Hitret(4505)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010138")
	Mess("...Okay...")
	hitretResult := Hitret(4506)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("We should get back to work, too. Um, whose turn is it?")
	hitretResult := Hitret(4507)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_10M", 0, 0)
	Talk("Ryouhei", "RH010199")
	Mess("G-Give me a break...")
	hitretResult := Hitret(4508)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1000, 0)
	SetCg("B30a", 0, 0)
	Flush("WHITE", 2000, "")
	WindowView(1)
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010447")
	Mess("The narrow sky.")
	hitretResult := Hitret(4509)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010448")
	Mess("Even if looking at nature clears up one's mood, all thatÅ^remains of nature in the town is the sky alone.")
	hitretResult := Hitret(4510)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010449")
	Mess("Even when I looked up at that sky... it was narrow.")
	hitretResult := Hitret(4511)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010450")
	Mess("With the buildings and electric lines covering it, itÅ^was almost like looking up at it from a prison or whileÅ^trapped in a spider web.")
	hitretResult := Hitret(4512)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010451")
	Mess("That's right... I was locked up in the prison known asÅ^illness.")
	hitretResult := Hitret(4513)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010452")
	Mess("No prince, ally of justice, or anyone else came to takeÅ^me away from here.")
	hitretResult := Hitret(4514)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010453")
	Mess("Because if I left here, I might die.")
	hitretResult := Hitret(4515)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010454")
	Mess("But when I couldn't leave, he said he'd take its placeÅ^and came to see me.")
	hitretResult := Hitret(4516)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010455")
	Mess("...Haru.")
	hitretResult := Hitret(4517)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010456")
	Mess("When Dad and Mom were always busy with work, Haru cameÅ^to see me.")
	hitretResult := Hitret(4518)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010457")
	Mess("Everyday, we ate together during visiting hours... andÅ^he told me about various things that happened outside.")
	hitretResult := Hitret(4519)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010458")
	Mess("The visiting hours were short... I wanted to be with himÅ^more, but he told me he would come tomorrow, and wentÅ^home.")
	hitretResult := Hitret(4520)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010459")
	Mess("And like promised... he would visit me the next day.")
	hitretResult := Hitret(4521)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010460")
	Mess("And time went by...")
	hitretResult := Hitret(4522)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010461")
	Mess("But even still, I could not leave from here.")
	hitretResult := Hitret(4523)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010462")
	Mess("Haru's personal time increased... and from what he toldÅ^me about school from time to time, I knew that he beganÅ^to make more friends.")
	hitretResult := Hitret(4524)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010463")
	Mess("Since then, the number of times he would visit meÅ^everyday became fewer ...and the number of times heÅ^would send me a text instead of meeting me grew.")
	hitretResult := Hitret(4525)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010464")
	Mess("During the boring time that Haru wouldn't come, I openedÅ^my notebook and studied.")
	hitretResult := Hitret(4526)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010465")
	Mess("Because even if I couldn't attend school, if I kept myÅ^grades up, I could be in the same school as him.")
	hitretResult := Hitret(4527)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010466")
	Mess("...I wanted to be close to Haru, even if just a littleÅ^bit.")
	hitretResult := Hitret(4528)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010467")
	Mess("Suddenly, another text came. Which means, he can't makeÅ^it today.")
	hitretResult := Hitret(4529)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010468")
	Mess("Reading that kind of text is annoying... But I couldn'tÅ^not read it.")
	hitretResult := Hitret(4530)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010469")
	Mess("When I opened it, there was a picture attached of himÅ^playing with a friend with a name I did not know.")
	hitretResult := Hitret(4531)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010470")
	Mess("Those kinds of texts generally didn't have muchÅ^content... It just ended with him saying he would comeÅ^bring me a present tomorrow.")
	hitretResult := Hitret(4532)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010471")
	Mess("In the folder with the collection of photos, they wereÅ^all of Haru having fun with his friends... I wasn't inÅ^there.")
	hitretResult := Hitret(4533)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010472")
	Mess("I wanted... to be in there together with Haru too.")
	hitretResult := Hitret(4534)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010473")
	Mess("I thought I would leave this prison, and try to seeÅ^everything Haru sees, and do everything Haru doesÅ^together with him.")
	hitretResult := Hitret(4535)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010474")
	Mess("Somewhere in my heart... I was waiting for Haru.")
	hitretResult := Hitret(4536)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅs‚uÅt", "SR010475")
	Mess("That some day, he would surely come and take me away.")
	hitretResult := Hitret(4537)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(0, 1)
	WindowView(0)
	ScWait(2000, 0)
	SetCg("B08a", 0, 0)
	SetBustup("CC02_01M", 0, 0)
	Transition("WIP_RL", 500)
	ScPlayBgm("BGM05", 0)
	Talk("Akira", "AK010139")
	Mess("Let's take a break!")
	hitretResult := Hitret(4538)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_02M", 0, 0)
	Talk("Ryouhei", "RH010200")
	Mess("I was waiting for you to say that!")
	hitretResult := Hitret(4539)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK010140")
	Mess("I'll bring some fresh tea for everyone.")
	hitretResult := Hitret(4540)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA010060")
	Mess("I hope you don't mind me putting the snacks on theÅ^table.")
	hitretResult := Hitret(4541)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san took the wrapping off the box she hadÅ^brought, revealing a super expensive-looking box.")
	hitretResult := Hitret(4542)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When she lifted the lid off it, there was a line of riceÅ^cookies individually wrapped in Japanese paper.")
	hitretResult := Hitret(4543)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010201")
	Mess("Whoa, these look expensive.")
	hitretResult := Hitret(4544)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_03M", 0, 0)
	Talk("Kazuha", "KA010061")
	Mess("They were a gift, but they're probably good.")
	hitretResult := Hitret(4545)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK010141")
	Mess("Ah, rice cookies! Yay!")
	hitretResult := Hitret(4546)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA010062")
	Mess("Don't eat it all, okay? You always eat way too much whenÅ^you have something you like.")
	hitretResult := Hitret(4547)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_06M", 0, 0)
	Talk("Akira", "AK010142")
	Mess("I won't do that~.")
	hitretResult := Hitret(4548)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH010202")
	Mess("Come on, Sora-chan, take one. They must be really good.")
	hitretResult := Hitret(4549)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR010476")
	Mess("Haru, pick the one you like.")
	hitretResult := Hitret(4550)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uh... I think they all look good. I guess... I'll goÅ^with the soy flavored one.")
	hitretResult := Hitret(4551)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH010203")
	Mess("Then I'll take the salt flavored one. Sora-chan, I'llÅ^give you half.")
	hitretResult := Hitret(4552)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04M", 0, 0)
	Talk("Sora", "SR010477")
	Mess("No thank you.")
	hitretResult := Hitret(4553)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_10M", 0, 0)
	Talk("Ryouhei", "RH010204")
	Mess("Waahh... Isn't this one awfully salty?")
	hitretResult := Hitret(4554)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK010143")
	Mess("I'll take a soy one too... The salt ones look good too,Å^though.")
	hitretResult := Hitret(4555)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA010063")
	Mess("You can have them both. There are plenty.")
	hitretResult := Hitret(4556)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("Here, Sora.")
	hitretResult := Hitret(4557)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I broke mine in half and gave half to Sora.")
	hitretResult := Hitret(4558)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR010478")
	Mess("...")
	hitretResult := Hitret(4559)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume's room was filled with the smell of deliciousÅ^food and the sound of people eating.")
	hitretResult := Hitret(4560)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("These are really good.")
	hitretResult := Hitret(4561)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010479")
	Mess("...Yeah.")
	hitretResult := Hitret(4562)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK010144")
	Mess("Yeah. By the way, there's a summer festival after theÅ^test ends. Haru-kun, you have to come play.")
	hitretResult := Hitret(4563)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, a festival?")
	hitretResult := Hitret(4564)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK010145")
	Mess("Haru-kun, did you come to the festival when you wereÅ^little?")
	hitretResult := Hitret(4565)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Maybe. I don't really remember...")
	hitretResult := Hitret(4566)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010146")
	Mess("The preparations are a lot of fun, it's almost like aÅ^school festival. Sora-chan, do you want to miko mikoÅ^too?")
	hitretResult := Hitret(4567)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_13M", 0, 0)
	Talk("Sora", "SR010480")
	Mess("...Eh? Miko miko...?")
	hitretResult := Hitret(4568)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Are you talking about the mikos at the festival?")
	hitretResult := Hitret(4569)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010147")
	Mess("Yeah, I am.")
	hitretResult := Hitret(4570)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	SetBustup("CF03_02M", 0, 0)
	Talk("Ryouhei", "RH010205")
	Mess("Miko miko! That sounds great. Hey princess, you andÅ^Haruka should try it too.")
	hitretResult := Hitret(4571)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Whoa, whoa, I'm a guy... but Sora might be a good miko.")
	hitretResult := Hitret(4572)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04M", 0, 0)
	Talk("Sora", "SR010481")
	Mess("...No way.")
	hitretResult := Hitret(4573)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK010148")
	Mess("Aww, what a bummer. What about you, Kazu-chan?")
	hitretResult := Hitret(4574)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	SetBustup("CD02_10M", 0, 0)
	Talk("Kazuha", "KA010064")
	Mess("Eeh, me!? No, I couldn't take on such an importantÅ^role...")
	hitretResult := Hitret(4575)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010206")
	Mess("It's easy. Even Akira can do it, you know?")
	hitretResult := Hitret(4576)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK010149")
	Mess("Aha~, that's right! It's easy! They'll even pay you, andÅ^you can eat whatever you want from all the differentÅ^stands.")
	hitretResult := Hitret(4577)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA010065")
	Mess("I'll help, but... being a miko sounds kind of hard...")
	hitretResult := Hitret(4578)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Really? Don't you just have to sell fortunes and charms?")
	hitretResult := Hitret(4579)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK010150")
	Mess("Well, first you have to go do the purification ceremonyÅ^in the mountains, and then you have to dance the kagura.")
	hitretResult := Hitret(4580)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010151")
	Mess("Then you just have to eat with the customers, and it'sÅ^over.")
	hitretResult := Hitret(4581)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...It sounds hard just listening to you describe it. TheÅ^first part sounded really amazing, but you talk about itÅ^like it's nothing.")
	hitretResult := Hitret(4582)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH010207")
	Mess("In her world, it really is nothing.")
	hitretResult := Hitret(4583)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_03M", 0, 0)
	Talk("Akira", "AK010152")
	Mess("It'll be fun. You have to come. Sora-chan too.")
	hitretResult := Hitret(4584)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA02_11M", 0, 0)
	Talk("Sora", "SR010482")
	Mess("...Sure.")
	hitretResult := Hitret(4585)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_03M", 0, 0)
	Talk("Ryouhei", "RH010208")
	Mess("All right! I'll have to go too, then! I'll get a newÅ^jinbei, too!")
	hitretResult := Hitret(4586)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD02_04M", 0, 0)
	Talk("Kazuha", "KA010066")
	Mess("You need to make sure you get a good grade on the testÅ^before you start worrying about that.")
	hitretResult := Hitret(4587)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC02_06M", 0, 0)
	SetBustup("CF03_10M", 0, 0)
	Talk("Akira & Ryouhei", "SYN00002")
	Mess("Ahh...")
	hitretResult := Hitret(4588)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahaha...")
	hitretResult := Hitret(4589)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("The entire study session was cheerful like that.")
	hitretResult := Hitret(4590)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wouldn't say we were focused on our studies, but we atÅ^least managed to finish the homework we had beenÅ^assigned.")
	hitretResult := Hitret(4591)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san helped Sora catch up with the work she hadÅ^missed while she was absent, too.")
	hitretResult := Hitret(4592)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And most importantly...")
	hitretResult := Hitret(4593)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was just happy that Sora had been here.")
	hitretResult := Hitret(4594)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Because this is probably the kind of experience SoraÅ^always wanted to have...")
	hitretResult := Hitret(4595)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	Change("00_a015a")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

