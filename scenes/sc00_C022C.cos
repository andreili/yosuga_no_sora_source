Include"yosuga.ch"

Function sc00_C022C()
	ScenarioEnter()
	SetCg("B34c", 400, 0)
	ScPlayBgm("BGM09", 0)
	ScPlayEnvSe("SE256", "", 0)
	Talk("Haruka", "")
	Mess("Looks like this meal is becoming a feast.")
	hitretResult := Hitret(23337)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK021252")
	Mess("I'm also staying here for too long. Haru-kun you allÅ^must be tired from the journey, I'm sorry.")
	hitretResult := Hitret(23338)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, I got to eat such delicious curry, it should beÅ^correct to say we gotten stamina instead.")
	hitretResult := Hitret(23339)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm still feeling a bit painful, from somehow finishingÅ^that extra large serving of curry rice.")
	hitretResult := Hitret(23340)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("So I decided to send Akira back, and to get someÅ^exercise to digest the food.")
	hitretResult := Hitret(23341)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Incidentally, Sora gave up and collapsed in her room.")
	hitretResult := Hitret(23342)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The surroundings doesn't have any lamps and it's aÅ^little dim. And then the temperature has also gone downÅ^a bit and the air became cool.")
	hitretResult := Hitret(23343)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira carried the empty pot, while I pushed the bicycleÅ^alongside with her.")
	hitretResult := Hitret(23344)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE256", 1)
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK021253")
	Mess("Haru-kun, thank you for the souvenir~")
	hitretResult := Hitret(23345)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, even though it's not really a thank you gift for theÅ^curry, but it seems you like it very much, it was rightÅ^of me to buy it.")
	hitretResult := Hitret(23346)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021254")
	Mess("Eheheh, let's eat it tomorrow after it has been placedÅ^in the fridge?")
	hitretResult := Hitret(23347)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira opened up the pot's cover and grinned whileÅ^looking at the bag containing the dessert.")
	hitretResult := Hitret(23348)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK021255")
	Mess("But it was good that Haru-kun you all came back.")
	hitretResult := Hitret(23349)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? Of course I'll come back. My home is right here.")
	hitretResult := Hitret(23350)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021256")
	Mess("That's true, Haru-kun's home is right here now.")
	hitretResult := Hitret(23351)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("? ....... Did something happen when I wasn't here?")
	hitretResult := Hitret(23352)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_14M", 0, 0)
	Talk("Akira", "AK021257")
	Mess("I still went around as usual helping out.")
	hitretResult := Hitret(23353)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Is that so...... It's good enough to see you healthy.Å^Akira has no relations with the summer heat.")
	hitretResult := Hitret(23354)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK021258")
	Mess("Haru-kun...... did something happen?")
	hitretResult := Hitret(23355)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh....... nothing special....")
	hitretResult := Hitret(23356)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Akira", "AK021259")
	Mess("You really look like you have a tired face. During theÅ^time when you called out my name.")
	hitretResult := Hitret(23357)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... I-Is that so.......")
	hitretResult := Hitret(23358)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...... At that moment, was my face really thatÅ^terrible......")
	hitretResult := Hitret(23359)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM08", 0)
	SetBustup("CC02_07M", 0, 0)
	Talk("Akira", "AK021260")
	Mess("It's me that's troubling Haru-kun right?")
	hitretResult := Hitret(23360)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's not true...... It's something else.")
	hitretResult := Hitret(23361)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I have an aunt on my relatives side, and she told usÅ^before in the past. We must have problems living on ourÅ^own, and she wants us to move in together with her.")
	hitretResult := Hitret(23362)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("And she mentioned this again, and she talked about itÅ^for a long time...... I said I have my own life here, soÅ^I rejected it.")
	hitretResult := Hitret(23363)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK021261")
	Mess("Haru-kun grew up in the city, surely you must still missÅ^the life over there?")
	hitretResult := Hitret(23364)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You can get use to living anywhere...... These wordsÅ^aren't wrong. Before I knew it I got used to here.")
	hitretResult := Hitret(23365)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I met Akira, Ryouhei, Nao, Migiwa-san and had a greatÅ^time playing with them, and I liked this life and placeÅ^here.")
	hitretResult := Hitret(23366)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So, I'm not going back......")
	hitretResult := Hitret(23367)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021262")
	Mess("Why did Haru-kun think of coming here?")
	hitretResult := Hitret(23368)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh.......")
	hitretResult := Hitret(23369)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021263")
	Mess("Is it because you came here to play when you're a child,Å^and liked this place? Or is it because Nao-oneechan isÅ^here?")
	hitretResult := Hitret(23370)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......... That's because........")
	hitretResult := Hitret(23371)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I had memories that were filled with warmth. I felt IÅ^will be accepted here just like in the past.")
	hitretResult := Hitret(23372)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Everyone will not ask about the unfortunate things aboutÅ^my parents. No one knows about things about me and pry,Å^this is a place where I can start from zero.")
	hitretResult := Hitret(23373)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Yes...... I chose her in order to find a place where IÅ^can escape.")
	hitretResult := Hitret(23374)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("How can I say something like this.......... that is soÅ^embarassing.")
	hitretResult := Hitret(23375)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK021264")
	Mess("You know, when I met Haru-kun for the first time, I feltÅ^that it's such a curious feeling.")
	hitretResult := Hitret(23376)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......?")
	hitretResult := Hitret(23377)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021265")
	Mess("I thought maybe Haru-kun will bring me to a placeÅ^somewhere far away.")
	hitretResult := Hitret(23378)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021266")
	Mess("Therefore, I find that you're a little different fromÅ^everyone.")
	hitretResult := Hitret(23379)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_08M", 0, 0)
	Talk("Akira", "AK021267")
	Mess("But, because you were gone so quickly, I was thinking ifÅ^it's my fault, and made you hated here...... That wasÅ^what I thought.")
	hitretResult := Hitret(23380)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_11M", 0, 0)
	Talk("Akira", "AK021268")
	Mess("....... Even so........ We still met once again.")
	hitretResult := Hitret(23381)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021269")
	Mess("...... I feel really happy to hear that you like me. IÅ^even wondered, if Haru-kun came back just to tell meÅ^this.")
	hitretResult := Hitret(23382)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021270")
	Mess("Therefore......")
	hitretResult := Hitret(23383)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A special person, ever since from that time, I felt thatÅ^there was an unbelievable fate-like thing...... If ifÅ^it's put that way, I feel the same.")
	hitretResult := Hitret(23384)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira has remembered throughout all this time, and hasÅ^used the same smile to welcome me.")
	hitretResult := Hitret(23385)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I also believe our encounter is special.")
	hitretResult := Hitret(23386)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("So I hope one day, I can bring Akira to the outsideÅ^world.")
	hitretResult := Hitret(23387)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Because I feel that if it's Akira, going anywhere isÅ^possible.")
	hitretResult := Hitret(23388)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK021271")
	Mess("Heyy, Haru-kun will you go back someday?")
	hitretResult := Hitret(23391)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No....... Even if I go back, there is nothing there forÅ^me.")
	hitretResult := Hitret(23392)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021272")
	Mess("But, maybe you will think of going back someday?")
	hitretResult := Hitret(23393)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Go back...... I decided to come here.")
	hitretResult := Hitret(23394)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Yes...... This is the place that I can escape too. IÅ^have taken the responsibility and other troublesomeÅ^things and threw them all away.")
	hitretResult := Hitret(23395)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Coming here and do what...... I have naturally notÅ^thought about it.")
	hitretResult := Hitret(23396)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021273")
	Mess("If Haru-kun finds his dream.")
	hitretResult := Hitret(23397)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!? ......My...... Dream........")
	hitretResult := Hitret(23398)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK021274")
	Mess("It's good if you can find it. No, I'm sure Haru-kun canÅ^definitely find it.")
	hitretResult := Hitret(23399)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uhh...... There's nothing like a dream......... SomoneÅ^like myself trying to escape reality........")
	hitretResult := Hitret(23400)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("After my parents' death, besides taking care ofÅ^Sora...... What else can I do......")
	hitretResult := Hitret(23401)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Facing the life over there again...... I can't doÅ^it...... Remembering things about my parents are justÅ^too much for me.")
	hitretResult := Hitret(23402)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My tears were falling when I came back to my senses.")
	hitretResult := Hitret(23403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I didn't even cry at the funeral, why am I crying now.")
	hitretResult := Hitret(23404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I rejected Akira's kind words, and my mind kept thinkingÅ^about the things that I hated about myself.")
	hitretResult := Hitret(23405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Till now, I have never faced reality, and had beenÅ^trying to get sympathy by pretending to have a hardÅ^time.")
	hitretResult := Hitret(23406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I stubbornly refused my aunt's invitation, and even gotÅ^Sora to go along with me.")
	hitretResult := Hitret(23407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I have done everything as I pleased to hide myself.")
	hitretResult := Hitret(23408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And then...... I had fallen in love with Akira duringÅ^this time.")
	hitretResult := Hitret(23409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And to that extent, used Akira as my excuse to stayÅ^here.")
	hitretResult := Hitret(23410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I am really the worst kind of person. I'm really ashamedÅ^of myself.")
	hitretResult := Hitret(23411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If I can't stay here, will I run away to somewhere else?Å^Bringing Sora along......")
	hitretResult := Hitret(23412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_11M", 0, 0)
	Talk("Akira", "AK021275")
	Mess("You can go back anytime. If Haru-kun decides to go back,Å^you can do so tomorrow.")
	hitretResult := Hitret(23413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021276")
	Mess("Haru-kun is thinking of accepting too many things atÅ^once, and you are just exhausted.")
	hitretResult := Hitret(23414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021277")
	Mess("Thinking that you're responsible for everything andÅ^blaming yourself.")
	hitretResult := Hitret(23415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021278")
	Mess("Therefore you can relax here and play as much you withÅ^Ryou-niichan and me, it's fine if you forget about theÅ^things that you dislike.")
	hitretResult := Hitret(23416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021279")
	Mess("Even if you wish to forget about the things that trulyÅ^matter, they will not disappear.")
	hitretResult := Hitret(23417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021280")
	Mess("Haru-kun, when you were young and came here to play, didÅ^your parents come to pick you up when you finally wentÅ^back?")
	hitretResult := Hitret(23418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes...... That is why I feel that if I'm here, it wouldÅ^be as if my parents did not have the accident, and fetchÅ^us when his job is over...... I had thought about itÅ^once before.")
	hitretResult := Hitret(23419)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021281")
	Mess("That is the event where everyone returns home right?Å^Back to where Haru-kun stayed.")
	hitretResult := Hitret(23420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!?")
	hitretResult := Hitret(23421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK021282")
	Mess("Therefore Haru-kun is not running away, but you are justÅ^waiting for them to go back together.")
	hitretResult := Hitret(23422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021283")
	Mess("So, before the time you must return home, you can playÅ^here as much as you want.")
	hitretResult := Hitret(23423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Akira......")
	hitretResult := Hitret(23424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira places her head on my body, as if to fawn on me.")
	hitretResult := Hitret(23425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_11L", 0, 0)
	Talk("Akira", "AK021284")
	Mess("But, I hope that day never comes...... when Haru-kunÅ^goes back.")
	hitretResult := Hitret(23426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't worry...... I have no plans of returning for theÅ^time being. And if I do return, I'll bring Akira too.")
	hitretResult := Hitret(23427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021285")
	Mess("Ehh..... Really?")
	hitretResult := Hitret(23428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes....... I'll answer that expectations, the one thatÅ^you had since you were young.")
	hitretResult := Hitret(23429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021286")
	Mess("Fufuu, thank you......")
	hitretResult := Hitret(23430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No...... I should be the one saying it......")
	hitretResult := Hitret(23431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I set the bike to stand upright, and held the pot thatÅ^Akira was holding.")
	hitretResult := Hitret(23432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though Akira lets me hold onto it, she alsoÅ^snuggles up to me with a comfortable look.")
	hitretResult := Hitret(23433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("To be honest, I have not decided to tell Akira all theseÅ^things.")
	hitretResult := Hitret(23434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But...... If I didn't told her about this, will I beÅ^still think repeatedly about this matter on runningÅ^away?")
	hitretResult := Hitret(23435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I subconsiously called out Akira's name.......")
	hitretResult := Hitret(23436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Did she constantly worry about me from that moment.")
	hitretResult := Hitret(23437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I used such a pathetic explanation...... And AkiraÅ^gently accepted it all.")
	hitretResult := Hitret(23438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even if we have to separate, Akira will most likely pushÅ^my back with a smile. Even if that kills her remainingÅ^feelings.")
	hitretResult := Hitret(23439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, Akira said, ÅuI hope that day never comesÅv.")
	hitretResult := Hitret(23440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My heart that has been dragged down by my feelings, wasÅ^rescued by that one line.")
	hitretResult := Hitret(23441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I continued to hug AKira, till my tears stopped.")
	hitretResult := Hitret(23442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(0, 1)
	ScWait(2000, 0)
	Change("00_c023")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

