Include"yosuga.ch"

Function sc00_B004()
	ScenarioEnter()
	SetCg("B02c", 0, 0)
	Talk("Haruka", "")
	Mess("...Ah... It's already time...")
	hitretResult := Hitret(11362)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM09", 0)
	Talk("êSÇÃê∫", "")
	Mess("As expected, summer is getting near, the sets slowly.")
	hitretResult := Hitret(11363)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's why, it's still bright outside at this time ofÅ^hour, but it seems that the forecast seems to be lateÅ^this time.")
	hitretResult := Hitret(11364)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("During today's lunch time, I recieved a separate lunchÅ^from Nao-chan.")
	hitretResult := Hitret(11365)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After eating everything, I thought of returning theÅ^lunch box.")
	hitretResult := Hitret(11366)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt like returning it right away after washing it,Å^but I didn't noticed that it's already eight'o'clockÅ^since I was so occupied with the things here.")
	hitretResult := Hitret(11367)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...I might disturb her if I give it to her at thisÅ^time...")
	hitretResult := Hitret(11368)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As for now, I should call them first.")
	hitretResult := Hitret(11369)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE153", 0)
	Talk("êSÇÃê∫", "")
	Mess("I took out my cellphone and dialed Nao-chan's homeÅ^number and called.")
	hitretResult := Hitret(11370)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopSe()
	ScPlayEnvSe("SE151", "", 0)
	Talk("êSÇÃê∫", "")
	Mess("Which reminds me, this might be the first time I'mÅ^calling Nao-chan over the phone.")
	hitretResult := Hitret(11371)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt nervous as that thought lingered in my mind.")
	hitretResult := Hitret(11372)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE151", 0)
	Talk("Phone CallÅsìﬁèèÇÃïÍÅt", "NP500001")
	Mess("...Yes, this is Yorihime.")
	hitretResult := Hitret(11373)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, umm, sorry for the bother... This is Kasugano, can IÅ^ask something, is Nao-san there?")
	hitretResult := Hitret(11374)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Phone CallÅsìﬁèèÇÃïÍÅt", "NP500002")
	Mess("Kasugano... Are you related to Kasugano-sensei?")
	hitretResult := Hitret(11375)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes... I'm Haruka, his grandson.")
	hitretResult := Hitret(11376)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Phone CallÅsìﬁèèÇÃïÍÅt", "NP500003")
	Mess("Ha-!! A-Ah... Haruka-chan?")
	hitretResult := Hitret(11377)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes... sorry for not being in touch, and I'm reallyÅ^sorry for not introducing myself properly.")
	hitretResult := Hitret(11378)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Phone CallÅsìﬁèèÇÃïÍÅt", "NP500004")
	Mess("Right now, is your family with you?")
	hitretResult := Hitret(11379)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, it's just me and Sora. Our parents have... alreadyÅ^passed away.")
	hitretResult := Hitret(11380)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Phone CallÅsìﬁèèÇÃïÍÅt", "NP500005")
	Mess("Eh?... Is that so?... I'm sorry to hear your loss... areÅ^you both doing fine?")
	hitretResult := Hitret(11381)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes... Somehow, we are.")
	hitretResult := Hitret(11382)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Also, Nao-cha... Nao-san also helped us in many thingsÅ^already...")
	hitretResult := Hitret(11383)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Phone CallÅsìﬁèèÇÃïÍÅt", "NP500006")
	Mess("Really?... Nao did?... If there happens to be some kindÅ^of trouble, don't hesitate to tell us.")
	hitretResult := Hitret(11384)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes... Thank you very much.")
	hitretResult := Hitret(11385)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Phone CallÅsìﬁèèÇÃïÍÅt", "NP500007")
	Mess("Ah, You were looking for Nao right? Just a moment.")
	hitretResult := Hitret(11386)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes, please.")
	hitretResult := Hitret(11387)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Maybe my call was too sudden that it surprised her...")
	hitretResult := Hitret(11388)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Also, we haven't really told them about what happened toÅ^our parents... though I didn't mean to be impolite.")
	hitretResult := Hitret(11389)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Phone CallÅsìﬁèèÅt", "NO030160")
	Mess("Hello...")
	hitretResult := Hitret(11390)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... Nao-chan? S-Sorry, were you sleeping?")
	hitretResult := Hitret(11391)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030161")
	Mess("Ah... Haru-chan?")
	hitretResult := Hitret(11392)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... Sorry if it was unexpected... it's about theÅ^lunch box that I've borrowed during lunch...")
	hitretResult := Hitret(11393)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030162")
	Mess("Ah, you didn't have to worry about it...")
	hitretResult := Hitret(11394)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I was thinking of giving it back, is it fine if I bringÅ^it there right now?")
	hitretResult := Hitret(11395)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030163")
	Mess("...Right now? Y-Yeah, it's fine but...")
	hitretResult := Hitret(11396)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, I'll get there away.")
	hitretResult := Hitret(11397)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030164")
	Mess("Ah, Haru-chan...")
	hitretResult := Hitret(11398)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...What is it?")
	hitretResult := Hitret(11399)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030165")
	Mess("Since you'll be here right away, is it fine if you waitÅ^for me outside my house?")
	hitretResult := Hitret(11400)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah.")
	hitretResult := Hitret(11401)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030166")
	Mess("Okay then.")
	hitretResult := Hitret(11402)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE152", 0)
	ScStopBgm()
	Talk("Haruka", "")
	Mess(".........?")
	hitretResult := Hitret(11403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopSe()
	BlackOut(0, 1)
	SetCg("B01c", 0, 0)
	ScPlayEnvSe("SE300", "", 0)
	Talk("êSÇÃê∫", "")
	Mess("After washing the lunch box properly and dried it with aÅ^clean piece of cloth, I went outside wearing my sandals.")
	hitretResult := Hitret(11404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan seems to be acting weird... I wonder ifÅ^something happened?")
	hitretResult := Hitret(11405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If they were in the middle of their dinner, I might haveÅ^bothered them.")
	hitretResult := Hitret(11406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I left the house, and immediately went on the corner ofÅ^the road.")
	hitretResult := Hitret(11407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B05c", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("We're neighbors after all, that's why Nao-chan's houseÅ^is right in front of me already.")
	hitretResult := Hitret(11408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess(".........")
	hitretResult := Hitret(11409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se100", 0)
	Talk("êSÇÃê∫", "")
	Mess("I waited outside the house like she told me to, then theÅ^lights turned on. Someone opened the door and went out.")
	hitretResult := Hitret(11410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_01M", 0, 0)
	Talk("Nao", "NO030167")
	Mess("Haru-chanÅ`, sorry for the wait.")
	hitretResult := Hitret(11411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, I should be the one saying that because I called youÅ^out so suddenly, sorry...")
	hitretResult := Hitret(11412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030168")
	Mess("No, it's fine. Tomorrow would also be fine but, I reallyÅ^appreciate it, thank you.")
	hitretResult := Hitret(11413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's nothing, thanks for the lunch. The leftovers wereÅ^also delicious, so we ate all of them.")
	hitretResult := Hitret(11414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_02M", 0, 0)
	Talk("Nao", "NO030169")
	Mess("Really? I'm glad that it suited your taste.")
	hitretResult := Hitret(11415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I also have studied many things on how to make lunch,Å^just wait and see.")
	hitretResult := Hitret(11416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030170")
	Mess("Really? Don't push yourself too hard, I'll always lookÅ^forward to it.")
	hitretResult := Hitret(11417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks, saying that is really helpful to me.")
	hitretResult := Hitret(11418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, that's right... I haven't greeted uncle and auntieÅ^properly yet Nao-chan... Is it all right if I greet themÅ^now?")
	hitretResult := Hitret(11419)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_13M", 0, 0)
	Talk("Nao", "NO030171")
	Mess("...Right now?")
	hitretResult := Hitret(11420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... I want to greet them properly, I also want toÅ^tell them properly on what happened to my parents.")
	hitretResult := Hitret(11421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_10M", 0, 0)
	Talk("Nao", "NO030172")
	Mess(".........")
	hitretResult := Hitret(11422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Nao-chan?")
	hitretResult := Hitret(11423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_13M", 0, 0)
	Talk("Nao", "NO030173")
	Mess("Ah... s-sorry!")
	hitretResult := Hitret(11424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Are you going to bed soon?")
	hitretResult := Hitret(11425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_10M", 0, 0)
	Talk("Nao", "NO030174")
	Mess("Y-Yeah... Also, about the that, I'll just tell themÅ^myself...")
	hitretResult := Hitret(11426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see...")
	hitretResult := Hitret(11427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_01M", 0, 0)
	Talk("Nao", "NO030175")
	Mess("Thank you, for being kind to me lately.")
	hitretResult := Hitret(11428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No... It should be me, since you're helping us inÅ^everything that we need.")
	hitretResult := Hitret(11429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_02M", 0, 0)
	Talk("Nao", "NO030176")
	Mess("Well, it can't be helped. With just the both of you,Å^it's hard.")
	hitretResult := Hitret(11430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030177")
	Mess("We should rely on each other on such things, that's whyÅ^at times like this, you shouldn't hesitate on askingÅ^help from me.")
	hitretResult := Hitret(11431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes, I got it.")
	hitretResult := Hitret(11432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_03M", 0, 0)
	Talk("Nao", "NO030178")
	Mess("Well then, thank you, Haru-chan.")
	hitretResult := Hitret(11433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes, sorry for letting you stay up this late.")
	hitretResult := Hitret(11434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030179")
	Mess("Good night.")
	hitretResult := Hitret(11435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... good night.")
	hitretResult := Hitret(11436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan see me off, watching me the whole time as IÅ^entered my house.")
	hitretResult := Hitret(11437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Nao-chan...")
	hitretResult := Hitret(11438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Usually, Nao-chan smiles naturaly.")
	hitretResult := Hitret(11439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, just when I saw her earlier, she also sounded weirdÅ^when I was talking with her to the phone, it worries me.")
	hitretResult := Hitret(11440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...She doesn't want to talk about it even to me...")
	hitretResult := Hitret(11441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, I want to return the favor, so if there's somethingÅ^I can do, I'll definitely want to help.")
	hitretResult := Hitret(11442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE300", 0)
	Talk("êSÇÃê∫", "")
	Mess("...It's just that, Nao-chan can talk to me easily, yetÅ^she's holding back about something...")
	hitretResult := Hitret(11443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("DATE", 0)
	Change("00_b005")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

