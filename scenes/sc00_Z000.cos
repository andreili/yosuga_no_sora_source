Include"yosuga.ch"

Function sc00_Z000()
	ScenarioEnter()
	ScPlayEnvSe("SE270", "", 0)
	SetCg("B27a", 0, 300)
	Talk("êSÇÃê∫", "")
	Mess("Last updated 2016-11-02. WIP - translation and qualityÅ^check incomplete. Latest info on trjr.wordpress.com.Å^I had a feeling that the sky's color here was extremelyÅ^clear. Unlike the sky in the city, it was a genuineÅ^blue.")
	hitretResult := Hitret(1)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In the same way, it seemed that even the white of theÅ^clouds was different to the ones seen in the city. IÅ^wonder if it's because the air is becoming cleaner.")
	hitretResult := Hitret(2)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The excitement of going on a trip was dying down. Now,Å^while listening to the sleep-inducing \"kachunk kachunk,\"Å^I began to feel completely bored.")
	hitretResult := Hitret(3)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(4)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Every so often, a telephone pole would move past theÅ^corner of my eye.")
	hitretResult := Hitret(5)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I remembered that when I was young, I would entertainÅ^myself by counting these telephone poles.")
	hitretResult := Hitret(6)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I would count while imagining myself jumping from poleÅ^to pole as if they were stakes sticking out of theÅ^water.")
	hitretResult := Hitret(7)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I originally figured that this was a weird idea that IÅ^had dreamt up, but the criminal in a mystery novel IÅ^read recently did the exact same thing.")
	hitretResult := Hitret(8)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The fact that someone commendable thought in a similarÅ^way to me gave me a weird feeling - a mix betweenÅ^security and doubt.")
	hitretResult := Hitret(9)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(10)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The flowing scenery was dull. Just the sight of it canÅ^make a person exhausted.")
	hitretResult := Hitret(11)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's not that I hate relaxing, but a constant lack ofÅ^variety is a different thing altogether.")
	hitretResult := Hitret(12)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("However, because there was another traveler on thisÅ^trip...")
	hitretResult := Hitret(13)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA01e", 0, 0)
	Talk("Haruka", "")
	Mess("Hey, you want to eat some Pochy?")
	hitretResult := Hitret(14)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000001")
	Mess("......")
	hitretResult := Hitret(15)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That said, Sora had been gazing at the scenery withoutÅ^even the slightest movement since a while ago and gaveÅ^no response.")
	hitretResult := Hitret(16)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(17)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With an attitude that didn't take things such as myÅ^feelings into account, she continued to ignore me.")
	hitretResult := Hitret(18)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Maybe she was still angry at me for dragging her out ofÅ^the house so early in the morning.")
	hitretResult := Hitret(19)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But that was something that couldn't be helped.")
	hitretResult := Hitret(20)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("This was because she was someone who you couldn't justÅ^leave alone.")
	hitretResult := Hitret(21)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(22)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("\"A younger sister knows not the older brother's heart,\"Å^...or something like that.")
	hitretResult := Hitret(23)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I took a stick from the box I was holding out and closedÅ^the lid.")
	hitretResult := Hitret(24)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There was still a bit less than 30 minutes until weÅ^would change trains. As for when we would arrive at ourÅ^destination, we were about halfway there.")
	hitretResult := Hitret(25)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Once we arrive, I suspect that it'll be really busy, soÅ^perhaps it's a good time to get some sleep.")
	hitretResult := Hitret(26)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("I sank deep into the chair and closed my eyes.")
	hitretResult := Hitret(27)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The carriage was much more quiet than I thought it wouldÅ^be. The boredom-inducing \"kachunk kachunk\" and shakingÅ^of the train seemed to act as a good lullaby.")
	hitretResult := Hitret(28)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se009", 0)
	Flush("RED", 500, "")
	Talk("Haruka", "")
	Mess("Ow!")
	hitretResult := Hitret(29)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A sharp, sudden pain made me open my eyes and look atÅ^Sora.")
	hitretResult := Hitret(30)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA01b", 0, 0)
	Talk("Haruka", "")
	Mess("W-What?")
	hitretResult := Hitret(31)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if she'd calculated the exact time at which I wouldÅ^start to fall asleep, I was lightly jabbed by a toe.")
	hitretResult := Hitret(32)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000002")
	Mess("......")
	hitretResult := Hitret(33)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At the same time, a hand was extended out toward me...Å^Geez, I wish she could be more straightforward.")
	hitretResult := Hitret(34)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Here!")
	hitretResult := Hitret(35)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I held out the box, she took it.")
	hitretResult := Hitret(36)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA01g", 0, 0)
	Talk("Sora", "SR000003")
	Mess("......")
	hitretResult := Hitret(37)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oi oi, save some for me!")
	hitretResult := Hitret(38)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA01c", 0, 0)
	Talk("Sora", "SR000004")
	Mess("......")
	hitretResult := Hitret(39)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora, who had not wanted any just a moment ago, ate herÅ^Pochy and looked away.")
	hitretResult := Hitret(40)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(41)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While this was considered normal to us, I wonder why itÅ^was so hard to get an answer out of a real sister?")
	hitretResult := Hitret(42)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA01d", 0, 0)
	Talk("Sora", "SR000005")
	Mess("...What?")
	hitretResult := Hitret(43)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Probably troubled by me silently looking at her, sheÅ^looked toward me as if it required great effort.")
	hitretResult := Hitret(44)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nah, it's nothing...I'm going to sleep for a bit.")
	hitretResult := Hitret(45)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA01c", 0, 0)
	Talk("Sora", "SR000006")
	Mess("Okay...")
	hitretResult := Hitret(46)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("After murmuring that single word, Sora looked toward theÅ^window, and this time as I tried to slowly close myÅ^eyes...")
	hitretResult := Hitret(47)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm?")
	hitretResult := Hitret(48)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt something being placed in my lap.")
	hitretResult := Hitret(49)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA01g", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("As I opened my eyes, I realized it was the Pochy box sheÅ^had just taken.")
	hitretResult := Hitret(50)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000007")
	Mess("You can have the rest, Haru.")
	hitretResult := Hitret(51)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh, is that okay with you?")
	hitretResult := Hitret(52)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What's with her? Is it something to do with being leftÅ^alone?")
	hitretResult := Hitret(53)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("So the jab just before was for that reason as well...?Å^Sigh... Would you call her perverse or just gentle?")
	hitretResult := Hitret(54)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA01d", 0, 0)
	Talk("Sora", "SR000008")
	Mess("...But don't eat them all, okay?")
	hitretResult := Hitret(55)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Saying that, she quickly grabbed a few sticks from theÅ^box in my hands.")
	hitretResult := Hitret(56)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oi oi, which is it, then?")
	hitretResult := Hitret(57)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA01c", 0, 0)
	Talk("Sora", "SR000009")
	Mess("H...Huh?")
	hitretResult := Hitret(58)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I could see her hiding her embarrassment by munching onÅ^the Pochy.")
	hitretResult := Hitret(59)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Well things like that are cute too, I suppose.")
	hitretResult := Hitret(60)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("However with things like this, I guess I won't be ableÅ^to sleep...")
	hitretResult := Hitret(61)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Giving up on the nap, I returned to viewing the scenery.")
	hitretResult := Hitret(62)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA01g", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("The place we're heading to now is our father's hometown.")
	hitretResult := Hitret(63)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There's no one there anymore. However, our grandparentsÅ^lived there a long time ago, so we'd stay there forÅ^around two weeks every summer break.")
	hitretResult := Hitret(64)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We'd eat sweets and talk to our parents when we wereÅ^little. As a result, we'd arrive in a blink of the eye.Å^But this time, it's surprisingly far...")
	hitretResult := Hitret(65)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("How many years has it been... since you last went there?")
	hitretResult := Hitret(66)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA01h", 0, 0)
	Talk("Sora", "SR000010")
	Mess("....Dunno.")
	hitretResult := Hitret(67)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It was when you were little, so I guess you don'tÅ^remember it very well, right?")
	hitretResult := Hitret(68)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000011")
	Mess("......Stop treating me like a child - Especially sinceÅ^we're the same age.")
	hitretResult := Hitret(69)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...I'm not treating you like a child. The last time youÅ^went there was a long time ago.")
	hitretResult := Hitret(70)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000012")
	Mess(".........Hmph.")
	hitretResult := Hitret(71)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA01g", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("That's right, we're both the same age...twins.")
	hitretResult := Hitret(72)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm treated as the older one for convinience's sake.Å^However, who's older has no meaning and we're equals, asÅ^far as Sora's concerned.")
	hitretResult := Hitret(73)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("So whenever I start treating her like a child, she getsÅ^pissed. But that can't be helped. Mom always told me toÅ^look after her and I've always done just that.")
	hitretResult := Hitret(74)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The places you liked before will still be there.")
	hitretResult := Hitret(75)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000013")
	Mess("......")
	hitretResult := Hitret(76)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wasn't really sure if she was listening or not, but IÅ^continued the conversation.")
	hitretResult := Hitret(77)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The conversation about the countryside we went to whenÅ^we were young.")
	hitretResult := Hitret(78)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	ScStopEnvSe("", 0)
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(79)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000014")
	Mess(".........Haru?")
	hitretResult := Hitret(80)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(81)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Having continued a conversation that was mostlyÅ^one-sided, I was attacked by drowsiness at some pointÅ^and fell into a light sleep.")
	hitretResult := Hitret(82)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM08", 0)
	Talk("Sora", "SR000015")
	Mess("Sigh.....")
	hitretResult := Hitret(83)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I vaguely regained consciousness, I got the feelingÅ^that my head was wrapped in something soft.")
	hitretResult := Hitret(84)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000016")
	Mess("Always deciding things by yourself.........")
	hitretResult := Hitret(85)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000017")
	Mess("Always trying hard by yourself........")
	hitretResult := Hitret(86)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000018")
	Mess("Always taking responsibility.........")
	hitretResult := Hitret(87)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000019")
	Mess("Really, Haru.........")
	hitretResult := Hitret(88)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000020")
	Mess("You're an idiot........")
	hitretResult := Hitret(89)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Absorbing that comforting feeling, I lose consciousness.")
	hitretResult := Hitret(90)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide()
	ScStopBgm()
	ScWait(3000, 0)
	ScPlayEnvSe("SE270", "", 0)
	Talk("Sora", "SR000021")
	Mess("Haru!..........Hey......Wake up!")
	hitretResult := Hitret(91)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My shoulders were being lightly shaken.")
	hitretResult := Hitret(92)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000022")
	Mess("Haru!.....Haru!!!")
	hitretResult := Hitret(93)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA01a", 0, 0)
	Talk("Haruka", "")
	Mess("........Ugh....Ahhh....Have we arrived already?")
	hitretResult := Hitret(94)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It felt like I'd just been separated from somethingÅ^warm.")
	hitretResult := Hitret(95)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I could feel a faint sensation from my arm. It's as if IÅ^was touching something warm......")
	hitretResult := Hitret(96)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000023")
	Mess("....Apparently, we're about to arrive at the station.")
	hitretResult := Hitret(97)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Is that so?...*Yawn*")
	hitretResult := Hitret(98)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As the train began to slow down, I was thrown forwardÅ^while trying to stretch.")
	hitretResult := Hitret(99)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......*stumbles around* Umm...Nothing left behind,Å^right?")
	hitretResult := Hitret(100)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That said, our luggage consisted of a small bag andÅ^rubbish from the box of sweets we'd finished eating andÅ^the juice boxes we drank.")
	hitretResult := Hitret(101)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was looking after Sora's ticket as well. Both of whichÅ^were in my pocket.")
	hitretResult := Hitret(102)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Annoucement", "AN000001")
	Mess("We will be arriving in Hozumi shortly. Please take careÅ^not to leave anything behind.")
	hitretResult := Hitret(103)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Off we go.")
	hitretResult := Hitret(104)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "")
	Mess("......")
	hitretResult := Hitret(105)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The train pulled into the station.")
	hitretResult := Hitret(106)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Finally.")
	hitretResult := Hitret(107)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA01f", 0, 0)
	Talk("Sora", "SR000025")
	Mess("......")
	hitretResult := Hitret(108)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I came here of my own free will. There's no going backÅ^now, and I have no intention of doing so.")
	hitretResult := Hitret(109)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Because it's a place from where I have a lot ofÅ^memories. It's familiar, but despite that, I have a lotÅ^of anxiety.")
	hitretResult := Hitret(110)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But...I think we'll make it somehow.")
	hitretResult := Hitret(111)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess(".Sora... and me. Just the two of us.")
	hitretResult := Hitret(112)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide()
	ScStopEnvSe("", 0)
	BlackOut(0, 0)
	ScPlayMovie("opening")
	Change("00_z001")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

