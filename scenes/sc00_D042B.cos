Include"yosuga.ch"

Function sc00_D042B()
	ScenarioEnter()
	ScPlayBgm("BGM17", 0)
	SetCg("ED16a", 0, 0)
	Talk("Kazuha", "KA041395")
	Mess("Nnn...")
	hitretResult := Hitret(32909)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I hugged her tightly from behind, and started kissingÅ^her just like that.")
	hitretResult := Hitret(32910)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kazuha's lips got wet, as if waiting eagerly for it allÅ^this time.")
	hitretResult := Hitret(32911)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041396")
	Mess("Slurp... Mmm... Slurp... Mmmm... Slurp...")
	hitretResult := Hitret(32912)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While sucking as if pecking on them, the tips of ourÅ^tongues were brushing against each other.")
	hitretResult := Hitret(32913)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041397")
	Mess("Ah... Mmm... Mmm... Lick... Slurp...... Ehehe.")
	hitretResult := Hitret(32914)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Separating for a moment, she giggled and gave me anotherÅ^kiss.")
	hitretResult := Hitret(32915)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Mmmmm... What, was there something strange?")
	hitretResult := Hitret(32916)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041398")
	Mess("Not at all... I just though it's the first time I hadÅ^such a kiss.")
	hitretResult := Hitret(32917)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Did you hate it?")
	hitretResult := Hitret(32918)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041399")
	Mess("The opposite... Mmmm... I love you... Even though it'sÅ^arousing, it's not perverted at all... Mmm, slurp...")
	hitretResult := Hitret(32919)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahmm... Lick...... Mmmm, slurp.")
	hitretResult := Hitret(32920)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041400")
	Mess("Ah...... Fummmmmm! W-Where are you kissing me!?")
	hitretResult := Hitret(32921)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("On the neck.")
	hitretResult := Hitret(32922)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041401")
	Mess("D-Don't answer so calmly... Ah, kuh...")
	hitretResult := Hitret(32923)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I ran along from the back of her ear to the nape of herÅ^neck, starting to kiss as if I were sucking.")
	hitretResult := Hitret(32924)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wanted to hide from embarrassment even if it couldÅ^only be a little bit.")
	hitretResult := Hitret(32925)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041402")
	Mess("Fuah, ah... Kuh... It tickles... Ah...")
	hitretResult := Hitret(32926)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The skin which allowed my lips to crawl upon was smoothÅ^and had a faint, fresh fragnance of soap on it.")
	hitretResult := Hitret(32927)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A smell like flowers wafted from her hair.")
	hitretResult := Hitret(32928)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Could it be that you had bath?")
	hitretResult := Hitret(32929)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041403")
	Mess("Nnn... A shower... You know, it's easy to get dirty...")
	hitretResult := Hitret(32930)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh really... Yeah, it's a pleasant smell.")
	hitretResult := Hitret(32931)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041404")
	Mess("Don't sniff the scent... It's embarrassing.")
	hitretResult := Hitret(32932)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I love that aroma though... Well then, I'll have toÅ^endure it with kissing.")
	hitretResult := Hitret(32933)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041405")
	Mess("What kind of enduring is that...? Ah... Mmm, kuh...Å^Nfu... Ah... Nn...")
	hitretResult := Hitret(32934)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I kissed her lips calmly and started kissing nape of herÅ^neck as if licking once again.")
	hitretResult := Hitret(32935)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I then slowly put my hand on her breast...")
	hitretResult := Hitret(32936)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED16b", 0, 0)
	Talk("Kazuha", "KA041406")
	Mess("Kya!")
	hitretResult := Hitret(32937)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Not minding Kazuha, who raised her voice, I brushedÅ^gently her bulge.")
	hitretResult := Hitret(32938)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041407")
	Mess("You're not... enduring anything at all, right?...")
	hitretResult := Hitret(32939)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I didn't say it would end with just kissing...")
	hitretResult := Hitret(32940)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While repeatedly kissing her around the nape of herÅ^neck, I stroked her breasts, which were covered with aÅ^thick cloth, in a circular motion.")
	hitretResult := Hitret(32941)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED16a", 0, 0)
	Talk("Kazuha", "KA041408")
	Mess("Hah... Kuh... Mmm... Ah... Hah...")
	hitretResult := Hitret(32942)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Your breathing got more wild, didn't it?")
	hitretResult := Hitret(32943)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041409")
	Mess("You're kissing me in weird places and... touching meÅ^there, so...")
	hitretResult := Hitret(32944)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's because I don't want to part from you, Kazuha...")
	hitretResult := Hitret(32945)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041410")
	Mess("If I'll be... held so strongly... I won't be able to doÅ^as much as even go away...")
	hitretResult := Hitret(32946)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then, I'll have to hold you in my arms forever, right?")
	hitretResult := Hitret(32947)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041411")
	Mess("Ah... Mm... Geez... Mmm, hah...")
	hitretResult := Hitret(32948)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Because of the obi squeezing her tighly, I couldn't feelÅ^the softness of her breasts.")
	hitretResult := Hitret(32949)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("However, I realized that under it, there was the prettyÅ^shape of her upward-faced breasts.")
	hitretResult := Hitret(32950)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm touching you, but you don't feel it because of theseÅ^Japanese clothes, huh...")
	hitretResult := Hitret(32951)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041412")
	Mess("...I feel it just enough... Because... the way you'reÅ^moving your hand is perverted...")
	hitretResult := Hitret(32952)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kazuha, your breasts are big.")
	hitretResult := Hitret(32953)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041413")
	Mess("Stop saying such things... It bothers me...")
	hitretResult := Hitret(32954)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? The fact that they're big?")
	hitretResult := Hitret(32955)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041414")
	Mess("... My sister is always rubbing me for fun... WhileÅ^saying that they've gotten bigger...")
	hitretResult := Hitret(32956)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041415")
	Mess("Because they were massaged by her, they got big. It'sÅ^strange isn't it...?")
	hitretResult := Hitret(32957)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Pff.")
	hitretResult := Hitret(32958)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED16b", 0, 0)
	Talk("Kazuha", "KA041416")
	Mess("D-Don't laugh! I-I'm serious here!")
	hitretResult := Hitret(32959)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sorry, sorry. But the two of you scuffled with eachÅ^other like that, huh?")
	hitretResult := Hitret(32960)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041417")
	Mess("I'm not rubbing her! It's only Akira who's coming at meÅ^to rub me out of her own accord!")
	hitretResult := Hitret(32961)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So that's how it is... Kazuha likes being touched,Å^huh... This way.")
	hitretResult := Hitret(32962)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In order to make her feel it through the clothes, I putÅ^more strength into it.")
	hitretResult := Hitret(32963)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED16a", 0, 0)
	Talk("Kazuha", "KA041418")
	Mess("Fuuh, ah... Not like... that... Mmm...")
	hitretResult := Hitret(32964)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041419")
	Mess("My sister is just asking \"Can I touch?\" and then comingÅ^at me, so... I'm just unable to reject her...")
	hitretResult := Hitret(32965)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kazuha, you love Amatsume, don't you?")
	hitretResult := Hitret(32966)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041420")
	Mess("Eh? Yes...")
	hitretResult := Hitret(32967)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If you love her, you have to get along with her, right?")
	hitretResult := Hitret(32968)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041421")
	Mess("Yes...")
	hitretResult := Hitret(32969)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kazuha's body has gotten stiff from being startled.")
	hitretResult := Hitret(32970)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Maybe she remembered the time she ended up refusingÅ^Amatsume.")
	hitretResult := Hitret(32971)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("We'll have to have a proper talk with her, right?")
	hitretResult := Hitret(32972)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041422")
	Mess("....................")
	hitretResult := Hitret(32973)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It seems Kazuha has understood that we ought to do so.")
	hitretResult := Hitret(32974)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("However, having understood how awful what she did was,Å^she was unable to nod obediently.")
	hitretResult := Hitret(32975)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Would it be better if Amatsume was the one to be hereÅ^with me right now?")
	hitretResult := Hitret(32976)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041423")
	Mess("Wha-What are you suggesting!?")
	hitretResult := Hitret(32977)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If we had sex together, then maybe you'd become meek,Å^Kazuha.")
	hitretResult := Hitret(32978)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041424")
	Mess("Do-Don't say such foolish things!")
	hitretResult := Hitret(32979)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Do you really want to be with her at any time?")
	hitretResult := Hitret(32980)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041425")
	Mess("Those are two different things. I love Akira, but havingÅ^sex is a different thing.")
	hitretResult := Hitret(32981)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041426")
	Mess("I-I can do such a thing... just with you, Haruka...Å^Geez! Don't make me say such things!")
	hitretResult := Hitret(32982)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kazuha closed her mouth just as she stopped talking,Å^entrusting me with her body.")
	hitretResult := Hitret(32983)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Bu I didn't plan to let her finish with that. I began toÅ^whisper close to her ear.")
	hitretResult := Hitret(32984)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Are your feelings toward me and Amatsume so different?")
	hitretResult := Hitret(32985)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041427")
	Mess("...They're... not different, but... Akira approaches toÅ^toy with me, so...")
	hitretResult := Hitret(32986)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kazuha, you also call me mean often, don't you?")
	hitretResult := Hitret(32987)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041428")
	Mess("That is... but...")
	hitretResult := Hitret(32988)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's no good, you have to face properly... bothÅ^Amatsume and yourself, right?")
	hitretResult := Hitret(32989)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041429")
	Mess("Uuh... Meanie...")
	hitretResult := Hitret(32990)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("After all, I want to see Kazuha feeling meek.")
	hitretResult := Hitret(32991)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041430")
	Mess("Don't dodge the issue... Aah!")
	hitretResult := Hitret(32992)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED16c", 0, 0)
	Talk("Haruka", "")
	Mess("Kazuha, your breasts are pretty.")
	hitretResult := Hitret(32993)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041431")
	Mess("Don't say the same things as Akira!")
	hitretResult := Hitret(32994)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Letting her breasts be exposed, Kazuha shrieked and herÅ^face turned deeply red.")
	hitretResult := Hitret(32995)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041432")
	Mess("Ah, that's why... Mmmm... Touching and making me beÅ^silent... That's not fair...")
	hitretResult := Hitret(32996)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though it's not like I wanted to caress her soÅ^strongly, Kazuha's nipples were already dyed pink and IÅ^was claiming her whole being.")
	hitretResult := Hitret(32997)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041433")
	Mess("Aah... Kuhnn... You keep touching only my breasts, so...")
	hitretResult := Hitret(32998)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Because Kazuha's breasts feel good... Touching themÅ^directly is really...")
	hitretResult := Hitret(32999)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041434")
	Mess("...Akira is only rubbing them, but... you're kneadingÅ^them, so it feels wrong...")
	hitretResult := Hitret(33000)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Between my kneading them and Amatsume's rubbing... whichÅ^one feels better?")
	hitretResult := Hitret(33001)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041435")
	Mess("I-I don't know such a thing...")
	hitretResult := Hitret(33002)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After becoming bright red and turning her face awayÅ^while having her nipples tampered with, her breathingÅ^sped up.")
	hitretResult := Hitret(33003)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("\"I'm sorry, I can't decide which one is better, so IÅ^can't answer.,\" huh?")
	hitretResult := Hitret(33004)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041436")
	Mess("It's not that, right... Don't make me say, that I...Å^like perverted things... or such...")
	hitretResult := Hitret(33005)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, sorry... Slurp.")
	hitretResult := Hitret(33006)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041437")
	Mess("Fuh... Geez, you kissed me on the neck again... Ah...Å^Mnnn...")
	hitretResult := Hitret(33007)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While kneading thoroughly from the bottom of those pairÅ^of bulges, I held her nipple between my fingers.")
	hitretResult := Hitret(33008)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041438")
	Mess("Fuh... Fuah, haah... Kuhn...")
	hitretResult := Hitret(33009)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Thanks to holding her from behind, I made Kazuha's bodyÅ^tremble in my arms each time I pinched her nipples, notÅ^allowing her to escape the accumulating pleasure.")
	hitretResult := Hitret(33010)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her appearance when I made her body bend back and forthÅ^restlessly was cute and made me think of how I love her.")
	hitretResult := Hitret(33011)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kazuha... I love you... From now on, forever... I wantÅ^to be with you...")
	hitretResult := Hitret(33012)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041439")
	Mess("Ah, while kneading... my nipples... Mmmnnn, don't sayÅ^that... Kuh, ahn... Even though I'm happy, ahn, theÅ^pleasant feeling will get stronger...!")
	hitretResult := Hitret(33013)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I want to touch her more... draw out the pleasureÅ^more...")
	hitretResult := Hitret(33014)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But not knowing how to remove a kimono, I slid my handÅ^inside through the place it's hems were joining.")
	hitretResult := Hitret(33015)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041440")
	Mess("Before that...")
	hitretResult := Hitret(33016)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You're saying that you don't wear underwear when wearingÅ^Japanese clothes...")
	hitretResult := Hitret(33017)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While saying so, I advanced with my hand to the gapÅ^between seams.")
	hitretResult := Hitret(33018)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041441")
	Mess("I am wearing them... So, ah... That place... Akh...")
	hitretResult := Hitret(33019)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's a little... wet?")
	hitretResult := Hitret(33020)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041442")
	Mess("!?!?")
	hitretResult := Hitret(33021)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Saying so, Kazuha was at loss of words this time.")
	hitretResult := Hitret(33022)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She looked down and turned bright red all the way toÅ^ears.")
	hitretResult := Hitret(33023)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041443")
	Mess("Kuh... haah... Ah... Ah... Fuah... Mmmm...")
	hitretResult := Hitret(33024)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt it with my fingertips, the sensation of softÅ^fabric and a small moisture.")
	hitretResult := Hitret(33025)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I moved slowly over there as if drawing circles, itÅ^made Kazuha immidiately start to gasp.")
	hitretResult := Hitret(33026)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I pushed through with my index finger, the warmthÅ^started to gradually appear.")
	hitretResult := Hitret(33027)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You can feel the stain spreading, right?")
	hitretResult := Hitret(33028)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041444")
	Mess("... Meanie... Ah, hah... Hah, kuh... My back is...Å^nnn... shivering...")
	hitretResult := Hitret(33029)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if trying to seek out this cavity, my finger crawledÅ^on top of her panties.")
	hitretResult := Hitret(33030)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041445")
	Mess("Uh, ahn... Hah, fuahnnn... Haah... Kuhn...")
	hitretResult := Hitret(33031)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While probing the point that made Kazuha react, I tracedÅ^her panties.")
	hitretResult := Hitret(33032)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041446")
	Mess("Mmm... ah, fuh... Uuhn... Hahn! ... Kuh... Ah... Ahn...Å^Aahn!")
	hitretResult := Hitret(33033)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At the time when I touched something stiff and veryÅ^elastic with my finger, Kazuha bend backward in shock.")
	hitretResult := Hitret(33034)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You can feel it with your clitoris, right?")
	hitretResult := Hitret(33035)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041447")
	Mess("Hyah, huu! That spot, ahn... It feels too good... Kuhnn!")
	hitretResult := Hitret(33036)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Each time I tampered with Kazuha's clitoris, her backÅ^sprung up.")
	hitretResult := Hitret(33037)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041448")
	Mess("Ah, don't... If you'll do it like that... Ah, mmmm!")
	hitretResult := Hitret(33038)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her panties suddenly became wet. I only touched herÅ^clitoris a bit, but I started making glistening threads.")
	hitretResult := Hitret(33039)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Moreover, I should use the present tense, since theÅ^stain is still widening.")
	hitretResult := Hitret(33040)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041449")
	Mess("Kuh... Hah, ah... It's unfair... that only you get to doÅ^it...")
	hitretResult := Hitret(33041)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041450")
	Mess("When Haruka too... over there... became so big...")
	hitretResult := Hitret(33042)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kazuha stroked me as I was becoming bigger beneath her,Å^on the top of my trousers.")
	hitretResult := Hitret(33043)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The swelled up thing felt as if it was painfullyÅ^scraping my pants. However, it soon changed to aÅ^pleasant feeling.")
	hitretResult := Hitret(33044)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Getting excited while being shy... Will you touch me?")
	hitretResult := Hitret(33045)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041451")
	Mess("Wh-What are you saying...")
	hitretResult := Hitret(33046)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I want her to stroke me even more. Even though I desiredÅ^it, Kazuha just ended up stiffening herself.")
	hitretResult := Hitret(33047)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You don't like it?")
	hitretResult := Hitret(33048)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041452")
	Mess("Don't like you say..... Is it no good... without myÅ^hand?")
	hitretResult := Hitret(33049)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh?")
	hitretResult := Hitret(33050)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041453")
	Mess("I-It's not like... I want do have sex or anything...!Å^U-Umm... Haruka might... be unsatisfied, so...")
	hitretResult := Hitret(33051)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041454")
	Mess("As for me... it's a-already the time for... D-Don't makeÅ^me say it!!")
	hitretResult := Hitret(33052)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ah... So that's what it was... definitely.")
	hitretResult := Hitret(33053)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm sorry, Kazuha. It seems I got a bit mean.")
	hitretResult := Hitret(33054)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I hugged her tightly and gave her a kiss.")
	hitretResult := Hitret(33055)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041455")
	Mess("Ah... Mmm...")
	hitretResult := Hitret(33056)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Me too... I longed to become one with her.")
	hitretResult := Hitret(33057)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Is it okay for me to remove your underwear?")
	hitretResult := Hitret(33058)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041456")
	Mess("Yeah... Please...")
	hitretResult := Hitret(33059)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	Talk("êSÇÃê∫", "")
	Mess("Laying Kazuha down, I rolled up the hems of her kimono.")
	hitretResult := Hitret(33060)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I was about to pull out the panties covering herÅ^secret place, slurp... They made a noise of beingÅ^dripping wet with her secretions.")
	hitretResult := Hitret(33061)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041457")
	Mess("...Ahh...")
	hitretResult := Hitret(33062)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I kissed Kazuha, who averted her face, and pulled herÅ^panties down from her legs.")
	hitretResult := Hitret(33063)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then, I'm putting it in...")
	hitretResult := Hitret(33064)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After that I forced my way through the place betweenÅ^Kazuha's legs.")
	hitretResult := Hitret(33065)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED17a", 0, 0)
	Talk("Kazuha", "KA041458")
	Mess("Kuh... Uuaaahhh!")
	hitretResult := Hitret(33066)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her dripping wet spot had easily accepted my swollenÅ^thing.")
	hitretResult := Hitret(33067)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was hot, slimy, and coiling around me. It feltÅ^incredibly good.")
	hitretResult := Hitret(33068)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041459")
	Mess("Haruka's thing is... filling me up...")
	hitretResult := Hitret(33069)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You wanted me to put it in so much?")
	hitretResult := Hitret(33070)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041460")
	Mess("Yes..... Ag, th-that's not it!?")
	hitretResult := Hitret(33071)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahaha... You're cute, Kazuha.")
	hitretResult := Hitret(33072)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It seems Kazuha unintentionally leaked out her realÅ^intentions and is now flustered.")
	hitretResult := Hitret(33073)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("For the time being, I stayed motionless, holding herÅ^like that.")
	hitretResult := Hitret(33074)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041461")
	Mess("I can feel yours... Haruka... It's twitching... andÅ^shivering, right?...")
	hitretResult := Hitret(33075)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yours too, Kazuha...")
	hitretResult := Hitret(33076)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Being tied together like this... I felt as if Kazuha'sÅ^feelings were passed along to me.")
	hitretResult := Hitret(33077)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her strong feelings of seeking me. Feelings of notÅ^wanting to part with me.")
	hitretResult := Hitret(33078)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When connecting our bodies together, I felt that weÅ^conveyed something that we wouldn't be able to convey byÅ^using words.")
	hitretResult := Hitret(33079)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When Amatsume kissed her... It wasn't a joke... MaybeÅ^she wanted to pass her feelings to her in such a way...")
	hitretResult := Hitret(33080)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041462")
	Mess("Haruka...?")
	hitretResult := Hitret(33081)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kazuha... You know, Amatsume...")
	hitretResult := Hitret(33082)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041463")
	Mess("...It's okay...")
	hitretResult := Hitret(33083)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But, you know...")
	hitretResult := Hitret(33084)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041464")
	Mess("I understand... The truth is, I understand it... I'mÅ^just scared...")
	hitretResult := Hitret(33085)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041465")
	Mess("I end up imagining that... Akira thinks that a personÅ^like me isn't worth worrying about...")
	hitretResult := Hitret(33086)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even if she knows that that can't be the case, sheÅ^probably can't help but consider the possibility.")
	hitretResult := Hitret(33087)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kazuha's seemingly uneasy throbbing was passed along toÅ^me from the point that we were connected at.")
	hitretResult := Hitret(33088)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Got it... Then... My role is to get rid of Kazuha'sÅ^anxiety.")
	hitretResult := Hitret(33089)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If her feelings are conveyed to me, I should be able toÅ^convey mine too.")
	hitretResult := Hitret(33090)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'll give you a tremendous amount of love so that you'llÅ^stop feeling anxious, Kazuha.")
	hitretResult := Hitret(33091)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041466")
	Mess("Eh?")
	hitretResult := Hitret(33092)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kazuha, I'll teach you... how loved you are!")
	hitretResult := Hitret(33093)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041467")
	Mess("... Ah, fuaaaah!!")
	hitretResult := Hitret(33094)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Putting my feelings into it, I forced my back to startÅ^moving like a piston.")
	hitretResult := Hitret(33095)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041468")
	Mess("Haruka... So suddenly, what are... Kuh, ah, aahn!")
	hitretResult := Hitret(33096)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED17b", 0, 0)
	Talk("Kazuha", "KA041469")
	Mess("Ah, hiin... I-Intense... Aaah, kuh, haahn, ah!")
	hitretResult := Hitret(33097)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While stirring up the inside of her vagina with myÅ^thing, I was pulling it out.")
	hitretResult := Hitret(33098)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041470")
	Mess("Kuhn... Aaah... Afu... Fuhnn...")
	hitretResult := Hitret(33099)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I then rapidly thrusted it in with one go all the wayÅ^inside her vagina.")
	hitretResult := Hitret(33100)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041471")
	Mess("Hyaahn! Ah, hii, nnaaahn...")
	hitretResult := Hitret(33101)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kazuha's secretions were overflowing just by thrustingÅ^it in. No matter how rough I striked it in with my back,Å^she was feeling it.")
	hitretResult := Hitret(33102)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041472")
	Mess("Ah, ah, kuh, ahn, hiih, ah, uu... Ahh, haahn, ah,Å^fuuhn...")
	hitretResult := Hitret(33103)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I love you... Kazuha... I'll always love you... Kuh...")
	hitretResult := Hitret(33104)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041473")
	Mess("Kuhaaahn, ah, hin, fuaaaah, aaaahn!")
	hitretResult := Hitret(33105)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Calling Kazuha's name made her vagina quiver.")
	hitretResult := Hitret(33106)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Worry or whatever, I don't need anything... As long asÅ^we're united in love, it's all right... After all, we'veÅ^became one... once again...")
	hitretResult := Hitret(33107)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041474")
	Mess("Ah, hah... Y-Yeah... Ah, hiihn...")
	hitretResult := Hitret(33108)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Hugging Kazuha closely, I rubbed the glans of my penisÅ^against the interior of her vagina as if to squash it.")
	hitretResult := Hitret(33109)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041475")
	Mess("Uu... Ah... Good, ah... Hah... Kuh...")
	hitretResult := Hitret(33110)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kazuha, I love you... I love you... I love you...")
	hitretResult := Hitret(33111)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While letting my back twist, I whispered this into herÅ^ear.")
	hitretResult := Hitret(33112)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041476")
	Mess("Ah, aaah... Ahn... Fuhn...")
	hitretResult := Hitret(33113)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The sticking of the creases of her flesh,, were makingÅ^obscene noises while separating, making it sound likeÅ^they were sucking me.")
	hitretResult := Hitret(33114)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041477")
	Mess("Haru... Haru...kaa... Ah... Haruka...")
	hitretResult := Hitret(33115)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I kissed Kazuha, who was calling my name with faintÅ^breaths.")
	hitretResult := Hitret(33116)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041478")
	Mess("Nfu, kiss, lick... Ah, hamph... Kiss, lick, slurp...")
	hitretResult := Hitret(33117)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While kissing, I stabbed her insides again.")
	hitretResult := Hitret(33118)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041479")
	Mess("Nfuah! Ah, hah, mmm, ahn! ... Lick, mmmm, agh, slurp...")
	hitretResult := Hitret(33119)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I love you. I adore you. While having those words in myÅ^mind, I sucked her tongue and stabbed her using my back.")
	hitretResult := Hitret(33120)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At times like this, I just want to make Kazuha all mine,Å^and not leave any for Amatsume.")
	hitretResult := Hitret(33121)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Putting my body together with the person I love, as ifÅ^to drown in the warmth of this happiness.")
	hitretResult := Hitret(33122)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED17a", 0, 0)
	Talk("Kazuha", "KA041480")
	Mess("Ah... Hah... Haru... kaa... Ah...")
	hitretResult := Hitret(33123)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After parting our tongues, a transparent thread ofÅ^saliva was extended between us, connecting me andÅ^Kazuha.")
	hitretResult := Hitret(33124)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kazuha, entranced, opened her eyes and gazed at me withÅ^teary eyes.")
	hitretResult := Hitret(33125)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You want me to do it more?")
	hitretResult := Hitret(33126)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041481")
	Mess(".....Yeah.")
	hitretResult := Hitret(33127)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kazuha was cute when she nodded so slightly.")
	hitretResult := Hitret(33128)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...I'll always love you.")
	hitretResult := Hitret(33129)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED17b", 0, 0)
	Talk("Kazuha", "KA041482")
	Mess("Kuha, ah, ahn! Fuah, ah, ah, hyan!")
	hitretResult := Hitret(33130)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Inserting it once again, I striked with my hipsÅ^violently.")
	hitretResult := Hitret(33131)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041483")
	Mess("Mmm, slurp! ...Ahmm, nnn... Ah, hah, ah... Slurp, mmm,Å^lick, hahnn, lick...")
	hitretResult := Hitret(33132)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Pressing my lips, I twisted my tongue around up to theÅ^back of her throat.")
	hitretResult := Hitret(33133)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I filled the insides of Kazuha's mouth with my saliva,Å^carressing it to the point of where we'd melt together.")
	hitretResult := Hitret(33134)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041484")
	Mess("Nhaah, mmm, lick, nghmmm...")
	hitretResult := Hitret(33135)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kissing her... continuing my piston motions... massagingÅ^her breasts...")
	hitretResult := Hitret(33136)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Forever... If we could always be like that...")
	hitretResult := Hitret(33137)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I might really be able to away all of the anxiety awayÅ^from Kazuha.")
	hitretResult := Hitret(33138)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041485")
	Mess("Ah, hah, ah, g-good, ahn, ahhn!")
	hitretResult := Hitret(33139)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("However, it would be impossible to do such a thing.")
	hitretResult := Hitret(33140)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kazuha, I love you, Kazuha!")
	hitretResult := Hitret(33141)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041486")
	Mess("Ah, yeah... me too, fuah... I love you too.")
	hitretResult := Hitret(33142)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("No matter how much I'd love her, having sex doesÅ^eventually come to an end.")
	hitretResult := Hitret(33143)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If we finish... then Kazuha might get uneasy again.")
	hitretResult := Hitret(33144)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("So, in order to make up for that, I want to convey theÅ^words \"I love you\" to her many times over.")
	hitretResult := Hitret(33145)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED17a", 0, 0)
	Talk("Kazuha", "KA041487")
	Mess("I understand... Ah... Haruka is thinking... Kuhn...Å^about me...")
	hitretResult := Hitret(33146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041488")
	Mess("Haruka is... here for me... so I... will definitely beÅ^all right...")
	hitretResult := Hitret(33147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041489")
	Mess("Satisfy me, with everything you have... Ahn.")
	hitretResult := Hitret(33148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I only nodded...")
	hitretResult := Hitret(33149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041490")
	Mess("Ah, hah, c-coming... Ah, ahn, fuah, ahn!")
	hitretResult := Hitret(33150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kuh, ah, Kazuha... Kazuhaa...!")
	hitretResult := Hitret(33151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041491")
	Mess("Ah, aah! Hii, hah! Ahh, ahn! Ah... good, ah, ah!")
	hitretResult := Hitret(33152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041492")
	Mess("Ah... Hah... Mmm, ah... Ahn, ah, ah!")
	hitretResult := Hitret(33153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041493")
	Mess("Ah, aah! Aaaah!!")
	hitretResult := Hitret(33154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED17c", 0, 0)
	Flush("WHITE", 1000, "")
	Talk("Kazuha", "KA041494")
	Mess("Nnnaaaaaaaaaaaaaaaaahhhhhh...!!!")
	hitretResult := Hitret(33155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	WhiteOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("I finished together with Kazuha.")
	hitretResult := Hitret(33156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Transition("", 1500)
	SetCg("ED17c", 0, 0)
	Talk("Kazuha", "KA041495")
	Mess("Hah... Hah... Mmm... Haah... hah...")
	hitretResult := Hitret(33157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Whew... haah... Kazuha...")
	hitretResult := Hitret(33158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041496")
	Mess("Haruka... Mmmnn...")
	hitretResult := Hitret(33159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We reached our climaxes at the same time and exchangedÅ^kisses while breathing heavily.")
	hitretResult := Hitret(33160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We just kissed lightly and looked into each other'sÅ^eyes.")
	hitretResult := Hitret(33161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041497")
	Mess("I... was afraid, right?... To become happy...")
	hitretResult := Hitret(33162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041498")
	Mess("I knew about Akira... I knew that my own happiness cameÅ^about through someone else's sacrifice...")
	hitretResult := Hitret(33163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041499")
	Mess("It's not like you making me happy would force Akira toÅ^become miserable...")
	hitretResult := Hitret(33164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kazuha, you're not understanding again.")
	hitretResult := Hitret(33165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041500")
	Mess("Eh?")
	hitretResult := Hitret(33166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's not like only I can make you happy. It's the two ofÅ^us that made our happiness...That combined with theÅ^people around us made our happiness grow.")
	hitretResult := Hitret(33167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't think that it's something that you can onlyÅ^receive. Just think \"I'm happy\" with confidence.")
	hitretResult := Hitret(33168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041501")
	Mess("Yes... You're right...")
	hitretResult := Hitret(33169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041502")
	Mess("It's okay to not let go of this happiness, right...?")
	hitretResult := Hitret(33170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... And then we can share it together withÅ^Amatsume... Together with everyone.")
	hitretResult := Hitret(33171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041503")
	Mess("Yes... I'll do it... I'll do it...")
	hitretResult := Hitret(33172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I believe these words made her another step forward.")
	hitretResult := Hitret(33173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	OnRecollectFlag(15)
	If (IsRecollectMode())
		Change("EXIT_SCENARIO")
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	Change("00_d043")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

