Include"yosuga.ch"

Function sc00_Z001()
	ScenarioEnter()
	ScWait(5000, 0)
	ScPlayBgm("BGM05", 0)
	SetCg("B34a", 0, 0)
	Scroll(966, 0, 20000, 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("How long has it been since Sora has had to walk for suchÅ^a long time?")
	hitretResult := Hitret(113)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000026")
	Mess("Sigh..... Are we there yet?")
	hitretResult := Hitret(114)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It had been around 40 minutes since we started walkingÅ^from the station.")
	hitretResult := Hitret(115)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B34a", 400, 0)
	Update(1)
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR000027")
	Mess("...I'm tired.")
	hitretResult := Hitret(116)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Which makes that the umpteenth time that Sora hasÅ^uttered those words.")
	hitretResult := Hitret(117)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's not every day that you have to walk this far, so IÅ^know it's hard on you... But there isn't very long toÅ^go. Hang in there.")
	hitretResult := Hitret(118)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000028")
	Mess("You said that last time.")
	hitretResult := Hitret(119)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Well if you're only going to say the same thing, I canÅ^only say the same thing back.")
	hitretResult := Hitret(120)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("We've walked further since last time, so we'reÅ^definitely closer.")
	hitretResult := Hitret(121)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_06M", 0, 0)
	Talk("Sora", "SR000029")
	Mess("Uuu........... That doesn't even give me peace of mind.")
	hitretResult := Hitret(122)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Judging from that weak voice barely leaking out of her,Å^it's clear that she wasn't acting.")
	hitretResult := Hitret(123)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Although we rested every now and then, this was probablyÅ^very hard on her feet. She never did have much physicalÅ^strength...")
	hitretResult := Hitret(124)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	Talk("Haruka", "")
	Mess("At any rate...This is some nice weather we'reÅ^having.....")
	hitretResult := Hitret(125)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The more enthusiasm I put into my voice, the more itÅ^feels that the sky I'm looking up at now is further awayÅ^than when it was on the train, and that no matter whereÅ^you look, it's just blue.")
	hitretResult := Hitret(126)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("To our relief, at least the air was as clean as the skyÅ^was clear.....That said, there were places with aÅ^rustling of wind, as well as no humidity. Thanks toÅ^that, we could walk without sweating much.")
	hitretResult := Hitret(127)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If we were having the same stubbornly still air that weÅ^always had in the city we were in just until thisÅ^morning, we'd be sweating buckets and there's no doubtÅ^even I'd be whining.")
	hitretResult := Hitret(128)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Here, I'll push you from behind, so hang in there.")
	hitretResult := Hitret(129)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04M", 7, 0)
	Talk("êSÇÃê∫", "")
	Mess("As I tried to reach for Sora's back, she quickly ranÅ^away.")
	hitretResult := Hitret(130)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_11M", 6, 0)
	Talk("Sora", "SR000030")
	Mess("Stop that...It's embarrassing......")
	hitretResult := Hitret(131)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's not like anyone's looking.....Å^If you don't like it, then please just walk and bearÅ^with it.")
	hitretResult := Hitret(132)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just as we tried to continue.")
	hitretResult := Hitret(133)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_12L", 0, 0)
	Jump(4, 0)
	Talk("Sora", "SR000031")
	Mess("Huh?! KYAAHHHH!!!!")
	hitretResult := Hitret(134)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hm? What's up?")
	hitretResult := Hitret(135)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	Talk("êSÇÃê∫", "")
	Mess("Gripping my shirt sleeve, Sora hid behind my back.")
	hitretResult := Hitret(136)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000032")
	Mess("Something just jumped out from the bushes over there!")
	hitretResult := Hitret(137)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B34a", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Haruka", "")
	Mess("Eh?")
	hitretResult := Hitret(138)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just as she said, a big, green insect had hopped out.")
	hitretResult := Hitret(139)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_13M", 0, 0)
	Jump(4, 0)
	Talk("Sora", "SR000033")
	Mess("Hiiii!?")
	hitretResult := Hitret(140)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahahaha! That's just a grasshopper.")
	hitretResult := Hitret(141)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Two or three more hopped out after the first.")
	hitretResult := Hitret(142)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_05M", 0, 0)
	Talk("Sora", "SR000034")
	Mess("Uuuuu....Get away!!")
	hitretResult := Hitret(143)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	SetCg("B34a", 400, 0)
	Transition("WIP_LR", 500)
	Talk("Haruka", "")
	Mess("Ah, Oi, oi, wait up!")
	hitretResult := Hitret(144)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With that, Sora tried to quickly escape from here.")
	hitretResult := Hitret(145)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hahaha, that's right. You used to be scared ofÅ^grasshoppers back then too.")
	hitretResult := Hitret(146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_11M", 0, 0)
	Talk("Sora", "SR000035")
	Mess("S-Shut up! ...Stop laughing!")
	hitretResult := Hitret(147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sorry, sorry. Anyway, let's get out of here.")
	hitretResult := Hitret(148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if being chased by grasshoppers, Sora stepped up theÅ^pace. Thanks to that, we were able to arrive at ourÅ^destination quicker than I thought.")
	hitretResult := Hitret(149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Hurray for grasshoppers, I guess.")
	hitretResult := Hitret(150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetCg("B01a", 0, 0)
	Talk("Haruka", "")
	Mess("We've finally arrived.")
	hitretResult := Hitret(151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_08M", 0, 0)
	Down(4, 25, 2000, 3)
	Talk("Sora", "SR000036")
	Mess("...Tired...")
	hitretResult := Hitret(152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Losing strength, Sora plopped down onto the ground.")
	hitretResult := Hitret(153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	Update()
	Down(4, -1, 0, 0)
	ScPlayBgm("BGM03", 0)
	Talk("êSÇÃê∫", "")
	Mess("In front of our eyes stood a worn out house with noÅ^signs that a person was living in there.")
	hitretResult := Hitret(154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Kasugano Hospital.Å^So read the worn out sign which hung from the door.")
	hitretResult := Hitret(155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The person who I had called \"grandpa\" was the townÅ^doctor, and the one who I had called \"grandma\" was theÅ^midwife.")
	hitretResult := Hitret(156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("This hospital was managed by the two of them.")
	hitretResult := Hitret(157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Both of them served the community until their deaths. IÅ^remember that grandpa was tall and skinny, while grandmaÅ^was short and plump.")
	hitretResult := Hitret(158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Both of them were really kind, and Sora and I were bothÅ^pampered by them.")
	hitretResult := Hitret(159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After they died, there was no one to succeed them, so inÅ^the usual fashion, the place was cleaned out and closed.")
	hitretResult := Hitret(160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Because it wasn't used for such a long time, we hired aÅ^contractor to clean out the place and get the lights,Å^gas, etc. running so that it was fit for people to liveÅ^in before we arrived.")
	hitretResult := Hitret(161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("........We'll be in your care from now on.")
	hitretResult := Hitret(162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After bowing lightly in greeting, I once again looked upÅ^at the building.")
	hitretResult := Hitret(163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As long as I'm with Sora, we'll somehow be able to liveÅ^here.")
	hitretResult := Hitret(164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As long as it's this place, where childhood memoriesÅ^remain....then we should be able to do it easily.")
	hitretResult := Hitret(165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There's no turning back.")
	hitretResult := Hitret(166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Now then, we have to do some preparations before theÅ^removalists arrive.")
	hitretResult := Hitret(167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora, sorry about this, but I'm thirsty, so can youÅ^please go to a nearby vending mechine....?")
	hitretResult := Hitret(168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Before I could finish, Sora had already charged into theÅ^house.")
	hitretResult := Hitret(169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heeey! Don't you want any juice?")
	hitretResult := Hitret(170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 2, 0)
	Talk("Sora", "SR000037")
	Mess("....Yes.")
	hitretResult := Hitret(171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupLeave(4, -50, 0, 1, 1000, 1)
	Talk("êSÇÃê∫", "")
	Mess("Unexpectedly, Sora poked her head out, said one word,Å^and promptly returned into the house.")
	hitretResult := Hitret(172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("So the one going to buy it was me...I began feelingÅ^slightly downcast.")
	hitretResult := Hitret(174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("After a while, the moving company's vehicle pulled in.")
	hitretResult := Hitret(175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A mountain of cardboard boxes were quickly broughtÅ^inside.")
	hitretResult := Hitret(176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("To some extent, they set up the big things for me, so IÅ^indicated the location of things like the pantry in theÅ^kitchen.")
	hitretResult := Hitret(177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B02a", 0, 0)
	ScPlaySe("SE015", 0)
	Talk("Haruka", "")
	Mess("Where's Sora....?")
	hitretResult := Hitret(178)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I followed a trail of luggage which steadily stretchedÅ^towards the back.")
	hitretResult := Hitret(179)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopSe()
	ScPlayBgm("BGM05", 0)
	SetCg("B04a", 0, 0)
	SetBustup("CA02_01M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Sora had taken up positions in the room at the veryÅ^back.")
	hitretResult := Hitret(180)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("This room's not going to be hit by the sun much, is thatÅ^okay with you?")
	hitretResult := Hitret(181)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000038")
	Mess("...I hate it when it's hot anyway, so it's fine.")
	hitretResult := Hitret(182)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("On the other hand, it's probably going to freeze in theÅ^winter.")
	hitretResult := Hitret(183)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04M", 0, 0)
	Talk("Sora", "SR000039")
	Mess("Hmph....")
	hitretResult := Hitret(184)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Also, mosquitoes are going to come in, so if you'reÅ^going to open the window, we'll have to get a flyscreen.")
	hitretResult := Hitret(185)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000040")
	Mess("What about insecticide?")
	hitretResult := Hitret(186)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haven't bought any yet. I thought we'd just set up aÅ^mosquito net like in the old days, though.")
	hitretResult := Hitret(187)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000041")
	Mess("......Can't be bothered.")
	hitretResult := Hitret(188)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...There you go again. Oh, and after you finishÅ^organizing the stuff over there, could you help me withÅ^the stuff outside?")
	hitretResult := Hitret(189)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	Talk("êSÇÃê∫", "")
	Mess("Just as I started saying that, she opened up herÅ^notebook computer. Looks like she doesn't even have theÅ^slightest intention of helping...")
	hitretResult := Hitret(190)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hey, we're not connected to the internet yet.")
	hitretResult := Hitret(191)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_13M", 0, 0)
	Talk("Sora", "SR000042")
	Mess("Eh?! Why not?")
	hitretResult := Hitret(192)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With a click, Sora closed her laptop in a clearly foulÅ^mood.")
	hitretResult := Hitret(193)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, we did only just move in.")
	hitretResult := Hitret(194)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_06M", 0, 0)
	Talk("Sora", "SR000043")
	Mess(".....Tsk, more like because you're an idiot.....")
	hitretResult := Hitret(195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So I'd be happy if you'd helped me organize -")
	hitretResult := Hitret(196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04M", 0, 0)
	Talk("Sora", "SR000044")
	Mess("What?")
	hitretResult := Hitret(197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Once we're done, I'll go buy the sweets and ice creamÅ^that you like.")
	hitretResult := Hitret(198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000045")
	Mess("Don't want any right now.")
	hitretResult := Hitret(199)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Not just one, but two or three?")
	hitretResult := Hitret(200)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000046")
	Mess("....In the first place, I can't be bothered.")
	hitretResult := Hitret(201)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Stop saying that....Come on...")
	hitretResult := Hitret(202)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("2 hours later.")
	hitretResult := Hitret(203)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("*pant*...It's done...Somehow...")
	hitretResult := Hitret(204)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B02a", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("I plopped myself down onto a chair in the kitchen.")
	hitretResult := Hitret(205)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Before heading over here, I had tried to reduce theÅ^amount of luggage as much as possible, but because evenÅ^the bare essentials were numerous...")
	hitretResult := Hitret(206)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Taking them out of the boxes, sorting through them, andÅ^then arranging them required a lot of effort.")
	hitretResult := Hitret(207)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was a relief that Sora helped out, but unfortunately,Å^we hadn't finished everything yet.")
	hitretResult := Hitret(208)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000047")
	Mess("...Haru.")
	hitretResult := Hitret(209)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	Talk("Haruka", "")
	Mess("Thanks, Sora. You were a great help.")
	hitretResult := Hitret(210)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000048")
	Mess("...*nod*")
	hitretResult := Hitret(211)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If I had to do this all myself, I probably wouldn't evenÅ^be halfway done.")
	hitretResult := Hitret(212)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000049")
	Mess("Haru...")
	hitretResult := Hitret(213)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So if you can help me with the rest...Hmm?")
	hitretResult := Hitret(214)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000050")
	Mess("...Hungry.")
	hitretResult := Hitret(215)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Huh? O-Oh, right...")
	hitretResult := Hitret(216)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As expected when you work for a long period of time, IÅ^only noticed that I was also hungry when she said that.")
	hitretResult := Hitret(217)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But what can I do...? We didn't pack any food in theÅ^luggage.")
	hitretResult := Hitret(218)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And even before we got here and started unpacking, weÅ^finished all the sweets that we had purchased while weÅ^were on the train.")
	hitretResult := Hitret(219)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...So...I guess I'll have to go out and buy something.")
	hitretResult := Hitret(220)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm going shopping then. Is there anything that youÅ^want?")
	hitretResult := Hitret(221)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000051")
	Mess("...Sweets.")
	hitretResult := Hitret(222)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Anything else?")
	hitretResult := Hitret(223)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000052")
	Mess("..Nope.")
	hitretResult := Hitret(224)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay. Here, have the rest of the tea - Drink up and haveÅ^a rest.")
	hitretResult := Hitret(225)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04M", 0, 0)
	Talk("Sora", "SR000053")
	Mess("....It's warm...")
	hitretResult := Hitret(226)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You'll have to bear with it. All right, I'm off.")
	hitretResult := Hitret(227)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	ScStopBgm()
	EyeCatchEnter("", 0)
	SetCg("B34b", 400, 0)
	Talk("êSÇÃê∫", "")
	Mess("...It's far.")
	hitretResult := Hitret(228)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The sun had completely gone down and the crows hadÅ^started cawing.")
	hitretResult := Hitret(229)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM07", 0)
	Talk("Haruka", "")
	Mess("Really, the countryside is like this, huh? ...Was itÅ^like this before?")
	hitretResult := Hitret(230)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was looking for a convenience store, but despite goingÅ^all the way to the station, I couldn't find anything.")
	hitretResult := Hitret(231)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I asked a passerby if there were any, he'd saidÅ^that there was one on the prefectural road, but...")
	hitretResult := Hitret(232)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("it was depressing that getting to that prefectural roadÅ^would take 30 minutes by car.")
	hitretResult := Hitret(233)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Instead, he told me where a supermarket was, so for theÅ^time being, I went shopping over there.")
	hitretResult := Hitret(234)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While I wouldn't call it a supermarket, since it wasÅ^really just a powered up greengrocer, it was a lifelineÅ^of sorts.")
	hitretResult := Hitret(235)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("A bike's going to be necessary here...Preferably aÅ^motorbike, but...")
	hitretResult := Hitret(236)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayEnvSe("SE256", "", 0)
	Talk("êSÇÃê∫", "")
	Mess("......")
	hitretResult := Hitret(237)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("?")
	hitretResult := Hitret(238)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("......")
	hitretResult := Hitret(239)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE256", 0)
	SetCg("B34b", 966, 0)
	Transition("WIP_LR", 500)
	Talk("êSÇÃê∫", "")
	Mess("I suddenly heard something and turned around.")
	hitretResult := Hitret(240)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I thought I had heard the \"karakara\" sound of a bikeÅ^being pushed, but...")
	hitretResult := Hitret(241)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...?")
	hitretResult := Hitret(242)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("However, the only thing there was the road I had beenÅ^walking on.")
	hitretResult := Hitret(243)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayEnvSe("SE256", "", 0)
	SetCg("B34b", 400, 0)
	Transition("WIP_RL", 500)
	Talk("êSÇÃê∫", "")
	Mess("Maybe I'm hearing things because I was thinking aboutÅ^bikes. But a bike...I'm sure the difference betweenÅ^having one and not having one is huge.")
	hitretResult := Hitret(244)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I had never ridden one when I lived in the city, butÅ^over here...going anywhere would make it essential.")
	hitretResult := Hitret(245)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE256", 0)
	Talk("Haruka", "")
	Mess("Hmm?")
	hitretResult := Hitret(246)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What the...? I get the feeling I just heard the noise ofÅ^a bike again.")
	hitretResult := Hitret(247)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B34b", 966, 0)
	Transition("WIP_LR", 500)
	Talk("êSÇÃê∫", "")
	Mess("Upon looking around again, there wasn't even a shadow ofÅ^a person. There was a crumbling farmhouse though.")
	hitretResult := Hitret(248)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Well, whatever.")
	hitretResult := Hitret(249)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The same hallucination caused by my desire to own aÅ^bike? Or am I just going crazy because I'm too tired?")
	hitretResult := Hitret(250)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	WindowView(1)
	ScPlayEnvSe("SE256", "", 0)
	Talk("êSÇÃê∫", "")
	Mess("......")
	hitretResult := Hitret(251)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("......")
	hitretResult := Hitret(252)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE256", 0)
	SetCg("B34b", 0, 0)
	SetBustup("CC06_01M", 0, 0)
	Talk("VoiceÅsâlÅt", "AK000001")
	Mess("...Hmm...That boy is...")
	hitretResult := Hitret(253)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("VoiceÅsâlÅt", "AK000002")
	Mess("...Aha!...Ehehehe...")
	hitretResult := Hitret(254)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("VoiceÅsâlÅt", "AK000003")
	Mess("......")
	hitretResult := Hitret(255)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("VoiceÅsâlÅt", "AK000004")
	Mess("...He's come backÅ`")
	hitretResult := Hitret(256)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	WindowView(0)
	ScWait(3000, 0)
	SetCg("B02c", 0, 0)
	Talk("Haruka", "")
	Mess("...I'm ho-")
	hitretResult := Hitret(257)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_05M", 0, 0)
	Talk("Haruka", "")
	Mess("...Huh?...What's wrong?")
	hitretResult := Hitret(258)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM09", 0)
	Talk("Sora", "SR000054")
	Mess("...Late.")
	hitretResult := Hitret(259)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, sorry.. I couldn't find a shop, so I got stuckÅ^wandering around here and there.")
	hitretResult := Hitret(260)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000055")
	Mess("...I was starting to think you ran away.")
	hitretResult := Hitret(261)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Huh? No way...")
	hitretResult := Hitret(262)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000056")
	Mess("...That.")
	hitretResult := Hitret(263)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Upon looking at what Sora was pointing at...A mobileÅ^phone sitting alone on the table.")
	hitretResult := Hitret(264)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh...I forgot to bring it...Sorry, sorry.")
	hitretResult := Hitret(265)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000057")
	Mess("......")
	hitretResult := Hitret(266)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("In any case, I've bought some stuff, so let's eat.")
	hitretResult := Hitret(267)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000058")
	Mess("...Whatever.")
	hitretResult := Hitret(268)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("An ill-humored voice that sounded as if she'd thrownÅ^everything away.")
	hitretResult := Hitret(269)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("O-Okay...")
	hitretResult := Hitret(270)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_06L", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("As I approached her, she gave me a quick scowl fromÅ^below.")
	hitretResult := Hitret(271)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay, I'm really-")
	hitretResult := Hitret(272)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR000059")
	Mess("...Grrr...")
	hitretResult := Hitret(273)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora brought her face so close that we could feel eachÅ^other's breath.")
	hitretResult := Hitret(274)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her small eyes were staring straight at my apologeticÅ^ones.")
	hitretResult := Hitret(275)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I also looked in Sora's eyes... I was reflecting in herÅ^eyes. We're twins, so it was a situation which felt likeÅ^something magical, but also like staring into a mirror.")
	hitretResult := Hitret(276)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even I, the older brother who she had spent a long timeÅ^with, could not understand what she was thinking behindÅ^those firmly staring eyes.")
	hitretResult := Hitret(277)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But...I felt a sensation of being drawn into those eyes.")
	hitretResult := Hitret(278)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At that moment.")
	hitretResult := Hitret(279)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	Update(1)
	ScStopBgm(1)
	Quake(0, 16, 1, 2, 30)
	Talk("Haruka", "")
	Mess("Ow! ...Sora?")
	hitretResult := Hitret(280)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM13", 0)
	Talk("êSÇÃê∫", "")
	Mess("The moment I flinched, a package of potato chips wasÅ^pulled out from the bags I was holding.")
	hitretResult := Hitret(281)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("H-Hey those are my Consomme flavored chips!")
	hitretResult := Hitret(282)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_09M", 0, 0)
	Talk("Sora", "SR000060")
	Mess("...Huh?")
	hitretResult := Hitret(283)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("*Sigh* Don't eat them all, okay?")
	hitretResult := Hitret(284)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora walked off toward the back of the house without aÅ^word.")
	hitretResult := Hitret(285)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("...Well, it can't be helped. Me being late is a fact,Å^after all.")
	hitretResult := Hitret(286)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I opened my mobile phone, I found an abundance ofÅ^missed calls and new emails.")
	hitretResult := Hitret(287)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Those missed calls and emails were all from Sora.")
	hitretResult := Hitret(288)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("\"You're late.\" \"Still out?\" \"What are you doing?\"Å^\"Haru?\" \"Stop that.\" \"Answer me...\"")
	hitretResult := Hitret(289)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("... \"I was starting to think you ran away,\" huh? ...")
	hitretResult := Hitret(290)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE153", 0)
	Talk("êSÇÃê∫", "")
	Mess("\"Sorry, I'm really sorry.\"")
	hitretResult := Hitret(291)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Entering only that, I sent the email to Sora.")
	hitretResult := Hitret(292)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From the back of the house, the sound indicating a newÅ^email could be faintly heard.")
	hitretResult := Hitret(293)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The reply came immediately.")
	hitretResult := Hitret(294)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess(".\"..Idiot.\"")
	hitretResult := Hitret(295)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	Change("00_z002")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

