Include"yosuga.ch"

Function sc00_D013()
	ScenarioEnter()
	ScPlayBgm("BGM08", 0)
	SetCg("B27b_", 0, 0)
	WindowView(1)
	Transition("CLOUD_A", 1000)
	Talk("モノローグ《一葉》", "KA040260")
	Mess("When I was a child, I was raised with great care.")
	hitretResult := Hitret(28243)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040261")
	Mess("To put it simply, during my childhood, I didn't really／have time to play with friends.")
	hitretResult := Hitret(28244)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040262")
	Mess("My father is a Member of the Diet, so we lived in a／mansion. Thus, people kept their distance from us... The／Migiwa name was rather famous, after all.")
	hitretResult := Hitret(28245)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040263")
	Mess("Although I wasn't bullied, I was lonely because I didn't／have many friends.")
	hitretResult := Hitret(28246)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040264")
	Mess("There was this one time, when I left the house alone／during the absence of my parents.")
	hitretResult := Hitret(28247)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040265")
	Mess("At that time, I only travelled to places with my dad's／secretary chauffeuring me. It was the first time I had／ever left the mansion on foot.")
	hitretResult := Hitret(28248)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040266")
	Mess("Things that I couldn't see from my window... Places／which I had always dreamt of visiting... It was like a／dream, actually being able to touch them...")
	hitretResult := Hitret(28249)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040267")
	Mess("I was unable to contain my nervousness and excitement.／Before long, I was already a considerable distance away／from the mansion.")
	hitretResult := Hitret(28250)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040268")
	Mess("Seeing a red torii, I came across a flight of long／steps. The very same steps which I had seen from the／passenger seat of the car... Unable to control my／enthusiasm, I ran up the stairs.")
	hitretResult := Hitret(28251)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040269")
	Mess("Although I'm called an Ojou-sama, I was really hyper and／energetic at that time. And besides...")
	hitretResult := Hitret(28252)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040270")
	Mess("To a child like me, the torii which extended from the／shrine road made me think it was a passage to another／world.")
	hitretResult := Hitret(28253)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040271")
	Mess("And at the end of that passage, Akira welcomed me to the／shrine grounds at the top of the stairs.")
	hitretResult := Hitret(28254)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B29a_", 0, 0)
	Transition("CLOUD_A", 1000)
	Talk("モノローグ《一葉》", "KA040272")
	Mess("\"Welcome!\" A girl dressed in the traditional shrine／maiden garments greeted me with a bubbly smile.")
	hitretResult := Hitret(28255)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040273")
	Mess("That was how I met Akira.")
	hitretResult := Hitret(28256)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040274")
	Mess("I knew that she would avoid me if I told her my family／name was Migiwa. As a result, I introduced myself as／Kazuha.")
	hitretResult := Hitret(28257)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040275")
	Mess("But Akira, being the carefree person that she was,／didn't mind my identity. She was just a innocent girl／looking for someone to play with.")
	hitretResult := Hitret(28258)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040276")
	Mess("Even after that fateful day, I'd slip out of the mansion／when given the chance and head to the shrine to play／with Akira.")
	hitretResult := Hitret(28259)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040277")
	Mess("Akira was always alone, so we both shared a mutual／feeling, a feeling of loneliness....")
	hitretResult := Hitret(28260)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040278")
	Mess("And also... Well, you've probably already realized, but／at that time, I was a bit of a tomboy...")
	hitretResult := Hitret(28261)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040279")
	Mess("I say tomboy, but when I was in front of others, I／observed my manners, so I only behaved like a tomboy／when I was around Akira and the other children...")
	hitretResult := Hitret(28262)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040280")
	Mess("Anyway... Akira accepted me with that soothing smile.")
	hitretResult := Hitret(28263)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040281")
	Mess("Moreover... even when Akira's grandfather revealed to／her that I was a member of the Migiwa family, Akira's／attitude toward me didn't change at all.")
	hitretResult := Hitret(28264)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040282")
	Mess("\"Playing hide and seek in that huge mansion... that／would be fun,\" she said, laughing as always. That made／me really happy.")
	hitretResult := Hitret(28265)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040283")
	Mess("That was probably the first time I actually made a／friend...or even that I really became friends with／someone.")
	hitretResult := Hitret(28266)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B27a_", 0, 0)
	Transition("CLOUD_A", 1000)
	Talk("モノローグ《一葉》", "KA040284")
	Mess("When we went to school, we were separated into different／classes, but despite that, we remained friends.")
	hitretResult := Hitret(28267)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040285")
	Mess("Because Akira was there for me, I could smile from the／bottom of my heart. Soon, the people around me became／less mindful of my family name and I grew to have many／friends.")
	hitretResult := Hitret(28268)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040286")
	Mess("I'm really grateful that I met Akira. Our accidental／meeting... I'm really glad that we met on that day.")
	hitretResult := Hitret(28269)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040287")
	Mess("But two years ago, when Akira's grandfather passed away,／everything changed.")
	hitretResult := Hitret(28270)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040288")
	Mess("At her grandfather's funeral, as her friend, I knew I／had to attend the funeral and console her during such／times.")
	hitretResult := Hitret(28271)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040289")
	Mess("There, Akira was sitting alone. She wasn't crying, but／was wordlessly deep in thought. I couldn't call out to／her.")
	hitretResult := Hitret(28272)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040290")
	Mess("But the real shock was something I learnt when I was／there.")
	hitretResult := Hitret(28273)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040291")
	Mess("That Akira grew up as an orphan.")
	hitretResult := Hitret(28274)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040292")
	Mess("Amatsume-san had a son with a wife, but Akira was not／their daughter.")
	hitretResult := Hitret(28275)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040293")
	Mess("They said that Amatsume-san took her in because she had／no relatives, not because of blood ties.")
	hitretResult := Hitret(28276)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040294")
	Mess("And his son and wife refused to take care of Akira...")
	hitretResult := Hitret(28277)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040295")
	Mess("In the first place, they had opposed Amatsume-san taking／in Akira, but he forcibly ignored their resistance and／even now, they don't think well of it...")
	hitretResult := Hitret(28278)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040296")
	Mess("Upon learning about that, I tried my best to say／something to the Akira who I loved and cared for, but I／couldn't even comfort her.")
	hitretResult := Hitret(28279)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040297")
	Mess("Even though I felt sorry for Akira being all alone, even／though I wanted to do something for her...")
	hitretResult := Hitret(28280)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040298")
	Mess("A few days after the funeral, I overheard my parents／arguing.")
	hitretResult := Hitret(28281)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040299")
	Mess("My mother who was usually calm and composed, lashed out／at my father with a fury I had never seen before, as I／stood there paralyzed and astounded.")
	hitretResult := Hitret(28282)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040300")
	Mess("Eavesdropping on their conversation, I heard those words／which were the ones that I had longed to hear.")
	hitretResult := Hitret(28283)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040301")
	Mess("My father...said he'd take in Akira.")
	hitretResult := Hitret(28284)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040302")
	Mess("My mind went blank. An inexpressible joy filled me.")
	hitretResult := Hitret(28285)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040303")
	Mess("One child living alone is hard, my house is big, it／should be able to accommodate Akira. Besides, Akira／would be overjoyed as well.")
	hitretResult := Hitret(28286)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040304")
	Mess("Although my father was usually hard to please at times,／he gave me a sense of hope.")
	hitretResult := Hitret(28287)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040305")
	Mess("\"If you insist on adopting that child, I will take／Kazuha and leave.\"")
	hitretResult := Hitret(28288)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040306")
	Mess("I couldn't believe my ears.")
	hitretResult := Hitret(28289)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040307")
	Mess("Having my happiness thrust away, I flew into a rage and／stomped toward them, screaming \"Why!?!\"")
	hitretResult := Hitret(28290)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040308")
	Mess("They were both completely dumbstruck and speechless, but／then the next moment, they started to rebuke me.")
	hitretResult := Hitret(28291)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040309")
	Mess("Even then, I drew closer yelling \"Why?\" ...And／unexpectedly, my mother slapped me.")
	hitretResult := Hitret(28292)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040310")
	Mess("I was dumbfounded... for my mother to slap me... why?／Startled by what she just did, she immediately hugged me／and apologized.")
	hitretResult := Hitret(28293)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040311")
	Mess("But I continued muttering under my voice, \"Why...?\"")
	hitretResult := Hitret(28294)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040312")
	Mess("At last, my father opened his mouth.")
	hitretResult := Hitret(28295)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040313")
	Mess("He started off saying that it was something he hadn't／told me yet, but I would have learnt sooner or later...")
	hitretResult := Hitret(28296)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040314")
	Mess("Akira was my sister, he told me.")
	hitretResult := Hitret(28297)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040315")
	Mess("A child his mistress gave birth to on the same day as／when I was born.")
	hitretResult := Hitret(28298)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040316")
	Mess("Because that woman disappeared soon after giving birth／to Akira, the shrine head took her in as an adopted／daughter. But now that he was dead, he felt that he was／responsible for Akira's welfare.")
	hitretResult := Hitret(28299)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040317")
	Mess("For Akira to suffer from such loneliness. For her to／grow up deprived of parental love. All of it was my／father's fault?")
	hitretResult := Hitret(28300)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040318")
	Mess("Akira and I were sisters?")
	hitretResult := Hitret(28301)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040319")
	Mess("The facts I had just learnt were sending me into／confusion.")
	hitretResult := Hitret(28302)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040320")
	Mess("Despite that, Akira was important to me and I wanted to／help her, so I pleaded that for us to do so.")
	hitretResult := Hitret(28303)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040321")
	Mess("But my mother sternly refused to let it happen.")
	hitretResult := Hitret(28304)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040322")
	Mess("\"I haven't said a word about my husband's／unfaithfulness, so why after all this time do I have to／live with an immoral child?\"")
	hitretResult := Hitret(28305)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040323")
	Mess("\"If you will take her in no matter what, just do what／you want. But it has nothing to do with me nor Kazuha,\"／she said.")
	hitretResult := Hitret(28306)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040324")
	Mess("I could only express my disapproval....")
	hitretResult := Hitret(28307)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040325")
	Mess("I didn't want my parents to seperate, but I also wanted／to help Akira. Not knowing what to do, I could only say／no.")
	hitretResult := Hitret(28308)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040326")
	Mess("Soon, Akira was called to my home and the circumstances／around her birth were explained to her. But Akira turned／down the offer to live with us.")
	hitretResult := Hitret(28309)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040327")
	Mess("While going home that day, I appealed to Akira, saying／that I wanted to live with her, crying all the while.")
	hitretResult := Hitret(28310)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040328")
	Mess("But Akira said that while she was happy that we were／more than friends, she couldn't agree to us living／together.")
	hitretResult := Hitret(28311)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040329")
	Mess("After that...the Migiwa family promised financial／assistance to Ifukube-san to become Akira's guardian,／but that was it.")
	hitretResult := Hitret(28312)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040330")
	Mess("I thought that my father would at least acknowledge／Akira as his daughter, but not even that...")
	hitretResult := Hitret(28313)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040331")
	Mess("I gradually stopped being able to talk to my parents,／and in exchange, I spent more and more time with Akira.")
	hitretResult := Hitret(28314)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA040332")
	Mess("To my parents, especially my mother, it was a／complicated matter, but I couldn't just stop without／doing something.")
	hitretResult := Hitret(28315)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	WindowView(0)
	Change("00_d014")
	Return
	Change("EXIT_SCENARIO")
EndFunc

