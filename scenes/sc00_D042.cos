Include"yosuga.ch"

Function sc00_D042()
	ScenarioEnter()
	ScPlayBgm("BGM17", 0)
	SetCg("B10c", 0, 0)
	Talk("Haruka", "")
	Mess("So this is Kazuha's room...")
	hitretResult := Hitret(32850)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Huge.")
	hitretResult := Hitret(32851)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And everything was so...tidy.")
	hitretResult := Hitret(32852)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The CDs and records were lined up in alphabetical order.Å^Methodical, very much like Kazuha.")
	hitretResult := Hitret(32853)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There was even some huge speakers, a record player and aÅ^clearly expensive high fidelity system.")
	hitretResult := Hitret(32854)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I guess she doesn't just play the viola, but alsoÅ^listens to classical music.")
	hitretResult := Hitret(32855)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The room's almost large enough to make me bendÅ^backward... It's been a while since I was last remindedÅ^of her Ojou-sama status.")
	hitretResult := Hitret(32856)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Speaking of Kazuha, she's gone to change out of herÅ^school uniform, which got dirty when she fell.")
	hitretResult := Hitret(32857)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se101", 0)
	Talk("VoiceÅsèââ¿Åt", "MT040055")
	Mess("Excuse me.")
	hitretResult := Hitret(32858)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_02M", 0, 0)
	Talk("Haruka", "")
	Mess("Ah, Motoka-san.")
	hitretResult := Hitret(32859)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT040056")
	Mess("I've brought you some tea... Eh, it's Haruka-kun?")
	hitretResult := Hitret(32860)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_01M", 0, 0)
	Talk("Motoka", "MT040057")
	Mess("Geez, when I heard that Ojou-sama had a guest, I got allÅ^nervous. Don't surprise me like that.")
	hitretResult := Hitret(32861)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The maid who knocked and entered was wearing a primÅ^smile. With her prudent way of speech, she seemed like aÅ^total stranger but it was definitely someone I knew.")
	hitretResult := Hitret(32862)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I wasn't trying to surprise you... Well, I see you'reÅ^doing your job properly.")
	hitretResult := Hitret(32863)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT040058")
	Mess("How rude. Even though I'm properly recieving my wages.")
	hitretResult := Hitret(32864)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even though your maid etiquette collapses the moment youÅ^learn that the guest is someone you know?")
	hitretResult := Hitret(32865)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_08M", 0, 0)
	Talk("Motoka", "MT040059")
	Mess("Eh, that's not fair...")
	hitretResult := Hitret(32866)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_02M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Even with her face twitching, Motoka-san more or lessÅ^returned to work mode, and poured some tea from theÅ^teapot.")
	hitretResult := Hitret(32867)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A cup for me, and one more.")
	hitretResult := Hitret(32868)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT040060")
	Mess("Here...")
	hitretResult := Hitret(32869)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("T-Thanks...")
	hitretResult := Hitret(32870)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If she starts being this polite, I'd start gettingÅ^nervous, too.")
	hitretResult := Hitret(32871)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I raised my arms to receive the small porcelain cup,Å^Kazuha returned.")
	hitretResult := Hitret(32872)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041384")
	Mess("Welcome back.")
	hitretResult := Hitret(32873)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_02M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Kazuha...was in a kimono... It's been a while since i'veÅ^seen her dressed like this...")
	hitretResult := Hitret(32874)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT040061")
	Mess("Ojou-sama, I've poured your tea.")
	hitretResult := Hitret(32875)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041385")
	Mess("Thank you, Mogisaka-san. It's fine for you to leave now.")
	hitretResult := Hitret(32876)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT040062")
	Mess("Okay. Have a nice time, then.")
	hitretResult := Hitret(32877)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_04M", 0, 0)
	Update()
	ScWait(300, 0)
	BustupClear(6)
	Talk("êSÇÃê∫", "")
	Mess("As she took her leave, I think I saw a wide andÅ^mischievous grin.")
	hitretResult := Hitret(32878)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I hope she doesn't misunderstand anything...")
	hitretResult := Hitret(32879)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Were your clothes fine?")
	hitretResult := Hitret(32880)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041386")
	Mess("Yes. After all, falling down and getting dirty wasÅ^something I've been doing since I was a child. Well,Å^Tatara-san had a laugh at me, though.")
	hitretResult := Hitret(32881)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With a wry smile, she took a slow sip from the tea thatÅ^Motoka-san had poured.")
	hitretResult := Hitret(32882)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Following her example, I took a sip, and was surprisedÅ^at how good it was.")
	hitretResult := Hitret(32883)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The aroma was completely different from the tea I brewedÅ^at home with teabags.")
	hitretResult := Hitret(32884)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("/It didn't have that strange bitterness, and felt likeÅ^it slid straight down my throat.")
	hitretResult := Hitret(32885)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even the tea was of the right temperature, a nice lukeÅ^warm. I gulped down the refreshing cup of tea quickly.")
	hitretResult := Hitret(32886)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_03M", 0, 0)
	Talk("Kazuha", "KA041387")
	Mess("Hehe, you must've been quite thirsty.")
	hitretResult := Hitret(32887)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While trying not to laugh, she poured me another cup.")
	hitretResult := Hitret(32888)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was only then that I realized how thirsty I was.")
	hitretResult := Hitret(32889)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks.")
	hitretResult := Hitret(32890)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And today's string of bizarre events continued...")
	hitretResult := Hitret(32891)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_11M", 0, 0)
	Talk("Kazuha", "KA041388")
	Mess("Not at all... If there's someone who needs to show theirÅ^gratitude, it'd be me.")
	hitretResult := Hitret(32892)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041389")
	Mess("Today... Well, not just to today... You've been helpingÅ^be one time after another.")
	hitretResult := Hitret(32893)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, it's just that you've become honest with yourÅ^feelings.")
	hitretResult := Hitret(32894)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041390")
	Mess("But the one who helped me accept those feelings wasÅ^you...")
	hitretResult := Hitret(32895)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041391")
	Mess("When you asked my dad for permission to go out withÅ^me... I was really surprised, but incredibly happy.")
	hitretResult := Hitret(32896)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, I was really nervous at that time....")
	hitretResult := Hitret(32897)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We then broke into laughter.")
	hitretResult := Hitret(32898)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It felt like we had just cleared a mountain.")
	hitretResult := Hitret(32899)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041392")
	Mess("Let me say it again...")
	hitretResult := Hitret(32900)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Mm...")
	hitretResult := Hitret(32901)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kazuha gazed silently into my eyes.")
	hitretResult := Hitret(32902)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And I was lost in her deep beautiful eyes.")
	hitretResult := Hitret(32903)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Time seemed to have froze around us. I could relish inÅ^this moment forever...")
	hitretResult := Hitret(32904)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041393")
	Mess("I...love you...please... go out with me.")
	hitretResult := Hitret(32905)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I love you too. I would be most delighted if you'd goÅ^out with me.")
	hitretResult := Hitret(32906)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_11L", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("We then spontaneously and passionately embraced eachÅ^other.")
	hitretResult := Hitret(32907)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041394")
	Mess("I'll never let go of this happiness... It is somethingÅ^that my sister encouraged and wanted for me...")
	hitretResult := Hitret(32908)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	Change("00_d042b")
	Return
	Change("EXIT_SCENARIO")
EndFunc

