Include"yosuga.ch"

Function sc00_C028()
	ScenarioEnter()
	ScPlayBgm("BGM06", 0)
	SetCg("B15a", 0, 0)
	Transition("WIP_MOZV", 500)
	Talk("Haruka", "")
	Mess("Excuse me--.")
	hitretResult := Hitret(24523)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK021654")
	Mess("Ah! Welcome!")
	hitretResult := Hitret(24524)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh, are you looking after the shop?")
	hitretResult := Hitret(24525)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021655")
	Mess("Yup, that's right~.")
	hitretResult := Hitret(24526)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020060")
	Mess("Huh? What, it's just you.")
	hitretResult := Hitret(24527)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("G-good afternoon...... I'm the customer.......Å^today.......")
	hitretResult := Hitret(24528)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK021656")
	Mess("Hiro-oneechan, do you want to deliver this?")
	hitretResult := Hitret(24529)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira seems to expand a lot of effort to carry a boxÅ^holding a dozen juice cans.")
	hitretResult := Hitret(24530)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_04M", 0, 0)
	Talk("Yahiro", "YH020061")
	Mess("Ahh, Takahashi gramps is over there. (TL: needs to checkÅ^ingame voiced name)")
	hitretResult := Hitret(24531)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, Let me help, Akira.")
	hitretResult := Hitret(24532)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK021657")
	Mess("Thanks, Haru-kun.")
	hitretResult := Hitret(24533)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("I received the box over from Akira, and placed it on theÅ^bicycle's luggage seat.")
	hitretResult := Hitret(24534)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then, I'll go with you together, putting it down isÅ^probably difficult.")
	hitretResult := Hitret(24535)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_11M", 0, 0)
	Talk("Akira", "AK021658")
	Mess("Ehh, is that fine?")
	hitretResult := Hitret(24536)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's okay, since I'm free let me accompany you.")
	hitretResult := Hitret(24537)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_06M", 0, 0)
	Talk("Yahiro", "YH020062")
	Mess("No, you can't, you can't.")
	hitretResult := Hitret(24538)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh, why's that?")
	hitretResult := Hitret(24539)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020063")
	Mess("In any case, Akira, quickly go off. Let me use this guy.")
	hitretResult := Hitret(24540)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_04M", 0, 0)
	Talk("Akira", "AK021659")
	Mess("Okay-! Then I'll go off.")
	hitretResult := Hitret(24541)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh........")
	hitretResult := Hitret(24542)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE256", 0)
	BustupClear(2)
	Talk("Akira", "AK021660")
	Mess("Yottotototo........ Mmpph, mmphhh!!")
	hitretResult := Hitret(24543)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira rides the bicycle in a zig-zag manner, the momentÅ^before she accelerates appears to be dangerous, lookingÅ^at her makes my heart skip a beat.")
	hitretResult := Hitret(24544)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("D....Dangerous...... Is she okay by herself.......")
	hitretResult := Hitret(24545)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020064")
	Mess("She's not a kid, even if she falls down she won't cry.")
	hitretResult := Hitret(24546)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's...... Is it better if I go?")
	hitretResult := Hitret(24547)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020065")
	Mess("No, there's more trouble over here.")
	hitretResult := Hitret(24548)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh..... What do you need me to do?")
	hitretResult := Hitret(24549)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020066")
	Mess("Here, the things that are piled over here, move it intoÅ^the storage.")
	hitretResult := Hitret(24550)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehhh!?")
	hitretResult := Hitret(24551)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There are 20 of the juice boxes that Akira just took offÅ^with, in the corner of the store.")
	hitretResult := Hitret(24552)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020067")
	Mess("I won't let you do it for free. After you finished withÅ^it...... Ice-cream..... Right, you can take two of itÅ^away.")
	hitretResult := Hitret(24553)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then, 200 yen per hour right......")
	hitretResult := Hitret(24554)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_08M", 0, 0)
	Talk("Yahiro", "YH020068")
	Mess("Alright get to work!")
	hitretResult := Hitret(24555)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Indeed, Akira moving it by herself is difficult......Å^Hiro-oneechan will also move right?")
	hitretResult := Hitret(24556)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020069")
	Mess("Hah? I already paid for it, why should I do it? YouÅ^should be of some use from time to time, student!")
	hitretResult := Hitret(24557)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I got it already......")
	hitretResult := Hitret(24558)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020070")
	Mess("When you stack it into the storage, bring out the oldÅ^ones to pile it outside, and the new ones to be placedÅ^inside, got it?")
	hitretResult := Hitret(24559)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh? Why?")
	hitretResult := Hitret(24560)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_08M", 0, 0)
	Talk("Yahiro", "YH020071")
	Mess("Think for yourself, stupid!!")
	hitretResult := Hitret(24561)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_06M", 0, 0)
	Talk("Yahiro", "YH020072")
	Mess("Ahh, also, after you finished moving, go restock theÅ^vending machine outside.")
	hitretResult := Hitret(24562)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehhhhh? That too?")
	hitretResult := Hitret(24563)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020073")
	Mess("Just to it at the same time! Look at you, you can't evenÅ^work properly at a convenience shop?")
	hitretResult := Hitret(24564)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... If there's a nearby convenience store here......")
	hitretResult := Hitret(24565)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("", 0)
	SetCg("B15a", 0, 0)
	Talk("Haruka", "")
	Mess("*Grunt noise*....... Is this okay? (TL: Haruka saysÅ^literally \"Yotto\".)")
	hitretResult := Hitret(24566)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I closed the vending machine, and let Hiro-san lock it.")
	hitretResult := Hitret(24567)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I have never replenished the vending machine's drink,Å^and I thought it was interesting.")
	hitretResult := Hitret(24568)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM05", 0)
	SetBustup("CG01_03M", 0, 0)
	Talk("Yahiro", "YH020074")
	Mess("Ahh, great work. As expected from a guy, you did itÅ^fast. Good, from now onwards how about helping me moveÅ^things?")
	hitretResult := Hitret(24569)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh, I still need to do it?")
	hitretResult := Hitret(24570)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020075")
	Mess("I'll pay for the job, don't be stingy.")
	hitretResult := Hitret(24571)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haa......")
	hitretResult := Hitret(24572)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020076")
	Mess("Come to think of it, that girl is so slow...... Did sheÅ^get invited to have some tea and sweets, and is enjoyingÅ^it.....")
	hitretResult := Hitret(24573)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If it's Akira, it's possible......")
	hitretResult := Hitret(24574)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020077")
	Mess("............")
	hitretResult := Hitret(24575)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What....... What is it?")
	hitretResult := Hitret(24576)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020078")
	Mess("Seriously...... Actually bragging about it.....")
	hitretResult := Hitret(24577)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh!? When did I brag? Akira isn't here also.....")
	hitretResult := Hitret(24578)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020079")
	Mess("This, this..... talking about Akira all the time.")
	hitretResult := Hitret(24579)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah.....")
	hitretResult := Hitret(24580)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020080")
	Mess("Recently, that girl is also going \"Haru-kun Haru-kun\",Å^always mentioning me, and I thought what was going on.")
	hitretResult := Hitret(24581)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sorry...... I didn't say anything.")
	hitretResult := Hitret(24582)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_06M", 0, 0)
	Talk("Yahiro", "YH020081")
	Mess("I'm not her parents, and you don't need to say somethingÅ^like \"Please give me your daughter\". If she is willing,Å^you can do anything you like.")
	hitretResult := Hitret(24583)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020082")
	Mess("But, you really found such a troublesome person......Å^You really aren't picky huh?")
	hitretResult := Hitret(24584)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It isn't much trouble. She's cheerful and always happy,Å^gentle and capable of taking care of others.....")
	hitretResult := Hitret(24585)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020083")
	Mess("And also a little silly, thick headed, flat chested.")
	hitretResult := Hitret(24586)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("The, the last one doesn't have any relation right?")
	hitretResult := Hitret(24587)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020084")
	Mess("I want to know which part of her you like... if it'sÅ^because of sympathy, then quickly break up. Don't keepÅ^looking at her with a sympathetic eye.")
	hitretResult := Hitret(24588)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM16", 0)
	Talk("Yahiro", "YH020085")
	Mess("If you look at her as a unfortunate character, that isÅ^the biggest inconvenience you give her.")
	hitretResult := Hitret(24589)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("All that things are found out later. I just likeÅ^Akira...... No other reason.")
	hitretResult := Hitret(24590)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_05M", 0, 0)
	Talk("Yahiro", "YH020086")
	Mess("Uwaa.... I really want to record thse words down andÅ^play it back 10 years later. If you regret these wordsÅ^to the death, you're normal. If you don't feel anything,Å^then you're really a true fool!")
	hitretResult := Hitret(24591)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What, what does that mean!?")
	hitretResult := Hitret(24592)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_06M", 0, 0)
	Talk("Yahiro", "YH020087")
	Mess("Just as the words mean. Should I say you're anÅ^embarrassing person, or will you find out your thinkingÅ^is very naive.")
	hitretResult := Hitret(24593)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uuu...... I just confessed normally and dated......")
	hitretResult := Hitret(24594)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020088")
	Mess("Hmmmph....... well you got along strangely, well, goodÅ^luck.")
	hitretResult := Hitret(24595)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes, thank you very much.")
	hitretResult := Hitret(24596)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020089")
	Mess("Hey...... About Akira's father, do you know about it?")
	hitretResult := Hitret(24597)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... Yes.")
	hitretResult := Hitret(24598)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020090")
	Mess("Is that so, she already told you to this extent.")
	hitretResult := Hitret(24599)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... It was a coincidence. I heard Migiwa-san calledÅ^Akira Onee-chan.")
	hitretResult := Hitret(24600)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020091")
	Mess("Hmph..... A guy with such horrible coincidence.")
	hitretResult := Hitret(24601)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hiro-oneechan..... Is there anything I can help with?")
	hitretResult := Hitret(24602)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_06M", 0, 0)
	Talk("Yahiro", "YH020092")
	Mess("..... Huh? Heck if I should know.")
	hitretResult := Hitret(24603)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... Indeed hmm.......")
	hitretResult := Hitret(24604)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_08M", 0, 0)
	Talk("Yahiro", "YH020093")
	Mess("Haa....... Listen well, okay?")
	hitretResult := Hitret(24605)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020094")
	Mess("Even though she didn't want it, she was given a tagÅ^number when she was born.")
	hitretResult := Hitret(24606)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020095")
	Mess("She doesn't want anything to change, and she's going toÅ^bring the secret silently to her grave.")
	hitretResult := Hitret(24607)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020096")
	Mess("So even if you did it for Akira and dig everything out,Å^she will still carry all the responsibility on herÅ^shoulders.")
	hitretResult := Hitret(24608)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020097")
	Mess("If you can't imagine the ending from this point, you'reÅ^just gambling. Losing the goal and still sticking aroundÅ^for the fun of it, you can only be seen as blindlyÅ^helping out.")
	hitretResult := Hitret(24609)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("ÅcÅcÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(24610)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020098")
	Mess("If you intend to do it, be prepared to do it, even ifÅ^she cries or hate you, keep on doing it. Otherwise, doÅ^nothing.")
	hitretResult := Hitret(24611)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020099")
	Mess("If you still insist of doing it...... I can listen toÅ^your complaints.")
	hitretResult := Hitret(24612)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hiro-san...... I'll remember that. Thank you.")
	hitretResult := Hitret(24613)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_07M", 0, 0)
	Talk("Yahiro", "YH020100")
	Mess("You're scary. Having such a cute face, but you seem toÅ^be capable of doing anything.")
	hitretResult := Hitret(24614)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020101")
	Mess("Sheesh, all of you are fulls, just forget about suchÅ^things, and finish it by laughing it off for the rest ofÅ^your life.")
	hitretResult := Hitret(24615)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("...... Maybe, this is for the best.")
	hitretResult := Hitret(24616)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021661")
	Mess("I'm back-!!!")
	hitretResult := Hitret(24617)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Quake(0, 16, 1, 2, 30)
	Talk("Haruka", "")
	Mess("!!!!")
	hitretResult := Hitret(24618)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM03", 0)
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020102")
	Mess("Y.... You're late!! Did you eat candy again?")
	hitretResult := Hitret(24619)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_12M", 0, 0)
	Talk("Akira", "AK021662")
	Mess("Eh!? How did you know?")
	hitretResult := Hitret(24620)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020103")
	Mess("Tsk, I actually got it right.")
	hitretResult := Hitret(24621)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_06M", 0, 0)
	Talk("Akira", "AK021663")
	Mess("Ahh, this line of leading is so cruel-!!")
	hitretResult := Hitret(24622)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_03M", 0, 0)
	Talk("Yahiro", "YH020104")
	Mess("Fool, there was a television broadcast yesterday right?Å^We watched it together but you still fell for it.")
	hitretResult := Hitret(24623)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020105")
	Mess("Haruka, I will give you 3 ice creams. Choose from theÅ^cheapest ones.")
	hitretResult := Hitret(24624)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Th, thank you.")
	hitretResult := Hitret(24625)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK021664")
	Mess("That's awesome, Haru-kun. Hmm, this is delicious. AlsoÅ^this one, even though it's cheap it's really big.")
	hitretResult := Hitret(24626)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Woah, this looks delicious, I will give Akira one. Let'sÅ^have them together?")
	hitretResult := Hitret(24627)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_04M", 0, 0)
	Talk("Akira", "AK021665")
	Mess("Eh, is that okay? Waa, thanks!!")
	hitretResult := Hitret(24628)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020106")
	Mess("I don't a share?")
	hitretResult := Hitret(24629)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!? My ice cream is payment right?")
	hitretResult := Hitret(24630)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_07M", 0, 0)
	Talk("Yahiro", "YH020107")
	Mess("Akira didn't do anything but you still gave it to her,Å^but you don't give me anything.....")
	hitretResult := Hitret(24631)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then it doesn't count it as giving it to me!!")
	hitretResult := Hitret(24632)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_04M", 0, 0)
	Talk("Yahiro", "YH020108")
	Mess("Stop saying something so stubborn, I want this one!")
	hitretResult := Hitret(24633)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("This, this isn't an adult's behaviour......")
	hitretResult := Hitret(24634)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK021666")
	Mess("I'll give back mine to you?")
	hitretResult := Hitret(24635)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, go ahead and eat it. This is my treat.")
	hitretResult := Hitret(24636)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK021667")
	Mess("Thank you-!!")
	hitretResult := Hitret(24637)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_03M", 0, 0)
	Talk("Yahiro", "YH020109")
	Mess("Thank you-!!")
	hitretResult := Hitret(24638)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Please don't imitate her!!")
	hitretResult := Hitret(24639)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_03M", 0, 0)
	Talk("Akira", "AK021668")
	Mess("Here, Haru-kun, have a bite?")
	hitretResult := Hitret(24640)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, Um.... thank you... Nmmm.... Woah.... cold..... but,Å^delicious ...")
	hitretResult := Hitret(24641)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021669")
	Mess("Right? It's delicious-!")
	hitretResult := Hitret(24642)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_05M", 0, 0)
	Talk("Yahiro", "YH020110")
	Mess("Uwah.... This is sickening......")
	hitretResult := Hitret(24643)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What the...... This person is.......")
	hitretResult := Hitret(24644)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK021670")
	Mess("Ah, that's right, Haru-kun. Are you free tomorrow?")
	hitretResult := Hitret(24645)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes, I don't have anything important.")
	hitretResult := Hitret(24646)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_03M", 0, 0)
	Talk("Akira", "AK021671")
	Mess("Then, let's go out tomorrow? To the neighbouring town.")
	hitretResult := Hitret(24647)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("óI", "")
	Mess("Ç§ÇÒÅAÇ¢Ç¢ÇÊÅBñlÇ‡Ç«Ç±Ç©çsÇ©Ç»Ç¢Ç©Ç¡ÇƒÅAóUÇ®Ç§Ç∆évÇ¡ÇƒÇΩ")
	Mess("ÇÒÇæÅB")
	hitretResult := Hitret(24648)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("âl", "AK021672")
	Mess("Ç†ÇÕÇ¡ÅAÇ∂Ç·Ç†ç°Ç©ÇÁçsÇ¡ÇøÇ·Ç§ÅH")
	hitretResult := Hitret(24649)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("óI", "")
	Mess("ãAÇËÇÃìdé‘Ç™ñ≥Ç≠Ç»ÇÈÇ©ÇÁÅAÇøÇÂÇ¡Ç∆ÇµÇ©óVÇ◊Ç»Ç¢ÇÊÅBÅ^")
	Mess("ñæì˙ÅAí©àÍÇ≈çsÇ±Ç§Ç©ÅB")
	hitretResult := Hitret(24650)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_04M", 0, 0)
	Talk("âl", "AK021673")
	Mess("Ç§ÇÒÅIÅ@çsÇ≠çsÇ≠ÅI")
	hitretResult := Hitret(24651)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_02M", 0, 0)
	Talk("Ç‚Ç–ÇÎ", "YH020111")
	Mess("ÉPÉbÅAÇ®ëOÇÁê¬ètÇ∑ÇÈÇÃÇ‡ÅAëÂäTÇ…ÇµÇÎÇÊÅI")
	hitretResult := Hitret(24652)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Quake(16, 0, 0, 2, 30)
	SetBustup("CC02_12M", 0, 0)
	Talk("óI", "")
	Mess("Ç†Ç§Ç¡ÅIÅH")
	hitretResult := Hitret(24653)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Shake(2)
	Talk("âl", "AK021674")
	Mess("Ç…Ç·Ç¡ÅIÅH")
	hitretResult := Hitret(24654)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(8)
	Talk("êSÇÃê∫", "")
	Mess("Ç‚Ç–ÇÎÇ≥ÇÒÇÕÅAñlÇÁÇÃì™ÇÇÆÇËÇÆÇËÇ∆Ç±Ç√Ç´âÒÇ∑Ç∆ÅAâúÇÃïîâÆ")
	Mess("Ç…ì¸Ç¡ÇƒÇ¢Ç¡ÇΩÅB")
	hitretResult := Hitret(24655)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("âl", "AK021675")
	Mess("Ç¶Ç÷Ç÷Ç¡ÅAÇ®Ç±ÇÁÇÍÇøÇ·Ç¡ÇΩÅB")
	hitretResult := Hitret(24656)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("óI", "")
	Mess("ÅcÅcÅcÇæÇÀÅB")
	hitretResult := Hitret(24657)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("âl", "AK021676")
	Mess("Ç”Ç”Ç¡ÅAÇ∂Ç·Ç†ÅAñæì˙ÇÕÉnÉãåNÇøÇ…å}Ç¶Ç…çsÇ≠ÇÀÅBÇ†ÅAÇ®ïŸìñ")
	Mess("ÇÕópà”ÇµÇΩï˚Ç™Ç¢Ç¢ÅH")
	hitretResult := Hitret(24658)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("óI", "")
	Mess("Ç§Ç§ÇÒÅAÇπÇ¡Ç©Ç≠ÇæÇµÅAå¸Ç±Ç§Ç≈âΩÇ©êHÇ◊ÇÊÇ§ÅB")
	hitretResult := Hitret(24659)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_03M", 0, 0)
	Talk("âl", "AK021677")
	Mess("ÇÌÇ©Ç¡ÇΩÅ[ÅIÅ@Ç∑Ç¡Ç≤Ç≠äyÇµÇ›ÇæÇÊÅ[ÅIÅ@Ç†ÅAÇ†Ç∆Ç≈ÅAÇ–ÇÎéo")
	Mess("ÇøÇ·ÇÒÇ©ÇÁÉpÉ\ÉRÉìéÿÇËÇƒÅAÇ¢ÇÎÇ¢ÇÎÇ®ìXÇ∆Ç©í≤Ç◊Ç∆Ç±Ç¡Ç∆ÅI")
	hitretResult := Hitret(24660)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("óI", "")
	Mess("Ç∂Ç·Ç†ÅAñlÇ‡í≤Ç◊Ç∆Ç≠ÇÊÅBñæì˙ÇÕÇ®å›Ç¢Ç®èßÇﬂÇÃÇ®ìXÇ…çsÇ±Ç§")
	Mess("Ç©ÅH")
	hitretResult := Hitret(24661)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("âl", "AK021678")
	Mess("Ç§ÇÒÅIÅ@ÇªÇ§ÇµÇÊÇ§ÅIÅ@Ç«ÇÒÇ»Ç®ìXÇ©íÖÇ≠Ç‹Ç≈ì‡èèÇæÇÊÅH")
	hitretResult := Hitret(24662)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("óI", "")
	Mess("Ç§ÇÒÅAÇÌÇ©Ç¡ÇΩÅB")
	hitretResult := Hitret(24663)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	Change("00_c029")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

