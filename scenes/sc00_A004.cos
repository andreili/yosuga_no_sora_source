Include"yosuga.ch"

Function sc00_A004()
	ScenarioEnter()
	ScPlaySe("SE357", 0)
	SetCg("B02a", 0, 0)
	Talk("Haruka", "")
	Mess("Sora! Hurry up and eat or you'll be late.")
	hitretResult := Hitret(2772)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM03", 0)
	Talk("êSÇÃê∫", "")
	Mess("This is the first morning that Sora would go to school.")
	hitretResult := Hitret(2773)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I had gotten up early, taking into account the fact thatÅ^she definitely wouldn't want to get up, but she stillÅ^wasn't showing any signs of coming out of her room...")
	hitretResult := Hitret(2774)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Geez...")
	hitretResult := Hitret(2775)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("For the moment, I decided to eat alone... if I keptÅ^waiting on Sora, I'd be late too.")
	hitretResult := Hitret(2776)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The morning news played on the television, which hadÅ^been on since the time I got up.")
	hitretResult := Hitret(2777)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Occasionally, pictures of Tokyo were shown... and I wasÅ^hit by nostalgic feelings.")
	hitretResult := Hitret(2778)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Information was overflowing, even here in theÅ^countryside... and, whether I'm in the city or theÅ^countryside, it didn't really matter to me.")
	hitretResult := Hitret(2779)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("All we needed to know for our lifestyle here was the dayÅ^Super Takano came and what was on sale.")
	hitretResult := Hitret(2780)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...But when someone lives in the countryside, theyÅ^really do start to admire the city.")
	hitretResult := Hitret(2781)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But looking at Nao-chan, Ryouhei, Amatsume andÅ^Migiwa-san... maybe it wasn't like that at all.")
	hitretResult := Hitret(2782)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Oh, it's already this late.")
	hitretResult := Hitret(2783)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While I was eating and watching TV, time had passedÅ^without me realizing it.")
	hitretResult := Hitret(2784)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The hands on the clock told me that I needed to startÅ^getting ready to go.")
	hitretResult := Hitret(2785)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Geez, Sora... she isn't still asleep, is she?")
	hitretResult := Hitret(2786)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Lately she's been cooped up in her room, with her daysÅ^and nights reversed... so she might not be able to getÅ^up.")
	hitretResult := Hitret(2787)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora? If you don't get up, you won't make it on time.")
	hitretResult := Hitret(2788)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se100", 0)
	Talk("êSÇÃê∫", "")
	Mess("When I said that, the door slowly opened.")
	hitretResult := Hitret(2789)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR010077")
	Mess("... What?")
	hitretResult := Hitret(2790)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, nothing... You were awake.")
	hitretResult := Hitret(2791)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm surprised. That she's awake, and of course...")
	hitretResult := Hitret(2792)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("This is the first time I'd seen Sora in her new uniform.")
	hitretResult := Hitret(2793)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And... it's been a while since I've seen Sora with herÅ^hair up. It's the same style she had when we attendedÅ^our old school.")
	hitretResult := Hitret(2794)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her shoulders were clearly visible, and the tresses nextÅ^to them were swaying.")
	hitretResult := Hitret(2795)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Good morning. Breakfast is ready.")
	hitretResult := Hitret(2796)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010078")
	Mess("... I don't need it.")
	hitretResult := Hitret(2797)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then at least have some milk... here.")
	hitretResult := Hitret(2798)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR010079")
	Mess("Geez... I'm not a child.")
	hitretResult := Hitret(2799)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even while she's complaining, she takes the cup andÅ^drinks from it.")
	hitretResult := Hitret(2800)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The school day is long. Will you be fine without eating?")
	hitretResult := Hitret(2801)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_05M", 0, 0)
	Talk("Sora", "SR010080")
	Mess("... We can't come home at noon?")
	hitretResult := Hitret(2802)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's not Saturday. We have classes till evening.")
	hitretResult := Hitret(2803)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("We haven't packed anything, so we'll have to go to theÅ^cafeteria or buy something for lunch... Oh, do you haveÅ^notebooks, pens, and pencils??")
	hitretResult := Hitret(2804)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010081")
	Mess("... I said not to treat me like a child. I already haveÅ^those.")
	hitretResult := Hitret(2805)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha, okay.")
	hitretResult := Hitret(2806)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Certainly, it's not... Well, maybe it does feel like I'mÅ^sending a child off to their first day of school.")
	hitretResult := Hitret(2807)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Maybe Sora was a bit nervous too. She was constantlyÅ^checking to make sure her clothes weren't messy orÅ^rumpled.")
	hitretResult := Hitret(2808)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... It looks good. Your hair is nice, too.")
	hitretResult := Hitret(2809)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_11M", 0, 0)
	Talk("Sora", "SR010082")
	Mess("I don't need your compliments...")
	hitretResult := Hitret(2810)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha, even if I didn't, I'm sure someone would tell youÅ^that you look cute.")
	hitretResult := Hitret(2811)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010083")
	Mess(".....")
	hitretResult := Hitret(2812)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The most likely candidates would be Nao-chan andÅ^Amatsume... I didn't really want to think about it, butÅ^Ryouhei too.")
	hitretResult := Hitret(2813)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, it's a bit early, but let's go.")
	hitretResult := Hitret(2814)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I cleaned up my dishes and put away the food Sora didn'tÅ^eat, then grabbed my bag.")
	hitretResult := Hitret(2815)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR010084")
	Mess("...Yeah.")
	hitretResult := Hitret(2816)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora grabbed her small bag, the same one she used at ourÅ^last school, and followed me.")
	hitretResult := Hitret(2817)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B01a", 0, 0)
	SetBustup("CA01_01M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("The sky was crisp and blue. The weather is nice.")
	hitretResult := Hitret(2818)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The sky is so blue it makes me think... This is perfectÅ^for Sora's first day.")
	hitretResult := Hitret(2819)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was starting to feel hot just standing in theÅ^sunlight.")
	hitretResult := Hitret(2820)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You should carry a key too. You won't lose it, right?")
	hitretResult := Hitret(2821)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010085")
	Mess("You're the only one who would do something like that.")
	hitretResult := Hitret(2822)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's not like I've ever lost a key before... Okay, it'sÅ^locked.")
	hitretResult := Hitret(2823)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010086")
	Mess("... It's not like there's anything we would miss if itÅ^was taken, is there?")
	hitretResult := Hitret(2824)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Your laptop.")
	hitretResult := Hitret(2825)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR010087")
	Mess("... Urgh.")
	hitretResult := Hitret(2826)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, it doesn't seem like there are thieves around hereÅ^anyway. Let's go.")
	hitretResult := Hitret(2827)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B34a", 400, 0)
	Transition("WIP_RL", 500)
	Talk("Haruka", "")
	Mess("Oh, right. Just in case, could you make sure to rememberÅ^the way so you can go on your own?")
	hitretResult := Hitret(2828)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_05M", 0, 0)
	Talk("Sora", "SR010088")
	Mess("Eh... We're not going to go together?")
	hitretResult := Hitret(2829)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, of course we will, but just in case. Even if we goÅ^together, we might come home separately.")
	hitretResult := Hitret(2830)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010089")
	Mess(".....")
	hitretResult := Hitret(2831)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was troubled by her anxious expression.")
	hitretResult := Hitret(2832)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Of course, I'd like to be together as often as possible,Å^but it's a different problem whether or not I canÅ^actually go with you every time.")
	hitretResult := Hitret(2833)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("... And, I'd like Sora to do her best to help out, too.")
	hitretResult := Hitret(2834)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	Talk("VoiceÅsó∫ïΩÅt", "RH010001")
	Mess("Wha!?")
	hitretResult := Hitret(2835)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... That voice.")
	hitretResult := Hitret(2836)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM06", 0)
	BustupClear(4)
	SetBustup("CF01_06M", 0, 0)
	Talk("Ryouhei", "RH010002")
	Mess("What is this... Is this what they call a fated meeting?")
	hitretResult := Hitret(2837)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... What are you saying? It's creepy.")
	hitretResult := Hitret(2838)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_04M", 0, 0)
	Talk("Ryouhei", "RH010003")
	Mess("Not you! ...So we meet again.")
	hitretResult := Hitret(2839)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010090")
	Mess("...Uh?")
	hitretResult := Hitret(2840)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora hides behind my back.")
	hitretResult := Hitret(2841)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH010004")
	Mess("I thought I wouldn't be able to meet you again... butÅ^what's this... this circumstance.")
	hitretResult := Hitret(2842)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010091")
	Mess("... Who's that?")
	hitretResult := Hitret(2843)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, um... my classmate, Ryouhei.")
	hitretResult := Hitret(2844)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010092")
	Mess("...He's weird.")
	hitretResult := Hitret(2845)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH010005")
	Mess("Oh! What's this. If you're acquaintances, you should'veÅ^said so.")
	hitretResult := Hitret(2846)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("We're not... She's my sister.")
	hitretResult := Hitret(2847)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_06M", 0, 0)
	Jump(7, 0)
	Talk("Ryouhei", "RH010006")
	Mess("Wha!? Sister?")
	hitretResult := Hitret(2848)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01L", 0, 0)
	Talk("Ryouhei", "RH010007")
	Mess("Good morning, big brother.")
	hitretResult := Hitret(2849)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_04M", 0, 0)
	Talk("Ryouhei", "RH010008")
	Mess("I'm Nakazato Ryouhei, your brother's classmate.")
	hitretResult := Hitret(2850)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't just talk respectfully all of a sudden likeÅ^that... It's creepy.")
	hitretResult := Hitret(2851)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH010009")
	Mess("Geez, the atmosphere is all wrong. I'm trying to give aÅ^good impression by acting out an interesting character.Å^Play along, okay?")
	hitretResult := Hitret(2852)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... It's coming across fine.")
	hitretResult := Hitret(2853)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora was kneading the hem of her skirt in her hands.")
	hitretResult := Hitret(2854)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her feeling of not wanting to associate with him cameÅ^across sharply.")
	hitretResult := Hitret(2855)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010010")
	Mess("But that's who she was... the mysterious beauty was yourÅ^sister.")
	hitretResult := Hitret(2856)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 4, 0)
	SetBustup("CA01_09L", 10, 0)
	Talk("Haruka", "")
	Mess("Yeah, her name is Sora. I hope you two get along.")
	hitretResult := Hitret(2857)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010011")
	Mess("Sora-chan, huh? Nice to meet you!!")
	hitretResult := Hitret(2858)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010093")
	Mess(".....")
	hitretResult := Hitret(2859)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As always, Sora was hiding behind my back, not trying toÅ^come out at all.")
	hitretResult := Hitret(2860)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010012")
	Mess("Hey, Haruka... if I put a smile on her face, can we callÅ^it my victory?")
	hitretResult := Hitret(2861)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I don't think there's any winning or losing here...Å^she's pretty tough.")
	hitretResult := Hitret(2862)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_05M", 0, 0)
	Talk("Ryouhei", "RH010013")
	Mess("Oh really, that just makes me even more excited!")
	hitretResult := Hitret(2863)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Learn some self-restraint.")
	hitretResult := Hitret(2864)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH010014")
	Mess("Leave it to me! Sora-chan, if there's anything you don'tÅ^know, go ahead and ask me.")
	hitretResult := Hitret(2865)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("...She was completely ignoring him.")
	hitretResult := Hitret(2866)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH010015")
	Mess("Hehehe, a worthy opponent, I see. By the way, you saidÅ^she was your little sister, but what's the ageÅ^difference? Is she in the same year as you?")
	hitretResult := Hitret(2867)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, I didn't mention it earlier. Sora and I are twins.")
	hitretResult := Hitret(2868)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010016")
	Mess("Twins? Hmm... Is that it.")
	hitretResult := Hitret(2869)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, I'm so much taller, most people think we were bornÅ^a year apart.")
	hitretResult := Hitret(2870)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora's body is fundamentally weak... When she was young,Å^she spent a lot of time in the hospital.")
	hitretResult := Hitret(2871)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH010017")
	Mess("Is that right... Then you have to take care of her.Å^We'll look out for her too.")
	hitretResult := Hitret(2872)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, of course... Thanks.")
	hitretResult := Hitret(2873)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_04M", 0, 0)
	Talk("Ryouhei", "RH010018")
	Mess("But everyone here is a busybody, so we would have takenÅ^care of her even if you hadn't said anything.")
	hitretResult := Hitret(2874)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO010047")
	Mess("Ah, Sora-chan, good morning. Oh, you're so cute.")
	hitretResult := Hitret(2880)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH010019")
	Mess("She seems happy to meet you guys too.")
	hitretResult := Hitret(2883)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Hmm?")
	hitretResult := Hitret(2884)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010020")
	Mess("Geez, I won't let you monopolize Sora.")
	hitretResult := Hitret(2885)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(7)
	Talk("Haruka", "")
	Mess("...?")
	hitretResult := Hitret(2886)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010001")
	Mess("Good morning, Haru-kun!")
	hitretResult := Hitret(2887)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	SetBustup("CD01_02M", 0, 0)
	Talk("Haruka", "")
	Mess("Good morning, Amatsume.")
	hitretResult := Hitret(2888)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010002")
	Mess("Ah, Haru-kun, your uniform is so new!")
	hitretResult := Hitret(2889)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, the uniform I ordered arrived, so I changed.")
	hitretResult := Hitret(2890)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010003")
	Mess("Hmm, the last one was cool since it had \"city\" writtenÅ^all over it, but this one suits you too.")
	hitretResult := Hitret(2891)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks. I like how it doesn't really feel like I'mÅ^wearing a uniform.")
	hitretResult := Hitret(2892)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But aside from that, don't you two usually go to schoolÅ^earlier than this?")
	hitretResult := Hitret(2893)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010004")
	Mess("Oh yeah. Kazu-chan slept in today.")
	hitretResult := Hitret(2894)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_07M", 0, 0)
	Talk("Kazuha", "KA010001")
	Mess("H-hey, Akira!")
	hitretResult := Hitret(2895)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha... Good morning, Migiwa-san.")
	hitretResult := Hitret(2896)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_10M", 0, 0)
	Talk("Kazuha", "KA010002")
	Mess("...G-Good morning.")
	hitretResult := Hitret(2897)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_03M", 0, 0)
	Talk("Akira", "AK010005")
	Mess("Still, there are a lot of people here, huh.")
	hitretResult := Hitret(2898)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah. My little sister is going to the same schoolÅ^starting today.")
	hitretResult := Hitret(2899)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_12M", 0, 0)
	Talk("Kazuha", "KA010003")
	Mess("Huh, little sister...?")
	hitretResult := Hitret(2900)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("She hasn't been going to school... Sora?")
	hitretResult := Hitret(2901)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR010094")
	Mess("...What?")
	hitretResult := Hitret(2902)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Let me introduce you. This is my sister, Sora.")
	hitretResult := Hitret(2903)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK010006")
	Mess("Nice to meet you! I'm Akira. Akira Amatsume.")
	hitretResult := Hitret(2904)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA010004")
	Mess("My name is Kazuha Migiwa.")
	hitretResult := Hitret(2905)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010007")
	Mess("It's been a while. Do you remember me, Sora-chan?")
	hitretResult := Hitret(2906)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_12M", 0, 0)
	Talk("Sora", "SR010095")
	Mess("...Huh?")
	hitretResult := Hitret(2907)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(5)
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH010021")
	Mess("What's this, what's this? Another fated meeting?")
	hitretResult := Hitret(2908)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010008")
	Mess("Ah, I guess I just brought you home that one time youÅ^got lost, so you might have forgotten me.")
	hitretResult := Hitret(2909)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR010096")
	Mess("...I don't remember you.")
	hitretResult := Hitret(2910)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010009")
	Mess("Aha, well then, nice to meet you.")
	hitretResult := Hitret(2911)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010097")
	Mess("...Sure...")
	hitretResult := Hitret(2912)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She nodded and then moved away, apparently uninterested.")
	hitretResult := Hitret(2913)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	Talk("Ryouhei", "RH010022")
	Mess("Akira, your memory is ridiculously good.")
	hitretResult := Hitret(2914)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010010")
	Mess("Hmm, maybe I remember her because she looks just likeÅ^Haru-kun.")
	hitretResult := Hitret(2915)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO010048")
	Mess("Which means she can remember your crime too.")
	hitretResult := Hitret(2916)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_06M", 0, 0)
	Talk("Ryouhei", "RH010023")
	Mess("S-Seriously!?")
	hitretResult := Hitret(2917)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_14M", 0, 0)
	Talk("Akira", "AK010011")
	Mess("Huh? Ah, like that time in the greenhouse...")
	hitretResult := Hitret(2918)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH010024")
	Mess("Let it go. A man doesn't look back at the past.")
	hitretResult := Hitret(2919)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_06M", 0, 0)
	Talk("Nao", "NO010049")
	Mess("You were up to no good, as usual.")
	hitretResult := Hitret(2920)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(7)
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA010005")
	Mess("... Akira, let's go.")
	hitretResult := Hitret(2921)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	ScStopBgm()
	Talk("Akira", "AK010012")
	Mess("Yeah, let's walk together, everyone.")
	hitretResult := Hitret(2922)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	SetCg("B17a", 0, 0)
	ScPlayBgm("BGM05", 0)
	Talk("Sora", "SR010098")
	Mess("We're... here.")
	hitretResult := Hitret(2923)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm. It's pretty far, but the route isn't that hard toÅ^remember, right?")
	hitretResult := Hitret(2924)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR010099")
	Mess("I told you, I can't remember it just from walking itÅ^once.")
	hitretResult := Hitret(2925)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You'll have to come this way for a while... Well, if youÅ^just walk the same direction everyone else is, you'llÅ^probably be fine.")
	hitretResult := Hitret(2926)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010100")
	Mess("... It's not as easy as that.")
	hitretResult := Hitret(2927)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Now then, we have to go to the staff room and talk toÅ^your teachers.")
	hitretResult := Hitret(2928)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010101")
	Mess("...Huh...")
	hitretResult := Hitret(2929)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's your first time coming, Sora. Of course you have toÅ^go to the staff room.")
	hitretResult := Hitret(2930)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH010025")
	Mess("Hmm? Oh, you're going to the staff room. We'll go onÅ^ahead, then.")
	hitretResult := Hitret(2931)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO010050")
	Mess("See you, Sora-chan.")
	hitretResult := Hitret(2932)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK010013")
	Mess("See you later, Haru-kun.")
	hitretResult := Hitret(2933)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA010006")
	Mess("...Excuse me, I'll be going.")
	hitretResult := Hitret(2934)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("Let's go, there are some things they have to explain toÅ^you.")
	hitretResult := Hitret(2935)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR010102")
	Mess("...Okay.")
	hitretResult := Hitret(2936)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We left the others and headed toward the staff room.")
	hitretResult := Hitret(2937)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010103")
	Mess("Hey, Haru...")
	hitretResult := Hitret(2938)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm? What is it?")
	hitretResult := Hitret(2939)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010104")
	Mess("My class... will I be in the same class as you andÅ^everyone else?")
	hitretResult := Hitret(2940)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...You're in a different class.")
	hitretResult := Hitret(2941)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_13M", 0, 0)
	Talk("Sora", "SR010105")
	Mess("...Eh?")
	hitretResult := Hitret(2942)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There can only be so many people in one class. Besides,Å^you're in the class right next door...")
	hitretResult := Hitret(2943)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010106")
	Mess("That's...")
	hitretResult := Hitret(2944)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But we have P.E. and some classes together, so it's notÅ^like we'll be completely separated.")
	hitretResult := Hitret(2945)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You can come visit during breaks too.")
	hitretResult := Hitret(2946)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_05M", 0, 0)
	Talk("Sora", "SR010107")
	Mess("...Traitor.")
	hitretResult := Hitret(2947)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Urgh, traitor... It's not like I was the one that splitÅ^us up.")
	hitretResult := Hitret(2948)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010108")
	Mess("...There's no point in me coming to school now, isÅ^there...")
	hitretResult := Hitret(2949)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Huh?")
	hitretResult := Hitret(2950)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_11M", 0, 0)
	Talk("Sora", "SR010109")
	Mess(".....")
	hitretResult := Hitret(2951)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The conversation just went off in a differentÅ^direction...")
	hitretResult := Hitret(2952)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("A-anyway, let's go.")
	hitretResult := Hitret(2953)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1000, 0)
	SetCg("B19a", 0, 0)
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK010014")
	Mess("Oh, welcome back.")
	hitretResult := Hitret(2954)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm back.")
	hitretResult := Hitret(2955)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora was peeved at me, but I left her with her teacherÅ^and came back to the classroom just before homeroomÅ^started.")
	hitretResult := Hitret(2956)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH010026")
	Mess("It's a pity she's in the other class, though.")
	hitretResult := Hitret(2957)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I don't think you have the same problem with it as sheÅ^does...")
	hitretResult := Hitret(2958)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010015")
	Mess("I'm sure she'll make a bunch of friends.")
	hitretResult := Hitret(2959)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I wonder...")
	hitretResult := Hitret(2960)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Honestly, I'm really worried about that.")
	hitretResult := Hitret(2961)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In that sense, I want to agree with Ryouhei.")
	hitretResult := Hitret(2962)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR010110")
	Mess("Haru...")
	hitretResult := Hitret(2963)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Huh? Wh-what's up, Sora? Your class is next door, right?")
	hitretResult := Hitret(2964)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010111")
	Mess("...I can't do it.")
	hitretResult := Hitret(2965)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Can't do... what?")
	hitretResult := Hitret(2966)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010112")
	Mess("...No one's there... I don't know anyone in there.")
	hitretResult := Hitret(2967)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("W-well if you transfer schools, of course you won't knowÅ^anyone... Don't worry, I'm sure there are nice people inÅ^there.")
	hitretResult := Hitret(2968)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_04M", 0, 0)
	Talk("Ryouhei", "RH010027")
	Mess("Hey, hey, think about poor Sora. There's no guaranteeÅ^that there are people like me in the other class.")
	hitretResult := Hitret(2969)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010028")
	Mess("Sora-chan, if you want, I could accompany you.")
	hitretResult := Hitret(2970)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_09M", 0, 0)
	Talk("Sora", "SR010113")
	Mess("No.")
	hitretResult := Hitret(2971)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH010029")
	Mess("Heh, you're straightforward, I like that...")
	hitretResult := Hitret(2972)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_04M", 0, 0)
	Talk("Kazuha", "KA010007")
	Mess("...Despite your consolations, she's clearly not happyÅ^with this.")
	hitretResult := Hitret(2973)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(7)
	BustupClear(5)
	Talk("Haruka", "")
	Mess("Yeah. But still, there's not much we can do, since sheÅ^just changed schools... All we can do is hope she getsÅ^used to it...")
	hitretResult := Hitret(2974)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_05M", 0, 0)
	Talk("Sora", "SR010114")
	Mess("But...")
	hitretResult := Hitret(2975)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010016")
	Mess("I'll go with you.")
	hitretResult := Hitret(2976)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_03M", 0, 0)
	Talk("Akira", "AK010017")
	Mess("It's not lonely at all. You can always come visitÅ^Haru-kun here.")
	hitretResult := Hitret(2977)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010018")
	Mess("You understand, right? That Haru-kun is worried aboutÅ^you.")
	hitretResult := Hitret(2978)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_11M", 0, 0)
	Talk("Sora", "SR010115")
	Mess(".....")
	hitretResult := Hitret(2979)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010019")
	Mess("You're a nice girl, Sora-chan.")
	hitretResult := Hitret(2980)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Amatsume pushes on her back and leads her to theÅ^classroom next door.")
	hitretResult := Hitret(2981)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora turned around and looked straight at me, as if sheÅ^wanted to say something...")
	hitretResult := Hitret(2982)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I nodded at her as if to say \"Do your best.\" She seemedÅ^to understand... Amatsume pushed her out the door andÅ^she disappeared.")
	hitretResult := Hitret(2983)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH010030")
	Mess("Sora-chan is cute, isn't she.")
	hitretResult := Hitret(2984)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... but I'm sorry if she's a bother.")
	hitretResult := Hitret(2985)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010031")
	Mess("Don't worry about it. But besides that, you guys getÅ^along really well, don't you.")
	hitretResult := Hitret(2986)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... I guess.")
	hitretResult := Hitret(2987)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Though I don't think we look like anything but a brotherÅ^being pushed around by his selfish sister.")
	hitretResult := Hitret(2988)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Though, that doesn't mean we don't get along...")
	hitretResult := Hitret(2989)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess(".....")
	hitretResult := Hitret(2990)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("But it's nice to hear him say that.")
	hitretResult := Hitret(2991)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScPlaySe("SE352", 0)
	SetCg("B17a", 0, 0)
	Update()
	ScWait(2000, 0)
	BlackOut(0, 1)
	SetCg("B19a", 0, 0)
	SetBustup("CF01_01M", 0, 0)
	Transition("WIP_RL", 500)
	ScPlayBgm("BGM06", 0)
	Talk("Ryouhei", "RH010032")
	Mess("All right! Time for food!")
	hitretResult := Hitret(2992)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_03M", 0, 0)
	Talk("Akira", "AK010020")
	Mess("Lunchtime!")
	hitretResult := Hitret(2993)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm gonna go buy something. I'll be right back... Oh...")
	hitretResult := Hitret(2994)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Haruka", "")
	Mess("Sora, let's go buy lunch.")
	hitretResult := Hitret(2995)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010116")
	Mess("...Sure.")
	hitretResult := Hitret(2996)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK010021")
	Mess("Sora-chan, you should get the cream one.")
	hitretResult := Hitret(2997)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010117")
	Mess("...Cream?")
	hitretResult := Hitret(2998)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010033")
	Mess("You really like that one, don't you.")
	hitretResult := Hitret(2999)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK010022")
	Mess("If you don't like it, I'll eat it.")
	hitretResult := Hitret(3000)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("From the way she's saying that, I wonder if she justÅ^wants to eat it herself. Well, we'll be right back.")
	hitretResult := Hitret(3001)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010023")
	Mess("See ya soon!")
	hitretResult := Hitret(3002)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	Talk("Ryouhei", "RH010034")
	Mess("Really, when I look at them, they don't look likeÅ^brother and sister, but rather...")
	hitretResult := Hitret(3003)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010024")
	Mess("They get along really well. They're all lovey-dovey,Å^aren't they.")
	hitretResult := Hitret(3004)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010035")
	Mess("It's a shame she's so lovey-dovey with him instead ofÅ^me.")
	hitretResult := Hitret(3005)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(2000, 0)
	Change("00_a005")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

