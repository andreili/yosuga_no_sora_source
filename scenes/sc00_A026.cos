Include"yosuga.ch"

Function sc00_A026()
	ScenarioEnter()
	Talk("Sora", "SR011190")
	Mess("Ahh... Nn... Fufufu, Haru... settle down...")
	hitretResult := Hitret(7999)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sorry...")
	hitretResult := Hitret(8000)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011191")
	Mess("Kyaa... Fufu... Hey, Haru..., Kiss me......")
	hitretResult := Hitret(8001)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nn......")
	hitretResult := Hitret(8002)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM18", 0)
	SetCg("EA10c", 0, 0)
	Transition("", 2000)
	Talk("Sora", "SR011192")
	Mess("Nn... *chuu*... Haa haa...")
	hitretResult := Hitret(8003)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011193")
	Mess("Haru... more...")
	hitretResult := Hitret(8004)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011194")
	Mess("Nnnn... *chuu*... Nnmu...")
	hitretResult := Hitret(8005)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While kissing her, I rubbed her breasts from behind as IÅ^embraced her.")
	hitretResult := Hitret(8006)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It felt like my hands were softly stuck to her breastsÅ^that were moist with sweat.")
	hitretResult := Hitret(8007)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I still wanted Sora.")
	hitretResult := Hitret(8008)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora surrendered herself to me with a pleased look onÅ^her face.")
	hitretResult := Hitret(8009)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It never separated from my head.")
	hitretResult := Hitret(8010)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's naked body.")
	hitretResult := Hitret(8011)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I cherished her lips that let out her sweet voice.")
	hitretResult := Hitret(8012)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's why, I binded my relation with Sora again.")
	hitretResult := Hitret(8013)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was with such force it even surprised me.")
	hitretResult := Hitret(8014)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011195")
	Mess("Ahh, aaahh... Do it more... Ahh... Haruu......")
	hitretResult := Hitret(8015)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I pinched her hard nipples, Sora bent back as ifÅ^electricity ran throughout her body.")
	hitretResult := Hitret(8016)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA10b", 0, 0)
	Talk("Sora", "SR011196")
	Mess("Ahaahh... That's good... Ahh, ahaahh...!!")
	hitretResult := Hitret(8017)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011197")
	Mess("Nnhuu... Nn...! Nnaahhaahh... Kiss me...")
	hitretResult := Hitret(8018)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While being wet with drool, I exchanged kisses withÅ^Sora.")
	hitretResult := Hitret(8019)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From her appearance of being in so much pleasure itÅ^hurts, I became aroused, and kissed her as I attackedÅ^her sensitive spots.")
	hitretResult := Hitret(8020)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011198")
	Mess("Nnhuu... Nn, nnuu!!")
	hitretResult := Hitret(8021)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011199")
	Mess("Haru... that's... amazing... Aahhnn......")
	hitretResult := Hitret(8022)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011200")
	Mess("Even though it tickles... it feels so good......")
	hitretResult := Hitret(8023)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011201")
	Mess("More... You can even pinch it strongly... Akuuu...!!")
	hitretResult := Hitret(8024)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Holding up the region under her breasts as if to gatherÅ^them, I began to rotate her entire breasts.")
	hitretResult := Hitret(8025)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I sucked on her pink nipple that was standing up.")
	hitretResult := Hitret(8026)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA10c", 0, 0)
	Talk("Sora", "SR011202")
	Mess("Ahaahh... Ahh... Nnhu... Ahh...")
	hitretResult := Hitret(8027)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011203")
	Mess("I--It's... good... Haru... It's kind of like you're aÅ^baby...")
	hitretResult := Hitret(8028)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's not like milk will come out, but I sucked herÅ^nipple and rolled my tongue around it over and over.")
	hitretResult := Hitret(8029)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's nipple, shining from being coated with saliva,Å^became even further sensitive.")
	hitretResult := Hitret(8030)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sucking with my lips, small waves of pleasure teasedÅ^Sora's entire body as she breathed wildly.")
	hitretResult := Hitret(8031)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even while her body was trembling, Sora braced herÅ^knees.")
	hitretResult := Hitret(8032)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I reached my hand inside her skirt, her underwearÅ^was wet with sweat and genital secretions.")
	hitretResult := Hitret(8033)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even visible from above her underwear, Sora's feverishÅ^place was sticking to her underwear.")
	hitretResult := Hitret(8034)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Already... This much......")
	hitretResult := Hitret(8035)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011204")
	Mess("Y...Yeah... It feels good... because you're feeling me aÅ^lot... I'm... going crazy too......")
	hitretResult := Hitret(8036)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011205")
	Mess("Ahh... Aaahh... Your hand is hot... Ahh...")
	hitretResult := Hitret(8037)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011206")
	Mess("Aauuu... Nn... Th--There...")
	hitretResult := Hitret(8038)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Rubbing the crease in her underwear, her hips jumped andÅ^collided between my legs.")
	hitretResult := Hitret(8039)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Every time it collided, my erect penis even felt goodÅ^unstimulated.")
	hitretResult := Hitret(8040)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011207")
	Mess("Nnn... You're... becoming hardd... Ahh...")
	hitretResult := Hitret(8041)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her hand went behind me, and stroked between my legs.")
	hitretResult := Hitret(8042)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When my rod expanded, she stroked it up and down, as ifÅ^to confirm its shape.")
	hitretResult := Hitret(8043)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA10b", 0, 0)
	Talk("Sora", "SR011208")
	Mess("Ahh... Haa haa... Is your penis becoming big?")
	hitretResult := Hitret(8044)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011209")
	Mess("Fufu... Does it feel good? When I do this?")
	hitretResult := Hitret(8045)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nn... Yeah... When you do it that strongly... I'llÅ^cum...")
	hitretResult := Hitret(8046)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011210")
	Mess("Okay... I'll do it as many times as you like... untilÅ^you're satisfied......")
	hitretResult := Hitret(8047)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011211")
	Mess("...Cum lots..... Your sperm......")
	hitretResult := Hitret(8048)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, in order not to cum, I reached between Sora's legsÅ^once again.")
	hitretResult := Hitret(8049)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011212")
	Mess("Yaa, ahh... Nnhu... Hii... Aaaahh...!!")
	hitretResult := Hitret(8050)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sucking on the nape of her neck while pinching herÅ^nipple, I slipped my finger into the crevice in herÅ^underwear.")
	hitretResult := Hitret(8051)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011213")
	Mess("Ahh aaahh... It's wet... My place...")
	hitretResult := Hitret(8052)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011214")
	Mess("...Yaa... Don't make that kind of sound... Aaahh...!!")
	hitretResult := Hitret(8053)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011215")
	Mess("H...Hot... It's hot... Your finger... is making myÅ^place... wettt...!!")
	hitretResult := Hitret(8054)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011216")
	Mess("Hii... Ahh...!! Nnhaa...!! If.. you rub there...Å^Nnguu... it feels like I'll pee......")
	hitretResult := Hitret(8055)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011217")
	Mess("*twitch*... Ahh, ahaaahh!! D--Don't... pinch there...")
	hitretResult := Hitret(8056)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011218")
	Mess("I...I'm cummingg... Nnguuu...!!")
	hitretResult := Hitret(8057)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's body jumped as if convulsing.")
	hitretResult := Hitret(8058)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011219")
	Mess("Nnhaahh... I'm cumming... Ahh ahhaaaaahh!!!!")
	hitretResult := Hitret(8059)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Flush("WHITE", 1000, "")
	Talk("Sora", "SR011220")
	Mess("Ahh... Aaaahh... Haa haa haa haa...")
	hitretResult := Hitret(8060)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......Sora... you're cute...")
	hitretResult := Hitret(8061)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011221")
	Mess("......Geeze... you tease... Nn... *chuu*...")
	hitretResult := Hitret(8062)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Turning our hands around each others faces, we kissed.")
	hitretResult := Hitret(8063)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Thick cum stuck to my fingers as they were touching herÅ^place.")
	hitretResult := Hitret(8064)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I once again caressed her clitoris, as if painting it.")
	hitretResult := Hitret(8065)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011222")
	Mess("Ahh... Nnhuu! Yaa... Stop... Aaahh...!!")
	hitretResult := Hitret(8066)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011223")
	Mess("Hii... Nnguu... E...Even though it's sensi...tive...!!")
	hitretResult := Hitret(8067)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011224")
	Mess("Ahaahh! Aaahnn... E...Even though I just came...")
	hitretResult := Hitret(8068)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011225")
	Mess("Ahh... Nnguu... Aaaahh!!")
	hitretResult := Hitret(8069)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA10d", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("With a tug, I began lowering her underwear.")
	hitretResult := Hitret(8070)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I pulled it, the genital secretions stained the partÅ^under her underwear.")
	hitretResult := Hitret(8071)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It looks like you wet yourself, doesn't it...")
	hitretResult := Hitret(8072)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011226")
	Mess("Ahh... Aau... Haa haa haa... Dummy...")
	hitretResult := Hitret(8073)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nn...!? Ahh... Oww... Kuu...")
	hitretResult := Hitret(8074)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Grabbing my pole tightly, Sora rubbed it from on top ofÅ^my boxers.")
	hitretResult := Hitret(8075)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011227")
	Mess("Fufufu.. Hurry up and cum... Come on... you cum too...")
	hitretResult := Hitret(8076)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oww, oww... Nn... Even if you rub it that strongly...")
	hitretResult := Hitret(8077)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011228")
	Mess("Come on, come on... Don't hold back... It's twitching inÅ^pain.")
	hitretResult := Hitret(8078)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nnkuua... Sora......")
	hitretResult := Hitret(8079)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From her strong rubbing across the cloth, a pleasantÅ^sensation arose along with the pain.")
	hitretResult := Hitret(8080)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011229")
	Mess("Fufufu... Hurry up and cum... Come on... You can cumÅ^too.")
	hitretResult := Hitret(8081)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Dropping my zipper, Sora's hand crawled inside.")
	hitretResult := Hitret(8082)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She put strength into her hand rubbing my rod, andÅ^caressed it.")
	hitretResult := Hitret(8083)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011230")
	Mess("Haru... Go ahead and cum... Because it feels good...")
	hitretResult := Hitret(8084)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011231")
	Mess("Come on... It feels good when I do this, right?")
	hitretResult := Hitret(8085)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011232")
	Mess("Ahh... Your tip is trembling...?")
	hitretResult := Hitret(8086)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011233")
	Mess("...From here... a lot of semen will come out...!!")
	hitretResult := Hitret(8087)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She rubbed the pre-cum around my urethra as I becameÅ^stimulated.")
	hitretResult := Hitret(8088)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011234")
	Mess("Come on... Cum... Cum, Haru...!!")
	hitretResult := Hitret(8089)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nnguuu...!!")
	hitretResult := Hitret(8090)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Flush("WHITE", 1000, "")
	Talk("Haruka", "")
	Mess("Nn...!!! Ahh......")
	hitretResult := Hitret(8091)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From the feeling of Sora's hand wrapping around myÅ^thing, semen scattered out.")
	hitretResult := Hitret(8092)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011235")
	Mess("Kyaa...!? Ahh... Haa... You're cumming... It's so hot...")
	hitretResult := Hitret(8093)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011236")
	Mess("Ahaahh... Haru came......")
	hitretResult := Hitret(8094)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uuu... Please don't touch it anymore...")
	hitretResult := Hitret(8095)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011237")
	Mess("No way....It tickles, right? Fufu... Your cute hips areÅ^escaping...")
	hitretResult := Hitret(8096)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After I was done ejaculating, she continued stroking itÅ^up and down until the last drop was squeezed out")
	hitretResult := Hitret(8097)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011238")
	Mess("Your semen... Nnkuu... is delicious...")
	hitretResult := Hitret(8098)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011239")
	Mess("*picha*... Nnkuu... Nnn nnn...")
	hitretResult := Hitret(8099)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While her finger was extended to the sticky milky whiteÅ^semen, she wrapped her tongue around it and began toÅ^slowly lick it up.")
	hitretResult := Hitret(8100)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011240")
	Mess("Nnn... *pichaa*... Nnhuu... Nnn... Nnn...")
	hitretResult := Hitret(8101)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As a gulping sound came from her throat, she swallowedÅ^it all down")
	hitretResult := Hitret(8102)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011241")
	Mess("Haru... Let's do it more...?")
	hitretResult := Hitret(8103)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Sora looked at me with an intoxicated look in herÅ^eyes.")
	hitretResult := Hitret(8104)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("On all fours, she crawled towards me.")
	hitretResult := Hitret(8105)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With a clatter, she unfastened my belt and lowered myÅ^zipper.")
	hitretResult := Hitret(8106)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She then dropped my boxers stained with semen, and putÅ^my half erect thing in her mouth.")
	hitretResult := Hitret(8107)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011242")
	Mess("Nn, *chuu*... Nn, nnuu... Nn, nn......")
	hitretResult := Hitret(8108)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011243")
	Mess("Nn, nn, nnuu...!! Nn... Haa haaa...")
	hitretResult := Hitret(8109)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011244")
	Mess("Haru's penis... is delicious... *rero*... Nn, *chuu*...")
	hitretResult := Hitret(8110)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011245")
	Mess("See... It's... become... energetic...")
	hitretResult := Hitret(8111)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With her tongue rolling along the crease, she startedÅ^licking it.")
	hitretResult := Hitret(8112)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Above Sora's face, blood started to return to my thingÅ^again.")
	hitretResult := Hitret(8113)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011246")
	Mess("Nnhuu... *chuu*... Come on... You can... still doÅ^it......")
	hitretResult := Hitret(8114)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011247")
	Mess("Nn nnhuu... Nn... *chuuu*......")
	hitretResult := Hitret(8115)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A slurping sound resounded as she stuffed her mouth withÅ^my glans.")
	hitretResult := Hitret(8116)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While narrowing her lips, Sora looked up to me with aÅ^lewd facial expression.")
	hitretResult := Hitret(8117)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011248")
	Mess("Nnpuaa...... Haru.. Take me......")
	hitretResult := Hitret(8118)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My thing shook and stood up straight as she released itÅ^from inside her mouth.")
	hitretResult := Hitret(8119)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora quietly laid down on the futon, and slowly raisedÅ^the cuff of her skirt.")
	hitretResult := Hitret(8120)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA11d", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("I'm starting to be able to see her place.")
	hitretResult := Hitret(8121)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With her pubic hair wet, she spread her crease with herÅ^fingers, and showed it to me.")
	hitretResult := Hitret(8122)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her small hidden asshole became visible.")
	hitretResult := Hitret(8123)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011249")
	Mess("Go ahead... Haru......")
	hitretResult := Hitret(8124)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora made it easy for me to insert as she waited.")
	hitretResult := Hitret(8125)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With the tip now in, my thing covered in semen andÅ^saliva slowly sunk inside Sora's vagina.")
	hitretResult := Hitret(8126)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA11a", 0, 0)
	Talk("Sora", "SR011250")
	Mess("Nnguu... It's in... Your...")
	hitretResult := Hitret(8127)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011251")
	Mess("Aaahh... Haru's penis is coming inside me......")
	hitretResult := Hitret(8128)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her twitching hips shook up and down.")
	hitretResult := Hitret(8129)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It seems as if just putting it in made her cum.")
	hitretResult := Hitret(8130)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011252")
	Mess("It's big... It's like my place... will break...")
	hitretResult := Hitret(8131)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's okay... if I do it slowly...")
	hitretResult := Hitret(8132)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA11b", 0, 0)
	Talk("Sora", "SR011253")
	Mess("Uu... No... it's okay... If it makes you feel good... IÅ^can handle it...")
	hitretResult := Hitret(8133)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011254")
	Mess("...Come on... Thrust all the way in... Nnguu... ComeÅ^on...")
	hitretResult := Hitret(8134)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As Sora's vagina wriggled, it started to constrict myÅ^thing.")
	hitretResult := Hitret(8135)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA11a", 0, 0)
	Talk("Sora", "SR011255")
	Mess("Yess... Touch thereee...! Nnn ahh...")
	hitretResult := Hitret(8136)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Gripping Sora's leg, I slowly thrust inside, and thenÅ^pulled back.")
	hitretResult := Hitret(8137)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Thrusting, I sunk my thing sunk back in.")
	hitretResult := Hitret(8138)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As Sora's vagina was stimulated, it wriggled as if herÅ^vagina was its own living being.")
	hitretResult := Hitret(8139)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011256")
	Mess("Ahh... Aaahh...!! If... If you touch there... Nnguu...")
	hitretResult := Hitret(8140)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora bent backwards as her hips became unsteady.")
	hitretResult := Hitret(8141)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011257")
	Mess("Ahh, ahaahh...! It's good... there...")
	hitretResult := Hitret(8142)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011258")
	Mess("Thrust more... more! Aaaahh...!!")
	hitretResult := Hitret(8143)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011259")
	Mess("It feels good... there... Ahaahh!!")
	hitretResult := Hitret(8144)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I slowly moved my thing in and out, but I slowlyÅ^increased my speed rhythmically.")
	hitretResult := Hitret(8145)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011260")
	Mess("Ahh... Aahh...! Haa haa haa...! Nnhuunn...!!")
	hitretResult := Hitret(8146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011261")
	Mess("It's... It's deep insidee...! Every time you thrust, IÅ^go numbb...!")
	hitretResult := Hitret(8147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With a scrape, my thing touched inside Sora's vagina.")
	hitretResult := Hitret(8148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I struck the deepest part inside Sora, her bodyÅ^twitched as a sweet moan escaped from her mouth.")
	hitretResult := Hitret(8149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011262")
	Mess("Haa haa... Haruu... It feels good...")
	hitretResult := Hitret(8150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With her hair disheveled, her whole body felt pleasure.")
	hitretResult := Hitret(8151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Driving into her waist, the loud sound of rubbingÅ^together became loud.")
	hitretResult := Hitret(8152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA11b", 0, 0)
	Talk("Sora", "SR011263")
	Mess("Ahh... Nn... Such a perverted sound... Ahh, ahhaahh...")
	hitretResult := Hitret(8153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011264")
	Mess("Haruu... Let's feel better? Ahh aaaahhh...!!")
	hitretResult := Hitret(8154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Genital secretions dripped down Sora's thigh.")
	hitretResult := Hitret(8155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sweat and semen came together and smeared.")
	hitretResult := Hitret(8156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The sharp sweet fragrance began to stimulate us.")
	hitretResult := Hitret(8157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA11a", 0, 0)
	Talk("Sora", "SR011265")
	Mess("Haru... Kiss me... Nn, *chuu*... Nnnnnuu!!")
	hitretResult := Hitret(8158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While continuing to drive into her waist, I kissed Sora.")
	hitretResult := Hitret(8159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Without minding the drool hanging down, our tonguesÅ^entwined as we kissed.")
	hitretResult := Hitret(8160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We continued kissing, forgetting even to breathe.")
	hitretResult := Hitret(8161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even that suffocating feeling began to put us in evenÅ^more of the trance we were in from the pleasure ofÅ^entwining our tongues.")
	hitretResult := Hitret(8162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011266")
	Mess("Nnbu... Nn, nn... *chuu*... *chuu*......")
	hitretResult := Hitret(8163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011267")
	Mess("Amuu... Suck my tongue... Haruu......")
	hitretResult := Hitret(8164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011268")
	Mess("Nnku... Nuu...... Nnnuuu...!! Nn*chuu*...")
	hitretResult := Hitret(8165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I sucked Sora's extended tongue, and my whole mouthÅ^became sticky from the sweet saliva.")
	hitretResult := Hitret(8166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA11b", 0, 0)
	Talk("Sora", "SR011269")
	Mess("Let's do it more? Nnhuu... *chuu*... Nnuu...")
	hitretResult := Hitret(8167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I entwined tongues with Sora, who had an absentmindedÅ^facial expression.")
	hitretResult := Hitret(8168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her face that had become sticky with drool approachedÅ^near, and sucked her lips again.")
	hitretResult := Hitret(8169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("For a moment, like a fine thread, drool connectedÅ^between Sora and I before vanishing.")
	hitretResult := Hitret(8170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011270")
	Mess("I'm glad... I'm glad... To become together with you thisÅ^much...")
	hitretResult := Hitret(8171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA11a", 0, 0)
	Talk("Sora", "SR011271")
	Mess("I want to... melt together more... Ahhuu... Nnuuu...!")
	hitretResult := Hitret(8172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......Sora......")
	hitretResult := Hitret(8173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My sweat dripped down from Sora's body.")
	hitretResult := Hitret(8174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's skin was dyed in pink, and large sweat began toÅ^transfer.")
	hitretResult := Hitret(8175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I sucked on Sora's nipple covered in that sweat.")
	hitretResult := Hitret(8176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011272")
	Mess("Nnaahh... You're sucking my nipppple... Nnn nnuuu...")
	hitretResult := Hitret(8177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011273")
	Mess("Roll more... with your tongue... Nnaauu...!")
	hitretResult := Hitret(8178)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While sucking, I licked her nipple.")
	hitretResult := Hitret(8179)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And as I rubbed her breasts, her nipples stood up.")
	hitretResult := Hitret(8180)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011274")
	Mess("Nnguu... O--Oww... Don't rub it that strongly...")
	hitretResult := Hitret(8181)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sory...")
	hitretResult := Hitret(8182)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011275")
	Mess("Fufu... To be entranced by my breasts, Haru's a man...")
	hitretResult := Hitret(8183)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011276")
	Mess("Sorry for getting big.........")
	hitretResult := Hitret(8184)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No... My breasts are sensitive, aren't they?")
	hitretResult := Hitret(8185)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011277")
	Mess("Nnkuu... Ahh, ahuu...... Nn, nnaah... Yes... likeÅ^that...")
	hitretResult := Hitret(8186)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011278")
	Mess("Since it's you... Haahnn... Touch it a lot...")
	hitretResult := Hitret(8187)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011279")
	Mess("Nnaaahh...!! It's getting sensitivee!!")
	hitretResult := Hitret(8188)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011280")
	Mess("Ahh... That... nn... tickles......")
	hitretResult := Hitret(8189)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I continued rolling my tongue on the tip of her nipple.")
	hitretResult := Hitret(8190)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I prod the place where breast milk comes out, as if toÅ^dig it out.")
	hitretResult := Hitret(8191)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011281")
	Mess("Nnguu... It feels... good... Nnnnn......")
	hitretResult := Hitret(8192)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's body trembled, and for a moment her legs betweenÅ^my body shut.")
	hitretResult := Hitret(8193)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's waist pulled back as if to escape, so I caughtÅ^her shoulder, and strongly put my body weight into myÅ^thing.")
	hitretResult := Hitret(8194)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011282")
	Mess("Nnkuu... Aaaahh...!! Ahh, haa haa haa...")
	hitretResult := Hitret(8195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011283")
	Mess("Ahh... It's colliding... deep insidee...")
	hitretResult := Hitret(8196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011284")
	Mess("Your penis is... banging.. against my stomachh!!")
	hitretResult := Hitret(8197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011285")
	Mess("Nnhuu... Ahh... Aaaahhh...!! The hot stuff...")
	hitretResult := Hitret(8198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011286")
	Mess("My inside are so hot... it feels like I'll get burned!!Å^Aaaahh...!!")
	hitretResult := Hitret(8199)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Every time my thing goes in and out, Sora's legs twitchÅ^in respone.")
	hitretResult := Hitret(8200)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her constricting feverish vagina makes me feel like I'llÅ^melt inside it.")
	hitretResult := Hitret(8201)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Hearing Sora's delighted voice as my hard thing thrustsÅ^inside her, I half-rise to my feet.")
	hitretResult := Hitret(8202)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Scraping against Sora's vaginal wall so hard it couldÅ^hurt, a large quantity of genital secretions from herÅ^genitalia scattered.")
	hitretResult := Hitret(8203)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The pleasure began to increase.")
	hitretResult := Hitret(8204)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I want to make you all jumbled up. I want to see you atÅ^the height of satisfaction.")
	hitretResult := Hitret(8205)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011287")
	Mess("It's good... Haru... do it more... Ahhhh...!!")
	hitretResult := Hitret(8206)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011288")
	Mess("Thrust moree...! Let me feel you a lot!!")
	hitretResult := Hitret(8207)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011289")
	Mess("It's good... The tingling sensation inside me feelsÅ^good...")
	hitretResult := Hitret(8208)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011290")
	Mess("Every time you move, nnuu...!! A blissful pleasureÅ^comess!!")
	hitretResult := Hitret(8209)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011291")
	Mess("So, you can make me... all jumbled up...!!")
	hitretResult := Hitret(8210)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011292")
	Mess("Haru... If it's for your sake... Aaahh...!")
	hitretResult := Hitret(8211)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I continued strongly moving like a piston, enough toÅ^worry that if her place would break.")
	hitretResult := Hitret(8212)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My thing made noises as it entered deep inside Sora.")
	hitretResult := Hitret(8213)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Grabbing my hand, Sora supported it, as it to guide it.")
	hitretResult := Hitret(8214)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's breathing looked painful, but she looked at meÅ^with a smile on her face.")
	hitretResult := Hitret(8215)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Every time I thrust, tears of joy ran down her cheek.")
	hitretResult := Hitret(8216)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011293")
	Mess("Aaahh... Haruu... Haru..!! I'm...")
	hitretResult := Hitret(8217)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011294")
	Mess("I'm cumminggggg... Aahh.... More...!")
	hitretResult := Hitret(8218)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011295")
	Mess("Cum lots... please? Your... semen......")
	hitretResult := Hitret(8219)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011296")
	Mess("Cum lotsss...! Nnaahh...!! Aaaahh...!!")
	hitretResult := Hitret(8220)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......Soraa......")
	hitretResult := Hitret(8221)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's trembling voice.")
	hitretResult := Hitret(8222)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm approaching my limit...")
	hitretResult := Hitret(8223)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My thing thrust strongly deep inside her, as if twistingÅ^her womb.")
	hitretResult := Hitret(8224)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011297")
	Mess("Hii... Guu... Ahh... Aaaahhh... Already...")
	hitretResult := Hitret(8225)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011298")
	Mess("Aahh... Ahaahh... I... I......")
	hitretResult := Hitret(8226)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011299")
	Mess("I'm... cumming... I'm cumming... Nnnuuuu...!!")
	hitretResult := Hitret(8227)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Sora...!! Kuu......!!")
	hitretResult := Hitret(8228)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA11c", 0, 0)
	Flush("WHITE", 1000, "")
	Talk("Sora", "SR011300")
	Mess("Nnuuu... Aaaaaaaaaaahhh!!!")
	hitretResult := Hitret(8229)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011301")
	Mess("Ahh, auuu... You're cumming... inside...")
	hitretResult := Hitret(8230)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011302")
	Mess("Lots of... your hot stuff....")
	hitretResult := Hitret(8231)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011303")
	Mess("Aaahh... Ahh... Haa haa haa...")
	hitretResult := Hitret(8232)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011304")
	Mess("Haruu... Nn.... *chuu*... Nn, *chuu*...")
	hitretResult := Hitret(8233)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My semen unloaded inside Sora's vagina in copiousÅ^amounts.")
	hitretResult := Hitret(8234)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That moment, semen from her vagina flowed out and mixedÅ^together.")
	hitretResult := Hitret(8235)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her waist moved over and over as I ejaculated.")
	hitretResult := Hitret(8236)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I squirted inside her... I temporarily became numbÅ^from the pleasure.")
	hitretResult := Hitret(8237)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her vagina squeezed on my thing as it convulsed inside.Å^As I enjoyed that sensation, I hung over Sora.")
	hitretResult := Hitret(8238)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Holding my hips, Sora tightly embraced me.")
	hitretResult := Hitret(8239)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011305")
	Mess("Haru.........")
	hitretResult := Hitret(8240)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011306")
	Mess("Haaaa... Stay like this with me forever...")
	hitretResult := Hitret(8241)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011307")
	Mess("I want to feel you more.")
	hitretResult := Hitret(8242)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay... Sora... Nn...")
	hitretResult := Hitret(8243)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011308")
	Mess("Aahh... Haru... Nn, *chuu*... Nnn, nnn...")
	hitretResult := Hitret(8244)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even in the moment of trying to end our wild breathing,Å^we continued kissing.")
	hitretResult := Hitret(8245)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Dripping with sweat, our feelings became one.")
	hitretResult := Hitret(8246)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("We wanted to feel that more.")
	hitretResult := Hitret(8247)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	OnRecollectFlag(4)
	If (IsRecollectMode())
		Change("EXIT_SCENARIO")
		Return
	EndIf
	EyeCatchEnter("DATE", 0)
	Change("00_a027")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

