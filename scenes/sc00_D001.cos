Include"yosuga.ch"

Function sc00_D001()
	ScenarioEnter()
	ScPlayBgm("BGM03", 0)
	SetCg("B01a", 0, 0)
	Talk("Haruka", "")
	Mess("I'm homeÅ`")
	hitretResult := Hitret(26707)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B32a", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("I don't know if Sora was out or not, but the house wasÅ^dead silent, with no signs of human life.")
	hitretResult := Hitret(26708)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Soraa? I'm back! You around?")
	hitretResult := Hitret(26709)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The entrance wasn't locked. She's so careless...")
	hitretResult := Hitret(26710)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B02a", 0, 0)
	Talk("Haruka", "")
	Mess("Whoa...")
	hitretResult := Hitret(26711)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In the kitchen, plates and cups were left out on theÅ^table, and a hard, dry piece of bread was left thereÅ^half eaten.")
	hitretResult := Hitret(26712)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She probably microwaved it again...")
	hitretResult := Hitret(26713)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...If she couldn't eat it all, then that can't beÅ^helped, but I wish she'd at least put away the jam andÅ^margarine...")
	hitretResult := Hitret(26714)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It would be troublesome if they went bad because we justÅ^bought them.")
	hitretResult := Hitret(26715)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Letting off a sigh, I put them in the fridge. While theÅ^fridge was open, I realized that we had no ingredientsÅ^for making side dishes.")
	hitretResult := Hitret(26716)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'll have to go shopping. We have to have more than justÅ^rice for dinner, after all.")
	hitretResult := Hitret(26717)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But I'd have to go all the way to the supermarket nearÅ^the station...")
	hitretResult := Hitret(26718)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I was grieving my dilemma, I suddenly rememberedÅ^something I saw on the way home.")
	hitretResult := Hitret(26719)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The trucks parked in the plaza.")
	hitretResult := Hitret(26720)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The people gathered around there.")
	hitretResult := Hitret(26721)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was called \"Takano,\" I think...theÅ^pseudo-supermarket.")
	hitretResult := Hitret(26722)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It apparently comes twice a week, and I think today isÅ^one of those days.")
	hitretResult := Hitret(26723)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I guess I have another option.")
	hitretResult := Hitret(26724)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B01a", 0, 0)
	Transition("WIP_LR", 500)
	Update()
	ScWait(500, 0)
	SetCg("B34a", 400, 0)
	Transition("WIP_LR", 500)
	Talk("Haruka", "")
	Mess("...Huh?...")
	hitretResult := Hitret(26725)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_01S", 0, 0)
	Update()
	ScWait(500, 0)
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("The person walking over there...was that Migiwa-san?")
	hitretResult := Hitret(26726)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_01M", 0, 0)
	Talk("Kazuha", "KA040001")
	Mess("Ah...Kasugano-kun?")
	hitretResult := Hitret(26727)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I thought that was you, Migiwa-san. Hello.")
	hitretResult := Hitret(26728)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040002")
	Mess("Hello....Kasugano-kun, were you about to go shoppingÅ^now?")
	hitretResult := Hitret(26729)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yep, that's right. Is that a kimono?")
	hitretResult := Hitret(26730)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Being properly dressed in Japanese clothes that had aÅ^refined colors and a pattern colors was very becoming ofÅ^Migiwa-san, who was standing there looking dignified.")
	hitretResult := Hitret(26731)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That makes me reaffirm that she is definitely anÅ^Ojou-sama.")
	hitretResult := Hitret(26732)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's quite nice. I wonder if it's because you have longÅ^hair, but it looks better on you than the schoolÅ^uniform.")
	hitretResult := Hitret(26733)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_10M", 0, 0)
	Talk("Kazuha", "KA040003")
	Mess("T-There's no point in flattering me.")
	hitretResult := Hitret(26734)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It wasn't flattery...umm...do you often wear that?")
	hitretResult := Hitret(26735)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040004")
	Mess("I did when I was a child, but I haven't really worn itÅ^lately. ...A guest of my dad's came over today, so...")
	hitretResult := Hitret(26736)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040005")
	Mess("I don't really like wearing it. It's not something youÅ^wear these days, so I definitely stand out.")
	hitretResult := Hitret(26737)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_08M", 0, 0)
	Talk("Kazuha", "KA040006")
	Mess("Children are really sensitive to that, so when I wasÅ^small, I didn't really get to play with everyone becauseÅ^of this.")
	hitretResult := Hitret(26738)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san lowered her eyebrows, looking depressed.")
	hitretResult := Hitret(26739)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Seeing Migiwa-san in that state really created anÅ^atmosphere, so it probably did happen to her.")
	hitretResult := Hitret(26740)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But Migiwa-san was worried about something like that...Å^She seems like a normal girl.")
	hitretResult := Hitret(26741)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But thinking about her in this way is probably rude.")
	hitretResult := Hitret(26742)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_10M", 0, 0)
	Talk("Kazuha", "KA040007")
	Mess("Tsk, what on earth am I saying, please just forget IÅ^said anything.")
	hitretResult := Hitret(26743)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040008")
	Mess("More importantly, weren't you in the middle of goingÅ^shopping?")
	hitretResult := Hitret(26744)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, yeah. To \"Takano.\"")
	hitretResult := Hitret(26745)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_01M", 0, 0)
	Talk("Kazuha", "KA040009")
	Mess("Ah, the one in the plaza...Are you going by yourself?")
	hitretResult := Hitret(26746)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There's my sister, Sora...but she can't be relied on. SoÅ^I have to go.")
	hitretResult := Hitret(26747)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040010")
	Mess("You have a sister? Isn't that...really troublesome?")
	hitretResult := Hitret(26748)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, just a bit. There's still things we're not usedÅ^to, after all.")
	hitretResult := Hitret(26749)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040011")
	Mess("You have to do the housework of two people, right? It'sÅ^a bit unimaginable for me...")
	hitretResult := Hitret(26750)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040012")
	Mess("You grew up in the city, so you're probably reallyÅ^reliable, right?")
	hitretResult := Hitret(26751)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No waay! Even now, the fridge is basically empty, so I'mÅ^rushing to the shops.")
	hitretResult := Hitret(26752)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_02M", 0, 0)
	Talk("Kazuha", "KA040013")
	Mess("Even then, you're still great.")
	hitretResult := Hitret(26753)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, stop that...It's really nothing...")
	hitretResult := Hitret(26754)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After being praised too much, my cheek started to getÅ^itchy.")
	hitretResult := Hitret(26755)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("In reality, I was quite irresponsible. If Sora wasn'tÅ^here, I'd still probably have only cup noodles forÅ^dinner.")
	hitretResult := Hitret(26756)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("And it's not like I get the housework done properly.")
	hitretResult := Hitret(26757)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_03M", 0, 0)
	Talk("Kazuha", "KA040014")
	Mess("Going shopping while thinking about your sister, that'sÅ^definitely great.")
	hitretResult := Hitret(26758)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040015")
	Mess("I can see that you're acting on your sense ofÅ^responsibility as a brother.")
	hitretResult := Hitret(26759)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I wonder...Haha, I don't think so, but...")
	hitretResult := Hitret(26760)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wonder how Migiwa-san sees me?")
	hitretResult := Hitret(26761)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It seems there's a big gap between how I see myself andÅ^how she sees me, giving me a bitter smile.")
	hitretResult := Hitret(26762)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_01M", 0, 0)
	Talk("Kazuha", "KA040016")
	Mess("But I don't think it's good for you to hang aroundÅ^Nakazato-senpai and go crazy with him.")
	hitretResult := Hitret(26763)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I'll be careful...")
	hitretResult := Hitret(26764)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Against her narrowed eyes, I reflexively nodded.")
	hitretResult := Hitret(26765)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I could see that Migiwa-san had a strict attitude towardÅ^Ryouhei. It seems like she really can't put up withÅ^him...")
	hitretResult := Hitret(26766)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Woman's voiceÅsèââ¿Åt", "MT040001")
	Mess("...Ojou-samaa! O-J-O-U-S-A-M-A!!!")
	hitretResult := Hitret(26767)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm?")
	hitretResult := Hitret(26768)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wonder what that is? It sounds like someone'sÅ^searching for something.")
	hitretResult := Hitret(26769)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040017")
	Mess("Eh?...That voice was...")
	hitretResult := Hitret(26770)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san turned to look behind me, so I looked towardÅ^the same way.")
	hitretResult := Hitret(26771)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Doing so, I saw someone calling out while waving theirÅ^hand...")
	hitretResult := Hitret(26772)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_01S", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("......")
	hitretResult := Hitret(26773)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("WomanÅsèââ¿Åt", "MT040002")
	Mess("Ojou-samaaaaa, there you are.")
	hitretResult := Hitret(26774)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("........")
	hitretResult := Hitret(26775)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("........Maid?")
	hitretResult := Hitret(26776)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD03_02M", 0, 0)
	Talk("Haruka", "")
	Mess("She's looking for you, right?")
	hitretResult := Hitret(26777)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040018")
	Mess("Yes, she helps out at my house.")
	hitretResult := Hitret(26778)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_01M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("A domestic helper...Ah! Come to think of it, she was theÅ^one to pick her up before...")
	hitretResult := Hitret(26779)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT040003")
	Mess("Ojou-sama, sorry for intruding while you were in theÅ^middle of a conversation.")
	hitretResult := Hitret(26780)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The maid respectfully bowed her head.")
	hitretResult := Hitret(26781)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Despite this person being clearly older than Migiwa-san,Å^she followed the master-servant relationship.")
	hitretResult := Hitret(26782)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wonder if she's someone that a mere commoner like meÅ^can casually talk to.")
	hitretResult := Hitret(26783)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT040004")
	Mess("Ojou-sama, your mother is looking for you.")
	hitretResult := Hitret(26784)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040019")
	Mess("Understood. I'll return immediately.Å^...Ah.")
	hitretResult := Hitret(26785)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040020")
	Mess("I haven't introduced her properly yet.Å^This is Nogisaka Motoka. I said this before, but sheÅ^helps out at my house.")
	hitretResult := Hitret(26786)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_02M", 0, 0)
	Talk("Motoka", "MT040005")
	Mess("Nogisaka's fine. Pleased to meet you.")
	hitretResult := Hitret(26787)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040021")
	Mess("And he's Kasugano Haruka-kun. We're classmates.")
	hitretResult := Hitret(26788)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Pleased to meet you too...I'm Kasugano Haruka.")
	hitretResult := Hitret(26789)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Being introduced like this makes even me feel weird,Å^becuase it's as if I were also a distiguished person.")
	hitretResult := Hitret(26790)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Trying not to be rude, I bowed at the same level as theÅ^maid, while worrying that I'd come off as awkward.")
	hitretResult := Hitret(26791)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_01M", 0, 0)
	Talk("Motoka", "MT040006")
	Mess("Ojou-sama's classmate...Ahh, the kid who was withÅ^everyone last time!")
	hitretResult := Hitret(26792)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, yes.")
	hitretResult := Hitret(26793)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT040007")
	Mess("I see, you were one of Ojou-sama's classmates. I wasÅ^wondering why a cool kid like you was with them.")
	hitretResult := Hitret(26794)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Cool?...When I first saw you, I was surprised too...")
	hitretResult := Hitret(26795)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_03M", 0, 0)
	SetBustup("CD03_01M", 0, 0)
	Talk("Motoka", "MT040008")
	Mess("Ahh, well yeah. With this kind of outfit, that can't beÅ^helped.")
	hitretResult := Hitret(26796)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("E-Eh...? Maybe it's just my imagination, but unlike whenÅ^she's talking to Migiwa-san, she seems really frank...")
	hitretResult := Hitret(26797)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, once again, pleased to meet you.")
	hitretResult := Hitret(26798)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_04M", 0, 0)
	Talk("Motoka", "MT040009")
	Mess("Yep, yep, pleased to meet you....ah.")
	hitretResult := Hitret(26799)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040022")
	Mess("......")
	hitretResult := Hitret(26800)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_02M", 0, 0)
	Talk("Motoka", "MT040010")
	Mess("I-I'm very pleased to make your acquaintance,Å^Kasugano-kun.")
	hitretResult := Hitret(26801)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh, ah...yeah.")
	hitretResult := Hitret(26802)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Eh, that ceremonial feel again...")
	hitretResult := Hitret(26803)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Don't tell me Nogisaka-san is forcibly making thatÅ^respectful attitude?")
	hitretResult := Hitret(26804)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040023")
	Mess("Well then, Kasugano-kun, sorry for cutting theÅ^conversation off halfway, but excuse me.")
	hitretResult := Hitret(26805)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay, I'll see you at school then.")
	hitretResult := Hitret(26806)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Down(5, 0, 0, 0, 0)
	Update()
	Down(5, -1, 0, 0)
	Update()
	BustupClear(5)
	Talk("Motoka", "MT040011")
	Mess("Please excuse us.")
	hitretResult := Hitret(26807)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Down(6, 0, 0, 0, 0)
	Update()
	Down(6, -1, 0, 0)
	Update()
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Following Migiwa-san's lead, Nogisaka-san bowedÅ^politely, and walked toward the mountains.")
	hitretResult := Hitret(26808)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm not one to talk, but that respectful behaviorÅ^somehow seems awkward when compared to how naturalÅ^Migiwa-san's is.")
	hitretResult := Hitret(26809)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even when taking that one step to follow Nogisaka-san,Å^it shows that she's a master at it.")
	hitretResult := Hitret(26810)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("An Ojou-sama through and through.")
	hitretResult := Hitret(26811)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Though it might be better if I think over how I dealÅ^with her...")
	hitretResult := Hitret(26812)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Judging from how Ryouhei and the rest of the class dealÅ^with it, it might be better if I don't worry about it.")
	hitretResult := Hitret(26813)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Well, once I get used to it, I'll know what to do.")
	hitretResult := Hitret(26814)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Besides, now's not the time to be thinking - I need toÅ^go shopping.")
	hitretResult := Hitret(26815)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	Change("00_d002")
	Return
	Change("EXIT_SCENARIO")
EndFunc

