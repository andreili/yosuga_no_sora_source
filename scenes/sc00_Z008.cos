Include"yosuga.ch"

Function sc00_Z008()
	ScenarioEnter()
	ScPlayBgm("BGM03", 0)
	SetCg("B02a", 0, 0)
	Talk("Haruka", "")
	Mess("Now then... What to do about tonight's dinner...")
	hitretResult := Hitret(1130)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Every time I stand in the kitchen like this, I can'tÅ^help but admire the mothers around the world that haveÅ^to do this every day.")
	hitretResult := Hitret(1131)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If it were just me, I wouldn't care about when we eat orÅ^even not eating at all, but...")
	hitretResult := Hitret(1132)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But there's no way I can let Sora not eat.")
	hitretResult := Hitret(1133)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In any case, when the environment changes, cooking forÅ^yourself is difficult.")
	hitretResult := Hitret(1134)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Over here, the supermarket and convenience stores areÅ^far away. It's not like in the city, where you can getÅ^something no matter how late.")
	hitretResult := Hitret(1135)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Guess I'll be going.")
	hitretResult := Hitret(1136)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If I don't save up food in the fridge to some extent,Å^I'd be worried if there was an emergency.")
	hitretResult := Hitret(1137)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Soraa! I'm going out for a bit.")
	hitretResult := Hitret(1138)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("......")
	hitretResult := Hitret(1139)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...If something comes up, call me on the mobile, okay?")
	hitretResult := Hitret(1140)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As usual, there's no answer.")
	hitretResult := Hitret(1141)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She probably heard me... This time, I made sure to putÅ^my phone in my pocket and left.")
	hitretResult := Hitret(1142)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE156", 0)
	SetCg("B12a", 0, 0)
	Transition("WIP_MOZH", 500)
	Talk("Haruka", "")
	Mess("...Hm?")
	hitretResult := Hitret(1143)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE153", 0)
	Talk("êSÇÃê∫", "")
	Mess("Subject: Sweets")
	hitretResult := Hitret(1144)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...So she did hear me.")
	hitretResult := Hitret(1145)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("No content in the message, just a concise request forÅ^sweets.")
	hitretResult := Hitret(1146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Geez...if you're going to have to send an email, itÅ^would've been quicker just to reply when I asked...")
	hitretResult := Hitret(1147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Closing my mobile while sighing out some unforgivableÅ^feelings, I made my way towards the supermarket inÅ^earnest.")
	hitretResult := Hitret(1148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If I had thought about it properly, it would've beenÅ^more efficient to drop by on the way home from school.")
	hitretResult := Hitret(1149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Next time, if I'm not walking home with anyone likeÅ^today, I'll drop by on the way home from school.")
	hitretResult := Hitret(1150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I was walking along a road of seemingly unchangingÅ^paddy fields...")
	hitretResult := Hitret(1151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("VoiceÅsìﬁèèÅt", "NO000105")
	Mess("Huh...Haru-chan? Short time no see.")
	hitretResult := Hitret(1152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_02M", 0, 0)
	Talk("Haruka", "")
	Mess("Ah, Nao-chan.")
	hitretResult := Hitret(1153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000106")
	Mess("Going out at this time? Ah, shopping?")
	hitretResult := Hitret(1154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, that's right. Heading to the supermarket at theÅ^station.")
	hitretResult := Hitret(1155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000107")
	Mess("Hmmm?...Ah, if that's the case, I'll show you somethingÅ^good.")
	hitretResult := Hitret(1156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? What?")
	hitretResult := Hitret(1157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000108")
	Mess("Hehe, if you follow me, you'll understand!")
	hitretResult := Hitret(1158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B34a", 400, 0)
	SetBustup("CB02_01M", 0, 0)
	Transition("WIP_RL", 500)
	Talk("êSÇÃê∫", "")
	Mess("At Nao-chan's suggestion, I followed her.")
	hitretResult := Hitret(1159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was a road different to the ones I know, but theÅ^relaxed scenery was unchanged.")
	hitretResult := Hitret(1160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000109")
	Mess("At any rate, I didn't think I'd be walking with you likeÅ^this again.")
	hitretResult := Hitret(1161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Yeah. I'm depending on you again.")
	hitretResult := Hitret(1162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_02M", 0, 0)
	Talk("Nao", "NO000110")
	Mess("It's nothing. Actually, I'm quite happy. It's like goingÅ^back to the past.")
	hitretResult := Hitret(1163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I suppose it is...you taking me to various places,Å^playing around...")
	hitretResult := Hitret(1164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_10M", 0, 0)
	Talk("Nao", "NO000111")
	Mess("Hey, Haru-chan...")
	hitretResult := Hitret(1165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm?")
	hitretResult := Hitret(1166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000112")
	Mess("After you returned...umm...nothing happened?")
	hitretResult := Hitret(1167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh...")
	hitretResult := Hitret(1168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000113")
	Mess("Ah, it's nothing...things were quite hectic...")
	hitretResult := Hitret(1169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah...")
	hitretResult := Hitret(1170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000114")
	Mess("Sora-chan...because of your parents...?")
	hitretResult := Hitret(1171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Well, there is that, but she's always been prone toÅ^sickness, so she's shut herelf in quite a bit.")
	hitretResult := Hitret(1172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000115")
	Mess("Is that so...")
	hitretResult := Hitret(1173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What's wrong?")
	hitretResult := Hitret(1174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_01M", 0, 0)
	Talk("Nao", "NO000116")
	Mess("It's nothing, you're having a hard time, so I'm justÅ^thinking how I can help...")
	hitretResult := Hitret(1175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Just that feeling is enough. You're helping me out now,Å^after all.")
	hitretResult := Hitret(1176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_11M", 0, 0)
	Talk("Nao", "NO000117")
	Mess("That's true, but...")
	hitretResult := Hitret(1177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	AddSelect("I'll be in your care", 0)
	AddSelect("We've grown, right?", 0)
	hitretResult := StartSelect()
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	If (ChkSelect(1))
		Talk("Haruka", "")
		Mess("But I'm sure you'll be helping me a lot from now on.")
		hitretResult := Hitret(1178)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("To be honest, I'm full of things I don'tÅ^understand...especially household things. I'm alwaysÅ^worrying about what I should be doing.")
		hitretResult := Hitret(1179)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		SetBustup("CB02_03M", 0, 0)
		Talk("Nao", "NO000118")
		Mess("Hehe, you're definitely a guy then.")
		hitretResult := Hitret(1180)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		SetBustup("CB02_11M", 0, 0)
		Talk("Nao", "NO000119")
		Mess("But I'm happier that that's the case.")
		hitretResult := Hitret(1181)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("...Eh?")
		hitretResult := Hitret(1182)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		SetBustup("CB02_01M", 0, 0)
		Talk("Nao", "NO000120")
		Mess("...No, it's nothing.")
		hitretResult := Hitret(1183)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		OnFlag(2)
	ElseIf (ChkSelect(2))
		Talk("Haruka", "")
		Mess("But despite all of that, we've grown, right?")
		hitretResult := Hitret(1184)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		SetBustup("CB02_01M", 0, 0)
		Talk("Nao", "NO000121")
		Mess("Oh oh, standing on your toes, eh?")
		hitretResult := Hitret(1185)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("Geez, you can't keep bringing out that image of thatÅ^little boy you knew, you know?")
		hitretResult := Hitret(1186)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		SetBustup("CB02_02M", 0, 0)
		Talk("Nao", "NO000122")
		Mess("That's right. To me, Haru-chan hasn't changed at allÅ^since back then.")
		hitretResult := Hitret(1187)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		SetBustup("CB02_01M", 0, 0)
		Talk("Nao", "NO000123")
		Mess("But in the time we haven't seen each other, you'veÅ^become...strong, right?")
		hitretResult := Hitret(1188)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("I wonder if that's true... Sometimes it feels like it'sÅ^just time flowing and I'm just barely hanging on. But ifÅ^you think that, I'm happy.")
		hitretResult := Hitret(1189)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Nao", "NO000124")
		Mess("...But, it's a bit disappointing for me.")
		hitretResult := Hitret(1190)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("Eh? What is?")
		hitretResult := Hitret(1191)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		SetBustup("CB02_03M", 0, 0)
		Talk("Nao", "NO000125")
		Mess("I wanted to act the Onee-san more, you know?")
		hitretResult := Hitret(1192)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("Hahahaha. But to me, Nao-chan will always be Nao-chan.")
		hitretResult := Hitret(1193)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		SetBustup("CB02_02M", 0, 0)
		Talk("Nao", "NO000126")
		Mess("Hehe, thanks.")
		hitretResult := Hitret(1194)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
	EndIf
	BlackOut(0, 0)
	ScStopBgm()
	Talk("Nao", "NO000127")
	Mess("Okay, we've arrived.")
	hitretResult := Hitret(1195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh... Here?")
	hitretResult := Hitret(1196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000128")
	Mess("Yup, our destination. But there's definitely no harm inÅ^remembering it yourself.")
	hitretResult := Hitret(1197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayEnvSe("SE351", "", 0)
	Talk("êSÇÃê∫", "")
	Mess("There was nothing special about this place. It hadÅ^similarities to a plaza.")
	hitretResult := Hitret(1198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And in this plaza, some really old speakers with soundÅ^quality so bad, that it wouldn't even be used at anÅ^athletics meet were booming out a tune.")
	hitretResult := Hitret(1199)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And at the source of the noise...")
	hitretResult := Hitret(1200)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B14a", 0, 0)
	Talk("Haruka", "")
	Mess("...What's that?")
	hitretResult := Hitret(1201)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000129")
	Mess("Supermarket Takano.")
	hitretResult := Hitret(1202)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Supermarket? Takano?")
	hitretResult := Hitret(1203)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Without thinking, I repeated Nao-chan's words.")
	hitretResult := Hitret(1204)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A truck had stopped in the middle of the plaza.")
	hitretResult := Hitret(1205)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The side of the trailer had been converted to an awning,Å^and under it was a showcase of things from aÅ^supermarket.")
	hitretResult := Hitret(1206)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000130")
	Mess("He comes around twice a week.")
	hitretResult := Hitret(1207)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("R-Right..")
	hitretResult := Hitret(1208)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Now that I think of it, I think I do remember aÅ^greengrocer moving around on wheels and selling things.")
	hitretResult := Hitret(1209)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wonder if this is similar to the old one...or whetherÅ^it IS the old one.")
	hitretResult := Hitret(1210)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000131")
	Mess("It's a lot closer than the other supermarket. You canÅ^get the necessary things from here.")
	hitretResult := Hitret(1211)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Well, with me and Sora's lifestyle, we don't really needÅ^to go to the supermarket every day...")
	hitretResult := Hitret(1212)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In which case, this is practical enough.")
	hitretResult := Hitret(1213)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE351", 0)
	ScPlayBgm("BGM15", 0)
	Talk("Takano Shopkeeper", "TK000001")
	Mess("Welcome! Welcome! Welcome! Welcome!")
	hitretResult := Hitret(1214)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Takano Shopkeeper", "TK000002")
	Mess("Pork's on special today! A cut of pork loin, 300 gramsÅ^for 400 yen! It'll sell out if you don't hurry! HowÅ^about it, girl over there?")
	hitretResult := Hitret(1215)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_01M", 0, 0)
	Talk("Nao", "NO000132")
	Mess("Haru-chan, what's your budget?")
	hitretResult := Hitret(1216)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, around 5000 yen would be fine.")
	hitretResult := Hitret(1217)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000133")
	Mess("Well then, I'll pick for you. Hey thereÅ[! I'll takeÅ^some pork!")
	hitretResult := Hitret(1218)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	Talk("êSÇÃê∫", "")
	Mess("With that, Nao-chan threw herself into the crowd ofÅ^customers.")
	hitretResult := Hitret(1219)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Quake(16, 0, 1, 2, 30)
	Talk("Nao", "NO000134")
	Mess("Ah, hold on! Gimme some of that milk!")
	hitretResult := Hitret(1220)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se003", 0)
	Quake(0, 16, 1, 2, 30)
	Talk("Nao", "NO000135")
	Mess("Ow! Ow! Ouch!! Old lady, you're stepping on me!")
	hitretResult := Hitret(1221)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Wading through the waves of the old ladies' army,Å^Nao-chan briskly continued shopping.")
	hitretResult := Hitret(1222)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I stood there thinking relatively rude things like,Å^\"Were there really this many people living aroundÅ^here...?\"")
	hitretResult := Hitret(1223)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Wow.")
	hitretResult := Hitret(1224)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I said \"wow\" without thinking, but something with thisÅ^sort of impact...I've witnessed scenes like this atÅ^department stores in the city, but...")
	hitretResult := Hitret(1225)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Seeing it at a moving supermarket in the countryside?")
	hitretResult := Hitret(1226)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000136")
	Mess("Huh? Where's the chicken?")
	hitretResult := Hitret(1227)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I admired Nao-chan's courage and strength as she pushedÅ^her way through the crowd, picking up one item sheÅ^wanted after another.")
	hitretResult := Hitret(1228)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_06M", 0, 0)
	Talk("Nao", "NO000137")
	Mess("Here you go, I'm ready to pay! Any free stuff?")
	hitretResult := Hitret(1229)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Takano Shopkeeper", "TK000003")
	Mess("Thank you, thank you! Homemade picked radish, specialÅ^service to you!")
	hitretResult := Hitret(1230)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_02M", 0, 0)
	Talk("Nao", "NO000138")
	Mess("What do you think? Good enough?")
	hitretResult := Hitret(1231)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks...W-Wow, that was overwhelming.")
	hitretResult := Hitret(1232)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I picked up the shopping bag from Nao-chan.")
	hitretResult := Hitret(1233)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_01M", 0, 0)
	Talk("Nao", "NO000139")
	Mess("Really? This is what passes as normal here.Å^But from now on, you're a rival too, Haru-chan.")
	hitretResult := Hitret(1234)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh!? Go easy on me...")
	hitretResult := Hitret(1235)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000140")
	Mess("Hehe, I'm joking. If you need stuff that isn't here,Å^you'll have to go to the supermarket.")
	hitretResult := Hitret(1236)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Takano Shopkeeper", "TK000004")
	Mess("Oi oi, that's troubling, introducing him to other shops.")
	hitretResult := Hitret(1237)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_03M", 0, 0)
	Talk("Nao", "NO000141")
	Mess("I'm sorry, but even we have to make our way in life.")
	hitretResult := Hitret(1238)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Takano Shopkeeper", "TK000005")
	Mess("Haha, tell me something I don't know. Well, I'll beÅ^counting on you again. That cool boyfriend too!")
	hitretResult := Hitret(1239)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? Ah...yeah. Wait, boyfriend?")
	hitretResult := Hitret(1240)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_11M", 0, 0)
	Jump(3, 0)
	Talk("Nao", "NO000142")
	Mess("D-Don't say that. Haru-chan's just a neighbor, we're notÅ^like that!")
	hitretResult := Hitret(1241)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The man at Takano who had sold most of his stuff wasÅ^starting to pack up and laughed aloud.")
	hitretResult := Hitret(1242)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000143")
	Mess("W-We don't have anything else to do here, so let's go!")
	hitretResult := Hitret(1243)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y-Yeah.")
	hitretResult := Hitret(1244)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("I knew it was just a polite joke by the Oji-san, but IÅ^was a bit embarrassed. For a while, I couldn't look atÅ^Nao-chan directly.")
	hitretResult := Hitret(1245)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM07", 0)
	SetCg("B34b", 400, 0)
	Transition("WIP_LR", 500)
	Talk("êSÇÃê∫", "")
	Mess("We went back onto the road with the shopping bag in oneÅ^hand.")
	hitretResult := Hitret(1246)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh, it's good that that place is so much closer thanÅ^the supermarket.")
	hitretResult := Hitret(1247)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The bag was heavy in itself, but when I think about theÅ^bags from the first days since moving here, this isÅ^nothing.")
	hitretResult := Hitret(1248)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_01M", 0, 0)
	Talk("Nao", "NO000144")
	Mess("It is a shop on a truck after all, so there's bound toÅ^be things it wouldn't have, though.")
	hitretResult := Hitret(1249)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, that can't be helped...if I need to, I'll go toÅ^that supermarket far away.")
	hitretResult := Hitret(1250)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's good exercise once in a while, too...and if I go onÅ^the way home from school, it'll be easier.")
	hitretResult := Hitret(1251)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_02M", 0, 0)
	Talk("Nao", "NO000145")
	Mess("That part of you... Yup, you're definitely a guy.")
	hitretResult := Hitret(1252)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("W-What do you mean by that?")
	hitretResult := Hitret(1253)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000146")
	Mess("Nothing, actually...it's probably good that you're likeÅ^that.")
	hitretResult := Hitret(1254)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B12b", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("After shopping, Nao-chan said \"I'll show you some moreÅ^underhanded tricks!\" and took me to a place to buy rice.")
	hitretResult := Hitret(1255)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After just saying \"The one at Kasugano-sensei's place,\"Å^he understood immediately...")
	hitretResult := Hitret(1256)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Without even haggling, he gave us a price much cheaperÅ^than the supermarket....I was surprised.")
	hitretResult := Hitret(1257)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Wow...is it really okay to buy it at such a low price?")
	hitretResult := Hitret(1258)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_02M", 0, 0)
	Talk("Nao", "NO000147")
	Mess("It's fine. The market price around here is around thatÅ^anyway. And besides, the number of people yourÅ^grandfather helped is huge.")
	hitretResult := Hitret(1259)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's all well and good, but I had nothing to do withÅ^it.")
	hitretResult := Hitret(1260)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000148")
	Mess("The countryside works through duty and kindness~")
	hitretResult := Hitret(1261)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Sure does.")
	hitretResult := Hitret(1262)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan and everyone at school sure treat me well,Å^despite only being slightly related to this place.")
	hitretResult := Hitret(1263)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If things are like this, then me...and Sora should beÅ^able to live here.")
	hitretResult := Hitret(1264)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Of course, we'll have to return the kindness.")
	hitretResult := Hitret(1265)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_01M", 0, 0)
	Talk("Nao", "NO000149")
	Mess("Your grandfather was really strict, but everyone trustedÅ^him completely.")
	hitretResult := Hitret(1266)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Yeah. I'm only here now because of him too.")
	hitretResult := Hitret(1267)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It'd turned a bit serious, but as we returned home, weÅ^smiled and spoke about our memories of my grandfather.")
	hitretResult := Hitret(1268)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B05b", 0, 0)
	Transition("WIP_MOZH", 500)
	Talk("êSÇÃê∫", "")
	Mess("As we walked, we reached Nao-chan's house.")
	hitretResult := Hitret(1269)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_01M", 0, 0)
	Talk("Nao", "NO000150")
	Mess("Well then, I'll get going.")
	hitretResult := Hitret(1270)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, thanks for today. I'm sure the things you taughtÅ^me will be useful.")
	hitretResult := Hitret(1271)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_02M", 0, 0)
	Talk("Nao", "NO000151")
	Mess("Don't worry about it. If you had to say thanks for everyÅ^little thing, there'd be no end. I still have a lot ofÅ^things to teach you, after all!")
	hitretResult := Hitret(1272)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha. I'll look forward to it, but go easy on me.")
	hitretResult := Hitret(1273)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000152")
	Mess("Hehe, see ya then!")
	hitretResult := Hitret(1274)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan stood there waving until I had gotten quiteÅ^far.")
	hitretResult := Hitret(1275)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("However, for some reason, she didn't enter the house,Å^but disappeared somewhere else.")
	hitretResult := Hitret(1276)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("..Huh?")
	hitretResult := Hitret(1277)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Did Nao-chan actually have something to do?")
	hitretResult := Hitret(1278)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If that's the case, then accompanying me was a badÅ^thing...thinking this, I stood there looking towardÅ^where Nao-chan disappeared.")
	hitretResult := Hitret(1279)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_z009")
	Return
	Change("EXIT_SCENARIO")
EndFunc

