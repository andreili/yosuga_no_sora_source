Include"yosuga.ch"

Function sc00_A025C()
	ScenarioEnter()
	SetCg("B17a", 0, 0)
	ScWait(2000, 0)
	Update()
	ScPlaySe("se116", 0)
	SetCg("B19a", 0, 0)
	ScPlayBgm("BGM05", 0)
	Transition("WIP_RL", 500)
	Talk("Haruka", "")
	Mess("I--I'm late!!")
	hitretResult := Hitret(7965)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I jumped into the classroom, the whole class fixedÅ^their eyes on me simultaneously.")
	hitretResult := Hitret(7966)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("TeacherÅsêîäwã≥étÅt", "NP120004")
	Mess("Kasugano is late? That's rare. Here, go sit down.")
	hitretResult := Hitret(7967)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes, sorry...")
	hitretResult := Hitret(7968)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06a", 0, 0)
	Talk("Ryouhei", "RH010365")
	Mess("Yo. You arrived pretty fast.")
	hitretResult := Hitret(7969)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei greeted me from his seat with a smile.")
	hitretResult := Hitret(7970)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Good morning, everyone.")
	hitretResult := Hitret(7971)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010269")
	Mess("Morning. What's up? I thought you were sick orÅ^something.")
	hitretResult := Hitret(7972)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("N--No... I was just simply late...")
	hitretResult := Hitret(7973)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010366")
	Mess("Geeze, Sora-chan wasn't here today either. I was sad,Å^you know?")
	hitretResult := Hitret(7974)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010270")
	Mess("Did Sora-chan come with you?")
	hitretResult := Hitret(7975)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, yeah.")
	hitretResult := Hitret(7976)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010367")
	Mess("Oh, so in other words your sleeping in is synchronized?Å^Err, is that kind of thing even possible?")
	hitretResult := Hitret(7977)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, it's not synchronized, but... occasionally we bothÅ^sleep in late.")
	hitretResult := Hitret(7978)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The questions coming at me from Ryouhei and AmatsumeÅ^were a little bit frightening.")
	hitretResult := Hitret(7979)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Though it was a joke, I thought they would ask me ifÅ^Sora and I did something to be late.")
	hitretResult := Hitret(7980)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While wiping my cold sweat, I could only pretend to beÅ^calm.")
	hitretResult := Hitret(7981)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(7982)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But...")
	hitretResult := Hitret(7983)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Coming to school like this, sitting in my seat duringÅ^class, the sensation of Sora's hands and mouth stillÅ^remained.")
	hitretResult := Hitret(7984)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Looking under my desk, I could see Sora slipping underÅ^it.")
	hitretResult := Hitret(7985)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her loving expression alone made my face blush.")
	hitretResult := Hitret(7986)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Losing track of time, I could feel nothing but theÅ^pleasure Sora gave me.")
	hitretResult := Hitret(7987)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even in the languidness that followed the end of beingÅ^inside Sora's mouth, I still felt good.")
	hitretResult := Hitret(7988)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even with the feeling of going to school, I still lostÅ^an hour.")
	hitretResult := Hitret(7989)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's how much the pleasure seemed to melt my heart.")
	hitretResult := Hitret(7990)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Doing that kind of thing, Sora made me forget all of myÅ^regrets.")
	hitretResult := Hitret(7991)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The words she said as she told me that she would doÅ^anything for me, still reverberated inside my head.")
	hitretResult := Hitret(7992)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Before I know it, the remains of that pleasure will beÅ^replaced by a new desire.")
	hitretResult := Hitret(7993)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I want to... feel Sora more.")
	hitretResult := Hitret(7994)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I want to... become one with Sora more.")
	hitretResult := Hitret(7995)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010368")
	Mess("...So, don't you agree with me?")
	hitretResult := Hitret(7996)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh... Ah, yeah...")
	hitretResult := Hitret(7997)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("Both the lesson and my friends' conversation no longerÅ^entered into my ears.")
	hitretResult := Hitret(7998)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(2000, 0)
	Change("00_a026")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

