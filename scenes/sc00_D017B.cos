Include"yosuga.ch"

Function sc00_D017B()
	ScenarioEnter()
	ScPlayBgm("BGM06", 0)
	SetCg("B08a", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("The place we ended up going, Amatsume's room, was a pureÅ^Japanese-style room.")
	hitretResult := Hitret(28885)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In complete contrast to Amatsume's cheerful personality,Å^the room was quite tame.")
	hitretResult := Hitret(28886)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The mismatch gave it a mysterious quality.")
	hitretResult := Hitret(28887)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK040253")
	Mess("What's wrong, Haru-kun? You've been looking around, isÅ^it really that strange?")
	hitretResult := Hitret(28888)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's my first time here, so I...sorry.")
	hitretResult := Hitret(28889)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040254")
	Mess("I see, that's why. Welcome to my room then, Haru-kun.")
	hitretResult := Hitret(28890)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK040255")
	Mess("And welcome to you too, Sora-chan.")
	hitretResult := Hitret(28891)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR040084")
	Mess("......")
	hitretResult := Hitret(28892)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Since everyone else was here, Sora ended up joining inÅ^too.")
	hitretResult := Hitret(28893)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira brought some barley tea to each of us, despite usÅ^sitting all over the place.")
	hitretResult := Hitret(28894)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01S", 0, 0)
	Talk("Akira", "AK040256")
	Mess("Okay, barely tea and rice crackers! Eat as much as youÅ^wantÅ`")
	hitretResult := Hitret(28895)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_01S", 0, 0)
	Talk("Ryouhei", "RH040169")
	Mess("Oh, I've been waiting! Okay, let's...")
	hitretResult := Hitret(28896)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_01S", 0, 0)
	Talk("Kazuha", "KA040443")
	Mess("Don't start relaxing before we even start.")
	hitretResult := Hitret(28897)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040170")
	Mess("I'm only taking a breather.")
	hitretResult := Hitret(28898)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sprawled over the tatami, holding a... Wait, that's noÅ^textbook, manga...?")
	hitretResult := Hitret(28899)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040171")
	Mess("First I have to heal my body which has been worn out byÅ^the day's lessons...")
	hitretResult := Hitret(28900)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA040444")
	Mess("Akira and Kasugano-kun, here. Okay, sit.")
	hitretResult := Hitret(28901)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Completely ignoring Ryouhei and his excuses, Migiwa-sanÅ^pointed directly opposite her.")
	hitretResult := Hitret(28902)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Is it okay to leave Ryouhei alone?")
	hitretResult := Hitret(28903)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK040257")
	Mess("Ryou-niichan's reading manga... That looks soÅ^relaxing...")
	hitretResult := Hitret(28904)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040445")
	Mess("You two, do you really have time to be worrying aboutÅ^others?")
	hitretResult := Hitret(28905)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I guess...")
	hitretResult := Hitret(28906)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_05M", 0, 0)
	Talk("Akira", "AK040258")
	Mess("Not..")
	hitretResult := Hitret(28907)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040446")
	Mess("So, worry about yourself first. I've explained what's inÅ^the test on the way home, so what don't you understandÅ^in there?")
	hitretResult := Hitret(28908)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As revenge for what happened in the classroom,Å^Migiwa-san continued relentlessly.")
	hitretResult := Hitret(28909)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If she's going to join in, she'd do it thoroughly...Å^That's the vibe I'm getting from her.")
	hitretResult := Hitret(28910)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Looks like she won't accept either of us being sluggishÅ^like Ryouhei.")
	hitretResult := Hitret(28911)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Not like I can complain, since she is teaching me...")
	hitretResult := Hitret(28912)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040447")
	Mess("Start with the math that you're all bad at, even inÅ^class.Å^You okay, Sora-san?")
	hitretResult := Hitret(28913)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR040085")
	Mess("...I don't need any help.")
	hitretResult := Hitret(28914)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora was solving the problems easily, turning one pageÅ^over after the next.")
	hitretResult := Hitret(28915)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC02_12M", 0, 0)
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA040448")
	Mess("You don't talk much, do you?")
	hitretResult := Hitret(28916)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040259")
	Mess("Amazing, you're really smartÅ`")
	hitretResult := Hitret(28917)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040086")
	Mess("...Average.")
	hitretResult := Hitret(28918)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora's always had a weak body, so she reads a lot ofÅ^books instead.")
	hitretResult := Hitret(28919)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH040172")
	Mess("So that's the reason you two are so different despiteÅ^being twins?")
	hitretResult := Hitret(28920)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well...")
	hitretResult := Hitret(28921)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Although we both started from the same place, I can'tÅ^even reach a third of what Sora can do.")
	hitretResult := Hitret(28922)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, our textbooks were different at our last school,Å^so the content was a bit different too.")
	hitretResult := Hitret(28923)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR040087")
	Mess("...Only difference was how they wrote it.")
	hitretResult := Hitret(28924)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040260")
	Mess("Which means that the contents were the same?")
	hitretResult := Hitret(28925)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040088")
	Mess("......")
	hitretResult := Hitret(28926)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	SetBustup("CF03_05M", 0, 0)
	Talk("Ryouhei", "RH040173")
	Mess("So she says, Haruka.")
	hitretResult := Hitret(28927)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("B-But...It looked completely different to me...")
	hitretResult := Hitret(28928)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_02M", 0, 0)
	Talk("Ryouhei", "RH040174")
	Mess("Their intelligence is definitely differentÅ`")
	hitretResult := Hitret(28929)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...It's at times like these I wish that Sora was theÅ^older one.")
	hitretResult := Hitret(28930)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupLeave(7, 100, 0, 1, 500, 1)
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA040449")
	Mess("Can the person who doesn't even try to study please shutÅ^up?")
	hitretResult := Hitret(28931)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040175")
	Mess("Whoa.")
	hitretResult := Hitret(28932)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA040450")
	Mess("Kasugano-kun, I'll explain the things you don'tÅ^understand until you do, so keep working.")
	hitretResult := Hitret(28933)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks, Migiwa-san. You're a great help.")
	hitretResult := Hitret(28934)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA040451")
	Mess("But really... You're so different to the image you giveÅ^out that it's surprising.")
	hitretResult := Hitret(28935)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sorry for not meeting your expectations...")
	hitretResult := Hitret(28936)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA040452")
	Mess("Ah, sorry. I didn't mean it that way...")
	hitretResult := Hitret(28937)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_02M", 0, 0)
	Talk("Ryouhei", "RH040176")
	Mess("She meant, \"Show me some of that coolness that beingÅ^raised in the city gives you~\"")
	hitretResult := Hitret(28938)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh!?")
	hitretResult := Hitret(28939)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_10M", 0, 0)
	Talk("Kazuha", "KA040453")
	Mess("N-No I didn't!! I didn't mean it that way at all!")
	hitretResult := Hitret(28940)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(7)
	SetBustup("CC02_03M", 0, 0)
	Talk("Akira", "AK040261")
	Mess("So, how did you mean to say it?")
	hitretResult := Hitret(28941)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040454")
	Mess("You too...? Grr...")
	hitretResult := Hitret(28942)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("For some reason, Migiwa-san was at a loss for words.")
	hitretResult := Hitret(28943)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Something that hard to say... It's a bit of a shock.")
	hitretResult := Hitret(28944)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	SetBustup("CD02_05M", 0, 0)
	Talk("Kazuha", "KA040455")
	Mess("....A-Anyway, get back to the textbook.")
	hitretResult := Hitret(28945)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040177")
	Mess("Not saying it? That's no fun....")
	hitretResult := Hitret(28946)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040262")
	Mess("What a disappointment...")
	hitretResult := Hitret(28947)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040089")
	Mess("........")
	hitretResult := Hitret(28948)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even Sora is sighing... That's rare.")
	hitretResult := Hitret(28949)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_06M", 0, 0)
	Talk("Kazuha", "KA040456")
	Mess("...Shut up. Akira, come over here. Kasugano-kun, work byÅ^yourself.")
	hitretResult := Hitret(28950)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040263")
	Mess("Fiine.")
	hitretResult := Hitret(28951)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("A-All right...")
	hitretResult := Hitret(28952)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("It's probably because she's beautiful that her scowl isÅ^so strong...")
	hitretResult := Hitret(28953)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It might be best if I obediently studied...")
	hitretResult := Hitret(28954)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...if it weren't for the fact that I don't get any ofÅ^them while looking at it myself.")
	hitretResult := Hitret(28955)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_10M", 0, 0)
	Talk("Kazuha", "KA040457")
	Mess("......")
	hitretResult := Hitret(28956)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Hm?")
	hitretResult := Hitret(28957)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Did she just look this way?")
	hitretResult := Hitret(28958)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_10M", 0, 0)
	Talk("Kazuha", "KA040458")
	Mess("............. ÅiGlanceÅj")
	hitretResult := Hitret(28959)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm?")
	hitretResult := Hitret(28960)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040459")
	Mess("...Ah.")
	hitretResult := Hitret(28961)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Jump(5, 0)
	Update()
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Yup, she's definitely looking.")
	hitretResult := Hitret(28962)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Umm, Migiwa-san...")
	hitretResult := Hitret(28963)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA040460")
	Mess("K-Kasugano-kun, I told you study it yourself, didn't I?")
	hitretResult := Hitret(28964)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y-Yeah, you did.")
	hitretResult := Hitret(28965)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040461")
	Mess("Geez, you're not Nakazato-senpai, so do some studying.")
	hitretResult := Hitret(28966)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("F-Fine, excuse me.")
	hitretResult := Hitret(28967)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Why is she so angry...?")
	hitretResult := Hitret(28968)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(2000, 1)
	Talk("êSÇÃê∫", "")
	Mess("And so, the sun had completely gone down and it was timeÅ^for Akira to go make dinner for Yahiro-san, so the studyÅ^group was suspended.")
	hitretResult := Hitret(28969)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM09", 0)
	SetCg("B34c", 400, 0)
	SetBustup("CA02_01M", 0, 0)
	SetBustup("CD02_01M", 0, 0)
	Talk("Haruka", "")
	Mess("We're this way, see ya.")
	hitretResult := Hitret(28970)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei had left earlier claiming that he was starving,Å^so it was only us and Migiwa-san at the intersection.")
	hitretResult := Hitret(28971)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA040462")
	Mess("Ah...about earlier, forget about it, okay?")
	hitretResult := Hitret(28972)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? ...About what?")
	hitretResult := Hitret(28973)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_10M", 0, 0)
	Talk("Kazuha", "KA040463")
	Mess("Uu...N-Nothing then...See you later.")
	hitretResult := Hitret(28974)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay, bye then. Thanks for today!")
	hitretResult := Hitret(28975)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040090")
	Mess("......")
	hitretResult := Hitret(28976)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(5)
	Talk("êSÇÃê∫", "")
	Mess("As Migiwa-san got further and further away, we startedÅ^making our way home.")
	hitretResult := Hitret(28977)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While I was anxiously wondering if what we did todayÅ^would be enough for the test...")
	hitretResult := Hitret(28978)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("Kazuha", "KA040464")
	Mess("Why is dealing with him so frustrating... What a strangeÅ^person...")
	hitretResult := Hitret(28979)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I think I just heard something like Migiwa-san'sÅ^muttering from very far away.")
	hitretResult := Hitret(28980)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	Change("00_d018")
	Return
	Change("EXIT_SCENARIO")
EndFunc

