Include"yosuga.ch"

Function sc00_D004()
	ScenarioEnter()
	ScPlaySe("SE357", 0)
	ScPlayBgm("BGM05", 0)
	SetCg("B02a", 0, 0)
	Talk("Haruka", "")
	Mess("Soraa! It's almost time to go, so eat your breakfast,Å^okay?")
	hitretResult := Hitret(27063)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Despite calling her countless times, there was no reply.")
	hitretResult := Hitret(27064)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The preperations for breakfast were finished, so theÅ^eggs and toast could only get cold from here on out.")
	hitretResult := Hitret(27065)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But more importantly is the issue of whether we'll beÅ^late or not.")
	hitretResult := Hitret(27066)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't tell me you won't be going! The uniforms haveÅ^arrived.")
	hitretResult := Hitret(27067)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan sewed the buttons on for us yesterday, so allÅ^she has to do is wear it...")
	hitretResult := Hitret(27068)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("She's not still sleeping, is she...")
	hitretResult := Hitret(27069)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But yesterday's email did arrive at such a time...")
	hitretResult := Hitret(27070)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oi! If you don't wake up, we'll seriously be late...")
	hitretResult := Hitret(27071)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just as I was going to knock on the door, the doorÅ^opened.")
	hitretResult := Hitret(27072)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR040001")
	Mess("...What?")
	hitretResult := Hitret(27073)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh, nothing...if you're awake, go eat breakfast.")
	hitretResult := Hitret(27074)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040002")
	Mess("......")
	hitretResult := Hitret(27075)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What?")
	hitretResult := Hitret(27076)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After looking at me closely, Sora let out a small sighÅ^and turned her head away.")
	hitretResult := Hitret(27077)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("And like that, she tried to leave the kitchen.")
	hitretResult := Hitret(27078)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("O-Oi...at least eat breakfast.")
	hitretResult := Hitret(27079)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01S", 0, 0)
	Talk("Sora", "SR040003")
	Mess("No need.")
	hitretResult := Hitret(27080)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You can't do that. It's been a while since you've beenÅ^to school, so if you don't eat properly, you'llÅ^collapse.")
	hitretResult := Hitret(27081)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040004")
	Mess("...Fine.")
	hitretResult := Hitret(27082)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("As if being forced, Sora dropped her shoulders and wentÅ^to get some milk from the fridge.")
	hitretResult := Hitret(27083)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She poured it into the cup that was on the table untilÅ^it was full, and then drank it with small gulpingÅ^noises.")
	hitretResult := Hitret(27084)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR040005")
	Mess("...I'm full.")
	hitretResult := Hitret(27085)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Just that?")
	hitretResult := Hitret(27086)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040006")
	Mess("I don't need any more.")
	hitretResult := Hitret(27087)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("And she then tried to leave the kitchen again.")
	hitretResult := Hitret(27088)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hold on. I haven't eaten yet.")
	hitretResult := Hitret(27089)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_13M", 0, 0)
	Talk("Sora", "SR040007")
	Mess("...What're you doing?")
	hitretResult := Hitret(27090)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't be so surprised, I was waiting for you.")
	hitretResult := Hitret(27091)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_09M", 0, 0)
	Talk("Sora", "SR040008")
	Mess("...Haa...")
	hitretResult := Hitret(27092)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I can't agree with her sighing at me as if I was theÅ^idiot.")
	hitretResult := Hitret(27093)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Dammit...fine, we'll go now. But I'm still going to eatÅ^my breakfast.")
	hitretResult := Hitret(27094)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040009")
	Mess("Do what you want?")
	hitretResult := Hitret(27095)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("At least let me do that.")
	hitretResult := Hitret(27096)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B12a", 0, 0)
	SetBustup("CA01_04M", 0, 0)
	Transition("WIP_LR", 500)
	Talk("Sora", "SR040010")
	Mess("...And, what is this?")
	hitretResult := Hitret(27097)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora looked at me as I was trying to eat a piece ofÅ^toast while walking.")
	hitretResult := Hitret(27098)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040011")
	Mess("...*sigh*")
	hitretResult := Hitret(27099)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That was her third sigh of the day.")
	hitretResult := Hitret(27100)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("*munch* *munch* ...It can't be helped. if I don't eat,Å^I'll get hungry.")
	hitretResult := Hitret(27101)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My toast is topped with a fried egg, so I have to walkÅ^slowly so I don't drop it... but even I think this isÅ^embarrassing.")
	hitretResult := Hitret(27102)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04S", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("I get the feeling that Sora's trying to get away from meÅ^for doing such things.")
	hitretResult := Hitret(27103)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, it's all good...Make sure you remember this routeÅ^because we might not always be together.")
	hitretResult := Hitret(27104)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_05M", 0, 0)
	Talk("Sora", "SR040012")
	Mess("Eh?")
	hitretResult := Hitret(27105)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("We're not in the same class, so we might go home atÅ^different times, and...")
	hitretResult := Hitret(27106)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_08M", 0, 0)
	Talk("Sora", "SR040013")
	Mess("Why...are we not in the same class?")
	hitretResult := Hitret(27107)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You'd have to ask the school that.")
	hitretResult := Hitret(27108)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_06M", 0, 0)
	Talk("Sora", "SR040014")
	Mess("......")
	hitretResult := Hitret(27109)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While looking at me, Sora's eyes twiched with anxiety.")
	hitretResult := Hitret(27110)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But even so, there's nothing I can do.")
	hitretResult := Hitret(27111)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It'll be okay. You'll get used it quickly, get someÅ^friends...")
	hitretResult := Hitret(27112)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040015")
	Mess("......")
	hitretResult := Hitret(27113)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even if that wasn't so, our classrooms are next to eachÅ^other, so you can come over when it's a break...")
	hitretResult := Hitret(27114)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR040016")
	Mess("I'm going home.")
	hitretResult := Hitret(27115)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("Oi, He...y..")
	hitretResult := Hitret(27116)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040017")
	Mess("...Kya!")
	hitretResult := Hitret(27117)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("VoiceÅsìﬁèèÅt", "NO040001")
	Mess("Whaa...ah, Sora-chan?")
	hitretResult := Hitret(27118)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As Sora tried to leave, she knocked into someone behindÅ^her.")
	hitretResult := Hitret(27119)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01S", 0, 0)
	SetBustup("CB01_01S", 0, 0)
	Talk("Sora", "SR040018")
	Mess("ÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(27120)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan, perfect timing! Stop Sora from getting away!")
	hitretResult := Hitret(27121)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO040002")
	Mess("Eh? Ah, okay, but...did something happen?")
	hitretResult := Hitret(27122)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan grabbed Sora's arm.")
	hitretResult := Hitret(27123)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04S", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Sora looked unhappy, but there was no way she could haveÅ^gone home.")
	hitretResult := Hitret(27124)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks, Nao-chan. Oh, and good morning.")
	hitretResult := Hitret(27125)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO040003")
	Mess("Eh, Ahhh...Good morning Haru-chan, S...")
	hitretResult := Hitret(27126)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("VoiceÅsó∫ïΩÅt", "RH040025")
	Mess("Whoa!?")
	hitretResult := Hitret(27127)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_13S", 0, 0)
	Talk("Nao", "NO040004")
	Mess("Eh?")
	hitretResult := Hitret(27128)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF01_06M", 0, 0)
	Talk("Ryouhei", "RH040026")
	Mess("Holy shit...Is this a fated reunion!?")
	hitretResult := Hitret(27129)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ryo-Ryouhei...?")
	hitretResult := Hitret(27130)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Where'd he come from...?")
	hitretResult := Hitret(27131)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_06M", 3, 0)
	SetBustup("CF01_04M", 6, 0)
	SetBustup("CA01_04M", 9, 0)
	Talk("êSÇÃê∫", "")
	Mess("As we stood dumbfounded, Ryouhei crossed over to SoraÅ^and Nao-chan.")
	hitretResult := Hitret(27132)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if only having Sora in his eyes, he winked and bowedÅ^with his right hand, showing his index and middleÅ^fingers.")
	hitretResult := Hitret(27133)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	SetBustup("CB01_06M", 4, 0)
	SetBustup("CF01_04M", 8, 0)
	Talk("Sora", "SR040019")
	Mess("...Who?")
	hitretResult := Hitret(27134)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Honestly, it was disgusting. It seems that even SoraÅ^felt the same way.")
	hitretResult := Hitret(27135)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While asking, she hid behind me.")
	hitretResult := Hitret(27136)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ryouhei, a classmate. Mine though, not yours.")
	hitretResult := Hitret(27137)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040020")
	Mess("...Gross.")
	hitretResult := Hitret(27138)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO040005")
	Mess("...Really, what are you trying to do...?")
	hitretResult := Hitret(27139)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH040027")
	Mess("Hahaha! Both of you are so cold! If you know her, youÅ^should say so!")
	hitretResult := Hitret(27140)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nah, she's my sister.")
	hitretResult := Hitret(27141)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_06M", 0, 0)
	Talk("Ryouhei", "RH040028")
	Mess("Wha!? S-Sister!?")
	hitretResult := Hitret(27142)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Update()
	SetBustup("CF01_01L", 0, 0)
	Talk("Ryouhei", "RH040029")
	Mess("Onii-sama, good morning.")
	hitretResult := Hitret(27143)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What're you doing...with your face so close...?")
	hitretResult := Hitret(27144)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_04M", 0, 0)
	Talk("Ryouhei", "RH040030")
	Mess("I am Nakazato Ryouhei. One of your Onii-sama'sÅ^classmates. Pleased to make your acquaintance.")
	hitretResult := Hitret(27145)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO040006")
	Mess("If you're going to introduce yourself, at least do itÅ^normally. Sorry, Sora-chan. He's a weird thing, butÅ^that's because he's weird from the roots.")
	hitretResult := Hitret(27146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040031")
	Mess("Hoho, So you're "Sora-chan." That is a wonderfully cuteÅ^name as well. I look forward to working with you.")
	hitretResult := Hitret(27147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Are you still acting like that?")
	hitretResult := Hitret(27148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_08M", 0, 0)
	Talk("Ryouhei", "RH040032")
	Mess("Shhh, I'm trying to be the cool guy, so shush.")
	hitretResult := Hitret(27149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO040007")
	Mess("How is that a cool guy? Haru-chan, Sora-chan, let's goÅ^before we're infected with idiocy.")
	hitretResult := Hitret(27150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_09M", 0, 0)
	Talk("Ryouhei", "RH040033")
	Mess("Ah, hey, both of them are my classmates! Senpai, keepÅ^your hands off of them!")
	hitretResult := Hitret(27151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO040008")
	Mess("Yeah, yeah, if you hang around him, then you'll be late.Å^So let's go, Sora-chan.")
	hitretResult := Hitret(27152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040021")
	Mess("......")
	hitretResult := Hitret(27153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	Talk("Ryouhei", "RH040034")
	Mess("Geez, playing the older sister...")
	hitretResult := Hitret(27154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You seem quite close to Nao-chan, huh?")
	hitretResult := Hitret(27155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH040035")
	Mess("Well, I guess. We were classmates last year, after all.")
	hitretResult := Hitret(27156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040036")
	Mess("But, she's...become really happy, huh? After meeting youÅ^two.")
	hitretResult := Hitret(27157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm?")
	hitretResult := Hitret(27158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Akira", "AK040034")
	Mess("Ah, morning! Haru-kun!!")
	hitretResult := Hitret(27159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	SetBustup("CD01_01M", 0, 0)
	Talk("Haruka", "")
	Mess("Morning, Amatsume, Migiwa-san. You're a bit later thanÅ^yesterday, aren't you?")
	hitretResult := Hitret(27160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_05M", 0, 0)
	Talk("Kazuha", "KA040062")
	Mess("...Keh.")
	hitretResult := Hitret(27161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040035")
	Mess("Yeah, Kazu-chan overslept today, so just a bitÅ`")
	hitretResult := Hitret(27162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA040063")
	Mess("W-Wait a minute, Akira!")
	hitretResult := Hitret(27163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-Is that so...?")
	hitretResult := Hitret(27164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_10M", 0, 0)
	Talk("Kazuha", "KA040064")
	Mess("G-Good Morning...")
	hitretResult := Hitret(27165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040036")
	Mess("It sure is lively today.")
	hitretResult := Hitret(27166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_04M", 0, 0)
	Talk("Kazuha", "KA040065")
	Mess("Rather than lively, it's just noisy...")
	hitretResult := Hitret(27167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("From today, Sora...my sister is going to school with me.")
	hitretResult := Hitret(27168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK040037")
	Mess("Ah, that's right. Your uniforms just came, right?")
	hitretResult := Hitret(27169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA040066")
	Mess("...It suits you. I think you looked cool in your clothesÅ^before, but you do now too.")
	hitretResult := Hitret(27170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh?")
	hitretResult := Hitret(27171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040067")
	Mess("Why are you so surprised?")
	hitretResult := Hitret(27172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, nah...I just didn't think I'd get any praise fromÅ^you...")
	hitretResult := Hitret(27173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_10M", 0, 0)
	Talk("Kazuha", "KA040068")
	Mess("E-Even I can tell the difference between good and badÅ^things!")
	hitretResult := Hitret(27174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("O-Oh yeah!")
	hitretResult := Hitret(27175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san's beautiful, so it feels hard to get close toÅ^her...")
	hitretResult := Hitret(27176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She is a genuine Ojou-sama, after all...")
	hitretResult := Hitret(27177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, that's right. Soraa!! Can you come over here for aÅ^sec?")
	hitretResult := Hitret(27178)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	SetBustup("CD01_02M", 0, 0)
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR040022")
	Mess("...What?")
	hitretResult := Hitret(27179)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'll introduce her. This is my sister, Sora.")
	hitretResult := Hitret(27180)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_04M", 0, 0)
	Talk("Akira", "AK040038")
	Mess("Pleased to meet youÅ`! I'm Akira, Amatsume Akira.")
	hitretResult := Hitret(27181)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040069")
	Mess("I'm Migiwa Kazuha.")
	hitretResult := Hitret(27182)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040039")
	Mess("Long time no see! Do you remember me? Sora-chan?")
	hitretResult := Hitret(27183)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_12M", 0, 0)
	Talk("Sora", "SR040023")
	Mess("...eh?")
	hitretResult := Hitret(27184)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(5)
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH040037")
	Mess("What's this? That string of fate again?")
	hitretResult := Hitret(27185)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK040040")
	Mess("Ahh, when you got lost once, I only took you back toÅ^your house, so you've probably forgotten, huh?")
	hitretResult := Hitret(27186)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR040024")
	Mess("...I don't remember.")
	hitretResult := Hitret(27187)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040041")
	Mess("Ahaaaa...Well, pleased to meet you again then.")
	hitretResult := Hitret(27188)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040025")
	Mess("....mm...")
	hitretResult := Hitret(27189)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nodding deeply once, Sora tried to get away in order toÅ^put some distance between her and Ryouhei.")
	hitretResult := Hitret(27190)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	Talk("Ryouhei", "RH040038")
	Mess("Akira, your memory is shockingly good.")
	hitretResult := Hitret(27191)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040042")
	Mess("Hmm... She looks exactly like Haru-kun, so maybe that'sÅ^why I remember.")
	hitretResult := Hitret(27192)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO040009")
	Mess("She remembers all your wrongdoings, in other words.")
	hitretResult := Hitret(27193)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_06M", 0, 0)
	Talk("Ryouhei", "RH040039")
	Mess("S-Seriously?")
	hitretResult := Hitret(27194)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_14M", 0, 0)
	Talk("Akira", "AK040043")
	Mess("Hmm... For example, In the plastic greenhouse,Å^Ryou-niichan...")
	hitretResult := Hitret(27195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	BustupClear(2)
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA040070")
	Mess("Akira, you can forget all about people like that.")
	hitretResult := Hitret(27196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH040040")
	Mess("Haha, real men don't need to look back onto the past,Å^after all.")
	hitretResult := Hitret(27197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040071")
	Mess("Hmph... It'd be a lot better if you did look back onÅ^your past.")
	hitretResult := Hitret(27198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040041")
	Mess("Ojou, you shouldn't imprison yourself in the past. TimeÅ^is something that only goes forward.")
	hitretResult := Hitret(27199)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040072")
	Mess("...Grr...")
	hitretResult := Hitret(27200)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei was probably trying to use a cool line, but itÅ^seems he's getting her irritated instead...")
	hitretResult := Hitret(27201)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040042")
	Mess("There's no need to be irritated about sleeping in, evenÅ^I'm constantly...")
	hitretResult := Hitret(27202)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED02a", 0, 0)
	ScPlayBgm("BGM14", 1)
	Update()
	ScPlaySe("se006", 0)
	Quake(16, 0, 1, 2, 30)
	Talk("Kazuha", "KA040073")
	Mess("Shut up!!!")
	hitretResult := Hitret(27203)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040043")
	Mess("Umph!")
	hitretResult := Hitret(27204)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040074")
	Mess("You... Always making a pass at everything! Just shut it!")
	hitretResult := Hitret(27205)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se006", 0)
	Quake(16, 0, 1, 1, 15)
	Talk("Ryouhei", "RH040044")
	Mess("Umph! Gwaa!")
	hitretResult := Hitret(27206)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B12a", 0, 0)
	ScPlaySe("SE204", 0)
	SetBustup("CD01_06M", 0, 0)
	Talk("Kazuha", "KA040075")
	Mess("Hah, hah, hah....")
	hitretResult := Hitret(27207)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("M-Migiwa-san...")
	hitretResult := Hitret(27208)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After yelling so loud that I thought her face had turnedÅ^completely red, Migiwa-san kicked Ryouhei down into aÅ^paddy field.")
	hitretResult := Hitret(27209)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her tone always went violent, and because she swung herÅ^leg as hard as she could, umm...")
	hitretResult := Hitret(27210)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I think I could just see her panties underneath... butÅ^if I told her that, I'd get kicked too, so I stayedÅ^quiet...")
	hitretResult := Hitret(27211)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040076")
	Mess("...*Gasp* !?")
	hitretResult := Hitret(27212)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA040077")
	Mess("S-Sorry. I'm sorry, showing you something so immodest.")
	hitretResult := Hitret(27213)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh, no... That's okay.")
	hitretResult := Hitret(27214)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was calmly said, but on the other hand, it was justÅ^as terrifying.")
	hitretResult := Hitret(27215)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("You wouldn't really say that Migiwa-san is just anÅ^Ojou-sama...")
	hitretResult := Hitret(27216)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The attack in the classroom yesterday was also ratherÅ^violent...")
	hitretResult := Hitret(27217)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK040044")
	Mess("Kazu-chan, you really don't have to hide your realÅ^personality all the time, you know...")
	hitretResult := Hitret(27218)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_10M", 0, 0)
	Talk("Kazuha", "KA040078")
	Mess("I-I'm not hiding anything!")
	hitretResult := Hitret(27219)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha, Ahaha...")
	hitretResult := Hitret(27220)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just which is the real Migiwa-san...?")
	hitretResult := Hitret(27221)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I think that, but there's no way I can actually ask her.")
	hitretResult := Hitret(27222)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("More importantly, is Ryouhei all right? I think he fellÅ^into a paddy field, but...")
	hitretResult := Hitret(27223)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	SetCg("B12a", 0, 0)
	Transition("WIP_BT", 500)
	Talk("êSÇÃê∫", "")
	Mess("Between the rice plants in the paddy farm, two legsÅ^growing out of the mud were twitching.")
	hitretResult := Hitret(27224)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I think he's alive, but...")
	hitretResult := Hitret(27225)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	SetBustup("CD01_01M", 0, 0)
	SetBustup("CB01_01M", 0, 0)
	Talk("Akira", "AK040045")
	Mess("He's all rightÅ` It happens all the time, after all.")
	hitretResult := Hitret(27226)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040079")
	Mess("Don't worry about that person.")
	hitretResult := Hitret(27227)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO040010")
	Mess("Well, he deserved it, so leave him there.")
	hitretResult := Hitret(27228)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("R-Really...?")
	hitretResult := Hitret(27229)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Is it really okay...?")
	hitretResult := Hitret(27230)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	ScPlaySe("SE201", 0)
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH040045")
	Mess("DAAAHAHAHAHA!!! I am revived!!!")
	hitretResult := Hitret(27231)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah.")
	hitretResult := Hitret(27232)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_04M", 0, 0)
	Talk("Ryouhei", "RH040046")
	Mess("Heh...It's been a while since I last took one of those.Å^And the Ojou's got a lot of fight as usual.")
	hitretResult := Hitret(27233)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei was showing a bright smile.")
	hitretResult := Hitret(27234)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Obviously, he was dripping wet and full of mud.")
	hitretResult := Hitret(27235)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If it's only affected him that much, there must beÅ^nothing wrong with him physically.")
	hitretResult := Hitret(27236)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA040080")
	Mess("Don't talk about people as if they were violent.")
	hitretResult := Hitret(27237)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040081")
	Mess("You'll cause misunderstandings!")
	hitretResult := Hitret(27238)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040047")
	Mess("Ahh, The Ojou not only packs kicks. She can punch too!")
	hitretResult := Hitret(27239)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_06M", 0, 0)
	Talk("Kazuha", "KA040082")
	Mess("I'm NOT talking about that!")
	hitretResult := Hitret(27240)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(5)
	Talk("êSÇÃê∫", "")
	Mess("After raising her voice for a moment, Migiwa-san squaredÅ^her shoulders and left.")
	hitretResult := Hitret(27241)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ryouhei, did you purposely piss her off?")
	hitretResult := Hitret(27242)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH040048")
	Mess("No no, I love the Ojou, you know?Å^Okay Akira! Let's calm down the Ojou together!")
	hitretResult := Hitret(27243)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK040046")
	Mess("Okaay! Kazu-chan, wait for me!!")
	hitretResult := Hitret(27244)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH040049")
	Mess("Wait up, Kazu-chaaaan!!")
	hitretResult := Hitret(27245)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Kazuha", "KA040083")
	Mess("W-What do you want? Get away from me!Å^A-Akira, you hurry up.")
	hitretResult := Hitret(27246)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040050")
	Mess("It's okay, Haruka will make sure to back us up!")
	hitretResult := Hitret(27247)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040084")
	Mess("I can't trust you to back up anything!")
	hitretResult := Hitret(27248)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040047")
	Mess("I'll back up tooooo!")
	hitretResult := Hitret(27249)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040085")
	Mess("If you want to do anything, do it when Nakazato-senpai'sÅ^not around...")
	hitretResult := Hitret(27250)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...They're lively....")
	hitretResult := Hitret(27251)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO040011")
	Mess("Like I said, right? It happens all the time.")
	hitretResult := Hitret(27252)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It would seem so.")
	hitretResult := Hitret(27253)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_07M", 0, 0)
	Talk("Sora", "SR040026")
	Mess("...I'm tired.")
	hitretResult := Hitret(27254)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'll say...but today's just started.")
	hitretResult := Hitret(27255)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040027")
	Mess("...*Sigh*")
	hitretResult := Hitret(27256)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO040012")
	Mess("Ahahah, If only every day was as fun as this.")
	hitretResult := Hitret(27257)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040028")
	Mess("No way.")
	hitretResult := Hitret(27258)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahahaha...")
	hitretResult := Hitret(27259)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm not sure if it's good or bad, but it's definitely aÅ^bit weird.")
	hitretResult := Hitret(27260)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_d005")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

