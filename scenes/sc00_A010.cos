Include"yosuga.ch"

Function sc00_A010()
	ScenarioEnter()
	Talk("êSÇÃê∫", "")
	Mess("Then...")
	hitretResult := Hitret(3715)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM15", 0)
	SetCg("B19a", 0, 0)
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH010093")
	Mess("Ahahahaha!")
	hitretResult := Hitret(3716)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't laugh... I know it more than anyone.")
	hitretResult := Hitret(3717)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR010325")
	Mess("Haru... Let's go over there...")
	hitretResult := Hitret(3718)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010094")
	Mess("Sorry, I said sorry... Look, I'll give you some of mine.")
	hitretResult := Hitret(3719)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK010091")
	Mess("But it looks really good. Sora-chan, can I have one?")
	hitretResult := Hitret(3720)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_11M", 0, 0)
	Talk("Sora", "SR010326")
	Mess("...S-Sure, but...")
	hitretResult := Hitret(3721)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH010095")
	Mess("Oh, me too, me too! I'll trade you some of mine.")
	hitretResult := Hitret(3722)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR010327")
	Mess("...No way.")
	hitretResult := Hitret(3723)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_10M", 0, 0)
	Talk("Ryouhei", "RH010096")
	Mess("Eeeh? Please, Sora-chan?")
	hitretResult := Hitret(3724)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If you don't mind mine, I'll trade you.")
	hitretResult := Hitret(3725)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH010097")
	Mess("Tch... I guess I have to. Sora-chan, could you just grabÅ^it once?")
	hitretResult := Hitret(3726)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_06M", 0, 0)
	Talk("Sora", "SR010328")
	Mess("...No.")
	hitretResult := Hitret(3727)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Knock it off.")
	hitretResult := Hitret(3728)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH010098")
	Mess("Fine. But if Sora-chan ever makes you a lunchbox, youÅ^have to trade me the whole thing, all right?")
	hitretResult := Hitret(3729)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't get your hopes up about the taste.")
	hitretResult := Hitret(3730)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR010329")
	Mess("...Haru!")
	hitretResult := Hitret(3731)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sorry, sorry...")
	hitretResult := Hitret(3732)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_03M", 0, 0)
	Talk("Akira", "AK010092")
	Mess("Aha~, Sora-chan, your rice balls are really good. ButÅ^putting sesame seeds on is kind of weird.")
	hitretResult := Hitret(3733)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, that's because my mom used to make them like thatÅ^all the time.")
	hitretResult := Hitret(3734)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK010093")
	Mess("I see... So this is your mom's flavor, huh. Maybe IÅ^should try doing it like this too.")
	hitretResult := Hitret(3735)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm, I don't think mine are nearly as good as hers,Å^though. I just shaped the rice and sprinkled a fewÅ^sesame seeds on.")
	hitretResult := Hitret(3736)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010094")
	Mess("Hmm, hmm, I see.")
	hitretResult := Hitret(3737)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_05M", 0, 0)
	Talk("Ryouhei", "RH010099")
	Mess("You actually look kind of cool when you say that, sinceÅ^you don't look like the type who would be into cooking.Å^Damn it, I'm jealous.")
	hitretResult := Hitret(3738)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010095")
	Mess("Ryou-niichan said he was a joker, a smoker, and aÅ^midnight toker.")
	hitretResult := Hitret(3739)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH010100")
	Mess("Well yeah, but maybe I need some kind of unexpectedÅ^talent, or something like that.")
	hitretResult := Hitret(3740)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Like I said, I'm not that good at cooking.")
	hitretResult := Hitret(3741)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I traded a rice ball or two away to each of them and ateÅ^a few myself. They disappeared in an instant.")
	hitretResult := Hitret(3742)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("Well, I guess I should wash it out now.")
	hitretResult := Hitret(3743)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR010330")
	Mess("...Haru... I'll do that.")
	hitretResult := Hitret(3744)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, sure.")
	hitretResult := Hitret(3745)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH010101")
	Mess("Oh, you're so kind, Sora-chan. All right! I'll go washÅ^mine too!")
	hitretResult := Hitret(3746)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You better hope she doesn't spray you with the water.")
	hitretResult := Hitret(3747)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010102")
	Mess("Huh? Sora-chan would never do that!")
	hitretResult := Hitret(3748)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(7)
	Talk("Ryouhei", "RH010103")
	Mess("Sora-chan! Let's go wash our lunchboxes togeth--Aabwaa!?")
	hitretResult := Hitret(3749)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_04M", 0, 0)
	Talk("Kazuha", "KA010029")
	Mess("...She really did.")
	hitretResult := Hitret(3750)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	SetBustup("CD01_02M", 0, 0)
	Talk("Akira", "AK010096")
	Mess("Ahaha, Sora-chan's funny.")
	hitretResult := Hitret(3751)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Hey, Amatsume.")
	hitretResult := Hitret(3752)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010097")
	Mess("Hmm, what~?")
	hitretResult := Hitret(3753)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Um... Did you say something to Sora earlier?")
	hitretResult := Hitret(3754)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010098")
	Mess("Earlier...? Oh, no, I didn't. I just drank my juice.")
	hitretResult := Hitret(3755)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, okay...")
	hitretResult := Hitret(3756)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK010099")
	Mess("...Haru-kun, I hope you keep discovering all the goodÅ^things about Sora-chan.")
	hitretResult := Hitret(3757)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Eh?")
	hitretResult := Hitret(3758)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010100")
	Mess("All right! I'm gonna go wash mine too! Kazu-chan, let'sÅ^go!")
	hitretResult := Hitret(3759)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 7, 0)
	SetBustup("CD01_12M", 9, 0)
	Talk("Kazuha", "KA010030")
	Mess("Eh? I-I don't really... Aaah!")
	hitretResult := Hitret(3760)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Amatsume dragged Migiwa-san out of the classroom.")
	hitretResult := Hitret(3761)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...")
	hitretResult := Hitret(3762)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was left alone in the classroom. Amatsume's words fromÅ^before replayed in my mind.")
	hitretResult := Hitret(3763)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Something really did happen... when Sora and AmatsumeÅ^were together.")
	hitretResult := Hitret(3764)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But still...")
	hitretResult := Hitret(3765)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Good things about Sora, huh...")
	hitretResult := Hitret(3766)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Those casual words actually had a note of encouragementÅ^hidden away in them.")
	hitretResult := Hitret(3767)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She said \"keep discovering,\" meaning there are stillÅ^more things to discover, and I just can't see them rightÅ^now... That's the only thing I can get out of it.")
	hitretResult := Hitret(3768)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Of course, Sora is my precious little sister, so I knowÅ^there are plenty of good things about her... Or IÅ^thought I knew that.")
	hitretResult := Hitret(3769)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But...")
	hitretResult := Hitret(3770)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Maybe I wasn't paying attention to her...")
	hitretResult := Hitret(3771)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("The rest of them were all laughing over by the sink, butÅ^I felt a certain gloom in my own heart.")
	hitretResult := Hitret(3772)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(2000, 0)
	Change("00_a011")
	Return
	Change("EXIT_SCENARIO")
EndFunc

