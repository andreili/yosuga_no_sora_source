Include"yosuga.ch"

Function sc00_A015B()
	ScenarioEnter()
	SetCg("B07a", 0, 0)
	ScPlayBgm("BGM03", 0)
	Talk("Haruka", "")
	Mess("*sigh*...")
	hitretResult := Hitret(4881)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I got back to the shrine, there were way moreÅ^people around.")
	hitretResult := Hitret(4882)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was a lot more crowded, though it was finallyÅ^starting to feel like a festival.")
	hitretResult := Hitret(4883)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Once Amatsume gets back, things will really get going.")
	hitretResult := Hitret(4884)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But still, my legs are shaking...")
	hitretResult := Hitret(4885)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I've heard people say it's harder going down than up,Å^but now I believe them...")
	hitretResult := Hitret(4886)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I had to carry that heavy package up, and my legs wereÅ^tired when I went down...")
	hitretResult := Hitret(4887)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I just want to take a break...")
	hitretResult := Hitret(4888)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I sat down on the stone steps...")
	hitretResult := Hitret(4889)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_02M", 0, 0)
	Talk("Class Rep", "KO010012")
	Mess("Oh... Kasugano-kun?")
	hitretResult := Hitret(4890)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, class rep... Did you come for the festival?")
	hitretResult := Hitret(4891)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_07M", 0, 0)
	Talk("Class Rep", "KO010013")
	Mess("N-No, I came to help, and then I heard from Nakazato-sanÅ^that you were here too...")
	hitretResult := Hitret(4892)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, so you're helping out too. Wait, where'd Ryouhei go?")
	hitretResult := Hitret(4893)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't think I've seen him since before I went to theÅ^lake. Where'd he go?")
	hitretResult := Hitret(4894)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_02M", 0, 0)
	Talk("Class Rep", "KO010014")
	Mess("If you mean Nakazato-san, he's over by the palanquin,Å^getting ready... Aren't you going to be one of theÅ^bearers?")
	hitretResult := Hitret(4895)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, uh, no. I'm a bit tired right now, and... I'm notÅ^sure if I should do that, since I just came to thisÅ^town.")
	hitretResult := Hitret(4896)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_08M", 0, 0)
	Talk("Class Rep", "KO010015")
	Mess("Tired? Are you okay? Was your job that bad?")
	hitretResult := Hitret(4897)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, no. I just went up to the lake for a bit.")
	hitretResult := Hitret(4898)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_05M", 0, 0)
	Jump(9, 0)
	Talk("Class Rep", "KO010016")
	Mess("Ehh!? All the way up there? That must have beenÅ^incredibly hard...")
	hitretResult := Hitret(4899)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahaha... I don't think I'd ever want to do it again.")
	hitretResult := Hitret(4900)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_02M", 0, 0)
	Talk("Class Rep", "KO010017")
	Mess("I know what you mean... I tried to go there once, but IÅ^gave up partway through. But why did you go to theÅ^lake...?")
	hitretResult := Hitret(4901)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, Migiwa-san asked me to. She wanted me to deliverÅ^Amatsume's clothes to her, since she forgot them.")
	hitretResult := Hitret(4902)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_08M", 0, 0)
	Talk("Class Rep", "KO010018")
	Mess("Oh, I see... Wait, did you watch the purificationÅ^ceremony!?")
	hitretResult := Hitret(4903)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? N-No, no way! I'm a guy, there's no way I could doÅ^that... besides, Sora stayed behind to help her.")
	hitretResult := Hitret(4904)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_09M", 0, 0)
	Talk("Class Rep", "KO010019")
	Mess("Oh, I'm so glad to hear that...")
	hitretResult := Hitret(4905)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...?")
	hitretResult := Hitret(4906)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Why does she look so relieved?")
	hitretResult := Hitret(4907)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, anyway, that's why I'm taking a break, but... NowÅ^that I think of it, I still haven't eaten lunch...")
	hitretResult := Hitret(4908)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_02M", 0, 0)
	Talk("Class Rep", "KO010020")
	Mess("Eh, you still haven't eaten? Well, I guess you just gotÅ^back, so that's to be expected.")
	hitretResult := Hitret(4909)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah. Ryouhei said that the lunchboxes they have for theÅ^festival were the best, so I was looking forward to it.")
	hitretResult := Hitret(4910)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010021")
	Mess("Oh, he's right. There are even some people who come toÅ^help just so they could get one.")
	hitretResult := Hitret(4911)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see... Are there any left?")
	hitretResult := Hitret(4912)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's not really lunchtime anymore... Everyone hasÅ^probably already eaten and moved on to their nextÅ^jobs...")
	hitretResult := Hitret(4913)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010022")
	Mess("Don't worry, there's one for everyone who helped out...Å^I'll go get yours.")
	hitretResult := Hitret(4914)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, it's fine. I'll go get it myself.")
	hitretResult := Hitret(4915)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010023")
	Mess("N-No, don't worry about it. You should just relax...")
	hitretResult := Hitret(4916)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Before I could say anything else, she turned and trottedÅ^off toward the shrine.")
	hitretResult := Hitret(4917)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt kind of bad for making her do that...")
	hitretResult := Hitret(4918)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Besides...")
	hitretResult := Hitret(4919)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't think I've ever heard the class rep talk thatÅ^much before.")
	hitretResult := Hitret(4920)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei and the others always cut her off...")
	hitretResult := Hitret(4921)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_02M", 0, 0)
	Talk("Class Rep", "KO010024")
	Mess("Sorry to keep you waiting. There were still some left.")
	hitretResult := Hitret(4922)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Th-Thank you.")
	hitretResult := Hitret(4923)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The class rep came back.")
	hitretResult := Hitret(4924)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The lunchbox in her hand was wrapped in expensive paper.")
	hitretResult := Hitret(4925)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_03M", 0, 0)
	Talk("Class Rep", "KO010025")
	Mess("Here you go.")
	hitretResult := Hitret(4926)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Whoa, this is amazing...")
	hitretResult := Hitret(4927)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I took it from her, it was surprisingly heavy. WhenÅ^I opened it, it was stuffed full of colorful foods.")
	hitretResult := Hitret(4928)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_02M", 0, 0)
	Talk("Class Rep", "KO010026")
	Mess("This really is amazing.")
	hitretResult := Hitret(4929)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I feel kind of bad about accepting it for free...")
	hitretResult := Hitret(4930)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010027")
	Mess("Hehe, I don't think you'll get another chance to eatÅ^something like that if you don't take this opportunity.")
	hitretResult := Hitret(4931)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Really?")
	hitretResult := Hitret(4932)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_03M", 0, 0)
	Talk("Class Rep", "KO010028")
	Mess("Yeah, if you wanted to buy something like that... youÅ^would have to say goodbye to Benjamin Franklin.")
	hitretResult := Hitret(4933)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eeh!?")
	hitretResult := Hitret(4934)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My chopsticks froze in place.")
	hitretResult := Hitret(4935)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010029")
	Mess("It's really good, though. I've heard of people becomingÅ^regular customers just because they had one.")
	hitretResult := Hitret(4936)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eeh!? A regular customer at that price?")
	hitretResult := Hitret(4937)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010030")
	Mess("Shouldn't you get eating? There's still a lot of work toÅ^be done...")
	hitretResult := Hitret(4938)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, that's right. Okay, then... Wait, what about you? DoÅ^you have anywhere else you need to be?")
	hitretResult := Hitret(4939)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_05M", 0, 0)
	Talk("Class Rep", "KO010031")
	Mess("Eh? Ah-... I-I'm actually, uh, on b-break too, so...")
	hitretResult := Hitret(4940)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_07M", 0, 0)
	Talk("Haruka", "")
	Mess("Oh, okay. Otherwise you wouldn't be chatting here, huh?")
	hitretResult := Hitret(4941)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, and... We're classmates, so you don't really need toÅ^use keigo around me.")
	hitretResult := Hitret(4942)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If you use keigo, I feel kind of distant from you...Å^It's like there's a wall between us.")
	hitretResult := Hitret(4943)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_06M", 0, 0)
	Talk("Class Rep", "KO010032")
	Mess("Eh... Oh, I-I'm sorry...")
	hitretResult := Hitret(4944)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, uh, you don't need to apologize...")
	hitretResult := Hitret(4945)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_09M", 0, 0)
	Talk("Class Rep", "KO010033")
	Mess("I'm always like this... I'd like to be able to talk moreÅ^freely, but it's just my personality, so...")
	hitretResult := Hitret(4946)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her expression clouded.")
	hitretResult := Hitret(4947)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh... I'm sorry, I guess some people are just like that.")
	hitretResult := Hitret(4948)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...It would be really creepy if Sora or AmatsumeÅ^suddenly started talking politely, though.")
	hitretResult := Hitret(4949)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You don't have to push yourself. Just be the same classÅ^rep as always. It's not a big deal.")
	hitretResult := Hitret(4950)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_02M", 0, 0)
	Talk("Class Rep", "KO010034")
	Mess("Y-Yeah... Thank you... U-Um, Kasugano-kun?")
	hitretResult := Hitret(4951)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm, what?")
	hitretResult := Hitret(4952)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_07M", 0, 0)
	Talk("Class Rep", "KO010035")
	Mess("U-Um... W-Would you mind if I stayed with you for aÅ^while...?")
	hitretResult := Hitret(4953)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, sure. Of course. I hope you don't mind me eatingÅ^while we talk, though.")
	hitretResult := Hitret(4954)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010036")
	Mess("Th-Thank you...")
	hitretResult := Hitret(4955)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She reservedly sat next to me.")
	hitretResult := Hitret(4956)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_02L", 0, 0)
	Talk("Class Rep", "KO010037")
	Mess("...")
	hitretResult := Hitret(4957)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She took short glances at me, avoiding eye contact... ItÅ^was kind of hard to focus on my lunch.")
	hitretResult := Hitret(4958)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010038")
	Mess("...H-Hey, Kasugano-kun?")
	hitretResult := Hitret(4959)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But I guess she couldn't handle the silence, so she wasÅ^the first one to speak.")
	hitretResult := Hitret(4960)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm, what?")
	hitretResult := Hitret(4961)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010039")
	Mess("H-How are you getting along? Are you used to life inÅ^this town yet?")
	hitretResult := Hitret(4962)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, yeah. I guess I've gotten used to it by now.")
	hitretResult := Hitret(4963)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010040")
	Mess("Isn't it inconvenient out here in the country comparedÅ^to the city?")
	hitretResult := Hitret(4964)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha... That's what I thought at first too, but... now IÅ^think there might be plenty to do out here.")
	hitretResult := Hitret(4965)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_09L", 0, 0)
	Talk("Class Rep", "KO010041")
	Mess("Really?")
	hitretResult := Hitret(4966)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah. When I was in the city, it was definitelyÅ^convenient since everything was nearby, but... It wasÅ^almost too convenient, and after a while you just stopÅ^thinking about much at all.")
	hitretResult := Hitret(4967)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_08L", 0, 0)
	Talk("Class Rep", "KO010042")
	Mess("...What do you mean?")
	hitretResult := Hitret(4968)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("For instance, if you need to go shopping, you don'tÅ^worry about forgetting to stop by on the way home, sinceÅ^there's a convenience store on the corner...")
	hitretResult := Hitret(4969)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("And cell phones work everywhere, so you don't have toÅ^worry so much about keeping promises or keeping in touchÅ^with people...")
	hitretResult := Hitret(4970)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...In the city, it kind of feels like you don't have toÅ^communicate at all.")
	hitretResult := Hitret(4971)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_02L", 0, 0)
	Talk("Class Rep", "KO010043")
	Mess("B-But... isn't it still nice when it's convenient?")
	hitretResult := Hitret(4972)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, I think so, but only to a certain extent.")
	hitretResult := Hitret(4973)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("In the city... Well, for instance, sitting here andÅ^talking to someone like this... You don't see people doÅ^that very often at all in the city.")
	hitretResult := Hitret(4974)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even basic human interaction is reduced to a voice onÅ^the cell phone... People don't talk face to face.")
	hitretResult := Hitret(4975)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_06L", 0, 0)
	Talk("Class Rep", "KO010044")
	Mess("I see...")
	hitretResult := Hitret(4976)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Of course, there are phones out here too, but... I feelÅ^like people really value the time they spend with otherÅ^people.")
	hitretResult := Hitret(4977)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't you think it's better to talk face to face insteadÅ^of on a cell phone?")
	hitretResult := Hitret(4978)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_07L", 0, 0)
	Talk("Class Rep", "KO010045")
	Mess("Eh... Uh... Y-Yeah, of course!")
	hitretResult := Hitret(4979)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha... You don't need to yell.")
	hitretResult := Hitret(4980)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_09M", 0, 0)
	Talk("Class Rep", "KO010046")
	Mess("Eh... Oh no...")
	hitretResult := Hitret(4981)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I saw her cheeks burning red in the second before sheÅ^turned away.")
	hitretResult := Hitret(4982)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010047")
	Mess("I-I'm sorry, that was a bit weird.")
	hitretResult := Hitret(4983)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't worry about... Though I don't know if I should beÅ^saying that.")
	hitretResult := Hitret(4984)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetBustup("CH02_06M", 0, 0)
	Talk("Class Rep", "KO010048")
	Mess("Uu...")
	hitretResult := Hitret(4985)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1500, 0)
	SetCg("B07a", 0, 0)
	Transition("WIP_RL", 500)
	ScPlayBgm("BGM05", 0)
	Talk("Haruka", "")
	Mess("I wonder when Amatsume and Sora are going to come backÅ^down...")
	hitretResult := Hitret(4986)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even after I finished my lunchbox, I still didn't seeÅ^any sign of the two of them.")
	hitretResult := Hitret(4987)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_08M", 0, 0)
	Talk("Class Rep", "KO010049")
	Mess("Oh, you're right...")
	hitretResult := Hitret(4988)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("They should have come down by now, but...")
	hitretResult := Hitret(4989)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_09M", 0, 0)
	Talk("Class Rep", "KO010050")
	Mess("You're right, but... Amatsume-san said the purificationÅ^ceremony would take quite a while, so maybe they can'tÅ^come down yet.")
	hitretResult := Hitret(4990)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I guess so... I'm not worried about Amatsume, but I hopeÅ^Sora is holding out...")
	hitretResult := Hitret(4991)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_02M", 0, 0)
	Talk("Class Rep", "KO010051")
	Mess("Didn't she stay with Amatsume-san to help her?")
	hitretResult := Hitret(4992)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, she did, but... Sora gets bored easily, and she'sÅ^not good at putting up with stuff she doesn't like...")
	hitretResult := Hitret(4993)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010052")
	Mess("I see. If I had to guess, I'd say she looks like veryÅ^patient.")
	hitretResult := Hitret(4994)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Not at all... I don't think so, anyway.")
	hitretResult := Hitret(4995)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But still, I'm surprised the class rep has been keepingÅ^an eye on her, even though she's in a different class...")
	hitretResult := Hitret(4996)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hey, class rep.")
	hitretResult := Hitret(4997)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_07M", 0, 0)
	Talk("Class Rep", "KO010053")
	Mess("Eh, wh-what is it?")
	hitretResult := Hitret(4998)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You know... I kind of hesitate to ask this, but... Uh,Å^can I ask you to look out for Sora?")
	hitretResult := Hitret(4999)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010054")
	Mess("Eh, Sora-san?")
	hitretResult := Hitret(5000)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... You've heard about our family circumstances,Å^right?")
	hitretResult := Hitret(5001)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010055")
	Mess("...Yeah. Just a little, though...")
	hitretResult := Hitret(5002)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then this should be easy... Well, after that happened,Å^Sora received a bigger shock than I did...")
	hitretResult := Hitret(5003)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_08M", 0, 0)
	Talk("Class Rep", "KO010056")
	Mess("Y-Yeah...")
	hitretResult := Hitret(5004)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Since we've come here, she's met a bunch of oldÅ^acquaintances, like Amatsume and Nao-chan. I thinkÅ^that's helped her calm a bit, but...")
	hitretResult := Hitret(5005)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'd like her to make more friends her own age.")
	hitretResult := Hitret(5006)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("She seems to get along all right with Migiwa-san, and...Å^I was just thinking you might be her friend too...")
	hitretResult := Hitret(5007)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_07M", 0, 0)
	Talk("Class Rep", "KO010057")
	Mess("...Y-Yes, gladly.")
	hitretResult := Hitret(5008)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm sorry for asking this out of the blue, but... thankÅ^you.")
	hitretResult := Hitret(5009)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_02M", 0, 0)
	Talk("Class Rep", "KO010058")
	Mess("...Kasugano-kun, you must really love your sister.")
	hitretResult := Hitret(5010)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahaha... She's the only family I have left... Or maybeÅ^I'm just a siscon.")
	hitretResult := Hitret(5011)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010059")
	Mess("Not at all... You're looking out for your sister. IÅ^think you're really wonderful...")
	hitretResult := Hitret(5012)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh...")
	hitretResult := Hitret(5013)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_05M", 0, 0)
	Jump(9, 0)
	Talk("Class Rep", "KO010060")
	Mess("...Ah!? I-I just... P-Please, please forget that!")
	hitretResult := Hitret(5014)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("A-Ahahaha...")
	hitretResult := Hitret(5015)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_06M", 0, 0)
	Down(9, 0, 0, 0)
	Talk("Class Rep", "KO010061")
	Mess("A-Aah...")
	hitretResult := Hitret(5016)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her face went bright red as she put her head in herÅ^hands.")
	hitretResult := Hitret(5017)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was a bit embarrassed too, but... thank you, classÅ^rep.")
	hitretResult := Hitret(5018)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I whispered that in my heart.")
	hitretResult := Hitret(5019)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(2000, 0)
	Down(9, -1, 0, 0)
	SetCg("B07a", 0, 0)
	Transition("WIP_MOZV", 500)
	Talk("Haruka", "")
	Mess("Now then... I have to get back to work.")
	hitretResult := Hitret(5020)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I ate my lunch and spent a while talking with the classÅ^rep... I feel like a lot of time has passed.")
	hitretResult := Hitret(5021)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_02M", 0, 0)
	Talk("Class Rep", "KO010062")
	Mess("Y-Yeah, you do.")
	hitretResult := Hitret(5022)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well then, let's go.")
	hitretResult := Hitret(5023)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010063")
	Mess("Yes... Oh, Kasugano-kun, are you going to be here theÅ^whole day?")
	hitretResult := Hitret(5024)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? Yeah, once I'm done helping out, I'll probably stickÅ^around.")
	hitretResult := Hitret(5025)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010064")
	Mess("Oh, good... You wouldn't mind going around theÅ^festival... with me, would you?")
	hitretResult := Hitret(5026)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, if I have time, I'd like to hang out withÅ^everyone.")
	hitretResult := Hitret(5027)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_06M", 0, 0)
	Talk("Class Rep", "KO010065")
	Mess("...W-With everyone...")
	hitretResult := Hitret(5028)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh?")
	hitretResult := Hitret(5029)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_09M", 0, 0)
	Talk("Class Rep", "KO010066")
	Mess("...I-It's nothing. Well then, I'll see you later...")
	hitretResult := Hitret(5030)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, thanks.")
	hitretResult := Hitret(5031)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(9)
	Talk("Haruka", "")
	Mess("...")
	hitretResult := Hitret(5032)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I never expected to have this kind of conversation withÅ^the class rep.")
	hitretResult := Hitret(5033)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From her attitude toward Ryouhei and the others, IÅ^thought she was uptight and strict, kind of likeÅ^Migiwa-san, but I guess I was wrong.")
	hitretResult := Hitret(5034)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I thought I had her figured out, but she wasÅ^surprisingly easy to talk to.")
	hitretResult := Hitret(5035)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...I hope she gets along with Sora, too.")
	hitretResult := Hitret(5036)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("", 0)
	Change("00_a015c")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

