Include"yosuga.ch"

Function sc00_B018()
	ScenarioEnter()
	SetCg("B21a", 0, 0)
	SetBustup("CB06_11L", 0, 0)
	Transition("WIP_LR", 500)
	ScPlayBgm("BGM17", 0)
	Talk("Nao", "NO030919")
	Mess("H-Haru-chan...")
	hitretResult := Hitret(14298)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...........")
	hitretResult := Hitret(14299)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I didn't mean to hug her, so I was lost at words.")
	hitretResult := Hitret(14300)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I want to tell her... There's a voice inside me thatÅ^wants to tell her.")
	hitretResult := Hitret(14301)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In this state, not only my feet are staggering but myÅ^words as well, I don't have any confidence in me.")
	hitretResult := Hitret(14302)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If that's the case...")
	hitretResult := Hitret(14303)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_02L", 0, 0)
	Talk("Nao", "NO030920")
	Mess("Wh-what's the matter? Are you tired?")
	hitretResult := Hitret(14304)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...N-No... Um...")
	hitretResult := Hitret(14305)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I should consider Nao-chan's feelings or else, she mightÅ^just shrug off my feelins in a nice way...")
	hitretResult := Hitret(14306)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It made me uneasy.")
	hitretResult := Hitret(14307)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But... If I say it in this kind of situation, I'll beÅ^regretting it over and over again.")
	hitretResult := Hitret(14308)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_11L", 0, 0)
	Talk("Nao", "NO030921")
	Mess("Um... I think it's about time, shall we go?")
	hitretResult := Hitret(14309)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Y-Yeah...")
	hitretResult := Hitret(14310)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I continued to hug Nao-chan tightly, she also seemed toÅ^be in pain.")
	hitretResult := Hitret(14311)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But... If I let go of Nao-chan's hands now, I feel likeÅ^she's gonna go right away.")
	hitretResult := Hitret(14312)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Again... I won't be able to avoid it...")
	hitretResult := Hitret(14313)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I slowly took my hands away from Nao-chan's shoulder andÅ^hands.")
	hitretResult := Hitret(14314)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030922")
	Mess("...Ah...")
	hitretResult := Hitret(14315)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan's body shook as if she was nervous.")
	hitretResult := Hitret(14316)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I took a deep breath, then I looked at Nao-chan in hisÅ^eyes once more.")
	hitretResult := Hitret(14317)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_02L", 0, 0)
	Talk("Nao", "NO030923")
	Mess("... What's the matter Haru-chan?")
	hitretResult := Hitret(14318)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan...")
	hitretResult := Hitret(14319)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I just decided, I'll tell her how I feel with onlyÅ^simple words.")
	hitretResult := Hitret(14320)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_11L", 0, 0)
	Talk("Haruka", "")
	Mess("...I love you.")
	hitretResult := Hitret(14321)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's all, I couldn't continue on anymore.")
	hitretResult := Hitret(14322)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan always reminds me of so many good things,Å^that's what I wanted to tell her, though it seems that IÅ^said something that I never planned to.")
	hitretResult := Hitret(14323)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There's so many things that I want to tell her... ButÅ^love is the thing that I really feel towards Nao-chan.")
	hitretResult := Hitret(14324)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Everyday, I'll show Nao-chan what I really feel.")
	hitretResult := Hitret(14325)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030924")
	Mess("... !...")
	hitretResult := Hitret(14326)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan's face became flushed as I was looking at her.")
	hitretResult := Hitret(14327)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I also said it so sudden, even though I didn't want toÅ^bother her...")
	hitretResult := Hitret(14328)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("This is what I feel deep inside.")
	hitretResult := Hitret(14329)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030925")
	Mess("...Haru-chan...")
	hitretResult := Hitret(14330)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan, quickly nodded as I looked at her.")
	hitretResult := Hitret(14331)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_02L", 0, 0)
	Talk("Nao", "NO030926")
	Mess("You surprised me...")
	hitretResult := Hitret(14332)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Sorry...")
	hitretResult := Hitret(14333)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030927")
	Mess("... But...")
	hitretResult := Hitret(14334)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_11L", 0, 0)
	Talk("Nao", "NO030928")
	Mess("...I'm really happy to know how you feel about me...")
	hitretResult := Hitret(14335)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Nao-chan...")
	hitretResult := Hitret(14336)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030929")
	Mess("I-It's embarrassing... For someone to confess to me...")
	hitretResult := Hitret(14337)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030930")
	Mess("...Haru-chan... Me too, I love you...")
	hitretResult := Hitret(14338)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just now, Nao-chan embraced me tightly.")
	hitretResult := Hitret(14339)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030931")
	Mess("...You know... I really feel happy, embarrassed, and IÅ^also feel so nervous...")
	hitretResult := Hitret(14340)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030932")
	Mess("Me and Haru-chan... I really can't believe it...")
	hitretResult := Hitret(14341)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030933")
	Mess("Because... We really looked like we were siblings...")
	hitretResult := Hitret(14342)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030934")
	Mess("That's why... If it's always like this... I thought thatÅ^our relationship wouldn't end if we stayed like this...")
	hitretResult := Hitret(14343)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030935")
	Mess("...Haru-chan, ever since you came here... You tried toÅ^live differently... And even though so many things haveÅ^changed throughout...")
	hitretResult := Hitret(14344)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030936")
	Mess("...And to go beyond that... You even loved someone...")
	hitretResult := Hitret(14345)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_02L", 0, 0)
	Talk("Nao", "NO030937")
	Mess("... But... Haru-chan, you still haven't changed everÅ^since...")
	hitretResult := Hitret(14346)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030938")
	Mess("...I'm really happy about it... That's why... I didn'tÅ^had the chance...")
	hitretResult := Hitret(14347)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_12L", 0, 0)
	Talk("Nao", "NO030939")
	Mess("Thank you... I'm very very happy... Haru-chan.")
	hitretResult := Hitret(14348)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030940")
	Mess("...I love you...")
	hitretResult := Hitret(14349)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("As she said it, she closed her eyes and drew her face toÅ^mine.")
	hitretResult := Hitret(14350)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I also did the same thing.")
	hitretResult := Hitret(14351)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030941")
	Mess("Ngh... *kiss*...")
	hitretResult := Hitret(14352)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("Our lips touched, I tightly held her with my handsÅ^around her.")
	hitretResult := Hitret(14353)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_b018b")
	Return
	Change("EXIT_SCENARIO")
EndFunc

