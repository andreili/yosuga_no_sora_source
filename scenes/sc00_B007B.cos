Include"yosuga.ch"

Function sc00_B007B()
	ScenarioEnter()
	ScPlaySe("SE357", 0)
	SetCg("B01a", 0, 0)
	Transition("WIP_LR", 500)
	Talk("êSÇÃê∫", "")
	Mess("...The next day.")
	hitretResult := Hitret(11983)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM05", 0)
	SetCg("B02a", 0, 0)
	Talk("Haruka", "")
	Mess("...Nao-chan will soon be here I guess?")
	hitretResult := Hitret(11984)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I washed the noodles, and then I divided them equally onÅ^the plates that were lined up.")
	hitretResult := Hitret(11985)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Then I wrapped them up, then I placed them in theÅ^fridge.")
	hitretResult := Hitret(11986)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The noodles were the free ones I got, so I've decidedÅ^that we'll eat them for today's lunch.")
	hitretResult := Hitret(11987)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora??")
	hitretResult := Hitret(11988)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...")
	hitretResult := Hitret(11989)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I called out to her room, but she didn't answer.")
	hitretResult := Hitret(11990)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There's no school today, so maybe she's still asleep.")
	hitretResult := Hitret(11991)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I left a note on the table for her.")
	hitretResult := Hitret(11992)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("ÅgNoodles for lunch. It's inside the refrigerator.Åh")
	hitretResult := Hitret(11993)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("ÅgThe milk is just beside it. Don't mistake it forÅ^barley tea!ÅhÅ^I also wrote something on the containers of the sauceÅ^that I placed in, "Noodle SauceÅh and sticked it on.")
	hitretResult := Hitret(11994)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With this, Sora won't drink and spit the sauce byÅ^mistaking it for the barley tea.")
	hitretResult := Hitret(11995)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Now then, her lunch is done.")
	hitretResult := Hitret(11996)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I changed into my uniform, so I'll be ready to go.")
	hitretResult := Hitret(11997)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Cleaning the pool, what would I need for it? ...I thinkÅ^I'll get wet for a while there, so I need some towel andÅ^some change of clothes.")
	hitretResult := Hitret(11998)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I placed my gym clothes and some towel on my second bag.Å^I placed some plastic bags as well since my clothes willÅ^get wet later.")
	hitretResult := Hitret(11999)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("VoiceÅsìﬁèèÅt", "NO030325")
	Mess("Haru-chan??")
	hitretResult := Hitret(12000)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes!! I'll be there in a bit!")
	hitretResult := Hitret(12001)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora! I'll be out for a while.")
	hitretResult := Hitret(12002)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("......")
	hitretResult := Hitret(12003)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It would be much better if you at least answer...")
	hitretResult := Hitret(12004)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I took my bags and went on my way out.")
	hitretResult := Hitret(12005)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora, I'll be going now!!")
	hitretResult := Hitret(12006)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Just to be sure, I locked the door and the gate, thenÅ^Nao-chan and I went to school.")
	hitretResult := Hitret(12007)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B21d", 0, 0)
	Transition("WIP_RL", 500)
	SetBustup("CF04_03M", 0, 0)
	Talk("Ryouhei", "RH030147")
	Mess("Yoh! Aren't you late?")
	hitretResult := Hitret(12008)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh!? Ryouhei?")
	hitretResult := Hitret(12009)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei was standing at the starting line of the pool,Å^he was brushing his hair as he made a pose.")
	hitretResult := Hitret(12010)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030148")
	Mess("I didn't had much free time, but I wonder what wouldÅ^happen if I came?")
	hitretResult := Hitret(12011)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora isn't here though.")
	hitretResult := Hitret(12012)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_06M", 0, 0)
	Jump(7, 0)
	Talk("Ryouhei", "RH030149")
	Mess("WH-WHAT'S THAT!?")
	hitretResult := Hitret(12013)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It looks like she's still sleeping.")
	hitretResult := Hitret(12014)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030150")
	Mess("IDIOT!! You should have took her along as she is!!")
	hitretResult := Hitret(12015)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh, while she's sleeping?")
	hitretResult := Hitret(12016)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_03M", 0, 0)
	Talk("Ryouhei", "RH030151")
	Mess("Ah, no, maybe I can just go right now to your house.")
	hitretResult := Hitret(12017)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I locked the door.")
	hitretResult := Hitret(12018)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_05M", 0, 0)
	Talk("Ryouhei", "RH030152")
	Mess("OK, I'll go there for a bit!")
	hitretResult := Hitret(12019)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_01M", 0, 0)
	Talk("Nao", "NO030326")
	Mess("Yes, yes, since you came all the way here, just take itÅ^slow, okay?")
	hitretResult := Hitret(12020)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030153")
	Mess("Tch, she's here...")
	hitretResult := Hitret(12021)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030327")
	Mess("Even if you're saying that, thank you for coming.Å^But, since you're here, I guess we'll need you to helpÅ^more, right?")
	hitretResult := Hitret(12022)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_10M", 0, 0)
	Talk("Ryouhei", "RH030154")
	Mess("So annoying...")
	hitretResult := Hitret(12023)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, I'm ready to help as much as possible.")
	hitretResult := Hitret(12024)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC01_01M", 0, 0)
	SetBustup("CD01_10M", 0, 0)
	Talk("Akira", "AK030030")
	Mess("Ah, you're here already?")
	hitretResult := Hitret(12025)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA030017")
	Mess("G-good morning...")
	hitretResult := Hitret(12026)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030328")
	Mess("Ah, Akira-chan, Migiwa-san... you guys too?")
	hitretResult := Hitret(12027)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK030031")
	Mess("Yeah, if we help in cleaning, we could play as much asÅ^we want in the pool later.")
	hitretResult := Hitret(12028)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA030018")
	Mess("I-I... particularly didn't want to...")
	hitretResult := Hitret(12029)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CB04_08M", 0, 0)
	Talk("Nao", "NO030329")
	Mess("Well, I think I know who persuaded you to come, it's soÅ^simple to know why.")
	hitretResult := Hitret(12030)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_05M", 0, 0)
	Talk("Ryouhei", "RH030155")
	Mess("Å`ÅÙ")
	hitretResult := Hitret(12031)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CB04_02M", 0, 0)
	Talk("Nao", "NO030330")
	Mess("Thank you, this would help. Now then, shall we assignÅ^the work?")
	hitretResult := Hitret(12032)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sure, let's finish this before it gets hot.")
	hitretResult := Hitret(12033)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030331")
	Mess("Akira-chan and Migiwa-san, you guys are in charge forÅ^weeding the grass at the pool side.")
	hitretResult := Hitret(12034)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK030032")
	Mess("Yeah, we got it. I also have pride in weeding you know?")
	hitretResult := Hitret(12035)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA030019")
	Mess("Understood.")
	hitretResult := Hitret(12036)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030332")
	Mess("All that's left is...")
	hitretResult := Hitret(12037)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(5)
	BustupClear(2)
	SetBustup("CF04_03M", 0, 0)
	Talk("Ryouhei", "RH030156")
	Mess("Should I clean the girl's locker room?")
	hitretResult := Hitret(12038)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_06M", 0, 0)
	Talk("Nao", "NO030333")
	Mess("Idiot, you would be sweeping the dirt off the bottom ofÅ^the pool.")
	hitretResult := Hitret(12039)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_06M", 0, 0)
	Talk("Ryouhei", "RH030157")
	Mess("Ah?! Only me?!")
	hitretResult := Hitret(12040)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'll do it too.")
	hitretResult := Hitret(12041)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_01M", 0, 0)
	SetBustup("CF04_01M", 0, 0)
	Talk("Nao", "NO030334")
	Mess("The three of us will do that in one course at a time.Å^The deck brush is ready as well.")
	hitretResult := Hitret(12042)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Roger.")
	hitretResult := Hitret(12043)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_02M", 0, 0)
	Talk("Nao", "NO030335")
	Mess("Also Haru-chan, change into some clothes that would beÅ^fine when it gets wet.")
	hitretResult := Hitret(12044)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, I thought of that already so I brought my gymÅ^clothes.")
	hitretResult := Hitret(12045)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030158")
	Mess("That's stupid, you could have brought your swimsuit soÅ^we could play right along.")
	hitretResult := Hitret(12046)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I really didn't think of playing, so I didn't think ofÅ^preparing for it.")
	hitretResult := Hitret(12047)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_01M", 0, 0)
	Talk("Nao", "NO030336")
	Mess("It's slippery, be careful.")
	hitretResult := Hitret(12048)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("Yeah, I got it. OK, let's go for it!")
	hitretResult := Hitret(12049)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan asked the teacher if we could use a little bitÅ^of water for the pool while we're at it.")
	hitretResult := Hitret(12050)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Then, with the help of the deck brush and the detergent,Å^we were able to scrub the moss and the dirt and pushedÅ^them on to the drainage.")
	hitretResult := Hitret(12051)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The floor feels so slippery now, so I felt a bitÅ^unstable around my feet since I already slipped a coupleÅ^of times earlier.")
	hitretResult := Hitret(12052)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B38a", 0, 0)
	ScPlayBgm("BGM06", 0)
	Talk("Haruka", "")
	Mess("Uwa!? Ouch...")
	hitretResult := Hitret(12053)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_13L", 0, 0)
	Quake(16, 0, 1, 2, 30)
	ScPlaySe("se012", 0)
	Talk("Nao", "NO030337")
	Mess("Kyaa!? A-are you okay?")
	hitretResult := Hitret(12054)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I slipped and bumped into Nao-chan, I bounced badly,Å^though she caught me in my arms and saved me fromÅ^falling.")
	hitretResult := Hitret(12055)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, yeah. ...Sorry.")
	hitretResult := Hitret(12056)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_03M", 0, 0)
	Talk("Nao", "NO030338")
	Mess("It's fine, just don't move too much with your legsÅ^spread open, okay?")
	hitretResult := Hitret(12057)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As we bumped, my arms sinked into Nao-chan's chest and IÅ^pushed it a bit.")
	hitretResult := Hitret(12058)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It wasn't that soft, though my body went stiff, andÅ^Nao-chan didn't seem to mind it at all.")
	hitretResult := Hitret(12059)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In her swimsuit today... it was the tenderness of herÅ^breast cups.")
	hitretResult := Hitret(12060)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahaha... I-I'm fine now.")
	hitretResult := Hitret(12061)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I slowly pulled my hands away from Nao-chan's body.")
	hitretResult := Hitret(12062)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_01M", 0, 0)
	Talk("Nao", "NO030339")
	Mess("Really, be careful okay?")
	hitretResult := Hitret(12063)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Okay.")
	hitretResult := Hitret(12064)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if I didn't notice as well, I pretended to lookÅ^cool...")
	hitretResult := Hitret(12065)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	SetBustup("CF04_01M", 0, 0)
	Talk("Ryouhei", "RH030159")
	Mess("Come to think of it, what are the other club membersÅ^doing?")
	hitretResult := Hitret(12066)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_01M", 0, 0)
	Talk("Nao", "NO030340")
	Mess("Ah, the president and one of the members went cleaningÅ^for the locker rooms.")
	hitretResult := Hitret(12067)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030160")
	Mess("Heh, are they exactly there? Those club members.")
	hitretResult := Hitret(12068)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030341")
	Mess("We're a small department after all.")
	hitretResult := Hitret(12069)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030342")
	Mess("Okay. ...Well, the course here is finished, next course.")
	hitretResult := Hitret(12070)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_02M", 0, 0)
	Talk("Haruka", "")
	Mess("Yeah, if everyone's doing their job, we'll be done rightÅ^away.")
	hitretResult := Hitret(12071)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030343")
	Mess("You're right, but before us, some classes had alreadyÅ^used their P.E. time to clean.")
	hitretResult := Hitret(12072)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's just as what she said, it already looked a bitÅ^clean when we first came here to clean.")
	hitretResult := Hitret(12073)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If this was just clean as it is right now, everyoneÅ^would probably feel like swimming and playing.")
	hitretResult := Hitret(12074)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Our feet sinked into the water as the water risesÅ^inside, though we still need to polish this twenty fiveÅ^meter pool.")
	hitretResult := Hitret(12075)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The molds are quite resistant, but we'll be successfulÅ^if the three of us cooperate properly.")
	hitretResult := Hitret(12076)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030161")
	Mess("There's not much left, okay... Here I gooooo....")
	hitretResult := Hitret(12077)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_02M", 0, 0)
	BustupLeave(7, -600, 0, 1, 2000, 1)
	Talk("Ryouhei", "RH030162")
	Mess("YAHOO!!!")
	hitretResult := Hitret(12078)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_06M", 0, 0)
	Talk("Nao", "NO030344")
	Mess("Hey, Ryouhei! That's dangerous right?")
	hitretResult := Hitret(12079)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei took the deck brush and used it to slide on theÅ^smooth flooring of the pool.")
	hitretResult := Hitret(12080)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Thanks to the water mixed with the detergent, we wereÅ^able to sweep as if we were skating and gliding throughÅ^the floor.")
	hitretResult := Hitret(12081)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_04S", 0, 0)
	Talk("Ryouhei", "RH030163")
	Mess("Hehehe, that looks kinda cool, just like ice hockey.")
	hitretResult := Hitret(12082)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030345")
	Mess("You know that you might also get hurt, right?")
	hitretResult := Hitret(12083)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupLeave(7, 600, 0, 1, 1000, 1)
	Talk("Ryouhei", "RH030164")
	Mess("I'm not that clumsy. Hyo... YAHOO!!")
	hitretResult := Hitret(12084)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030165")
	Mess("Hey, Haruka why don't you come here?")
	hitretResult := Hitret(12085)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? Me?")
	hitretResult := Hitret(12086)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Earlier, this was covered with slime, and Nao-chan saidÅ^before not to step on it.")
	hitretResult := Hitret(12087)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_02S", 0, 0)
	Talk("Nao", "NO030346")
	Mess("Geez, boys really do what they want to.")
	hitretResult := Hitret(12088)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030347")
	Mess("Aren't you doing it too much, doesn't it hurt under yourÅ^feet?")
	hitretResult := Hitret(12089)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF04_02S", 0, 0)
	Talk("Ryouhei", "RH030166")
	Mess("No, not at aaaall.")
	hitretResult := Hitret(12090)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Quake(0, 16, 1, 2, 30)
	BustupClear(7)
	Talk("Ryouhei", "RH030167")
	Mess("WHOA!?")
	hitretResult := Hitret(12091)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(7)
	ScPlaySe("SE208", 0)
	SetBustup("CB04_08M", 0, 0)
	ScStopBgm()
	Talk("Nao", "NO030348")
	Mess("Hey, don't tell me I didn't warn you.")
	hitretResult := Hitret(12092)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("Ryouhei, are you all right?")
	hitretResult := Hitret(12093)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030168")
	Mess("H-Hands, give me your hands...")
	hitretResult := Hitret(12094)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Here, it looks like you sat on you hips... WHOA!!")
	hitretResult := Hitret(12095)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM14", 0)
	Quake(0, 16, 1, 2, 30)
	ScPlaySe("SE208", 0)
	Talk("Haruka", "")
	Mess("Ugh... ughhhh...")
	hitretResult := Hitret(12096)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_05M", 0, 0)
	Talk("Ryouhei", "RH030169")
	Mess("My bad, my bad, was it slippery?")
	hitretResult := Hitret(12097)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's mean, Ryouhei...")
	hitretResult := Hitret(12098)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I lost my balance as I pulled Ryouhei up, and I plungedÅ^into the water.")
	hitretResult := Hitret(12099)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_02M", 0, 0)
	Talk("Ryouhei", "RH030170")
	Mess("Hehehehe, but, it feels good right?")
	hitretResult := Hitret(12100)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Definitely... hahahaha...")
	hitretResult := Hitret(12101)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My pants weren't wet until now, that cool feeling fromÅ^the water really felt good at that moment.")
	hitretResult := Hitret(12102)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_06M", 0, 0)
	Talk("Nao", "NO030349")
	Mess("Geez, you shouldn't had included Haru-chan in yourÅ^mischiefs!")
	hitretResult := Hitret(12103)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_06M", 0, 0)
	Shake(7)
	Talk("Ryouhei", "RH030171")
	Mess("OUCH!? Ughh? Wh-why don't you stop it?")
	hitretResult := Hitret(12104)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030350")
	Mess("This would also make your filthy guts clean!")
	hitretResult := Hitret(12105)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030172")
	Mess("S-stop that, wouldn't everyone be more worried if IÅ^became more cleaner than before?")
	hitretResult := Hitret(12106)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She held the deck brush against Ryouhei's face.")
	hitretResult := Hitret(12107)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Now, now, isn't that enough, Nao-chan?")
	hitretResult := Hitret(12108)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_01M", 0, 0)
	Talk("Ryouhei", "RH030173")
	Mess("Look, why don't you also take a bath, it might make youÅ^feel better?")
	hitretResult := Hitret(12109)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei pulled Nao-chan's deck brush as she push himÅ^with it.")
	hitretResult := Hitret(12110)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_13M", 0, 0)
	Jump(3, 0)
	Talk("Nao", "NO030351")
	Mess("Eh!? Whaaa!!!")
	hitretResult := Hitret(12111)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CB04_13L", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan lost her balance too, she slipped and fell onÅ^me.")
	hitretResult := Hitret(12112)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh!?")
	hitretResult := Hitret(12113)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Quake(0, 16, 1, 2, 30)
	BlackOut(0, 0)
	ScPlaySe("se018", 0)
	Talk("Haruka", "")
	Mess("U-Uggh...")
	hitretResult := Hitret(12114)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan was on top of me as she fell.")
	hitretResult := Hitret(12115)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I tried to catch her with my arms, though the shock wasÅ^just too much...")
	hitretResult := Hitret(12116)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan's face was close to mine, and just like that...")
	hitretResult := Hitret(12117)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	Talk("Nao", "NO030352")
	Mess("Nghh... Mhhmm...")
	hitretResult := Hitret(12118)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("ÅIÅIÅI")
	hitretResult := Hitret(12119)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Our lips touched.")
	hitretResult := Hitret(12120)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's so sudden that it surprised me, I wasn't able toÅ^react right away, and just like that Nao-chan pushed meÅ^down.")
	hitretResult := Hitret(12121)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B38a", 0, 0)
	Transition("WIP_BT", 500)
	Talk("Haruka", "")
	Mess("...")
	hitretResult := Hitret(12122)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030353")
	Mess("...Ngh...")
	hitretResult := Hitret(12123)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_06M", 0, 0)
	Talk("Ryouhei", "RH030174")
	Mess("H-hey, are you guys all right? My bad, that may be tooÅ^much...")
	hitretResult := Hitret(12124)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CB04_11L", 0, 0)
	Talk("Nao", "NO030354")
	Mess("...")
	hitretResult := Hitret(12125)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(12126)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM08", 0)
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan didn't move at all as she was on top of me.")
	hitretResult := Hitret(12127)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her face was flushed, she was deeply staring at me.")
	hitretResult := Hitret(12128)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was also entranced by her eyes as I stared into her,Å^then I couldn't move eyes away from her.")
	hitretResult := Hitret(12129)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030355")
	Mess("...Ah...")
	hitretResult := Hitret(12130)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Um...")
	hitretResult := Hitret(12131)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My heart started to beat violently as I felt her skinÅ^touched mine.")
	hitretResult := Hitret(12132)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My face became hot.")
	hitretResult := Hitret(12133)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though we tried not to give off an awkwardÅ^atmosphere, I took a deep breath and distanced myselfÅ^since I still felt shy about it.")
	hitretResult := Hitret(12134)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And...")
	hitretResult := Hitret(12135)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As our lips touched, it wasn't just like any switch...Å^Nao-chan and I are clearly aware about what happened inÅ^the past.")
	hitretResult := Hitret(12136)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uh... s-sorry...")
	hitretResult := Hitret(12137)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030356")
	Mess("N-no... I'm also at fault...")
	hitretResult := Hitret(12138)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_11M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan staggered as she walked away from me.")
	hitretResult := Hitret(12139)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_10M", 0, 0)
	Talk("Ryouhei", "RH030175")
	Mess("Are you really all right?")
	hitretResult := Hitret(12140)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... though I'm soaked now.")
	hitretResult := Hitret(12141)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030357")
	Mess("Haru-chan...")
	hitretResult := Hitret(12142)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm fine, I knew that I'm gonna get wet.")
	hitretResult := Hitret(12143)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_01M", 0, 0)
	Talk("Ryouhei", "RH030176")
	Mess("That's why, I said that it would be better if youÅ^brought your swimsuit.")
	hitretResult := Hitret(12144)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y-you're right.")
	hitretResult := Hitret(12145)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I stood up.")
	hitretResult := Hitret(12146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030177")
	Mess("Well, you can dry that up in your house.")
	hitretResult := Hitret(12147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My gym clothes were dripping lots of water.")
	hitretResult := Hitret(12148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_06M", 0, 0)
	Talk("Nao", "NO030358")
	Mess("Geez... so who's fault is it?")
	hitretResult := Hitret(12149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_10M", 0, 0)
	Talk("Ryouhei", "RH030178")
	Mess("My bad. But seriously, are you both all right?")
	hitretResult := Hitret(12150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... I'm all right.")
	hitretResult := Hitret(12151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_01M", 0, 0)
	Talk("Nao", "NO030359")
	Mess("You were able to catch me there, thank you, Haru-chan.")
	hitretResult := Hitret(12152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y-yeah...")
	hitretResult := Hitret(12153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though I'm soaked to the skin, I'm more anxiousÅ^about my lips touching Nao-chan's.")
	hitretResult := Hitret(12154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... I think it would be better if I squeeze it aÅ^little...")
	hitretResult := Hitret(12155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I took of my shirt which was soaked and squeezed theÅ^water out.")
	hitretResult := Hitret(12156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_11M", 0, 0)
	Talk("Nao", "NO030360")
	Mess("Ah...")
	hitretResult := Hitret(12157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmmm? What is it?")
	hitretResult := Hitret(12158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030361")
	Mess("No... nothing.")
	hitretResult := Hitret(12159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan quickly glanced away from me.")
	hitretResult := Hitret(12160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_01M", 0, 0)
	Talk("Ryouhei", "RH030179")
	Mess("Why not dry it off the air?")
	hitretResult := Hitret(12161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay.")
	hitretResult := Hitret(12162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I hanged my clothes on the side of the pool. Then IÅ^started helping out again without my shirt on.")
	hitretResult := Hitret(12163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_04M", 0, 0)
	Talk("Ryouhei", "RH030180")
	Mess("Heh, you got a nice body there.")
	hitretResult := Hitret(12164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I don't really work out that much.")
	hitretResult := Hitret(12165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I went and took the deck brush.")
	hitretResult := Hitret(12166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Now then, we just need to tidy up the rest right?")
	hitretResult := Hitret(12167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_02M", 0, 0)
	Talk("Nao", "NO030362")
	Mess("Y-yeah... that's right.")
	hitretResult := Hitret(12168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM03", 0)
	Talk("Ryouhei", "RH030181")
	Mess("All right! That's it!! YAHOOOO!!")
	hitretResult := Hitret(12169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupLeave(7, -300, 0, 1, 1000, 1)
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei slided again as if he was skating.")
	hitretResult := Hitret(12170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_01M", 0, 0)
	Talk("Nao", "NO030363")
	Mess("Really, you never learn.")
	hitretResult := Hitret(12171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan, why don't you do it too? It's fun.")
	hitretResult := Hitret(12172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_02M", 0, 0)
	Talk("Nao", "NO030364")
	Mess("Geez, Haru-chan too.")
	hitretResult := Hitret(12173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("First, try it slowly, until you get the hang of it okay?")
	hitretResult := Hitret(12174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030365")
	Mess("...L-like this I guess?")
	hitretResult := Hitret(12175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Nao-chan slowly slided.")
	hitretResult := Hitret(12176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_02M", 0, 0)
	Talk("Nao", "NO030366")
	Mess("Ahaha, it's great!")
	hitretResult := Hitret(12177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You can feel the wind coming, just like this, right?")
	hitretResult := Hitret(12178)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupLeave(3, 600, 0, 1, 1000, 1)
	Talk("Nao", "NO030367")
	Mess("That's right, let's go!!")
	hitretResult := Hitret(12179)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sliding through, Nao-chan took the deck brush for herÅ^balance and started sliding again.")
	hitretResult := Hitret(12180)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You're doing great huh, count me in too.")
	hitretResult := Hitret(12181)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_04M", 0, 0)
	Talk("Nao", "NO030368")
	Mess("Ahahahaha! This is the best!!")
	hitretResult := Hitret(12182)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF04_04M", 0, 0)
	Talk("Ryouhei", "RH030182")
	Mess("See that! See what I'm telling?")
	hitretResult := Hitret(12183)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030369")
	Mess("I'm quite shocked to say this to you... but you areÅ^right!")
	hitretResult := Hitret(12184)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's as if we weren't cleaning at all.")
	hitretResult := Hitret(12185)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The pool was so big that we can slide freely where weÅ^wanted to.")
	hitretResult := Hitret(12186)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We just laugh if someone slips off, we just played inÅ^the water, and it's like we're on a fun break.")
	hitretResult := Hitret(12187)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(0, 1)
	ScWait(2000, 0)
	Change("00_b007c")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

