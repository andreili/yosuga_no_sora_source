Include"yosuga.ch"

Function sc00_B001C()
	ScenarioEnter()
	SetCg("B20a", 0, 0)
	Talk("Ryouhei", "RH030009")
	Mess("Whooo... It's really hot...")
	hitretResult := Hitret(10724)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM06", 0)
	SetBustup("CF05_01M", 0, 0)
	Talk("Haruka", "")
	Mess("Certainly, P.E. classes during the afternoon is reallyÅ^hot...")
	hitretResult := Hitret(10725)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The rays of the sun during the afternoon is at its peak,Å^that's why P.E. classes in the afternoon are so hot.")
	hitretResult := Hitret(10726)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Today, we're playing softball out in the field, and itÅ^feels like I'm zoning out, if this continues, I feelÅ^like I'll fall perfecly ill and collapse right here.")
	hitretResult := Hitret(10727)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030010")
	Mess("I wonder if the pool classes would start early?")
	hitretResult := Hitret(10728)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So you like the pool?")
	hitretResult := Hitret(10729)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF05_04M", 0, 0)
	Talk("Ryouhei", "RH030011")
	Mess("Well, I have my own various reasons for it.")
	hitretResult := Hitret(10730)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Huh?")
	hitretResult := Hitret(10731)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF05_02M", 0, 0)
	Talk("Ryouhei", "RH030012")
	Mess("I'm always looking forward to the pooooool!")
	hitretResult := Hitret(10732)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I really don't mind if we'll have pool classes on P.E.,Å^and I think it would be much better.")
	hitretResult := Hitret(10733)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF05_01M", 0, 0)
	Talk("Ryouhei", "RH030013")
	Mess("What? You didn't have pool classes in your previousÅ^school?")
	hitretResult := Hitret(10734)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There was but, I didn't pay much attention so it was aÅ^disaster.")
	hitretResult := Hitret(10735)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030014")
	Mess("Such a waste.")
	hitretResult := Hitret(10736)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF05_04M", 0, 0)
	Talk("Ryouhei", "RH030015")
	Mess("Look at them, this year's line up for girls!")
	hitretResult := Hitret(10737)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA05_01S", 0, 0)
	SetBustup("CC05_01S", 0, 0)
	SetBustup("CD05_02S", 0, 0)
	SetBustup("CH05_03S", 0, 0)
	Talk("Ryouhei", "RH030016")
	Mess("Those perfect curves on their body, it makes me want toÅ^examine them carefully, right?")
	hitretResult := Hitret(10738)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030017")
	Mess("Really, it's something that you would look forward to!")
	hitretResult := Hitret(10739)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y-Yeah, it's something that you would look forward in aÅ^way...")
	hitretResult := Hitret(10740)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	ScPlaySe("se059", 0)
	Talk("Classmate", "NP170001")
	Mess("Oyy! Center!!")
	hitretResult := Hitret(10741)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF05_06M", 0, 0)
	Talk("Ryouhei", "RH030018")
	Mess("D-Dyowhoa!? It's heading here!!")
	hitretResult := Hitret(10742)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Got it, to the third base!!!")
	hitretResult := Hitret(10743)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The first runner is approaching the second base, andÅ^running real fast.")
	hitretResult := Hitret(10744)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I went to Ryouhei's back, just in case it would needÅ^some follow-up at some point.")
	hitretResult := Hitret(10745)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Flush("WHITE", 250, "")
	Shake(7)
	Talk("Ryouhei", "RH030019")
	Mess("Whooaa!? I-it's so bright!!")
	hitretResult := Hitret(10746)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was only a fly ball, but the sun's powerful raysÅ^blinded Ryouhei's eyesight at that moment.")
	hitretResult := Hitret(10747)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030020")
	Mess("Ah... shit!!")
	hitretResult := Hitret(10748)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("The ball just grazed the tip of his gloves, and the ballÅ^went over.")
	hitretResult := Hitret(10749)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030021")
	Mess("Sorry! Harukaa!!")
	hitretResult := Hitret(10750)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Looking at the error, the runners were in the secondÅ^base already, I stood firm and pass the ball through.")
	hitretResult := Hitret(10751)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eeeeiiii!!!!")
	hitretResult := Hitret(10752)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A direct ball throw rolled and gathered from my rightÅ^hand, throwing it to the person in the third base.")
	hitretResult := Hitret(10753)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The runner and the ball made it to the third baseÅ^simultaneously.")
	hitretResult := Hitret(10754)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se001", 1)
	ScPlaySe("se061", 0)
	Talk("Haruka", "")
	Mess("ÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(10755)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Classmate", "NP170002")
	Mess("OUTTT!!")
	hitretResult := Hitret(10756)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF05_02M", 0, 0)
	Talk("Ryouhei", "RH030022")
	Mess("All right!! NICE!")
	hitretResult := Hitret(10757)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahaha... I'm glad... In fact, I thought I wasn't inÅ^control there...")
	hitretResult := Hitret(10758)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030023")
	Mess("Liar!! You must have an experience from the LittleÅ^League right?")
	hitretResult := Hitret(10759)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, really, I only had played very little.")
	hitretResult := Hitret(10760)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Classmate", "NP170003")
	Mess("That was great transfer student!! Two out!! One more toÅ^go!")
	hitretResult := Hitret(10761)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF05_04M", 0, 0)
	Talk("Ryouhei", "RH030024")
	Mess("Shit, so I'm the next one to decide huh? And they wantÅ^me to hand it down?")
	hitretResult := Hitret(10762)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("We got it already.")
	hitretResult := Hitret(10763)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei led the arrangements and shouted as his fightingÅ^spirit bursted out.")
	hitretResult := Hitret(10764)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I went back to my original position so I won't get inÅ^the way.")
	hitretResult := Hitret(10765)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030025")
	Mess("Come!! I'll stop you, you bastard!!")
	hitretResult := Hitret(10766)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei pounded on his glove, waiting for the rightÅ^time.")
	hitretResult := Hitret(10767)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se059", 0)
	Talk("Classmate", "NP170004")
	Mess("Center!!")
	hitretResult := Hitret(10768)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As he was hoping for it, the ball flew to Ryouhei'sÅ^direction.")
	hitretResult := Hitret(10769)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF05_04M", 0, 0)
	Jump(7, 0)
	Talk("Ryouhei", "RH030026")
	Mess("Teryaaaa!!")
	hitretResult := Hitret(10770)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt that a liner is coming for the ball, so I jumpÅ^and thrust for it!")
	hitretResult := Hitret(10771)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF05_06M", 0, 0)
	Talk("Ryouhei", "RH030027")
	Mess("Ah...")
	hitretResult := Hitret(10772)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I think it was a miscalculation, the ball just grazedÅ^the tip of the glove as it pass through.")
	hitretResult := Hitret(10773)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF05_09S", 0, 0)
	Talk("Ryouhei", "RH030028")
	Mess("Shit!")
	hitretResult := Hitret(10774)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei became confused as he went to look for the ball.")
	hitretResult := Hitret(10775)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The ball had rolled to a group of girls, and as RyouheiÅ^approaches them, their screams got louder.")
	hitretResult := Hitret(10776)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1000, 0)
	SetCg("B20a", 0, 0)
	Transition("WIP_LR", 500)
	SetBustup("CF05_01M", 0, 0)
	Talk("Ryouhei", "RH030029")
	Mess("AhhÅ`, I'm totally worn out.")
	hitretResult := Hitret(10777)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It was a big match after all.")
	hitretResult := Hitret(10778)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF05_03M", 0, 0)
	Talk("Ryouhei", "RH030030")
	Mess("You can say that, but the group of girls were callingÅ^out and screaming \"KyaÅ`KyaÅ`\" was a success.")
	hitretResult := Hitret(10779)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-In many ways I guess...")
	hitretResult := Hitret(10780)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And with that, we won for today's P.E. class all becauseÅ^of Ryouhei's efforts.")
	hitretResult := Hitret(10781)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It felt so hot at first, but in the end, after wipingÅ^some of my sweat, I felt great and glad.")
	SetBustup("CF05_01M", 0, 0)
	hitretResult := Hitret(10782)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030031")
	Mess("At such times, washing off our sweat with a shower willÅ^feel much better.")
	hitretResult := Hitret(10783)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The school doesn't have one?")
	hitretResult := Hitret(10784)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030032")
	Mess("Ah, we have no choice but to use the pool's shower. ButÅ^the pool isn't open yet, I don't know if we could useÅ^it, maybe we can go to Nao's to do so.")
	hitretResult := Hitret(10785)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh... Nao-chan's?")
	hitretResult := Hitret(10786)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030033")
	Mess("Hmm? Ah, you don't know? She's part of the swimmingÅ^club.")
	hitretResult := Hitret(10787)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see... so Nao-chan is in the swimming club...")
	hitretResult := Hitret(10788)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her image gives off a cultural kind of girl, but itÅ^seems that she's into sports...")
	hitretResult := Hitret(10789)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030034")
	Mess("The pool will open soon, she's most likely gonna use itÅ^for her club activities after school, you can come asÅ^much as you would want to.")
	hitretResult := Hitret(10790)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh, are you sure? Aren't we gonna bother them?")
	hitretResult := Hitret(10791)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030035")
	Mess("It's fine, there's no problem if we use it.")
	hitretResult := Hitret(10792)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heh...")
	hitretResult := Hitret(10793)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(0, 1)
	SetCg("B20a", 0, 0)
	Transition("WIP_RL", 500)
	ScPlaySe("SE352", 0)
	Talk("êSÇÃê∫", "")
	Mess("After we have placed the things we used in our P.E. inÅ^the school storage, the bell had rung as we didn't seeÅ^it was time already.")
	hitretResult := Hitret(10794)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We quickly changed our clothes, but I think we won'tÅ^make it in time for the next class.")
	hitretResult := Hitret(10795)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As we ran back quietly to the classroom...")
	hitretResult := Hitret(10796)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_05S", 0, 0)
	Talk("Haruka", "")
	Mess("...Nao-cha....")
	hitretResult := Hitret(10797)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A group of students came out from another classroom,Å^they were heading to the science lab, I first noticedÅ^Nao-chan's figure.")
	hitretResult := Hitret(10798)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, Nao-chan looks depressed and totteringly countingÅ^her steps.")
	hitretResult := Hitret(10799)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess(".........")
	hitretResult := Hitret(10800)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM10", 0)
	Talk("êSÇÃê∫", "")
	Mess("I felt like calling to her but, suddenly my voiceÅ^wouldn't come out.")
	hitretResult := Hitret(10801)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Everyone from behind were having fun conversations,Å^while she had a cold facial expression as we follow.")
	hitretResult := Hitret(10802)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Something bothered me as I saw her expressionless face.Å^Nao-chan isn't usually like that.")
	hitretResult := Hitret(10803)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF05_01M", 0, 0)
	Talk("Ryouhei", "RH030036")
	Mess("What's the matter, Haruka?")
	hitretResult := Hitret(10804)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... nothing...")
	hitretResult := Hitret(10805)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF05_08M", 0, 0)
	Talk("Ryouhei", "RH030037")
	Mess("...")
	hitretResult := Hitret(10806)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh?")
	hitretResult := Hitret(10807)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei looked at Nao-chan, he was whispering something,Å^though I didn't hear it.")
	hitretResult := Hitret(10808)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF05_01M", 0, 0)
	Talk("Ryouhei", "RH030038")
	Mess("Ahh-yaa, it's nothing. Shall we go back to the room?")
	hitretResult := Hitret(10809)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Yeah...")
	hitretResult := Hitret(10810)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I looked at Nao-chan again as she go, I staredÅ^Nao-chan's back.")
	hitretResult := Hitret(10811)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...That face...")
	hitretResult := Hitret(10812)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I've seen that face before.")
	hitretResult := Hitret(10813)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I saw it once again, it brings back those heavy feelingsÅ^and memories of that time...")
	hitretResult := Hitret(10814)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("SP04", 0, 0)
	Talk("VoiceÅsìﬁèèÅt", "NO030042")
	Mess("...Are you all right?")
	hitretResult := Hitret(10815)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B20a", 0, 0)
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("At that time, she had the same face.")
	hitretResult := Hitret(10816)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_b001d")
	Return
	Change("EXIT_SCENARIO")
EndFunc

