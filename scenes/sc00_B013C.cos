Include"yosuga.ch"

Function sc00_B013C()
	ScenarioEnter()
	Talk("S‚Ìº", "")
	Mess("We went around the Shinto dance as we're about to help^as scene shifters.")
	hitretResult := Hitret(13245)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B07a", 0, 0)
	Transition("WIP_RL", 500)
	Talk("S‚Ìº", "")
	Mess("\"Shinto dance\" huh, does Amatsume always dance this?")
	hitretResult := Hitret(13246)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("There's a signboard along the wing of the stage.")
	hitretResult := Hitret(13247)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("\"Founding of the New Land\", it says.")
	hitretResult := Hitret(13248)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What's that?")
	hitretResult := Hitret(13249)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH030277")
	Mess("Who knows? You can see it right?")
	hitretResult := Hitret(13250)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Amatsume looked great when she came out right?")
	hitretResult := Hitret(13251)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030278")
	Mess("That girl... yeah, though...")
	hitretResult := Hitret(13252)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("S‚Ìº", "")
	Mess("We were at the back of the Shinto dance, then we took^out some tools from the box to unfasten the lines, then^the speaker went ahead of us.")
	hitretResult := Hitret(13253)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("And, at that time...")
	hitretResult := Hitret(13254)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM21", 0)
	Talk("S‚Ìº", "")
	Mess("Don-don-don-bang! The sound of the gongs, the Shinto^dance started as everyone clapped their hands.")
	hitretResult := Hitret(13255)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B29a", 0, 0)
	Transition("WIP_LR", 500)
	Talk("S‚Ìº", "")
	Mess("The only image I had in mind about the Shrine dance is^that the shrine maiden hops and dances around along with^the music.")
	hitretResult := Hitret(13256)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("In the end, she'll wave her holy rod to purify everyone.")
	hitretResult := Hitret(13257)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I really don't understand, but I caved in and secluded^those feelings as the Gods will dance in front of us, I^went on even though I have no information about it.")
	hitretResult := Hitret(13258)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("But, we were busy preparing for the story of the Shinto^dance.")
	hitretResult := Hitret(13259)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Amatsume hopped at the beginning, then every guy was^wearing a scary mask, I think everyone was shocked in^the beginning.")
	hitretResult := Hitret(13260)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("And then, they went to the persons who were eating and^drinking and took them, as if they're implementing^discipline as the show went on.")
	hitretResult := Hitret(13261)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("That made me worried, Amatsume might feel bad for those^people if she sees them, the group of guys held them^down as they disciplined them.")
	hitretResult := Hitret(13262)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Amatsume steps on her skirt some times, though she's^doing her best as she recieved tons of cheering from the^crowd, then she answered them with a smile as she^continued to dance.")
	hitretResult := Hitret(13263)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("And then, the locals offered their gratitude as Amatsume^fluttered around in the end, two of them even gave her a^basket.")
	hitretResult := Hitret(13264)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("In the middle of the Shinto dance, we also joined the^gallery.")
	hitretResult := Hitret(13265)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Even Nao-chan took a pause on her job, she watched us as^we stand beside the Shinto dancers.")
	hitretResult := Hitret(13266)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The Shinto dance has different plays, and I just noticed^that right now.")
	hitretResult := Hitret(13267)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Because the shrine's main foundation after all is^talking to its God.")
	hitretResult := Hitret(13268)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("S‚Ìº", "")
	Mess("In the middle of the Shinto dance earlier, the locals^were calling out to Sayohime-sama, which is probably^acted out by Amatsume.")
	hitretResult := Hitret(13269)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM03", 0)
	SetCg("B07a", 0, 0)
	SetBustup("CB03_02M", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Nao", "NO030590")
	Mess("We didn't even know she's practiced that much right?")
	hitretResult := Hitret(13270)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's right... with that attitude, we really didn't^notice it.")
	hitretResult := Hitret(13271)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, even though she stumbled at some parts, we can say^that it's really Amatsume.")
	hitretResult := Hitret(13272)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO030591")
	Mess("Hehehe, that's right.")
	hitretResult := Hitret(13273)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well then, we need to tidy up now, we came here to help^as soon as it's finished after all.")
	hitretResult := Hitret(13274)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030592")
	Mess("I see, nice job.")
	hitretResult := Hitret(13275)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan, are you guys still going to be busy?")
	hitretResult := Hitret(13276)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO030593")
	Mess("I see... hmm`, after the Shinto dance ends, everyone^would be going drinking tea, so maybe a little.")
	hitretResult := Hitret(13277)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Should I also help?")
	hitretResult := Hitret(13278)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030594")
	Mess("It's fine, you can also go around the festival if you^want? Then I'll just catch up right after.")
	hitretResult := Hitret(13279)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I got it, see you later then. I'll be around here.")
	hitretResult := Hitret(13280)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO030595")
	Mess("Yeah, let's go around together.")
	hitretResult := Hitret(13281)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	Talk("Haruka", "")
	Mess("Now then, I should also go and tidy up...")
	hitretResult := Hitret(13282)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I didn't know how the festival goes. Helping other^people and cooperating with them.")
	hitretResult := Hitret(13283)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Since I was a scene-shifter today, it looks like I^learned a little from it.")
	hitretResult := Hitret(13284)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("If it's always like this, I may meet more people around^the stalls as they visit, the festival also gave me a^satisifed feeling, I won't ever forget this experience.")
	hitretResult := Hitret(13285)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Though I feel so tired now...")
	hitretResult := Hitret(13286)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("", 0)
	Change("00_b013d")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

