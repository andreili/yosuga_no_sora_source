Include"yosuga.ch"

Function sc00_A018B()
	ScenarioEnter()
	SetCg("B27a", 0, 600)
	ScPlayBgm("BGM16", 0)
	Talk("êSÇÃê∫", "")
	Mess("After we made our second shopping trip, and thenÅ^finished lunch, I went outside alone.")
	hitretResult := Hitret(6221)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Scroll(0, 0, 25000, 1, 0)
	Talk("êSÇÃê∫", "")
	Mess("Entrusting Sora with the laundry, it was like I wasÅ^secretly running away.")
	hitretResult := Hitret(6222)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Because I had a feeling that... surely, if I told her,Å^she'd come along...")
	hitretResult := Hitret(6223)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...*sigh*.")
	hitretResult := Hitret(6224)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Passing the Bon Festival, the rays of the sun are evenÅ^stronger.")
	hitretResult := Hitret(6225)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I was starting to get sunburnt, I still staggered asÅ^I walked around without aim.")
	hitretResult := Hitret(6226)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wonder what I'll do, from now on...")
	hitretResult := Hitret(6227)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's... suddenly starting to change.")
	hitretResult := Hitret(6228)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I think that without doubt... last night was whatÅ^started it.")
	hitretResult := Hitret(6229)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just, Sora's love, and my love, are different...")
	hitretResult := Hitret(6230)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("How should we face each other...")
	hitretResult := Hitret(6231)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I... love Sora too. That feeling is... without lie.")
	hitretResult := Hitret(6232)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We spent time together, as family, as siblings, whichÅ^made me able to feel that bond.")
	hitretResult := Hitret(6233)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wonder if Sora still views me as a special person toÅ^her, one who knows everything about her.")
	hitretResult := Hitret(6234)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Of course, she's happy when I praise her for being cute,Å^and though we're both equal, there's still the issueÅ^about being immersed in a superiority complex.")
	hitretResult := Hitret(6235)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, I never saw Sora, my family, crossing the line.")
	hitretResult := Hitret(6236)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As someone close to her, even if she's someone whoseÅ^cuteness attracts me... as family, as a sibling, I neverÅ^considered it more than that.")
	hitretResult := Hitret(6237)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, Sora's different. Without a doubt, she sees me asÅ^someone other than just family.")
	hitretResult := Hitret(6238)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And, that's probably not something that just happenedÅ^yesterday.")
	hitretResult := Hitret(6239)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("For some reason, I have that feeling...")
	hitretResult := Hitret(6240)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("VoiceÅsâlÅt", "AK010218")
	Mess("AhhÅ`! Haru-kun, discovered-!")
	hitretResult := Hitret(6241)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B12a", 0, 0)
	Talk("Haruka", "")
	Mess("Nn...?")
	hitretResult := Hitret(6242)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM03", 0)
	Talk("êSÇÃê∫", "")
	Mess("The shrine maiden riding a bike made her grand entranceÅ^next to me.")
	hitretResult := Hitret(6243)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE254", 0)
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK010219")
	Mess("Haru-kun, helloÅ`.")
	hitretResult := Hitret(6244)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, Amatsume.")
	hitretResult := Hitret(6245)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010220")
	Mess("Hehe... Ah, today I'm not alone.")
	hitretResult := Hitret(6246)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Eh?")
	hitretResult := Hitret(6247)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_07M", 0, 0)
	Talk("VoiceÅsàœàıí∑Åt", "KO010119")
	Mess("Ka...Kasugano-kun, helloÅ`.")
	hitretResult := Hitret(6248)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Behind me, I could hear a voice I hadn't heard in aÅ^while.")
	hitretResult := Hitret(6249)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh, Chairman... and, Sora?")
	hitretResult := Hitret(6250)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I turned around, I saw Chairman... but she had someÅ^unexpected luggage.")
	hitretResult := Hitret(6251)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE255", 0)
	SetBustup("CA02_02M", 0, 0)
	Talk("Sora", "SR010696")
	Mess("Thanks, Chairman.")
	hitretResult := Hitret(6252)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010120")
	Mess("Fuu, haa... No, it's good you found him, Sora-san.")
	hitretResult := Hitret(6253)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora descended down the back of the chairman's bicycle.")
	hitretResult := Hitret(6254)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010697")
	Mess("Haru, you suddenly disappeared, so I was worried.")
	hitretResult := Hitret(6255)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The vague criticism I could sense in her voice... wasÅ^probably just from my guilty conscience.")
	hitretResult := Hitret(6256)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... Well, I felt like I'd ate too much, so I went on aÅ^walk to help with the digestion...")
	hitretResult := Hitret(6257)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010698")
	Mess("Fuuun... Then, you can have the watermelon later forÅ^desert.")
	hitretResult := Hitret(6258)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_14M", 0, 0)
	Talk("Akira", "AK010221")
	Mess("Watermelon, huh... It's the most delicious nowÅ`.")
	hitretResult := Hitret(6259)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Without caring that Amatsume or the chairman were here,Å^Sora nestled close to me.")
	hitretResult := Hitret(6260)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She pulled tightly on my cuff, as if she was never goingÅ^to let me escape again.")
	hitretResult := Hitret(6261)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_02M", 0, 0)
	Talk("Class Rep", "KO010121")
	Mess("You've become something other than the sea, right? HaveÅ^you been healthy?")
	hitretResult := Hitret(6262)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, yeah, I guess that's right... Long time. Anyway,Å^Chairman and Amatsume is an unusual pair.")
	hitretResult := Hitret(6263)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK010222")
	Mess("Umm, well my grandpa, the shrine parishioner left, andÅ^she was helping me clean the storage. We're just now onÅ^our way back.")
	hitretResult := Hitret(6264)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010122")
	Mess("When I went to go shopping, I unexpectedly ran intoÅ^Amatsume-san.")
	hitretResult := Hitret(6265)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010223")
	Mess("Yep, yep, and then afterwards we ran into Sora-chan.")
	hitretResult := Hitret(6266)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh, really...?")
	hitretResult := Hitret(6267)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010123")
	Mess("Yep, but I never thought she would call out to us. ItÅ^was the first time I saw Sora-chan like that, but it wasÅ^kind of interesting.")
	hitretResult := Hitret(6268)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("'Like that'... Like what...?")
	hitretResult := Hitret(6269)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010124")
	Mess("Panicking, maybe a little bit angry...")
	hitretResult := Hitret(6270)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_11M", 0, 0)
	Talk("Sora", "SR010699")
	Mess("Ch-Chairman...!")
	hitretResult := Hitret(6271)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010125")
	Mess("Fufu, Im sorry. But, I was... able to meetÅ^Kasogano-kun...")
	hitretResult := Hitret(6272)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_07M", 0, 0)
	SetBustup("CA02_08M", 0, 0)
	Talk("Sora", "SR010700")
	Mess("......")
	hitretResult := Hitret(6273)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see... but Chairman, with the two of you riding in theÅ^middle of this heat... are you okay?")
	hitretResult := Hitret(6274)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010126")
	Mess("Eh, ah, y-yes. This much was no problem.")
	hitretResult := Hitret(6275)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK010224")
	Mess("When it's this hot, I want to go to the beach or theÅ^poolÅ`.")
	hitretResult := Hitret(6276)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("For sure. When it's as hot as today, I bet the poolÅ^would feel good...")
	hitretResult := Hitret(6277)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 3, 0)
	SetBustup("CH02_07M", 6, 0)
	SetBustup("CA02_06L", 10, 0)
	Talk("Sora", "SR010701")
	Mess("Haru, let's go home? At this rate, you'll getÅ^heatstroke.")
	hitretResult := Hitret(6278)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Without hiding her dissatisfaction, Sora tugged on myÅ^sleeve.")
	hitretResult := Hitret(6279)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay. Amatsume, Chairman, sorry. Thanks for accompanyingÅ^Sora.")
	hitretResult := Hitret(6280)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_08M", 0, 0)
	Talk("Class Rep", "KO010127")
	Mess("Ah, s-sorry, I went on a rant.")
	hitretResult := Hitret(6281)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No worries. Though for letting Sora ride with you, I'mÅ^worried you're the one that'll get heatstroke.")
	hitretResult := Hitret(6282)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_07M", 0, 0)
	Talk("Class Rep", "KO010128")
	Mess("Ah, yeah...")
	hitretResult := Hitret(6283)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010702")
	Mess("......")
	hitretResult := Hitret(6284)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, right. Amatsume and Chairman, do you likeÅ^watermelon?")
	hitretResult := Hitret(6285)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_02M", 0, 0)
	Talk("Class Rep", "KO010129")
	Mess("Eh? Yeah, I usually like it...")
	hitretResult := Hitret(6286)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03M", 0, 0)
	Talk("Akira", "AK010225")
	Mess("W--Watermelon? Yeah, I love it.")
	hitretResult := Hitret(6287)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, well we actually just had a piece, but between theÅ^two of us we can't finish it, so would you want to takeÅ^about half of it?")
	hitretResult := Hitret(6288)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010130")
	Mess("Eh... Is it okay if I do?")
	hitretResult := Hitret(6289)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Of course. Though, you'll need to come to our house...")
	hitretResult := Hitret(6290)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_07M", 0, 0)
	Talk("Class Rep", "KO010131")
	Mess("Th--That's completely okay! I--In fact, I'd beÅ^delighted...!")
	hitretResult := Hitret(6291)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK010226")
	Mess("Thanks! Uwaa, I can't wait. Watermelon, watermelonnn!")
	hitretResult := Hitret(6292)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha, well it's good that you're happy...")
	hitretResult := Hitret(6293)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04L", 0, 0)
	Talk("Sora", "SR010703")
	Mess("...Grr......")
	hitretResult := Hitret(6294)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What's wrong, pouting like that...")
	hitretResult := Hitret(6295)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010704")
	Mess("Even though I said we should hurry up and go home......")
	hitretResult := Hitret(6296)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hey, hey, we should at least thank Chairman for lettingÅ^you ride with her, right?")
	hitretResult := Hitret(6297)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010705")
	Mess("......Dummy.")
	hitretResult := Hitret(6298)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	SetCg("B01a", 0, 0)
	Talk("Haruka", "")
	Mess("Hey Sora, can you do me a favor and slice it in half?")
	hitretResult := Hitret(6299)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_05M", 0, 0)
	Talk("Sora", "SR010706")
	Mess("Okay...")
	hitretResult := Hitret(6300)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK010227")
	Mess("Sorry, for making you do all that...")
	hitretResult := Hitret(6301)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, it's no problem.")
	hitretResult := Hitret(6302)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_02M", 0, 0)
	Talk("Class Rep", "KO010132")
	Mess("It doesn't have to be that big. I like it, but it's notÅ^like I could eat that much.")
	hitretResult := Hitret(6303)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010228")
	Mess("I love watermelon, so no matter how much... I'm sureÅ^Master would be pleased as well.Åô")
	hitretResult := Hitret(6304)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Master? ...Oh, the cat that was in the shrine?")
	hitretResult := Hitret(6305)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK010229")
	Mess("Yep, that's right. You met him before, right?")
	hitretResult := Hitret(6306)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, yeah... Can a cat eat watermelon?")
	hitretResult := Hitret(6307)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010230")
	Mess("Master pretty much eats anything.")
	hitretResult := Hitret(6308)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010133")
	Mess("A cat eating watermelon... I've never seen that before.")
	hitretResult := Hitret(6309)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("True... It's amazing, the kind of strange things it'llÅ^eat.")
	hitretResult := Hitret(6310)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_06M", 0, 0)
	Talk("Akira", "AK010231")
	Mess("Really? Though I don't think that's true...")
	hitretResult := Hitret(6311)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Slanting her head, Amatsume looked in the direction ofÅ^the house.")
	hitretResult := Hitret(6312)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK010232")
	Mess("Ah, by the way, Haru-kunÅ`. It's been bugging me aÅ^little, but Sora-chan......")
	hitretResult := Hitret(6313)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh...")
	hitretResult := Hitret(6314)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Suddenly, Amatsume glanced at Sora.")
	hitretResult := Hitret(6315)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Unexpectedly, suddenly felt shocked... Maybe she noticedÅ^that Sora's been acting differently.")
	hitretResult := Hitret(6316)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I can't really explain it, but I felt kind of uneasy...")
	hitretResult := Hitret(6317)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM13", 0)
	SetBustup("CC06_02M", 0, 0)
	SetBustup("CH02_05M", 0, 0)
	Talk("Akira", "AK010233")
	Mess("...She gained a little weight?")
	hitretResult := Hitret(6318)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh...I-It was about that?")
	hitretResult := Hitret(6319)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From Amatsume's mouth, came the biggest anticlimax ever.")
	hitretResult := Hitret(6320)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_07M", 0, 0)
	Talk("Class Rep", "KO010134")
	Mess("I don't think that's true... If Sora-chan's fat... thenÅ^that means I'm... Aaahh...")
	hitretResult := Hitret(6321)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Down(9, 0, 0, 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("For some reason, Chairman started to get depressed.")
	hitretResult := Hitret(6322)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010234")
	Mess("Aha, I think you're normal thoughÅ`.")
	hitretResult := Hitret(6323)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Down(9, -1, 0, 0)
	SetBustup("CH02_09M", 0, 0)
	Talk("Class Rep", "KO010135")
	Mess("R--Really...? When it became break, I just had to haveÅ^sweets... It's scary not paying attention to myÅ^weight...")
	hitretResult := Hitret(6324)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I don't think you put on weight either. The thoughtÅ^never occurred to me when we went to the beach.")
	hitretResult := Hitret(6325)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_07M", 0, 0)
	Talk("Class Rep", "KO010136")
	Mess("Ehh, i--it's good if you say that, but...")
	hitretResult := Hitret(6326)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK010235")
	Mess("AhahaÅ`, Chairman is just shy.")
	hitretResult := Hitret(6327)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_01M", 0, 0)
	Talk("Class Rep", "KO010137")
	Mess("A--Amatsume-san...!")
	hitretResult := Hitret(6328)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha... Well, I think that's just your imagination. Ah,Å^and please don't tell Sora about that. She'll probablyÅ^obsess over it.")
	hitretResult := Hitret(6329)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK010236")
	Mess("Okay, got itÅ`.")
	hitretResult := Hitret(6330)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_08M", 0, 0)
	Talk("Class Rep", "KO010138")
	Mess("Y--Yes. That's true...")
	hitretResult := Hitret(6331)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("......")
	hitretResult := Hitret(6332)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM03", 0)
	Talk("êSÇÃê∫", "")
	Mess("When Amatsume first mentioned Sora, my back was sweatingÅ^for a moment, but I guess I was just needlesslyÅ^worrying.")
	hitretResult := Hitret(6333)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Now that I think about it, Amatsume usually doesn't payÅ^attention to things like that.")
	hitretResult := Hitret(6334)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_07M", 0, 0)
	Talk("Class Rep", "KO010139")
	Mess("Ah, Kusugano-kun... I--I have a small favor to ask,Å^but... umm, well...")
	hitretResult := Hitret(6335)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...? What?")
	hitretResult := Hitret(6336)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010140")
	Mess("I--I was wondering... if I can use the restroom...")
	hitretResult := Hitret(6337)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? Ah, s--sure. When you go inside, it's right alongÅ^the hallway.")
	hitretResult := Hitret(6338)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010141")
	Mess("S--Sorry......")
	hitretResult := Hitret(6339)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(9)
	Talk("êSÇÃê∫", "")
	Mess("Embarressed, Chairman goes inside the house.")
	hitretResult := Hitret(6340)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Well, she's a girl after all.. So asking those kinds ofÅ^things might be embarrassing.")
	hitretResult := Hitret(6341)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK010237")
	Mess("...Chairman has it hard as well.")
	hitretResult := Hitret(6342)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh...? If it's about the restroom... I don't reallyÅ^mind.")
	hitretResult := Hitret(6343)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Honestly, I have a little sister, so saying things aboutÅ^the restroom or the bath doesn't particularly bug me.")
	hitretResult := Hitret(6344)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If it did bug me, there'd be no end to it.")
	hitretResult := Hitret(6345)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK010238")
	Mess("Ahaha, not about that.")
	hitretResult := Hitret(6346)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...? What's that?")
	hitretResult := Hitret(6347)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	Talk("Akira", "AK010239")
	Mess("Anyways...")
	hitretResult := Hitret(6348)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("For an instant, the atmosphere changed. I had thatÅ^feeling.")
	hitretResult := Hitret(6349)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM08", 0)
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK010240")
	Mess("I know about Sora-chan... and Haru-kun.")
	hitretResult := Hitret(6350)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh...?")
	hitretResult := Hitret(6351)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010241")
	Mess("So, that's why you can always feel at ease, and be byÅ^her side.")
	hitretResult := Hitret(6352)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I turned toward Amatsume without realizing it.")
	hitretResult := Hitret(6353)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, there was no change there, just her usual smilingÅ^face.")
	hitretResult := Hitret(6354)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK010242")
	Mess("It'd be okay for you to be honest as well, aboutÅ^Sora-chan's feelings.")
	hitretResult := Hitret(6355)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Tone("MONOCHROME")
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(6356)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What...... What was that?")
	hitretResult := Hitret(6357)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It felt like Amatsume's words resounded not in my ears,Å^but directly in my head.")
	hitretResult := Hitret(6358)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just what was... Amatsume saying...?")
	hitretResult := Hitret(6359)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010243")
	Mess("......")
	hitretResult := Hitret(6360)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if she could see straight through me, Amatsume justÅ^smiled quietly.")
	hitretResult := Hitret(6361)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Be honest about... Sora's feelings?")
	hitretResult := Hitret(6362)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Tone("")
	ScPlayBgm("BGM05", 0)
	SetBustup("CH02_02M", 0, 0)
	Talk("Class Rep", "KO010142")
	Mess("*sigh*... Ah, Kasugano-kun, thank y-- Huh, what's wrong?")
	hitretResult := Hitret(6363)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It felt like... the instant I heard Chairman's voice, IÅ^instantly returned to reality.")
	hitretResult := Hitret(6364)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK010244")
	Mess("Ah, Chairman, you took a whileÅ`.")
	hitretResult := Hitret(6365)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_05M", 0, 0)
	Talk("Class Rep", "KO010143")
	Mess("Eh, y--you're wrong, I just dropped by the kitchen for aÅ^bit. It's not that I was in the restroom for a while...!")
	hitretResult := Hitret(6366)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_02M", 0, 0)
	Talk("Sora", "SR010707")
	Mess("...Sorry for the wait.")
	hitretResult := Hitret(6367)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora appeared behind the flustered Chairman.")
	hitretResult := Hitret(6368)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03M", 0, 0)
	Jump(2, 0)
	Talk("Akira", "AK010245")
	Mess("Uwaaoh! The watermelon is amazing-!")
	hitretResult := Hitret(6369)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_02M", 0, 0)
	SetBustup("CA02_10M", 0, 0)
	Talk("Sora", "SR010708")
	Mess("...But, can you eat all this?")
	hitretResult := Hitret(6370)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The watermelon Sora carried was sliced into three piecesÅ^and wrapped up.")
	hitretResult := Hitret(6371)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks, Sora. Chairman, Amatsume, is this much okay?")
	hitretResult := Hitret(6372)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK010246")
	Mess("Yep, it's plenty, it's plentyÅ`. If I eat it withÅ^Kazu-chan and Hiro-neechan, there will even be enoughÅ^left for Master.")
	hitretResult := Hitret(6373)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010144")
	Mess("I know, right? This is the perfect amount for my wholeÅ^family.")
	hitretResult := Hitret(6374)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_02M", 0, 0)
	Talk("Sora", "SR010709")
	Mess("...It'd probably be better to eat it before it getsÅ^lukewarm.")
	hitretResult := Hitret(6375)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010247")
	Mess("Yep, that's trueÅ`.")
	hitretResult := Hitret(6376)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010145")
	Mess("Right? It'd probably go bad in the heat.")
	hitretResult := Hitret(6377)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Amatsume and Chairman each took a watermelon, and putÅ^them carefully on their respective bicycles.")
	hitretResult := Hitret(6378)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK010248")
	Mess("Thanks...! I'll eat it valuablyÅ`.")
	hitretResult := Hitret(6379)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_03M", 0, 0)
	Talk("Class Rep", "KO010146")
	Mess("Sorry for intruding.")
	hitretResult := Hitret(6380)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No problem. Feel free to drop by whenever you get aÅ^chance.")
	hitretResult := Hitret(6381)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH02_07M", 0, 0)
	Talk("Class Rep", "KO010147")
	Mess("O--Okay.")
	hitretResult := Hitret(6382)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010249")
	Mess("Well then, see yaÅ`...!")
	hitretResult := Hitret(6383)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("While Amatsume jumped on her bicycle while running,Å^Chairman slowly pedaled as she started to take off.")
	hitretResult := Hitret(6384)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Each turned once while waving their hands, and sped upÅ^until they went out of sight.")
	hitretResult := Hitret(6385)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_03L", 0, 0)
	Talk("Sora", "SR010710")
	Mess("Haru, it's hot, so let's go inside?")
	hitretResult := Hitret(6386)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("Okay...")
	hitretResult := Hitret(6387)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora softly tugged on my arm... and like that, sheÅ^embraced it.")
	hitretResult := Hitret(6388)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM07", 0)
	SetCg("B02a", 0, 0)
	SetBustup("CA02_10L", 0, 0)
	Talk("Sora", "SR010711")
	Mess("Haru...")
	hitretResult := Hitret(6389)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nn... Sora, it's hard to walk...")
	hitretResult := Hitret(6390)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010712")
	Mess("...You won't go anywhere?")
	hitretResult := Hitret(6391)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... I won't go anywhere without saying anything.")
	hitretResult := Hitret(6392)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_02L", 0, 0)
	Talk("Sora", "SR010713")
	Mess("Fufu... Okay...")
	hitretResult := Hitret(6393)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Hearing those words, Sora finally let my arm go.")
	hitretResult := Hitret(6394)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, this time she intently gazed at me.")
	hitretResult := Hitret(6395)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her eyes seem like they're expecting something.")
	hitretResult := Hitret(6396)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Those honest, gazing eyes... I couldn't understand them.")
	hitretResult := Hitret(6397)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well then... Let's go do homework or something...")
	hitretResult := Hitret(6398)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I averted my eyes.")
	hitretResult := Hitret(6399)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010714")
	Mess("Then, if you don't understand something... I'll teachÅ^you.")
	hitretResult := Hitret(6400)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? I'll be fine doing it by myself.")
	hitretResult := Hitret(6401)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_05L", 0, 0)
	Talk("Sora", "SR010715")
	Mess("...That's boring...")
	hitretResult := Hitret(6402)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("This time her eyes conveyed blame, or maybe petitioning.")
	hitretResult := Hitret(6403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora... for her eyes to convey something like this soÅ^clearly...")
	hitretResult := Hitret(6404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I couldn't understand them.")
	hitretResult := Hitret(6405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay... Then let's do it together...")
	hitretResult := Hitret(6406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_03M", 0, 0)
	Talk("Sora", "SR010716")
	Mess("...Okay.")
	hitretResult := Hitret(6407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("As if she never thought I would reject her, SoraÅ^returned to her room, looking pleased.")
	hitretResult := Hitret(6408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(6409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If I... turned her down or something... I wonder whatÅ^would have happened.")
	hitretResult := Hitret(6410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I couldn't read her response...... No, I just didn'tÅ^want to know.")
	hitretResult := Hitret(6411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(2000, 0)
	Change("00_a018c")
	Return
	Change("EXIT_SCENARIO")
EndFunc

