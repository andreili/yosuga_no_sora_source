Include"yosuga.ch"

Function sc00_D043()
	ScenarioEnter()
	SetCg("B27a", 0, 300)
	ScPlayBgm("BGM20", 0)
	WindowView(1)
	Talk("モノローグ《一葉》", "KA041504")
	Mess("While I was having sex with Haruka, I confronted my own／feelings.")
	hitretResult := Hitret(33174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041505")
	Mess("I love Haruka, but I cherish Akira as well...")
	hitretResult := Hitret(33175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041506")
	Mess("I want to make it up to Akira.")
	hitretResult := Hitret(33176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041507")
	Mess("But how should I approach her? I'm in a dilemma.")
	hitretResult := Hitret(33177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041508")
	Mess("First I have to apologize. but what about after that?")
	hitretResult := Hitret(33178)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041509")
	Mess("But... I'm full of \"buts.\"")
	hitretResult := Hitret(33179)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041510")
	Mess("It's probably because of my cowardice that I've been／running away from my problems for all this time.")
	hitretResult := Hitret(33180)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041511")
	Mess("I wish I could be like Akira, who is brave and daring...／or like Haruka...")
	hitretResult := Hitret(33181)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yamanashi", "YM040003")
	Mess("Ojou-sama.")
	hitretResult := Hitret(33182)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B09a", 0, 0)
	SetBustup("CJ01_02M", 0, 0)
	Talk("Kazuha", "KA041512")
	Mess("What is it?")
	hitretResult := Hitret(33183)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yamanashi", "YM040004")
	Mess("If you aren't feeling well, then I can escort you to／school.")
	hitretResult := Hitret(33184)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042001")
	Mess("Sensing my doubt, Yamanoshi-san stopped wiping the car.")
	hitretResult := Hitret(33185)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041513")
	Mess("Thank you, but that's okay.／Besides, you have to chauffeur my father, right?")
	hitretResult := Hitret(33186)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042002")
	Mess("However, I can't let them spoil me like that.")
	hitretResult := Hitret(33187)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042003")
	Mess("Haruka taught me to face my own problems.")
	hitretResult := Hitret(33188)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yamanashi", "YM040005")
	Mess("If you say so, Ojou-sama.")
	hitretResult := Hitret(33189)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(10)
	Talk("モノローグ《一葉》", "KA042004")
	Mess("Bowing once, he turned away and continued to clean the／car, as I continued to be lost in my thoughts.")
	hitretResult := Hitret(33190)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042005")
	Mess("After reaching the gate, I couldn't step any further.")
	hitretResult := Hitret(33191)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042006")
	Mess("If I stood here a bit longer, then I could reach the／school.... No... I could reach the classroom without／meeting Akira.")
	hitretResult := Hitret(33192)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042007")
	Mess("How pathetic can I be...?")
	hitretResult := Hitret(33193)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CJ01_01M", 0, 0)
	Talk("Yamanashi", "YM040006")
	Mess("Ojou-sama... Are you sure you don't...")
	hitretResult := Hitret(33194)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041514")
	Mess("No, so...")
	hitretResult := Hitret(33195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Voice《悠》", "")
	Mess("Oiii, Kazuha!!")
	hitretResult := Hitret(33196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042008")
	Mess("A voice cut into what would have been a hard-to-refuse／offer to drive me to school...")
	hitretResult := Hitret(33197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_12M", 0, 0)
	Talk("Kazuha", "KA041515")
	Mess("...Haruka?")
	hitretResult := Hitret(33198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042009")
	Mess("As I stood there, I was bewildered by Haruka's sudden／appearance as he strolled over to join me by the gate.")
	hitretResult := Hitret(33199)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Good morning, Kazuha.")
	hitretResult := Hitret(33200)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041516")
	Mess("G-Good morning... What are you doing here?")
	hitretResult := Hitret(33201)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What am I... Oh! I came to pick you up. I thought that／maybe we could head to school together.")
	hitretResult := Hitret(33202)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042010")
	Mess("And then he gently offered me his hand.")
	hitretResult := Hitret(33203)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042011")
	Mess("This man would always stand by me, supporting me. This／is the man I would entrust myself to...")
	hitretResult := Hitret(33204)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042012")
	Mess("Without a moment of doubt, I knew I wanted to take the／hand of the person I loved.")
	hitretResult := Hitret(33205)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_08M", 0, 0)
	Talk("モノローグ《一葉》", "KA042013")
	Mess("But the moment I reached out my hand, I wondered if／Haruka would change into Akira, and brush my hand aside,／just like how I rejected her...")
	hitretResult := Hitret(33206)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Amatsume was seriously wishing for your happiness. She／has no reason at all to hate you.")
	hitretResult := Hitret(33207)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_12L", 0, 0)
	Talk("モノローグ《一葉》", "KA042014")
	Mess("As if seeing through my doubt, Haruka forcibly grabbed／my hand.")
	hitretResult := Hitret(33208)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042015")
	Mess("It was obvious, but since Haruka was Haruka, there was／no way he'd brush my hand aside.")
	hitretResult := Hitret(33209)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("See?")
	hitretResult := Hitret(33210)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_11L", 0, 0)
	Talk("Kazuha", "KA041517")
	Mess("Ah...")
	hitretResult := Hitret(33211)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B27a", 0, 0)
	Talk("モノローグ《一葉》", "KA042016")
	Mess("In the direction that Haruka was pointing toward...")
	hitretResult := Hitret(33212)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040486")
	Mess("Kaaazu-chaaan! Haaru-kuuun!!")
	hitretResult := Hitret(33213)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042017")
	Mess("...was the spirited Akira, with her everlasting smile on／her face, waving her hand.")
	hitretResult := Hitret(33214)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042018")
	Mess("And next to her, was Sora-san with her usual grouchy and／sullen expression.")
	hitretResult := Hitret(33215)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042019")
	Mess("Indeed, nothing has changed.")
	hitretResult := Hitret(33216)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Let's go!")
	hitretResult := Hitret(33217)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042020")
	Mess("Led by Haruka, who started to run toward those two, I／also ran.")
	hitretResult := Hitret(33218)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA042021")
	Mess("Behind us, Yamanoshi-san made a wry smile, as if to say,／\"I guess that the car wasn't necessary.\"")
	hitretResult := Hitret(33219)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(2000, 1)
	WindowView(0)
	SetCg("B12a", 0, 0)
	SetBustup("CA01_04S", 0, 0)
	SetBustup("CC01_02S", 0, 0)
	SetBustup("CD01_08S", 0, 0)
	Talk("Haruka", "")
	Mess("Amatsume, sorry to make you wait.")
	hitretResult := Hitret(33220)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040487")
	Mess("Ahahaa, that's okay, Haru-kun.")
	hitretResult := Hitret(33221)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041518")
	Mess(".....Ah....umm....")
	hitretResult := Hitret(33222)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040488")
	Mess("Good morning, Kazu-chan.")
	hitretResult := Hitret(33223)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041519")
	Mess("G...Good morning...")
	hitretResult := Hitret(33224)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040489")
	Mess("Ahaha, looks like you two have made up! Good for you!")
	hitretResult := Hitret(33225)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's all thanks to you!")
	hitretResult := Hitret(33226)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041520")
	Mess("A....Akira...I....")
	hitretResult := Hitret(33227)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Kazuha had a strained expression.... She seemed to be／tensing up. Or was this anxiety...?")
	hitretResult := Hitret(33228)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	SetBustup("CC01_02M", 4, 0)
	SetBustup("CD01_08M", 7, 0)
	Talk("心の声", "")
	Mess("Trying to encourage Kazuha, I grasped her hand tightly／as she slowly brought forth the courage to face Akira.")
	hitretResult := Hitret(33229)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040490")
	Mess("Mm, I understand. I understand how you feel. Thanks.")
	hitretResult := Hitret(33230)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041521")
	Mess("......")
	hitretResult := Hitret(33231)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Kazuha hung her head at Amatsume's apology.")
	hitretResult := Hitret(33232)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("But I knew that to Kazuha, it was like a load had been／taken off her chest.")
	hitretResult := Hitret(33233)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("And then she slowly raised her head, her eyes brimming／with tears.")
	hitretResult := Hitret(33234)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_09M", 0, 0)
	Talk("Kazuha", "KA041522")
	Mess("........Onee-chan... Sorry.... I'm sorry...")
	hitretResult := Hitret(33235)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Overcome with regret and anguish, she tearfully embraced／Amatsume.")
	hitretResult := Hitret(33236)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA01_13M", 0, 0)
	Talk("心の声", "")
	Mess("Amazed by the sudden change in atmosphere, Sora eyes／went wide with shock as she stood there, unable to say／anything. Her comical expression made me smirk to／myself.")
	hitretResult := Hitret(33237)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC01_11M", 4, 0)
	SetBustup("CD01_09M", 7, 0)
	Talk("Akira", "AK040491")
	Mess("They won't change... No matter what happens, our／feelings won't change. Not now, not never.")
	hitretResult := Hitret(33238)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041523")
	Mess("Mm, they won't. They won't change...")
	hitretResult := Hitret(33239)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("As I watched them in their deep embrace, I knew that／they'd finally been set free from the misery and／troubles that had been stored away in their hearts.")
	hitretResult := Hitret(33240)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("If they can solve their problems like this one after／another, then I'm sure that someday...")
	hitretResult := Hitret(33241)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_14M", 0, 0)
	Talk("Akira", "AK040492")
	Mess("Sorry if I'm disturbing your lovey-dovey reunion～")
	hitretResult := Hitret(33242)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_03M", 0, 0)
	Talk("Kazuha", "KA041524")
	Mess("I don't mind that much. We'll show you so much that／you'll get embarrassed!")
	hitretResult := Hitret(33243)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_10L", 3, 0)
	SetBustup("CD01_11L", 9, 0)
	Talk("Haruka", "")
	Mess("W-Wha!?")
	hitretResult := Hitret(33244)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Nodding deeply as if to say it was okay, she suddenly／locked her arms around ours.")
	hitretResult := Hitret(33245)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041525")
	Mess("Akira and Haruka! I'll love both of you so much that／you'll be jealous of each other!")
	hitretResult := Hitret(33246)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Holding one of our arms in each of hers, she declared／something outrageous.")
	hitretResult := Hitret(33247)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("But...")
	hitretResult := Hitret(33248)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_11L", 0, 0)
	Talk("Haruka", "")
	Mess("Yeah.")
	hitretResult := Hitret(33249)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("This is fine, I thought.")
	hitretResult := Hitret(33250)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA01_13M", 0, 0)
	Talk("Sora", "SR040143")
	Mess("....The two of you.....Hmm? Three of you...? Are going／out....???")
	hitretResult := Hitret(33251)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("And of course, Sora, being the one person who didn't／know what was going on, was flustered.")
	hitretResult := Hitret(33252)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041526")
	Mess("Hehe! Ahahahaha!")
	hitretResult := Hitret(33253)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahahaha!")
	hitretResult := Hitret(33254)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Infected by Kazuha's laughter, I also laughed.")
	hitretResult := Hitret(33255)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_14M", 4, 0)
	SetBustup("CA01_13M", 7, 0)
	Talk("Akira", "AK040493")
	Mess("I guess so? Next time, let's get in their way together,／Sora-chan!!")
	hitretResult := Hitret(33256)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Slipping free from Kazuha's arm, Amatsume went over to／link her arm around Sora's.")
	hitretResult := Hitret(33257)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Watching Sora as she stood there stunned and lost for／words was definitely a hilarious sight.")
	hitretResult := Hitret(33258)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("心の声", "")
	Mess("I wonder how long we'll be able to laugh from the bottom／of hearts, having this much fun for?")
	hitretResult := Hitret(33259)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("A laugh that is much more fulfilling than even the／happiness of being alone with Kazuha.")
	hitretResult := Hitret(33260)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("And besides, maybe's it's true that good fortune and／happiness will come to the homes of those who smile.")
	hitretResult := Hitret(33261)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE259", 1)
	ScPlaySe("SE262", 0)
	Talk("Voice《一葉の父》", "NP040038")
	Mess("It looks like you're having fun.")
	hitretResult := Hitret(33262)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CK01_04M", 0, 0)
	Talk("Kazuha", "KA041528")
	Mess("Father...")
	hitretResult := Hitret(33263)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha's Father", "NP040039")
	Mess("Good morning, everyone.")
	hitretResult := Hitret(33264)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Exiting the black car that was just parked next to us,／Kazuha's father greeted us with a warm smile.")
	hitretResult := Hitret(33265)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("That smile melted this moment of tension that we had／around us.")
	hitretResult := Hitret(33266)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("My intuition told me that something good was about to／happen again.")
	hitretResult := Hitret(33267)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha's Father", "NP040040")
	Mess("...Akira.")
	hitretResult := Hitret(33268)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_12M", 0, 0)
	Talk("Akira", "AK040494")
	Mess("Y-Yes!")
	hitretResult := Hitret(33269)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha's Father", "NP040041")
	Mess("In the near future... Can you come over to our place?")
	hitretResult := Hitret(33270)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040495")
	Mess("Eh.... Is that okay....?")
	hitretResult := Hitret(33271)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha's Father", "NP040042")
	Mess("Ahh, it's been a long time, and I've always wanted to／sit down and have a nice long chat with you. About／what's been done, and what will happen.")
	hitretResult := Hitret(33272)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_04M", 0, 0)
	Talk("Akira", "AK040496")
	Mess("Yes!")
	hitretResult := Hitret(33273)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha's Father", "NP040043")
	Mess("Okay. be careful on your way to school.")
	hitretResult := Hitret(33274)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("心の声", "")
	Mess("After watching Kazuha's father leave, we started walking／again.")
	hitretResult := Hitret(33275)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	SetBustup("CD01_12M", 0, 0)
	SetBustup("CA01_12M", 0, 0)
	Talk("心の声", "")
	Mess("I walked while holding Kazuha's hand, who still had an／expression of disbelief on her face.")
	hitretResult := Hitret(33276)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("She nodded vigorously.")
	hitretResult := Hitret(33277)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_11M", 0, 0)
	Talk("Kazuha", "KA041534")
	Mess("Akira....")
	hitretResult := Hitret(33278)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_11M", 0, 0)
	Talk("Akira", "AK040497")
	Mess("I've been invited again...to Kazu-chan's place.")
	hitretResult := Hitret(33279)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_09M", 0, 0)
	Talk("Kazuha", "KA041535")
	Mess("........I'm glad... I'm so glad....")
	hitretResult := Hitret(33280)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Kazuha was moved to tears and Amatsume was looking／bashful.")
	hitretResult := Hitret(33281)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("As I thought, what came from that was happiness.")
	hitretResult := Hitret(33282)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Voice《亮平》", "RH040364")
	Mess("Oohh, already so lively so early in the morn-...Whoa!?")
	hitretResult := Hitret(33283)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF01_06M", 0, 0)
	Talk("Ryouhei", "RH040365")
	Mess("What're you all doing linking arms...Grr... Especially／you Akira! Linking arms with my Sora-chan... That's／going too far!!")
	hitretResult := Hitret(33284)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK040498")
	Mess("Ahaha, you want to try too?")
	hitretResult := Hitret(33285)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH040366")
	Mess("Mm Mm! Let me in, too!!")
	hitretResult := Hitret(33286)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040144")
	Mess("Hiii! N-Nooo!....")
	hitretResult := Hitret(33287)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD01_11L", 0, 0)
	Talk("Kazuha", "KA041536")
	Mess("Haruka, let's go!")
	hitretResult := Hitret(33288)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Mm!")
	hitretResult := Hitret(33289)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("We ran off, holding hands.")
	hitretResult := Hitret(33290)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC01_03M", 0, 0)
	SetBustup("CA01_08M", 0, 0)
	SetBustup("CF01_10M", 0, 0)
	Talk("Akira", "AK040499")
	Mess("Ahhh, Kazu-chan! Wait up! Wait up～～!")
	hitretResult := Hitret(33291)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040145")
	Mess("D-Don't run off so suddenly!...")
	hitretResult := Hitret(33292)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040367")
	Mess("D-Don't leave me behind～～～")
	hitretResult := Hitret(33293)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD01_03L", 0, 0)
	Talk("心の声", "")
	Mess("At that moment, a light breeze blew in, and Kazuha's／hair began to blow with the wind.")
	hitretResult := Hitret(33294)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("As I watched, fascinated by the sparkling view, I began／to think...")
	hitretResult := Hitret(33295)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("From now on, I have to protect Kazuha, my dear sister,／and everyone else... My \"family.\"")
	hitretResult := Hitret(33296)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	StaffRoll(5)
	Change("00_d044")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

