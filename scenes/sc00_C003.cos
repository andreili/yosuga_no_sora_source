Include"yosuga.ch"

Function sc00_C003()
	ScenarioEnter()
	SetCg("EZ06a", 0, 0)
	Transition("WIP_LR", 500)
	Talk("Akira", "AK020099")
	Mess("It's a shame about Sora-chan, isn't it?")
	hitretResult := Hitret(18300)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, it can't be helped, it was a sudden transfer andÅ^all.")
	hitretResult := Hitret(18301)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020100")
	Mess("Poor Sora... I wonder how she's doing all alone?")
	hitretResult := Hitret(18302)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM03", 0)
	Talk("êSÇÃê∫", "")
	Mess("It was good up until she got to school, because ofÅ^number of people in each class, Sora ended up in aÅ^different class.")
	hitretResult := Hitret(18303)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora looked like she had just been betrayed when she wasÅ^taken by her homeroom teacher, she has probably been inÅ^bad mood since then.")
	hitretResult := Hitret(18304)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020101")
	Mess("After so much trouble, it would have been great if sheÅ^got to be in same class, right?")
	hitretResult := Hitret(18305)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH020016")
	Mess("Yeah, you're right! Now we can't have a mysteriousÅ^setting like "He's my senpai, but, for some reason, aÅ^classmate!", right?.")
	hitretResult := Hitret(18306)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Can't have, you say... If you tease her too much, she'llÅ^kick you.")
	hitretResult := Hitret(18307)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06d", 0, 0)
	Talk("Ryouhei", "RH020017")
	Mess("That's just fine! Pain and love, I'll take both of them!")
	hitretResult := Hitret(18308)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020102")
	Mess("Ryou-nii-chan, you really like Sora-chan, don't you?")
	hitretResult := Hitret(18309)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH020018")
	Mess("Yeah, she looks like a doll, so she's very cute. That'sÅ^why I like that part of Haruka's face too.")
	hitretResult := Hitret(18310)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uwaah... Don't go around saying things like that...")
	hitretResult := Hitret(18311)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06a", 0, 0)
	Talk("Ryouhei", "RH020019")
	Mess("Even though, she still won't be in our class, huh...")
	hitretResult := Hitret(18312)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06c", 0, 0)
	Talk("Kazuha", "KA020006")
	Mess("If that's so, I think that Nakazato-senpai should swapÅ^classes, that would be good.")
	hitretResult := Hitret(18313)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH020020")
	Mess("Oh, good idea... you say, but that defeats the purpose!")
	hitretResult := Hitret(18314)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020103")
	Mess("Eeh? If Ryou-nii-chan wasn't here, it would be lonely.")
	hitretResult := Hitret(18315)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06f", 0, 0)
	Talk("Ryouhei", "RH020021")
	Mess("Ooh, Akira. So you feel that strongly about me!!")
	hitretResult := Hitret(18316)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020007")
	Mess("Akira, the test is coming soon, so pay attention toÅ^class properly. Don't just follow senpai's bad example.")
	hitretResult := Hitret(18317)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH020022")
	Mess("It's okay, you know. If she won't understand something,Å^I'll teach.")
	hitretResult := Hitret(18318)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020008")
	Mess("No, thank you... I can see you in so called studyÅ^meeting starting with eating snacks.")
	hitretResult := Hitret(18319)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020009")
	Mess("If you think about Akira at all, please hold yourselfÅ^back a little.")
	hitretResult := Hitret(18320)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH020023")
	Mess("I know it. Akira and Ojou are all over each other, soÅ^she's just jealous.")
	hitretResult := Hitret(18321)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06e", 0, 0)
	Talk("Kazuha", "KA020010")
	Mess("I- I'm not all over her!!")
	hitretResult := Hitret(18322)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020104")
	Mess("Kazu-chan, you're being too loud.")
	hitretResult := Hitret(18323)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020011")
	Mess("Uuuu... And you're the one saying that?")
	hitretResult := Hitret(18324)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1500, 0)
	SetCg("B19a", 0, 0)
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH020024")
	Mess("Well then, Haruka, let's go to check on Sora-chan.")
	hitretResult := Hitret(18325)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, right... I'm worried a little too.")
	hitretResult := Hitret(18326)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA020012")
	Mess("Akira, can you come with me?")
	hitretResult := Hitret(18327)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK020105")
	Mess("Eh, what is it? Toilet?")
	hitretResult := Hitret(18328)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_13M", 0, 0)
	Shake(5)
	Talk("Kazuha", "KA020013")
	Mess("Don't say such things!!")
	hitretResult := Hitret(18329)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("While her face was getting all red, Migiwa-san tookÅ^Amatsume's hand and left clasroom.")
	hitretResult := Hitret(18330)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Migiwa-san has it rough too, huh... Having both AmatsumeÅ^and Ryouhei as company...")
	hitretResult := Hitret(18331)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH020025")
	Mess("Her reaction are amusing, she's always doing thingsÅ^unintentionally.")
	hitretResult := Hitret(18332)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("........ Why don't I feel any evil intent from you? But,Å^if you go too far, you'll start to be hated.")
	hitretResult := Hitret(18333)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH020026")
	Mess("I know when I'm just about to cross that line, so it'sÅ^okay.")
	hitretResult := Hitret(18334)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So you plan to tease her up until the point of overdoingÅ^it, huh........")
	hitretResult := Hitret(18335)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR020033")
	Mess("........ Haru...")
	hitretResult := Hitret(18336)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah...... Sora. You came just at the right time, we wereÅ^just thinking about going over there to see you.")
	hitretResult := Hitret(18337)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_04M", 0, 0)
	Talk("Ryouhei", "RH020027")
	Mess("Sora-chaan! I missed you! ...what the?")
	hitretResult := Hitret(18338)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(7)
	SetBustup("CA01_01L", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Sora slipped past Ryouhei and got really close, almostÅ^to the point of filling my field of vision entirely.")
	hitretResult := Hitret(18339)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("How is it? Can you live with it?")
	hitretResult := Hitret(18340)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020034")
	Mess("...... Impossible.")
	hitretResult := Hitret(18341)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh........")
	hitretResult := Hitret(18342)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_05L", 0, 0)
	Talk("Sora", "SR020035")
	Mess("........ I want to go home.")
	hitretResult := Hitret(18343)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Do- don't give up. At first, it might be hard, butÅ^eventually you'll get used to it.")
	hitretResult := Hitret(18344)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_06M", 0, 0)
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH020028")
	Mess("If you'd like, I can always come to play with you onÅ^breaks, okay?")
	hitretResult := Hitret(18345)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR020036")
	Mess("........ I don't need it.")
	hitretResult := Hitret(18346)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well it seem that we have combined PE together with yourÅ^class, so we'll be together from time to time.")
	hitretResult := Hitret(18347)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020037")
	Mess("........ Lies, boys and girls have it separately........")
	hitretResult := Hitret(18348)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH020029")
	Mess("That's why, in times like that, I'll gently watch overÅ^you.")
	hitretResult := Hitret(18349)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...... Ryo- Ryouhei........ I'm trying my best, but youÅ^seem to be having the opposite effect......")
	hitretResult := Hitret(18350)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_05M", 0, 0)
	Talk("Sora", "SR020038")
	Mess("........ Forget it.")
	hitretResult := Hitret(18351)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Next time, I'll come over there.")
	hitretResult := Hitret(18352)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_11M", 0, 0)
	Talk("Sora", "SR020039")
	Mess("................")
	hitretResult := Hitret(18353)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH020030")
	Mess("Sora-chan sure is cute, huh?")
	hitretResult := Hitret(18354)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ryouhei, your heart still hasn't quite broken yet, huh?Å^I'm really grateful.")
	hitretResult := Hitret(18355)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH020031")
	Mess("Idiooot, I'll look after her over and over and when I'llÅ^suddently get cold to her, then she'll be like "Why?".Å^Then, she'll realize how precious am I to her.")
	hitretResult := Hitret(18356)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("............ I'm thanking you seriously here.")
	hitretResult := Hitret(18357)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_08M", 0, 0)
	Talk("Ryouhei", "RH020032")
	Mess("I hope that she can make a friend she can get along withÅ^soon. If she only sticks close to you, it'll beÅ^tiresome.")
	hitretResult := Hitret(18358)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Ryouhei...")
	hitretResult := Hitret(18359)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH020033")
	Mess("Well, I want to understand the pressures of being a bigÅ^brother too.")
	hitretResult := Hitret(18360)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("........ Ryouhei, do you have a little sister orÅ^brother?")
	hitretResult := Hitret(18361)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH020034")
	Mess("Nah, I'm just a selfish only child.")
	hitretResult := Hitret(18362)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("........ So thats how it is? ........ But........")
	hitretResult := Hitret(18363)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_05M", 0, 0)
	Talk("Ryouhei", "RH020035")
	Mess("Just to say it, it's not like you're the older one.")
	hitretResult := Hitret(18364)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("........ Thanks, Ryouhei.")
	hitretResult := Hitret(18365)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1500, 0)
	ScPlaySe("SE352", 0)
	ScPlayBgm("BGM05", 0)
	SetCg("B19a", 0, 0)
	SetBustup("CC01_02M", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Akira", "AK020106")
	Mess("It's lunchtime Åô Shall we have a bento? Oh, and oneÅ^more thing, we have to call Sora-chan, right?")
	hitretResult := Hitret(18366)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah. I'll go buy some bread on the way.")
	hitretResult := Hitret(18367)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020107")
	Mess("Then, I'll go too~. Because today is the day for bread.Å^Ah, yes, I have to tell Sora-chan about the bread withÅ^fresh cream.")
	hitretResult := Hitret(18368)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA020014")
	Mess("I'll come too.")
	hitretResult := Hitret(18369)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020108")
	Mess("Huh? I'll come back right away, so you can start eatingÅ^without me.")
	hitretResult := Hitret(18370)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_07M", 0, 0)
	Talk("Kazuha", "KA020015")
	Mess("Let me keep you company. I don't want to lose myÅ^appetite before eating.")
	hitretResult := Hitret(18371)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_06M", 0, 0)
	Talk("Ryouhei", "RH020036")
	Mess("Whaat!? Why do I have to stay to keep watch? So cruel...")
	hitretResult := Hitret(18372)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'll be quick. Well then, I'm going!")
	hitretResult := Hitret(18373)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	BustupClear(5)
	Talk("Ryouhei", "RH020037")
	Mess("........ Tch... Everyone's so cold...")
	hitretResult := Hitret(18374)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B18a", 0, 0)
	Transition("WIP_MOZV", 500)
	Talk("Haruka", "")
	Mess("So- sorry to have kept you waiting.")
	hitretResult := Hitret(18375)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR020040")
	Mess("...... You're late.")
	hitretResult := Hitret(18376)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK020109")
	Mess("Sorry. Well then, shall we buy some bread in the store?Å^I'll give you my recommendations.")
	hitretResult := Hitret(18377)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume took Sora arm in arm and looking like a coupleÅ^of friends, they headed to the store.")
	hitretResult := Hitret(18378)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD01_08M", 0, 0)
	Talk("Kazuha", "KA020016")
	Mess("Ah........")
	hitretResult := Hitret(18379)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san was about to stop Amatsume, but her hand justÅ^passed through air as she was left behind.")
	hitretResult := Hitret(18380)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She looked as if her beloved one was taken by someoneÅ^else.")
	hitretResult := Hitret(18381)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Migiwa-san, you really are close to Amatsume, aren'tÅ^you? You do a lot of things together too...")
	hitretResult := Hitret(18382)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA020017")
	Mess("...So it looks like we are close to each other?")
	hitretResult := Hitret(18383)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah. Amatsume seems to be having fun around you and youÅ^seem to care about her a lot.")
	hitretResult := Hitret(18384)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020018")
	Mess("I'm only... worrying about that girl. It's because that,Å^if left alone, she starts being unsteady and it getsÅ^dangerous.")
	hitretResult := Hitret(18385)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_04M", 0, 0)
	Talk("Kazuha", "KA020019")
	Mess("Besides, she gets carried away easily, so she's alwaysÅ^up to no good with Nakazato-senpai... I think that sheÅ^doesn't understand other people's feelings.")
	hitretResult := Hitret(18386)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just from hearing that, I realized that Migiwa-san likedÅ^Amatsume.")
	hitretResult := Hitret(18387)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Taking her hand, Its almost like she's your littleÅ^sister.")
	hitretResult := Hitret(18388)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_12M", 0, 0)
	Talk("Kazuha", "KA020020")
	Mess("..... Little sister?")
	hitretResult := Hitret(18389)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, well..... it's just as-if.")
	hitretResult := Hitret(18390)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_04M", 0, 0)
	Talk("Kazuha", "KA020021")
	Mess("Please don't say strange things... If she heard that,Å^she would go overboard again.")
	hitretResult := Hitret(18391)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You're right, Kazu-ojou-chaan!... she would say.")
	hitretResult := Hitret(18392)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_07M", 0, 0)
	Talk("Kazuha", "KA020022")
	Mess("Please stop joking around!")
	hitretResult := Hitret(18393)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... Sorry.....")
	hitretResult := Hitret(18394)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_10M", 0, 0)
	Talk("Kazuha", "KA020023")
	Mess("No, it's nothing... You sounded just Nakazato-senpai, soÅ^I just...")
	hitretResult := Hitret(18395)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA020024")
	Mess("A- anyway... Kasugano-kun too, please be careful. ThatÅ^is if you care about Akira's well-being.")
	hitretResult := Hitret(18396)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_08M", 0, 0)
	Talk("Kazuha", "KA020025")
	Mess("To bring sorrow upon that girl... I definitely don'tÅ^want that.")
	hitretResult := Hitret(18397)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("................")
	hitretResult := Hitret(18398)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02S", 0, 0)
	Talk("Akira", "AK020110")
	Mess("Kazu-chaan, Haru-kuun? What are you doing? I'm going toÅ^leave you behind.")
	hitretResult := Hitret(18399)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, yeah, I'm comming.")
	hitretResult := Hitret(18400)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA020026")
	Mess("Why you... you already left us behind.")
	hitretResult := Hitret(18401)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("................")
	hitretResult := Hitret(18402)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Unexpectedly, in the corner of my eye, I saw just howÅ^much Migiwa-san felt about Amatume.")
	hitretResult := Hitret(18403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san, just like a guardian, snuggled up toÅ^Amatsume and started walking with her.")
	hitretResult := Hitret(18404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Doesn't it seem like there is something else besidesÅ^friendship between those two?")
	hitretResult := Hitret(18405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("It's not merely just getting along. That was what IÅ^understood from Migiwa-san's words.")
	hitretResult := Hitret(18406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_c004")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

