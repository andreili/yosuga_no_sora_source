Include"yosuga.ch"

Function sc00_C020B()
	ScenarioEnter()
	ScPlayEnvSe("SE301", "", 0)
	SetCg("B34a", 400, 0)
	Transition("WIP_BT", 500)
	Talk("Haruka", "")
	Mess("Urgh...... hot......")
	hitretResult := Hitret(22645)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The placed that I lived in before, even if it's anyÅ^hotter the streets are still full of people, but rightÅ^here there's not even one person.")
	hitretResult := Hitret(22646)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Is everyone staying at home to stay cool?")
	hitretResult := Hitret(22647)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In the past, I used to come here and avoid the summerÅ^heat, and I didn't care how hot")
	hitretResult := Hitret(22648)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Coming out in this hot weather and taking a walk, I'mÅ^probably the only one......")
	hitretResult := Hitret(22649)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021114")
	Mess("Haru-kun!!")
	hitretResult := Hitret(22650)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE255", 0)
	Talk("Haruka", "")
	Mess("There is, one another.")
	hitretResult := Hitret(22651)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE301", 0)
	ScPlayBgm("BGM06", 0)
	SetCg("EC02a", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Akira", "AK021115")
	Mess("Hm? Haru-kun, are you taking a stroll?")
	hitretResult := Hitret(22652)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, I just wanted to see Amatsu..... Akira.")
	hitretResult := Hitret(22653)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EC02b", 0, 0)
	Talk("Akira", "AK021116")
	Mess("Fufuu, is that so? That's great, we met when we passedÅ^by here.")
	hitretResult := Hitret(22654)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though I used to call her Amatsume and hasn't gotÅ^used to that, When I call her Akira, she showed a reallyÅ^happy face.")
	hitretResult := Hitret(22655)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Are you going out? Helping out someone today too?")
	hitretResult := Hitret(22656)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EC02c", 0, 0)
	Talk("Akira", "AK021117")
	Mess("Yup, Oji-chan bought a new television, but it doesn'tÅ^display anyhing, and asked me to take a look.")
	hitretResult := Hitret(22657)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EC02a", 0, 0)
	Talk("Akira", "AK021118")
	Mess("Because it's a new television, giving it a chop wouldn'tÅ^work. Mmm~ What should I do? Haru-kun can you come andÅ^help out?")
	hitretResult := Hitret(22658)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sure I can, but I'm also not well informed with theÅ^details.")
	hitretResult := Hitret(22659)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EC02c", 0, 0)
	Talk("Akira", "AK021119")
	Mess("No problem, no problem! Here, take a seat behind!")
	hitretResult := Hitret(22660)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh yeah, let me ride in the front. I want to flatten myÅ^waist a little.")
	hitretResult := Hitret(22661)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021120")
	Mess("Is that so? Ah, Haru-kun, your stomach's is poking out-!")
	hitretResult := Hitret(22662)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh..... D-don't touch meee......")
	hitretResult := Hitret(22663)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021121")
	Mess("Fufufuu, you ate a lot in the afternoon, it feels soÅ^squishy~.")
	hitretResult := Hitret(22664)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira seems to find it interesting to stroke my slightlyÅ^bloated stomach from overeating, and it feels itchy.")
	hitretResult := Hitret(22665)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("T-this is so embarrassing......")
	hitretResult := Hitret(22666)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021122")
	Mess("Fufufuu, then I'll leave it to you to be the driver.")
	hitretResult := Hitret(22667)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("OK. But when you hold on to me later, don't hug my waistÅ^too tightly.")
	hitretResult := Hitret(22668)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Akira and I went to Ujiko-ojichan's home, and there wasÅ^a LCD television placed in the living room, but itÅ^displayed only a blue screen.")
	hitretResult := Hitret(22669)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When it was initially installed the display was fine,Å^but it suddenly failed to show. Akira also didn't knowÅ^what was wrong, and in the end they both started toÅ^knock the television.")
	hitretResult := Hitret(22670)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After reading the instruction manual and confirming theÅ^television's settings, even though the television's modeÅ^for digital broadcasting has been set up, it seems thatÅ^this town cannot receive them yet.")
	hitretResult := Hitret(22671)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It seems that when the television was bought, it wasÅ^thought it didn't need any conditions to receive theÅ^signal, and the store's salesman didn't explain it well.")
	hitretResult := Hitret(22672)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In any case, analog broadcasting can still be used, andÅ^put up with it for a while, and it's finally decided.")
	hitretResult := Hitret(22673)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B12a", 0, 0)
	SetBustup("CC06_01M", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Akira", "AK021123")
	Mess("Haru-kun, thank you! You really helped out here.")
	hitretResult := Hitret(22674)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's great that I was able to help out..... But, IÅ^really didn't think you gave it a chop.")
	hitretResult := Hitret(22675)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021124")
	Mess("I saw Hiro-neechan, do it to my home's washing machine,Å^I just imitated it.")
	hitretResult := Hitret(22676)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Uuu...... The culprit, is that person!!")
	hitretResult := Hitret(22677)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021125")
	Mess("Well then, off to the next home!")
	hitretResult := Hitret(22678)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Next!? What's the problem this time?")
	hitretResult := Hitret(22679)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_14M", 0, 0)
	Talk("Akira", "AK021126")
	Mess("Switching the light bulb, washing the air-conditioner'sÅ^filter, and repairing the fence.")
	hitretResult := Hitret(22680)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("And you're even repairing the television, Akira's reallyÅ^a jack-of-all-trades.")
	hitretResult := Hitret(22681)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK021127")
	Mess("There are a lot of things that I can help out with, andÅ^I like it since it's interesting. Also, it's good that IÅ^can help Oji-chan and Oba-chan. (Oji-chan, Oba-chan =Å^addressing the elderly man and woman respectively)")
	hitretResult := Hitret(22682)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Most likely, the real objective from them is to chat.Å^The home that we just went, only had Oji-san livingÅ^there. (TL: Haruka uses -san, Akira uses -chan becauseÅ^she's more familiar).")
	hitretResult := Hitret(22683)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When Akira came, Oji-san looks really happy, almostÅ^treating her like a granddaughter.")
	hitretResult := Hitret(22684)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Come to think of it, when did Akira went about goingÅ^from one house to another house to chat with them?")
	hitretResult := Hitret(22685)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("This is not something someone will normally do, and IÅ^once again felt Akira's kindness.")
	hitretResult := Hitret(22686)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(2000, 0)
	SetCg("B15b", 0, 0)
	Transition("WIP_MOZRL", 500)
	Talk("óI", "")
	Mess("Ç”Ç£ÅcÅcÅc")
	hitretResult := Hitret(22687)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM07", 0)
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK021128")
	Mess("You worked hard today, Haru-kun.")
	hitretResult := Hitret(22688)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Akira has been doing something like this all the time?Å^That's amazing......")
	hitretResult := Hitret(22689)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After the home vist has ended, we went to the front ofÅ^Hiro-san's shop, and sat on top of the juice-case toÅ^rest.")
	hitretResult := Hitret(22690)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The problem itself was quickly finished, but we'reÅ^treated generously to watermelons, barley tea, sweetsÅ^and pocket money.")
	hitretResult := Hitret(22691)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though we're already sated, it was difficult toÅ^decline when they keep telling us \"don't be reserved\",Å^and we just ate a little at every place, we soon reachedÅ^our limits.")
	hitretResult := Hitret(22692)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_05M", 0, 0)
	Talk("Akira", "AK021129")
	Mess("I was originally wanted to treat you to cake forÅ^accompanying me, but I think you can't take in anymoreÅ^food right?")
	hitretResult := Hitret(22693)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes...... I thought I was able to quickly digest theÅ^food in my stomach, but I'm completely stuffed again.")
	hitretResult := Hitret(22694)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_14M", 0, 0)
	Talk("Akira", "AK021130")
	Mess("Your stomach's round again~")
	hitretResult := Hitret(22695)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Please stop t-touching...... Don't you feel terrible,Å^Akira? You ate so much.")
	hitretResult := Hitret(22696)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK021131")
	Mess("Hmmm~ I think I'm actually fine.")
	hitretResult := Hitret(22697)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You can't judge people by their appearance, is itÅ^possible that you have a surprising large appetite?")
	hitretResult := Hitret(22698)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021132")
	Mess("Perhaps, if there's something delicious, I might overeatÅ^without noticing.")
	hitretResult := Hitret(22699)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But even though you eat that much, you don't seem to beÅ^getting fat?")
	hitretResult := Hitret(22700)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021133")
	Mess("Mmm, I think think it's quite intriguing too. I ate soÅ^much but my chest is still flat.")
	hitretResult := Hitret(22701)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Akira, you...... hope your c-chest grows bigger?")
	hitretResult := Hitret(22702)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03M", 0, 0)
	Talk("Akira", "AK021134")
	Mess("I think bigger is more interesting, but what doesÅ^Haru-kun think about that? Do you think bigger isÅ^better? Or staying like this is fine?")
	hitretResult := Hitret(22703)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("This...... I t-think it's good to just leave it to yourÅ^natural growth.")
	hitretResult := Hitret(22704)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira frequently mentions the topic about her chest,Å^could it be that she has a complex about this? But sheÅ^doesn't appear to feel that way......")
	hitretResult := Hitret(22705)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In the past, Akira tangled around my wrist at that time,Å^even though I felt her chest was small, but it wasÅ^incredibly soft and feels nice. Therefore, I think it'sÅ^better to remain that way.")
	hitretResult := Hitret(22706)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK021135")
	Mess("Ah, that's right, do you want to drink some medicine?")
	hitretResult := Hitret(22707)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh, indeed, I should take it just in case.")
	hitretResult := Hitret(22708)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021136")
	Mess("Then wait a while, I'll get some from Hiro-neechan.")
	hitretResult := Hitret(22709)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Akira ran inside the candy shop's room.")
	hitretResult := Hitret(22711)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK021137")
	Mess("I'm sorry to keep you waiting, Hiro-neechan tells me toÅ^take it by myself. So I brought the first-aid kit, whichÅ^medicine should I take out?")
	hitretResult := Hitret(22712)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Just as expected, there's a lot of gastrointestinalÅ^medicine.")
	hitretResult := Hitret(22713)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_14M", 0, 0)
	Talk("Akira", "AK021138")
	Mess("There are even medicine for hangovers. There are alsoÅ^vitamin pills and hyaluronic acid! Haru-kun, do you wantÅ^to try and make your skin smooth?")
	hitretResult := Hitret(22714)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("This looks e-expensive, if I take it without permission,Å^she will kill me, let's just stop there. In any case,Å^digestive medicine... this will do.")
	hitretResult := Hitret(22715)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021139")
	Mess("Here is some water.")
	hitretResult := Hitret(22716)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thank you. Akira are you alright with not taking anyÅ^medicine?")
	hitretResult := Hitret(22717)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_04M", 0, 0)
	Talk("Akira", "AK021140")
	Mess("No problems! My stomach is seriously sturdy!")
	hitretResult := Hitret(22718)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira knocks her own stomach with a plonk sound. If IÅ^imitated her, there will be a catastrophic incident.")
	hitretResult := Hitret(22719)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Haruka", "")
	Mess("That's kind of envious, well...... itadakimasu.")
	hitretResult := Hitret(22720)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Before the bitterness of the medicine spread on myÅ^tongue, I washed it down with water.")
	hitretResult := Hitret(22721)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021141")
	Mess("Do you still need another cup of water?")
	hitretResult := Hitret(22722)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No...... that's enough. Thank you, Akira.")
	hitretResult := Hitret(22723)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020363")
	Mess("Urgh.....")
	hitretResult := Hitret(22724)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK021142")
	Mess("Ah, Kazu-chan! Welcome!!")
	hitretResult := Hitret(22725)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_12M", 0, 0)
	Talk("Kazuha", "KA020364")
	Mess("Y-you're here.")
	hitretResult := Hitret(22726)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021143")
	Mess("Yup, I went together with Haru-kun to help out and justÅ^came back.")
	hitretResult := Hitret(22727)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020365")
	Mess("Is that so......")
	hitretResult := Hitret(22728)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA020366")
	Mess("Hmmm...... Kasugano-kun, thank you for that previousÅ^time. You celebrated my birthday as well.")
	hitretResult := Hitret(22729)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, it's nothing, I'm the one who should be sorry forÅ^surprising you. But did I create any annoyance for you?")
	hitretResult := Hitret(22730)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020367")
	Mess("No, I was delighted. I don't even know how to thank youÅ^actually.")
	hitretResult := Hitret(22731)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03M", 0, 0)
	Talk("Akira", "AK021144")
	Mess("Kazu-chan, once Haru-kun and Sora-chan birthdays are up,Å^let's get revenge on them with a bigger celebration!")
	hitretResult := Hitret(22732)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("R-revenge!?")
	hitretResult := Hitret(22733)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_03M", 0, 0)
	Talk("Kazuha", "KA020368")
	Mess("It's not revenge, but a return in favor right? But,Å^indeed Kasugano-kun, when are your birthdays?")
	hitretResult := Hitret(22734)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021145")
	Mess("February 5th!")
	hitretResult := Hitret(22735)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's enough if you can remember it. Plus, the two of youÅ^don't really need to mind it that much...")
	hitretResult := Hitret(22736)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK021146")
	Mess("That won't happen, right Kazu-chan?")
	hitretResult := Hitret(22737)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA020369")
	Mess("Yeah, I don't want others to think I'm an ungratefulÅ^person.")
	hitretResult := Hitret(22738)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If it's Akira, I think she will cook enough to last oneÅ^whole week.")
	hitretResult := Hitret(22739)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_12M", 0, 0)
	Talk("Kazuha", "KA020370")
	Mess("!")
	hitretResult := Hitret(22740)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK021147")
	Mess("That's a pretty good idea. But, Haru-kun has said veforeÅ^that he likes Karage, so I'll make one week's worth ofÅ^it.")
	hitretResult := Hitret(22741)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh, but Sora and I, really can't finish that much.")
	hitretResult := Hitret(22742)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020371")
	Mess("Y-Yeah... don't make such troublesome things.")
	hitretResult := Hitret(22743)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("? What's wrong, Migiwa-san?")
	hitretResult := Hitret(22744)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020372")
	Mess("N-Nothing... nothing at all.......")
	hitretResult := Hitret(22745)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK021148")
	Mess("Then, just look forward to your birthday.")
	hitretResult := Hitret(22746)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay, I'll wait for it with bated breath from todayÅ^onwards.")
	hitretResult := Hitret(22747)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK021149")
	Mess("Aaah, that's right! Kazu-chan, do you want to eat theÅ^cake? The remainder of it from the birthday.")
	hitretResult := Hitret(22748)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA020373")
	Mess("S..... Sure.......")
	hitretResult := Hitret(22749)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021150")
	Mess("Haru-kun....., you just had medicine, it's probablyÅ^impossible for you.")
	hitretResult := Hitret(22750)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's okay, don't worry about me. Furthermore it's yourÅ^cake for the birthdays, the two of you should eat it.")
	hitretResult := Hitret(22751)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Migiwa-san, this Akira from just now, has been eating aÅ^lot of things earlier, did she always have such a goodÅ^appetite? (TL note: Haruka used âlÇ¡ÇΩÇÁ, where itÅ^expresses exasperation.)")
	hitretResult := Hitret(22752)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020374")
	Mess("Y-Yeah......")
	hitretResult := Hitret(22753)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("ÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(22754)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020375")
	Mess("......I t-think I should pass on the offer...... I'mÅ^going back.")
	hitretResult := Hitret(22755)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_06M", 0, 0)
	Talk("Akira", "AK021151")
	Mess("Ehh, you're going back already?")
	hitretResult := Hitret(22756)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Kazuha", "KA020376")
	Mess("I feel bad for bothering the two of you. T-Then, I'llÅ^see you later. Akira...... Ka...Kasugano-kun.")
	hitretResult := Hitret(22757)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san turned her face away, and simply left. HerÅ^languishing face made me feel bad.")
	hitretResult := Hitret(22758)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Anyone would have noticed it, since there was such aÅ^distinct change in addressing her.")
	hitretResult := Hitret(22759)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Furthermore, it was only a few days after theÅ^birthday..... this was proof that our relationshipÅ^suddenly changed.")
	hitretResult := Hitret(22760)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san's shoulders perk up and walk away, seeminglyÅ^in anger.")
	hitretResult := Hitret(22761)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As we did not explain to her clearly, this had hurt herÅ^and it's a fact that won't change.")
	hitretResult := Hitret(22762)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... Akira.")
	hitretResult := Hitret(22763)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK021152")
	Mess("Haru-kun, are you going to go after her?")
	hitretResult := Hitret(22764)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes..... I think there's a lot of things to explain toÅ^her properly.")
	hitretResult := Hitret(22765)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021153")
	Mess("Kazu-chan also understands it actually.")
	hitretResult := Hitret(22766)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But.........")
	hitretResult := Hitret(22767)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira has a good relationship with her...... Is itÅ^really okay to leave just like that?")
	hitretResult := Hitret(22768)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("............ I'll go over and come back quickly.")
	hitretResult := Hitret(22769)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("I had questions over Akira's behavior, and I chasedÅ^after Migiwa-san's figure which was gradually becomingÅ^smaller.")
	hitretResult := Hitret(22770)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM10", 0)
	SetCg("B34b", 400, 0)
	Transition("WIP_RL", 1000)
	Talk("Haruka", "")
	Mess("Migiwa-san!!")
	hitretResult := Hitret(22771)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san's shoulders shake a little, and it seems thatÅ^she has no intention of turning back to face me.")
	hitretResult := Hitret(22772)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Just wait for me......")
	hitretResult := Hitret(22773)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I go around Migiwa-san and face her.")
	hitretResult := Hitret(22774)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, she once again turns her back on me.")
	hitretResult := Hitret(22775)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020377")
	Mess("Is there anything you need?")
	hitretResult := Hitret(22776)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("ÇªÇ§åæÇ¢Ç»Ç™ÇÁÇ‡ÅAéãê¸ÇçáÇÌÇπÇÊÇ§Ç∆ÇµÇ»Ç¢ÅB")
	hitretResult := Hitret(22777)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She really does mid it...... Migiwa-san is rejecting me,Å^and it is much more than I had imagined.")
	hitretResult := Hitret(22778)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... To have things end up like this of a sudden, I'mÅ^really sorry......")
	hitretResult := Hitret(22779)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020378")
	Mess("....... Why...... is it you......")
	hitretResult := Hitret(22780)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... Ehh.....")
	hitretResult := Hitret(22781)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020379")
	Mess("Why is it, just you.......")
	hitretResult := Hitret(22782)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At this moment I finally understood. Migiwa-san wasn'tÅ^angry about not telling her of our relationship.")
	hitretResult := Hitret(22783)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020380")
	Mess("Why is it only just you who can freely call out toÅ^Onee-chan?")
	hitretResult := Hitret(22784)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_06M", 0, 0)
	Talk("Kazuha", "KA020381")
	Mess("You can always be with Akira, and there is no need toÅ^hesitate over someone!")
	hitretResult := Hitret(22785)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020382")
	Mess("How is it, so easy...... to snatch Onee-chan away?")
	hitretResult := Hitret(22786)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Snatch-- that's....... It is still the same as before,Å^Migiwa-san's relationship with Akira has never change.")
	hitretResult := Hitret(22787)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020383")
	Mess("But, in Onee-chan's heart, you have become the mostÅ^important person.")
	hitretResult := Hitret(22788)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020384")
	Mess("And that is, over anyone else she would prioritize you.Å^To date with someone else, that's the case right?")
	hitretResult := Hitret(22789)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020385")
	Mess("And then finally one day, Onee-chan finally will notÅ^need me....")
	hitretResult := Hitret(22790)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_06M", 0, 0)
	Talk("Kazuha", "KA020386")
	Mess("In the end, compared to sisters who will not beÅ^acknowledged, it's better to recognize lovers!!")
	hitretResult := Hitret(22791)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Do not say that!!")
	hitretResult := Hitret(22792)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020387")
	Mess("Wha........")
	hitretResult := Hitret(22793)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Does Migiwa-san find that everything you went throughÅ^with Akira is pointless?")
	hitretResult := Hitret(22794)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Is the bond between the two of you is easily brokenÅ^because I barged into it?")
	hitretResult := Hitret(22795)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("During the time that I'm not here, the two of you areÅ^together supporting each other, and I think that isÅ^something that I can never match up to!")
	hitretResult := Hitret(22796)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But if all of that is denied by Migiwa-san.......")
	hitretResult := Hitret(22797)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then isn't Migiwa-san the same as those people who leftÅ^Akira to be all alone!?")
	hitretResult := Hitret(22798)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_06M", 0, 0)
	Talk("Kazuha", "KA020388")
	Mess("....... No!!! I.......")
	hitretResult := Hitret(22799)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_09M", 0, 0)
	Talk("Kazuha", "KA020389")
	Mess("Just wanted..... Akira not to feel lonely......")
	hitretResult := Hitret(22800)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020390")
	Mess("But...... it's so frustrating......")
	hitretResult := Hitret(22801)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020391")
	Mess("Without me even noticing..... you had progressed so farÅ^till the point you can directly call her name, and I'mÅ^frustrated and envious......")
	hitretResult := Hitret(22802)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020392")
	Mess("Watching the two of you smile naturally made me thinkÅ^that there isn't a place for me.....")
	hitretResult := Hitret(22803)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That is......")
	hitretResult := Hitret(22804)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If I explain that we're dating, perhaps Migiwa-san willÅ^not feel terrible inside......")
	hitretResult := Hitret(22805)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Tone("MONOCHROME")
	SetCg("B02a", 0, 0)
	SetBustup("CA02_01M", 0, 0)
	Transition("CLOUD_A", 1000)
	Talk("Sora", "SR020159")
	Mess("....... I had enough of this.")
	hitretResult := Hitret(22806)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020160")
	Mess("....... Haru you, are looking as if you're floating inÅ^cloud nine, be careful of failing.")
	hitretResult := Hitret(22807)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...... I even forgot what Sora hates most...... I'm.....")
	hitretResult := Hitret(22808)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Floating too high up...... So much that I did not evenÅ^notice another's feelings.")
	hitretResult := Hitret(22809)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Tone("")
	SetCg("B34b", 400, 0)
	SetBustup("CD02_08M", 0, 0)
	Transition("CLOUD_A", 1000)
	Talk("Haruka", "")
	Mess("I'm sorry...... Migiwa-san.")
	hitretResult := Hitret(22810)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020393")
	Mess("No, this fault does not belong to both of you..... IÅ^know this clearly...... It's just that I can't acceptÅ^this immediately...... But I....")
	hitretResult := Hitret(22811)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020394")
	Mess("Got jealous, and said something that let AkiraÅ^down......")
	hitretResult := Hitret(22812)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020395")
	Mess("I am really, have done some terrible things......")
	hitretResult := Hitret(22813)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No...... Once again I realize how much considerationÅ^Migiwa-san has for Akira.")
	hitretResult := Hitret(22814)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I am envious of the bonds between Migiwa-san and Akira.")
	hitretResult := Hitret(22815)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The two of them are connected strong feelings, and thereÅ^is no one who can break them. This is something that IÅ^cannot achieve no matter how much I try.")
	hitretResult := Hitret(22816)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira knows this...... and that is the reason why sheÅ^believes in Migiwa-san.")
	hitretResult := Hitret(22817)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020396")
	Mess("........ Where's Akira?")
	hitretResult := Hitret(22818)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("She is not coming. She says that Migiwa-san willÅ^understand.")
	hitretResult := Hitret(22819)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020397")
	Mess("Urgh...... Even if someone is nice there is a limit toÅ^it.....")
	hitretResult := Hitret(22820)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020398")
	Mess("Even if she's hated, her heart wouldn't change, andÅ^remains as before.......")
	hitretResult := Hitret(22821)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_09M", 0, 0)
	Talk("Kazuha", "KA020399")
	Mess("Even from the past she is like this..... Just like aÅ^silly person..... smiling.....")
	hitretResult := Hitret(22822)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020400")
	Mess("ÅuIt's because I have nothing at allÅv....... Even ifÅ^that's the case, there's no need to treasure theÅ^feelings that are hurting her......")
	hitretResult := Hitret(22823)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020401")
	Mess("She should be like me ...... It will be fine to get madÅ^at the illogical things......")
	hitretResult := Hitret(22824)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... Let's go back, Migiwa-san. Akira should beÅ^waiting for you.")
	hitretResult := Hitret(22825)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020402")
	Mess("........ Yeah. Just treat it that I'm angry because yourÅ^relationship was concealed from me.")
	hitretResult := Hitret(22826)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1000, 0)
	SetCg("B15b", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Akira", "AK021154")
	Mess("Ahh-! Kazu-chan, Haru-kun!!")
	hitretResult := Hitret(22827)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM16", 0)
	SetBustup("CC06_04M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Akira who was shaking her legs while sitting on theÅ^juice case, jumped straight down and waved over at us.")
	hitretResult := Hitret(22828)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_14M", 0, 0)
	Talk("Akira", "AK021155")
	Mess("What's wrong? In the end do you still want to eat theÅ^cake?")
	hitretResult := Hitret(22829)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_14M", 4, 0)
	SetBustup("CD02_11M", 8, 0)
	Talk("Kazuha", "KA020403")
	Mess("...... Idiot......")
	hitretResult := Hitret(22830)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020404")
	Mess("Yeah..... That's right.")
	hitretResult := Hitret(22831)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK021156")
	Mess("Then, let's go! My stomach's hungry. Haru-kun eat withÅ^us!")
	hitretResult := Hitret(22832)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira walked between Migiwa-san and I, hugged each ofÅ^our arms and locking it, then led both of us.")
	hitretResult := Hitret(22833)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021157")
	Mess("Since it's such a rare opportunity, I'll make red teaÅ^that I received during the Bon Festival!")
	hitretResult := Hitret(22834)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020405")
	Mess("Just like that, you want to manipulate your way through?Å^I'm not so easy to be bluffed.")
	hitretResult := Hitret(22835)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_09M", 0, 0)
	Shake(2)
	Talk("Akira", "AK021158")
	Mess("Ouuu!? Ouch, ouch!!!")
	hitretResult := Hitret(22836)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san pulled Akira's cheeks, as if she's scolding aÅ^mischievious child.")
	hitretResult := Hitret(22837)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_06M", 0, 0)
	Talk("Kazuha", "KA020406")
	Mess("The two of you dated each other behind my back, and keptÅ^it as a secret from me!")
	hitretResult := Hitret(22838)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_14M", 0, 0)
	Talk("Akira", "AK021159")
	Mess("Oh, I did think of telling you? During the moment whenÅ^we eat the cake.")
	hitretResult := Hitret(22839)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_04M", 0, 0)
	Talk("Kazuha", "KA020407")
	Mess("In any case once you eat the cake, you will forget allÅ^about it right?")
	hitretResult := Hitret(22840)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK021160")
	Mess("Yup, that's very likely -!")
	hitretResult := Hitret(22841)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_06M", 0, 0)
	Talk("Kazuha", "KA020408")
	Mess("\"That's very likely-!\", what do you mean by that, huh?")
	hitretResult := Hitret(22842)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK021161")
	Mess("When it comes to that, Haru-kun will explain it clearly.")
	hitretResult := Hitret(22843)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehhh, me?")
	hitretResult := Hitret(22844)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_04M", 0, 0)
	Talk("Kazuha", "KA020409")
	Mess("Sheesh I'm really surprised, suddenly saying ~Akira,Å^calling you so intimately.")
	hitretResult := Hitret(22845)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("T-That is... certainly......")
	hitretResult := Hitret(22846)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020410")
	Mess("I want to hear exactly what happened from your side.Å^Otherwise, I'm not going to let you eat the cake.")
	hitretResult := Hitret(22847)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_06M", 0, 0)
	Talk("Akira", "AK021162")
	Mess("Ehh! But, the cake is mine?")
	hitretResult := Hitret(22848)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_06M", 0, 0)
	Talk("Kazuha", "KA020411")
	Mess("Since my birthday is on the same day, I have half theÅ^right to eat it as well!")
	hitretResult := Hitret(22849)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK021163")
	Mess("Ah, I see. Then it cannot be helped.")
	hitretResult := Hitret(22850)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020412")
	Mess("...... I..... won't give up.")
	hitretResult := Hitret(22851)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021164")
	Mess("Hmm?")
	hitretResult := Hitret(22852)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020413")
	Mess("No matter what happens, from today onwards, I'mÅ^Onee-chan's imouto!")
	hitretResult := Hitret(22853)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK021165")
	Mess("....... Mmm.")
	hitretResult := Hitret(22854)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 6, 0)
	SetBustup("CD02_11M", 8, 0)
	Talk("êSÇÃê∫", "")
	Mess("After hugging Akira to her side, Migiwa sticks her cheekÅ^over in a slightly shy manner.")
	hitretResult := Hitret(22855)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though Akira seems to feel itchy from that, sheÅ^brings her face over with a purring sound.")
	hitretResult := Hitret(22856)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Looking at the great relationship between the two, itÅ^seems that I feel like an extra person in thisÅ^atmosphere, and start to feel jealous.")
	hitretResult := Hitret(22857)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, I won't hate Migiwa-san over this. To me, she's aÅ^rival who will steal Akira away when I'm not payingÅ^attention.")
	hitretResult := Hitret(22858)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Therefore, I cannot lose to Migiwa-san.")
	hitretResult := Hitret(22859)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I, also tightly grab Akira's hand.")
	hitretResult := Hitret(22860)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And as a reply, Akira also grabs back my hand tightly.")
	hitretResult := Hitret(22861)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From another person's view, we must be the strange trioÅ^who fawns on Akira.")
	hitretResult := Hitret(22862)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But that is fine.")
	hitretResult := Hitret(22863)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Because in Akira's heart, there is no ranking.")
	hitretResult := Hitret(22864)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	Change("00_c021")
	Return
	Change("EXIT_SCENARIO")
EndFunc

