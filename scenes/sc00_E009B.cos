Include"yosuga.ch"

Function sc00_E009B()
	ScenarioEnter()
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("At night, after returning from the store.")
	hitretResult := Hitret(34536)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM09", 0)
	SetCg("B15c", 0, 0)
	Talk("High-spirited voiceÅsÇ‚Ç–ÇÎÅt", "YH050032")
	Mess("Gyahaha, the heck you guys doin'?")
	hitretResult := Hitret(34537)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Someone's jubilant laughter could be heard from inside.")
	hitretResult := Hitret(34538)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It looks like Yahiro-san's back, but I'm kind ofÅ^hesitant to go inside...")
	hitretResult := Hitret(34539)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Timid VoiceÅsèââ¿Åt", "MT050279")
	Mess("But, but~")
	hitretResult := Hitret(34540)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It looks like other people are over, too, plus there'sÅ^an unsettling atmosphere within.")
	hitretResult := Hitret(34541)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Well, I guess we can't just stand around outside likeÅ^this, either.")
	hitretResult := Hitret(34542)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I'm sorry ... wha-.")
	hitretResult := Hitret(34543)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What is this smell? ... wine? It's really strong.")
	hitretResult := Hitret(34544)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It overwhelmed my nose and made me dizzy.")
	hitretResult := Hitret(34545)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK050027")
	Mess("Ah, Haruka! We were waiting for you. Come in!")
	hitretResult := Hitret(34546)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, yeah, excuse me...")
	hitretResult := Hitret(34547)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050028")
	Mess("Haruka, have you eaten? I'm sorry to have called youÅ^over so late.")
	hitretResult := Hitret(34548)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050029")
	Mess("Hiro-neechan told me to call you over immediately")
	hitretResult := Hitret(34549)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("A-ah....")
	hitretResult := Hitret(34550)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The smell of wine was getting stronger, adding to myÅ^confusion of the situation. And also, it seemsÅ^incredibly difficult to break the silence given theÅ^atmosphere.")
	hitretResult := Hitret(34551)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050033")
	Mess("Hey, -matsume, there aren't enough snacks!")
	hitretResult := Hitret(34552)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050030")
	Mess("Yes! I'll go now. Haruka, you too.")
	hitretResult := Hitret(34553)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("I suppose I can help. And, is \"-mastume\" Amatsume'sÅ^nickname?")
	hitretResult := Hitret(34554)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And as for the owner of this house, she did not seem toÅ^notice that I came over...")
	hitretResult := Hitret(34555)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050031")
	Mess("Haruka, hurry up~!")
	hitretResult := Hitret(34556)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...If I'm a bother, I'll go back home. But as for now,Å^I'll just come in.")
	hitretResult := Hitret(34557)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("EE01a", 0, 0)
	Transition("WIP_MOZV", 500)
	Talk("Yahiro", "YH050034")
	Mess("Really, just what were you doing... eh? Why is theÅ^Kasugano kid here?")
	hitretResult := Hitret(34558)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050280")
	Mess("Huh...? Haruka...?")
	hitretResult := Hitret(34559)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yahiro-san... and Motoka-san?")
	hitretResult := Hitret(34560)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At the back, near the counter, there was a strong smellÅ^of alcohol and there were two adults drinking said beer.")
	hitretResult := Hitret(34561)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Of course, there's no way Amatsume could have beenÅ^the one drinking it.")
	hitretResult := Hitret(34562)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt...")
	hitretResult := Hitret(34563)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050032")
	Mess("Well, I got the new snacks. Should we eat them now?")
	hitretResult := Hitret(34564)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050035")
	Mess("Just eat some yourself... but, don't eat all of them.")
	hitretResult := Hitret(34565)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050033")
	Mess("Ok~ fmfmfmfmfm...")
	hitretResult := Hitret(34566)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't eat nor drink...")
	hitretResult := Hitret(34567)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE01b", 0, 0)
	Talk("Motoka", "MT050281")
	Mess("Ah~ really... why can't I do it well...?")
	hitretResult := Hitret(34568)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050036")
	Mess("You used to just be clumsy. But, now, it's just become aÅ^bother.")
	hitretResult := Hitret(34569)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050034")
	Mess("Mfmfmfmfmfmfm...")
	hitretResult := Hitret(34570)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("H-huh? Has my existance here been ignored?")
	hitretResult := Hitret(34571)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050282")
	Mess("That's true... but now that we're not students anymore,Å^a non-confident air... is no good.")
	hitretResult := Hitret(34572)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050037")
	Mess("What do you want to be, a bride? Hahahaha, Motoka beingÅ^a bride!")
	hitretResult := Hitret(34573)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050283")
	Mess("It's not something you should laugh at. Besides, I neverÅ^said such a thing.")
	hitretResult := Hitret(34574)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Motoka-san wore a childlike face as she said that andÅ^proceeded to drain a can.")
	hitretResult := Hitret(34575)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Since first meeting Motoka-san, she seemed to be nearÅ^alcohol often.")
	hitretResult := Hitret(34576)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That was strange, suddenly thinking such a thing.")
	hitretResult := Hitret(34577)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE01a", 0, 0)
	Talk("Motoka", "MT050284")
	Mess("Mm? Someone there?")
	hitretResult := Hitret(34578)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ah, so finally noticed me.")
	hitretResult := Hitret(34579)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050035")
	Mess("It's Haru-kun. Yahiro-nee and I said it before.")
	hitretResult := Hitret(34580)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050038")
	Mess("Haruka? ... yea, I did say something about that before,Å^didn't I?")
	hitretResult := Hitret(34581)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If I'm a disturbance I could always leave [and come backÅ^later].")
	hitretResult := Hitret(34582)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050039")
	Mess("Nah, you're no bother. Let's just hurry up [and finish].")
	hitretResult := Hitret(34583)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... yea...")
	hitretResult := Hitret(34584)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE01c", 0, 0)
	Talk("Motoka", "MT050285")
	Mess("Eh?! Haruka? Why?")
	hitretResult := Hitret(34585)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Please don't say that you've just noticed me now...")
	hitretResult := Hitret(34586)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE01a", 0, 0)
	Talk("Motoka", "MT050286")
	Mess("N-not at all. I knew you were here all along. GoodÅ^evening, Haruka-kun.")
	hitretResult := Hitret(34587)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050287")
	Mess("What is it? Something we can do for you?")
	hitretResult := Hitret(34588)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, yes. I wanted to ask something about Yahiro-san.")
	hitretResult := Hitret(34589)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050288")
	Mess("Eh...")
	hitretResult := Hitret(34590)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("W-Why do you look so suprised?")
	hitretResult := Hitret(34591)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050289")
	Mess("N-no, I'm not suprised~~.")
	hitretResult := Hitret(34592)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050040")
	Mess("Him not coming to see you was a shock, huh.")
	hitretResult := Hitret(34593)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050041")
	Mess("You don't have to care about that old drunk.")
	hitretResult := Hitret(34594)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE01b", 0, 0)
	Talk("Motoka", "MT050290")
	Mess("It's not like that! I haven't been drinking!")
	hitretResult := Hitret(34595)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050042")
	Mess("Seeing as how you were just crying about that guyÅ^earlier, I just can't see why people would think youÅ^were drunk.")
	hitretResult := Hitret(34596)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050291")
	Mess("I was not crying! There's nothing wrong with complainingÅ^after work!")
	hitretResult := Hitret(34597)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Motoka-san... is the just same as when she was at myÅ^house...")
	hitretResult := Hitret(34598)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Hey, Amatsume. Is Motoka-san always like this?")
	hitretResult := Hitret(34599)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050036")
	Mess("Mm~~~, basically.")
	hitretResult := Hitret(34600)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050043")
	Mess("She'll always come to my house after work to complain.")
	hitretResult := Hitret(34601)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050044")
	Mess("I was wondering what she would say today.")
	hitretResult := Hitret(34602)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE01c", 0, 0)
	Talk("Motoka", "MT050292")
	Mess("Don't~! Don't say that with Haruka-kun here!")
	hitretResult := Hitret(34603)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A familiar arguement began again between the two.")
	hitretResult := Hitret(34604)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Somehow, I began to realize that they were probably likeÅ^this everyday.")
	hitretResult := Hitret(34605)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE01a", 0, 0)
	Talk("Motoka", "MT050293")
	Mess("Are you misunderstanding? I'm actually a much happierÅ^person than that...")
	hitretResult := Hitret(34606)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050045")
	Mess("Don't lie! You were never happy even when you were aÅ^student.")
	hitretResult := Hitret(34607)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050294")
	Mess("Th-that's not true. It may have turned out like thatÅ^before, but now...")
	hitretResult := Hitret(34608)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050046")
	Mess("You're bad with both men and work.")
	hitretResult := Hitret(34609)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE01b", 0, 0)
	Talk("Motoka", "MT050295")
	Mess("Uguu... d-don't say that...")
	hitretResult := Hitret(34610)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Yahiro-san transformed the conversation into failure inÅ^school, probably hurting Motoka-san.")
	hitretResult := Hitret(34611)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ç©Ç∆Ç¢Ç¡Çƒèââ¿Ç≥ÇÒÇ‡Ç‚ÇÁÇÍÇ¡ÇœÇ»ÇµÇ≈ÇÕÇ»Ç≠ÅAéûÇ…ÇÕÇ‚Ç–ÇÎ")
	Mess("Ç≥ÇÒÇãlÇ‹ÇÁÇπÇΩÇËÅAÇ«Ç±Ç©äyÇµÇ∞ÇæÅB")
	hitretResult := Hitret(34612)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I guess this is what they call being close.")
	hitretResult := Hitret(34613)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It feels like the trust between them has been built upÅ^over the years.")
	hitretResult := Hitret(34614)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Those two being so together, as well as Amatsume-sanÅ^there smiling and eating snacks makes me feel as if theyÅ^are sharing quite the special relationship.")
	hitretResult := Hitret(34615)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Motoka-san and Yahiro-san, were you two childhoodÅ^friends?")
	hitretResult := Hitret(34616)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE01a", 0, 0)
	Talk("Motoka", "MT050296")
	Mess("I guess. It just because of Yahiro and because we're theÅ^same age.")
	hitretResult := Hitret(34617)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050047")
	Mess("... We just went to the same school and kind of gotÅ^stuck together as friends from then.")
	hitretResult := Hitret(34618)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050297")
	Mess("After that we began to differ. Yahiro-san wanted to findÅ^a job in Tokyo and I- I- u-uuu....")
	hitretResult := Hitret(34619)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("W-what is it? Why are you crying?")
	hitretResult := Hitret(34620)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050048")
	Mess("Leave it alone. She's a single woman without a good jobÅ^so its pretty shameful.")
	hitretResult := Hitret(34621)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE01b", 0, 0)
	Talk("Motoka", "MT050298")
	Mess("It is not!")
	hitretResult := Hitret(34622)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050299")
	Mess("What about you, Yahiro? Didn't you only take over theÅ^family business? You're the same as me!")
	hitretResult := Hitret(34623)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050049")
	Mess("Ah, ah, yes. There's no difference between you and me.")
	hitretResult := Hitret(34624)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Yahiro-san finished her beer, stretched out her handsÅ^and got a new can.")
	hitretResult := Hitret(34625)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The tab opened with a crisp \"psst\" and she began toÅ^drink again.")
	hitretResult := Hitret(34626)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("They were drinking away their sorrows... at least that'sÅ^what it looks like.")
	hitretResult := Hitret(34627)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What happened in Tokyo?")
	hitretResult := Hitret(34628)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050050")
	Mess("Shut up.")
	hitretResult := Hitret(34629)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh?")
	hitretResult := Hitret(34630)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050051")
	Mess("...Tch, no more wine. Amatume, bring some more from theÅ^fridge.")
	hitretResult := Hitret(34631)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050037")
	Mess("Ok~.")
	hitretResult := Hitret(34632)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE01a", 0, 0)
	Talk("Motoka", "MT050300")
	Mess("Ah~ really~ more drinks! I'll drink until I'm full againÅ^today.")
	hitretResult := Hitret(34633)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Motoka-san reached out to pick up a new beer.")
	hitretResult := Hitret(34634)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("One empty bottle, two empty bottles... beer bottles areÅ^being emptied and drunk as they were water.")
	hitretResult := Hitret(34635)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's hard to speak with this atmosphere...")
	hitretResult := Hitret(34636)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uh, I think I'll go back now... you two try not to drinkÅ^so much.")
	hitretResult := Hitret(34637)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE01b", 0, 0)
	Talk("Motoka", "MT050301")
	Mess("Huh? Haru-kun is telling me not to drink so much? WhatÅ^right do you have to say that to me?")
	hitretResult := Hitret(34638)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uh, no, I didn't mean it like that...")
	hitretResult := Hitret(34639)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050052")
	Mess("Idiot, you don't give advice to drunks. It's as ifÅ^you're asking to be tangled up with her.")
	hitretResult := Hitret(34640)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050053")
	Mess("This guy especially ... *gulp, gulp, gulp* ... ah,Å^you'll just become a target of Motoka's interest.")
	hitretResult := Hitret(34641)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050302")
	Mess("It's not like that, it's just... I'm a little lonely!")
	hitretResult := Hitret(34642)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050054")
	Mess("You can't say that! You live with your parents!")
	hitretResult := Hitret(34643)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050303")
	Mess("What's wrong with that?! What's wrong with living withÅ^your parents? I'm just filial!")
	hitretResult := Hitret(34644)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050055")
	Mess("Say that when you start doing housework for them. Hey,Å^Haruka, you know? You know how good her cooking is?")
	hitretResult := Hitret(34645)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh... ah, hah, I guess... she came over once to doÅ^that...")
	hitretResult := Hitret(34646)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050056")
	Mess("... What... seriously?")
	hitretResult := Hitret(34647)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uh, yea.")
	hitretResult := Hitret(34648)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050057")
	Mess("Hey... are you desperate enough to go after this kid?")
	hitretResult := Hitret(34649)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE01a", 0, 0)
	Talk("Motoka", "MT050304")
	Mess("That's wrong. It was an apology, a simple apology.")
	hitretResult := Hitret(34650)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050038")
	Mess("Motoka-oneechan, what did you do that you needed toÅ^apologize for?")
	hitretResult := Hitret(34651)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE01c", 0, 0)
	Talk("Motoka", "MT050305")
	Mess("Um, I was a little drunk and... nothing, nothing!")
	hitretResult := Hitret(34652)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050058")
	Mess("Hahaha, you're a criminal! And you didn't even do it atÅ^your house, you did it at his!")
	hitretResult := Hitret(34653)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050306")
	Mess("That's wrong! That's not what happened! Haruka-kun sayÅ^something. It wasn't like that.")
	hitretResult := Hitret(34654)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050059")
	Mess("Hahaha, it's easy to imagine what happened with thisÅ^guy!")
	hitretResult := Hitret(34655)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050060")
	Mess("You're in big trouble now right? A drunk Motoka reallyÅ^is the worst.")
	hitretResult := Hitret(34656)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, nothing like that happened.")
	hitretResult := Hitret(34657)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Motoka-san was drunk at the time, but nothing wierdÅ^happened.")
	hitretResult := Hitret(34658)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050307")
	Mess("Haruka-kun~~~~... You're the only one on my side~~~.")
	hitretResult := Hitret(34659)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050061")
	Mess("What are you saying... if you do dumb things, you'llÅ^always be left behind.")
	hitretResult := Hitret(34660)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE01b", 0, 0)
	Talk("Motoka", "MT050308")
	Mess("That's not true. I don't do stupid things.")
	hitretResult := Hitret(34661)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050062")
	Mess("Huh, what are you always doing then?")
	hitretResult := Hitret(34662)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Yahiro-san sounds like she's always unhappy at night.")
	hitretResult := Hitret(34663)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There was a good mood just before... I wonder ifÅ^Yahiro-san is also pretty durnk.")
	hitretResult := Hitret(34664)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'll be going back now. Please excuse my intrusion.")
	hitretResult := Hitret(34665)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050039")
	Mess("Bye-bye, I'll se you at school then.")
	hitretResult := Hitret(34666)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B15c", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Back outside, the afternoon sun had already disappearedÅ^like a ghost.")
	hitretResult := Hitret(34667)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The wind blowing from the mountains felt good.")
	hitretResult := Hitret(34668)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Somehow, it feels like it also blew away regrets left inÅ^my heart.")
	hitretResult := Hitret(34669)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("This constricting feeling in my heart is definatelyÅ^regret.")
	hitretResult := Hitret(34670)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's not that I didn't listen to some important thingsÅ^and regret it, but that I heard things that I shouldn'tÅ^have.")
	hitretResult := Hitret(34671)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("ãCåyÇ…òbÇµÇƒÇΩÇ©ÇÁê¢ä‘òbÇÃÉmÉäÇ≈ï∑Ç¢ÇøÇ·Ç¡ÇΩÇØÇ«ÅAÇ†ÇÍÇÕ")
	Mess("Ç´Ç¡Ç∆ÅAê[Ç¢èäÇÃòbÇæÇ¡ÇΩÇÒÇæÇÎÇ§ÅB")
	hitretResult := Hitret(34672)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("For Motoka-san and Yahiro-san, it's probably a thing ofÅ^the past.")
	hitretResult := Hitret(34673)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Or, is it fine because it'e between just the two ofÅ^them.")
	hitretResult := Hitret(34674)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B27c", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Huh...")
	hitretResult := Hitret(34675)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Wine is scary.")
	hitretResult := Hitret(34676)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	Change("00_e010")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

