Include"yosuga.ch"

Function sc00_C006()
	ScenarioEnter()
	ScPlaySe("SE357", 0)
	SetCg("B01a", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Haruka", "")
	Mess("Sora, are you ready?")
	hitretResult := Hitret(18653)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM05", 0)
	SetBustup("CA01_07M", 0, 0)
	Down(4, 30, 1000, 3)
	Talk("Sora", "SR020047")
	Mess("........ Sleepy.")
	hitretResult := Hitret(18654)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's why I told you to stop staying up so late...")
	hitretResult := Hitret(18655)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With a dreamy expression, Sora was putting on her shoesÅ^by bumping them around with her toes.")
	hitretResult := Hitret(18656)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Did you forget anything? If I remember, we have P.E.Å^today, right?")
	hitretResult := Hitret(18657)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Down(4, -1, 0, 0)
	Talk("Sora", "SR020048")
	Mess("Ah........")
	hitretResult := Hitret(18658)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Do you have your gym clothes? I won't let you use mine.")
	hitretResult := Hitret(18659)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_06M", 0, 0)
	Talk("Sora", "SR020049")
	Mess("Then, I'll sit out.")
	hitretResult := Hitret(18660)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Good grief, if you don't move, you'll put on weight.")
	hitretResult := Hitret(18661)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR020050")
	Mess("...And look at you, you're lacking excercises too...")
	hitretResult := Hitret(18662)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Quake(0, 16, 0, 2, 30)
	Talk("Haruka", "")
	Mess("Uwah!?")
	hitretResult := Hitret(18663)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Plucking weakly my stomach, Sora went to get herÅ^clothes.")
	hitretResult := Hitret(18664)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? And I though I was still moving well enough.")
	hitretResult := Hitret(18665)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, if there's enough meat to be plucked, have I gainedÅ^weight?")
	hitretResult := Hitret(18666)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I stopped going to convenience store as much, so IÅ^started eating less snacks and as a result now I usuallyÅ^finish my dinner.")
	hitretResult := Hitret(18667)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ugh... If that's the reason... then I'm going to have toÅ^start watching how much I eat...")
	hitretResult := Hitret(18668)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Haruka", "")
	Mess("Did you forget anything else?")
	hitretResult := Hitret(18670)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020051")
	Mess("........ No.")
	hitretResult := Hitret(18671)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay then, I'm locking up.")
	hitretResult := Hitret(18672)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I locked the door with my key and stuck into my pocket.")
	hitretResult := Hitret(18673)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK020184")
	Mess("Good morning! Haru-kun, Sora-chan!")
	hitretResult := Hitret(18674)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Morning, Amatsume... Huh? And Migiwa-san?")
	hitretResult := Hitret(18675)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020185")
	Mess("Hmm. For some reason she had to leave earlier, so todayÅ^its just me.")
	hitretResult := Hitret(18676)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh I see. Thank you for yesterday, I hung it up rightÅ^away and got to sleep. Sora, thank Amatsume.")
	hitretResult := Hitret(18677)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK020186")
	Mess("No problem, the most important thing is that you usedÅ^it. You fell asleep soundly, right?")
	hitretResult := Hitret(18678)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, even though I kept the window open, it was fine.Å^It's handy, so i think Sora leaves hers hanging all theÅ^time.")
	hitretResult := Hitret(18679)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_05M", 0, 0)
	Talk("Sora", "SR020052")
	Mess("Haru's like that too, he said that putting it away is aÅ^bother.")
	hitretResult := Hitret(18680)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I- I didn't say that! I only said that folding Sora'sÅ^too is a pain.")
	hitretResult := Hitret(18681)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC01_14M", 0, 0)
	Talk("Akira", "AK020187")
	Mess("I understand you though... It would be nice if there wasÅ^a machine with a single button that would make it comeÅ^down from the ceiling.")
	hitretResult := Hitret(18682)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's sounds like a trap that catches wild animals in it.")
	hitretResult := Hitret(18683)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_03M", 0, 0)
	Talk("Akira", "AK020188")
	Mess("Ahaha, you're right! But, it sounds like fun! BeingÅ^seized by such a trap and screaming "Nyaa! Nyaa!".")
	hitretResult := Hitret(18684)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha, you'd be all too happy to get trapped. But, inÅ^your case, laying traps wouldn't be necessary, you'd getÅ^caught with just some fresh cream there.")
	hitretResult := Hitret(18685)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK020189")
	Mess("Ah, that sounds good! If it was fresh cream, you'd catchÅ^me.")
	hitretResult := Hitret(18686)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I know! As a thanks for mosquito net, I'll treat you toÅ^some fresh cream.")
	hitretResult := Hitret(18687)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK020190")
	Mess("Eeeh? Thats too much. It wasn't that big of a deal.")
	hitretResult := Hitret(18688)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm telling you not to hold back. One from me and oneÅ^from Sora, let me do at least that much.")
	hitretResult := Hitret(18689)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR020053")
	Mess("...Okay.")
	hitretResult := Hitret(18690)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_03M", 0, 0)
	Talk("Akira", "AK020191")
	Mess("Really? Woow, thank you~! And two at that? I'm so happy.")
	hitretResult := Hitret(18691)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'll go buy it today during our lunch break.")
	hitretResult := Hitret(18692)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020192")
	Mess("Yeah! I'll be looking forward to it. Well then, let's goÅ^to school.")
	hitretResult := Hitret(18693)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1500, 0)
	SetCg("B12a", 0, 0)
	SetBustup("CF01_02M", 0, 0)
	Transition("WIP_MOZH", 500)
	Talk("Ryouhei", "RH020038")
	Mess("Hi there!! So~ra-chan! You're so cute today as well!")
	hitretResult := Hitret(18694)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR020054")
	Mess("................................")
	hitretResult := Hitret(18695)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH020039")
	Mess("What happened? You don't seem energetic today. Today weÅ^have P.E. together, you're looking forward to it too,Å^right?")
	hitretResult := Hitret(18696)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020055")
	Mess("........ Not at all.")
	hitretResult := Hitret(18697)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH020040")
	Mess("Ooh, you're such a shy one.")
	hitretResult := Hitret(18698)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	SetBustup("CF01_06M", 0, 0)
	Talk("Ryouhei", "RH020041")
	Mess("Whoah!? Wa- Wait for me! Let's talk while walking.")
	hitretResult := Hitret(18699)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei rushed to follow Sora.")
	hitretResult := Hitret(18700)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("So early in the morning, exchanges between the guy withÅ^the most energy around and one lacking it the mostÅ^seemed to be difficult.")
	hitretResult := Hitret(18701)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well then, I can't let myself be left behind.")
	hitretResult := Hitret(18702)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Haruka", "")
	Mess("Ryouhei is always so energetic in mornings.")
	hitretResult := Hitret(18703)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020193")
	Mess("Right. Ever since you showed up, Ryou-nii-chan seems toÅ^be having so much fun.")
	hitretResult := Hitret(18704)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh, me? Or maybe you should say since Sora came?")
	hitretResult := Hitret(18705)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK020194")
	Mess("Not at all. Even though he acts like that, Ryouhei getsÅ^lonely easily, so he's happy that you're keeping himÅ^company.")
	hitretResult := Hitret(18706)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So it's like that... But, its all thanks to the factÅ^that Ryouhei talked to me, that I was fortunate enoughÅ^to get along with everyone.")
	hitretResult := Hitret(18707)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK020195")
	Mess("Yeah, it may be true.")
	hitretResult := Hitret(18708)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("And Amatsume. I was able to meet you again.")
	hitretResult := Hitret(18709)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetBustup("CC01_02M", 0, 0)
	Jump(2, 0)
	Talk("Akira", "AK020196")
	Mess("Yeah!")
	hitretResult := Hitret(18710)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	ScPlayBgm("BGM08", 0)
	Talk("êSÇÃê∫", "")
	Mess("Although I have memory from my childhood, AmatsumeÅ^remembered me clearly, as if we were a couple of oldÅ^friends being reacquainted.")
	hitretResult := Hitret(18711)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Although I spent some time here during the summerÅ^holidays, I never imagined that one day I would beÅ^living here together with Sora in such uncertainty, justÅ^the two of us.")
	hitretResult := Hitret(18712)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's why having acquaitances and close friends isÅ^reassuring to the extent that even now, this veryÅ^second, I can feel it.")
	hitretResult := Hitret(18713)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, on the other hand, there are things that worry me.Å^The fact that Amatsume's grandfather has passed away andÅ^she doesn't have any parents...")
	hitretResult := Hitret(18714)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was warned by Yahiro-san not to poke my nose intoÅ^Amatsume's affairs out of curiosity, so that I don't sayÅ^anything insensitive...")
	hitretResult := Hitret(18715)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("However, Amatsume does not seem to be worried byÅ^anything, she is always so optimistic and smiling.")
	hitretResult := Hitret(18716)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's why it might be better for me not to worry aboutÅ^it too much. After all, I have problems of my own that IÅ^find difficult to talk about.")
	hitretResult := Hitret(18717)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK020197")
	Mess("Hey, Haru-kun...")
	hitretResult := Hitret(18718)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm?")
	hitretResult := Hitret(18719)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_08M", 0, 0)
	Talk("Akira", "AK020198")
	Mess("I'm sorry for surprising you yesterday about my home.")
	hitretResult := Hitret(18720)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020199")
	Mess("It was not my intentions to to hide it from you... It'sÅ^just that, for me, it had already become the norm.")
	hitretResult := Hitret(18721)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK020200")
	Mess("Well, I'm happy that I got the chance to explainÅ^everything properly.")
	hitretResult := Hitret(18722)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, thank you Amatsume. After all, I'm in similarÅ^situation. I haven't told you about the reason that IÅ^moved here.")
	hitretResult := Hitret(18723)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's why, it's not something you need to apologizeÅ^for. Just because I haven't asked about it, doesn't meanÅ^that you don't matter to me.")
	hitretResult := Hitret(18724)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The fact that you cared is enough for me.")
	hitretResult := Hitret(18725)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_11M", 0, 0)
	Talk("Akira", "AK020201")
	Mess("...Thank you, Haru-kun. Next time I'll have a properÅ^talk with you.")
	hitretResult := Hitret(18726)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Yeah... Me too.")
	hitretResult := Hitret(18727)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH020042")
	Mess("Heey!! What are the two of you doing?? I'll just end upÅ^accompanying Sora-chan to school myself. Ouch, thatÅ^hurts!!")
	hitretResult := Hitret(18728)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha, Rouhei is in for some trouble if we leave himÅ^alone. Shall we go?")
	hitretResult := Hitret(18729)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	ScStopBgm()
	Talk("Akira", "AK020202")
	Mess("Yeah! Ryou-nii-chan, wait for me!")
	hitretResult := Hitret(18730)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_c006b")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

