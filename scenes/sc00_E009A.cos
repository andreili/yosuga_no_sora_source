Include"yosuga.ch"

Function sc00_E009A()
	ScenarioEnter()
	SetCg("EZ06a", 0, 0)
	ScPlayBgm("BGM05", 0)
	Talk("Ryouhei", "RH050068")
	Mess("Yo, Haruka, did you see that commercial duringÅ^yesterday's episode?")
	hitretResult := Hitret(34421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Of what?")
	hitretResult := Hitret(34422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050069")
	Mess("The commercial during 'Miku and Takami's JungleÅ^Survivor'.")
	hitretResult := Hitret(34423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Commercial...? Wait, Jungle Survivor? Isn't that alreadyÅ^finished?")
	hitretResult := Hitret(34424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050070")
	Mess("Huh? Nah, man, it's still going. Episode 7 last nightÅ^was pretty bad, though.")
	hitretResult := Hitret(34425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050071")
	Mess("Anyway, enough about that. About that commercial.")
	hitretResult := Hitret(34426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050016")
	Mess("I saw it, I saw it! The one that was like 'check onlineÅ^for the full details'.")
	hitretResult := Hitret(34427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06d", 0, 0)
	Talk("Ryouhei", "RH050072")
	Mess("Right, right! I wanna know what happened.")
	hitretResult := Hitret(34428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050017")
	Mess("I wonder what'll happen to that guy's head?")
	hitretResult := Hitret(34429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That guy's head? Online? What kind of commercial isÅ^this?")
	hitretResult := Hitret(34430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Why not just check it out for yourself if you're thatÅ^interested?")
	hitretResult := Hitret(34431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06a", 0, 0)
	Talk("Ryouhei", "RH050073")
	Mess("How do ya expect us to do that?")
	hitretResult := Hitret(34432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Online, of course.")
	hitretResult := Hitret(34433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050074")
	Mess("Online? You mean where you connect your computer to?")
	hitretResult := Hitret(34434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You don't have a computer?")
	hitretResult := Hitret(34435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050075")
	Mess("Of course not, dude. Wait, you got one?!")
	hitretResult := Hitret(34436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, but it's not mine, it's Sora's.")
	hitretResult := Hitret(34437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Is there anyone else besides us that does?")
	hitretResult := Hitret(34438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050018")
	Mess("Hiro-neechan has one, but she won't let us use it~")
	hitretResult := Hitret(34439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050076")
	Mess("Figured as much. She's too much of a cheapskate to letÅ^us.")
	hitretResult := Hitret(34440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06c", 0, 0)
	Talk("Kazuha", "KA050022")
	Mess(".. Do you guys realize what time it is?")
	hitretResult := Hitret(34441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050077")
	Mess("Oh that's right, Ojou's got a computer at her place,Å^right? Let us use it.")
	hitretResult := Hitret(34442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA050023")
	Mess("...Nakazato-senpai... can you hear people when theyÅ^speak to you?")
	hitretResult := Hitret(34443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("English Teacher", "NP490001")
	Mess("Good grief. Exactly because it's your second time, ifÅ^you'll act as if you have time to spare, whatever willÅ^happen with you at end of term, I won't care.")
	hitretResult := Hitret(34444)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06f", 0, 0)
	Talk("Ryouhei", "RH050078")
	Mess("I've got reliable friends!")
	hitretResult := Hitret(34445)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("English Teacher", "NP490002")
	Mess("You know, I'd rather you rely on your teacher over yourÅ^friends.")
	hitretResult := Hitret(34446)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06c", 0, 0)
	Talk("Ryouhei", "RH050079")
	Mess("Y-yeah, but supplemental lessons seem like they'd beÅ^expensive!")
	hitretResult := Hitret(34447)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050080")
	Mess("I have faith in my friends' abilities! Right, Akira,Å^Ojou, and Haruka?")
	hitretResult := Hitret(34448)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050019")
	Mess("Yep, leave it to us!")
	hitretResult := Hitret(34449)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? Uh, yeah...")
	hitretResult := Hitret(34450)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA050024")
	Mess("...")
	hitretResult := Hitret(34451)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06f", 0, 0)
	Talk("Ryouhei", "RH050081")
	Mess("Hey now, Ojou. Gettin' embarrassed cause I'm looking forÅ^help from ya?")
	hitretResult := Hitret(34452)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06e", 0, 0)
	Talk("Kazuha", "KA050025")
	Mess("Of course not! You only go overboard like that wheneverÅ^it's convenient for you...")
	hitretResult := Hitret(34453)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA050026")
	Mess("Plus, you're the senpai here. You should be helpingÅ^Akira and I out.")
	hitretResult := Hitret(34454)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050082")
	Mess("Y-you're going to rely on me? Sounds good to me!")
	hitretResult := Hitret(34455)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san's face went blue.")
	hitretResult := Hitret(34456)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("English Teacher", "NP490003")
	Mess("Nakazato, you may not care, but can you please not dragÅ^your juniors in on this?")
	hitretResult := Hitret(34457)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("English Teacher", "NP490004")
	Mess("Also, Kasugano, although everything including theÅ^textbooks may be different than what you were used to,Å^can you try not to be dragged along by Nakazato?")
	hitretResult := Hitret(34458)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I'm sorry.")
	hitretResult := Hitret(34459)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I guess we were chatting a bit too much.")
	hitretResult := Hitret(34460)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06a", 0, 0)
	Talk("Ryouhei", "RH050083")
	Mess("SorryÅc Haruka")
	hitretResult := Hitret(34461)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("SorryÅc HarukaÅc")
	hitretResult := Hitret(34462)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ó∫ïΩ", "RH050084")
	Mess("Ç†Ç†ÅAÇøÇÂÇ¡Ç∆ñ{ãCÇèoÇ∑Ç©Ç»ÅB")
	hitretResult := Hitret(34463)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("English Teacher", "NP490005")
	Mess("Alright, now that weÅfre warmed up, why donÅft you tryÅ^to do the next translation up in front?")
	hitretResult := Hitret(34464)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050085")
	Mess("Eeeeh!?")
	hitretResult := Hitret(34465)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("ÇªÇÎÇªÇÎÅAê^ñ ñ⁄Ç…Ç‚Ç¡ÇƒÇ¢Ç©Ç»Ç¢Ç∆ïsñ°Ç¢éûä˙Ç»ÇÒÇæÇÊÇ»Åc")
	hitretResult := Hitret(34466)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though Ryouhei was smiling, I still felt uneasy.")
	hitretResult := Hitret(34467)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B19a", 0, 0)
	Talk("Haruka", "")
	Mess("Y-you knowÅc thatÅfs concerning the thing we went overÅ^earlier.")
	hitretResult := Hitret(34468)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	SetBustup("CC01_01M", 0, 0)
	Talk("Ryouhei", "RH050086")
	Mess("Oh, Haruka, you... final exam *(general area)")
	hitretResult := Hitret(34469)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What's that?")
	hitretResult := Hitret(34470)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050087")
	Mess("Come on ~ don't pretend. I understand your feelings ofÅ^being worried over the final exam.")
	hitretResult := Hitret(34471)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I won't say that I'm not worried, but that's not what IÅ^meant.")
	hitretResult := Hitret(34472)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050020")
	Mess("W-what's that? An advertisement?")
	hitretResult := Hitret(34473)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, right there. Well I guess that theseÅ^advertisements suit computers better.")
	hitretResult := Hitret(34474)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050088")
	Mess("What? Go to Akihabara and we'll get cheap computers?")
	hitretResult := Hitret(34475)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH050089")
	Mess("And afterwards, refresh your tired body at the shop...!")
	hitretResult := Hitret(34476)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("N-no, I don't have money for that...")
	hitretResult := Hitret(34477)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050090")
	Mess("Ahh, no problem. We always have our sponsor. Right,Å^Ojou?")
	hitretResult := Hitret(34478)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD01_07M", 0, 0)
	Talk("Kazuha", "KA050027")
	Mess("Tsu, and why would that be me?")
	hitretResult := Hitret(34479)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Although she complains, she'll give us a good responseÅ^since she's a good person.")
	hitretResult := Hitret(34480)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA050028")
	Mess("My father does own a computer. However, it's for hisÅ^work; it's not mine.")
	hitretResult := Hitret(34481)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050091")
	Mess("Don't say it like that, it'll be alright if we only useÅ^it a little bit.")
	hitretResult := Hitret(34482)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA050029")
	Mess("I'm not sure what you mean by \"a little bit\".")
	hitretResult := Hitret(34483)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA050030")
	Mess("And about what I said earlier, the computer is myÅ^father's so I wouldn't know anything about it.")
	hitretResult := Hitret(34484)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Also, about the internet...")
	hitretResult := Hitret(34485)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_08M", 0, 0)
	Talk("Kazuha", "KA050031")
	Mess("The internet? I don't know much about it...")
	hitretResult := Hitret(34486)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So then... Ryouhei, do you not know anything about itÅ^either?")
	hitretResult := Hitret(34487)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH050092")
	Mess("I don't have a computer, so... what about Akira then?Å^It's your turn!")
	hitretResult := Hitret(34488)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK050021")
	Mess("Huh? Me?")
	hitretResult := Hitret(34489)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA050032")
	Mess("Akira is the same as you. What do you want from her?")
	hitretResult := Hitret(34490)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH050093")
	Mess("Hahaha. Just leave it to me.")
	hitretResult := Hitret(34491)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What are you planning?")
	hitretResult := Hitret(34492)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050094")
	Mess("Just be quiet and go along with it.")
	hitretResult := Hitret(34493)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("After school ends.")
	hitretResult := Hitret(34494)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B15b", 0, 0)
	ScPlayBgm("BGM06", 0)
	Talk("Haruka", "")
	Mess("... Yahiro-san's house?")
	hitretResult := Hitret(34495)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We were in front of Yahiro-san's house for some reason.")
	hitretResult := Hitret(34496)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What kind of relationship do Amatsume and Yahiro have?")
	hitretResult := Hitret(34497)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH050095")
	Mess("Hey Akira, is she in?")
	hitretResult := Hitret(34498)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK050022")
	Mess("...Eh, it doesn't look as if that's the case")
	hitretResult := Hitret(34499)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("She's not here? The store's open, though? HowÅ^careless...")
	hitretResult := Hitret(34500)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050096")
	Mess("What's the matter? Worrying about the prices? In thatÅ^case look over there.")
	hitretResult := Hitret(34501)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei pointed to the back with his thumb.")
	hitretResult := Hitret(34502)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("On the table, there was an empty can pinning down aÅ^piece of paper with an arrow drawn on it.")
	hitretResult := Hitret(34503)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Haruka", "")
	Mess("Wait, does she mean leave the money in the can?")
	hitretResult := Hitret(34504)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050097")
	Mess("That's right. ....*")
	hitretResult := Hitret(34505)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei took out a soda flavored Garinba-kun out of theÅ^fridge at the front of the store, then dropped two coinsÅ^in the previously mentioned empty can.")
	hitretResult := Hitret(34506)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I can't believe it, but... I guess that's what she wantsÅ^us to do.")
	hitretResult := Hitret(34507)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I took out a Garinba-kun and put two coins in the emptyÅ^can, as well.")
	hitretResult := Hitret(34508)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH050098")
	Mess("Don't be so afraid. Everybody, go take one.")
	hitretResult := Hitret(34509)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050099")
	Mess("Isn't it common for people to sell, maybe vegetables, inÅ^umanned places? Bad things don't really happen.")
	hitretResult := Hitret(34510)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I know.")
	hitretResult := Hitret(34511)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I thought that I was used to what it's like around hereÅ^for the most part, but I guess I've still got a ways toÅ^go.")
	hitretResult := Hitret(34512)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK050023")
	Mess("Ah! Guess I'll have some ice cream.")
	hitretResult := Hitret(34513)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050100")
	Mess("Yeah, eat up. Since today is hot, it'll be my treat.")
	hitretResult := Hitret(34514)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_04M", 0, 0)
	Talk("Akira", "AK050024")
	Mess("Really? Thanks a lot, Ryouhei-chan!")
	hitretResult := Hitret(34515)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	SetBustup("CF01_01M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Looking happily at the fridge, Amatsume chewed on theÅ^ice cream.")
	hitretResult := Hitret(34516)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Eating ice cream in the summer is so enjoyable that IÅ^can't helpl but feel sorry for Sora.")
	hitretResult := Hitret(34517)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050101")
	Mess("Well, since Yahiro-san still isn't here, what should weÅ^do...")
	hitretResult := Hitret(34518)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What did you want to do here anyway, Ryouhei?")
	hitretResult := Hitret(34519)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050102")
	Mess("Nah, I was just thinking that if Yahiro-san's got aÅ^computer, she should know how to use it and should beÅ^able to teach us a bunch.")
	hitretResult := Hitret(34520)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh, I get it.")
	hitretResult := Hitret(34521)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Given she's not here though, guess we'll have to comeÅ^back.")
	hitretResult := Hitret(34522)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050103")
	Mess("Akira, what are you planning on doing?")
	hitretResult := Hitret(34523)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK050025")
	Mess("I gotta make Hiro-neechan's dinner, so I'm going toÅ^stick around here for the time being.")
	hitretResult := Hitret(34524)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Could you give Yahiro-san a little message then? ThatÅ^we'll be back later?")
	hitretResult := Hitret(34525)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050026")
	Mess("Sure, no problem. I'll let her know when she gets back.")
	hitretResult := Hitret(34526)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("In that case, I'll take another ice cream.")
	hitretResult := Hitret(34527)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	SetBustup("CF01_06M", 0, 0)
	Talk("Ryouhei", "RH050104")
	Mess("You eat a lot, man.")
	hitretResult := Hitret(34528)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's not for me. It's for Sora.")
	hitretResult := Hitret(34529)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH050105")
	Mess("What?! Sora-chan's?! In that case, it's my treat! TakeÅ^as many as you'd like!")
	hitretResult := Hitret(34530)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH050106")
	Mess("Just let her know I was the one to buy them in exchange,Å^alright?")
	hitretResult := Hitret(34531)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I don't mind...")
	hitretResult := Hitret(34532)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She might not eat them if I tell her that, though...")
	hitretResult := Hitret(34533)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Is what I thought, but dared not say.")
	hitretResult := Hitret(34535)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("", 0)
	Change("00_g000")
	Return
	Change("EXIT_SCENARIO")
EndFunc

