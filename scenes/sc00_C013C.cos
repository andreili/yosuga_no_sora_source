Include"yosuga.ch"

Function sc00_C013C()
	ScenarioEnter()
	SetCg("B26a", 0, 0)
	ScPlaySe("se014", 0)
	Talk("êSÇÃê∫", "")
	Mess("............")
	hitretResult := Hitret(20411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The way on the mountain on the back was steep. I onlyÅ^climbed it a little and my breathing got heavier.")
	hitretResult := Hitret(20412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I might lack exercise, but that Amatsume is oftenÅ^climbing such a hill wearing Japanese sandals...")
	hitretResult := Hitret(20413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Whew... How much longer do I have to climb?")
	hitretResult := Hitret(20414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I think I climbed up a lot, but there are no signs ofÅ^lake Migiwa-san was talking about.")
	hitretResult := Hitret(20415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Speaking of which, I came here with Amatsume for cicadasÅ^hunting when I was a kid.")
	hitretResult := Hitret(20416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And, I felt piercing pain in my chest, remembering thatÅ^at that time, I made Amatsume lose her importantÅ^memento.")
	hitretResult := Hitret(20417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume said that I shouldn't care about that, but itÅ^still haunts me since that time.")
	hitretResult := Hitret(20418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	ScPlaySe("se014", 0)
	Talk("Haruka", "")
	Mess(".........")
	hitretResult := Hitret(20419)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wonder how long have I been climbing. It's gottenÅ^brighter above me.")
	hitretResult := Hitret(20420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Huh? Maybe over there...")
	hitretResult := Hitret(20421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I raised my pace, going towards the light shiningÅ^through the gaps between trees.")
	hitretResult := Hitret(20422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopSe()
	SetCg("B25a", 0, 0)
	Transition("MOZCIR", 500)
	ScPlayBgm("BGM03", 0)
	Talk("Haruka", "")
	Mess("Wow... Amazing... It's so vast.")
	hitretResult := Hitret(20423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I haven't checked it on map, but I didn't think thatÅ^there would be such a large lake here.")
	hitretResult := Hitret(20424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was dripping with sweat, because of climbing thatÅ^mountain trail. That's why the wind that blowed againstÅ^me felt especially good.")
	hitretResult := Hitret(20425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That perfectly clear water was cold too and it lookedÅ^that it would feel good.")
	hitretResult := Hitret(20426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020539")
	Mess("Ah! Haru-kuun!!")
	hitretResult := Hitret(20427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Ah, Amatsume... Can I go over there now?")
	hitretResult := Hitret(20428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020540")
	Mess("Eh? Ah, yeah, it's okay! Come!")
	hitretResult := Hitret(20429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-Is it really okay? ...Even though it's me?")
	hitretResult := Hitret(20430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020541")
	Mess("Eh? Yeah, it's all right. It's Haru-kun after all.")
	hitretResult := Hitret(20431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's all right? ...G-Got it...")
	hitretResult := Hitret(20432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...It's okay, because it's me? W-What did she mean byÅ^that?")
	hitretResult := Hitret(20433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If it's me... showing herself is okay? N-No way... ItÅ^couldn't be, though...")
	hitretResult := Hitret(20434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("P-Pardon me...")
	hitretResult := Hitret(20435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020542")
	Mess("Take that!!")
	hitretResult := Hitret(20436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE205", 0)
	Flush("BLUE", 1000, "")
	Talk("Haruka", "")
	Mess("Uwah!?")
	hitretResult := Hitret(20437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020543")
	Mess("Ahahaha!")
	hitretResult := Hitret(20438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Turning over in the direction Amatsume's voice cameÅ^from, I was splashed with water out of nowhere.")
	hitretResult := Hitret(20439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03M", 0, 0)
	Talk("Akira", "AK020544")
	Mess("Thank you. You brought it, even though you're busy.")
	hitretResult := Hitret(20440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... You haven't done the purification ceremony yet,Å^huh?")
	hitretResult := Hitret(20441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020545")
	Mess("Eh? Yeah, I noticed it while climbing here.")
	hitretResult := Hitret(20442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume showed me the telephone extension she's alwaysÅ^carrying with her.")
	hitretResult := Hitret(20443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_05M", 0, 0)
	Talk("Akira", "AK020546")
	Mess("If I phoned Kazu-chan to bring it here, she would getÅ^angry, so I ended up asking you.")
	hitretResult := Hitret(20444)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So that's what it was... I'm relieved...")
	hitretResult := Hitret(20445)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_14M", 0, 0)
	Talk("Akira", "AK020547")
	Mess("Huh? Relieved?")
	hitretResult := Hitret(20446)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, no... Umm... Q-Quite a large lake it is, isn't it?")
	hitretResult := Hitret(20447)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt relieved, or perhaps I should say, a littleÅ^disappointed? It felt strange, though it was normal,Å^huh... While thinking so, in reality, I let out a sigh.")
	hitretResult := Hitret(20448)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020548")
	Mess("Yeah, it's many times bigger than the pool. Ah, look,Å^can you see the shrine over there? I heard that a reallyÅ^long time ago, festival was being held over there.")
	hitretResult := Hitret(20449)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Zoom(560, 180, 400, 300, 0, 0, 0)
	Talk("Haruka", "")
	Mess("Ah, you're right... I can see something looking like aÅ^shrine's archway.")
	hitretResult := Hitret(20450)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just as Amatsume said, an old building was visible onÅ^the opposite shore. Moreover, at the bottom of the lake,Å^though hazy, something like a stone paved passage wasÅ^there.")
	hitretResult := Hitret(20451)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Did the lake appear after it was made? There seems to beÅ^a stone paving there and all.")
	hitretResult := Hitret(20452)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Zoom(400, 300, 800, 600, 0, 0, 0)
	Talk("Akira", "AK020549")
	Mess("There seem to be lot of theories. Like after praying forÅ^a rain, it rained too much and lake was made, or thatÅ^the ground cracked and underground water has gushed out.")
	hitretResult := Hitret(20453)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("E-Either one is disturbing... Though, I guess that'sÅ^just an oral legend.")
	hitretResult := Hitret(20454)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020550")
	Mess("Aah, the Kagura dance I'll be performing today too, I'llÅ^tell you it's story too.")
	hitretResult := Hitret(20455)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heh, seems interesting.")
	hitretResult := Hitret(20456)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020551")
	Mess("Haru-kun, after the first stage of it, come to see,Å^okay?")
	hitretResult := Hitret(20457)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, I've never seen it, so I'll come to watch forÅ^sure.")
	hitretResult := Hitret(20458)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_04M", 0, 0)
	Talk("Akira", "AK020552")
	Mess("Ahah, thank you. I'll put my heart into it and do myÅ^best!")
	hitretResult := Hitret(20459)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I haven't seen you practicing, Amatsume, but have youÅ^had a special training overnight or so?")
	hitretResult := Hitret(20460)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_14M", 0, 0)
	Talk("Akira", "AK020553")
	Mess("No, not at all. Hmm, I have watched the video once, IÅ^guess.")
	hitretResult := Hitret(20461)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh!! Is it so easy?")
	hitretResult := Hitret(20462)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020554")
	Mess("Well then, I'll have to perform the purificationÅ^ceremony soon.")
	hitretResult := Hitret(20463)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!!!")
	hitretResult := Hitret(20464)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though I'm here, Amatsume took the cord around herÅ^waist in her hand and started unfastening it with aÅ^whizz.")
	hitretResult := Hitret(20465)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("A-Amatsume...")
	hitretResult := Hitret(20466)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03M", 0, 0)
	Talk("Akira", "AK020555")
	Mess("Eh? What's wrooong?")
	hitretResult := Hitret(20467)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I'm... still here...")
	hitretResult := Hitret(20468)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020556")
	Mess("Eh, don't you want to see the purification ceremony? YouÅ^haven't seen it yet, right?")
	hitretResult := Hitret(20469)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-Indeed, I haven't seen it yet, but...")
	hitretResult := Hitret(20470)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I heard that the purification ceremony is cleansingÅ^one's body, and it's supposed to be having water pouredÅ^onto yourself, but don't you need to undress everything?")
	hitretResult := Hitret(20471)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("No, if she's about to undo the hakama, will she take offÅ^the top as well?...")
	hitretResult := Hitret(20472)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She's okay with me watching, meaning she has a swimsuitÅ^or something underneath?")
	hitretResult := Hitret(20473)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Could it be that you'll be taking off everything?")
	hitretResult := Hitret(20474)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020557")
	Mess("Just as you say!")
	hitretResult := Hitret(20475)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uooh!?")
	hitretResult := Hitret(20476)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	Update()
	Quake(16, 0, 0, 2, 30)
	Update()
	SetCg("BLACK", 0, 0)
	Transition("WIP_RL", 250)
	Update()
	SetCg("B25a", 0, 0)
	Transition("WIP_RL", 250)
	Talk("êSÇÃê∫", "")
	Mess("In the instant the hakama fell down with a thump, IÅ^hurriedly faced the other way.")
	hitretResult := Hitret(20477)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The purification ceremony is also a ritual, so it couldÅ^be that Amatsume doesn't mind it at all...")
	hitretResult := Hitret(20478)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Well, umm... Amatsume is too defenseless after all...")
	hitretResult := Hitret(20479)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020558")
	Mess("Eeh, what happened, Haru-kun? You won't watch?")
	hitretResult := Hitret(20480)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahaha... As I though... Umm...")
	hitretResult := Hitret(20481)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	SetBustup("CD03_07L", 0, 0)
	Talk("Kazuha", "KA020174")
	Mess("What are you doing?")
	hitretResult := Hitret(20482)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Quake(16, 0, 1, 2, 30)
	Font(36, "")
	Talk("Haruka", "")
	Mess("Uwaaah!!!")
	hitretResult := Hitret(20483)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM14", 0)
	Talk("Kazuha", "KA020175")
	Mess("I came worried if you have forgotten anything, but...Å^you two...")
	hitretResult := Hitret(20484)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020559")
	Mess("Ah, Haru-kun has delivered what I forgot.")
	hitretResult := Hitret(20485)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_05L", 0, 0)
	Talk("Kazuha", "KA020176")
	Mess("Ngh! Anyway, quit undressing in front of other people!!")
	hitretResult := Hitret(20486)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020560")
	Mess("Eeh. But it's the time for me to perform theÅ^purification ceremony. And, since Haru-kun haven't seenÅ^it yet, I asked him if he wanted to watch it.")
	hitretResult := Hitret(20487)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_07L", 0, 0)
	Talk("Haruka", "")
	Mess("Ahaha... Migiwa-san coming has saved me... Well then,Å^I'm going back.")
	hitretResult := Hitret(20488)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020561")
	Mess("Are you? I understand. Thank you for delivering it.")
	hitretResult := Hitret(20489)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("N-Not at all, well then, see you later...")
	hitretResult := Hitret(20491)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_05L", 0, 0)
	Update()
	Quake(16, 0, 0, 3, 30)
	Talk("Haruka", "")
	Mess("Whoa!?")
	hitretResult := Hitret(20492)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020177")
	Mess("Don't you turn around.")
	hitretResult := Hitret(20493)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("O-Okay...")
	hitretResult := Hitret(20494)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I was about to turn back nonchalantly, I had myÅ^head pinned down by Migiwa-san firmly.")
	hitretResult := Hitret(20495)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("", 0)
	Change("00_c013d")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

