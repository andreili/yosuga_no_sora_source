Include"yosuga.ch"

Function sc00_B013D()
	ScenarioEnter()
	SetCg("B07b", 0, 0)
	ScPlayBgm("BGM07", 0)
	Talk("Ryouhei", "RH030279")
	Mess("Unghhh. ...At last it's done huh?")
	hitretResult := Hitret(13287)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_01M", 0, 0)
	Talk("Haruka", "")
	Mess("It was great...")
	hitretResult := Hitret(13288)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's from cleaning things up to helping people fromÅ^here to there, even the day has started to ended.")
	hitretResult := Hitret(13289)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_01M", 0, 0)
	Talk("Sora", "SR030120")
	Mess("...Haru...")
	hitretResult := Hitret(13290)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks, the work here is finished as well right?")
	hitretResult := Hitret(13291)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_04M", 0, 0)
	Talk("Sora", "SR030121")
	Mess("...I want to go home.")
	hitretResult := Hitret(13292)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh, don't you want to go around and look at the stallsÅ^and booths from here?")
	hitretResult := Hitret(13293)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_05M", 0, 0)
	Talk("Sora", "SR030122")
	Mess("...I'm tired.")
	hitretResult := Hitret(13294)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_02M", 0, 0)
	Talk("Ryouhei", "RH030280")
	Mess("Sora-chan, thank's for the work! Do you want me to treatÅ^you with some energetic juice?")
	hitretResult := Hitret(13295)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_04M", 0, 0)
	Talk("Sora", "SR030123")
	Mess("...I don't need it.")
	hitretResult := Hitret(13296)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030281")
	Mess("Now, now, you don't need to be shy right?")
	hitretResult := Hitret(13297)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_13M", 0, 0)
	Shake(4)
	Talk("Sora", "SR030124")
	Mess("...Ah!?")
	hitretResult := Hitret(13298)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_13S", 3, 0)
	SetBustup("CF03_02S", 2, 0)
	Talk("Haruka", "")
	Mess("H-Hey!! Ryouhei! Sora?")
	hitretResult := Hitret(13299)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei took Sora on her arms, though she was alreadyÅ^against it, so she slapped him on the way to the stall.")
	hitretResult := Hitret(13300)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_02M", 0, 0)
	Talk("Nao", "NO030596")
	Mess("The same as usual.")
	hitretResult := Hitret(13301)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, nice job, are you done now?")
	hitretResult := Hitret(13302)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030597")
	Mess("Yeah, I'm glad that the timing was just right.")
	hitretResult := Hitret(13303)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030598")
	Mess("Well then, shall we go look at the booths now?")
	hitretResult := Hitret(13304)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah.")
	hitretResult := Hitret(13305)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_04L", 0, 0)
	Talk("Nao", "NO030599")
	Mess("Well! I'm getting kinda hungry too, let's also eat!!")
	hitretResult := Hitret(13306)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Quake(16, 0, 1, 2, 30)
	Talk("Haruka", "")
	Mess("Whoa?!")
	hitretResult := Hitret(13307)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan wrapped her arms in mine, she kept on draggingÅ^me as we go around the stalls.")
	hitretResult := Hitret(13308)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("I can't feel the emptiness in my stomach, as I looked atÅ^Nao-chan from the side smiling.")
	hitretResult := Hitret(13309)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayEnvSe("SE402", "1", 0)
	SetCg("Black", 0, 0)
	Transition("WIP_MOZH", 500)
	Update()
	SetCg("B07e", 0, 0)
	Transition("WIP_MOZH", 500)
	Talk("êSÇÃê∫", "")
	Mess("We continued to walk until we reached the stalls nearÅ^the Shinto shrine.")
	hitretResult := Hitret(13310)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm familiar with them, Takoyaki, Ikayaki, Yakisoba,Å^goldfish scooping and balloon fishing, it's like aÅ^different pace, they were also gourds with spicesÅ^inside.")
	hitretResult := Hitret(13311)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE402", 0)
	ScPlayBgm("BGM15", 0)
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO030600")
	Mess("Here, Haru-chan.")
	hitretResult := Hitret(13312)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan took some sample from the Caramel food stall,Å^then she broke in half and gave some to me.")
	hitretResult := Hitret(13313)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks, th-this is so... sweet...")
	hitretResult := Hitret(13314)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_02M", 0, 0)
	Talk("Nao", "NO030601")
	Mess("As I thought, this is much more sweeter when you eat it.")
	hitretResult := Hitret(13315)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan licked the powder on her finger and smiled atÅ^me.")
	hitretResult := Hitret(13316)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030602")
	Mess("Haru-chan, do you want some Takoyaki? Or some Yakisoba?")
	hitretResult := Hitret(13317)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Which one should I choose, there's so many eating by theÅ^Takoyaki.")
	hitretResult := Hitret(13318)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030603")
	Mess("Really? We'll have Takoyaki later then, should we eatÅ^Yakisoba first?")
	hitretResult := Hitret(13319)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah sure, I'll get the same as yours.")
	hitretResult := Hitret(13320)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO030604")
	Mess("Okay, I'll treat you. Mister, large serving for two.")
	hitretResult := Hitret(13321)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Street Stall VendorÅsèƒÇ´ÇªÇŒâÆÅt", "NP430001")
	Mess("Here! Thanks for waiting!")
	hitretResult := Hitret(13322)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_04M", 0, 0)
	Talk("Nao", "NO030605")
	Mess("Aha! It's here, it's here! As I thought, this place isÅ^the best!")
	hitretResult := Hitret(13323)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO030606")
	Mess("Here, Haru-chan, do you want some mayonnaise?")
	hitretResult := Hitret(13324)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? No, I don't use that.")
	hitretResult := Hitret(13325)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030607")
	Mess("Okay then, I'll just put a little on mine.")
	hitretResult := Hitret(13326)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan went to get the mayonaise that's colored like aÅ^mustard and placed it on the corner of her Yakisoba.")
	hitretResult := Hitret(13327)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_02M", 0, 0)
	Talk("Nao", "NO030608")
	Mess("Thanks for the food.")
	hitretResult := Hitret(13328)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks for the food.")
	hitretResult := Hitret(13329)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_04M", 0, 0)
	Jump(3, 0)
	Talk("Nao", "NO030609")
	Mess("Mmhmmhmmhmm! Delicious!")
	hitretResult := Hitret(13330)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmmm! You're right! It's totally different from what IÅ^can make back at home.")
	hitretResult := Hitret(13331)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030610")
	Mess("As I thought, this big sizzling plate makes the noodlesÅ^more delicious. There seems to be a secret sauce tooÅ^right?")
	hitretResult := Hitret(13332)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You're right, it gives off that taste right? Somehow,Å^you need to eat it right after you buy it, I wasÅ^thinking of buying two more and bring it home.")
	hitretResult := Hitret(13333)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO030611")
	Mess("Hehehe, is that so Haru-chan? I also thought of thatÅ^though I it's much better if we eat it here.")
	hitretResult := Hitret(13334)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even for a large serving of a Yakisoba, we can feel theÅ^smoothness in our tongues, then we looked for a nextÅ^stall.")
	hitretResult := Hitret(13335)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO030612")
	Mess("What do you want next? Do you want something sweetÅ^Haru-chan? Or do you like another round of Yakisoba?")
	hitretResult := Hitret(13336)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? Again? Nao-chan, you really like Yakisoba.")
	hitretResult := Hitret(13337)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO030613")
	Mess("Yeah, I like it very much. The shop uses differentÅ^ingredients too, it's like we ate something that canÅ^compete on events.")
	hitretResult := Hitret(13338)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030614")
	Mess("But, it would be bad if we got some again, let's eatÅ^those Takoyaki that you like so much Haru-chan! Come on,Å^let's go to that stall. It looks delicious.")
	hitretResult := Hitret(13339)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess(""Dako's Big And Rounded Ikkoyaki!" it says, it looksÅ^like he was selling a bomb-o-yaki as Nao-chan pointed atÅ^the sale's person.")
	hitretResult := Hitret(13340)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_02M", 0, 0)
	Talk("Nao", "NO030615")
	Mess("Excuse me, two pleaase! I'll treat you again because ofÅ^your efforts earlier.")
	hitretResult := Hitret(13341)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thank you, Nao-chan.")
	hitretResult := Hitret(13342)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The person on the booth went out and delivered ourÅ^Takoyaki, the aroma smelled delicious.")
	hitretResult := Hitret(13343)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO030616")
	Mess("I wonder what they put inside. Thanks for the food.")
	hitretResult := Hitret(13344)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... Thanks for the food.")
	hitretResult := Hitret(13345)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_13M", 0, 0)
	Talk("Nao", "NO030617")
	Mess("Ngh!?")
	hitretResult := Hitret(13346)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!!")
	hitretResult := Hitret(13347)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030618")
	Mess("Amazing... the octopus are so many...")
	hitretResult := Hitret(13348)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess(""Rounded Ikkoyaki" is the store's name, they fill up theÅ^Takoyaki with octopus up to its limit and sell them.")
	hitretResult := Hitret(13349)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After that, it's like a heaven full of cabbageÅ^underneath as if the whole Takoyaki was divided.")
	hitretResult := Hitret(13350)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_04M", 0, 0)
	Talk("Nao", "NO030619")
	Mess("Ahaha, it's so fun!")
	hitretResult := Hitret(13351)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The volume of the Takoyaki was really great, we alsoÅ^felt the smoothness of its flavor in our tongues.")
	hitretResult := Hitret(13352)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_02M", 0, 0)
	Talk("Nao", "NO030620")
	Mess("It looks like there's so many new stalls this yearÅ^again, though they're well prepared.")
	hitretResult := Hitret(13353)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There are so many single stores too.")
	hitretResult := Hitret(13354)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030621")
	Mess("Haru-chan, if you're going to operate a booth, what willÅ^you be selling?")
	hitretResult := Hitret(13355)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmmm, a triangle lottery, molding, also... Kebab?")
	hitretResult := Hitret(13356)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO030622")
	Mess("Eh, what's a kebab?")
	hitretResult := Hitret(13357)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("A big meat that's been fried while spinning around, youÅ^scrape the meat of and you put into some kind of bread,Å^then you eat it.")
	hitretResult := Hitret(13358)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030623")
	Mess("Heh, that's something different. As expected from theÅ^city-boy.")
	hitretResult := Hitret(13359)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Though I also don't want to do it alone, just like thoseÅ^shops.")
	hitretResult := Hitret(13360)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030624")
	Mess("That's great, ah, Akira-chan also said something likeÅ^making a booth next year, do you want to join?")
	hitretResult := Hitret(13361)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh, can I do that?")
	hitretResult := Hitret(13362)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO030625")
	Mess("Not sure? Ahahaha, but, there's a lot of stalls thatÅ^comes every year, there couldn't be anything like yourÅ^idea, so just tell us.")
	hitretResult := Hitret(13363)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see... I think it would be really good if we letÅ^Amatsume choose. Though I wonder, what kind of line upÅ^we'll have if that was the case.")
	hitretResult := Hitret(13364)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_02M", 0, 0)
	Talk("Nao", "NO030626")
	Mess("You're right, that girl is quite the unexpected when itÅ^comes to her favorites, like being an Oden vendor,Å^something like a Ramen stall as I can recall.")
	hitretResult := Hitret(13365)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Th-That's like an Oden stall too...")
	hitretResult := Hitret(13366)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO030627")
	Mess("Yeah, she sees that on the television. If I'm notÅ^mistaken, she also wanted to become a Pork Miso soupÅ^vendor, and also wanted to sell Sweetfish soup.")
	hitretResult := Hitret(13367)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Whoa, you would rarely hear those stalls nowadays.")
	hitretResult := Hitret(13368)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Nao", "NO030628")
	Mess("We used to have those back then.")
	hitretResult := Hitret(13369)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If we had those in our summer festivals, we'd be havingÅ^more fun even for a short time...")
	hitretResult := Hitret(13370)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM08", 0)
	SetBustup("CB03_09M", 0, 0)
	Talk("Nao", "NO030629")
	Mess("Well, it can't be helped during these times...")
	hitretResult := Hitret(13371)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_11M", 0, 0)
	Talk("Nao", "NO030630")
	Mess("But... will you always be here... starting this year?")
	hitretResult := Hitret(13372)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh...")
	hitretResult := Hitret(13373)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030631")
	Mess("Even next year... even after next year-- always...")
	hitretResult := Hitret(13374)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030632")
	Mess("I want you to come around again... Haru-chan.")
	hitretResult := Hitret(13375)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Nao-chan.")
	hitretResult := Hitret(13376)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan and I looked at each other straight in ourÅ^eyes.")
	hitretResult := Hitret(13377)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_11L", 0, 0)
	Talk("Nao", "NO030633")
	Mess("Ah... Th-That's... wh-why don't we go there?")
	hitretResult := Hitret(13378)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan softly grabbed my sleeves.")
	hitretResult := Hitret(13379)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was different earlier, I feel like she's being shy onÅ^showing me her feminine side.")
	hitretResult := Hitret(13380)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("Black", 0, 0)
	Transition("WIP_LR", 500)
	Update()
	SetCg("B07e", 0, 0)
	Transition("WIP_LR", 500)
	SetBustup("CB03_01L", 0, 0)
	Talk("Nao", "NO030634")
	Mess("Let's eat some Kakigoori(shaved ice).")
	hitretResult := Hitret(13381)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Yeah.")
	hitretResult := Hitret(13382)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Next time, I'll buy something for Nao-chan when IÅ^something nice.")
	hitretResult := Hitret(13383)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Huh, Nao-chan?")
	hitretResult := Hitret(13384)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030635")
	Mess("I-I'm fine... I think I'll just get a little bit fromÅ^you Haru-chan.")
	hitretResult := Hitret(13385)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The spoon has a hole on its tip which makes it a strawÅ^as well, then Nao-chan ate the oranged flavorÅ^Kakigoori(shaved ice).")
	hitretResult := Hitret(13386)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03L", 0, 0)
	Talk("Nao", "NO030636")
	Mess("Mmm, it's cold and tasty.")
	hitretResult := Hitret(13387)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Let try some too...")
	hitretResult := Hitret(13388)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was dripping, the mountain of ice started to crumble,Å^I also ate the part where there's more syrup in it.")
	hitretResult := Hitret(13389)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I can feel it above my tongue as it slowly melted, thenÅ^I swallowed it.")
	hitretResult := Hitret(13390)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The ice felt like it's twinkling inside, it's soÅ^delicious.")
	hitretResult := Hitret(13391)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_02L", 0, 0)
	Talk("Nao", "NO030637")
	Mess("Yeah... If I can make this myself, then I would alwaysÅ^eat something like this.")
	hitretResult := Hitret(13392)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After we crushed the mountain of ice, we continued toÅ^eat them.")
	hitretResult := Hitret(13393)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Ah...")
	hitretResult := Hitret(13394)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01L", 0, 0)
	Talk("Nao", "NO030638")
	Mess("Ah, look out...")
	hitretResult := Hitret(13395)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The ice fell as if it's like an avalance, but Nao-chanÅ^caught them with her spoon.")
	hitretResult := Hitret(13396)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sorry... thank you.")
	hitretResult := Hitret(13397)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_02L", 0, 0)
	Talk("Nao", "NO030639")
	Mess("It's fine... ah... Haru-chan, here... Ahn?")
	hitretResult := Hitret(13398)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh... I-It's fine...")
	hitretResult := Hitret(13399)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030640")
	Mess("Look, it's melting you know?")
	hitretResult := Hitret(13400)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Y-Yeah... Ahn...")
	hitretResult := Hitret(13401)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I made sure everything was inside my mouth, though IÅ^felt a bit embarrassed right away.")
	hitretResult := Hitret(13402)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Th-Thank you...")
	hitretResult := Hitret(13403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_11L", 0, 0)
	Talk("Nao", "NO030641")
	Mess("It's nothing...")
	hitretResult := Hitret(13404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We don't usually do these things, it's quite strange,Å^and it just made more embarrassed.")
	hitretResult := Hitret(13405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Eating the Kakigoori(shaved ice) is certainlyÅ^refreshing, though I felt an unusual heat on my cheeks.")
	hitretResult := Hitret(13406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030642")
	Mess("Now then... what shall we do next...?")
	hitretResult := Hitret(13407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Umm...")
	hitretResult := Hitret(13408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	BustupClear()
	SetBustup("CA03_08L", 0, 0)
	Quake(0, 16, 1, 2, 30)
	Talk("Sora", "SR030125")
	Mess("Haruuu!!")
	hitretResult := Hitret(13409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh?!")
	hitretResult := Hitret(13410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora suddenly jumped on my chest.")
	hitretResult := Hitret(13411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("S-Sora? Where have you been?")
	hitretResult := Hitret(13412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	ScPlayBgm("BGM13", 0)
	SetBustup("CF03_05M", 0, 0)
	Talk("Ryouhei", "RH030282")
	Mess("What the... just a little more I could have set a greatÅ^mood guys, you know?")
	hitretResult := Hitret(13413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Ryouhei!")
	hitretResult := Hitret(13414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030283")
	Mess("I even gave it all just for my love time with Sora-chan.")
	hitretResult := Hitret(13415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!? ...Thanks, for being with Sora.")
	hitretResult := Hitret(13416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_03M", 0, 0)
	Talk("Ryouhei", "RH030284")
	Mess("No, no, there's no need for that.")
	hitretResult := Hitret(13417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("B-But...")
	hitretResult := Hitret(13418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora looked like she's been buying stuffs, as she alsoÅ^had water balloons on her fingers. Aside from her mask,Å^there's a lot more attatched to her.")
	hitretResult := Hitret(13419)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Wh-What have you been doing?")
	hitretResult := Hitret(13420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_04M", 0, 0)
	Talk("Ryouhei", "RH030285")
	Mess("Date! YAHOO!")
	hitretResult := Hitret(13421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_06M", 0, 0)
	Talk("Nao", "NO030643")
	Mess("Disgusting... I somehow felt my hair stood there...")
	hitretResult := Hitret(13422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_06M", 0, 0)
	Talk("Sora", "SR030126")
	Mess("...Ughh...")
	hitretResult := Hitret(13423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora held onto me while holding the things she bought.")
	hitretResult := Hitret(13424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even if it was quite disturbing, at least Sora canÅ^coordinate properly and keep quiet.")
	hitretResult := Hitret(13425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030286")
	Mess("Oh my, it was really fun. Eating sweets, playing games,Å^giving presents!")
	hitretResult := Hitret(13426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I'm glad, Sora...")
	hitretResult := Hitret(13427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA03_04L", 0, 0)
	Talk("Sora", "SR030127")
	Mess("Hmph!!")
	hitretResult := Hitret(13428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se006", 0)
	Quake(0, 16, 1, 2, 30)
	Talk("Haruka", "")
	Mess("Ouch!?")
	hitretResult := Hitret(13429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR030128")
	Mess("...Let's go home.")
	hitretResult := Hitret(13430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("A-Already?")
	hitretResult := Hitret(13431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF03_03M", 0, 0)
	Talk("Ryouhei", "RH030287")
	Mess("Why don't you take it slow? The night is still young youÅ^know?")
	hitretResult := Hitret(13432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_06M", 0, 0)
	Talk("Nao", "NO030644")
	Mess("Really, what the heck are you thinking?")
	hitretResult := Hitret(13433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030288")
	Mess("It's fine you know, you guys are even eating theÅ^Kakigoori and being in a lovey-dovey mood.")
	hitretResult := Hitret(13434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_13M", 0, 0)
	Jump(3, 0)
	Talk("Nao", "NO030645")
	Mess("Hiii! Th-That's different--! That's just-- I was...")
	hitretResult := Hitret(13435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_04M", 0, 0)
	Talk("Ryouhei", "RH030289")
	Mess("That's fine... You guys are perfect, showing off likeÅ^that.")
	hitretResult := Hitret(13436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_08M", 0, 0)
	Talk("Nao", "NO030646")
	Mess("Wh-What the heck are you saying?!")
	hitretResult := Hitret(13437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA03_04L", 0, 0)
	Talk("Sora", "SR030129")
	Mess("Haru... let's go home...")
	hitretResult := Hitret(13438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With a jerk, Sora pulled my hand.")
	hitretResult := Hitret(13439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("B-But...")
	hitretResult := Hitret(13440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR030130")
	Mess("Come on...")
	hitretResult := Hitret(13441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA03_04M", 0, 0)
	SetBustup("CF03_02M", 0, 0)
	Talk("Ryouhei", "RH030290")
	Mess("If you're going home, do you want me to buy someÅ^souvenir?")
	hitretResult := Hitret(13442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_06M", 0, 0)
	Talk("Sora", "SR030131")
	Mess("I-DON'T-NEED-IT!!!")
	hitretResult := Hitret(13443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, that's right... cotton candy! You like that right?")
	hitretResult := Hitret(13444)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030291")
	Mess("Alright! I'll give it to you as a present!")
	hitretResult := Hitret(13445)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_13M", 0, 0)
	Shake(4)
	Talk("Sora", "SR030132")
	Mess("NOOO!!")
	hitretResult := Hitret(13446)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_06M", 0, 0)
	Talk("Nao", "NO030647")
	Mess("Really, why don't just cut it out?")
	hitretResult := Hitret(13447)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_03M", 0, 0)
	Talk("Ryouhei", "RH030292")
	Mess(""Cast your embarrassment away" As they say right?")
	hitretResult := Hitret(13448)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030648")
	Mess("Are you really going to continue with that?")
	hitretResult := Hitret(13449)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030293")
	Mess("Well, even if those are uncertain words, I still gave itÅ^my all on this festival, just to have a littleÅ^bit-more-of-fun-right?")
	hitretResult := Hitret(13450)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetBustup("CB03_08M", 0, 0)
	Talk("Nao", "NO030649")
	Mess("You're really annoying...")
	hitretResult := Hitret(13451)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_01M", 0, 0)
	Talk("Sora", "SR030133")
	Mess("...Let's go home?")
	hitretResult := Hitret(13452)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Do you really want to go?")
	hitretResult := Hitret(13453)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR030134")
	Mess("...Yeah.")
	hitretResult := Hitret(13454)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay then, let's just buy the cotton candy and go home.")
	hitretResult := Hitret(13455)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_09M", 0, 0)
	Talk("Nao", "NO030650")
	Mess("...Haru-chan...")
	hitretResult := Hitret(13456)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora was also outside throughout the whole day, sheÅ^might be tired and might collapse as well.")
	hitretResult := Hitret(13457)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030294")
	Mess("If that's the case, I'll carry her to the bed!")
	hitretResult := Hitret(13458)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_06M", 0, 0)
	Talk("Nao", "NO030651")
	Mess("Stop it already.")
	hitretResult := Hitret(13459)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM16", 0)
	Talk("Haruka", "")
	Mess("Well, we'll be going home early. Nao-chan, thanks,Å^thanks for everything.")
	hitretResult := Hitret(13460)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_11M", 0, 0)
	Talk("Nao", "NO030652")
	Mess("It's nothing...")
	hitretResult := Hitret(13461)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'll treat you next time.")
	hitretResult := Hitret(13462)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_03M", 3, 0)
	SetBustup("CB03_11M", 6, 0)
	SetBustup("CA03_01L", 10, 0)
	Talk("êSÇÃê∫", "")
	Mess("Sora held on to my arms tighyly.")
	hitretResult := Hitret(13463)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH030295")
	Mess("What's that... that's so boring.")
	hitretResult := Hitret(13464)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm really sorry... well, good night.")
	hitretResult := Hitret(13465)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO030653")
	Mess("...Yeah, thanks for the work, Haru-chan, Sora-chan.")
	hitretResult := Hitret(13466)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030296")
	Mess("Bye then, see you again okay? Sora-chan too!")
	hitretResult := Hitret(13467)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("We bought the cotton as we were heading home. SoraÅ^quickly let go of my arms as soon as we reached ourÅ^house.")
	hitretResult := Hitret(13468)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1000, 0)
	WindowView(1)
	SetCg("B07e", 0, 0)
	SetBustup("CB03_05M", 0, 0)
	Transition("", 1000)
	Talk("Nao", "NO030654")
	Mess("Sigh...")
	hitretResult := Hitret(13469)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH030297")
	Mess("What's that, what are you sighing for?")
	hitretResult := Hitret(13470)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_09M", 0, 0)
	Talk("Nao", "NO030655")
	Mess("Nothing...")
	hitretResult := Hitret(13471)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030298")
	Mess("Well, I took care of Sora-chan for you, and you also hadÅ^a good time as it looked like.")
	hitretResult := Hitret(13472)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(7)
	SetBustup("CB03_11M", 0, 0)
	Talk("Nao", "NO030656")
	Mess("...Eh...")
	hitretResult := Hitret(13473)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030657")
	Mess(".........")
	hitretResult := Hitret(13474)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_05M", 0, 0)
	Talk("Nao", "NO030658")
	Mess("...What's that all about?...")
	hitretResult := Hitret(13475)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	WindowView(0)
	Change("00_b014")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

