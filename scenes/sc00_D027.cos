Include"yosuga.ch"

Function sc00_D027()
	ScenarioEnter()
	SetCg("B12a", 0, 0)
	ScPlayBgm("BGM05", 0)
	Update()
	ScPlaySe("SE259", 1)
	Talk("VoiceÅsàÍótÅt", "KA040944")
	Mess("...Kasugano-kun? That's you, isn't it?")
	hitretResult := Hitret(31215)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While heading toward the plaza to do some shopping, aÅ^car pulled up beside me, and a voice called out.")
	hitretResult := Hitret(31216)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The black car was one I recognized, and the voice wasÅ^also one I knew.")
	hitretResult := Hitret(31217)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kazu...Migiwa-san, going out?")
	hitretResult := Hitret(31218)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I almost used her first name on accident. Was thatÅ^bad...?")
	hitretResult := Hitret(31219)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Since it's a car, there had to be a driver in there, andÅ^what if the whole family was...?")
	hitretResult := Hitret(31220)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA040945")
	Mess("...Be careful! Yaminoshi-san's the only one in thereÅ^now, so I don't think he noticed.")
	hitretResult := Hitret(31221)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I even got Kazuha angry.")
	hitretResult := Hitret(31222)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA040946")
	Mess("I'm going to the next town. I've got something to doÅ^there, but if you're free, do you want to come too?")
	hitretResult := Hitret(31223)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The next town? Not the one where the school is?")
	hitretResult := Hitret(31224)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040947")
	Mess("I think it's about 40 minutes away by car. It's the mostÅ^civilized town in the area, but...")
	hitretResult := Hitret(31225)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kazuha glanced at the shopping basket I had, and a smallÅ^shadow of disappointment crossed her face.")
	hitretResult := Hitret(31226)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA040948")
	Mess("If you're not free, then I guess you can't...")
	hitretResult := Hitret(31227)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, I was going to do some shopping, so it's fine.Å^But is it okay for me to get into the Migiwa's car?")
	hitretResult := Hitret(31228)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040949")
	Mess("It's just a car like any other. Don't worry about it.")
	hitretResult := Hitret(31229)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's too elegant to be a normal car, though.")
	hitretResult := Hitret(31230)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Well, I guess I'll go with you, then. I haven't beenÅ^to the next town, anyway.")
	hitretResult := Hitret(31231)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_03M", 0, 0)
	Talk("Kazuha", "KA040950")
	Mess("Hehe, I'm glad. Now that that's decided, get on, get on!")
	hitretResult := Hitret(31232)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("O-O-Okay, no need to push!")
	hitretResult := Hitret(31233)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide()
	SetCg("B27a", 0, 0)
	Update()
	ScPlaySe("SE263", 1)
	ScPlaySe("SE260", 0)
	ScWait(3000, 0)
	Talk("êSÇÃê∫", "")
	Mess("That Kazuha...when she's in a good mood, she gets asÅ^excited as Akira...")
	hitretResult := Hitret(31234)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Can't blame her though!ÅÙ")
	hitretResult := Hitret(31235)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(0, 1)
	SetCg("B39a", 0, 0)
	Transition("WIP_LR", 500)
	ScPlayBgm("BGM15", 0)
	Talk("êSÇÃê∫", "")
	Mess("And so, a little over a 40 minute drive away from HomiÅ^Station,")
	hitretResult := Hitret(31236)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_03M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Kazuha and I finally arrived at an arcade.")
	hitretResult := Hitret(31237)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The secretary who was driving the car left on some otherÅ^business, so it was just the two of us.")
	hitretResult := Hitret(31238)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040951")
	Mess("It's finally just the two of us now, Haruka.")
	hitretResult := Hitret(31239)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There's still people shopping out here, so we might haveÅ^to...")
	hitretResult := Hitret(31240)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040952")
	Mess("It's fine. There's barely anyone who comes out here. WeÅ^won't meet anyone that we know.")
	hitretResult := Hitret(31241)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_03L", 0, 0)
	Jump(5, 0)
	Talk("Kazuha", "KA040953")
	Mess("O-Over there! There seems to be a crowd forming overÅ^there! Let's go!")
	hitretResult := Hitret(31242)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("W-Wait up, Kazuha...")
	hitretResult := Hitret(31243)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("After thinking about whether or not to grab my hand, sheÅ^ran off.")
	hitretResult := Hitret(31244)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Having no choice, I went with her.")
	hitretResult := Hitret(31245)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But there was no way I could think negatively, since theÅ^chances we have to act as a couple are few, making meÅ^feel more content that anything else.")
	hitretResult := Hitret(31246)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide()
	SetCg("BLACK", 0, 0)
	Transition("WIP_LR", 500)
	Update()
	SetCg("B39a", 0, 0)
	SetBustup("CD02_03M", 0, 0)
	Transition("WIP_RL", 500)
	Talk("êSÇÃê∫", "")
	Mess("That "crowd" turned out to be so sparse that even thoughÅ^there were two of us, we could get through easily.")
	hitretResult := Hitret(31247)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It turns out that the crowd was the audience of aÅ^pantomime performance.")
	hitretResult := Hitret(31248)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In the center, two people were moving hesitatingly likeÅ^robots.")
	hitretResult := Hitret(31249)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("One of them was dancing like a rusty toy soÅ^realistically that you could almost hear the creakingÅ^sounds.")
	hitretResult := Hitret(31250)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The second one was copying the first, though one beatÅ^slower... However, for some reason he was doing theÅ^opposite action, like a mirror.")
	hitretResult := Hitret(31251)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The fact that they were mismatched made them lookÅ^strange, making people laugh.")
	hitretResult := Hitret(31252)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahahaha, there's stuff like this too.")
	hitretResult := Hitret(31253)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA040954")
	Mess("All sorts of minstrels and people like that appearÅ^during summer vacation. I've even seen someone on aÅ^unicycle.")
	hitretResult := Hitret(31254)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("M-Minstrels? Not street performers?")
	hitretResult := Hitret(31255)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's a really old term.")
	hitretResult := Hitret(31256)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_10M", 0, 0)
	Talk("Kazuha", "KA040955")
	Mess("W-Who cares about things like that!? ...We're hereÅ^anyway, so let's do some shopping!")
	hitretResult := Hitret(31257)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Saying this, she let go of my hand and walked off, but IÅ^knew she was anxious, since she kept glancing backward.")
	hitretResult := Hitret(31258)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt sorry for the street performers, but to me,Å^Kazuha was acting funnier.")
	hitretResult := Hitret(31259)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If they're still here after we're done shopping, I'llÅ^suggest we stay and watch.")
	hitretResult := Hitret(31260)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	SetCg("B39a", 0, 0)
	SetBustup("CD02_03M", 0, 0)
	Transition("WIP_LR", 500)
	Talk("Haruka", "")
	Mess("Hey, what did you have to here, anyway?")
	hitretResult := Hitret(31261)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040956")
	Mess("There's a dressmaker over here. I was asked to go there,Å^as well as a decorative arts store.")
	hitretResult := Hitret(31262)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040957")
	Mess("Hoever, that comes afterward. We can't do much if we'reÅ^carrying all that.")
	hitretResult := Hitret(31263)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess(""Dressmaker..." "was asked..." That doesn't really feelÅ^like shopping...")
	hitretResult := Hitret(31264)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Do you come here often?")
	hitretResult := Hitret(31265)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA040958")
	Mess("Not really. Just when I have things to do here, I guess?Å^It's quite far, after all.")
	hitretResult := Hitret(31266)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040959")
	Mess("But from now on, I might come here a lot more often...")
	hitretResult := Hitret(31267)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_11L", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("All of a sudden, Kazuha grabbed my arm.")
	hitretResult := Hitret(31268)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Which means, "for dates."")
	hitretResult := Hitret(31269)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Unlike Homi, there were hamburgers and fast food, andÅ^although it was small, there was even something like aÅ^shopping center here.")
	hitretResult := Hitret(31270)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was probably perfect for dates.")
	hitretResult := Hitret(31271)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, you've got a Seattle here. When I used to live inÅ^the city, I'd visit it sometimes on my way home fromÅ^school.")
	hitretResult := Hitret(31272)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_12L", 0, 0)
	Talk("Kazuha", "KA040960")
	Mess("...You've been in there?")
	hitretResult := Hitret(31273)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? Why are you so shocked?")
	hitretResult := Hitret(31274)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_10L", 0, 0)
	Talk("Kazuha", "KA040961")
	Mess("I-I'm not shocked...I've just never been in thereÅ^before, so...")
	hitretResult := Hitret(31275)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah...are you the type that hates coffee?")
	hitretResult := Hitret(31276)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Espresso bar Seattle was, as the name says, a cafe thatÅ^specializes in espresso coffee.")
	hitretResult := Hitret(31277)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("They add milk and cream, as well as toppings likeÅ^chocolate or caramel sauce, and you can even customizeÅ^it. There's juice too, but it's definitely more of aÅ^coffee place.")
	hitretResult := Hitret(31278)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's been ages, the shop has a good atmosphere, so IÅ^thought it was a perfect place to go, but if Kazuha saysÅ^no...")
	hitretResult := Hitret(31279)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040962")
	Mess("I-I don't dislike it at all... Okay, let's go in.")
	hitretResult := Hitret(31280)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("Ah, wait up, Kazuha.")
	hitretResult := Hitret(31281)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Did I say something to hurt her feelings? I don't thinkÅ^so, but...")
	hitretResult := Hitret(31282)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide()
	SetCg("BLACK", 0, 0)
	Transition("WIP_LR", 500)
	Update()
	SetCg("B39a", 0, 0)
	SetBustup("CD02_01M", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Kazuha", "KA040963")
	Mess("......")
	hitretResult := Hitret(31283)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Although Kazuha had entered first, for some reason sheÅ^was standing in front of the counter, dead silent.")
	hitretResult := Hitret(31284)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A couple that entered after her glanced at her, and thenÅ^went in front to order first.")
	hitretResult := Hitret(31285)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's mean, going in without me.Å^But I'm glad you waited before ordering.")
	hitretResult := Hitret(31286)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040964")
	Mess("......")
	hitretResult := Hitret(31287)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kazuha?")
	hitretResult := Hitret(31288)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even when I called out to her, she was still standingÅ^there stiffly.")
	hitretResult := Hitret(31289)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Aren't you going to order?")
	hitretResult := Hitret(31290)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA040965")
	Mess("........")
	hitretResult := Hitret(31291)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She murmured something softly, but it was too soft and IÅ^couldn't catch it.")
	hitretResult := Hitret(31292)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Thinking that she actually must not like coffee, IÅ^started getting uneasy again.")
	hitretResult := Hitret(31293)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Do you want to go to another shop? It's not like I'mÅ^attached to this one...")
	hitretResult := Hitret(31294)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040966")
	Mess("...There's a huge range....")
	hitretResult := Hitret(31295)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh?")
	hitretResult := Hitret(31296)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040967")
	Mess("The people from before just recited their order like aÅ^chant.")
	hitretResult := Hitret(31297)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh..."Hazelnut Frappuccino with extra caramel andÅ^whipped cream."")
	hitretResult := Hitret(31298)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_12M", 0, 0)
	Talk("Kazuha", "KA040968")
	Mess("You got that!?")
	hitretResult := Hitret(31299)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's right, it's your first time here. I guess youÅ^would be confused about the toppings on your first time.")
	hitretResult := Hitret(31300)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040969")
	Mess("...You're used to it, huh... City kids really areÅ^different...")
	hitretResult := Hitret(31301)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's easy once you do it yourself. I'll show you how.Å^First select what you want to drink and what size.")
	hitretResult := Hitret(31302)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_10M", 0, 0)
	Talk("Kazuha", "KA040970")
	Mess("O-Okay...Umm... What is there...?")
	hitretResult := Hitret(31303)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The most basic is a latte, I suppose. There's otherÅ^coffee, but I'm going to have a latte with extra milk,Å^whipped cream, and caramel sauce.")
	hitretResult := Hitret(31304)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040971")
	Mess("You're amazing...I have no clue what to get.")
	hitretResult := Hitret(31305)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You don't need to say it as fluently as the people fromÅ^before. It's fine, as long as you say all the toppingsÅ^you want. Okay, have a try.")
	hitretResult := Hitret(31306)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040972")
	Mess("Mm...")
	hitretResult := Hitret(31307)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("After I made my order, Kazuha timidly, very timidly,Å^made hers.")
	hitretResult := Hitret(31308)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Something like: lots of milk, not too sweet, lots ofÅ^whipped cream.")
	hitretResult := Hitret(31309)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Watching the inexperienced Kazuha order her drink wasÅ^pleasant, but being able to see naivety made me evenÅ^happier.")
	hitretResult := Hitret(31310)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_03M", 0, 0)
	Talk("Kazuha", "KA040973")
	Mess("That's a strange container they use. It's not the normalÅ^paper cup.")
	hitretResult := Hitret(31311)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Do you know how to drink it? The part of the lid thatÅ^sticks out has a hole in it...")
	hitretResult := Hitret(31312)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040974")
	Mess("Ah, this?...*gulp* *gulp*")
	hitretResult := Hitret(31313)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040975")
	Mess("Mm, not bad... But it's still a bit too sweet, I think.")
	hitretResult := Hitret(31314)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Perhaps you should try a double shot next time. It'llÅ^make it more bitter.")
	hitretResult := Hitret(31315)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040976")
	Mess("I'll try that.Å^...Hehe, it's actually quite interesting.")
	hitretResult := Hitret(31316)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Making a coffee you like is quite fun. It seems thatÅ^you've caught on, too.")
	hitretResult := Hitret(31317)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_10M", 0, 0)
	Talk("Kazuha", "KA040977")
	Mess("...But I'm still uneasy about it, so...you'll be withÅ^me, right?")
	hitretResult := Hitret(31318)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Of course, you can count on me.")
	hitretResult := Hitret(31319)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_11M", 0, 0)
	Talk("Kazuha", "KA040978")
	Mess("Thanks, Haruka.")
	hitretResult := Hitret(31320)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("We decided to get take-out coffee and walk around theÅ^arcade.")
	hitretResult := Hitret(31321)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We had a laugh at the pantomime performance we sawÅ^before, and also had a peek into the 100 yen shop.")
	hitretResult := Hitret(31322)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There was also a huge bookstore there, and since it'dÅ^been so long, we went in. However, when we noticed, anÅ^hour had already passed.")
	hitretResult := Hitret(31323)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA040979")
	Mess("It's my first time ever spending so long in a bookstore.")
	hitretResult := Hitret(31324)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Same here. And I'm usually standing in the magazineÅ^corner reading.")
	hitretResult := Hitret(31325)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I can't believe we went and looked through all theÅ^shelves.")
	hitretResult := Hitret(31326)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You really read a lot, huh? Just listening to yourÅ^explanations was fun.")
	hitretResult := Hitret(31327)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_10M", 0, 0)
	Talk("Kazuha", "KA040980")
	Mess("I haven't read that much. Just a few novels...")
	hitretResult := Hitret(31328)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Probably the only books I've read with all text are myÅ^textbooks.")
	hitretResult := Hitret(31329)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But I'll be sure to properly read those novels youÅ^recommended for me.")
	hitretResult := Hitret(31330)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her explanations made them sound interesting, so IÅ^bought them on impulse.")
	hitretResult := Hitret(31331)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("They were paperback books, but they were quite thick...")
	hitretResult := Hitret(31332)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA040981")
	Mess("Once you've read them, tell me what you think, okay? Ah,Å^that's right. Since we're here anyway, let's get a bookÅ^cover.")
	hitretResult := Hitret(31333)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_03M", 0, 0)
	Talk("Kazuha", "KA040982")
	Mess("There's a good shop over there. It specializes inÅ^stationery like that.")
	hitretResult := Hitret(31334)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_03L", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Grabbing my hand again, we ran off.")
	hitretResult := Hitret(31335)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("Ah, o-okay... There's no need to drag me like that.")
	hitretResult := Hitret(31336)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("How many times have I been dragged along by KazuhaÅ^today?")
	hitretResult := Hitret(31337)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But thanks to that, I've started to go into highÅ^tension.")
	hitretResult := Hitret(31338)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Most of it was just window shopping, but talking aboutÅ^this and that with her was very fun.")
	hitretResult := Hitret(31339)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Actually, as long as I'm with Kazuha, anything andÅ^everything would probably be fun.")
	hitretResult := Hitret(31340)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And as we hold hands, I think I can sense that Kazuha'sÅ^also having a lot of fun too.")
	hitretResult := Hitret(31341)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Responding to that, I tightened my grip on her hand,Å^making her blush deeply. Very cute.")
	hitretResult := Hitret(31342)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Both of us had forgotten the passage of time, andÅ^completely enjoyed this spontaneous date.")
	hitretResult := Hitret(31343)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(3000, 1)
	ScPlayBgm("BGM07", 0)
	SetCg("B01b", 0, 0)
	SetBustup("CD02_02M", 0, 0)
	Transition("WIP_TLBR", 500)
	Talk("Kazuha", "KA040983")
	Mess("I'm sorry, I've used up all your time today.")
	hitretResult := Hitret(31344)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And as the sun began to sink below the mountains, weÅ^returned to our own town.")
	hitretResult := Hitret(31345)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With the radiant setting sun shining in our eyes, weÅ^looked at each other with squinting eyes.")
	hitretResult := Hitret(31346)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The shopping I set out to do before was done at aÅ^discount supermarket on the road home, thanks to KazuhaÅ^and her car.")
	hitretResult := Hitret(31347)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Not at all, I had a lot of fun too.Å^And you also helped me with the shopping.")
	hitretResult := Hitret(31348)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_03M", 0, 0)
	Talk("Kazuha", "KA040984")
	Mess("That was something minor. Don't worry about it.")
	hitretResult := Hitret(31349)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It was a big help, though. Thanks.")
	hitretResult := Hitret(31350)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_10M", 0, 0)
	Talk("Kazuha", "KA040985")
	Mess("Y-Yeah...")
	hitretResult := Hitret(31351)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kazuha nodded, but had made no signs of leaving.")
	hitretResult := Hitret(31352)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was also a bit reluctant to part, so I didn't reallyÅ^feel like opening the gate to my house, either.")
	hitretResult := Hitret(31353)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just a bit longer...just a bit...")
	hitretResult := Hitret(31354)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Thinking that, we stood there looking at each other.")
	hitretResult := Hitret(31355)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Only when the sun was completely hidden, and the radiantÅ^rays had dyed the sky scarlet, was I able to shake offÅ^my feelings.")
	hitretResult := Hitret(31356)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_11M", 0, 0)
	Talk("Haruka", "")
	Mess("Thanks for today... It was really fun.")
	hitretResult := Hitret(31357)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040986")
	Mess("Yes, it was... Let's... Let's go somewere togetherÅ^again.")
	hitretResult := Hitret(31358)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Mm, next time, I'll invite you out.")
	hitretResult := Hitret(31359)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040987")
	Mess("I'll be waiting... See you later then.")
	hitretResult := Hitret(31360)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Bye.")
	hitretResult := Hitret(31361)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Hide()
	Update()
	ScPlaySe("SE263", 1)
	ScPlaySe("SE260", 0)
	Talk("Haruka", "")
	Mess("Again... Huh?")
	hitretResult := Hitret(31362)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I muttered, as Kazuha's car finally disappeared into theÅ^distance.")
	hitretResult := Hitret(31363)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I said I'd invite her next time, but what kind of dateÅ^would satisfy an Ojou-sama like Kazuha?")
	hitretResult := Hitret(31364)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Something like today...?")
	hitretResult := Hitret(31365)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Hmmm, this is a difficult problem...")
	hitretResult := Hitret(31366)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	Change("00_d028")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

