Include"yosuga.ch"

Function sc00_B020B()
	ScenarioEnter()
	ScPlayBgm("BGM05", 0)
	Talk("Soccer Club Member A", "NP230001")
	Mess("Ooiy! Yorihime!! Can we use the pool?")
	hitretResult := Hitret(14632)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_11M", 0, 0)
	Talk("Nao", "NO031012")
	Mess("Ah... Yes, it's fine but...")
	hitretResult := Hitret(14633)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan looked at me as she said that.")
	hitretResult := Hitret(14634)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Let's take a break.")
	hitretResult := Hitret(14635)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's quite embarrassing when someone sees us practicingÅ^alone.")
	hitretResult := Hitret(14636)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Soccer Club Member A", "NP230002")
	Mess("Alright! It's fine with you right?")
	hitretResult := Hitret(14637)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I saw a soccer ball from one of their bags, they'reÅ^probably from the club.")
	hitretResult := Hitret(14638)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From the school grounds, we can hear the sound of a ballÅ^being hit, perhaps the baseball team is practicing inÅ^the school grounds.")
	hitretResult := Hitret(14639)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Soccer Club Member B", "NP240001")
	Mess("Hyaa, it's so tiring today, let's wash our sweats shallÅ^we?")
	hitretResult := Hitret(14640)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Soccer Club Member C", "NP250001")
	Mess("Idiot! This isn't a bath.")
	hitretResult := Hitret(14641)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_09M", 0, 0)
	Talk("Nao", "NO031013")
	Mess("Sorry, Haru-chan...")
	hitretResult := Hitret(14642)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's fine, don't worry about it.")
	hitretResult := Hitret(14643)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("As we got off the pool, the soccer club went in, weÅ^heard so many footsteps as they all came in.")
	hitretResult := Hitret(14644)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There were about ten people mixed in with the seniorsÅ^and the juniors, they're swimming, diving, and they'reÅ^also noisy, our peaceful environment in the pool wereÅ^gone.")
	hitretResult := Hitret(14645)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We sat by the poolside as we watched them.")
	hitretResult := Hitret(14646)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_01M", 0, 0)
	Talk("Nao", "NO031014")
	Mess("Even though they were tired by their club activities,Å^they still had so much energy left.")
	hitretResult := Hitret(14647)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, playing in your club is quite different fromÅ^playing here right?")
	hitretResult := Hitret(14648)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031015")
	Mess("Ah, oh yeah Haru-chan, did you have any club activities?")
	hitretResult := Hitret(14649)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, because I was with the \"Let's go home\"-club. WeÅ^usually play around the streets, in the arcade, we justÅ^go anywhere.")
	hitretResult := Hitret(14650)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031016")
	Mess("That sounds great, so that's how students live in theÅ^city huh, sounds fun.")
	hitretResult := Hitret(14651)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("And because I don't have any money, it was hard to getÅ^by.")
	hitretResult := Hitret(14652)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031017")
	Mess("Well then, it looks like you can save more here right?Å^Since you don't have to spend much money here.")
	hitretResult := Hitret(14653)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, I guess you're right about that.")
	hitretResult := Hitret(14654)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_02M", 0, 0)
	Talk("Nao", "NO031018")
	Mess("Hehehe, you will be able to in any way.")
	hitretResult := Hitret(14655)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Female Student AÅsìﬁèèÇÃóFíBÅt", "NP260001")
	Mess("Ah, Nao? Is it fine if we use the pool?")
	hitretResult := Hitret(14656)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_10M", 0, 0)
	Talk("Nao", "NO031020")
	Mess("Ah... Y-Yeah, it's fine...")
	hitretResult := Hitret(14657)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Female Student AÅsìﬁèèÇÃóFíBÅt", "NP260002")
	Mess("Thaaank youuu! We'll just borrow 'kay?")
	hitretResult := Hitret(14658)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She called Nao-chan's by her first name, they'reÅ^probably close friends.")
	hitretResult := Hitret(14659)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She's just lively as the sun today, she waved her handsÅ^from the locker room and every girl followed her.")
	hitretResult := Hitret(14660)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Someone from your class?")
	hitretResult := Hitret(14661)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_11M", 0, 0)
	Talk("Nao", "NO031022")
	Mess("Yeah... She was in my school before. That girl is aÅ^member of the basketball club.")
	hitretResult := Hitret(14662)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heh, everybody really came to swim.")
	hitretResult := Hitret(14663)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_01M", 0, 0)
	Talk("Nao", "NO031023")
	Mess("The others didn't come here to play or anything, theyÅ^just really came here to feel refreshed.")
	hitretResult := Hitret(14664)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031024")
	Mess("Though today, I'm sorry if it's noisy.")
	hitretResult := Hitret(14665)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's fine, me too, since I feel like I'm butting-inÅ^since I'm not part of any club.")
	hitretResult := Hitret(14666)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_02M", 0, 0)
	Talk("Nao", "NO031025")
	Mess("It's fine... Haru-chan, you're also in the same schoolÅ^as we are, so there's no need to be shy.")
	hitretResult := Hitret(14667)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Female Student B", "NP270001")
	Mess("Ah, is that the soccer club?")
	hitretResult := Hitret(14668)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Female Student C", "NP280001")
	Mess("You guys, aren't you occupying the whole pool with yourÅ^club?")
	hitretResult := Hitret(14669)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When that girl came, the people from the soccer clubÅ^went out, and it became much noisier.")
	hitretResult := Hitret(14670)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The girls from the basketball club cornered the guysÅ^from the soccer club, then they didn't notice thatÅ^they're all playing together.")
	hitretResult := Hitret(14671)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("They were shouting, and some of them were shrilling,Å^they were splashing in the pool, and they kept onÅ^laughing around.")
	hitretResult := Hitret(14672)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_05M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan was the same as always, she was staring atÅ^somewhere while she held her knees with her arms as sheÅ^was sitting down.")
	hitretResult := Hitret(14673)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan, if your friends are there, why don't you joinÅ^them?")
	hitretResult := Hitret(14674)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_11M", 0, 0)
	Talk("Nao", "NO031026")
	Mess("N-No... You're also here Haru-chan, I can't just leaveÅ^you here alone.")
	hitretResult := Hitret(14675)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't mind me, I'll just rest here like this since I'm aÅ^bit tired.")
	hitretResult := Hitret(14676)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031027")
	Mess("No... It's fine, I'll also stay here.")
	hitretResult := Hitret(14677)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan placed her chin on her knees, then she watchedÅ^the pool again.")
	hitretResult := Hitret(14678)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(0, 1)
	ScWait(1500, 0)
	SetCg("B21a", 0, 0)
	Transition("WIP_LR", 500)
	ScPlayEnvSe("SE301", "1", 0)
	WindowView(1)
	Talk("Nao's friend", "NP260003")
	Mess("NaoÅ`!")
	hitretResult := Hitret(14679)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_05M", 0, 0)
	Talk("Nao", "NO031029")
	Mess("... What?")
	hitretResult := Hitret(14680)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao's friend", "NP260004")
	Mess("You don't need to make that kind of face. Sorry if IÅ^disturbed you guys.")
	hitretResult := Hitret(14681)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031031")
	Mess("Well, just don't bother us that much...")
	hitretResult := Hitret(14682)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao's friend", "NP260005")
	Mess("Huh, where's he going?")
	hitretResult := Hitret(14683)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031033")
	Mess("...Toilet.")
	hitretResult := Hitret(14684)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao's friend", "NP260006")
	Mess("So, he's Haru-chan?")
	hitretResult := Hitret(14685)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_11M", 0, 0)
	Talk("Nao", "NO031035")
	Mess("...Yeah.")
	hitretResult := Hitret(14686)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao's friend", "NP260007")
	Mess("Heh, is that so. He looks like a girl, I'm shocked thatÅ^he looks beautiful.")
	hitretResult := Hitret(14687)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_06M", 0, 0)
	Talk("Nao", "NO031037")
	Mess("... What's that?")
	hitretResult := Hitret(14688)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao's friend", "NP260008")
	Mess("Nothing, because, recently, I thought you were always inÅ^a good mood.")
	hitretResult := Hitret(14689)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_10M", 0, 0)
	Talk("Nao", "NO031039")
	Mess("Sachi... Stop that...")
	hitretResult := Hitret(14690)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao's friend", "NP260009")
	Mess("...I think that's fine.")
	hitretResult := Hitret(14691)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031041")
	Mess("...Eh...")
	hitretResult := Hitret(14692)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao's friend", "NP260010")
	Mess("Make sure to introduce me next time okay? He's so cute,Å^I just want to know what will happen if I butt-in.")
	hitretResult := Hitret(14693)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_07M", 0, 0)
	Talk("Nao", "NO031043")
	Mess("Sachi!")
	hitretResult := Hitret(14694)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao's friend", "NP260011")
	Mess("Ahaha, it's been a while since I saw your angry face.")
	hitretResult := Hitret(14695)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao's friend", "NP260012")
	Mess("I really think it's good for you, you got what youÅ^wanted after all.")
	hitretResult := Hitret(14696)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_10M", 0, 0)
	Talk("Nao", "NO031046")
	Mess("Wh-what are you saying...")
	hitretResult := Hitret(14697)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao's friend", "NP260013")
	Mess("Okay then, I'll just come again next time. I feel likeÅ^I'm bothering your school-date, right?")
	hitretResult := Hitret(14698)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_09M", 0, 0)
	Talk("Nao", "NO031048")
	Mess("Sachi, geez just go!")
	hitretResult := Hitret(14699)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao's friend", "NP260014")
	Mess("Okay, okay. Well, see you?")
	hitretResult := Hitret(14700)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("............")
	hitretResult := Hitret(14701)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_05M", 0, 0)
	Talk("Nao", "NO031050")
	Mess("*sigh*... What is she saying...")
	hitretResult := Hitret(14702)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE301", 0)
	BlackOut(0, 1)
	WindowView(0)
	ScWait(2000, 0)
	Change("00_b020c")
	Return
	Change("EXIT_SCENARIO")
EndFunc

