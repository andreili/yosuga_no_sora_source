Include"yosuga.ch"

Function sc00_A023B()
	ScenarioEnter()
	SetCg("B02a", 0, 0)
	Transition("WIP_RL", 500)
	ScPlayBgm("BGM05", 0)
	Talk("Haruka", "")
	Mess("Sora, were you able to get ready?")
	hitretResult := Hitret(7397)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_02M", 0, 0)
	Talk("Sora", "SR011033")
	Mess("Yep, I'm fine.")
	hitretResult := Hitret(7398)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I got the feeling that the uniform she hadn't worn in aÅ^while was a little tight.")
	hitretResult := Hitret(7399)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As of today, the second semester has begun.")
	hitretResult := Hitret(7400)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It doesn't feel that summer break has ended, but...Å^today it's only the opening ceremony.")
	hitretResult := Hitret(7401)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There's no class, so we can go home right away... Plus,Å^when we arrive at school that feeling will probablyÅ^change.")
	hitretResult := Hitret(7402)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("All right, shall we go?")
	hitretResult := Hitret(7403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011034")
	Mess("Yep.")
	hitretResult := Hitret(7404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B12a", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("It felt cool, probably because we had waken up soÅ^early... When the sun goes up, it'll get hot.")
	hitretResult := Hitret(7405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_10L", 0, 0)
	Talk("Sora", "SR011035")
	Mess("Haru......")
	hitretResult := Hitret(7406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I could feel Sora's hand, who had lined up beside me.")
	hitretResult := Hitret(7407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011036")
	Mess("Can we... hold hands?")
	hitretResult := Hitret(7408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Okay, if it's just for a bit.")
	hitretResult := Hitret(7409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011037")
	Mess("...Ehe...")
	hitretResult := Hitret(7410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Right when I thought her fingertips had lost their way,Å^they grasped mine tightly, as if to wrap them up.")
	hitretResult := Hitret(7411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011038")
	Mess("...Your hand, it's a bit cold.")
	hitretResult := Hitret(7412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Really? Though I don't think they've changed much...")
	hitretResult := Hitret(7413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, I could feel a faint warmth from Sora's hand.")
	hitretResult := Hitret(7414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And also... its softness.")
	hitretResult := Hitret(7415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Although she's my twin... Sora's a girl, and I've comeÅ^to recognize that.")
	hitretResult := Hitret(7416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Our connected hands gradually started to become warmer.")
	hitretResult := Hitret(7417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I became aware of it, I became unable to stop it...")
	hitretResult := Hitret(7418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	Talk("GirlÅsìﬁèèÅt", "NO010195")
	Mess("Good morning... Haru-chan, Sora-chan!")
	hitretResult := Hitret(7419)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Hearing the voice calling us from far behind us, neitherÅ^of us separated our hands.")
	hitretResult := Hitret(7420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("G--Good morning, Nao-chan.")
	hitretResult := Hitret(7421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	SetBustup("CA01_05M", 0, 0)
	Talk("Sora", "SR011039")
	Mess("...Good morning.")
	hitretResult := Hitret(7422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's lips became sharp, as if she was dissatisfied.")
	hitretResult := Hitret(7423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO010196")
	Mess("You two seem energetic. It looks like you hadn't had anyÅ^summer heat fatigue.")
	hitretResult := Hitret(7424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan caught up to us in a trot.")
	hitretResult := Hitret(7425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah. Nao-chan, you seem energetic too.")
	hitretResult := Hitret(7426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_03M", 0, 0)
	Talk("Nao", "NO010197")
	Mess("I'm tired... I've been staying up late the whole summer.")
	hitretResult := Hitret(7427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO010198")
	Mess("Today will end in the morning, so maybe I'll take a napÅ^during the time for club activities... *yawn*s...!")
	hitretResult := Hitret(7428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("In that case, there aren't club activities today? SinceÅ^there's only the opening ceremony anyway.")
	hitretResult := Hitret(7429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO010199")
	Mess("Though if I could do that, I would...")
	hitretResult := Hitret(7430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC01_02M", 0, 0)
	Jump(2, 0)
	Talk("Akira", "AK010250")
	Mess("Morning...!!!")
	hitretResult := Hitret(7431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA010101")
	Mess("Good morning.")
	hitretResult := Hitret(7432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Before long, Amatsume and Migiwa-san appeared.")
	hitretResult := Hitret(7433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Good morning. Long time, you two.")
	hitretResult := Hitret(7434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010251")
	Mess("Yep, it's been a long time Haru-kun! You lookÅ^energetic...!")
	hitretResult := Hitret(7435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Really? But if anyone's energetic, you look like you'reÅ^full of it...")
	hitretResult := Hitret(7436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010252")
	Mess("Ehhehe... Right now it's the high tension from myÅ^all-nighter.")
	hitretResult := Hitret(7437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_12M", 0, 0)
	Talk("Kazuha", "KA010102")
	Mess("Eh, all-nighter? Wh--What did you do?")
	hitretResult := Hitret(7438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK010253")
	Mess("I wasn't done with my homework, so I was doing itÅ`.")
	hitretResult := Hitret(7439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When my eyes wandered downwards, for some reason I couldÅ^faintly make out a bear")
	hitretResult := Hitret(7440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Also, it seemed like her footsteps were unsteady, kindÅ^of like she was staggering.")
	hitretResult := Hitret(7441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_04M", 0, 0)
	Talk("Kazuha", "KA010103")
	Mess("If you weren't done with it, you should have told me. IÅ^would have helped you...")
	hitretResult := Hitret(7442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_05M", 0, 0)
	Talk("Akira", "AK010254")
	Mess("Eh, reallyÅ`? I thought if I said that, you'd get mad...")
	hitretResult := Hitret(7443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA010104")
	Mess("Of course I'd get mad. But that doesn't mean I wouldn'tÅ^have helped you.")
	hitretResult := Hitret(7444)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA010105")
	Mess("So, is there anything you're not done with? Since it hasÅ^to be turned it in early, I'll help you.")
	hitretResult := Hitret(7445)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH010319")
	Mess("For the time being, all of my fifth period homework isÅ^done.")
	hitretResult := Hitret(7446)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK010255")
	Mess("Ah, Ryou-niichan!")
	hitretResult := Hitret(7447)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_06M", 0, 0)
	Talk("Nao", "NO010200")
	Mess("Just where did you appear from...")
	hitretResult := Hitret(7448)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010320")
	Mess("Yo, everyone. You all look more energetic than usual.")
	hitretResult := Hitret(7449)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_04M", 0, 0)
	Talk("Haruka", "")
	Mess("...Ryouhei, didn't you say you already did yourÅ^homework?")
	hitretResult := Hitret(7450)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010321")
	Mess("Heh... I said it was easy, but I don't remember everÅ^saying that I was done.")
	hitretResult := Hitret(7451)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Th--Then... When you said let's compare answers... youÅ^meant, copying...")
	hitretResult := Hitret(7452)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_05M", 0, 0)
	Talk("Ryouhei", "RH010322")
	Mess("Oops, well let's leave it at that... Well then, I'mÅ^counting on you, Ojou.")
	hitretResult := Hitret(7453)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	SetBustup("CD01_07M", 0, 0)
	Talk("Kazuha", "KA010106")
	Mess("You've already studied it once, so isn't it easy? InÅ^that case, I wouldn't think you'd need my help.")
	hitretResult := Hitret(7454)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010323")
	Mess("Hey Akira, help me out here.")
	hitretResult := Hitret(7455)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_14M", 0, 0)
	Talk("Akira", "AK010256")
	Mess("Please, Kazu-chan? Help Ryou-niichanÅ`.")
	hitretResult := Hitret(7456)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_13M", 0, 0)
	Jump(5, 0)
	Talk("Kazuha", "KA010107")
	Mess("C--Coward! Using Akira!")
	hitretResult := Hitret(7457)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK010257")
	Mess("It's bad if you only show me, so, please...?")
	hitretResult := Hitret(7458)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_04M", 0, 0)
	Talk("Ryouhei", "RH010324")
	Mess("Å`ÅÙ Å`ÅÙ")
	hitretResult := Hitret(7459)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_05M", 0, 0)
	Talk("Kazuha", "KA010108")
	Mess("Uuuuu...")
	hitretResult := Hitret(7460)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH010325")
	Mess("Well then, I'm counting on you, Ojou.")
	hitretResult := Hitret(7461)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_03M", 0, 0)
	Talk("Akira", "AK010258")
	Mess("Aha... He's counting on you, Kazu-chan.")
	hitretResult := Hitret(7462)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Down(5, 30, 1000, 3)
	Talk("êSÇÃê∫", "")
	Mess("With Amatsume jumping, and Ryouhei clapping...Å^Migiwa-san dropped her shoulders in disappointment.")
	hitretResult := Hitret(7463)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO010201")
	Mess("Well, it's kind of like, using the comedian Akira canÅ^even get Migiwa-san to spoil people...")
	hitretResult := Hitret(7464)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hahaha... Though instead of comedian, it's more likeÅ^easy to figure out.")
	hitretResult := Hitret(7465)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR011040")
	Mess("Haru... let's go? If we hang out with them, we'll beÅ^late.")
	hitretResult := Hitret(7466)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, okay... Well everyone... it's about time...")
	hitretResult := Hitret(7467)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	Quake(16, 0, 1, 2, 30)
	Talk("êSÇÃê∫", "")
	Mess("The moment I said that to everyone, my hand was pulledÅ^tightly.")
	hitretResult := Hitret(7468)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR011041")
	Mess("You're wasting time talking.")
	hitretResult := Hitret(7469)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, yeah... but...")
	hitretResult := Hitret(7470)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH010326")
	Mess("Hey, hey, wait... Don't leave me behind.")
	hitretResult := Hitret(7471)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR011042")
	Mess("......")
	hitretResult := Hitret(7472)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010327")
	Mess("Sora-chan, long time, right? You must have been lonelyÅ^without me, right?")
	hitretResult := Hitret(7473)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011043")
	Mess("Not really... I don't care if you're there or not.")
	hitretResult := Hitret(7474)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010328")
	Mess("Well, my family went on a rare vacation we decided on atÅ^the last minute, and we went to the hot springs-!")
	hitretResult := Hitret(7475)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, so that's why I haven't seen you for a few days.")
	hitretResult := Hitret(7476)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010329")
	Mess("Yeah, were you worried about me?")
	hitretResult := Hitret(7477)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_09M", 0, 0)
	Talk("Sora", "SR011044")
	Mess("Not really.")
	hitretResult := Hitret(7478)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010330")
	Mess("So, I bought souviners for everyone.")
	hitretResult := Hitret(7479)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 6, 0)
	ScPlaySe("se008", 0)
	Update(1)
	SetBustup("CF01_02M", 3, 0)
	SetBustup("CA01_13M", 9, 0)
	Shake(4)
	Update()
	ScPlayBgm("BGM14", 0)
	Talk("Sora", "SR011045")
	Mess("Au...!?")
	hitretResult := Hitret(7480)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 3, 0)
	Jump(2)
	Talk("Akira", "AK010259")
	Mess("Hey, hey... Wooden sword? It's a wooden sword, right?")
	hitretResult := Hitret(7481)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("For some reason, Amatsume jumped at Sora, rather thanÅ^Ryouhei.")
	hitretResult := Hitret(7482)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011046")
	Mess("...Wo--Wooden sword?")
	hitretResult := Hitret(7483)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH010331")
	Mess("Hmm, well it's true that a wooden sword would have madeÅ^the best present, but... There wasn't any where I went.")
	hitretResult := Hitret(7484)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_06M", 0, 0)
	Down(2, 0, 0, 0)
	Talk("Akira", "AK010260")
	Mess("Ehhh...")
	hitretResult := Hitret(7485)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010332")
	Mess("But it's nothing to be depressed about, I bought youÅ^something else instead... It's this..!!")
	hitretResult := Hitret(7486)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CB01_05M", 0, 0)
	Talk("Nao", "NO010202")
	Mess("Hot spring manjuu*? *A manjuu is a steamed yeast bunÅ^with filling.")
	hitretResult := Hitret(7487)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_06M", 0, 0)
	Talk("Ryouhei", "RH010333")
	Mess("Heyy, d--don't spoil the surprise!")
	hitretResult := Hitret(7488)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO010203")
	Mess("It's not like I was spoiling it, I just tried saying theÅ^most stereotypical hot spring souviner.")
	hitretResult := Hitret(7489)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	ScPlayBgm("BGM05", 0)
	SetBustup("CF01_09M", 0, 0)
	Talk("Ryouhei", "RH010334")
	Mess("Be quiet, you. Well, I suppose there's no more surprise.Å^Here, I'll give everyone a piece.")
	hitretResult := Hitret(7490)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei took the manjuu from the box and distributed itÅ^to everyone.")
	hitretResult := Hitret(7491)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nn, thanks.")
	hitretResult := Hitret(7492)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK010261")
	Mess("Ahaa, this manjuu looks like a boob, doesn't it.")
	hitretResult := Hitret(7493)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_12M", 0, 0)
	Talk("Kazuha", "KA010109")
	Mess("A--Akira!")
	hitretResult := Hitret(7494)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK010262")
	Mess("It's kind of soft and bouncy, right?")
	hitretResult := Hitret(7495)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_04M", 0, 0)
	Talk("Kazuha", "KA010110")
	Mess("Stop rubbing it then!")
	hitretResult := Hitret(7496)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CB01_03M", 0, 0)
	Talk("Nao", "NO010204")
	Mess("Well, when all's said and done, your souviner was a hit,Å^right?")
	hitretResult := Hitret(7497)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH010335")
	Mess("Right?")
	hitretResult := Hitret(7498)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010336")
	Mess("Here, Sora-chan too!")
	hitretResult := Hitret(7499)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	SetBustup("CA01_09M", 0, 0)
	Talk("Sora", "SR011047")
	Mess("......")
	hitretResult := Hitret(7500)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_06M", 0, 0)
	Talk("Ryouhei", "RH010337")
	Mess("Oh oh, did you hate it?")
	hitretResult := Hitret(7501)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011048")
	Mess("......")
	hitretResult := Hitret(7502)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Haruka", "")
	Mess("Well, thanks, Ryouhei. Sora... where's your thanks?")
	hitretResult := Hitret(7503)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR011049")
	Mess("...Don't want it.")
	hitretResult := Hitret(7504)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I took the majuu from Sora's hands.")
	hitretResult := Hitret(7505)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well... It'd be a waste, so I'll eat it later.")
	hitretResult := Hitret(7506)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010338")
	Mess("Nn, thanks. There's no pressure, so no worries.")
	hitretResult := Hitret(7507)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh...")
	hitretResult := Hitret(7508)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_04M", 0, 0)
	Talk("Ryouhei", "RH010339")
	Mess("By the way, Haruka?")
	hitretResult := Hitret(7509)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Wh--What?")
	hitretResult := Hitret(7510)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010340")
	Mess("Why have you two been holding hands the whole time?")
	hitretResult := Hitret(7511)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Quake(0, 16, 0, 2, 30)
	Talk("Haruka", "")
	Mess("!?")
	hitretResult := Hitret(7512)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH010341")
	Mess("Even I can read the atmosphere.")
	hitretResult := Hitret(7513)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010342")
	Mess("Well, the whole time, you've been all lovey-doveyÅ^lovey-dovey...It's extreeemely inexcusable...!!")
	hitretResult := Hitret(7514)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, ahaha... W--Well...")
	hitretResult := Hitret(7515)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("Before I knew it, Sora had fixed my grip.")
	hitretResult := Hitret(7516)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When Ryouhei said that, I had tried to separate my handÅ^without realizing it.")
	hitretResult := Hitret(7517)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!?")
	hitretResult := Hitret(7518)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 4, 0)
	SetBustup("CA01_04L", 10, 0)
	Talk("Sora", "SR011050")
	Mess("We can do what we want, right?")
	hitretResult := Hitret(7519)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I tried that, Sora squeezed my hand tightly.")
	hitretResult := Hitret(7520)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_06M", 0, 0)
	Talk("Ryouhei", "RH010343")
	Mess("Eh? So--Sora-chan......?")
	hitretResult := Hitret(7521)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was so tight it could hurt, as if she was never goingÅ^to let me go.")
	hitretResult := Hitret(7522)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011051")
	Mess("...Haru, let's go?")
	hitretResult := Hitret(7523)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh... Yeah. S--Sorry, guys...")
	hitretResult := Hitret(7524)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Leaving eveyone else behind, Sora began running, tightlyÅ^pulling my hand.")
	hitretResult := Hitret(7525)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(2000, 0)
	Talk("Haruka", "")
	Mess("Sora...!")
	hitretResult := Hitret(7526)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM08", 0)
	SetCg("B17a", 0, 0)
	SetBustup("CA01_02L", 0, 0)
	Talk("Sora", "SR011052")
	Mess("You're already tired?")
	hitretResult := Hitret(7527)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, no... It's about time to... let go of...")
	hitretResult := Hitret(7528)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As we approached school, the number of studentsÅ^increased.")
	hitretResult := Hitret(7529)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011053")
	Mess("...Not yet.")
	hitretResult := Hitret(7530)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(7531)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Tightly gripping my hand, it was as if Sora wanted toÅ^show it off.")
	hitretResult := Hitret(7532)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Us doing that... obviously, made us stand out.")
	hitretResult := Hitret(7533)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Some people looked at us in surprise.")
	hitretResult := Hitret(7534)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, Sora didn't mind that... and the entire time sheÅ^looked only at me.")
	hitretResult := Hitret(7535)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Tenderly smiling, she continued walking.")
	hitretResult := Hitret(7536)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(7537)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Like that, I felt we had a different atmosphere thanÅ^everyone else... as we passed through the school gate.")
	hitretResult := Hitret(7538)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B18a", 0, 0)
	SetBustup("CA01_02M", 0, 0)
	Talk("Sora", "SR011054")
	Mess("Well then, see ya afterwards.")
	hitretResult := Hitret(7539)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yep.")
	hitretResult := Hitret(7540)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("In the end, Sora and I held hands all the way to theÅ^classroom.")
	hitretResult := Hitret(7541)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("Until the last moment, with reluctance... we separatedÅ^our hands, and Sora went into her classroom.")
	hitretResult := Hitret(7542)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(7543)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The tension of people watching us as we came here, andÅ^my hand that was gently filled with Sora's warmth.")
	hitretResult := Hitret(7544)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...The lingering scent of Sora is still on my hand.")
	hitretResult := Hitret(7545)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM05", 0)
	SetBustup("CH01_02M", 0, 0)
	Talk("Class Rep", "KO010148")
	Mess("G--Good morning, Kasogano-kun.")
	hitretResult := Hitret(7546)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While I was looking at my own hand, a voice suddenlyÅ^called out to me.")
	hitretResult := Hitret(7547)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("G--Good morning, Chairman.... Long time.")
	hitretResult := Hitret(7548)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010149")
	Mess("Yeah, long time... I think the last time we met was whenÅ^you gave me the watermelon.")
	hitretResult := Hitret(7549)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010150")
	Mess("That watermelon was very delicious, my whole family wasÅ^pleased... They told me to tell you they said thanks.")
	hitretResult := Hitret(7550)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("R--Really? Well, that's good.")
	hitretResult := Hitret(7551)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010151")
	Mess("Anyway, why are you standing right there? Did you comeÅ^with Sora-chan or something?")
	hitretResult := Hitret(7552)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh... Y-Yeah. We just split up.")
	hitretResult := Hitret(7553)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Class Rep", "KO010152")
	Mess("I see. Well, shall we go to our classroom?")
	hitretResult := Hitret(7554)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, yeah. Let's do that.")
	hitretResult := Hitret(7555)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I walked beside the chairman to the classroom......")
	hitretResult := Hitret(7556)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_05M", 0, 0)
	Talk("Ryouhei", "RH010344")
	Mess("Hey hey Haruka... Right when I thought you had just wentÅ^with Sora-chan, this time you're all lovey-dovey withÅ^Chairman in the classroom...!?")
	hitretResult := Hitret(7557)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH01_05M", 0, 0)
	Jump(9, 0)
	Talk("Class Rep", "KO010153")
	Mess("Wha, wh-wh-wh-wh, l--lovey-dovey...!?")
	hitretResult := Hitret(7558)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, Ryouhei, sorry for before......")
	hitretResult := Hitret(7559)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_09M", 0, 0)
	Talk("Ryouhei", "RH010345")
	Mess("No, I won't forgive you. I want answers as to why youÅ^did that!")
	hitretResult := Hitret(7560)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH01_10M", 0, 0)
	Talk("Class Rep", "KO010154")
	Mess("Na-Nakasato-san, wh-what have you been talking aboutÅ^just now...!")
	hitretResult := Hitret(7561)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_05M", 0, 0)
	Talk("Ryouhei", "RH010346")
	Mess("Yeah, of course I want to hear about it from Chairman asÅ^well.")
	hitretResult := Hitret(7562)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CH01_05M", 0, 0)
	Talk("Class Rep", "KO010155")
	Mess("Wh-what are you talking aboutttt!?")
	hitretResult := Hitret(7563)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B19a", 0, 0)
	Transition("WIP_RL", 500)
	Talk("êSÇÃê∫", "")
	Mess("When we went inside the bustling classroom, everyone inÅ^the class looked at us.")
	hitretResult := Hitret(7564)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Half of it was out of curiousity, and the other half outÅ^of amusement.")
	hitretResult := Hitret(7565)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From all the attention on her, Chairman's face wasÅ^bright red as she went to her seat.")
	hitretResult := Hitret(7566)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She should have just swept it aside... I guess sheÅ^really is a serious person...")
	hitretResult := Hitret(7567)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Though, I might just be getting used to Ryouhei.")
	hitretResult := Hitret(7568)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH010347")
	Mess("Hahaha, seeing Chairman's face bright red like that, I'mÅ^starting to think she might even be cute!")
	hitretResult := Hitret(7569)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You have bad taste... Though I do think she's cute.")
	hitretResult := Hitret(7570)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_04M", 0, 0)
	Talk("Kazuha", "KA010111")
	Mess("Seriously...")
	hitretResult := Hitret(7571)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I turned around, Migiwa-san and Amatsume hadÅ^arrived too.")
	hitretResult := Hitret(7572)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK010263")
	Mess("Ryou-niichan, don't pick on Chairman that much, okay?")
	hitretResult := Hitret(7573)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_04M", 0, 0)
	Talk("Ryouhei", "RH010348")
	Mess("Heh, this is like a ritual of summer break ending.")
	hitretResult := Hitret(7574)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Certainly, this happens often when summer break ends.")
	hitretResult := Hitret(7575)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At my previous school in the city... I wonder if theseÅ^kinds of close conversations happened after a longÅ^summer vacation.")
	hitretResult := Hitret(7576)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("About going out with students from upper grades, lowerÅ^grades, and from the same class, students from otherÅ^schools, business people......")
	hitretResult := Hitret(7577)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And not just whether they were going out, but whetherÅ^they did it... those kinds of stories that you don't howÅ^true they are.")
	hitretResult := Hitret(7578)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("Boasting about embarrassing stories, in a sense, mayÅ^have been just glorifying our rights as young people.")
	hitretResult := Hitret(7579)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(7580)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, something cold suddenly ran through my heart.")
	hitretResult := Hitret(7581)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM16", 0)
	Tone("MONOCHROME")
	SetCg("B17a", 0, 0)
	Transition("WIP_BRTL", 500)
	Talk("êSÇÃê∫", "")
	Mess("The look of people as Sora and I walked into school.")
	hitretResult := Hitret(7582)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The look of people as Chairman and I walked into class.")
	hitretResult := Hitret(7583)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("They are, obviously something different.")
	hitretResult := Hitret(7584)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Without caring for the people around us, Sora behavedÅ^affectionate towards me.")
	hitretResult := Hitret(7585)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("However, even with only the number of people that lookedÅ^at us, they were from various viewpoints. They weren'tÅ^necessarily all favorable viewpoints.")
	hitretResult := Hitret(7586)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's right... we are siblings. Even if we get alongÅ^well, there are limits.")
	hitretResult := Hitret(7587)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Tone("")
	SetCg("B19a", 0, 0)
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(7588)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("All of the happiness I've felt up until now, began toÅ^quickly fade.")
	hitretResult := Hitret(7589)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("This is... definitely, that kind of thing.")
	hitretResult := Hitret(7590)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Now stepping into a relationship with Sora... The worldÅ^I had once surrounded myself with, changed greatly.")
	hitretResult := Hitret(7591)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("No... Sora and I might be going against the world.")
	hitretResult := Hitret(7592)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("That's how much of a dangerous relationship we're in...")
	hitretResult := Hitret(7593)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("DATE", 0)
	Change("00_a024")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

