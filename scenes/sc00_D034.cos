Include"yosuga.ch"

Function sc00_D034()
	ScenarioEnter()
	WindowView(1)
	ScPlayBgm("BGM10", 0)
	SetCg("B27b", 0, 0)
	Update()
	ScWait(500, 0)
	Scroll(0, 600, 5000, 0, 0)
	WaitScroll()
	Update()
	SetCg("B07b", 0, 0)
	Talk("S‚Ìº", "")
	Mess("Around sunset...")
	hitretResult := Hitret(32254)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("In the grounds where the lonely chirping of the cicadas^echoed.")
	hitretResult := Hitret(32255)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("On the steps leading to the shrine in front sat Akira,^with her back leaning against the offertory box, doing^nothing.")
	hitretResult := Hitret(32256)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("She was in a daze, gazing at the sinking evening sun.")
	hitretResult := Hitret(32257)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("But seeing a person in the middle of the wavering red^sun, she quickly sat up.")
	hitretResult := Hitret(32258)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01S", 0, 0)
	Talk("Akira", "AK040440")
	Mess("Kazu-chan!")
	hitretResult := Hitret(32259)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The person coming up the stairs was Kazuha.")
	hitretResult := Hitret(32260)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Like the sun at the edge of the mountains, Kazuha^staggered up the stairs as Akira ran toward her.")
	hitretResult := Hitret(32261)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08S", 0, 0)
	Talk("S‚Ìº", "")
	Mess("However, Kazuha stopped walking and waited for Akira to^approach her.")
	hitretResult := Hitret(32262)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("She was biting her lips as if she was holding something^back.")
	hitretResult := Hitret(32263)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Even though she was right in front of such an important^person, she couldn't smile.")
	hitretResult := Hitret(32264)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("And couldn't help but hate herself for it.")
	hitretResult := Hitret(32265)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 5, 0)
	SetBustup("CD02_08M", 7, 0)
	Talk("Akira", "AK040441")
	Mess("How did the date g-... Nnn!?")
	hitretResult := Hitret(32266)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Before Akira could finish, Kazuha hugged her tightly.")
	hitretResult := Hitret(32267)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("A desperate hug, putting all her strength into her^shaking arms.")
	hitretResult := Hitret(32268)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("But even then, there was no strength in that hug,")
	hitretResult := Hitret(32269)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040442")
	Mess("...What's wrong? Kazu-chan?")
	hitretResult := Hitret(32270)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Akira asked this with a blank voice that somehow calmed^her down.")
	hitretResult := Hitret(32271)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041280")
	Mess("...We broke up...")
	hitretResult := Hitret(32272)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Kazuha replied with a single sentence.")
	hitretResult := Hitret(32273)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_07M", 0, 0)
	Talk("Akira", "AK040443")
	Mess("Why?")
	hitretResult := Hitret(32274)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Kazuha, who even now was still about to cry, was^reflected in the pupils of Akira's questioning eyes.")
	hitretResult := Hitret(32275)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_09M", 0, 0)
	Talk("Kazuha", "KA041281")
	Mess("Because I can't leave you. We promised to always be^together, right?")
	hitretResult := Hitret(32276)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041282")
	Mess("So... this is good enough...")
	hitretResult := Hitret(32277)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Refusing to look into those eyes, Kazuha shut closed^eyes tightly, and put her strength into the arms that^were holding Akira.")
	hitretResult := Hitret(32278)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("While in her heart, cursing those arms which wouldn't^listen to her commands.")
	hitretResult := Hitret(32279)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Seeing her like that, Akira didn't question her any^further, nor did she return the hug.")
	hitretResult := Hitret(32280)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	WindowView(0)
	Change("00_d035")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

