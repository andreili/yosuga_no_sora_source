Include"yosuga.ch"

Function sc00_D028()
	ScenarioEnter()
	ScPlayBgm("BGM03", 0)
	SetCg("B17a", 0, 0)
	Update()
	ScPlaySe("SE352", 0)
	ScWait(3000, 0)
	Talk("S‚Ìº", "")
	Mess("Just as I arrived at school, the chime sounded.")
	hitretResult := Hitret(31367)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("There's no one here to hear it, so I think they must^ring it during the breaks too, which makes me want to^comment on their hard work.")
	hitretResult := Hitret(31368)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Or maybe there are people that have to do stuff at^school, like me, and they ring it for those people?")
	hitretResult := Hitret(31369)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("If that's the case, they really are hardworking.")
	hitretResult := Hitret(31370)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Halfway through the vacation...actually, it's almost^finished, but vacations are vacations.")
	hitretResult := Hitret(31371)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("But if you ask why I'm at school today, it's because I^couldn't come because of the all the hassle during the^Obon festival.")
	hitretResult := Hitret(31372)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The homeroom teacher called me yesterday and said that^some printouts were handed out while we were away.")
	hitretResult := Hitret(31373)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("That means that Sora should've had to come too, but...^Well, there's no need to say.")
	hitretResult := Hitret(31374)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Hmm?")
	hitretResult := Hitret(31375)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("As I passed through the school gate, a calming sound^could be heard.")
	hitretResult := Hitret(31376)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("An instrument... Probably a string instrument.")
	hitretResult := Hitret(31377)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Club activity? ...No, this school only has a wind^instrument club.")
	hitretResult := Hitret(31378)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Which means...")
	hitretResult := Hitret(31379)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("A single name popped into mind.")
	hitretResult := Hitret(31380)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("If she's here...")
	hitretResult := Hitret(31381)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The feeling that this was a bother suddenly changed into^hope.")
	hitretResult := Hitret(31382)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B18a", 0, 0)
	Talk("Haruka", "")
	Mess("Excuse me!")
	hitretResult := Hitret(31383)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("After retrieving what I needed to get from the staff^office, I quickly left the room.")
	hitretResult := Hitret(31384)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I think I'm making a bitter smile, but right now, that^viola is more important.")
	hitretResult := Hitret(31385)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The sound I heard while the homeroom teacher was talking^to me, had now faded into nothing.")
	hitretResult := Hitret(31386)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("She might already have gone home.")
	hitretResult := Hitret(31387)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("But it hasn't been that long since the sound stopped.")
	hitretResult := Hitret(31388)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("So for now, I go looking, then I start thinking.")
	hitretResult := Hitret(31389)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide()
	SetCg("BLACK", 0, 0)
	Transition("WIP_RL", 500)
	Update()
	ScWait(500, 0)
	SetCg("B18a", 0, 0)
	Transition("WIP_LR", 500)
	Talk("S‚Ìº", "")
	Mess("I wandered around the school building with no luck...^But if she was playing an instrument, she'd be near the^music room.")
	hitretResult := Hitret(31390)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("When I just transferred here I was shown the room, but^I'm usually in the classrooms. However, I think I^roughly know...")
	hitretResult := Hitret(31391)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah!")
	hitretResult := Hitret(31392)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("As I was passing our classroom, I happened to hear^Kazuha talking to someone.")
	hitretResult := Hitret(31393)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("That someone was a teacher, but...I don't remember which^one.")
	hitretResult := Hitret(31394)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("While I was thinking that I shouldn't disturb them while^they were talking near the door, the teacher started to^walk away and Kazuha went into the classroom.")
	hitretResult := Hitret(31395)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Following Kazuha, I went inside.")
	hitretResult := Hitret(31396)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B19a", 0, 0)
	SetBustup("CD01_01M", 0, 0)
	Transition("WIP_LR", 500)
	Talk("Haruka", "")
	Mess("Hello, Migiwa-san.")
	hitretResult := Hitret(31397)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040988")
	Mess("Ah, Haruka...What's up?")
	hitretResult := Hitret(31398)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Kazuha was the only one in the classroom, which was^empty, except for one viola. It was a lonely sight.")
	hitretResult := Hitret(31399)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Migiwa-san, names.")
	hitretResult := Hitret(31400)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA040989")
	Mess("It's fine, the teacher just left, so there's only us^here.")
	hitretResult := Hitret(31401)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Kazuha was confident about it, and that was good enough^for me.")
	hitretResult := Hitret(31402)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I heard a viola as I walked into school, so thinking it^might be you, I went around looking.")
	hitretResult := Hitret(31403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I thought for sure you'd be in the music room, though.")
	hitretResult := Hitret(31404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_03M", 0, 0)
	Talk("Kazuha", "KA040990")
	Mess("The music room's being used by the wind instrument club^right now.")
	hitretResult := Hitret(31405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, club activities. I see.")
	hitretResult := Hitret(31406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The chime is probably for the students doing club^activities, then.")
	hitretResult := Hitret(31407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Now that I think about it, it was obvious.")
	hitretResult := Hitret(31408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You're...not in a club, right? I think you said that^before.")
	hitretResult := Hitret(31409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA040991")
	Mess("The music teacher asked me to play the viola for her. It^seems she wants to run not only a wind instrument club,^but a strings one too.")
	hitretResult := Hitret(31410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040992")
	Mess("So she's talking to the students that already play a^string instrument. I think there's quite a few other^people than me.")
	hitretResult := Hitret(31411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Wow, it's great that the school's the one starting a^club! Are you going to join?")
	hitretResult := Hitret(31412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040993")
	Mess("I wonder...I still don't know. And she still has to ask^the principal and vice-principal, too...")
	hitretResult := Hitret(31413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_08M", 0, 0)
	Talk("Kazuha", "KA040994")
	Mess("And besides, even if I were to join, I don't know if the^others will want to...")
	hitretResult := Hitret(31414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040995")
	Mess("And if we can't get new students to join, then the club^would end with just my generation.")
	hitretResult := Hitret(31415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha, there's definitely no need to worry about that.")
	hitretResult := Hitret(31416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA040996")
	Mess("Huh? Why not?")
	hitretResult := Hitret(31417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The moment they see you play the viola, the new students^will be looking up to you.")
	hitretResult := Hitret(31418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You look so beautiful and cool when you play it, that^people wanting to join the club will flood into the^classroom.")
	hitretResult := Hitret(31419)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_11M", 0, 0)
	Talk("Kazuha", "KA040997")
	Mess("Geez, all those jokes again...")
	hitretResult := Hitret(31420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("With a bitter smile on her face, Kazuha started packing^up her viola.")
	hitretResult := Hitret(31421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("She said it was something passed down to her by her^mother, and she was treating it gently.")
	hitretResult := Hitret(31422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Kazuha wasn't getting along with her parents anymore^after the incident with Akira, but I think her actions^now contradict that.")
	hitretResult := Hitret(31423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA040998")
	Mess("I didn't think you'd come to school, as well.")
	hitretResult := Hitret(31424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("And finishing packing it up, she spoke again.")
	hitretResult := Hitret(31425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I had to come and get some print outs.^...Sora's, too.")
	hitretResult := Hitret(31426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040999")
	Mess("That means your business is finished here, right? Are^you free now?")
	hitretResult := Hitret(31427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Want to go somewhere? It'll turn into another sudden^date though.")
	hitretResult := Hitret(31428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA041000")
	Mess("Of course I'll go.^Hehe, if we want to do that, we'll have to tidy up the^classroom.")
	hitretResult := Hitret(31429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Roger!")
	hitretResult := Hitret(31430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("S‚Ìº", "")
	Mess("The classroom's table and chairs had been pushed aside^to make something like a concert stage in the middle.")
	hitretResult := Hitret(31431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Kazuha had probably stood here and played her viola.")
	hitretResult := Hitret(31432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("She must've looked so beautiful...")
	hitretResult := Hitret(31433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("And as the wind blowed into the classroom, her long hair^would sway in the wind...")
	hitretResult := Hitret(31434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("As I moved my arms and legs, I was imagining that.")
	hitretResult := Hitret(31435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("If it worked out, I want to hear her perform... I want^to see her play.")
	hitretResult := Hitret(31436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("As I started resenting the homeroom teacher for talking^so much, a wind blew into the room.")
	hitretResult := Hitret(31437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetBustup("CD01_12S", 0, 0)
	Talk("Kazuha", "KA041001")
	Mess("Kya..")
	hitretResult := Hitret(31438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Just as I had imagined...the swaying of Kazuha's hair.")
	hitretResult := Hitret(31439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Floating lightly on the wind that I can't see.")
	hitretResult := Hitret(31440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Each strand shining brightly, as if the wind itself was^shining.")
	hitretResult := Hitret(31441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_07S", 0, 0)
	Talk("Kazuha", "KA041002")
	Mess("I should've closed the window first... My hair's all^over the place now...")
	hitretResult := Hitret(31442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("As the wind stopped, Kazuha raised her hands as if to^tidy up her hair.")
	hitretResult := Hitret(31443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I couldn't help but stare, first at her white nape which^was visible for a few moments, and then at her girlish^behavior.")
	hitretResult := Hitret(31444)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I want to touch her... Kazuha's so beautiful that I want^to hug her...")
	hitretResult := Hitret(31445)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kazuha.")
	hitretResult := Hitret(31446)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("In this empty room with no one but the two of us, I^couldn't hold back my feelings.")
	hitretResult := Hitret(31447)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Change("00_d028b")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

