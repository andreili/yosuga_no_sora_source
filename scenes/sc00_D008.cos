Include"yosuga.ch"

Function sc00_D008()
	ScenarioEnter()
	ScPlayBgm("BGM03", 0)
	SetCg("B32b", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("A few days after that, on a Sunday evening.")
	hitretResult := Hitret(27603)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After taking in the laundry that had been left out toÅ^dry since morning, I glanced at the clock. It looks likeÅ^there's still some free time.")
	hitretResult := Hitret(27604)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("All the programs broadcasted on TV were travelÅ^documentaries for tourists, which were really boring.")
	hitretResult := Hitret(27605)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Soraa? Ooii, Soooraaa?")
	hitretResult := Hitret(27606)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm not sure if Sora had gone out or was just ignoringÅ^me, but there wasn't any answer.")
	hitretResult := Hitret(27607)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sitting around dazed like this felt like a waste ofÅ^time, so I decided to go on a walk.")
	hitretResult := Hitret(27608)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B27b", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("I didn't have a destination in mind, but I headed towardÅ^the shrine anyway.")
	hitretResult := Hitret(27609)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Perhaps Migiwa-san was there playing the viola...")
	hitretResult := Hitret(27610)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetCg("B07b", 0, 0)
	SetBustup("CC03_01S", 0, 0)
	ScPlayEnvSe("SE406", "", 0)
	Talk("êSÇÃê∫", "")
	Mess("However, when I arrived, the only person at the shrineÅ^was Akira with a bamboo broom engaged in her usualÅ^cleaning duty.")
	hitretResult := Hitret(27611)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040096")
	Mess("Ahh, it's Haru-kun!")
	hitretResult := Hitret(27612)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_04M", 0, 0)
	Talk("Akira", "AK040097")
	Mess("Welcome! What're you here for today?")
	hitretResult := Hitret(27613)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("A walk, I guess. I was just going to drop by, but itÅ^looks like I'm interrupting your cleaning,")
	hitretResult := Hitret(27614)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Half of the grounds seemed to be swept clean, but theÅ^remaining half was covered by a layer of dust.")
	hitretResult := Hitret(27615)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Despite that, Amatsume put down the broom and came overÅ^to me.")
	hitretResult := Hitret(27616)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sorry for the bad timing...")
	hitretResult := Hitret(27617)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040098")
	Mess("It's all right, I was just trying to find something toÅ^pass the time. Besides, I'm really glad to see you!")
	hitretResult := Hitret(27618)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040099")
	Mess("Ah, I'll go fetch some barley tea, so please wait aÅ^moment!")
	hitretResult := Hitret(27619)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("You don't have to do that...")
	hitretResult := Hitret(27620)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I tried to stop her, but she had already left.")
	hitretResult := Hitret(27621)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Alone again, I looked around the ground with nothing inÅ^mind.")
	hitretResult := Hitret(27622)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Having nothing to do, I realized that I knew how toÅ^sweep...")
	hitretResult := Hitret(27623)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I picked up the bamboo broom, which was laying on theÅ^floor. Maybe I should do some cleaning to pass theÅ^time....")
	hitretResult := Hitret(27624)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...I guess I'll help Akira with the cleaning to thankÅ^her for the tea..")
	hitretResult := Hitret(27625)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I didn't really know what to do, so I'll just sweep theÅ^dust from the stone pavement and put it to the side.")
	hitretResult := Hitret(27626)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I didn't realize this before because there were noÅ^fallen leaves or noticeable debris, but now that I'veÅ^started cleaning, this place is actually quite dirty.")
	hitretResult := Hitret(27627)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was mostly just dust, but mud and grime unexpectedlyÅ^stained the pavements while stone fragments were lyingÅ^all over the place.")
	hitretResult := Hitret(27628)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("They didn't pose much of a problem to the worshippers,Å^but they're annoying for the person who's cleaning.")
	hitretResult := Hitret(27629)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If I sweep lightly, the stones will end up all over theÅ^place. Even if I were to carefully sweep them manyÅ^times, they still won't gather in one place.")
	hitretResult := Hitret(27630)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was only a few minutes, but my arms had startedÅ^aching.")
	hitretResult := Hitret(27631)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...It's hard to believe that Amatsume always cleansÅ^these huge grounds...")
	hitretResult := Hitret(27632)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But since there's normally no one except Amatsume at theÅ^shrine, she does it alone.")
	hitretResult := Hitret(27633)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She really is amazing.")
	hitretResult := Hitret(27634)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE406", 0)
	Talk("VoiceÅsàÍótÅt", "KA040151")
	Mess("Onee-chan, are you cleaning?")
	hitretResult := Hitret(27635)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh....?")
	hitretResult := Hitret(27636)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("An unexpected voice interrupted my thoughts.")
	hitretResult := Hitret(27637)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_12S", 0, 0)
	Talk("Kazuha", "KA040152")
	Mess("*Gasp*...")
	hitretResult := Hitret(27638)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I turned around, Migiwa-san was there, covering herÅ^mouth.")
	hitretResult := Hitret(27639)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Migiwa...san?")
	hitretResult := Hitret(27640)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She just called someone Onee-chan...")
	hitretResult := Hitret(27641)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040100")
	Mess("Ah! Kazu-chan, welcome!!")
	hitretResult := Hitret(27642)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM05", 0)
	SetBustup("CC06_01S", 0, 0)
	Talk("Akira", "AK040101")
	Mess("Ah, Haru-kun were you cleaning for me? You could've justÅ^waited thereÅ`")
	hitretResult := Hitret(27643)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, nah, even if I just stood there, I was free anyway.")
	hitretResult := Hitret(27644)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK040102")
	Mess("Ahaha, same as me then. Thanks, Haru-kun.Å^Here, have some tea.")
	hitretResult := Hitret(27645)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("T-Thanks...")
	hitretResult := Hitret(27646)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume poured some golden colored barley tea into oneÅ^of the cups she had brought with her.")
	hitretResult := Hitret(27647)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And then she poured it into the other and offered it toÅ^Migiwa-san.")
	hitretResult := Hitret(27648)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01S", 6, 0)
	SetBustup("CD02_12S", 8, 0)
	Talk("Akira", "AK040103")
	Mess("Here, have some too, Kazu-chanÅ`")
	hitretResult := Hitret(27649)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_01S", 10, 0)
	Talk("Kazuha", "KA040153")
	Mess("It...It's okay..I'm going home now, so...")
	hitretResult := Hitret(27650)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01S", 6, 0)
	Talk("Akira", "AK040104")
	Mess("Hmm? Really? Even though you came all this way.")
	hitretResult := Hitret(27651)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040154")
	Mess("I'll come again...bye.")
	hitretResult := Hitret(27652)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("See ya, Migiwa-sa....")
	hitretResult := Hitret(27653)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD02_07S", 10, 0)
	Talk("Kazuha", "KA040155")
	Mess("......")
	hitretResult := Hitret(27654)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(5)
	SetBustup("CC06_01S", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Eh? Did she just glare at me?")
	hitretResult := Hitret(27655)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040105")
	Mess("Kazu-chan! Bye, bye!")
	hitretResult := Hitret(27656)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Hurrying down the steps, she left without sayingÅ^goodbye.")
	hitretResult := Hitret(27657)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Was it just me or was Migiwa-san acting really wierd?")
	hitretResult := Hitret(27658)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03M", 0, 0)
	Talk("Akira", "AK040106")
	Mess("Hmm...really? Well, it's nothing for you to worry about!Å^Come on, come on, drink the tea! It's gonna get warmÅ^soon.")
	hitretResult := Hitret(27659)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay, thanks...*glug* *glug* ... *sigh*.Å^Wow, this tea is really good when it's served cold.")
	hitretResult := Hitret(27660)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK040107")
	Mess("Isn't it? Kazu-chan should've stayed to have some tea asÅ^well.Å^Here, have another cup.")
	hitretResult := Hitret(27661)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks.")
	hitretResult := Hitret(27662)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But no matter how many cups of cold barley tea I drank,Å^I still couldn't erase this uneasy feeling.")
	hitretResult := Hitret(27663)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_d009")
	Return
	Change("EXIT_SCENARIO")
EndFunc

