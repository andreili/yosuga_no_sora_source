Include"yosuga.ch"

Function sc00_C031B()
	ScenarioEnter()
	SetCg("B15b", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Come to think of it, Akira is likely to make dinner atÅ^this timing.")
	hitretResult := Hitret(25278)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I feel like I can't see her when something like thatÅ^just happened......")
	hitretResult := Hitret(25279)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I will even disturb her from making dinner, so I will goÅ^back after buying the things.")
	hitretResult := Hitret(25280)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM07", 0)
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020120")
	Mess("Ah?")
	hitretResult := Hitret(25281)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!?, Ya-Yahiro-san?")
	hitretResult := Hitret(25282)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_06M", 0, 0)
	Talk("Yahiro", "YH020121")
	Mess("What, can't I be here?")
	hitretResult := Hitret(25283)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, you're looking after the shop right? Akira is makingÅ^food.")
	hitretResult := Hitret(25284)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020122")
	Mess("Ah? When you say that it's really that time, what's thatÅ^kid doing?")
	hitretResult := Hitret(25285)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... Hmmm? Akira's not here yet?")
	hitretResult := Hitret(25286)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020123")
	Mess("It seems like, Grandfather called her earlier, she'sÅ^probably having desserts and watching the television.")
	hitretResult := Hitret(25287)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Is that so...... She's the same as usual.")
	hitretResult := Hitret(25288)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020124")
	Mess("Sheesh, she goes off immediately when she's called. But,Å^you......")
	hitretResult := Hitret(25289)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh...... W-what's wrong?")
	hitretResult := Hitret(25290)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_06M", 0, 0)
	ScPlayBgm("BGM10", 0)
	Talk("Yahiro", "YH020125")
	Mess("......Actually made Akira cried.")
	hitretResult := Hitret(25291)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Guuuh......")
	hitretResult := Hitret(25292)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020126")
	Mess("It's something silly right? Did you reveal your hentaiÅ^fetishes and force it on her or something?")
	hitretResult := Hitret(25293)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If that's the case, it might be more interesting......")
	hitretResult := Hitret(25294)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020127")
	Mess("Tch, you did something even more boring huh......")
	hitretResult := Hitret(25295)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You said that you will listen to my ramblings the lastÅ^time right?")
	hitretResult := Hitret(25296)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020128")
	Mess("...... I did say it?")
	hitretResult := Hitret(25297)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I...... I don't want to let Akira keep on lying.")
	hitretResult := Hitret(25298)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020129")
	Mess("This might be something really nosy?")
	hitretResult := Hitret(25299)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If we smile together and keep on laughing, I feel thatÅ^we will enjoy life.")
	hitretResult := Hitret(25300)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But, it's too cruel...... We already know about it, butÅ^we still have to keep on pretending.")
	hitretResult := Hitret(25301)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even if it's a lie, the truth behind us keeps staring atÅ^us.")
	hitretResult := Hitret(25302)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I escaped to this town. But I don't want to escape fromÅ^stuff about Akira's troubles.")
	hitretResult := Hitret(25303)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Akira's holding everything by herself, if I can't shareÅ^her burden, there's no meaning if we are together.")
	hitretResult := Hitret(25304)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm really not sure how to support her or share herÅ^burden.")
	hitretResult := Hitret(25305)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So, I want to know...... Because I want to help Akira.")
	hitretResult := Hitret(25306)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_06M", 0, 0)
	Talk("Yahiro", "YH020130")
	Mess("Tch...... If you're just saying it, I won't takeÅ^responsibility okay?")
	hitretResult := Hitret(25307)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020131")
	Mess("Then, what the heck do you want?")
	hitretResult := Hitret(25308)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I want to investigate Akira's parents.")
	hitretResult := Hitret(25309)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020132")
	Mess("Ha? Nobody knows where her mum went, but isn't the dadÅ^staying in that grand house?")
	hitretResult := Hitret(25310)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(25311)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020133")
	Mess("You don't believe it? Your grandmother delivered theÅ^baby right?")
	hitretResult := Hitret(25312)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("AKira has Grandmother's diary. It's written that Akira'sÅ^mother might have exchanged her own child to theÅ^Migiwa's family......")
	hitretResult := Hitret(25313)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_08M", 0, 0)
	Talk("Yahiro", "YH020134")
	Mess("...... That's something not funny at all. Cuckoo bird?Å^Placing their own child to let other people raise.")
	hitretResult := Hitret(25314)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020135")
	Mess("Then, revealing the truth like that and let the Ojou'sÅ^family break into pieces?")
	hitretResult := Hitret(25315)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(25316)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020136")
	Mess("...... So it's something like that right?")
	hitretResult := Hitret(25317)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... Yes.")
	hitretResult := Hitret(25318)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_06M", 0, 0)
	Talk("Yahiro", "YH020137")
	Mess("Yuu're really thinking of something really amazing.")
	hitretResult := Hitret(25319)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Akira isn't wishing for something like this. If I don'tÅ^say anything she will still laugh and continue livingÅ^on.")
	hitretResult := Hitret(25320)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... But, I know.")
	hitretResult := Hitret(25321)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Akira cries on me and asks me to maintain thisÅ^situation.")
	hitretResult := Hitret(25322)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("She even thinks that if she can't continue doing this,Å^it's best to disappear from this town.")
	hitretResult := Hitret(25323)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But she can't do that either...... Because there's manyÅ^important people in this town, the people that sheÅ^likes, she doesn't want to leave here...... doesn't wantÅ^to leave Yahiro-san......")
	hitretResult := Hitret(25324)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020138")
	Mess("Urgh......")
	hitretResult := Hitret(25325)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No matter whose child it is, Akira is still Akira right?Å^So, I hope that she lifts her chest up and continueÅ^living here.")
	hitretResult := Hitret(25326)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I have never done anything for her until now...... ThisÅ^burden can be shared together.")
	hitretResult := Hitret(25327)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_06M", 0, 0)
	Talk("Yahiro", "YH020139")
	Mess("...... It wouldn't go smoothly you know.")
	hitretResult := Hitret(25328)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... Indeed.")
	hitretResult := Hitret(25329)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020140")
	Mess("......Seesh, are you kids. Doing all these kinds ofÅ^troublesome things.")
	hitretResult := Hitret(25330)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Allowing me to say all these, is Yahiro-san right?")
	hitretResult := Hitret(25331)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_08M", 0, 0)
	Talk("Yahiro", "YH020141")
	Mess("Aaaah sheesh ...... Oi, come over for a bit!")
	hitretResult := Hitret(25332)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE118", 0)
	SetCg("B16a", 0, 0)
	SetBustup("CG01_02M", 0, 0)
	Transition("WIP_LR", 500)
	Talk("Haruka", "")
	Mess("...... To do what?")
	hitretResult := Hitret(25333)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE072", 0)
	Talk("Yahiro", "YH020142")
	Mess("...... Damn it, ahh- where is it ......")
	hitretResult := Hitret(25334)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE073", 0)
	Talk("êSÇÃê∫", "")
	Mess("Yahiro-san sits in front of the computer, and startsÅ^investigating something/")
	hitretResult := Hitret(25335)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What are you searching for?")
	hitretResult := Hitret(25336)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Yahiro-san is always so sloven, but I'm surprised at herÅ^unimaginable speed at hitting the keyboard keys.")
	hitretResult := Hitret(25337)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You feel like you're very used to this, did you a jobÅ^related to computers?")
	hitretResult := Hitret(25338)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE072", 0)
	SetBustup("CG01_06M", 0, 0)
	Talk("Yahiro", "YH020143")
	Mess("Don't. Ask. Me. I don't want to remember that---......")
	hitretResult := Hitret(25339)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Guuh......")
	hitretResult := Hitret(25340)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She quit her job probably because of someÅ^trouble........")
	hitretResult := Hitret(25341)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020144")
	Mess("When I took her in, I also considered something thingsÅ^before.")
	hitretResult := Hitret(25342)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020145")
	Mess("But I really couldn't do it on my own.")
	hitretResult := Hitret(25343)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	ScPlaySe("se073", 0)
	Talk("Yahiro", "YH020146")
	Mess("Hm...... Look at this......")
	hitretResult := Hitret(25344)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM16", 0)
	Talk("Haruka", "")
	Mess("Hm?")
	hitretResult := Hitret(25345)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Yahiro-san lets me look at the screen. That's the DNAÅ^test that Sora mentioned earlier.")
	hitretResult := Hitret(25346)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_06M", 0, 0)
	Talk("Yahiro", "YH020147")
	Mess("Lying Akira will easily get the saliva and her hairÅ^strands. But after that there's nothing I can do.")
	hitretResult := Hitret(25347)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020148")
	Mess("If I ask Ojou-san's mother, can you take a test forÅ^Akira's sake? Do you really think she will reply withÅ^[Yes, I'm fine with that]?")
	hitretResult := Hitret(25348)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... I'll ask Migiwa-san for help.")
	hitretResult := Hitret(25349)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020149")
	Mess("Seriously...... You don't show any mercy.")
	hitretResult := Hitret(25350)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Who said that, you won't take any responsibilities ifÅ^you're just saying it?")
	hitretResult := Hitret(25351)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020150")
	Mess("Ah? I don't know.")
	hitretResult := Hitret(25352)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020151")
	Mess("Well, if you successfully get it we will try it. In anyÅ^case, I'll apply to send the results to your home. IfÅ^you send it to here, Akira will find it in a heartbeat.")
	hitretResult := Hitret(25353)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I listen to Yahiro-san's words, and entered my ownÅ^address. There are two applications. Akira andÅ^Migiwa-san's share.")
	hitretResult := Hitret(25354)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020152")
	Mess("To the people other than Ojou herself, it's best to keepÅ^this a secret. Right now the evidence isn't suited toÅ^make things big.")
	hitretResult := Hitret(25355)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_07M", 0, 0)
	Talk("Yahiro", "YH020153")
	Mess("I believe, that this is something that you overthink,Å^and the final joke will be on you.")
	hitretResult := Hitret(25356)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I will also, laugh freely when it comes to that.")
	hitretResult := Hitret(25357)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_06M", 0, 0)
	Talk("Yahiro", "YH020154")
	Mess("And Haru...... Don't think you're an outsider justÅ^because you're only here for a few months, and it's okayÅ^to disappear alright?")
	hitretResult := Hitret(25358)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...............................")
	hitretResult := Hitret(25359)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020155")
	Mess("You're the doctor's grandson, you can stay here with aÅ^straight face without any worry.")
	hitretResult := Hitret(25360)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_07M", 0, 0)
	Talk("Yahiro", "YH020156")
	Mess("Haaaaaa, I'm now an accomplice. I don't wish to beÅ^unable to eat Akira's food.")
	hitretResult := Hitret(25361)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......Yahiro-san...... Thank you.")
	hitretResult := Hitret(25362)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_05M", 0, 0)
	Talk("Yahiro", "YH020157")
	Mess("Damn it...... Don't look at me with that expression! IÅ^will get embarrassed!!")
	hitretResult := Hitret(25363)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE118", 0)
	ScStopBgm(1)
	Talk("Akira", "AK021880")
	Mess("I'm back-!! I'm sorry to keep you waiting Hiro-oneechan!")
	hitretResult := Hitret(25364)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Jump(8, 0)
	Talk("Yahiro", "YH020158")
	Mess("Woaaaah----!!!")
	hitretResult := Hitret(25365)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Yahiro-san quickly hits the keyboard, and changes toÅ^another webpage.")
	hitretResult := Hitret(25366)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM03", 0)
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK021881")
	Mess("Ah! Haru-kun is also here? Welcomeee!")
	hitretResult := Hitret(25367)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, hmm...... I'm sorry to bother you.")
	hitretResult := Hitret(25368)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira's eyes are slightly puffy, but she's back to herÅ^cheerful self, you can't feel that she cried at allÅ^earlier.")
	hitretResult := Hitret(25369)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...... I quickly understand Yahiro-san's frighteningÅ^reasons.")
	hitretResult := Hitret(25370)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What sort of feelings does Akira have when she smiles?")
	hitretResult := Hitret(25371)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...... Ryouhei also said it before.")
	hitretResult := Hitret(25372)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B36b", 0, 0)
	Talk("Ryouhei", "RH020490")
	Mess("Are you stupid?")
	hitretResult := Hitret(25373)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH020491")
	Mess("That fellow, has always been thinking of others sinceÅ^young.")
	hitretResult := Hitret(25374)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH020492")
	Mess("To make me not to feel unhappy...... She acts likeÅ^nothing has happened......")
	hitretResult := Hitret(25375)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B16a", 0, 0)
	SetBustup("CG01_05M", 0, 0)
	SetBustup("CC02_02M", 0, 0)
	Transition("WIP_RL", 500)
	Talk("êSÇÃê∫", "")
	Mess("To make me not to feel unhappy......")
	hitretResult := Hitret(25376)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I have given Akira trouble once again. From now onwards,Å^I'll also cause trouble to her......")
	hitretResult := Hitret(25377)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But...... If Akira is able to show her smile from theÅ^bottom of her heart...... I'll be fine...... with losingÅ^this smile.")
	hitretResult := Hitret(25378)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I smile at Akira in return. I won't regret this noÅ^matter when it will become the last smile.")
	hitretResult := Hitret(25379)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_14M", 0, 0)
	Talk("Akira", "AK021882")
	Mess("Hey, hey, what are you looking at? Hm~ A gorgeous dinnerÅ^and after that letting two people be alone in aÅ^suite......")
	hitretResult := Hitret(25380)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Shake(8)
	Talk("Yahiro", "YH020159")
	Mess("Awawawa, Haru asked if there's any place for aÅ^date...... B, but it's too early for you guys......")
	hitretResult := Hitret(25381)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......Do you have......someone?")
	hitretResult := Hitret(25382)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_08M", 0, 0)
	Talk("Yahiro", "YH020160")
	Mess("Shut up!! Leave me alone!! An adult must be prepared forÅ^everything.")
	hitretResult := Hitret(25383)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK021883")
	Mess("The night scenery is really beautiful--, Haru-kun shouldÅ^we go try going there the next time?")
	hitretResult := Hitret(25384)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh!? It might be a little expensive......")
	hitretResult := Hitret(25385)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021884")
	Mess("THen, let's save our pocket money!")
	hitretResult := Hitret(25386)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020161")
	Mess("I already said, it's too early for you guys!")
	hitretResult := Hitret(25387)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_09M", 0, 0)
	Shake(2)
	Talk("Akira", "AK021885")
	Mess("Auauauuuu-!!")
	hitretResult := Hitret(25388)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Yahiro-san uses her fists to drill into Akira's head,Å^much fierce than before.")
	hitretResult := Hitret(25389)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	Change("00_c032")
	Return
	Change("EXIT_SCENARIO")
EndFunc

