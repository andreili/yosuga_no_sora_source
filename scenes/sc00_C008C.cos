Include"yosuga.ch"

Function sc00_C008C()
	ScenarioEnter()
	SetCg("B34a", 400, 0)
	Transition("WIP_BT", 500)
	Talk("Haruka", "")
	Mess("She's not at home, so... maybe she went to Amatsume'sÅ^place?...")
	hitretResult := Hitret(19148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("........ It's a difficult topic, therefore........")
	hitretResult := Hitret(19149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Probably she'll take Amatsume somewhere again...")
	hitretResult := Hitret(19150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("................")
	hitretResult := Hitret(19151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_12M", 0, 0)
	Talk("Kazuha", "KA020064")
	Mess("Ah!?")
	hitretResult := Hitret(19152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Haruka", "")
	Mess("Mi-Migiwa-san...")
	hitretResult := Hitret(19153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA020065")
	Mess("Do you have some business with me? If not then...")
	hitretResult := Hitret(19154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I want to talk with you about what happened yesterday.")
	hitretResult := Hitret(19155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020066")
	Mess("I don't particularly have anything to talk about...")
	hitretResult := Hitret(19156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM16", 0)
	Talk("Haruka", "")
	Mess("If I did anything bad, I would like to apologise...")
	hitretResult := Hitret(19157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020067")
	Mess("I-I don't think that Kasugano-kun did anything wrong inÅ^particular...")
	hitretResult := Hitret(19158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But, Migiwa-san, you're somehow having a hard time toÅ^talk with me, right? That's because yesterday atÅ^Amatsume's place I was...")
	hitretResult := Hitret(19159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA020068")
	Mess("What are you talking about? I don't understand whereÅ^this conversation is going.")
	hitretResult := Hitret(19160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's why... That is...")
	hitretResult := Hitret(19161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020069")
	Mess("I'm sorry that I took an attitude that made you feelÅ^unpleasant.")
	hitretResult := Hitret(19162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020070")
	Mess("Because I'm a way too serious person, I tend to burstÅ^into anger unintentionally. I might seem to be a dullÅ^person...")
	hitretResult := Hitret(19163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020071")
	Mess("I think it's inexcusable that I'm bad at bothÅ^socializing and getting into the mood too.")
	hitretResult := Hitret(19164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Is Migiwa-san trying to say that I should just forgetÅ^everything?...")
	hitretResult := Hitret(19165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Could it be that it's a subject she doesn't want toÅ^friendly talk about?")
	hitretResult := Hitret(19166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If that's the case, maybe it would be better not toÅ^bring it up again.")
	hitretResult := Hitret(19167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Not at all... I'm at fault too, sorry for sayingÅ^strange things.")
	hitretResult := Hitret(19168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA020072")
	Mess("No, I'm sorry for taking an attitude that caused thoseÅ^misunderstanings. But... I'm a little afraid ofÅ^strangers...")
	hitretResult := Hitret(19169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The talk... ends here...")
	hitretResult := Hitret(19170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san changed the subject into her taking blame forÅ^everything.")
	hitretResult := Hitret(19171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Because of that, she ended up feeling uneasy concealingÅ^too much.")
	hitretResult := Hitret(19172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......... I'm not going to tell anyone.")
	hitretResult := Hitret(19173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_12M", 0, 0)
	Talk("Kazuha", "KA020073")
	Mess("Eh?........")
	hitretResult := Hitret(19174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But, I just wanted to say this one thing...")
	hitretResult := Hitret(19175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020074")
	Mess("...You heard it, right?......")
	hitretResult := Hitret(19176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...It might sound like an excuse, but it was by chance.")
	hitretResult := Hitret(19177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA020075")
	Mess("And so, you would like have it explained in more detail?")
	hitretResult := Hitret(19178)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No... If you'll tell me to forget it, I intent to do so.")
	hitretResult := Hitret(19179)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't take any pleasure from scavenging gossips.")
	hitretResult := Hitret(19180)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, because of my careless words, I hurt someone'sÅ^feelings involuntarily.")
	hitretResult := Hitret(19181)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020076")
	Mess("...How much do you know about Akira?")
	hitretResult := Hitret(19182)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The other day I heard that since her grandfather died,Å^Yahiro-san became her guardian.")
	hitretResult := Hitret(19183)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I didn't ask anything beyond that. That's why itÅ^bothered me... what Migiwa-san said.")
	hitretResult := Hitret(19184)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020077")
	Mess("...I can't speak of it, because of my own discretion,")
	hitretResult := Hitret(19185)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I understand, I won't enquire any further.")
	hitretResult := Hitret(19186)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("Even though my mouth may say so, it might take a whileÅ^for the words, that once carved into my consciousness,Å^to fade.")
	hitretResult := Hitret(19187)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020286")
	Mess("Ah, Haru-kun, Kazu-chan!")
	hitretResult := Hitret(19188)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE255", 0)
	SetCg("EC02c", 0, 0)
	Transition("WIP_LR", 500)
	Talk("Akira", "AK020287")
	Mess("We meet at just right time, I was just thinking aboutÅ^going to Haru-kun's home to deliver his laundry.")
	hitretResult := Hitret(19189)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks, I was also thinking about going back to receiveÅ^it.")
	hitretResult := Hitret(19190)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EC02b", 0, 0)
	ScPlayBgm("BGM03", 0)
	Talk("Akira", "AK020288")
	Mess("Kazu-chan, sorry. I heard that grandpa Motegi'sÅ^television stopped displaying the image, so I striked itÅ^in the corner like that - \"Ei!\".")
	hitretResult := Hitret(19191)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020078")
	Mess("Yeah... It's okay with me, I was just going to come toÅ^see you.")
	hitretResult := Hitret(19192)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EC02a", 0, 0)
	Talk("Akira", "AK020289")
	Mess("So, what are the two of you doing? Ah, did I intrudeÅ^you?")
	hitretResult := Hitret(19193)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Not at all, we also met by chance some time ago.")
	hitretResult := Hitret(19194)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020290")
	Mess("...Are you two not fighting?")
	hitretResult := Hitret(19195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!?")
	hitretResult := Hitret(19196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B34a", 400, 0)
	SetBustup("CD02_07M", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Kazuha", "KA020079")
	Mess("B-By no means! Why would we need to do such a thing?")
	hitretResult := Hitret(19197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020291")
	Mess("Because, Kazu-chan was angry all the time sinceÅ^yesterday.")
	hitretResult := Hitret(19198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_12M", 0, 0)
	Talk("Kazuha", "KA020080")
	Mess("Eh?......")
	hitretResult := Hitret(19199)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_05M", 0, 0)
	Talk("Akira", "AK020292")
	Mess("Could it be that it was over me? If that's the case thenÅ^sorry, Haru-kun.")
	hitretResult := Hitret(19200)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020081")
	Mess("It's not a thing you should apologise for, Akira! IÅ^have, of my own accord...")
	hitretResult := Hitret(19201)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020293")
	Mess("Huh? Of your own accord?")
	hitretResult := Hitret(19202)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020082")
	Mess("........ Don't be mean...")
	hitretResult := Hitret(19203)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020294")
	Mess("It's okay not to overdo things for my sake, Kazu-chan.Å^Besides, I'm the wrong one here.")
	hitretResult := Hitret(19204)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	SetBustup("CD02_06M", 0, 0)
	Talk("Kazuha", "KA020083")
	Mess("It's not like that! In the first place... father was...!")
	hitretResult := Hitret(19205)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM10", 0)
	SetBustup("CD02_12M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san has stopped her mouth holding her breath.")
	hitretResult := Hitret(19206)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Father was? With those words, what was unclear graduallyÅ^started to connect. Amatsume with no parents... andÅ^Migiwa-san calling \"Onee-chan\" such a girl.")
	hitretResult := Hitret(19207)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("........ Could it be that the two of you are........")
	hitretResult := Hitret(19208)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020295")
	Mess("Yeah... we are.")
	hitretResult := Hitret(19209)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020084")
	Mess("...We are sisters with different mothers.")
	hitretResult := Hitret(19210)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With a heavy tone, seemingly embarassing words came fromÅ^Migiwa-san's mouth.")
	hitretResult := Hitret(19211)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If it was a problem concerning only Migiwa-san, sheÅ^probably would tell me about it right away. But it alsoÅ^involved Amatsume, so she was so obstinate.")
	hitretResult := Hitret(19212)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020296")
	Mess("It happens that Kazu-chan's father is also my father.")
	hitretResult := Hitret(19213)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020085")
	Mess("It's as embarrasing as it can get... Going along withÅ^emotions lasting such a short time, he brought sorrow onÅ^people.")
	hitretResult := Hitret(19214)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020297")
	Mess("No, I don't feel like that at all.")
	hitretResult := Hitret(19215)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020086")
	Mess("I can't agree at all... and your circumstances are aÅ^good proof of it.")
	hitretResult := Hitret(19216)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020087")
	Mess("In the end, not acknowledging you, he just gives you theÅ^money out of obligation.")
	hitretResult := Hitret(19217)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020088")
	Mess("And to live in such a lonesome place alone...")
	hitretResult := Hitret(19218)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020298")
	Mess("I'm not alone and I'm not lonely at all.")
	hitretResult := Hitret(19219)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020089")
	Mess("Are you talking about the cat? That has nothing to doÅ^with it, right?")
	hitretResult := Hitret(19220)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020299")
	Mess("There is a grandpa there. Besides, I'm a Amatsume'sÅ^child, right?")
	hitretResult := Hitret(19221)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020090")
	Mess("........ Akira........")
	hitretResult := Hitret(19222)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020300")
	Mess("My mother has passed away right after giving birth toÅ^me.")
	hitretResult := Hitret(19223)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020301")
	Mess("At that time, father didn't know who I was, so everyoneÅ^was troubled... Grandpa said that he would take me in.")
	hitretResult := Hitret(19224)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020302")
	Mess("That's why, since the beginning, I was thinking ofÅ^myself as Amatsume's kid. But, when grandpa has died, IÅ^was called in to Kazu-chan's house... At that time,Å^circumstances were explained to me.")
	hitretResult := Hitret(19225)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA020091")
	Mess("Properly speaking, even though it was sudden, he had aÅ^responsibility to take you in right after you were born.")
	hitretResult := Hitret(19226)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020092")
	Mess("........ That's too late... To put it off until itÅ^became like that, and behave like a father at thisÅ^time...")
	hitretResult := Hitret(19227)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020093")
	Mess("Me too... If I knew the circumstances earlier, I wouldÅ^have came to meet you right away...")
	hitretResult := Hitret(19228)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020094")
	Mess("But, nothing has changed after all...")
	hitretResult := Hitret(19229)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020303")
	Mess("It's okay. Nevertheless, I have just found out thatÅ^Kazu-chan was my sister.")
	hitretResult := Hitret(19230)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020304")
	Mess("Me also, when I was thinking that I have sibling, IÅ^became happy right away with just that.")
	hitretResult := Hitret(19231)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020305")
	Mess("Me and Kazu-chan, we were born in the same day.")
	hitretResult := Hitret(19232)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_05M", 0, 0)
	Talk("Akira", "AK020306")
	Mess("It's weird, right?... Even though I'm the one notÅ^reliable at all, I was born just a little beforeÅ^Kazu-chan.")
	hitretResult := Hitret(19233)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020307")
	Mess("Kazu-chan's the one that gives off the feeling ofÅ^Onee-san, right?")
	hitretResult := Hitret(19234)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_09M", 0, 0)
	Talk("Kazuha", "KA020095")
	Mess("........ What are you talking about?... It's not... likeÅ^that...")
	hitretResult := Hitret(19235)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020308")
	Mess("In the end, I would be just a bother in Kazu-chan'sÅ^home, so I decided that I'm okay with things as they areÅ^now.")
	hitretResult := Hitret(19236)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020309")
	Mess("Kazu-chan's father has a hard time at work too.")
	hitretResult := Hitret(19237)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020096")
	Mess("............")
	hitretResult := Hitret(19238)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(5)
	Talk("Akira", "AK020310")
	Mess("That's why there aren't really many people knowing aboutÅ^that. If it's Haru-kun, I though that it would be okayÅ^to tell it. I'm sorry for telling it in such a form.")
	hitretResult := Hitret(19239)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Not at all... Me too, I'm sorry... Even though it wasÅ^such an important thing, I was poking nose into yourÅ^affairs.")
	hitretResult := Hitret(19240)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020311")
	Mess("Probably, it's a thing you would got to know some day,Å^so you don't have to apologise.")
	hitretResult := Hitret(19241)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020312")
	Mess("Please don't get angry at Kazu-chan, okay?")
	hitretResult := Hitret(19242)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There's no way I would get angry... It's Amatsume'sÅ^matter after all.")
	hitretResult := Hitret(19243)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_11M", 0, 0)
	Talk("Akira", "AK020313")
	Mess("Thank you.")
	hitretResult := Hitret(19244)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("I think that was a shocking situation for Amatsume. EvenÅ^so, I wonder why ordinarly nothing has changed.")
	hitretResult := Hitret(19245)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though it's about her parents, why can she go onÅ^easily like that, I wonder...")
	hitretResult := Hitret(19246)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Could it be that Amatsume is quite dry in situationsÅ^like this?")
	hitretResult := Hitret(19247)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Probably not........ When thinking about Migiwa-san, sheÅ^seemed to take an attitude like it wasn't a big deal.")
	hitretResult := Hitret(19248)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's why, Migiwa-san, who noticed it, is frustrated...")
	hitretResult := Hitret(19249)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She must feel like that for sure. That's why she'sÅ^always together with her, worrying about her.")
	hitretResult := Hitret(19250)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just like Amatsume said, if we wondered which one wouldÅ^be the elder sister, it looks like it would fitÅ^Migiwa-san more.")
	hitretResult := Hitret(19251)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("But... even so, for Migiwa-san, Amatsume is herÅ^\"Onee-chan\".")
	hitretResult := Hitret(19252)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020314")
	Mess("From now on it's okay not to hold back in front ofÅ^Haru-kun, right?")
	hitretResult := Hitret(19253)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020097")
	Mess("No........ it's embarrassing.")
	hitretResult := Hitret(19254)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM03", 0)
	SetBustup("CC06_06M", 0, 0)
	Talk("Akira", "AK020315")
	Mess("Eeh, why is it? We already explained him the situation,Å^we don't have to hide it anymore.")
	hitretResult := Hitret(19255)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020098")
	Mess("I said no! I won't call you like that anymore.")
	hitretResult := Hitret(19256)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_07M", 0, 0)
	Talk("Akira", "AK020316")
	Mess("So is it?... I won't be called \"Onee-chan\" by Kazu-chan,Å^right?")
	hitretResult := Hitret(19257)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_12M", 0, 0)
	Talk("Kazuha", "KA020099")
	Mess("Eh........ D-Don't make a face like that... O-Of courseÅ^it was a joke, right?")
	hitretResult := Hitret(19258)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020317")
	Mess("Really? That's relieveing. Haru-kun, she said that sheÅ^will call me like that.")
	hitretResult := Hitret(19259)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_06M", 0, 0)
	Talk("Kazuha", "KA020100")
	Mess("Eh!? G-Geez! Don't tease me!! Why are you always likeÅ^that, Akira?")
	hitretResult := Hitret(19260)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03M", 0, 0)
	Talk("Akira", "AK020318")
	Mess("Ahaha... It's because Kazu-chan's cute.")
	hitretResult := Hitret(19261)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020101")
	Mess("Ah! Cu-Cute? I am??")
	hitretResult := Hitret(19262)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020319")
	Mess("Yeah, somehow hard to approach, but really itching to doÅ^so.")
	hitretResult := Hitret(19263)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_04M", 0, 0)
	Talk("Kazuha", "KA020102")
	Mess("I... I don't care... Besides, what are you saying withÅ^that \"itching\"!")
	hitretResult := Hitret(19264)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's interesting how in times like this Migiwa-sanÅ^doesn't seem so strong.")
	hitretResult := Hitret(19265)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even when she's being teased, she has a character toÅ^take it in an upright way, so she ends up being teasedÅ^again.")
	hitretResult := Hitret(19266)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I thought that at pool too, but maybe Amatsume is theÅ^one on offense in their relationship, making a goodÅ^combination with Migiwa-san.")
	hitretResult := Hitret(19267)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The two of you really get along, huh?")
	hitretResult := Hitret(19268)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA020103")
	Mess("How are you seeing it, saying such a thing? GoodÅ^grief... And here I though that Kasugano-kun was more ofÅ^a decent person...")
	hitretResult := Hitret(19269)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020104")
	Mess("Anyway, if you'll get carried away too much with Akira,Å^like Nakazato-senpai, next time I won't go so easy onÅ^you!")
	hitretResult := Hitret(19270)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh!? Me to? ...B-Be gentle with me, okay?...")
	hitretResult := Hitret(19271)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_14M", 0, 0)
	Talk("Akira", "AK020320")
	Mess("Fufufu, Haru-kun will also get ripostes from Kazu-chan,Å^right? Friendship, friendship~Åô")
	hitretResult := Hitret(19272)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_04M", 0, 0)
	Talk("Kazuha", "KA020105")
	Mess("Number of people being a handful just keeps increasing,Å^what kind of situation is that...")
	hitretResult := Hitret(19273)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020321")
	Mess("Really, Kazu-chan has it hard, right?")
	hitretResult := Hitret(19274)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_06M", 0, 0)
	Talk("Kazuha", "KA020106")
	Mess("You're being the biggest handful of them all!!")
	hitretResult := Hitret(19275)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se011", 0)
	Flush("WHITE", 100, "")
	SetBustup("CC06_09M", 0, 0)
	Down(2, 50, 500, 3)
	Talk("Akira", "AK020322")
	Mess("Anya!?")
	hitretResult := Hitret(19276)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_10M", 0, 0)
	Talk("Kazuha", "KA020107")
	Mess("For God's sake... Cut it out... O-Onee-chan...")
	hitretResult := Hitret(19277)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Down(2, -1, 0, 0)
	Talk("Akira", "AK020323")
	Mess("Yeah... Sorry, Kazu-chan.")
	hitretResult := Hitret(19278)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san doesn't seem willing to accept it, but IÅ^really think that the two of them make up a goodÅ^combination.")
	hitretResult := Hitret(19279)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As for the circumstances, even though the two of themÅ^aren't recognised as a family, they are considerate ofÅ^each other's feelings and tied to each other as far asÅ^possible. I think that the two of them already became aÅ^splendid sisters.")
	hitretResult := Hitret(19280)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	Change("00_c009")
	Return
	Change("EXIT_SCENARIO")
EndFunc

