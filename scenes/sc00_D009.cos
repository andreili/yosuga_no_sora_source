Include"yosuga.ch"

Function sc00_D009()
	ScenarioEnter()
	SetCg("BLACK", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("...Perhaps,")
	hitretResult := Hitret(27664)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm just overthinking about that "Onee-chan" incident.")
	hitretResult := Hitret(27665)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I haven't heard of Migiwa-san having any siblings.Å^Anyway, what's going on?")
	hitretResult := Hitret(27666)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM05", 0)
	SetCg("B34a", 400, 0)
	Talk("êSÇÃê∫", "")
	Mess("The following day.")
	hitretResult := Hitret(27667)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	SetBustup("CB01_01M", 0, 0)
	Talk("Ryouhei", "RH040083")
	Mess("Sora-chan, mornin'! Mornin' to you as well, Haruka!")
	hitretResult := Hitret(27668)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO040013")
	Mess("Morning, Haru-chan.")
	hitretResult := Hitret(27669)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Morning, Ryouhei, Nao-chan.Å^...You greet them as well, Sora.")
	hitretResult := Hitret(27670)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR040043")
	Mess("...Mor...ning.")
	hitretResult := Hitret(27671)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH040084")
	Mess("WHOAA! Being greeted by Sora-chan!!Å^Is today my lucky day!?")
	hitretResult := Hitret(27672)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO040014")
	Mess("Oh, shut up.Å^Good morning to you too, Sora-chan.")
	hitretResult := Hitret(27673)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040044")
	Mess("...*nod*.")
	hitretResult := Hitret(27674)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora and I had moved out here less than a month ago....")
	hitretResult := Hitret(27675)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But to us, gathering like this and going to school wasÅ^something we did everyday.")
	hitretResult := Hitret(27676)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I bet the fact that there were cheerful people likeÅ^Ryouhei, or nostalgic people like Nao-chan wasÅ^important.")
	hitretResult := Hitret(27677)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Besides...")
	hitretResult := Hitret(27678)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Akira", "AK040108")
	Mess("Ahh, everyone's already here! Kazu-chan, hurry, hurry!")
	hitretResult := Hitret(27679)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040156")
	Mess("Wai, Wait a moment, Akira!...Wai...Wait for me...")
	hitretResult := Hitret(27680)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	SetBustup("CD01_08M", 0, 0)
	Talk("Akira", "AK040109")
	Mess("Morning everyone!")
	hitretResult := Hitret(27681)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040157")
	Mess("*Pant* *pant* Geez...there's no reason to be...in such aÅ^hurry...")
	hitretResult := Hitret(27682)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If there wasn't someone as friendly as Amatsume, IÅ^wonder if I would get so close to everyone, includingÅ^Migiwa-san...")
	hitretResult := Hitret(27683)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040085")
	Mess("Well, well, which one of you slept in today?")
	hitretResult := Hitret(27684)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_05M", 0, 0)
	Talk("Akira", "AK040110")
	Mess("Ahaa, I was waiting for Hiro-neechan to return, so IÅ^fell asleep really late.")
	hitretResult := Hitret(27685)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040158")
	Mess("*pant*...Really...who's...taking care of ...who...?Å^*pant*")
	hitretResult := Hitret(27686)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Good morning, Amatsume, Migiwa-san.")
	hitretResult := Hitret(27687)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK040111")
	Mess("Morning, Haru-kun. Thanks for yesterday!")
	hitretResult := Hitret(27688)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's nothing, I just helped with the cleaning. Besides,Å^you already given me tea in return.")
	hitretResult := Hitret(27689)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040112")
	Mess("If it's just tea, I'll give you some anytime. Drop byÅ^whenever you want, okay?")
	hitretResult := Hitret(27690)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA040159")
	Mess("...Good morning, Kasugano-kun.")
	hitretResult := Hitret(27691)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah...")
	hitretResult := Hitret(27692)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040160")
	Mess("Akira, let's go. We'll be late.")
	hitretResult := Hitret(27693)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(5)
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK040113")
	Mess("Eh? Kazu-chan?")
	hitretResult := Hitret(27694)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD01_01S", 0, 0)
	Talk("Kazuha", "KA040161")
	Mess("Akira! Hurry up!")
	hitretResult := Hitret(27695)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	SetBustup("CC01_08M", 0, 0)
	Talk("Akira", "AK040114")
	Mess("...Sorry. I'll see you at school.")
	hitretResult := Hitret(27696)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	BustupClear(5)
	Talk("Ryouhei", "RH040086")
	Mess("...She's really in a hurry, huh?")
	hitretResult := Hitret(27697)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_08M", 0, 0)
	Talk("Nao", "NO040015")
	Mess("Did you make another weird pass at her again?")
	hitretResult := Hitret(27698)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040087")
	Mess("Hmm...perhaps...my charming appearance...dazzled her?")
	hitretResult := Hitret(27699)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH040088")
	Mess("Hitting on Akira isn't such a bad idea... Heh, but beingÅ^loved by the Ojou, I'm such a sinful man.")
	hitretResult := Hitret(27700)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No way am I letting Sora go out with such a playboy.")
	hitretResult := Hitret(27701)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_06M", 0, 0)
	Talk("Ryouhei", "RH040089")
	Mess("No, no, no! I'm joking, it was a joke! Onii-sama!")
	hitretResult := Hitret(27702)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("And don't call me Onii-sama.")
	hitretResult := Hitret(27703)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR040045")
	Mess("...Idiots.")
	hitretResult := Hitret(27704)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("But.")
	hitretResult := Hitret(27705)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I think the reason why Migiwa-san went without us wasÅ^definitely because of my presence.")
	hitretResult := Hitret(27706)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was as if she was avoiding me.....like yesterday.")
	hitretResult := Hitret(27707)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	Change("00_d010")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

