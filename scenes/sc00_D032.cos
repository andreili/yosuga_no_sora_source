Include"yosuga.ch"

Function sc00_D032()
	ScenarioEnter()
	SetCg("BLACK", 0, 0)
	WindowView(1)
	ScPlayBgm("BGM10", 0)
	Talk("モノローグ《一葉》", "KA041215")
	Mess("Until Akira had said that to me, I hadn't realized that／I was running away from something.")
	hitretResult := Hitret(32122)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041216")
	Mess("Because of that, the change in Akira's attitude was／natural.")
	hitretResult := Hitret(32123)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041217")
	Mess("Because of that...I had neglected Akira.")
	hitretResult := Hitret(32124)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041218")
	Mess("Being with Haruka was so fun that I lost sight of Akira.")
	hitretResult := Hitret(32125)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041219")
	Mess("The time I spent with Haruka, were times when I had／forgotten about Akira.")
	hitretResult := Hitret(32126)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041220")
	Mess("Even though I tried to hide it, I probably changed too.")
	hitretResult := Hitret(32127)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041221")
	Mess("Being as perceptive as she is, there's no way Akira／wouldn't have noticed that.")
	hitretResult := Hitret(32128)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041222")
	Mess("But why was running away even necessary...?")
	hitretResult := Hitret(32129)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041223")
	Mess("Was it because I had forgotten about Akira for a while?／Because I had forgotten about my own sister...?")
	hitretResult := Hitret(32130)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041224")
	Mess("Because forgetting...was fun?")
	hitretResult := Hitret(32131)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041225")
	Mess("Because I, who would do anything if it was for her sake,／neglected her instead?")
	hitretResult := Hitret(32132)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041226")
	Mess("But I didn't mean for that to happen!")
	hitretResult := Hitret(32133)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041227")
	Mess("I love Haruka.")
	hitretResult := Hitret(32134)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041228")
	Mess("But I love my sister, too!")
	hitretResult := Hitret(32135)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041229")
	Mess("Just beacuse I want to be with Haruka, it doesn't mean／it's okay for me lose Akira!")
	hitretResult := Hitret(32136)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041230")
	Mess("Has my mother and father...our whole family made my／sister on guard against us?")
	hitretResult := Hitret(32137)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041231")
	Mess("But she doesn't have to be on guard against me any more／than that!")
	hitretResult := Hitret(32138)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041232")
	Mess("Why can't she understand that?")
	hitretResult := Hitret(32139)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041233")
	Mess("Why would she say that it's okay if she disappeared!?")
	hitretResult := Hitret(32140)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041234")
	Mess("Why...did she say...we'd be friends...and not／sisters...not family...")
	hitretResult := Hitret(32141)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041235")
	Mess("Because I...because I left my sister behind and went／searching for my own happiness...?")
	hitretResult := Hitret(32142)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041236")
	Mess("Because I...had become happy with Haruka?")
	hitretResult := Hitret(32143)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041237")
	Mess("Is that why my sister is becoming unhappy...?")
	hitretResult := Hitret(32144)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041238")
	Mess("I can't...I can't let that happen... My sister is／someone who has to be happy...")
	hitretResult := Hitret(32145)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041239")
	Mess("............")
	hitretResult := Hitret(32146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041240")
	Mess("Then...Then, I have to...let go...let go of Haruka...?")
	hitretResult := Hitret(32147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041241")
	Mess("If I'm alone again, Onee-chan will definitely come back／to me...")
	hitretResult := Hitret(32148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041242")
	Mess("But...")
	hitretResult := Hitret(32149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041243")
	Mess("Breaking up with Haruka... Forgetting those happy days／together...")
	hitretResult := Hitret(32150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041244")
	Mess("I...love Haruka...")
	hitretResult := Hitret(32151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041245")
	Mess("Akira... Onee-chan is also precious...")
	hitretResult := Hitret(32152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041246")
	Mess("Why.... Why can't I have them both...?")
	hitretResult := Hitret(32153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041247")
	Mess("Why only one of them... Why will only one of them be／there for me...?")
	hitretResult := Hitret(32154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《一葉》", "KA041248")
	Mess("Why...?")
	hitretResult := Hitret(32155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	WindowView(0)
	Change("00_d033")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

