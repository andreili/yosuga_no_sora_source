Include"yosuga.ch"

Function sc00_D019B()
	ScenarioEnter()
	SetCg("B07a", 0, 0)
	ScPlayBgm("BGM15", 0)
	Talk("êSÇÃê∫", "")
	Mess("They said it was manual labor, but the things to moveÅ^were the shrine goods and things for the ceremony, soÅ^they weren't very heavy by themselves.")
	hitretResult := Hitret(29232)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But going back and forth countless times made it a lotÅ^harder.")
	hitretResult := Hitret(29233)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And after the ceremony's over, I'll have to help takeÅ^everything down.")
	hitretResult := Hitret(29234)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And if the shrine doesn't sell out, I'll have to carryÅ^them too.")
	hitretResult := Hitret(29235)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But now, the first round of work was done, and it wasÅ^time for a short vacation.")
	hitretResult := Hitret(29236)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK040288")
	Mess("Ahaa, nice work, Haru-kun.")
	hitretResult := Hitret(29237)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha...a bit rough since I'm not that buff. You'reÅ^having a break too?")
	hitretResult := Hitret(29238)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040289")
	Mess("I'm off for the purification ceremonyÅ` I have to cleanÅ^myself before the ceremony.")
	hitretResult := Hitret(29239)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The purification ceremony is...um...pouring water overÅ^your head?")
	hitretResult := Hitret(29240)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040290")
	Mess("Yeah, yeah! There's a lot more steps to it though, soÅ^it's a bit annoyingÅ`")
	hitretResult := Hitret(29241)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_09M", 0, 0)
	Talk("Akira", "AK040291")
	Mess("It's a good thing the festival isn't in the winter.")
	hitretResult := Hitret(29242)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Whoa...if it was in the winter, would you still have toÅ^do it?")
	hitretResult := Hitret(29243)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040292")
	Mess("It would be impossible, huh...")
	hitretResult := Hitret(29244)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It wouldn't make a nice story if she were to catch aÅ^cold...")
	hitretResult := Hitret(29245)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK040293")
	Mess("Ahh, I'm going to run out of time, so I'm going nowÅ`")
	hitretResult := Hitret(29246)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, sorry for stopping you. Good luck.")
	hitretResult := Hitret(29247)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("She ran off toward the back of the shrine while waving.")
	hitretResult := Hitret(29248)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I think that there's only the mountain trail that way,Å^but where is she going?")
	hitretResult := Hitret(29249)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Don't you do it in a bathroom?")
	hitretResult := Hitret(29250)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_01M", 0, 0)
	Talk("Kazuha", "KA040485")
	Mess("Ah, Kasugano-kun! have you seen Akira?")
	hitretResult := Hitret(29251)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("She said she was off to do the purification ceremony,Å^and ran off that way.")
	hitretResult := Hitret(29252)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_12M", 0, 0)
	Talk("Kazuha", "KA040486")
	Mess("Eh? She left already? What should I do...")
	hitretResult := Hitret(29253)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What's wrong?")
	hitretResult := Hitret(29254)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040487")
	Mess("She forgot to take her new undershirt and stuff withÅ^her...")
	hitretResult := Hitret(29255)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, she was empty handed, but I thought there wasÅ^something to change into there.")
	hitretResult := Hitret(29256)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_07M", 0, 0)
	Talk("Kazuha", "KA040488")
	Mess("No way there's something like that at a lake. AfterÅ^telling her not to forget that many times...")
	hitretResult := Hitret(29257)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Lake? She's doing it at a place like that?")
	hitretResult := Hitret(29258)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_03M", 0, 0)
	Talk("Kazuha", "KA040489")
	Mess("Ah, you don't know, I guess. There's a lake even higherÅ^up on the mountain than this shrine. That's the shrine'sÅ^cleansing water.")
	hitretResult := Hitret(29259)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040490")
	Mess("The Chouzuya's water is pulled up from there too.")
	hitretResult := Hitret(29260)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see...")
	hitretResult := Hitret(29261)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Today is a whole day of discovery of new facts...")
	hitretResult := Hitret(29262)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_12M", 0, 0)
	Talk("Kazuha", "KA040491")
	Mess("Wait, this isn't a time for talking.Å^Yeah, Kasugano-kun! Can you do me a favor?")
	hitretResult := Hitret(29263)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No problems, luggage carrying?")
	hitretResult := Hitret(29264)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_02M", 0, 0)
	Talk("Kazuha", "KA040492")
	Mess("It's a girl's things, so I'll carry it up for her. CanÅ^you go tell Akira to wait up a bit?")
	hitretResult := Hitret(29265)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040493")
	Mess("I can't run very fast in this kimono.")
	hitretResult := Hitret(29266)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Roger that, just chase her down. You can come overÅ^slowly, Migiwa-san.")
	hitretResult := Hitret(29267)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040494")
	Mess("Thanks, be careful, okay?")
	hitretResult := Hitret(29268)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("I ran after Akira down the road from the main shrine.")
	hitretResult := Hitret(29269)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I seem to remember going through this road before.")
	hitretResult := Hitret(29270)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I was going cicada hunting with Amatsume, I wentÅ^into this forest.")
	hitretResult := Hitret(29271)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At the time, I didn't know there was a lake up here...")
	hitretResult := Hitret(29272)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B26a", 0, 0)
	Transition("WIP_LR", 500)
	ScPlayEnvSe("se014", "", 0)
	Talk("êSÇÃê∫", "")
	Mess("It's been a long time since then, and it was a lotÅ^rougher than in my memories.")
	hitretResult := Hitret(29273)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There was nothing to help you up like stairs, and treeÅ^roots randomly jutted out, making it hard to walk.")
	hitretResult := Hitret(29274)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There were small rocks as well, so it was also easy toÅ^trip.")
	hitretResult := Hitret(29275)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Road isn't the word for it, it's more like an animalÅ^trail.")
	hitretResult := Hitret(29276)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That Amatsume went running through here...?")
	hitretResult := Hitret(29277)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She was wearing sandals, I was wearing sneakers. As aÅ^bonus, it's a mountain road, so I thought I'd catch upÅ^to her easily, but i still haven't caught up.")
	hitretResult := Hitret(29278)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Don't tell me I'm on the wrong road?")
	hitretResult := Hitret(29279)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Thanks to the noisy racket from the cicadas, I couldn'tÅ^hear the shrine grounds.")
	hitretResult := Hitret(29280)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't think I should've gone that far, but I can'tÅ^hear the festival music.")
	hitretResult := Hitret(29281)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just the swaying of branches and the sound of leaves.")
	hitretResult := Hitret(29282)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's almost like I've been lost in another world...")
	hitretResult := Hitret(29283)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No way that's true. What kind of coward am I!?")
	hitretResult := Hitret(29284)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With a purposely loud voice, I tried to encourageÅ^myself.")
	hitretResult := Hitret(29285)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If I didn't do that, I'd be squashed by uncertainty.")
	hitretResult := Hitret(29286)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE014", 1)
	BlackOut(0, 1)
	SetCg("B25a", 0, 0)
	Transition("WIP_TB", 500)
	ScPlayEnvSe("SE406", "", 0)
	Talk("Haruka", "")
	Mess("I-I'm here...")
	hitretResult := Hitret(29287)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Suddenly, the trees parted and the glittering lakeÅ^spread out before me.")
	hitretResult := Hitret(29288)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A cool breeze crossed over the lake's surface andÅ^brushed away my fatigue.")
	hitretResult := Hitret(29289)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I couldn't feel the heat, and it was such a beautifulÅ^place that it's hard to believe that it's in the sameÅ^world as the area below.")
	hitretResult := Hitret(29290)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK040294")
	Mess("Eh? What's up, Haru-kun?")
	hitretResult := Hitret(29291)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("On the shores of the lake was what looked like a plainÅ^wooden shelf, perhaps a Kamidana, and Amatsume wasÅ^standing in front of it.")
	hitretResult := Hitret(29292)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While I can't really say that I made it in time, atÅ^least she hadn't started the purification ceremony.")
	hitretResult := Hitret(29293)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, Migiwa-san told me to tell you to wait a bit whileÅ^she goes to get you a change of clothes.")
	hitretResult := Hitret(29294)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_05M", 0, 0)
	Talk("Akira", "AK040295")
	Mess("Ahh, i completely forgot those.Å^Thanks for coming all this way, Haru-kun.")
	hitretResult := Hitret(29295)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I thought I'd be able to catch your before you got here,Å^though. Guess I didn't make it.")
	hitretResult := Hitret(29296)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK040296")
	Mess("Ahaa, must've been hard getting up here. Want some tea?")
	hitretResult := Hitret(29297)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks, I'll have a cup.")
	hitretResult := Hitret(29298)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("She took out a tea kettle from the lake.")
	hitretResult := Hitret(29299)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Zoom(560, 180, 400, 300, 0, 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("An old shrine extended out at the other side of theÅ^lake.")
	hitretResult := Hitret(29300)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was an extremely worn out building, making it feelÅ^like an antique.")
	hitretResult := Hitret(29301)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There's a shrine here too?")
	hitretResult := Hitret(29302)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Zoom(400, 300, 800, 600, 0, 0, 0)
	Talk("Akira", "AK040297")
	Mess("Yep. That place is pretty much unused now, though.")
	hitretResult := Hitret(29303)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While having my tea poured, I nodded.")
	hitretResult := Hitret(29304)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I scanned the area once again, thinking that IÅ^definitely don't have any memories of this place.")
	hitretResult := Hitret(29305)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("When we were kids, we went cicada hunting, right? Did weÅ^come up here at the time?")
	hitretResult := Hitret(29306)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040298")
	Mess("I wonder if we did... Nah, I don't think so.")
	hitretResult := Hitret(29307)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So this is my first time, then... After coming once,Å^there's no way I'd forget such a gorgeous scene, IÅ^suppose.")
	hitretResult := Hitret(29308)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK040299")
	Mess("It's a good thing you like it tooÅ`")
	hitretResult := Hitret(29309)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, I like it. A lot.")
	hitretResult := Hitret(29310)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040495")
	Mess("...Akira!!....Your clothes!...")
	hitretResult := Hitret(29311)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And then Migiwa-san finally arrived.")
	hitretResult := Hitret(29312)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_01M", 0, 0)
	Talk("Kazuha", "KA040496")
	Mess("...Akira, you really managed to forget it...")
	hitretResult := Hitret(29313)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It must have been hard going up that mountain trail in aÅ^kimono.")
	hitretResult := Hitret(29314)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She passed the undershirt and a Japanese towel to Akira,Å^sweat clearly visible on her brow.")
	hitretResult := Hitret(29315)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040300")
	Mess("Thanks, Kazu-chan.")
	hitretResult := Hitret(29316)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Taking the change of clothes and putting it on theÅ^shelf, she started taking off her shrine maiden outfit.")
	hitretResult := Hitret(29317)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide()
	ScStopEnvSe("SE406", 1)
	BustupClear()
	Update(1)
	SetCg("BLACK", 0, 0)
	Transition("WIP_RL", 250)
	Update()
	SetCg("B25a", 0, 0)
	Transition("WIP_RL", 250)
	ScPlayBgm("BGM14", 0)
	Talk("Haruka", "")
	Mess("Akira, Wai-...")
	hitretResult := Hitret(29318)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040301")
	Mess("Eh? Whaaat?")
	hitretResult := Hitret(29319)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("N-Nothing.")
	hitretResult := Hitret(29320)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("S-Suddenly undressing like that...if I hadn't quicklyÅ^turned around, I'd have seen her...")
	hitretResult := Hitret(29321)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_07M", 0, 0)
	Talk("Kazuha", "KA040497")
	Mess("Kasugano-kun?")
	hitretResult := Hitret(29322)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("M-Migiwa-san...")
	hitretResult := Hitret(29323)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She's mad...she's mad...isn't..she...?")
	hitretResult := Hitret(29324)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040498")
	Mess("Kasugano-kun, go back down now.")
	hitretResult := Hitret(29325)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes...I'll do that...")
	hitretResult := Hitret(29326)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040302")
	Mess("Eh? Aren't you going to watch?")
	hitretResult := Hitret(29327)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_06M", 0, 0)
	Talk("Kazuha", "KA040499")
	Mess("Akira!!")
	hitretResult := Hitret(29328)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sorry, Akira. I'm curious, but it'll have to be nextÅ^time.")
	hitretResult := Hitret(29329)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_04M", 0, 0)
	Talk("Kazuha", "KA040500")
	Mess("...How dirty... So you're that type of person...")
	hitretResult := Hitret(29330)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-It was a joke...Trying to lighten the...")
	hitretResult := Hitret(29331)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_05M", 0, 0)
	Talk("Kazuha", "KA040501")
	Mess("Hurry up and go.")
	hitretResult := Hitret(29332)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes! Right away!")
	hitretResult := Hitret(29333)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Whoa, it looks like I choose the wrong topic to jokeÅ^around with... I tried to flee back down the mountain...")
	hitretResult := Hitret(29334)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Wha!?")
	hitretResult := Hitret(29335)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Quake(0, 16, 1, 2, 30)
	Update()
	ScWait(150, 0)
	SetCg("B27a", 0, 600)
	Update()
	ScPlaySe("se018", 0)
	Scroll(0, 300, 100, 3, 0)
	WaitScroll()
	Talk("Haruka", "")
	Mess("...Ouch...")
	hitretResult := Hitret(29336)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("On top of fleeing, falling over...")
	hitretResult := Hitret(29337)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B25a", 0, 0)
	SetBustup("CD03_03S", 0, 0)
	Transition("WIP_LR", 500)
	Talk("Kazuha", "KA040502")
	Mess("Ahaha...what are you doing, Geez....Ahaha...")
	hitretResult := Hitret(29338)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("You really don't have to laugh!")
	hitretResult := Hitret(29339)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_d019c")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

