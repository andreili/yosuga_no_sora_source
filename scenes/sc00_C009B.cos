Include"yosuga.ch"

Function sc00_C009B()
	ScenarioEnter()
	SetCg("B34a", 400, 0)
	Transition("WIP_TB", 500)
	Talk("ê∫ÅsâlÅt", "AK020335")
	Mess("Ç®Å[Ç¢ÅIÅ@ÉnÅ[ÉãåNÅIÅI")
	hitretResult := Hitret(19348)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM06", 0)
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK020336")
	Mess("Whew... I finally catched up with you.")
	hitretResult := Hitret(19349)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Huh? Weren't you going back together with Migiwa-san?")
	hitretResult := Hitret(19350)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020337")
	Mess("Kazu-chan has japanese dance practice today, so weÅ^parted halfway.")
	hitretResult := Hitret(19351)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Japanese dance? So, Migiwa-san takes various lessonsÅ^too, huh?")
	hitretResult := Hitret(19352)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK020338")
	Mess("She might be better in Shinto dance than me.")
	hitretResult := Hitret(19353)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020339")
	Mess("Haru-kun, you're alone too, right? Sora-chan went backÅ^earlier?")
	hitretResult := Hitret(19354)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, it seems she became fed up with waiting.")
	hitretResult := Hitret(19355)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Child A", "NP180010")
	Mess("Hey, let's go quickly.")
	hitretResult := Hitret(19356)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Child B", "NP190016")
	Mess("Wait!!")
	hitretResult := Hitret(19357)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kids were passing us. They were holding snacks andÅ^games, so they must be on their way to play at someone'sÅ^house.")
	hitretResult := Hitret(19358)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("These days, they're not going on bug catching, right?")
	hitretResult := Hitret(19359)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK020340")
	Mess("It's not true. Small kids come to catch those toÅ^mountain at the back of my house.")
	hitretResult := Hitret(19360)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020341")
	Mess("Aside from that, crickets, bell crickets, frogs andÅ^others are popular too. When going to sleep in theÅ^evening, they don't seem to be much of a hindrance.")
	hitretResult := Hitret(19361)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes, but, Sora was trying to repel mosquitos withÅ^hairspray and all. I have no idea what was she trying toÅ^do, so I think raising is a relentless job...")
	hitretResult := Hitret(19362)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_03M", 0, 0)
	Talk("Akira", "AK020342")
	Mess("Ahaha, probably feeling some sweet aroma, everyone wasÅ^coming for a visit, huh.")
	hitretResult := Hitret(19363)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Though, it sure brings back memories... Our firstÅ^meeting was when I went for bug catching.")
	hitretResult := Hitret(19364)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK020343")
	Mess("Yeah, Haru-kun, bringing a net and insect cage, came toÅ^play.")
	hitretResult := Hitret(19365)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Grandfather was like \"If you play console games, youÅ^should go out sometimes\" and gave me net and insectÅ^cage.")
	hitretResult := Hitret(19366)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At first I didn't know how to use it, but I graduallyÅ^got the hand of it and started catching grasshoppers andÅ^butterflies.")
	hitretResult := Hitret(19367)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After getting interested in cicadas, I searched for themÅ^a lot in unusual places. One of those was shrineÅ^Amatsume was at.")
	hitretResult := Hitret(19368)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("At that time, you told me about a place with a lot ofÅ^cicadas, right? It happened all of a sudden, but weÅ^became friends right away.")
	hitretResult := Hitret(19369)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK020344")
	Mess("Because, you came holding a net, so I knew that youÅ^wanted to go for a bug catching. Besides, you went outÅ^of your way to come here, so I though I should give youÅ^some hospitality.")
	hitretResult := Hitret(19370)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScPlayEnvSe("SE301", "1", 0)
	SetCg("B07a_", 0, 0)
	Transition("CLOUD_A", 2000)
	Talk("êSÇÃê∫", "")
	Mess("At that time...")
	hitretResult := Hitret(19371)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Being lured here by the voice of cicadas, I'm climbing aÅ^long stairs. As I was about to get to the top, I saw aÅ^spacious and pretty shrine.")
	hitretResult := Hitret(19372)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was a first time I came to that place, so I wonderedÅ^for a moment if it was okay to enter, but then I noticedÅ^someone standing in the middle of shrine grounds.")
	hitretResult := Hitret(19373)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A white kimono and deep red skirt. A small miko noticedÅ^me and was coming here.")
	hitretResult := Hitret(19374)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("GirlÅsâlÅt", "AK020345")
	Mess("Welcome! You came here to catch cicadas, right? ThereÅ^are plenty of them!")
	hitretResult := Hitret(19375)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Above kimono, she had a jewel attacked to a necklace. ItÅ^was moving around as she was sweetly smiling at me.")
	hitretResult := Hitret(19376)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("This was my and her first meeting.")
	hitretResult := Hitret(19377)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("GirlÅsâlÅt", "AK020346")
	Mess("I am Amatsume Akira. And you are?")
	hitretResult := Hitret(19378)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm Kasugano Haruka...")
	hitretResult := Hitret(19379)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020347")
	Mess("Kasugano... Are you from sensei's place?")
	hitretResult := Hitret(19380)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sensei's?")
	hitretResult := Hitret(19381)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020348")
	Mess("Yes, from doctor's place.")
	hitretResult := Hitret(19382)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Grandfather is a doctor, but do you know him?")
	hitretResult := Hitret(19383)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020349")
	Mess("Yeah! He can heal a disease or injury right away, he's aÅ^good doctor.")
	hitretResult := Hitret(19384)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If you say so...")
	hitretResult := Hitret(19385)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Grandfather was easily getting angry, he was pushy, IÅ^wasn't good at dealing with him, but when I was toldÅ^that he's a good doctor, I was strangely happy aboutÅ^that.")
	hitretResult := Hitret(19386)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020350")
	Mess("Haru-kun, come here.")
	hitretResult := Hitret(19387)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh?... Where are you going?")
	hitretResult := Hitret(19388)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020351")
	Mess("I'll take you to a place with a lot of cicadas, okay?")
	hitretResult := Hitret(19389)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Where to?... Umm...")
	hitretResult := Hitret(19390)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020352")
	Mess("\"Akira\" is fine, Haru-kun.")
	hitretResult := Hitret(19391)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... Nice to meet you, Akira.")
	hitretResult := Hitret(19392)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was pulled by hand and taken into the interior ofÅ^Shinto shrine.")
	hitretResult := Hitret(19393)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was a little surprised, since I haven't held handsÅ^with any girl besides Sora and mom.")
	hitretResult := Hitret(19394)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her hand was small and soft, but she was firmly holdingÅ^me while pulling along... Somehow I became embarrassed.")
	hitretResult := Hitret(19395)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Because of embarrassment, I couldn't look on Akira'sÅ^face straight, averting mine. While walking, her pendantÅ^was shaking and hopping with a plopping sound, whichÅ^piqued my interest.")
	hitretResult := Hitret(19396)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was a violet stone with a size of pinky's nailÅ^attached just to a thin leather string. It looked like aÅ^toy.")
	hitretResult := Hitret(19397)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hey, Akira... Are you the one that made that pendant?")
	hitretResult := Hitret(19398)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020353")
	Mess("Eh, this one? Yeah... When I was born, I tore it of fromÅ^mother's necklace...")
	hitretResult := Hitret(19399)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's pretty, that pendant-like thing.")
	hitretResult := Hitret(19400)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Seeming just a little embarrassed, she let me have aÅ^closer look at it.")
	hitretResult := Hitret(19401)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	SetCg("B24a_", 0, 600)
	Scroll(0, 0, 20000, 1, 0)
	Talk("êSÇÃê∫", "")
	Mess("I was climbing the mountain behind the shrine togetherÅ^with Amatsume.")
	hitretResult := Hitret(19402)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When we reached our destination, hearing a great chorusÅ^of cicadas made me excited.")
	hitretResult := Hitret(19403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020354")
	Mess("Aah, a nii-nii cicada, it's over there.")
	hitretResult := Hitret(19404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You're right, and it's making a \"nii-nii\" sound.")
	hitretResult := Hitret(19405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020355")
	Mess("When evening comes, evening cicadas start singing.")
	hitretResult := Hitret(19406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("And what about Meimuna opalifera?")
	hitretResult := Hitret(19407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020356")
	Mess("They don't come out before the end of August, I think.Å^Ah, now that you mention it, cicadas start appearingÅ^around May.")
	hitretResult := Hitret(19408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eeh? No way.")
	hitretResult := Hitret(19409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020357")
	Mess("It's true. Even when it's spring, if there are few hotÅ^days, there are some confused cicadas that come outÅ^early.")
	hitretResult := Hitret(19410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heeh, strange.")
	hitretResult := Hitret(19411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The mountain behind the shrine, the more you climb it,Å^the more cicadas you can hear, so we climbed with itÅ^marking our course.")
	hitretResult := Hitret(19412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hey, is it okay to go in such clothes?")
	hitretResult := Hitret(19413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020358")
	Mess("It's okay! After all, I'm always climbing here.")
	hitretResult := Hitret(19414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The improbable miko-san was facing mountain climbingÅ^wearing such clothes, and wearing a Japanese sandals atÅ^that, while I, wearing sport shoes, found it hard toÅ^climb up such a steep hill smoothly.")
	hitretResult := Hitret(19415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I was wondering how long are we going to climb, theÅ^girl suddenly stopped and raised her finger.")
	hitretResult := Hitret(19416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020359")
	Mess("There they are... It looks like we can catch them.")
	hitretResult := Hitret(19417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, you're right! Alright!!")
	hitretResult := Hitret(19418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020360")
	Mess("Give it your best!")
	hitretResult := Hitret(19419)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B24a_", 0, 250)
	Talk("êSÇÃê∫", "")
	Mess("There was a large brown cicada on a low place on theÅ^tree. I approached it slowly as to not let it notice me.")
	hitretResult := Hitret(19420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With a buzzing sound, probably not noticing me, whileÅ^letting it's body shake weakly, it continued makingÅ^sound.")
	hitretResult := Hitret(19421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There!!")
	hitretResult := Hitret(19422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I swung down, aiming at the cicada I targeted.")
	hitretResult := Hitret(19423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I did it!!")
	hitretResult := Hitret(19424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Immediately after being covered with a net, the startledÅ^cicada jumped up, struggling inside the web.")
	hitretResult := Hitret(19425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Umm... W-What should I do now...")
	hitretResult := Hitret(19426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I didn't know what to do now, when I had the net pressedÅ^down to the tree.")
	hitretResult := Hitret(19427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020361")
	Mess("It'll become quiet, you can catch it then.")
	hitretResult := Hitret(19428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The cicada that was raging around in the net, seeingÅ^that there is no way out, became docile, catched in theÅ^net.")
	hitretResult := Hitret(19429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I put my hand inside the net and slowly grabbed it byÅ^the back, catching it.")
	hitretResult := Hitret(19430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I catched it!!")
	hitretResult := Hitret(19431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020362")
	Mess("Congratulations! How nice! Well then, next one is there!")
	hitretResult := Hitret(19432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah...")
	hitretResult := Hitret(19433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I put it in a bug cage and finally felt relieved. Then,Å^she grabbed my hand and I was pulled to the next placeÅ^with cicadas.")
	hitretResult := Hitret(19434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020363")
	Mess("Let me see... That one over there, it's probably aÅ^Cryptotympana facialis.")
	hitretResult := Hitret(19435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At the place she pointed at, a completely black cicadaÅ^was making a jangling noise.")
	hitretResult := Hitret(19436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Alright!")
	hitretResult := Hitret(19437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was at a little high place, so I climbed a slope and,Å^from behind the tree, I had to go around it and take outÅ^my net there.")
	hitretResult := Hitret(19438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020364")
	Mess("Be careful not to fall down, okay?")
	hitretResult := Hitret(19439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... yeah.")
	hitretResult := Hitret(19440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Saying so, she held me delicately by shirttail.")
	hitretResult := Hitret(19441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While I was pitching forward, I swung the net with allÅ^my might.")
	hitretResult := Hitret(19442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ei!!")
	hitretResult := Hitret(19443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020365")
	Mess("Ah......")
	hitretResult := Hitret(19444)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Maybe sensing me, the cicada flew away, but it jumpedÅ^right into the net by chance.")
	hitretResult := Hitret(19445)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020366")
	Mess("Ahaha! Nice catch!")
	hitretResult := Hitret(19446)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Right, that makes it second one!")
	hitretResult := Hitret(19447)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020367")
	Mess("Ah, that one is a male. A male Cryptotympana facialis.")
	hitretResult := Hitret(19448)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heh, you're good at it.")
	hitretResult := Hitret(19449)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020368")
	Mess("On it's stomach, it has a pattern looking like an orangeÅ^paw. I heard that this is a mark that males have.")
	hitretResult := Hitret(19450)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I didn't know that at all. You're a walkingÅ^encyclopedia, you know?")
	hitretResult := Hitret(19451)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020369")
	Mess("I was taught this by grandpa. It was when we wereÅ^catching cicadas together.")
	hitretResult := Hitret(19452)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020370")
	Mess("At this place, there are many different bugs besidesÅ^cicadas. For example rhinoceros beetle, stag beetle,Å^long-horned beetle or scarabaeid beetle.")
	hitretResult := Hitret(19453)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Aren't you afraid of bugs? Girls don't really seem toÅ^like them.")
	hitretResult := Hitret(19454)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020371")
	Mess("Huh? I like watching them, but I'm a little afraid ofÅ^touching them. But, if it's a frog or something, I canÅ^touch it.")
	hitretResult := Hitret(19455)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, I'm bad with frogs with them looking as if theyÅ^would collapse.")
	hitretResult := Hitret(19456)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020372")
	Mess("It's interesting to watch those hopping on the top ofÅ^your hand.")
	hitretResult := Hitret(19457)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though she's a girl, she's not afraid of bugs,Å^she's kind of strange... is what I though back then.")
	hitretResult := Hitret(19458)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, being together with her was fun.")
	hitretResult := Hitret(19459)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Without swaggering, she was behaving naturally, so IÅ^could keep talking with her as if we were friends thatÅ^knew each other for ages. Even when she was silent, sheÅ^could communicate her feelings, such a mysterious girlÅ^she was.")
	hitretResult := Hitret(19460)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020373")
	Mess("Ah, just a little more... Be careful...")
	hitretResult := Hitret(19461)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... Ah!!")
	hitretResult := Hitret(19462)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Cicada noticed the net and flew away.")
	hitretResult := Hitret(19463)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, damn it!!")
	hitretResult := Hitret(19464)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020374")
	Mess("It's a shame, right? Just a bit more would be enough.")
	hitretResult := Hitret(19465)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's frustrating... Even though it was a tenth one...")
	hitretResult := Hitret(19466)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020375")
	Mess("It's all right, shall we find next one?")
	hitretResult := Hitret(19467)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wonder around how much time has passed already. WeÅ^already travelled quite a distance up that mountain onÅ^the back of the shrine.")
	hitretResult := Hitret(19468)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, found it! On that tree over there!")
	hitretResult := Hitret(19469)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020376")
	Mess("I wonder if you can reach it...")
	hitretResult := Hitret(19470)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's alright, I'm telling you! Nnngh...")
	hitretResult := Hitret(19471)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020377")
	Mess("Ah... It might be dangerous... Wait a second...")
	hitretResult := Hitret(19472)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Just a little more... Nnnngh...")
	hitretResult := Hitret(19473)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("On the slope, I was reaching out the net with anÅ^unbelieveable posture.")
	hitretResult := Hitret(19474)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A cicada just like the one that ran away before wasÅ^ringing on that tree.")
	hitretResult := Hitret(19475)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kuh... Just a bit more......")
	hitretResult := Hitret(19476)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was reaching out the net while standing on tiptoes.")
	hitretResult := Hitret(19477)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020378")
	Mess("Maybe it's better to give up? Shall we look for anotherÅ^one?")
	hitretResult := Hitret(19478)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While holding my hand, trying to support me, she said soÅ^worriedly.")
	hitretResult := Hitret(19479)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE301", 0)
	Quake(16, 0, 1, 2, 30)
	Talk("Haruka", "")
	Mess("... Wait... I'll reach it........ Ah!?")
	hitretResult := Hitret(19480)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Suddenly, surface under my feet has collapsed.")
	hitretResult := Hitret(19481)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uwah!?")
	hitretResult := Hitret(19482)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020379")
	Mess("Hya!?")
	hitretResult := Hitret(19483)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After losing my balance, I fell down the slope just likeÅ^that.")
	hitretResult := Hitret(19484)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That steep slope we climbed up with so many hardships.Å^Trees were growing here thickly, so if we hit them, itÅ^wouldn't end up with just few bump with this height.")
	hitretResult := Hitret(19485)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The time was passing slowly, as if in slow motion, but IÅ^wasn't able to do anything.")
	hitretResult := Hitret(19486)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If we would fall just like that, would we die? AnÅ^overbearing fear ran through my whole body. I closed myÅ^eyes without thinking.")
	hitretResult := Hitret(19487)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Thudding... While beeing jostled, I was falling down.Å^Mysteriously, I didn't feel any pain, my consciousnessÅ^was fading. \"Ah, so this is death\" was the last thing IÅ^though.")
	hitretResult := Hitret(19488)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide(1)
	ScPlaySe("se018", 0)
	ScWait(3000, 0)
	Talk("VoiceÅsâlÅt", "AK020380")
	Mess("........ Haru........")
	hitretResult := Hitret(19489)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess(".....................................................")
	hitretResult := Hitret(19490)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("VoiceÅsâlÅt", "AK020381")
	Mess("........ Haru-kun........")
	hitretResult := Hitret(19491)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nghh........ Ouch... it hurts........")
	hitretResult := Hitret(19492)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("VoiceÅsâlÅt", "AK020382")
	Mess("Are you okay?.... Does it hurt anywhere?")
	hitretResult := Hitret(19493)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Somehow, it seems I'm scraped........")
	hitretResult := Hitret(19494)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("VoiceÅsâlÅt", "AK020383")
	Mess("........ Besides that, you're not injured anywhere?")
	hitretResult := Hitret(19495)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah........ You are...")
	hitretResult := Hitret(19496)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM08", 0)
	SetCg("EC01b", 0, 0)
	Transition("CLOUD_A", 2000)
	Talk("Akira", "AK020384")
	Mess("...I am... okay...... Nghh......")
	hitretResult := Hitret(19497)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Erm... your legs... what happened!? Could it be........")
	hitretResult := Hitret(19498)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EC01a", 0, 0)
	Transition("MOZCIR", 500)
	Talk("Akira", "AK020385")
	Mess("No, it just hurts a little........")
	hitretResult := Hitret(19499)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Being injured, we were defeated by the slope ofÅ^mountain.")
	hitretResult := Hitret(19500)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't know from how much we have fallen, but ourÅ^bodies were all covered in mud and full of scratches.")
	hitretResult := Hitret(19501)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020386")
	Mess("I'm sorry, Haru-kun... If I had more reliably supportedÅ^you...")
	hitretResult := Hitret(19502)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Not at all........ I was overdoing it, and it ended upÅ^in such a...")
	hitretResult := Hitret(19503)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I approached her, I saw Akira holding her leg. MaybeÅ^she twisted it when we were falling, it looked painful.")
	hitretResult := Hitret(19504)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Did you break it?")
	hitretResult := Hitret(19505)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020387")
	Mess("It's all right, I'll be able to walk if I rest for aÅ^while. More importantly, we need to check if Haru-kunÅ^wasn't injured... Does it hurt anywhere?")
	hitretResult := Hitret(19506)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Looking worried, she was gently brushing over myÅ^bruises. There wasn't any place I was hit in, I reallyÅ^was just grazed, nothing really happened.")
	hitretResult := Hitret(19507)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020388")
	Mess("Cicadas, even though we were catching them with suchÅ^troubles, they all ran away, right?")
	hitretResult := Hitret(19508)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("........ Eh?........")
	hitretResult := Hitret(19509)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The insect cage, that rolled down nearby, had brokenÅ^lid. All cicadas that have been inside were gone now.")
	hitretResult := Hitret(19510)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't mind that... For now, your injury is moreÅ^important......")
	hitretResult := Hitret(19511)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020389")
	Mess("I'm fine already.")
	hitretResult := Hitret(19512)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's a lie, it hurts so much, right?")
	hitretResult := Hitret(19513)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020390")
	Mess("... Haru-kun...")
	hitretResult := Hitret(19514)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I wonder where we are... Would it be good to go callÅ^somebody?")
	hitretResult := Hitret(19515)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020391")
	Mess("If we descend a bit from here, we'll get at the back ofÅ^shrine office. That's why, I'll be able to walk down.")
	hitretResult := Hitret(19516)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then... I'll go call somebody.")
	hitretResult := Hitret(19517)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020392")
	Mess("...... No...... It's okay......")
	hitretResult := Hitret(19518)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But! You're injured, right!?")
	hitretResult := Hitret(19519)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020393")
	Mess("It'll become better soon. If I'll rest just a littleÅ^more, I'll be able to walk.")
	hitretResult := Hitret(19520)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("........................")
	hitretResult := Hitret(19521)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Saying so, Akira has smiled brightly, but there wereÅ^drops of sweat dripping down her cheek.")
	hitretResult := Hitret(19522)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020394")
	Mess("Sorry. Because I took you to a dangerous place, you wereÅ^injured, Haru-kun...")
	hitretResult := Hitret(19523)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's not it, it's my fault... Even though you saidÅ^it's dangerous, I was overdoing it by trying to catchÅ^that cicada... To the point of you being hurt...")
	hitretResult := Hitret(19524)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020395")
	Mess("After sensei will give me the red tincture and put aÅ^bandage for me, it'll heal right away. That's why,Å^Haru-kun, you don't have to worry so much.")
	hitretResult := Hitret(19525)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But...")
	hitretResult := Hitret(19526)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was anxious and just about to burst into tears. I gotÅ^comforted while being carresed by the injured AkiraÅ^instead.")
	hitretResult := Hitret(19527)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020396")
	Mess("I'm really alright, so...")
	hitretResult := Hitret(19528)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sorry... I'm sorry... Sob...")
	hitretResult := Hitret(19529)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020397")
	Mess("Don't, I'm telling you it's okay...")
	hitretResult := Hitret(19530)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was gently embraced by my head and, even though it wasÅ^embarrassing, I ended up crying.")
	hitretResult := Hitret(19531)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's when I noticed it.")
	hitretResult := Hitret(19532)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The pendant that was hanging over Akira's breasts,Å^disappeared before we even noticed it.")
	hitretResult := Hitret(19533)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Huh... Your pendant...? What happened? Don't tell meÅ^it...")
	hitretResult := Hitret(19534)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020398")
	Mess("... Eh?... Yes... Don't mind that.")
	hitretResult := Hitret(19535)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Huh, but wasn't it an important memento?")
	hitretResult := Hitret(19536)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020399")
	Mess("It's okay... Well then, shall we go down?")
	hitretResult := Hitret(19537)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But... We have to look for it......")
	hitretResult := Hitret(19538)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020400")
	Mess("No... Now we have to heal Haru-kun's injuries.")
	hitretResult := Hitret(19539)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira was about to stand up, staggering, so I hurriedlyÅ^supported her with my shoulder.")
	hitretResult := Hitret(19540)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We have to search for that important pendant, but first,Å^I need to take Akira, with her twisted her leg.")
	hitretResult := Hitret(19541)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I though, the pendant was really gone.")
	hitretResult := Hitret(19542)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("While I was supporting Akira, we slowly went down theÅ^mountain trail. We took care not to fall down again likeÅ^before.")
	hitretResult := Hitret(19543)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It took a lot of time, but when we came to the back ofÅ^shrine office, Akira's grandpa happened to see us. AfterÅ^that, it became quite an uproar.")
	hitretResult := Hitret(19544)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Because of how terrible we looked, my grandfather wasÅ^called in a hurry. It turned out that we weren't hurtÅ^badly and it really ended up with red tincture andÅ^bandage.")
	hitretResult := Hitret(19545)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Grandpa was really mad at me for letting Akira get hurt,Å^but her grandfather forgave me with a smile.")
	hitretResult := Hitret(19546)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("And, because of all that uproar, without, even once,Å^being able to check what happened with Akira's pendant,Å^I had to return from this town.")
	hitretResult := Hitret(19547)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B34a", 400, 0)
	SetBustup("CC01_01M", 0, 0)
	Transition("WIP_MOZV", 500)
	ScPlayBgm("BGM05", 0)
	Talk("Haruka", "")
	Mess("Now that you mention it... There was one thing thatÅ^bothered me...")
	hitretResult := Hitret(19548)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020401")
	Mess("Hmm?")
	hitretResult := Hitret(19549)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So, in the end, what happened with that pendant ofÅ^yours? Because of all that noise, we weren't able toÅ^search for it...")
	hitretResult := Hitret(19550)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_05M", 0, 0)
	Talk("Akira", "AK020402")
	Mess("It couldn't be helped, because we were both injured andÅ^got in trouble.")
	hitretResult := Hitret(19551)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That belonged to your mother, right? And you lost thatÅ^because of me...")
	hitretResult := Hitret(19552)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_07M", 0, 0)
	Talk("Akira", "AK020403")
	Mess("Not at all, it's not Haru-kun's fault. So, don't worryÅ^about it.")
	hitretResult := Hitret(19553)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But... Even though it might be the key to find yourÅ^mother, Amatsume...")
	hitretResult := Hitret(19554)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If I went back to search for it back then... MaybeÅ^Amatsume's situation would have changed.")
	hitretResult := Hitret(19555)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's too late now, but... But, if I had just made effortÅ^to search for it...")
	hitretResult := Hitret(19556)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK020404")
	Mess("I'm telling you it's okay, I'm a scatterbrain anyway, soÅ^maybe I would have lost it some other way.")
	hitretResult := Hitret(19557)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020405")
	Mess("Even if I don't have it, Kazu-chan's father knows aboutÅ^my mother, so it's not a thing to be troubled with.")
	hitretResult := Hitret(19558)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Sorry...")
	hitretResult := Hitret(19559)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_07M", 0, 0)
	Talk("Akira", "AK020406")
	Mess("No... Me too, I'm sorry for making you feel responsibleÅ^for it, Haru-kun.")
	hitretResult := Hitret(19560)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_11M", 0, 0)
	Talk("Akira", "AK020407")
	Mess("But, Haru-kun, thank you for always worrying about it...Å^Thank you for always remembering it, really.")
	hitretResult := Hitret(19561)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Amatsume...")
	hitretResult := Hitret(19562)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020408")
	Mess("It might be that, because of what happened back then, IÅ^am able to meet you again now.")
	hitretResult := Hitret(19563)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020409")
	Mess("If you think like that, it those weren't only sorrowfulÅ^happenings.")
	hitretResult := Hitret(19564)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020410")
	Mess("That's why... Haru-kun... let's get along like we haveÅ^until now, okay?")
	hitretResult := Hitret(19565)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay... got it.")
	hitretResult := Hitret(19566)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Amatsume looked into my eyes and smiled brightly.")
	hitretResult := Hitret(19567)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Thank you...")
	hitretResult := Hitret(19568)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume, from the first time we meet, was alwaysÅ^thinking about me.")
	hitretResult := Hitret(19569)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That hasn't changed, even though we reunited after soÅ^much time has passed.")
	hitretResult := Hitret(19570)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her likeable smile really did stay like that since thatÅ^time.")
	hitretResult := Hitret(19571)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("", 0)
	Change("00_c010")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

