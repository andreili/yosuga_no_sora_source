Include"yosuga.ch"

Function sc00_D033()
	ScenarioEnter()
	SetCg("B27a", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Since that incident at the shrine, Kazuha's face hasÅ^always been clouded.")
	hitretResult := Hitret(32156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Four days passed, and even though I saw her countlessÅ^times since then, she was always depressed.")
	hitretResult := Hitret(32157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And one time, I saw her with Amatsume.")
	hitretResult := Hitret(32158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And after I saw them together, I got a call from Kazuha.")
	hitretResult := Hitret(32159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM03", 0)
	SetCg("B39a", 0, 0)
	SetBustup("CD02_03M", 0, 0)
	Talk("Kazuha", "KA041249")
	Mess("Hehe, it's been a while since I came here with you.")
	hitretResult := Hitret(32160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("\"A long time?\" It's barely been half a month since lastÅ^time.")
	hitretResult := Hitret(32161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041250")
	Mess("Oh, is that right? For some reason, it feels like it wasÅ^something that happened ages ago.")
	hitretResult := Hitret(32162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("\"Let's go out. Just me and you.\"")
	hitretResult := Hitret(32163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ever since she invited me yesterday, Kazuha has had aÅ^smile on her face.")
	hitretResult := Hitret(32164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It must be because summer vacation is almost over. Let'sÅ^make the most of today.")
	hitretResult := Hitret(32165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Without hesitating, I agreed, and we're currently in theÅ^next town.")
	hitretResult := Hitret(32166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We waited at Homi station, and came here by train.")
	hitretResult := Hitret(32167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There was no car and no secretary driving one. AmatsumeÅ^and everyone else weren't here, either. It really wasÅ^just the two of us.")
	hitretResult := Hitret(32168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041251")
	Mess("I don't mind going to the shops we couldn't get to lastÅ^time, but exploring some of the new shops would be fineÅ^too.")
	hitretResult := Hitret(32169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Since we're here anyway, how about a movie? They mightÅ^be showing something new.")
	hitretResult := Hitret(32170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041252")
	Mess("Okay... Ah, it looks like a movie's just about to start.")
	hitretResult := Hitret(32171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hey you're right...\"Black Wall...\" I think it's one thatÅ^just started screening.")
	hitretResult := Hitret(32172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041253")
	Mess("Okay, let's watch this, then.Å^Let's go Haruka! It'll start without us!")
	hitretResult := Hitret(32173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("R-Roger! Two adults, and please make it quick!")
	hitretResult := Hitret(32174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B39a", 0, 0)
	SetBustup("CD02_01M", 0, 0)
	Talk("Haruka", "")
	Mess("*Sigh* That was interesting, with people flyingÅ^everywhere like that. It was so flashy.")
	hitretResult := Hitret(32175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041254")
	Mess("......")
	hitretResult := Hitret(32176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I think they call that \"Wire fu?\"Å^...Hey, Kazuha?")
	hitretResult := Hitret(32177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041255")
	Mess("........")
	hitretResult := Hitret(32178)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ever since we came out of the cinema, she looks veryÅ^distracted.")
	hitretResult := Hitret(32179)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... I guess it didn't really suit your tastes...? ItÅ^was an action film, after all...")
	hitretResult := Hitret(32180)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("We rushed in there without thinking, but perhaps itÅ^might've been better if we had chosen somethingÅ^properly... Haha...")
	hitretResult := Hitret(32181)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While the movie was showing, she had made smallÅ^\"amazing\" and \"wow\" noises, so it looked like sheÅ^enjoyed it.")
	hitretResult := Hitret(32182)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_12M", 0, 0)
	Talk("Kazuha", "KA041256")
	Mess("Ah... Eh, did you say something?")
	hitretResult := Hitret(32183)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nothing much, just that I'm sorry that the movie wasÅ^something that only I enjoyed.")
	hitretResult := Hitret(32184)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041257")
	Mess("Why just you? I liked the movie too, you know?")
	hitretResult := Hitret(32185)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Really? If that's the case, that's fine, but...")
	hitretResult := Hitret(32186)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If that's what she really thinks, then why was she allÅ^spaced out...?")
	hitretResult := Hitret(32187)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_03M", 0, 0)
	Talk("Kazuha", "KA041258")
	Mess("Hey, I'm a bit thirsty. Let's go to Seattle.")
	hitretResult := Hitret(32188)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Did you want to try out a new topping?")
	hitretResult := Hitret(32189)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041259")
	Mess("Order a double shot, right? I made sure to remember it.Å^I'm looking forward to seeing how it tastes.")
	hitretResult := Hitret(32190)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide()
	SetCg("BLACK", 0, 0)
	Transition("WIP_LR", 500)
	Update()
	SetCg("B39a", 0, 0)
	SetBustup("CD02_12M", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Kazuha", "KA041260")
	Mess("Wha, it really did get more bitter.")
	hitretResult := Hitret(32191)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA041261")
	Mess("But it's a good feeling. Since it was sweet to start outÅ^with...")
	hitretResult := Hitret(32192)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There's normal coffee and espresso too, so how aboutÅ^trying one of those next time?")
	hitretResult := Hitret(32193)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041262")
	Mess("Yeah... There's espresso too, isn't there?")
	hitretResult := Hitret(32194)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA041263")
	Mess("...Next time...")
	hitretResult := Hitret(32195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ah... She's doing it again...")
	hitretResult := Hitret(32196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just like when we walked out of the cinema, herÅ^expression is going blank.")
	hitretResult := Hitret(32197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just a short while ago, she was smiling. She even lookedÅ^like she was happier and having more fun than she did onÅ^our last date.")
	hitretResult := Hitret(32198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041264")
	Mess("........")
	hitretResult := Hitret(32199)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A blank expression, as if time itself had stopped.")
	hitretResult := Hitret(32200)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kazuha? Is something wrong?")
	hitretResult := Hitret(32201)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_03M", 0, 0)
	Talk("Kazuha", "KA041265")
	Mess("Eh?... Ah, no, it's nothing. I'm sorry.Å^Come on, where should we go next?")
	hitretResult := Hitret(32202)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("After this, Kazuha repeated switched between gettingÅ^excited and suddenly going quiet like just then.")
	hitretResult := Hitret(32203)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And then, I somehow worked it out.")
	hitretResult := Hitret(32204)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That Kazuha still hadn't broken through, nor had sheÅ^been able to sort anything out.")
	hitretResult := Hitret(32205)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B39b", 0, 0)
	SetBustup("CD02_03M", 0, 0)
	Transition("WIP_TLBR", 500)
	Talk("Kazuha", "KA041266")
	Mess("Today was reallllly fun.")
	hitretResult := Hitret(32206)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She sat down on a bench, sighing as if to say that sheÅ^was a bit tired.")
	hitretResult := Hitret(32207)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I answered her with a bitter smile, sat down next toÅ^her, and then took out my cell phone to check when theÅ^return train would arrive.")
	hitretResult := Hitret(32208)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There's aren't many trains that stop at our station.")
	hitretResult := Hitret(32209)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The sky had only begun to get red, but if we don't leaveÅ^soon,, it'll be night once we get home.")
	hitretResult := Hitret(32210)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...There's still about ten minutes until the next train.Å^After that, there doesn't seem to be one for anotherÅ^hour.")
	hitretResult := Hitret(32211)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA041267")
	Mess("I see...")
	hitretResult := Hitret(32212)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She nodded, her face a bit stiff.")
	hitretResult := Hitret(32213)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Was that reluctance toward finishing our date, orÅ^because of something else she was worried about...?")
	hitretResult := Hitret(32214)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Do you want to stay here for a bit longer? We can eatÅ^dinner here, if you want.")
	hitretResult := Hitret(32215)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Kazuha", "KA041268")
	Mess("...No, Kasugano-kun... Can you go home please?")
	hitretResult := Hitret(32216)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm? Me? If you have something to do here, then I'llÅ^come along too.")
	hitretResult := Hitret(32217)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041269")
	Mess("...My relationship with you, Kasugano-kun, will have toÅ^end today.")
	hitretResult := Hitret(32218)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...I suddenly noticed.")
	hitretResult := Hitret(32219)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kazuha wasn't using my name, but my surname.")
	hitretResult := Hitret(32220)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM10", 0)
	Talk("Kazuha", "KA041270")
	Mess("Today, we have to break up.")
	hitretResult := Hitret(32221)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was strange, but after hearing those words, I beganÅ^to understand.")
	hitretResult := Hitret(32222)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That face, those eyes. She was thinking about this theÅ^entire time.")
	hitretResult := Hitret(32223)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And at the same time, I also felt abandoned.")
	hitretResult := Hitret(32224)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Being hit by the meaning of those words, the thought ofÅ^not wanting to accept them started to grow within me.")
	hitretResult := Hitret(32225)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("W...Why...?")
	hitretResult := Hitret(32226)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My voice went hoarse as I replied.")
	hitretResult := Hitret(32227)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041271")
	Mess("I don't...have a reason...")
	hitretResult := Hitret(32228)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There can't...not be one, right?...Up until now, we'veÅ^had this much fun...")
	hitretResult := Hitret(32229)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA041272")
	Mess("......")
	hitretResult := Hitret(32230)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She suddenly dropped her gaze.")
	hitretResult := Hitret(32231)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Seeing that expression, I realized that I had touchedÅ^somewhere that hurt, and instantly regretted it.")
	hitretResult := Hitret(32232)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But the feeling of not wanting to be seperated fromÅ^Kazuha made me continue talking.")
	hitretResult := Hitret(32233)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's to do with Amatsume, right...?")
	hitretResult := Hitret(32234)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041273")
	Mess(".........That's right.")
	hitretResult := Hitret(32235)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I began hating myself for making her feel so bad thatÅ^she would make such a painful face.")
	hitretResult := Hitret(32236)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041274")
	Mess("From now on... I can only have Akira.")
	hitretResult := Hitret(32237)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I began to feel depressed for myself, who couldn't evenÅ^put a smile on the face of the one he loved.")
	hitretResult := Hitret(32238)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041275")
	Mess("I know I'm hurting you by dragging you around and sayingÅ^such selfish things.")
	hitretResult := Hitret(32239)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041276")
	Mess("But... I... can't go out with you anymore.")
	hitretResult := Hitret(32240)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her words showed hesitation, but she said them clearly.")
	hitretResult := Hitret(32241)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What could I possibly say in response to that...?")
	hitretResult := Hitret(32242)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kazuha...")
	hitretResult := Hitret(32243)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just saying her name took everything I had. I couldn'tÅ^say anything else.")
	hitretResult := Hitret(32244)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Maybe I should throw a tantrum like a child, screaming,Å^\"No! No!\"")
	hitretResult := Hitret(32245)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Maybe I should try to persuade her, and change her mind.")
	hitretResult := Hitret(32246)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But I couldn't do either of those.")
	hitretResult := Hitret(32247)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041277")
	Mess("I thought about it...for a long time...About you...andÅ^Akira...")
	hitretResult := Hitret(32248)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041278")
	Mess("Even hating...myself... for thinking...of such things...")
	hitretResult := Hitret(32249)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I watched Kazuha try to squeeze out everything, butÅ^could say nothing.")
	hitretResult := Hitret(32250)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_09M", 0, 0)
	Talk("Kazuha", "KA041279")
	Mess("I'm so sorry... Even though it's not something... that IÅ^can solve by apologizing...")
	hitretResult := Hitret(32251)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("The moment she turned and ran away, I saw her cryingÅ^face.")
	hitretResult := Hitret(32252)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though I was supposed to be the one who got dumped,Å^the one who was crying was Kazuha.")
	hitretResult := Hitret(32253)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide()
	BlackOut(0, 0)
	Change("00_d034")
	Return
	Change("EXIT_SCENARIO")
EndFunc

