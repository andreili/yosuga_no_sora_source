Include"yosuga.ch"

Function sc00_C010B()
	ScenarioEnter()
	Tone("MONOCHROME")
	SetCg("B31a", 0, 0)
	ScPlayBgm("BGM03", 0)
	Talk("êSÇÃê∫", "")
	Mess("When we were small, our parents were buying anÅ^unreasonable amounts of things and we kept living in anÅ^apartment.")
	hitretResult := Hitret(19602)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That new building was in a sunny and quiet place, it hadÅ^a nice view from it.")
	hitretResult := Hitret(19603)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Our parents' work was importing and selling houseÅ^interiors. Even though it was small, they owned a store,Å^in which they were selling furniture, that they ordered.")
	hitretResult := Hitret(19604)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In order to pay back the store's debts, they were alwaysÅ^busy working. But, after getting back home, it didn'tÅ^feel like it, because they were so carefree andÅ^cheerful.")
	hitretResult := Hitret(19605)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In summer holidays, they left us at grandfather's homeÅ^and went overseas to purchase furniture, but sinceÅ^grandfather has died, they left me and Sora at home,Å^anticipating souvenirs from them.")
	hitretResult := Hitret(19606)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And then...")
	hitretResult := Hitret(19607)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was domestic, but sometimes parents left us to takeÅ^care of the house for two, three days, as usual.")
	hitretResult := Hitret(19608)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Tone("")
	SetCg("B31a", 0, 0)
	Transition("CLOUD_A", 2000)
	Update()
	ScPlaySe("SE159", 0)
	Talk("Haruka", "")
	Mess("? Ah, okay, okay......")
	hitretResult := Hitret(19609)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopSe()
	Talk("Haruka", "")
	Mess("Hello? Kasugano here.")
	hitretResult := Hitret(19610)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Phone CallÅsóIÇÃïÍÅt", "NP030001")
	Mess("Ah, hello? I am... on my way back right now.")
	hitretResult := Hitret(19611)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Did you finish your work already? Thank you for yourÅ^hard work.")
	hitretResult := Hitret(19612)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka's mother", "NP030002")
	Mess("Sorry for letting you take care of various things whileÅ^you're staying at home. I got on highway right now, so IÅ^think I'll be back around evening.")
	hitretResult := Hitret(19613)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay, got it. Ah, can you buy the usual thing? I wasÅ^told to ask for this from Sora.")
	hitretResult := Hitret(19614)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka's mother", "NP030003")
	Mess("...The usual thing? Ah, the restricted to the serviceÅ^area snacks, right? Hmm, but, I already passed thatÅ^place.")
	hitretResult := Hitret(19615)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, is it so?... Well then, don't bother. Then, anythingÅ^suitable is okay. For Sora too, she said.")
	hitretResult := Hitret(19616)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka's mother", "NP030004")
	Mess("Umm... Eh...? Yeah, that one... the usual one... IÅ^understand. Well then, I'll buy something suitable then.")
	hitretResult := Hitret(19617)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka's mother", "NP030005")
	Mess("How is Sora doing? We'll be back late, so she's sulkingÅ^and shutting herself in her room?")
	hitretResult := Hitret(19618)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, moreover, because the meal I make isn't tasty,Å^she's in bad humor.")
	hitretResult := Hitret(19619)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka's mother", "NP030006")
	Mess("Oh Haru, next time, I should teach you how to cookÅ^properly, okay?")
	hitretResult := Hitret(19620)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It can't be helped, I'll ask you to teach me...Å^Following that, won't I become like a normal girl?")
	hitretResult := Hitret(19621)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka's mother", "NP030007")
	Mess("Not at all, do you know that boys that can cook areÅ^popular? So, it's not bad to learn it.")
	hitretResult := Hitret(19622)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eeeh, but when I'll learn it, Sora won't be making itÅ^anymore...")
	hitretResult := Hitret(19623)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka's mother", "NP030008")
	Mess("Ahaha, you're right. When I'll get back, I'll makeÅ^something tasty for you, so tell Sora to be patient andÅ^not eat too many snacks, okay?")
	hitretResult := Hitret(19624)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay, I'm looking forward to it then.")
	hitretResult := Hitret(19625)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka's mother", "NP030009")
	Mess("Well then, see you later.")
	hitretResult := Hitret(19626)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, be careful and come back.")
	hitretResult := Hitret(19627)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR020067")
	Mess("...... Was it from mama?")
	hitretResult := Hitret(19628)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, she told me that she should be back by evening.")
	hitretResult := Hitret(19629)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020068")
	Mess("...That's late.")
	hitretResult := Hitret(19630)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("She said that she will make a delicious supper, so waitÅ^without eating snacks.")
	hitretResult := Hitret(19631)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I was about to take Sora out of the kitchen, sheÅ^pointed at the bag of potato chips.")
	hitretResult := Hitret(19632)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04M", 0, 0)
	Talk("Sora", "SR020069")
	Mess("............")
	hitretResult := Hitret(19633)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I asked her for a suitable souvenir too.")
	hitretResult := Hitret(19634)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR020070")
	Mess("...The same snacks as always?")
	hitretResult := Hitret(19635)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It seems she already passed the place where it was sold,Å^so she will buy something else for us.")
	hitretResult := Hitret(19636)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04M", 0, 0)
	Talk("Sora", "SR020071")
	Mess("... Tch... It would be okay, if you had only told herÅ^that earlier...")
	hitretResult := Hitret(19637)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It couldn't be helped, they had their arrangements afterÅ^all.")
	hitretResult := Hitret(19638)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But, isn't it good that you finally won't need to eat myÅ^means for a while?")
	hitretResult := Hitret(19639)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR020072")
	Mess("...You're right.")
	hitretResult := Hitret(19640)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Saying so, mom told me that she will show how to cookÅ^something tasty.")
	hitretResult := Hitret(19641)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020073")
	Mess("...The one she'll be teaching is Haru, right?")
	hitretResult := Hitret(19642)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh?... How did you find out?")
	hitretResult := Hitret(19643)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020074")
	Mess("...... Who knows?")
	hitretResult := Hitret(19644)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Saying so, Sora took the bag of potato chips and wentÅ^back to her room.")
	hitretResult := Hitret(19645)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, I told you, put that back!")
	hitretResult := Hitret(19646)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_05M", 0, 0)
	Talk("Sora", "SR020075")
	Mess("...Nah.")
	hitretResult := Hitret(19647)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("As I was trying to take it from her, she slammed theÅ^door just before my nose.")
	hitretResult := Hitret(19648)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... I won't care if you'll get fat.")
	hitretResult := Hitret(19649)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se114", 0)
	Quake(16, 0, 0, 2, 30)
	Talk("Haruka", "")
	Mess("Uwah!?")
	hitretResult := Hitret(19650)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("...She cares about that, huh...")
	hitretResult := Hitret(19651)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(2000, 0)
	ScPlaySe("SE159", 0)
	Talk("Haruka", "")
	Mess("Ah, coming, coming...")
	hitretResult := Hitret(19652)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B31a", 0, 0)
	Transition("WIP_RL", 500)
	Talk("êSÇÃê∫", "")
	Mess("There's still some time until evening, but maybe she'llÅ^be back soon? Thinking so, I picked up the phone.")
	hitretResult := Hitret(19653)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hello, Kasugano here.")
	hitretResult := Hitret(19654)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Phone CallÅsåxé@Åt", "NP350001")
	Mess("Hello... Is this Kasugano's household? I am...")
	hitretResult := Hitret(19655)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I had a bad feeling.")
	hitretResult := Hitret(19656)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There was a stiff tone coming from the telephoneÅ^receiver.")
	hitretResult := Hitret(19657)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And the words following it were......")
	hitretResult := Hitret(19658)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM11", 0)
	Talk("Phone CallÅsåxé@Åt", "NP350002")
	Mess("The car you parents were in was caught up in anÅ^accident. I'm truly afraid to say, that the two of themÅ^have, while being conveyed to the hospital, have...")
	hitretResult := Hitret(19659)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_08M", 0, 0)
	Talk("Sora", "SR020076")
	Mess("... Haru... What happened?")
	hitretResult := Hitret(19660)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Phone CallÅsåxé@Åt", "NP350003")
	Mess("We had to confirm with someone from the family...Å^urgently...")
	hitretResult := Hitret(19661)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... Is that true? Isn't there some kind of mistake...")
	hitretResult := Hitret(19662)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Phone CallÅsåxé@Åt", "NP350004")
	Mess("...... It's painful, but please calm down.")
	hitretResult := Hitret(19663)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... It can't be......")
	hitretResult := Hitret(19664)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020077")
	Mess("... Hey... What happened?")
	hitretResult := Hitret(19665)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... Father, and mother had an... accident...")
	hitretResult := Hitret(19666)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_12M", 0, 0)
	Talk("Sora", "SR020078")
	Mess("...... Eh?......")
	hitretResult := Hitret(19667)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Phone CallÅsåxé@Åt", "NP350005")
	Mess("Hello?...... From the present moment, at the saidÅ^location...")
	hitretResult := Hitret(19668)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was writing down what I was told, but my hands wereÅ^trembling, so I couldn't write the characters well.")
	hitretResult := Hitret(19669)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was writing in hiragana, which looked like crawlingÅ^earthworms. After I finished writing, I hung up theÅ^phone.")
	hitretResult := Hitret(19670)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Sora... Let's go.")
	hitretResult := Hitret(19671)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_08M", 0, 0)
	Talk("Sora", "SR020079")
	Mess("...... Where to?")
	hitretResult := Hitret(19672)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Both of them were to the hospital, or so I heard.")
	hitretResult := Hitret(19673)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020080")
	Mess("...Are they injured? Are they safe?")
	hitretResult := Hitret(19674)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("..................")
	hitretResult := Hitret(19675)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020081")
	Mess("Lie... You're lying......")
	hitretResult := Hitret(19676)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_12M", 0, 0)
	Talk("Sora", "SR020082")
	Mess("Hey, it's a lie, right? Why are you lying?")
	hitretResult := Hitret(19677)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There's no way I would be joking... Over such aÅ^thing...... !!")
	hitretResult := Hitret(19678)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_08M", 0, 0)
	Talk("Sora", "SR020083")
	Mess("Kuh......")
	hitretResult := Hitret(19679)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sorry... Let's go, Sora... To where our parentsÅ^are......")
	hitretResult := Hitret(19680)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_07M", 0, 0)
	Talk("Sora", "SR020084")
	Mess("... No...")
	hitretResult := Hitret(19681)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What are you saying? If we won't go, who will?")
	hitretResult := Hitret(19682)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_12M", 0, 0)
	Talk("Sora", "SR020085")
	Mess("... No... Don't!! ... If we'll wait here, papa and mamaÅ^will come back...")
	hitretResult := Hitret(19683)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Sora...")
	hitretResult := Hitret(19684)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_08M", 0, 0)
	Talk("Sora", "SR020086")
	Mess("Some kind of lie... Hey, it must be a lie... Right,Å^Haru?")
	hitretResult := Hitret(19685)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I want to think that it's a lie too. I want to thinkÅ^that if we'll keep waiting, they will come back, sayingÅ^carefreely \"I'm sorry, we're late.\"... But...")
	hitretResult := Hitret(19686)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...I'm going to see them. You stay here, Sora. I'm goingÅ^to call our relatives, so...")
	hitretResult := Hitret(19687)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_12M", 0, 0)
	Talk("Sora", "SR020087")
	Mess("...... Haru... Is that true? Both papa and mama...")
	hitretResult := Hitret(19688)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... Yes.")
	hitretResult := Hitret(19689)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_07L", 0, 0)
	Talk("Sora", "SR020088")
	Mess("Papa... Mama... Why... Whaaaaa...")
	hitretResult := Hitret(19690)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("........................")
	hitretResult := Hitret(19691)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Sora was crying, clinging to me.")
	hitretResult := Hitret(19692)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("... Father... Mother... They seemed lively until just aÅ^short time ago, so why...")
	hitretResult := Hitret(19693)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though I said \"Please be careful and come back,Å^okay?\"...")
	hitretResult := Hitret(19694)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was unreasonable, but even right this moment, theÅ^urge to let out a cry was overcoming my whole body.")
	hitretResult := Hitret(19695)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020089")
	Mess("Kuh... Hii... Whaaaaa...")
	hitretResult := Hitret(19696)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's crying voice was piercing my heart. But, if I'llÅ^start crying too, we'll be unable to move... Even now,Å^strength was leaving my body.")
	hitretResult := Hitret(19697)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I embraced Sora tightly. When I was somehow suppressingÅ^the screaming about their death in my heart, theÅ^strength started leaving me once again.")
	hitretResult := Hitret(19698)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...I'm going... Sora...")
	hitretResult := Hitret(19699)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("After I'll meet them, I'll take them home.")
	hitretResult := Hitret(19700)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Clenching my teeth, I could only think out that much,Å^killing my feelings.")
	hitretResult := Hitret(19701)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Hide(1)
	ScWait(2000, 0)
	SetCg("B31c", 0, 0)
	Transition("WIP_LR", 500)
	ScPlayBgm("BGM10", 0)
	Talk("êSÇÃê∫", "")
	Mess("At the time of the funeral, the room people wereÅ^continuously visiting, became now only mine and Sora's.")
	hitretResult := Hitret(19702)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Thanks to relatives and friends helping in variousÅ^things, dealing with formalities afterwards went withoutÅ^trouble.")
	hitretResult := Hitret(19703)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Once every seven days, people came to pay theirÅ^respects, meanwhile giving us condolences. While havingÅ^such a company, that time passed in a blink of eye.")
	hitretResult := Hitret(19704)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The new school we were to commute to, ended withÅ^delivering the notice of absence before we even went toÅ^the opening ceremony.")
	hitretResult := Hitret(19705)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wanted only Sora to commute, but since the funeral,Å^her shutting herself in her room became even worse. SheÅ^didn't go out of the room at all.")
	hitretResult := Hitret(19706)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Sigh...")
	hitretResult := Hitret(19707)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The cause for this sigh were documents spread out on theÅ^table, next to a cup of cold coffee.")
	hitretResult := Hitret(19708)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Those are documents about father's shop, from bank aboutÅ^the loan, demand for payment for ordered furniture andÅ^so on. All of those were things I didn't have clueÅ^about.")
	hitretResult := Hitret(19709)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It seems that we'll be able to repay the debt,Å^consisting of our sales and payments, but the problem isÅ^how to maintain the store from now on.")
	hitretResult := Hitret(19710)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The store is small, but it has its employees. We wouldÅ^have to pay them salaries and, similarly to our parents,Å^travel overseas to purchase furniture. For me, having noÅ^knowledge about it, it's impossible, no matter what.")
	hitretResult := Hitret(19711)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Father's acquaitance from the same business told us,Å^that if we would like to close the shop, he could takeÅ^care of the inventory.")
	hitretResult := Hitret(19712)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Everyone was telling us that, no matter what we'll do,Å^we won't be able to run it. I understood that much, so IÅ^was thinking about closing the store.")
	hitretResult := Hitret(19713)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, it's not only the store. Problem with thisÅ^apartment has arised too.")
	hitretResult := Hitret(19714)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("For us two to be living together from now on, we'll needÅ^money no matter what.")
	hitretResult := Hitret(19715)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("For now we're somehow okay with the money fromÅ^insurance, but even if I were to start working from nowÅ^on, I'd have my hands full just trying to maintainÅ^current situation.")
	hitretResult := Hitret(19716)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I can't let Sora to be dragged into such an insecureÅ^livelihood.")
	hitretResult := Hitret(19717)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There are also our relatives, that said that they couldÅ^take care of us. So either so that or rent a cheapÅ^apartment...")
	hitretResult := Hitret(19718)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Without any solutions that I could comprehend, thereÅ^were lots of problems I was worrying about.")
	hitretResult := Hitret(19719)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("........................")
	hitretResult := Hitret(19720)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was sitting here, feeling strange.")
	hitretResult := Hitret(19721)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt as if I had a call from our parents, who were nowÅ^in the middle of their business trip, that they wouldÅ^come back soon.")
	hitretResult := Hitret(19722)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, that is impossible, not anymore.")
	hitretResult := Hitret(19723)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There are lots of memories of our parents here. NoÅ^matter what would I look at, the image of them two wouldÅ^pop out in my head.")
	hitretResult := Hitret(19724)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's why, I want to stay like that forever.")
	hitretResult := Hitret(19725)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But... it would be too hard on me.")
	hitretResult := Hitret(19726)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Store management, maintenance of this apartment too...")
	hitretResult := Hitret(19727)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I took the accident report, buried under a mountain ofÅ^documents, in my hands.")
	hitretResult := Hitret(19728)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I didn't scan it even once, but I bet I won't be able toÅ^forget what was written here for my whole life.")
	hitretResult := Hitret(19729)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Our parents passed away in a collision with a truck. AndÅ^after seeing the scene of the accident with my ownÅ^eyes... I...")
	hitretResult := Hitret(19730)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After our parents made the call, they took U-turnÅ^especially to go buy presents for us... And then, theyÅ^were dragged into the accident.")
	hitretResult := Hitret(19731)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If I hadn't said such unnecessary things back then...Å^Our parents would be...")
	hitretResult := Hitret(19732)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The one that caused their accident was... me... At thatÅ^time, I took away their lives with just one word.")
	hitretResult := Hitret(19733)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Everyone was comforting me that I'm wrong, but IÅ^couldn't forgive myself.")
	hitretResult := Hitret(19734)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Tone("MONOCHROME")
	Talk("êSÇÃê∫", "")
	Mess("After that, I made a decision... We will move to ourÅ^father's parents' home.")
	hitretResult := Hitret(19735)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...... Taking Sora with me, running away.")
	hitretResult := Hitret(19737)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(0, 1)
	Tone("")
	ScWait(2000, 0)
	SetCg("B02c", 0, 0)
	Transition("WIP_RL", 500)
	Talk("êSÇÃê∫", "")
	Mess("I finished cleaning up by putting the last plate on theÅ^drainer.")
	hitretResult := Hitret(19738)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020090")
	Mess("Haru, you can do whatever you want...")
	hitretResult := Hitret(19739)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(19740)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I looked back, Sora wasn't there anymore.")
	hitretResult := Hitret(19741)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After I decided to move in, when I was about to disposeÅ^of our parents' luggage, Sora resisted ferociously.")
	hitretResult := Hitret(19742)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But... In the end, she said that I could do whatever IÅ^wanted.")
	hitretResult := Hitret(19743)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The only family I have left... I don't want to lose SoraÅ^too.")
	hitretResult := Hitret(19744)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The things I can do extend to about that much. ThisÅ^isn't enough to atone, but... I pledged that I wouldÅ^accomplish at least that.")
	hitretResult := Hitret(19745)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("DATE", 0)
	Change("00_c011")
	Return
	Change("EXIT_SCENARIO")
EndFunc

