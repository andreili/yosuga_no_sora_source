Include"yosuga.ch"

Function sc00_A023A()
	ScenarioEnter()
	ScPlaySe("SE357", 0)
	SetCg("B03a", 0, 0)
	Transition("WIP_MOZV", 500)
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(7284)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The faint light I felt from the other side of my eyelidsÅ^woke me up.")
	hitretResult := Hitret(7285)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM19", 0)
	Talk("êSÇÃê∫", "")
	Mess("It was a usual morning... or so it should have been, butÅ^for some reason it felt like I was in a different world.")
	hitretResult := Hitret(7286)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Unlike usual, I feel like I'm not wearing anythingÅ^inside the futon.")
	hitretResult := Hitret(7287)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Though my brain was still half-asleep, the reality ofÅ^last night still faintly remained inside it, as if I hadÅ^been watching a dream.")
	hitretResult := Hitret(7288)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And, with the two of us sleeping in the same futon... IÅ^immediately felt beside me, Sora's warmth.")
	hitretResult := Hitret(7289)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Everything had come together. The fulfillment of thatÅ^gave me a light, fluffy senation.")
	hitretResult := Hitret(7290)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011004")
	Mess("Nnhuu... You're finally up.")
	hitretResult := Hitret(7291)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Ah, yeah... Good morning, Sora.")
	hitretResult := Hitret(7292)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I finished opening my eyelids, before my eyes, SoraÅ^was there, quietly watching me.")
	hitretResult := Hitret(7293)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wonder if she's been watching me the entire time...")
	hitretResult := Hitret(7294)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Now, there's nothing left to be embarrassed about,Å^but... it still gave me kind of a ticklish feeling, so IÅ^tried to get up.")
	hitretResult := Hitret(7295)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011005")
	Mess("...It's okay, you can stay in bed.")
	hitretResult := Hitret(7296)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("However, Sora grabbed my arm... and brought me back toÅ^the futon.")
	hitretResult := Hitret(7297)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se063", 0)
	Talk("Sora", "SR011006")
	Mess("...Nfufu......")
	hitretResult := Hitret(7298)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora rubbed her checks against mine, purring like a cat.")
	hitretResult := Hitret(7299)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I went to softly hug her arm, it was accompanied byÅ^a tight embrace.")
	hitretResult := Hitret(7300)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And then...")
	hitretResult := Hitret(7301)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011007")
	Mess("Good morning...")
	hitretResult := Hitret(7302)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Close to my ear... was a whisper.")
	hitretResult := Hitret(7303)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1000, 0)
	SetCg("B02a", 0, 0)
	SetBustup("CA03_02L", 0, 0)
	Talk("Sora", "SR011008")
	Mess("Haru, get the eggs out.")
	hitretResult := Hitret(7304)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, okay.")
	hitretResult := Hitret(7305)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With surprising familiarity, Sora cracked the eggs I hadÅ^carried to her, and made fried eggs with her right handÅ^holding the square frying pan.")
	hitretResult := Hitret(7306)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se069", 0)
	Talk("Sora", "SR011009")
	Mess("Grrr...")
	hitretResult := Hitret(7307)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Though she had a little difficulty, the contents of theÅ^frying pain cannot be compared to a while ago.")
	hitretResult := Hitret(7308)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Looks good...")
	hitretResult := Hitret(7309)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_03L", 0, 0)
	Talk("Sora", "SR011010")
	Mess("Ehe... I'm become good at this, haven't I...")
	hitretResult := Hitret(7310)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora triumphantly puffed out her chest when she saw theÅ^finished product.")
	hitretResult := Hitret(7311)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yep, maybe I'll leave it to you from now on...")
	hitretResult := Hitret(7312)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011011")
	Mess("Well... I'll do my best, but for cooking... doing itÅ^together is better.")
	hitretResult := Hitret(7313)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Yep, that's true.")
	hitretResult := Hitret(7314)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We started cooking the other food while splitting up theÅ^tasks.")
	hitretResult := Hitret(7315)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's as if we're newly-weds.")
	hitretResult := Hitret(7316)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...That's the only example I could come up with, becauseÅ^I don't recall ever cooking food together with SoraÅ^before.")
	hitretResult := Hitret(7317)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And so, faster than one person could cook, we finishedÅ^making breakfast.")
	hitretResult := Hitret(7318)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well then, shall we eat?")
	hitretResult := Hitret(7319)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_02M", 0, 0)
	Talk("Sora", "SR011012")
	Mess("Yeah, let's dig in.")
	hitretResult := Hitret(7320)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora took a slice of fried egg, and put it in her mou...Å^No, it was headed in my direction.")
	hitretResult := Hitret(7321)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011013")
	Mess("Haru, want to try it? Aaahhn?")
	hitretResult := Hitret(7322)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, yeah... Nmmunmmu...")
	hitretResult := Hitret(7323)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was a little embarrassing, but I let her feed me likeÅ^that.")
	hitretResult := Hitret(7324)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nnn... I have a feeling it's lightly seasoned, but it'sÅ^about the level of my tastes...")
	hitretResult := Hitret(7325)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011014")
	Mess("Really? I'm glad. Next time I'll make it better.")
	hitretResult := Hitret(7326)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011015")
	Mess("Well, me too... MmmÅ`?")
	hitretResult := Hitret(7327)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When Sora closed her eyes, I pushed it into her slightlyÅ^open mouth.")
	hitretResult := Hitret(7328)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Yes, yes.")
	hitretResult := Hitret(7329)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011016")
	Mess("Nnmm...Nn....")
	hitretResult := Hitret(7330)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_03M", 0, 0)
	Talk("Sora", "SR011017")
	Mess("Delicious~. Here, Haru, me too.")
	hitretResult := Hitret(7331)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After gladly eating the portion of food I held out, sheÅ^held out food to me again, this time from a differentÅ^side-dish.")
	hitretResult := Hitret(7332)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I--I can feed myself.")
	hitretResult := Hitret(7333)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011018")
	Mess("Too bad. Here, Haru?")
	hitretResult := Hitret(7334)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("O--Okay... Nnmmu....")
	hitretResult := Hitret(7335)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011019")
	Mess("Ah, it spilled... Fufu... You're like a child.")
	hitretResult := Hitret(7336)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Eh...")
	hitretResult := Hitret(7337)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_02L", 0, 0)
	Talk("Sora", "SR011020")
	Mess("Don't move... Nn... *chuu*...")
	hitretResult := Hitret(7338)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora bent over forwards, approaching my mouth... HerÅ^tongue creeped onto my mouth.")
	hitretResult := Hitret(7339)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The soft, warm tip of her tongue began to lick it off.")
	hitretResult := Hitret(7340)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011021")
	Mess("Nnh... Good flavor...")
	hitretResult := Hitret(7341)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Th--That was a surprise......")
	hitretResult := Hitret(7342)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011022")
	Mess("Fufufu...")
	hitretResult := Hitret(7343)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("It really looked like she thought it was delicious...Å^but, for some reason, her eyes looked a little dim.")
	hitretResult := Hitret(7344)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1000, 0)
	SetCg("B15a", 0, 0)
	Transition("", 2000)
	ScPlayBgm("BGM03", 0)
	Talk("êSÇÃê∫", "")
	Mess("The end of summer vacation is close.")
	hitretResult := Hitret(7345)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But as the sun shines relentlessly, the temperatureÅ^still feels like it's in the height of summer.")
	hitretResult := Hitret(7346)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In that kind of heat, I went to buy the icecream thatÅ^Sora requested.")
	hitretResult := Hitret(7347)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With only the sound of crickets chirping, and trees andÅ^grass swaying, there wasn't an indication of anyoneÅ^else.")
	hitretResult := Hitret(7348)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Opposite to the heat, it was a calm, early afternoon.")
	hitretResult := Hitret(7349)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Here, your portion.")
	hitretResult := Hitret(7350)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_02L", 0, 0)
	Talk("Sora", "SR011023")
	Mess("Thanks.")
	hitretResult := Hitret(7351)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Among the things Sora was looking for in the freezer,Å^she chose two long sticks of icecream. It was the typeÅ^that oculd be divided in the middle.")
	hitretResult := Hitret(7352)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The two were in one... Selecting something similar toÅ^us, I wonder if Sora thought about it too much.")
	hitretResult := Hitret(7353)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When the slightly melted icecream was edible, anÅ^artificial soda flavor spread out.")
	hitretResult := Hitret(7354)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_03M", 0, 0)
	Jump(4, 0)
	Talk("Sora", "SR011024")
	Mess("CoÅ`ld!")
	hitretResult := Hitret(7355)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha, if you eat it that fast, your head will hurt.")
	hitretResult := Hitret(7356)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011025")
	Mess("If it's only this much, I'm fine.")
	hitretResult := Hitret(7357)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora smiled as I licked the melted icecream that wasÅ^drooping down.")
	hitretResult := Hitret(7358)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B34a", 400, 0)
	Talk("êSÇÃê∫", "")
	Mess("We kept walking aimlessly on the road.")
	hitretResult := Hitret(7359)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("On this road where no one else was visible, it felt asÅ^though we were on a journey in a world where no one butÅ^us existed.")
	hitretResult := Hitret(7360)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though I was close enough to Sora to feel herÅ^clothes, I could not feel any sweltering.")
	hitretResult := Hitret(7361)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's eyes as she smiled... reflected no one but me.")
	hitretResult := Hitret(7362)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I could not feel the time, nor the heat, nor anything...Å^I could only feel Sora.")
	hitretResult := Hitret(7363)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011026")
	Mess("Ahh, geeze... Haru...")
	hitretResult := Hitret(7364)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_02L", 0, 0)
	Talk("Haruka", "")
	Mess("......Eh?")
	hitretResult := Hitret(7365)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011027")
	Mess("Your icecream melted.")
	hitretResult := Hitret(7366)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... You're right......")
	hitretResult := Hitret(7367)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I realized it, the icecream I was holding hadÅ^become half of what it was.")
	hitretResult := Hitret(7368)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The melted icecream trickling down the corner hadÅ^periodically created dark spots on the ground.")
	hitretResult := Hitret(7369)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011028")
	Mess("What a waste...")
	hitretResult := Hitret(7370)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha...")
	hitretResult := Hitret(7371)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I tossed the melted icecream that looked like it wasÅ^about to fall off the stick, into my mouth.")
	hitretResult := Hitret(7372)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uwaa... It's already slow.")
	hitretResult := Hitret(7373)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I could no longer taste the icecream in my mouth... IÅ^got it off of the top of my tongue.")
	hitretResult := Hitret(7374)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The coldness of the little icecream left on the stickÅ^stole the hedat off the tip of my tongue.")
	hitretResult := Hitret(7375)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("My hands are all sticky too...")
	hitretResult := Hitret(7376)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_03L", 0, 0)
	Talk("Sora", "SR011029")
	Mess("...You didn't notice that much icecream trickling down?Å^I guess there's no choice... I'll clean it for you......")
	hitretResult := Hitret(7377)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hey... Sora...")
	hitretResult := Hitret(7378)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I gently resisted Sora, who had taken my hand and triedÅ^to carry it to her mouth.")
	hitretResult := Hitret(7379)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With this kind of traffic, licking my finger isÅ^overdoing it a bit...")
	hitretResult := Hitret(7380)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_05L", 0, 0)
	Talk("Sora", "SR011030")
	Mess("Whatever......")
	hitretResult := Hitret(7381)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Although Sora looked a little unhappy, it looked likeÅ^she could sense that too.")
	hitretResult := Hitret(7382)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I want to wash my sticky hands somewhere... but on theÅ^side of this road, there's nowhere I can do that.")
	hitretResult := Hitret(7383)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I guess there's no choice, let's go back to the storeÅ^for just a sec.")
	hitretResult := Hitret(7384)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_02L", 0, 0)
	Talk("Sora", "SR011031")
	Mess("Okay.")
	hitretResult := Hitret(7385)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Turning around at leasure, we returned to the road weÅ^were just on.")
	hitretResult := Hitret(7386)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(7387)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In the dry road, there were black spots that looked likeÅ^it had been drilled.")
	hitretResult := Hitret(7388)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_01L", 0, 0)
	Talk("Sora", "SR011032")
	Mess("...What's wrong?")
	hitretResult := Hitret(7389)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... No, it's nothing.")
	hitretResult := Hitret(7390)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("They're the stains from the melted icecream.")
	hitretResult := Hitret(7391)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From the time we were gone, they had now become small.")
	hitretResult := Hitret(7392)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(7393)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was strangly interested in them.")
	hitretResult := Hitret(7394)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In the middle of the wide street, like water, withoutÅ^drying or vanishing... there were just a few traces ofÅ^it remaining.")
	hitretResult := Hitret(7395)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("Somewhere inside my own heart... I could feel it, likeÅ^it was something that had become isolated... I couldn'tÅ^help it.")
	hitretResult := Hitret(7396)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	EyeCatchEnter("DATE", 0)
	Change("00_a023b")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

