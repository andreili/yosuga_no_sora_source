Include"yosuga.ch"

Function sc00_D035()
	ScenarioEnter()
	ScPlayBgm("BGM08", 0)
	SetCg("B27a", 0, 0)
	Talk("S‚Ìº", "")
	Mess("September 1.")
	hitretResult := Hitret(32281)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B02a", 0, 0)
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR040135")
	Mess("...What a pain.")
	hitretResult := Hitret(32282)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Although she spouted a depressing line as soon as she^entered the kitchen, I couldn't help but be surprised at^the fact that she woke up without me calling her.")
	hitretResult := Hitret(32283)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I had just been preparing myself to deal with a small^tantrum.")
	hitretResult := Hitret(32284)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040136")
	Mess("What?")
	hitretResult := Hitret(32285)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......Ah, it's nothing... Breakfast is ready.")
	hitretResult := Hitret(32286)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040137")
	Mess("I can see that.")
	hitretResult := Hitret(32287)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Yeah.")
	hitretResult := Hitret(32288)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Sora sat in her chair and silently spread jam onto her^toast.")
	hitretResult := Hitret(32289)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I poured some milk into my own coffee, and then poured^some for Sora before sitting down into my own chair.")
	hitretResult := Hitret(32290)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("As I sat across from her, it felt like it had been a^long time since we last did this, for some reason.")
	hitretResult := Hitret(32291)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Even though we see each other and eat meals together^everyday...")
	hitretResult := Hitret(32292)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("As I sat there thinking, "Why was it?" I understood.")
	hitretResult := Hitret(32293)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's been a while since we ate breakfast together,^huh...")
	hitretResult := Hitret(32294)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_09M", 0, 0)
	Talk("Sora", "SR040138")
	Mess("Who cares?")
	hitretResult := Hitret(32295)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, whatever... Have you finished packing for school?")
	hitretResult := Hitret(32296)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040139")
	Mess("....*Sigh*")
	hitretResult := Hitret(32297)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("She sighed as if I was asking something that I should^already know the answer to.")
	hitretResult := Hitret(32298)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("...I think she found my asking her all these things^annoying.")
	hitretResult := Hitret(32299)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B12a", 0, 0)
	Talk("S‚Ìº", "")
	Mess("As we stepped outside, a cool breeze blew in from the^mountains. Surprised, I looked around.")
	hitretResult := Hitret(32300)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The paddy fields had also changed from green to yellow.^I hadn't noticed it at all, making it all the more^surprising.")
	hitretResult := Hitret(32301)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("It probably wasn't that sudden of a change. It was just^that I hadn't noticed.")
	hitretResult := Hitret(32302)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("In a way, I wonder if that means that up until now, I^was someone who couldn't see what was around him.")
	hitretResult := Hitret(32303)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Perhaps after I was dumped, I started seeing other^things than Kazuha.")
	hitretResult := Hitret(32304)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Or maybe it's because I couldn't accept being dumped by^her, and started thinking about anything that wasn't^her.")
	hitretResult := Hitret(32305)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Either way, I could feel how tremendous a part of me^Kazuha was.")
	hitretResult := Hitret(32306)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Even though I was dumped, it doesn't seem like I can^give her up that easily.")
	hitretResult := Hitret(32307)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR040140")
	Mess("...Spineless. Depressed... You're being weird.")
	hitretResult := Hitret(32308)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Choose your words better...")
	hitretResult := Hitret(32309)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("As her voice came from next to me, I lifted my head.")
	hitretResult := Hitret(32310)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC01_01S", 0, 0)
	SetBustup("CD01_01S", 0, 0)
	Talk("S‚Ìº", "")
	Mess("When I saw Amatsume and Kazuha walking on the road on^the other side of the paddy field.")
	hitretResult := Hitret(32311)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Before summer vacation, we normally met them at this^time, on this road, as we went to school.")
	hitretResult := Hitret(32312)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("This is the first time I've seen them use the other^road.")
	hitretResult := Hitret(32313)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Though it might be because I'm looking with eyes clouded^with doubt.")
	hitretResult := Hitret(32314)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_07S", 0, 0)
	Talk("S‚Ìº", "")
	Mess("Kazuha turned to look this way.")
	hitretResult := Hitret(32315)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Should I call out? I didn't know.")
	hitretResult := Hitret(32316)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("How should I be treating her, so soon after being^dumped? I had no clue.")
	hitretResult := Hitret(32317)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01S", 0, 0)
	Talk("S‚Ìº", "")
	Mess("While I was lost in thought, Kazuha averted her eyes^when she realized that I was there.")
	hitretResult := Hitret(32318)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("S‚Ìº", "")
	Mess("And then she sped up her pace.")
	hitretResult := Hitret(32319)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("She was clearly avoiding me.")
	hitretResult := Hitret(32320)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR040141")
	Mess("What are you doing standing there?")
	hitretResult := Hitret(32321)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(32322)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Which reminds me, she definitely is mean.")
	hitretResult := Hitret(32323)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I wanted to think that there was some kind of mistake,^but I couldn't shake off these feelings of emptiness and^sadness.")
	hitretResult := Hitret(32324)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The toughest part wasn't whether we broke up or not. It^was trying not to love her anymore.")
	hitretResult := Hitret(32325)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I wondered if it was possible that Kazuha had also felt^this way....")
	hitretResult := Hitret(32326)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Dumping me... For Amatsume's sake...")
	hitretResult := Hitret(32327)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B27a", 0, 0)
	Talk("S‚Ìº", "")
	Mess("That wounded expression on her face before she turned^away...")
	hitretResult := Hitret(32328)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_09L", 0, 0)
	Talk("S‚Ìº", "")
	Mess("She must have been crying quite a bit.")
	hitretResult := Hitret(32329)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_d036")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

