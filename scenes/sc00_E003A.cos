Include"yosuga.ch"

Function sc00_E003A()
	ScenarioEnter()
	ScPlaySe("SE357", 0)
	SetCg("B02a", 0, 0)
	ScPlayBgm("BGM05", 0)
	Talk("êSÇÃê∫", "")
	Mess("I place the fried eggs on a plate, then put a piece ofÅ^bread in the microwave and press the toast button.")
	hitretResult := Hitret(33652)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Once the microwave begins to beep I check the clock.")
	hitretResult := Hitret(33653)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Still a bit too early to head to school.")
	hitretResult := Hitret(33654)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Although this'll be the first day Sora goes to school.Å^There's a chance she'll dilly-dally, so it wouldn't hurtÅ^to have some leeway time-wise.")
	hitretResult := Hitret(33655)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora! Breakfast's ready. You should probably startÅ^eating soon!")
	hitretResult := Hitret(33656)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("No response.")
	hitretResult := Hitret(33657)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She can't still be sleeping...")
	hitretResult := Hitret(33658)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yooo! Sora! Are you...")
	hitretResult := Hitret(33659)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01S", 0, 0)
	Talk("Sora", "SR050005")
	Mess("...")
	hitretResult := Hitret(33660)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora appears just when I was about to raise my voiceÅ^even more.")
	hitretResult := Hitret(33661)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You're still not dressed? Hurry up and get changed, thenÅ^come and eat.")
	hitretResult := Hitret(33662)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR050006")
	Mess("... Mm.")
	hitretResult := Hitret(33663)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora sticks her arm out at me just as I was beginning toÅ^feel relieved that she wasn't procrastinating.")
	hitretResult := Hitret(33664)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She has her uniform in her hands.")
	hitretResult := Hitret(33665)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR050007")
	Mess("... It's not sticking on...")
	hitretResult := Hitret(33666)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("'What isn't...?' I think to myself as I take herÅ^uniform. Sure enough, there were no buttons stitched on.")
	hitretResult := Hitret(33667)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Crap...")
	hitretResult := Hitret(33668)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Obviously she can't wear this without any buttons, norÅ^go to school, for that matter.")
	hitretResult := Hitret(33669)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR050008")
	Mess("Now what?")
	hitretResult := Hitret(33670)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... I guess sew on some buttons.")
	hitretResult := Hitret(33671)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm not sure I'd make it in time if I started now,Å^though.")
	hitretResult := Hitret(33672)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE109", 0)
	Talk("VoiceÅsìﬁèèÅt", "NO050016")
	Mess("Excuse me~!")
	hitretResult := Hitret(33673)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? Who could be here so early? Nao-chan?")
	hitretResult := Hitret(33674)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04M", 0, 0)
	Talk("Sora", "SR050009")
	Mess("... This voice...")
	hitretResult := Hitret(33675)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	SetCg("B01a", 0, 0)
	Transition("WIP_LR", 500)
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO050017")
	Mess("Good morning, Haru-chan. Oohh, new uniform, huh. ItÅ^looks good on you.")
	hitretResult := Hitret(33676)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, thanks... Good morning, Nao-chan.")
	hitretResult := Hitret(33677)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO050018")
	Mess("Given that you're wearing a new uniform... does thatÅ^mean Sora-chan will be joining us today?")
	hitretResult := Hitret(33678)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well... You see...")
	hitretResult := Hitret(33679)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("How am I supposed to explain this...? Maybe Nao-chan canÅ^sew the buttons on... but is it really okay to ask herÅ^for help?")
	hitretResult := Hitret(33680)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO050019")
	Mess("What's wrong? ... Is there something you're havingÅ^trouble with?")
	hitretResult := Hitret(33681)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO050020")
	Mess("I could help you out if you'd like.")
	hitretResult := Hitret(33682)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Ahhh, the truth is...")
	hitretResult := Hitret(33683)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR050010")
	Mess("Haru, roasted bunsÅc")
	hitretResult := Hitret(33684)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR050011")
	Mess("...")
	hitretResult := Hitret(33685)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO050021")
	Mess("Ah, good morning, Sora-chan. You still haven't changed?")
	hitretResult := Hitret(33686)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Yeah, that's what we were having a bit of a problemÅ^with...")
	hitretResult := Hitret(33687)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	SetCg("B32a", 0, 0)
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO050022")
	Mess("... No buttons, huh.")
	hitretResult := Hitret(33688)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I know this is a bit pushy, but, if you could...")
	hitretResult := Hitret(33689)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_03M", 0, 0)
	Talk("Nao", "NO050023")
	Mess("What're you going on about?! There's no need to be soÅ^hesitant with me. Leave it to me.")
	hitretResult := Hitret(33690)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO050024")
	Mess("I'll finish it up in a jiffy. Do you have a needle andÅ^some thread around?")
	hitretResult := Hitret(33691)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Sorry, that's another thing I'd need you to help outÅ^with...")
	hitretResult := Hitret(33692)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO050025")
	Mess("Alright, I got it.")
	hitretResult := Hitret(33693)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan calmly takes out a portable sewing kit from herÅ^bag.")
	hitretResult := Hitret(33694)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Then, she skillfully begins to stitch on the buttons.")
	hitretResult := Hitret(33695)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You're good at this. You seem pretty experienced.")
	hitretResult := Hitret(33696)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO050026")
	Mess("I wouldnÅft put it like that, but I'm happy you sayÅ^that.")
	hitretResult := Hitret(33697)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She finishes up so quickly that it makes me think ÅeIsÅ^sewing really that easy?'")
	hitretResult := Hitret(33698)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO050027")
	Mess("All done. It came out kinda rough, but I think it shouldÅ^hold up for today.")
	hitretResult := Hitret(33699)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO050028")
	Mess("I'll re-stitch them we get back from school, plus hem upÅ^Haru-chan's uniform.")
	hitretResult := Hitret(33700)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh?")
	hitretResult := Hitret(33701)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO050029")
	Mess("Don't just pin up the excess with a safety bin.")
	hitretResult := Hitret(33702)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, heh. Thanks. Anyway, Sora, be sure to thank her,Å^too.")
	hitretResult := Hitret(33703)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR050012")
	Mess("... Thank you...")
	hitretResult := Hitret(33704)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO050030")
	Mess("Ah, not a problem. You two still haven't had breakfast,Å^huh.")
	hitretResult := Hitret(33705)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO050031")
	Mess("Okay, let me hem up your pants too, Haru-chan. Could youÅ^take them off?")
	hitretResult := Hitret(33706)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Huh?... Ah, sure...")
	hitretResult := Hitret(33707)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Change("00_e003b")
	Return
	Change("EXIT_SCENARIO")
EndFunc

