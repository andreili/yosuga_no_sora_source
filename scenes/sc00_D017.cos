Include"yosuga.ch"

Function sc00_D017()
	ScenarioEnter()
	ScPlayBgm("BGM03", 0)
	SetCg("EZ06a", 0, 0)
	Talk("Ryouhei", "RH040139")
	Mess("Hey, Haruka. Did you see it last night?")
	hitretResult := Hitret(28753)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm, see what?")
	hitretResult := Hitret(28754)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040140")
	Mess("\"Miku and Takami's Jungle Survivor.\"")
	hitretResult := Hitret(28755)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...I think I just heard the name of an extremelyÅ^nostalgic TV program.")
	hitretResult := Hitret(28756)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040233")
	Mess("AhÅ` I watched it! Getting seperated from a universityÅ^excavation team and then getting lost in the jungle withÅ^a poacher, right?")
	hitretResult := Hitret(28757)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06d", 0, 0)
	Talk("Ryouhei", "RH040141")
	Mess("Yeah, yeah, and they find the location of the goldenÅ^ivory! Kahaaa, it's going to be the first hit in aÅ^while.")
	hitretResult := Hitret(28758)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Yeah, I can still recall slight bits of thatÅ^interesting story.")
	hitretResult := Hitret(28759)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh...but are they broadcasting the show again? Or is itÅ^a new season?")
	hitretResult := Hitret(28760)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06a", 0, 0)
	Talk("Ryouhei", "RH040142")
	Mess("Hah? What're you talking about, there is no secondÅ^season. It's only up to episode six, right?")
	hitretResult := Hitret(28761)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040234")
	Mess("Yup, that's rightÅ`")
	hitretResult := Hitret(28762)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...But that show's already finished.")
	hitretResult := Hitret(28763)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040143")
	Mess("...Are you mistaking it for something else?")
	hitretResult := Hitret(28764)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("N-No... They finished airing it last year where I usedÅ^to live...")
	hitretResult := Hitret(28765)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040144")
	Mess("Wha...!? O-Oi! No spoilers! I'm actually enjoying it!")
	hitretResult := Hitret(28766)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040235")
	Mess("Oh wowww, Haru-kun's already saw the ending.")
	hitretResult := Hitret(28767)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah....no... Maybe they just broadcast things later here?")
	hitretResult := Hitret(28768)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040145")
	Mess("Tch, and that's why you're a city kid...")
	hitretResult := Hitret(28769)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see... The local broadcasting offices are different...")
	hitretResult := Hitret(28770)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040146")
	Mess("Don't think it only airs the key station programs!Å^Things like \"It's Okay to Laugh!\" starts at 4, you know?")
	hitretResult := Hitret(28771)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh? Then what do they do from 12?")
	hitretResult := Hitret(28772)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040147")
	Mess("The local broadcasting office's originals... Don't lookÅ^down on them!")
	hitretResult := Hitret(28773)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I see...")
	hitretResult := Hitret(28774)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I didn't notice at all, but that's because I haven'tÅ^watched much TV since we moved here...")
	hitretResult := Hitret(28775)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The channels are few and besides, I don't really enjoyÅ^the shows broadcasted either.")
	hitretResult := Hitret(28776)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, because it's the city, it has several programs onÅ^many channels, so I can't watch everything anyway...")
	hitretResult := Hitret(28777)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I've flicked through the narrow range of channels hereÅ^when I was bored before, and there seemed to be aÅ^ridiculous number of talk shows.")
	hitretResult := Hitret(28778)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...But from what I can see, digital broadcasting hasn'tÅ^arrived in this area yet.")
	hitretResult := Hitret(28779)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06c", 0, 0)
	Talk("Kazuha", "KA040423")
	Mess("You three... We're in the middle of class, so focus atÅ^least a little bit...")
	hitretResult := Hitret(28780)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06f", 0, 0)
	Talk("Ryouhei", "RH040148")
	Mess("What's that? You want to join in?")
	hitretResult := Hitret(28781)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06e", 0, 0)
	Talk("Kazuha", "KA040424")
	Mess("Stop saying such stupid things!")
	hitretResult := Hitret(28782)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06f", 0, 0)
	Talk("Kazuha", "KA040425")
	Mess("And you, Kasugano-kun. I've told you not to get carriedÅ^away with Nakazato-senpai, haven't I?")
	hitretResult := Hitret(28783)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I think so, yeah...")
	hitretResult := Hitret(28784)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san's jab at Ryouhei is in some ways an everydayÅ^thing, but it seems that I'd forgotten about her tellingÅ^me to avoid Ryouhei.")
	hitretResult := Hitret(28785)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Besides, when Amatsume and Ryouhei get together, itÅ^turns out fun one way or another.")
	hitretResult := Hitret(28786)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040149")
	Mess("Hehe, too bad, huh? Today, Haruka is my ally!")
	hitretResult := Hitret(28787)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040236")
	Mess("Ahaa, welcome to the gang for today, Haru-kun.")
	hitretResult := Hitret(28788)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040426")
	Mess("...I see, is that so? Kasugano-kun wants to joinÅ^Nakazato-senpai in receiving detention.")
	hitretResult := Hitret(28789)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san's eyes were extremely cold.")
	hitretResult := Hitret(28790)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Those cold words stabbed me like a rain of daggers.")
	hitretResult := Hitret(28791)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I did say before that I wasn't allied with anyone...")
	hitretResult := Hitret(28792)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06c", 0, 0)
	Talk("Maths Teacher", "NP120006")
	Mess("Hooo, what was that, Kasugano-kun?")
	hitretResult := Hitret(28793)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040150")
	Mess("Oho!")
	hitretResult := Hitret(28794)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040237")
	Mess("Achaa.")
	hitretResult := Hitret(28795)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040427")
	Mess("......")
	hitretResult := Hitret(28796)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san's expression grew even more disgusted afterÅ^we caught the teacher's attention.")
	hitretResult := Hitret(28797)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Maths Teacher", "NP120007")
	Mess("Well, since we're talking anyway, Nakazato, Kasugano,Å^Amatsume, how about solving a question?")
	hitretResult := Hitret(28798)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040151")
	Mess("N-Nah... We're fine, we'll pass on that.")
	hitretResult := Hitret(28799)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Maths Teacher", "NP120008")
	Mess("Okay, Nakazato has page 82 for homework. What about you,Å^Amatsume?")
	hitretResult := Hitret(28800)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040152")
	Mess("What? A surprise attack!?")
	hitretResult := Hitret(28801)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040238")
	Mess("A-AhaÅ`...If I pass too...homework?")
	hitretResult := Hitret(28802)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Maths Teacher", "NP120009")
	Mess("I would guess so.")
	hitretResult := Hitret(28803)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B19a", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("...So if I can't answer either, it'll be homework for meÅ^as well...")
	hitretResult := Hitret(28804)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Well, it's not like I don't know what's written on theÅ^board.")
	hitretResult := Hitret(28805)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I would understand it if I had been listening properlyÅ^in class, but...")
	hitretResult := Hitret(28806)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06a", 0, 0)
	Talk("Akira", "AK040239")
	Mess("Uumm...Therefoooore, x and y aaaareÅ`Å`Å`ÅcÅc")
	hitretResult := Hitret(28807)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Being as flustered as Amatsume, I couldn't get theÅ^answer either.")
	hitretResult := Hitret(28808)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If only I started concentrating on the lesson whyÅ^Migiwa-san told me to...")
	hitretResult := Hitret(28809)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040240")
	Mess("Ah...umm...y = - (x +...")
	hitretResult := Hitret(28810)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Hm? Amatsume knows the answer? Or is she justÅ^desperately spouting nonsense... Ah.")
	hitretResult := Hitret(28811)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040428")
	Mess("......")
	hitretResult := Hitret(28812)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At that moment, I saw Migiwa's hand quickly return fromÅ^under Amatsume's table.")
	hitretResult := Hitret(28813)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And Amatsume's eyes were fixed on her desk.")
	hitretResult := Hitret(28814)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Maths Teacher", "NP120010")
	Mess("...Well, that's fine I guess. Got the answer right, atÅ^least.")
	hitretResult := Hitret(28815)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040153")
	Mess("Ah, that's dirty! Ojou, you traitor!")
	hitretResult := Hitret(28816)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Maths Teacher", "NP120011")
	Mess("Shut it, Nakazato. Do you want your homework to be theÅ^printouts as well as the textbook?")
	hitretResult := Hitret(28817)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040154")
	Mess("No, no, that is more than enough, your majesty.")
	hitretResult := Hitret(28818)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Maths Teacher", "NP120012")
	Mess("Geez, all you're good at is running your mouth. Next,Å^Kasugano!")
	hitretResult := Hitret(28819)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y-Yes.")
	hitretResult := Hitret(28820)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040241")
	Mess("Haru-kun, good luck!")
	hitretResult := Hitret(28821)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040155")
	Mess("Haruka! I believe in you!")
	hitretResult := Hitret(28822)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even after looking at the blackboard once again, I haveÅ^no clue.")
	hitretResult := Hitret(28823)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("They must've gone to another topic while we wereÅ^talking, since it's completely different to my notes.")
	hitretResult := Hitret(28824)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I can't understand the one Amatsume answered either.")
	hitretResult := Hitret(28825)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Maths Teacher", "NP120013")
	Mess("Looks like it might be best if you did the homework asÅ^revision like Nakazato, eh?")
	hitretResult := Hitret(28826)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, no, Let me try a bit more...ah.")
	hitretResult := Hitret(28827)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06c", 0, 0)
	Talk("Kazuha", "KA040429")
	Mess("....This...")
	hitretResult := Hitret(28828)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("All of a sudden, a hand came from beside me.")
	hitretResult := Hitret(28829)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From a torn piece of paper, an equation was written inÅ^neat handwriting. No mistaking it, it's definitely theÅ^answer.")
	hitretResult := Hitret(28830)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040430")
	Mess("...This is it...I'm never helping you again...")
	hitretResult := Hitret(28831)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Migiwa-san...")
	hitretResult := Hitret(28832)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040431")
	Mess("...This is an apology...for the time when Akira wasÅ^being a nuisance in the pool...so now we're even...")
	hitretResult := Hitret(28833)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Thanks, Migiwa-san...")
	hitretResult := Hitret(28834)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While think that Migiwa-san apologizing for the poolÅ^incident was strange, I read out what was on the note.")
	hitretResult := Hitret(28835)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(0, 1)
	ScWait(1500, 0)
	ScPlaySe("SE352", 0)
	Update()
	ScWait(3000, 0)
	SetCg("B19a", 0, 0)
	ScPlayBgm("BGM05", 0)
	Talk("êSÇÃê∫", "")
	Mess("And class ended.")
	hitretResult := Hitret(28836)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_10M", 0, 0)
	Talk("Ryouhei", "RH040156")
	Mess("WAAAAA, I was betrayed, you traitorrr. Ojou's meeaaan,Å^WAAAA!!!")
	hitretResult := Hitret(28837)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Being the only one who received homework, Ryouhei wasÅ^crying.")
	hitretResult := Hitret(28838)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA040432")
	Mess("Exams are coming up, so it's perfect timing for helpingÅ^you study, isn't it?")
	hitretResult := Hitret(28839)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040433")
	Mess("We went to use our summer vacation effectively, so don'tÅ^drag us down with your supplementary lessons.")
	hitretResult := Hitret(28840)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But he was no match for Migiwa-san.")
	hitretResult := Hitret(28841)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Although it might just be because it's Ryouhei, theÅ^extremely fake crying probably didn't help.")
	hitretResult := Hitret(28842)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But what she says applies to me too.")
	hitretResult := Hitret(28843)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC01_09M", 0, 0)
	Talk("Akira", "AK040242")
	Mess("Poor Ryou-niichan...I'll help you with it!")
	hitretResult := Hitret(28844)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_13M", 0, 0)
	Talk("Kazuha", "KA040434")
	Mess("W-Why do you have to go that far for him?")
	hitretResult := Hitret(28845)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040243")
	Mess("Well, I was talking to Ryou-niichan, and besides, it'sÅ^about time I studied too...")
	hitretResult := Hitret(28846)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH040157")
	Mess("Oh, Akira! You really are a nice person!Å^Unlike some other person...wink.")
	hitretResult := Hitret(28847)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And this time, yet another easy to understand wink fromÅ^Ryouhei.")
	hitretResult := Hitret(28848)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040158")
	Mess("...wink...come on...wink...")
	hitretResult := Hitret(28849)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("W-Well...although I did manage to avoid homework, it'sÅ^not like I'm ready for the test...")
	hitretResult := Hitret(28850)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I'll help too then. We're friends, and besides, I haveÅ^to study too!")
	hitretResult := Hitret(28851)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	SetBustup("CF01_02M", 0, 0)
	SetBustup("CD01_12M", 0, 0)
	Talk("Ryouhei", "RH040159")
	Mess("That's right! That's exactly why we're friends!")
	hitretResult := Hitret(28852)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040244")
	Mess("Okay, should we do it at my place? I've got barley teaÅ^there!")
	hitretResult := Hitret(28853)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040435")
	Mess("W-Wait a minute...")
	hitretResult := Hitret(28854)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040160")
	Mess("The barley tea at Akira's place is delicious! PrepareÅ^some snacks too!")
	hitretResult := Hitret(28855)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040245")
	Mess("Leave it to me! I'll go buy lots of rice crackers!")
	hitretResult := Hitret(28856)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040161")
	Mess("Oh, oh, I'm counting on you. Starting it today is fine,Å^right? The homework's due tomorrow, after all.")
	hitretResult := Hitret(28857)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040246")
	Mess("That's fine. That's okay with you too, Haru-kun?")
	hitretResult := Hitret(28858)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah...Y-yeah...")
	hitretResult := Hitret(28859)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_07M", 0, 0)
	Talk("Kazuha", "KA040436")
	Mess("Y-You three...")
	hitretResult := Hitret(28860)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And we continued our conversation and ignored whatÅ^Migiwa-san was trying to say...")
	hitretResult := Hitret(28861)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	SetBustup("CC01_01M", 0, 0)
	Talk("Ryouhei", "RH040162")
	Mess("We can do the homework as a main, but maybe we shouldÅ^make some counter-measures for the test...does anyoneÅ^know what's in it?")
	hitretResult := Hitret(28862)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I wouldn't really know.")
	hitretResult := Hitret(28863)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040248")
	Mess("I roughly know itÅ` I'm always listening when theÅ^teacher says, \"This will be in the exam!\"")
	hitretResult := Hitret(28864)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040163")
	Mess("All right, you're great, Akira!")
	hitretResult := Hitret(28865)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040249")
	Mess("Ahaa.")
	hitretResult := Hitret(28866)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_05M", 0, 0)
	Talk("Kazuha", "KA040437")
	Mess("W-Wait a minute!")
	hitretResult := Hitret(28867)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if she was unable to bear it any longer, Migiwa-sanÅ^raised her voice.")
	hitretResult := Hitret(28868)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH040164")
	Mess("What do you want, Kazu-chan? We're busy arranging aÅ^study group.")
	hitretResult := Hitret(28869)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040438")
	Mess("...Grr...I'll go too...")
	hitretResult := Hitret(28870)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040165")
	Mess("Ah? What was that?")
	hitretResult := Hitret(28871)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_06M", 0, 0)
	Talk("Kazuha", "KA040439")
	Mess("I'll go too!!")
	hitretResult := Hitret(28872)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH040166")
	Mess("All right! Reliable supporter, coerced!")
	hitretResult := Hitret(28873)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040250")
	Mess("And now we're all good for the test!")
	hitretResult := Hitret(28874)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And then, congratulating themselves on a job well done,Å^they high fived.")
	hitretResult := Hitret(28875)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_05M", 0, 0)
	Talk("Kazuha", "KA040440")
	Mess("W-Wait just a second! You...you were relying on me fromÅ^the start!?")
	hitretResult := Hitret(28876)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira & Ryouhei", "SYN00012")
	Mess("Kekekeke...")
	hitretResult := Hitret(28877)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040441")
	Mess("Y-You...")
	hitretResult := Hitret(28878)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040168")
	Mess("Okay! Since you said it yourself, we're counting on you,Å^Kazu-chan!")
	hitretResult := Hitret(28879)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040252")
	Mess("Thanks for volunteering, Kazu-chan!")
	hitretResult := Hitret(28880)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_09M", 0, 0)
	Talk("Kazuha", "KA040442")
	Mess("Ah..Ahhh...tricked again...")
	hitretResult := Hitret(28881)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Was this...really all planned?")
	hitretResult := Hitret(28882)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Getting me involved too, and the left out Migiwa-sanÅ^would be encouraged to help out...")
	hitretResult := Hitret(28883)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Being able to read that far into Migiwa-san'sÅ^thoughts...I know now not to take these two lightly.")
	hitretResult := Hitret(28884)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_d017b")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

