Include"yosuga.ch"

Function sc00_D024()
	ScenarioEnter()
	SetCg("BLACK", 0, 0)
	WindowView(1)
	ScPlayBgm("BGM08", 0)
	Talk("ƒ‚ƒmƒ[ƒOsˆê—tt", "KA040714")
	Mess("I was really surprised.")
	hitretResult := Hitret(30403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsˆê—tt", "KA040715")
	Mess("I didn't think for a moment that I could do something so^daring.")
	hitretResult := Hitret(30404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsˆê—tt", "KA040716")
	Mess("I was so happy, I couldn't contain myself.")
	hitretResult := Hitret(30405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsˆê—tt", "KA040717")
	Mess("To me, as long as Akira could celebrate her birthday,^that was fine. I didn't think once about celebrating my^own.")
	hitretResult := Hitret(30406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsˆê—tt", "KA040718")
	Mess("Because the fact that we're sisters is a secret, So that^no one would find it suspicious if we had our birthdays^on the same day, I had decided not to tell anyone^myself.")
	hitretResult := Hitret(30407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsˆê—tt", "KA040719")
	Mess("However, it's not like I didn't want it to be^celebrated.")
	hitretResult := Hitret(30408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsˆê—tt", "KA040720")
	Mess("It's just that I wanted to celebrate Akira's more...")
	hitretResult := Hitret(30409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsˆê—tt", "KA040721")
	Mess("So when Kasugano-kun celebrated it with even me, I was^happy from the bottom of my heart.")
	hitretResult := Hitret(30410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsˆê—tt", "KA040722")
	Mess("So happy that I clung onto him as he tried to leave.")
	hitretResult := Hitret(30411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	EyeCatchEnter("DATE", 0)
	WindowView(0)
	Update()
	ScPlayEnvSe("SE301", "", 0)
	SetCg("B03a", 0, 0)
	Talk("S‚Ìº", "")
	Mess("I awoke to the loud shrills of the cicadas.")
	hitretResult := Hitret(30412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I knew that they grow louder as it gets closer to Obon,^but it was only 7am and they were chirping away already.")
	hitretResult := Hitret(30413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Furthermore, with the window being open, I heard the^sound directly, making it unbearable.")
	hitretResult := Hitret(30414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Not feeling like getting to sleep again, I decided to^wash my face and prepare breakfast.")
	hitretResult := Hitret(30415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE301", 1)
	BlackOut(0, 0)
	SetCg("B02a", 0, 0)
	Transition("WIP_LR", 500)
	Talk("S‚Ìº", "")
	Mess("Sora wasn't awake yet, so I decided to make something^simple - eggs and toast.")
	hitretResult := Hitret(30416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("While cracking the egg, I stared closely at my hand.")
	hitretResult := Hitret(30417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The hand I held Migiwa-san's with.")
	hitretResult := Hitret(30418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The sound of jumping oil, the sound of frying eggs, the^sound of chirping cicadas. The monotonous rhythm made me^think back.")
	hitretResult := Hitret(30419)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScPlayBgm("BGM08", 0)
	Talk("S‚Ìº", "")
	Mess("That day...after the party, when Migiwa-san hugged me,^and let go almost immediately.")
	hitretResult := Hitret(30420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("And with her face completely red, she wordlessly^returned to the shrine office.")
	hitretResult := Hitret(30421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I could only stand stunned while watching her go, even^forgetting to chase after Sora.")
	hitretResult := Hitret(30422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Since then, it seemed like Migiwa-san was making more^physical contact with me.")
	hitretResult := Hitret(30423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Smiling over the smallest things, and taking my hand.")
	hitretResult := Hitret(30424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Should I react to things like that?")
	hitretResult := Hitret(30425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Can I really...think of it as affection...?")
	hitretResult := Hitret(30426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Back when I first met her, she was very protective of^Amatsume, giving me an almost scary impression of her.")
	hitretResult := Hitret(30427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("But as time went by, there was also that cute, normal^girl's smile.")
	hitretResult := Hitret(30428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I think I'm fairly lucky to have seen so many sides of^her.")
	hitretResult := Hitret(30429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("That kind gaze that she directed toward Amatsume,^straight and true, was something I also yearned for.")
	hitretResult := Hitret(30430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Even more when I learned of her and Amatsume's^relationship.")
	hitretResult := Hitret(30431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I even thought that the way she treated Amatsume was^just how I wanted to treat Sora.")
	hitretResult := Hitret(30432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("So when that gazed was turned toward me, I was shocked,^and I couldn't keep still.")
	hitretResult := Hitret(30433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("What happened that day, and when we held hands on the^beach...")
	hitretResult := Hitret(30434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("What if her straighforwardness was making her show her^true feelings?")
	hitretResult := Hitret(30435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The affection being directed at me, it's not like I feel^unhappy about it.")
	hitretResult := Hitret(30436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	ScPlaySe("SE301", 0)
	SetCg("B07a", 0, 0)
	Talk("S‚Ìº", "")
	Mess("On these grounds which have many trees, it sounded like^their were more cicadas than anything else here.")
	hitretResult := Hitret(30437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("This outburst of chirping cicadas sounded exactly like a^sudden downpour of evening rain.")
	hitretResult := Hitret(30438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Despite how noisy it was, I strangely felt cool and^calm.")
	hitretResult := Hitret(30439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I even think it's a similar atmosphere to the one that^surrounds Migiwa-san.")
	hitretResult := Hitret(30440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The cicadas were Amatsume, and the trees on which the^cicadas perch were Migiwa-san.")
	hitretResult := Hitret(30441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("This is definitely their place.")
	hitretResult := Hitret(30442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("If I come here, I can expect to meet Migiwa-san.")
	hitretResult := Hitret(30443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("S‚Ìº", "")
	Mess("Since she wasn't on the grounds, I went toward the main^wing, and the person I was looking for was there.")
	hitretResult := Hitret(30444)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopSe()
	SetCg("ED03a", 0, 0)
	ScPlayBgm("BGM04", 0)
	Talk("Haruka", "")
	Mess("Sleeping on your lap, is she?")
	hitretResult := Hitret(30445)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Without thinking, I let out a sigh.")
	hitretResult := Hitret(30446)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Smiling as she gently stroked Amatsume's head, she^seemed so motherly, and made my heart race.")
	hitretResult := Hitret(30447)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("As if I was in a daze, I couldn't take my eyes off her.")
	hitretResult := Hitret(30448)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040723")
	Mess("...Kasugano-kun...?")
	hitretResult := Hitret(30449)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah...Hello....")
	hitretResult := Hitret(30450)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I wanted to watch this gentle scene more, but that was^no longer possible.")
	hitretResult := Hitret(30451)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED03b", 0, 0)
	Talk("Kazuha", "KA040724")
	Mess("S-Stop that... Don't stare so much!")
	hitretResult := Hitret(30452)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If you use such a loud voice, you'll wake Amatsume.")
	hitretResult := Hitret(30453)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040725")
	Mess("Geez...")
	hitretResult := Hitret(30454)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Amatsume looks happy.")
	hitretResult := Hitret(30455)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED03c", 0, 0)
	Talk("Kazuha", "KA040726")
	Mess("This girl, as soon as she finds a cool place, she^immediately falls asleep.")
	hitretResult := Hitret(30456)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heeh, she is strange.")
	hitretResult := Hitret(30457)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040727")
	Mess("You know, that cat...there's a cat named Master here,^right?")
	hitretResult := Hitret(30458)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040728")
	Mess("I once asked her why it was named Master. She said it^was because it was the cat that taught her how to take^naps, as well as all the back roads.")
	hitretResult := Hitret(30459)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's just like her.")
	hitretResult := Hitret(30460)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040729")
	Mess("When she was little, she really did follow the cat like^a henchman.")
	hitretResult := Hitret(30461)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED03a", 0, 0)
	Talk("S‚Ìº", "")
	Mess("Her face turned motherly again, as she stroked^Amatsume's hair.")
	hitretResult := Hitret(30462)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("A kind, pure gesture, and once again I'm watching with^fascination.")
	hitretResult := Hitret(30463)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040730")
	Mess("What are you doing today?")
	hitretResult := Hitret(30464)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("But Migiwa-san was having none of that.")
	hitretResult := Hitret(30465)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nothing...just wondering if you two would be in if I^dropped by...")
	hitretResult := Hitret(30466)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Actually, I was just looking for her.")
	hitretResult := Hitret(30467)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("But when I think of Migiwa-san's reason for being here,^then Amatsume would have to be here too.")
	hitretResult := Hitret(30468)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Not even realizing that at first shocked me.")
	hitretResult := Hitret(30469)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040731")
	Mess("We're just eating lunch together, then finishing our^homework.")
	hitretResult := Hitret(30470)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see, if only I could join in.")
	hitretResult := Hitret(30471)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040732")
	Mess("Ah, I wouldn't mind that. I'll contact you next time.")
	hitretResult := Hitret(30472)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040733")
	Mess("But don't jump onto this girl's bandwagon and start^playing, okay? The moment she sees a chance, she'll^start slacking off.")
	hitretResult := Hitret(30473)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's okay, there's a scary Onee-san standing guard.")
	hitretResult := Hitret(30474)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED03b", 0, 0)
	Talk("Kazuha", "KA040734")
	Mess("Geez...")
	hitretResult := Hitret(30475)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("She made a face which told me that my remark hurt a bit.")
	hitretResult := Hitret(30476)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The reason her reaction to being teased was so small was^probably because Amatsume was on her lap.")
	hitretResult := Hitret(30477)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Normally, Amatsume would be tottering about, and she^would be watching over us attentively.")
	hitretResult := Hitret(30478)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("At that time, I would be on Migiwa-san's...")
	hitretResult := Hitret(30479)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I understand why Amatsume is number one for Migiwa-san,^but I just felt something like jealousy.")
	hitretResult := Hitret(30480)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It looks like she's tired today.")
	hitretResult := Hitret(30481)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED03c", 0, 0)
	Talk("Kazuha", "KA040735")
	Mess("If you asked her, she'd say something silly like, \"It's^because I studied so hard today.\"")
	hitretResult := Hitret(30482)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If you put it that way, you don't think she was trying^hard?")
	hitretResult := Hitret(30483)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040736")
	Mess("Running around and not concentrating, as usual.")
	hitretResult := Hitret(30484)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahaha...")
	hitretResult := Hitret(30485)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I didn't think Amatsume was in the way.^But, I did wish I could talk to Migiwa-san somewhere^where we're closer.")
	hitretResult := Hitret(30486)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040380")
	Mess("Mnngggg.....")
	hitretResult := Hitret(30487)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Perhaps because our conversation was too loud, Amatsume^shifted positions slightly.")
	hitretResult := Hitret(30488)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED03b", 0, 0)
	Talk("Kazuha", "KA040737")
	Mess("......")
	hitretResult := Hitret(30489)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(30490)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040381")
	Mess("Mng.........")
	hitretResult := Hitret(30491)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Disregarding us, who had shut up in fear of waking her^up, Amatsume continued to shift positions.")
	hitretResult := Hitret(30492)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Squirming about with her head, she tried to bury her^face into Migiwa-san's thighs.")
	hitretResult := Hitret(30493)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040738")
	Mess("W-Wait...Akira...there is...")
	hitretResult := Hitret(30494)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040382")
	Mess("....Mng...*sniff**sniff*...")
	hitretResult := Hitret(30495)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("And then began to sniff, as if smelling her crotch.")
	hitretResult := Hitret(30496)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("...For some reason it feels like I'm watching something^that I shouldn't be...")
	hitretResult := Hitret(30497)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040739")
	Mess("What are you doing, stop that...!")
	hitretResult := Hitret(30498)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040383")
	Mess("....Mng....")
	hitretResult := Hitret(30499)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B07a", 0, 0)
	SetBustup("CD02_10M", 0, 0)
	SetBustup("CC06_07M", 0, 0)
	Talk("Akira", "AK040384")
	Mess("....Pee....")
	hitretResult := Hitret(30500)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("P-Pee...?")
	hitretResult := Hitret(30501)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	Talk("S‚Ìº", "")
	Mess("After saying something so embarrassing, Amatsume^staggered into the main wing.")
	hitretResult := Hitret(30502)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040740")
	Mess("Hey, are you okay?")
	hitretResult := Hitret(30503)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01S", 0, 0)
	Talk("Akira", "AK040385")
	Mess("I'm ooookaaayy...Kazu-chan...want to cooome tooo?")
	hitretResult := Hitret(30504)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_13M", 0, 0)
	Talk("Kazuha", "KA040741")
	Mess("No!")
	hitretResult := Hitret(30505)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03S", 0, 0)
	Talk("Akira", "AK040386")
	Mess("Ahaa.")
	hitretResult := Hitret(30506)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	Talk("S‚Ìº", "")
	Mess("With a smile I didn't understand, she staggered away.")
	hitretResult := Hitret(30507)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA040742")
	Mess("...I wonder if she's okay...just as long as she doesn't^stumble...")
	hitretResult := Hitret(30508)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("She'll be fine.^Looks like she's heading to the toilet like that,^though.")
	hitretResult := Hitret(30509)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA040743")
	Mess("That's bad itself!!")
	hitretResult := Hitret(30510)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B08a", 0, 0)
	SetBustup("CD02_04M", 0, 0)
	Talk("Kazuha", "KA040744")
	Mess("There we go.")
	hitretResult := Hitret(30511)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("In the end, she tucked Akira into her futon.")
	hitretResult := Hitret(30512)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Even at the toilet, we had to call out to her from^outside the door, eventually grab her, take her to her^room, and lay her down on the futon.")
	hitretResult := Hitret(30513)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Pulling up her towel blanket, we set her alarm clock to^wake her up toward the evening.")
	hitretResult := Hitret(30514)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("And Amatsume fell into a deep sleep just like that.")
	hitretResult := Hitret(30515)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Looks like she really did want to sleep.")
	hitretResult := Hitret(30516)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040745")
	Mess("She should've just said so...instead of using my lap as^a pillow.")
	hitretResult := Hitret(30517)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("And you just spoiled her by letting her, right?")
	hitretResult := Hitret(30518)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA040746")
	Mess("Don't say it like I wanted to do it for her!")
	hitretResult := Hitret(30519)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Shhh!^If you use such a loud voice, you'll wake her.")
	hitretResult := Hitret(30520)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_10M", 0, 0)
	Talk("Akira", "AK040387")
	Mess("............")
	hitretResult := Hitret(30521)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040747")
	Mess("......")
	hitretResult := Hitret(30522)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Still okay, it seems.")
	hitretResult := Hitret(30523)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040748")
	Mess("...That's mean of you...")
	hitretResult := Hitret(30524)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("S‚Ìº", "")
	Mess("Migiwa-san gathered the textbooks and stationery on the^table and put them in her bag.")
	hitretResult := Hitret(30525)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Going home already?")
	hitretResult := Hitret(30526)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA040749")
	Mess("In this state, she probably won't wake up. Not until the^alarm clock rings.")
	hitretResult := Hitret(30527)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040750")
	Mess("Ah, you needed something from us, right?")
	hitretResult := Hitret(30528)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nope, I just...just came to see you two...")
	hitretResult := Hitret(30529)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_10M", 0, 0)
	Talk("Kazuha", "KA040751")
	Mess("Eh...I-If that's the case...I should've gotten out some^tea. Ah, I can still...")
	hitretResult := Hitret(30530)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't worry about it, I'm going home as well...^I'll walk you home.")
	hitretResult := Hitret(30531)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_d024b")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

