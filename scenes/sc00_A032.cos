Include"yosuga.ch"

Function sc00_A032()
	ScenarioEnter()
	ScPlayBgm("BGM18", 0)
	Talk("Sora", "SR011565")
	Mess("Nnn... *chuu*... Nn.......")
	hitretResult := Hitret(10013)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011566")
	Mess("Haru...... I'll never let you go again...... Nnn...Å^*chuu*...")
	hitretResult := Hitret(10014)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Hanging over me, Sora kissed me over and over again.")
	hitretResult := Hitret(10015)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011567")
	Mess("Your body... is cold... Are you okay?")
	hitretResult := Hitret(10016)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Since I was hot from running around... the coldness fromÅ^the lake's water felt good, but... I wonder if I was tooÅ^immersed......")
	hitretResult := Hitret(10017)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011568")
	Mess("......Dummy... Nn... *chuu*...")
	hitretResult := Hitret(10018)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011569")
	Mess("I'll warm you up...... Haru......")
	hitretResult := Hitret(10019)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh... Ah... Sora......")
	hitretResult := Hitret(10020)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Lifting up my shirt, and unfasting the belt on my pants,Å^my zipper went down.")
	hitretResult := Hitret(10021)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So--Sora......")
	hitretResult := Hitret(10022)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011570")
	Mess("*kushuu*......")
	hitretResult := Hitret(10023)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You're wet too, aren't you...")
	hitretResult := Hitret(10024)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011571")
	Mess("...So, Haru... Let's warm each other up? Nnn...")
	hitretResult := Hitret(10025)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While she closed my mouth with a kiss, she took out myÅ^thing from my boxers.")
	hitretResult := Hitret(10026)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Kuu... It's cold... Your hand...")
	hitretResult := Hitret(10027)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A cold feeling made my hips pull back for a moment.")
	hitretResult := Hitret(10028)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011572")
	Mess("It's okay... I'll warm you up from now on......")
	hitretResult := Hitret(10029)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As she rubbed my thing up and down, it gradually rose upÅ^as the blood flow increased.")
	hitretResult := Hitret(10030)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011573")
	Mess("Here... it's become healthy.")
	hitretResult := Hitret(10031)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So...Sora...")
	hitretResult := Hitret(10032)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011574")
	Mess("More... I'll do it... more... Nnn...")
	hitretResult := Hitret(10033)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA16a", 0, 0)
	Transition("", 3000)
	Talk("êSÇÃê∫", "")
	Mess("Before slowly undoing her blouse, Sora unhooked her bra,Å^exposing her breasts.")
	hitretResult := Hitret(10034)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And, she rubbed the steeply risen thing between my legsÅ^with her small breast.")
	hitretResult := Hitret(10035)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011575")
	Mess("Nnn... Ahaa... Your penis... is getting hot...")
	hitretResult := Hitret(10036)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011576")
	Mess("It's cute how it twitches... Nnn... Nnn...")
	hitretResult := Hitret(10037)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Each time Sora moved above me, the warmth of her skinÅ^was transmitted.")
	hitretResult := Hitret(10038)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora put my thing between her breasts, although herÅ^breasts weren't big.")
	hitretResult := Hitret(10039)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, when she wrapped my thing up along with both of herÅ^breasts, it felt good.")
	hitretResult := Hitret(10040)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011577")
	Mess("Nnn... Huu... H...How is it... am I doing well?")
	hitretResult := Hitret(10041)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh... Yeah... It feels... great...")
	hitretResult := Hitret(10042)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011578")
	Mess("I'm glad... I... don't have breasts, so I thought thatÅ^it didn't feel good.")
	hitretResult := Hitret(10043)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No... they're soft... and warm...")
	hitretResult := Hitret(10044)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011579")
	Mess("Fufu... Haru... I'll do it more for you... Nnn... Nnn...")
	hitretResult := Hitret(10045)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora lightly grabbed my glans and rubbed it with herÅ^breasts.")
	hitretResult := Hitret(10046)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The way the shape of my glans changed as the elasticityÅ^of it pressing against her breast was interesting. AndÅ^her breasts rubbing my frenulum made me tremble.")
	hitretResult := Hitret(10047)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011580")
	Mess("Nnn... *chubu*... Nnn... It doesn't hurt? I'll wet it aÅ^little...")
	hitretResult := Hitret(10048)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora licked my glans with the tip of her tongue, as ifÅ^she was licking ice cream.")
	hitretResult := Hitret(10049)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And, while covering it with plenty of saliva, her lipsÅ^drew it over and over.")
	hitretResult := Hitret(10050)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011581")
	Mess("Nnn... *chuu*... *chiro*... Nnbuu... *chuu*...")
	hitretResult := Hitret(10051)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011582")
	Mess("Ah... You're we too... Fufu......")
	hitretResult := Hitret(10052)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The pre-cum that had mixed with Sora's tongue becameÅ^smeared around my glans.")
	hitretResult := Hitret(10053)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011583")
	Mess("Nnbuu... Nnn... *chuu*... Nn...! Nn...! Nnhaa...")
	hitretResult := Hitret(10054)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011584")
	Mess("Nnn... Maybe it's like that... *chuu*... Nnn...")
	hitretResult := Hitret(10055)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora traced my thing in a circle around her nipple.")
	hitretResult := Hitret(10056)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Before long, her nipples had become hard, and when itÅ^rubbed against my glans, electricity ran through myÅ^lower back.")
	hitretResult := Hitret(10057)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011585")
	Mess("Nnhu... Nnn... Ahh... This... might feel good... Nnn...")
	hitretResult := Hitret(10058)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011586")
	Mess("When your penis rubs against my nipple, it feels weird.")
	hitretResult := Hitret(10059)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011587")
	Mess("Ahh... Nnn... The liquid coming out of the tip isÅ^slippery...")
	hitretResult := Hitret(10060)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011588")
	Mess("Nnn... It seems like it came from my breasts... Ahh...Å^Haa haa...")
	hitretResult := Hitret(10061)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011589")
	Mess("My nipple... is sticky... Nnn... Ahh... Aauu...")
	hitretResult := Hitret(10062)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While stroking the rod portion, Sora continued rubbingÅ^it against her nipple.")
	hitretResult := Hitret(10063)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The cold hand grabbing it felt so good, it wasÅ^stiffening my thing.")
	hitretResult := Hitret(10064)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My body that was cold until just recently, graduallyÅ^became warmer around my waist. While Sora caressed myÅ^thing in a trance, sweat began to form on her forehead.")
	hitretResult := Hitret(10065)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011590")
	Mess("Haruu... Nnn... *chuu*... You're not cold?")
	hitretResult := Hitret(10066)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm gradually starting to warm up......")
	hitretResult := Hitret(10067)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011591")
	Mess("I see... That's good... *chuu*... *chubu*...")
	hitretResult := Hitret(10068)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Putting my thing in her mouth again, Sora pursed herÅ^lips as they rubbed my glans.")
	hitretResult := Hitret(10069)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As she performed fellatio, Sora pinched her nipple.")
	hitretResult := Hitret(10070)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011592")
	Mess("Nnpuu... Haa haa... Tell me if it hurts... Nnn...")
	hitretResult := Hitret(10071)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011593")
	Mess("I'll make it wetter for you... Nnn... *chubu chubu*...")
	hitretResult := Hitret(10072)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011594")
	Mess("Nnhuu.... Nnn... Nnn... Your penis is... hot... It seemsÅ^as if it's burning... Nnn... Ahuu...")
	hitretResult := Hitret(10073)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011595")
	Mess("Nn*chuu*... Nnn... *rero rero*... *chubu*...")
	hitretResult := Hitret(10074)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As Sora licked my tip and rubbed her breasts, she becameÅ^strongly stimulated.")
	hitretResult := Hitret(10075)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Each time her breasts that have become sticky withÅ^pre-cum and saliva, and my thing, rubbed against eachÅ^other, a lewd sound arose that was arousing.")
	hitretResult := Hitret(10076)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uuu... Sora... Th--That's amazing... Nnn...")
	hitretResult := Hitret(10077)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011596")
	Mess("Fufu... Haru's thing is cute when it twitches and jumps.")
	hitretResult := Hitret(10078)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011597")
	Mess("Do you want to cum...? But you can't... Hold on for justÅ^a little longer...")
	hitretResult := Hitret(10079)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011598")
	Mess("I'll make you feel really good.")
	hitretResult := Hitret(10080)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011599")
	Mess("You, who treasures me the most...")
	hitretResult := Hitret(10081)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011600")
	Mess("Nnn... Nnn...! Nnn nnn... Aaahh... Nnn... Nnuu...")
	hitretResult := Hitret(10082)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011601")
	Mess("Nn*chuu*... Nnbu... Nnn, nnuuu... Nn*chuu*...")
	hitretResult := Hitret(10083)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011602")
	Mess("Nnpu... Haa haa haa... Your body has become hot...")
	hitretResult := Hitret(10084)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011603")
	Mess("Because the two of us... are warming each other up...")
	hitretResult := Hitret(10085)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011604")
	Mess("But I... was trying to part with this warmth...")
	hitretResult := Hitret(10086)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I too... thought the two of us would try to disappear...")
	hitretResult := Hitret(10087)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011605")
	Mess("Even though it's this warm... even though we bothÅ^confirmed it many times...")
	hitretResult := Hitret(10088)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... even though it was something precious...")
	hitretResult := Hitret(10089)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011606")
	Mess("Haru... Let me feel it more... your warmth...")
	hitretResult := Hitret(10090)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora... Nnn...")
	hitretResult := Hitret(10091)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We tightly embraced each other.")
	hitretResult := Hitret(10092)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("To warm up our completely frozen bodies... there's noÅ^way but to warm each other.")
	hitretResult := Hitret(10093)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't to... lose that anymore.")
	hitretResult := Hitret(10094)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With our frozen bodies approaching, we once againÅ^realized... each other's existences.")
	hitretResult := Hitret(10095)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I want to get it back... this sensation...")
	hitretResult := Hitret(10096)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With the the person I love... With Sora...")
	hitretResult := Hitret(10097)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011607")
	Mess("Nnn... Haru... Let's mix them together? Our bodies, andÅ^our feelings...")
	hitretResult := Hitret(10098)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah......")
	hitretResult := Hitret(10099)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011608")
	Mess("Nnn... *chuu*......")
	hitretResult := Hitret(10100)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even when we embraced, my thing kept standing withoutÅ^becoming flaccid, and continuously pressed againstÅ^Sora's stomach.")
	hitretResult := Hitret(10101)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011609")
	Mess("It's healthy, isn't it... Fufu... *chuu*... Nn*chuu*...Å^Nnn...")
	hitretResult := Hitret(10102)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011610")
	Mess("Cum lots... Nnn... *rero*... *chupu*...")
	hitretResult := Hitret(10103)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011611")
	Mess("Because with my mouth and breasts, I'll give you lots ofÅ^love... Nnn... Ahhu...")
	hitretResult := Hitret(10104)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Held by Sora's breasts, as if being wrapped up by bothÅ^hands, my thing was tenderly rubbed by her breasts andÅ^nipples.")
	hitretResult := Hitret(10105)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011612")
	Mess("Ahh... Haa... Nnn...! Nnn...! Your penis is twitching...Å^You're already... going to cum?")
	hitretResult := Hitret(10106)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uu... Yeah... Kuu... Aahh.... R--Right there...")
	hitretResult := Hitret(10107)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011613")
	Mess("Fufu... Nn*chuu*... *chubu*... *picha picha*... Nnn...")
	hitretResult := Hitret(10108)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011614")
	Mess("From now on... Nn*chuu*... Lots of semen will come out,Å^right...")
	hitretResult := Hitret(10109)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011615")
	Mess("Nnhuu... *chubu*... *chuu*... Nnn... Haa haa...")
	hitretResult := Hitret(10110)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While stroking the rod portion, she rolled her tongueÅ^around my glans, and it started to softly nudge at myÅ^urethra...")
	hitretResult := Hitret(10111)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011616")
	Mess("Haru... When I do this... to the tip... does it feelÅ^good? Nn*chuu*...")
	hitretResult := Hitret(10112)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011617")
	Mess("Nnhuu... I'll peck at it with my tongue... Fufu... DoesÅ^it tickle?")
	hitretResult := Hitret(10113)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011618")
	Mess("Nnn... *chubu*... NnnÅ`... *rero*... *picha*...")
	hitretResult := Hitret(10114)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011619")
	Mess("Nnn... *chuu*... Ah... It might be cumming... The liquidÅ^is starting to change...")
	hitretResult := Hitret(10115)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011620")
	Mess("Nnn*chuu*... Haru.. You can cum.. Nnn... *chuu*...")
	hitretResult := Hitret(10116)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011621")
	Mess("Because I'll accept... all of it... Nnn... *chuu*......")
	hitretResult := Hitret(10117)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uuahh... Nnkuu... If you do it that intensely...")
	hitretResult := Hitret(10118)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011622")
	Mess("It's okay... You don't have to hold back anymore, so...Å^Nnn... cum...")
	hitretResult := Hitret(10119)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uu... Kuu... Aaahh...")
	hitretResult := Hitret(10120)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011623")
	Mess("Nnn... Nnuu... Nnn... You can't hold back anymore, canÅ^you...")
	hitretResult := Hitret(10121)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011624")
	Mess("Nnhuu... Nnn... Aahh, ahh... Your face looks painful...")
	hitretResult := Hitret(10122)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011625")
	Mess("Cum... Haru... Show me more of... your cute face...")
	hitretResult := Hitret(10123)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011626")
	Mess("Nnn... *chuu*... *chubu*, *chubu*... Nn*chuuu*...")
	hitretResult := Hitret(10124)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011627")
	Mess("Nnhuu... Nnnuu... *chuu*... *chuu*...")
	hitretResult := Hitret(10125)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Aahh... Soraa... I...")
	hitretResult := Hitret(10126)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011628")
	Mess("It's okay... cum... lots... Nnn... Nn*chuuu*...")
	hitretResult := Hitret(10127)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora strongly rubbed my thing, as if she was wrapping itÅ^up in her breasts.")
	hitretResult := Hitret(10128)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And, she licked my penis with her tongue that was stickyÅ^from the opening between her breasts.")
	hitretResult := Hitret(10129)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011629")
	Mess("Nnn.. Nnhuu... Ahuu... Ahh, ahh... Haruuu!!")
	hitretResult := Hitret(10130)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011630")
	Mess("Haruu... Cum... Nnn... Nnn... Nnnuuu...!")
	hitretResult := Hitret(10131)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011631")
	Mess("Cum... on my breasts... Nnn, nnhuu... Aaahh...")
	hitretResult := Hitret(10132)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("S--Sora... Nnkuu... Aahh...!!")
	hitretResult := Hitret(10133)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011632")
	Mess("Nn*chuu*... Nnn, nnhuu... *chubu*... *chuu*...")
	hitretResult := Hitret(10134)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011633")
	Mess("Nnn, nnuuu... Haa haa haa... Nnn... *chuu*...")
	hitretResult := Hitret(10135)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011634")
	Mess("Nnkuu... Nnnkuu, nnn, nnn...!! Nnhaaaahh...!!")
	hitretResult := Hitret(10136)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...!!!")
	hitretResult := Hitret(10137)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA16b", 0, 0)
	Flush("WHITE", 1000, "")
	Talk("Sora", "SR011635")
	Mess("Ahh... Ahhaaaaaaaaahhh...!!")
	hitretResult := Hitret(10138)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011636")
	Mess("Nnn... Ahh... Aahh... Nnn... it's cumming... yourÅ^sperm...")
	hitretResult := Hitret(10139)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011637")
	Mess("Haa haa haa... Nnn... Aahh... Nnuuu...")
	hitretResult := Hitret(10140)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My sperm vigorously shot out.")
	hitretResult := Hitret(10141)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While ejaculating, my lower back spontaneously moved, myÅ^white liquid began to copiously overflow.")
	hitretResult := Hitret(10142)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In order to not make it spill, Sora gulped it down.")
	hitretResult := Hitret(10143)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011638")
	Mess("...Ahuu... Nnn... *gulp gulp*... Nnhuu... Aahh...")
	hitretResult := Hitret(10144)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011639")
	Mess("*gulp*... Delicious... your sperm liquid... Haa haa...")
	hitretResult := Hitret(10145)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011640")
	Mess("...I'm happy... that you came lots...... Nnn...")
	hitretResult := Hitret(10146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011641")
	Mess("*chuu*... Nnhuu... *picha*... Nnhuu...")
	hitretResult := Hitret(10147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011642")
	Mess("You're still cumming? Nn, fufu... It felt good, huh...")
	hitretResult := Hitret(10148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011643")
	Mess("*chuu*... Nnn... *gulp*... *gulp*... Nnn...")
	hitretResult := Hitret(10149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011644")
	Mess("Nnn... Haaaaa......")
	hitretResult := Hitret(10150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As her shoulders moved up and down from her breathing,Å^Sora looked at me with her intoxicated eyes.")
	hitretResult := Hitret(10151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora......")
	hitretResult := Hitret(10152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("I embraced Sora tightly once more.")
	hitretResult := Hitret(10153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011645")
	Mess("Nnn...... Ahh...... Haru...... Ouch......")
	hitretResult := Hitret(10154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora......... Sora..... Nnn......")
	hitretResult := Hitret(10155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011646")
	Mess("Haru...... I love you......... Nnn...... *chuu*......")
	hitretResult := Hitret(10156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Tightly embracing each other, we just quietly kissed.")
	hitretResult := Hitret(10157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Feeling each other's warmth, it was a relaxing feelingÅ^to feel each other's heartbeat.")
	hitretResult := Hitret(10158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't really know why, but... it was a relaxingÅ^feeling from long ago...")
	hitretResult := Hitret(10159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Maybe it's because they were the heartbeats we couldÅ^feel ever since we were in our mother's belly.")
	hitretResult := Hitret(10160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora... *chuu*... Nnn...")
	hitretResult := Hitret(10161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Not just the lips, we continued kissing each other onÅ^our cheeks and napes of the necks.")
	hitretResult := Hitret(10162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A faint fragrance tickled my nose. Sora's fragranceÅ^smelled sweet, and pure, like the image of a baby.")
	hitretResult := Hitret(10163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I lovingly kised Sora's body.")
	hitretResult := Hitret(10164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011647")
	Mess("Nnn... Haru...... Kiss me lots...")
	hitretResult := Hitret(10165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay......")
	hitretResult := Hitret(10166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011648")
	Mess("...Nn... Nnhu... *chuu*... Nnn...")
	hitretResult := Hitret(10167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora... This time I'll...")
	hitretResult := Hitret(10168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011649")
	Mess("Okay...... Go ahead...... Haru......")
	hitretResult := Hitret(10169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora put both hands on my chest, and got on me, as ifÅ^she was straddling me.")
	hitretResult := Hitret(10170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011650")
	Mess("All right.... Nnn.........")
	hitretResult := Hitret(10171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When Sora tucked her skirt left and right, sheÅ^unfastened her shorts.")
	hitretResult := Hitret(10172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After shifting her shorts over, she sat on my lap again.")
	hitretResult := Hitret(10173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Huh... Sora... you're on top?")
	hitretResult := Hitret(10174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if taking into consideration my body exhausted fromÅ^almost drowning, Sora gently stopped her intent to getÅ^on top of me.")
	hitretResult := Hitret(10175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011651")
	Mess("Yep... I'll be on top...... it's okay, right?")
	hitretResult := Hitret(10176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Sure.")
	hitretResult := Hitret(10177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011652")
	Mess("Your penis... is okay now?")
	hitretResult := Hitret(10178)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora rubbed my thing, which had become flaccid fromÅ^ejaculating, and it began to swell up again.")
	hitretResult := Hitret(10179)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh... Uuu... If you rub it that strongly...")
	hitretResult := Hitret(10180)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011653")
	Mess("Don't cum right away... Nnn.... Ahh, it's become hard...")
	hitretResult := Hitret(10181)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011654")
	Mess("You're putting it in... Your......")
	hitretResult := Hitret(10182)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah...")
	hitretResult := Hitret(10183)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora rolled up her skirt, showing me the part betweenÅ^her legs.")
	hitretResult := Hitret(10184)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With her own finger, she opened up the muscle part,Å^addressing my thing.")
	hitretResult := Hitret(10185)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011655")
	Mess("Nnn... Uuu... Ahh... Haaaahh......")
	hitretResult := Hitret(10186)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA17a", 0, 0)
	Talk("Sora", "SR011656")
	Mess("Ahh... It's in... Your... Nnuu......")
	hitretResult := Hitret(10187)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As she slowly sat down, my thing became taken up insideÅ^her.")
	hitretResult := Hitret(10188)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As her vagina was wet, there wasn't much resistance, andÅ^it didn't cause her pain.")
	hitretResult := Hitret(10189)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA17b", 0, 0)
	Talk("Sora", "SR011657")
	Mess("Uu... Okay... It's in...")
	hitretResult := Hitret(10190)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011658")
	Mess("Haru... Nn... Nnhu... W--Watch... We're connected...Å^Nnuu... Both... our bodies... and minds...")
	hitretResult := Hitret(10191)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As Sora stripped off her skirt, the place where we wereÅ^connected became visible.")
	hitretResult := Hitret(10192)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if she was a little embarrassed, her cheeks wereÅ^bright red, and her face looked glad.")
	hitretResult := Hitret(10193)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's vagina had taken in my thing down to the base.Å^Without moving, it began to tighten and stimulate me.")
	hitretResult := Hitret(10194)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her body began to tremble from us being connected.")
	hitretResult := Hitret(10195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Up until now, we've piled our bodies on each otherÅ^several times.")
	hitretResult := Hitret(10196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From the joy of finally communicating our long-hiddenÅ^feelings for each other, I thought we had reached ourÅ^true feelings.")
	hitretResult := Hitret(10197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But... that was wrong. We felt a temporary happinessÅ^from piling our bodies on each other, but when thatÅ^warmth cooled, it resulted in anxiety for me.")
	hitretResult := Hitret(10198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Guilt... Morals... People's eyes... Various things gaveÅ^me doubts.")
	hitretResult := Hitret(10199)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And, those doubts even led to opposition to my feelingsÅ^for Sora.")
	hitretResult := Hitret(10200)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But... now, finally... it seems we're truly connected.")
	hitretResult := Hitret(10201)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though we were scared our relation may end, neitherÅ^of us could end our feelings for each other.")
	hitretResult := Hitret(10202)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Now we've both realized... the profoundness of thoseÅ^feelings.")
	hitretResult := Hitret(10203)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I will no longer waver...")
	hitretResult := Hitret(10204)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I smile at Sora, looking in my direction. Because I knowÅ^that my feelings are the same as Sora's.")
	hitretResult := Hitret(10205)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("This precious person... I will no longer let her go.")
	hitretResult := Hitret(10206)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora...... Nnn...... I'm going to move......")
	hitretResult := Hitret(10207)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011659")
	Mess("Okay...... Go ahead...... I want you to feel good......Å^so much I can hear your heart......")
	hitretResult := Hitret(10208)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Grabbing Sora's hips, I started to move over and over asÅ^if I was pulling them towards me.")
	hitretResult := Hitret(10209)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA17a", 0, 0)
	Talk("Sora", "SR011660")
	Mess("Nnn... Ahh... Huu... It's hitting... m--my deepestÅ^place...")
	hitretResult := Hitret(10210)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011661")
	Mess("Your... your penis is... deep inside mee...")
	hitretResult := Hitret(10211)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011662")
	Mess("Ahnn.... Ahh... Haahh... Nnn, nnnnuuu, ahh...!")
	hitretResult := Hitret(10212)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Together with Sora's hips I was pulling, I began to pushÅ^my thing into her vagina.")
	hitretResult := Hitret(10213)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My tip that was rapidly filling up deep inside herÅ^touched her womb.")
	hitretResult := Hitret(10214)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011663")
	Mess("Nnguu... Nnn... Ahh... Haa... Aahh... Numb...")
	hitretResult := Hitret(10215)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011664")
	Mess("Nnn... Ahaahh... I'll going numb... Every time youÅ^thrust inside me...")
	hitretResult := Hitret(10216)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011665")
	Mess("Ahahh... Nnn... Nnn...! Aaann... Haru... Haruu...")
	hitretResult := Hitret(10217)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011666")
	Mess("Haa haa haa... Nnn...! Nnuu... Ahh, ahh ahhh...")
	hitretResult := Hitret(10218)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011667")
	Mess("...Y--Your penis... is like it's raging inside me...")
	hitretResult := Hitret(10219)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011668")
	Mess("Nnn, nnuu! Aahh...... Ahh, ahhahhÅ`...... Nnkuu......")
	hitretResult := Hitret(10220)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011669")
	Mess("My insides.. are being rubbed a lot... both front andÅ^back... Nnuu...")
	hitretResult := Hitret(10221)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Every time I thrust, semen flies out, and soaks myÅ^stomach.")
	hitretResult := Hitret(10222)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While Sora feels it, her vagina fastens down, and beginsÅ^stimulating my thing.")
	hitretResult := Hitret(10223)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Succumbing to it, I stir her vagina as I push my thingÅ^up, and rub my glans against her vaginal wall.")
	hitretResult := Hitret(10224)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA17b", 0, 0)
	Talk("Sora", "SR011670")
	Mess("Nnhuu... Nn, ahahh... Ahh... You're amazing...")
	hitretResult := Hitret(10225)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011671")
	Mess("I can... feel you a lot... Nnn... Ahh... Haa haa...")
	hitretResult := Hitret(10226)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora puts her hand near the entrance of her mouth as ifÅ^she was embarrassed.")
	hitretResult := Hitret(10227)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, moving her hips moved as if in circles, she didn'tÅ^try to stop her lust.")
	hitretResult := Hitret(10228)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011672")
	Mess("Nnn... Ahh, ahh...! Aahh... Nnn... Do you feel good too?")
	hitretResult := Hitret(10229)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... Nnn... Kuu... If I let my guard down, it seemsÅ^I'll cum right away...")
	hitretResult := Hitret(10230)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA17a", 0, 0)
	Talk("Sora", "SR011673")
	Mess("Aahh... Ann... Aahh... M...Me too... Nnuunnn...")
	hitretResult := Hitret(10231)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011674")
	Mess("Nnn... My hips... are moving themselves... Nnnahh... HaaÅ^haa...")
	hitretResult := Hitret(10232)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Lightly moving as if springing up, Sora, who wasÅ^accepting my thing, started to rub her small breastsÅ^with both of her hands.")
	hitretResult := Hitret(10233)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011675")
	Mess("Aahh... Nnn... Ahh... My nipples... are hardd......")
	hitretResult := Hitret(10234)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Extending hands from below, she rubbed her small breastsÅ^as if wrapping them up.")
	hitretResult := Hitret(10235)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011676")
	Mess("Nnuu... Ahh... Kuu... No... Haru... Don't pinch themm!")
	hitretResult := Hitret(10236)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011677")
	Mess("Hii... Ahhh... Nngu... Ahh, aaaahh... Nnn...!!")
	hitretResult := Hitret(10237)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Then, she tightly pinched and pulled both nipples.")
	hitretResult := Hitret(10238)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011678")
	Mess("Hiiuuu...!! Aahnn... Haruu... Don't pull on my nipplesÅ^that muchhh... Ahh... Nnuu...")
	hitretResult := Hitret(10239)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011679")
	Mess("If...If you do it that much, aahh... Nnkuu... my nipplesÅ^will come off...")
	hitretResult := Hitret(10240)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011680")
	Mess("Nnaahhh...!! Hii... I...I'll go numb... if you pinchÅ^them that tightt...")
	hitretResult := Hitret(10241)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011681")
	Mess("Aahh...! Haa haa haahh...... Nnn...... Nnaaaahh...!")
	hitretResult := Hitret(10242)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The moment Sora bent backwards, my thing came out, andÅ^semen spurted out from her vagina.")
	hitretResult := Hitret(10243)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011682")
	Mess("Nnaahh... Aaaahhh... Aahh...!! It...It's coming......Å^from my place... Nnku...")
	hitretResult := Hitret(10244)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011683")
	Mess("Kuu... Ahh... Aaahh... I...I peed?")
	hitretResult := Hitret(10245)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("How perverted... Did it feel good?")
	hitretResult := Hitret(10246)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011684")
	Mess("D...Dummy... It's because you were so forceful... Nnn...Å^Aaahh...")
	hitretResult := Hitret(10247)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011685")
	Mess("Aahh... No... It's still sensitive theree, so don't putÅ^your penis insideee!")
	hitretResult := Hitret(10248)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's okay to become more disheveled... It's nothing toÅ^worry about, right? Since there's no one but you and me.")
	hitretResult := Hitret(10249)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011686")
	Mess("Th--That's right, but... Aahhnn! Aauu... Nnn, nnhuu...")
	hitretResult := Hitret(10250)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA17b", 0, 0)
	Talk("Sora", "SR011687")
	Mess("Hii... Nnguuu... I'm... already...... Ha...Haruu...")
	hitretResult := Hitret(10251)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Above me, Sora's body writhed twitchingly. She mightÅ^have cum a little from her nipples being stimulated.")
	hitretResult := Hitret(10252)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I want to feel you more... accept me a lot... and... IÅ^want you to show me that face...")
	hitretResult := Hitret(10253)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011688")
	Mess("...Haru...... Okay... You can... do it more...")
	hitretResult := Hitret(10254)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA17a", 0, 0)
	Talk("Sora", "SR011689")
	Mess("Aaahh... Nnn... Ahh...! Aahh...!! Ahhaahh... Nnn...!")
	hitretResult := Hitret(10255)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011690")
	Mess("Nnn, nnuu... It...It's good, Haru... Nnn...! Aaahh...!")
	hitretResult := Hitret(10256)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011691")
	Mess("It feels good... Can you feel it too? Ahh... Nnn...Å^Nnuu...!")
	hitretResult := Hitret(10257)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("On top of me, Sora's hips swung around.")
	hitretResult := Hitret(10258)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As even our wet public hair intertwined, Sora's clitorisÅ^was stimulated.")
	hitretResult := Hitret(10259)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011692")
	Mess("Nhaahh... Ahh... It's touching my clitoris... Ahh...Å^Nnn...!")
	hitretResult := Hitret(10260)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011693")
	Mess("Nnunn... Ahh... It...It's swelling... Nn... Aahh! Hii...Å^If...If you touch it... Hii...! Aahh...!!")
	hitretResult := Hitret(10261)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011694")
	Mess("No... Don't touch it... Ahh... No... I'll go crazyyy...!")
	hitretResult := Hitret(10262)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011695")
	Mess("Nnguuu... Nnaaaahh...!! Aaahh, ahaahh...!!")
	hitretResult := Hitret(10263)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After Sora convulsed as she sprung up for an instant,Å^she collapsed on top of me.")
	hitretResult := Hitret(10264)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And, while her shoulder went up and down from herÅ^breathing, she was still trembling as there wasÅ^remaining reverberation.")
	hitretResult := Hitret(10265)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA17b", 0, 0)
	Talk("Sora", "SR011696")
	Mess("Hii... It's amazing... It's tingles... When you touch meÅ^it's like electricity is running through my body...")
	hitretResult := Hitret(10266)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011697")
	Mess("Haa... Haa.... Aaahh......")
	hitretResult := Hitret(10267)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Shedding tears in large drops, they drooped down fromÅ^Sora's mouth as she kissed me.")
	hitretResult := Hitret(10268)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011698")
	Mess("Nnn... *chuu*... Haru... Nnn...")
	hitretResult := Hitret(10269)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With our tongues entwined, we exchanged kisses as weÅ^were both exchanging saliva.")
	hitretResult := Hitret(10270)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Sora... You don't have to hold back.")
	hitretResult := Hitret(10271)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011699")
	Mess("No... I want to cum together with you... So... you...Å^feel good too...")
	hitretResult := Hitret(10272)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Okay... I'm almost there...")
	hitretResult := Hitret(10273)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I half-rose Sora to her feet, and continued thrustingÅ^her like a bridge.")
	hitretResult := Hitret(10274)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA17a", 0, 0)
	Talk("Sora", "SR011700")
	Mess("Ahnn... Ahh... Nnn... Hii.... M...My ass...")
	hitretResult := Hitret(10275)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011701")
	Mess("I--If you say it like thatt... It...it's embarrassing...Å^Nnn...!")
	hitretResult := Hitret(10276)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011702")
	Mess("Nn...!! Nnahh...! Ahh, aahh...! Haa haa... Aahnn...!")
	hitretResult := Hitret(10277)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The entrance to Sora's vagina stretched as if pullingÅ^tightly. And, bit by bit my thing continued to thrustÅ^into her vagina.")
	hitretResult := Hitret(10278)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Everytime Sora's body twitched as it bent backwards, itÅ^tightened and twisted on my thing, as if stroking it.")
	hitretResult := Hitret(10279)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if exploiting the semen from the base, my tip wasÅ^attacked with stimulation from deep inside her.")
	hitretResult := Hitret(10280)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, Sora was on top of me, so I couldn't pull my waistÅ^back from the impact of the pleasant feeling.")
	hitretResult := Hitret(10281)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kuu... Nnnu,,, Haa haa haa...")
	hitretResult := Hitret(10282)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My whole body was attacked in waves by pleasantÅ^feelings. But, I can't escape it.")
	hitretResult := Hitret(10283)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011703")
	Mess("Nnguu... Ahh... Aahh...! Nnn...! Are you... Are youÅ^also... cumming... already...? Aahh... Haa haa haa...")
	hitretResult := Hitret(10284)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... J--Just about... me too......")
	hitretResult := Hitret(10285)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011704")
	Mess("Nnn... Aaahh... L...Let's both... Together......")
	hitretResult := Hitret(10286)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora strongly descended on me, and restricted my thing.")
	hitretResult := Hitret(10287)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011705")
	Mess("...Un...Until the end... Together......")
	hitretResult := Hitret(10288)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nnuuu... Aahh... Okay......")
	hitretResult := Hitret(10289)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Embracing Sora's waist, I pulled it in my directed, asÅ^if to forcefully become deeply glued to her.")
	hitretResult := Hitret(10290)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011706")
	Mess("Ahh, aahh... Nnn... Haru... Aahnn... Haa haa...")
	hitretResult := Hitret(10291)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011707")
	Mess("I--It's deep inside me... Your penis... Nnn...")
	hitretResult := Hitret(10292)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011708")
	Mess("Nnn... It feels like the entrance to my room... Aahh...Å^Hii... Nnguu... is going to wrench open... Nnuu...Å^Ahh... Aahh...")
	hitretResult := Hitret(10293)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011709")
	Mess("Nnn, nnn...! Aahh...... Ahh, ahhnn...! Ahh...!!")
	hitretResult := Hitret(10294)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While trying to restrain myself as if I was about toÅ^cum, I pushed up against Sora with all my strength.")
	hitretResult := Hitret(10295)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As Sora shed tears, I accepted them.")
	hitretResult := Hitret(10296)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Right now, we're becoming one.")
	hitretResult := Hitret(10297)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Melting together, as if we'll never again separate.")
	hitretResult := Hitret(10298)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011710")
	Mess("Nnn...! Nnuuu...! Ahh, ahhn...... Ha...Haruu!!")
	hitretResult := Hitret(10299)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011711")
	Mess("I...I'm cumminggg......Nn, nnhuu...... Nnuuu...!")
	hitretResult := Hitret(10300)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So......Soraa......")
	hitretResult := Hitret(10301)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I felt waves of pleasure, it felt like my strengthÅ^was escaping me.")
	hitretResult := Hitret(10302)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011712")
	Mess("Th-This moment... I feel like I'm together with you...Å^...Nnuu... Aaahh...!!")
	hitretResult := Hitret(10303)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011713")
	Mess("I'm glad... Aaahnn... Ahh, aahh... Aaahhnn!!")
	hitretResult := Hitret(10304)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So...Sora...! Guuu......")
	hitretResult := Hitret(10305)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011714")
	Mess("Nnguu... Ahh... Haruu..... I'm cumming...... I'm...Å^cumminggg...!!")
	hitretResult := Hitret(10306)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011715")
	Mess("Nnguu...... Haa haa...... Ahh, aahh...!! Nnaaah...!")
	hitretResult := Hitret(10307)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011716")
	Mess("Aahh...... Ahh, ahh, aahhh...... I'm already......Å^Ahaaahh......")
	hitretResult := Hitret(10308)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011717")
	Mess("Nnuu..... Ahh...... Aaaaaaaaaaaaaahhh!!")
	hitretResult := Hitret(10309)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA17c", 0, 0)
	Flush("WHITE", 1000, "")
	Talk("Sora", "SR011718")
	Mess("I'm...... cumminggggg...... Ahh, aahhÅ`n...... Ahh......")
	hitretResult := Hitret(10310)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011719")
	Mess("Ahh...... Aaaahhh...... It's hot...... d...deep...Å^inside...... Ahh...... Nnn...")
	hitretResult := Hitret(10311)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011720")
	Mess("It's comingg... lots of semen... Haa haa haa... Nn...Å^Ah...")
	hitretResult := Hitret(10312)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My thing trembled, as if it was still ejaculating.")
	hitretResult := Hitret(10313)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora felt her vagina with a smile as if it was valuable.")
	hitretResult := Hitret(10314)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011721")
	Mess("It's hot... inside me... Haa haa... Nn...")
	hitretResult := Hitret(10315)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011722")
	Mess("Nnuu...... Aaahh.......... Uuu......")
	hitretResult := Hitret(10316)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora...")
	hitretResult := Hitret(10317)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The moment I came, my whole body trembled, and my headÅ^became white, to the point I couldn't think aboutÅ^anything.")
	hitretResult := Hitret(10318)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But at that moment..... before my consciousness jumped,Å^I reached my hand to Sora and embraced her.")
	hitretResult := Hitret(10319)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("Sora", "SR011723")
	Mess("...Haru... *chuu*......")
	hitretResult := Hitret(10320)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nnn......")
	hitretResult := Hitret(10321)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011724")
	Mess("...I'm glad that you embraced me at the end.")
	hitretResult := Hitret(10322)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011725")
	Mess("Embracing me until the last moment...... felt like youÅ^were saying we would be together forever......")
	hitretResult := Hitret(10323)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Because when I blanked out, I was scared... that youÅ^would disappear.")
	hitretResult := Hitret(10324)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011726")
	Mess("......Haru......")
	hitretResult := Hitret(10325)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But... You were here... always inside my arms.")
	hitretResult := Hitret(10326)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So now I'm glad...")
	hitretResult := Hitret(10327)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011727")
	Mess("Haru... me too...")
	hitretResult := Hitret(10328)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I embraced Sora.")
	hitretResult := Hitret(10329)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's warmth... her sweet smell... her sweet longÅ^breaths, and her body weight, captured me.")
	hitretResult := Hitret(10330)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I've started to... accept everything from now on.")
	hitretResult := Hitret(10331)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, it's no longer painful...")
	hitretResult := Hitret(10332)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("Because we've obtained bonds we share.")
	hitretResult := Hitret(10333)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	OnRecollectFlag(5)
	If (IsRecollectMode())
		Change("EXIT_SCENARIO")
		Return
	EndIf
	Hide(1)
	ScWait(3000, 0)
	Change("00_a033")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

