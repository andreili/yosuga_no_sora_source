Include"yosuga.ch"

Function sc00_B001D()
	ScenarioEnter()
	SetCg("B20b", 0, 0)
	Transition("WIP_LR", 500)
	ScPlayBgm("BGM07", 0)
	Talk("êSÇÃê∫", "")
	Mess("Watching the sun set, makes my eyes sore.")
	hitretResult := Hitret(10817)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Everyone at school is enjoying and laughing, hearingÅ^them yelling as they practice during the end of theÅ^school day.")
	hitretResult := Hitret(10818)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't have any activities anymore, and the baseballÅ^team's practice is about to start at the sport's ground,Å^it's about time for me to go home now.")
	hitretResult := Hitret(10819)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO030043")
	Mess("Haaaaru-chan.")
	hitretResult := Hitret(10820)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, Nao-chan. Are you going home now?")
	hitretResult := Hitret(10821)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030044")
	Mess("No, I have some club activities today.")
	hitretResult := Hitret(10822)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heeh, so you're going to swim?")
	hitretResult := Hitret(10823)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_13M", 0, 0)
	Talk("Nao", "NO030045")
	Mess("Eh, have I told you about that Haru-chan?")
	hitretResult := Hitret(10824)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, uhh, I heard it from Ryouhei. That you are in theÅ^swimming club.")
	hitretResult := Hitret(10825)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO030046")
	Mess("Ahh, from him huh? I see. I was surprised about thatÅ^Haru-chan, since I haven't mentioned it to you yet.")
	hitretResult := Hitret(10826)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You looked like the traditional type for me, so I wasÅ^surprised as well.")
	hitretResult := Hitret(10827)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030047")
	Mess("Really? Maybe it was the glasses that gave thatÅ^impression?")
	hitretResult := Hitret(10828)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Probably, though maybe because you are alsoÅ^intelligent... I guess?")
	hitretResult := Hitret(10829)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_03L", 0, 0)
	Talk("Nao", "NO030048")
	Mess("Oh my, you know that nothing good will come out of itÅ^even if you praise me like that?")
	hitretResult := Hitret(10830)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se009", 0)
	Quake(16, 0, 1, 2, 30)
	Talk("Haruka", "")
	Mess("Oomphh!!")
	hitretResult := Hitret(10831)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt her on my shoulders, I unexpectedly staggered asÅ^she shook me.")
	hitretResult := Hitret(10832)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO030049")
	Mess("Ah, perhaps you'd like to take a look?")
	hitretResult := Hitret(10833)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh... but... wouldn't I be a bother?")
	hitretResult := Hitret(10834)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030050")
	Mess("I'm alone today, and it's not like I would swim, soÅ^don't worry about it.")
	hitretResult := Hitret(10835)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay... however, just a little.")
	hitretResult := Hitret(10836)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B21b", 0, 0)
	Talk("Haruka", "")
	Mess("Ah, there's water already?")
	hitretResult := Hitret(10837)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO030051")
	Mess("No, we we're going throw this water tomorrow.")
	hitretResult := Hitret(10838)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh... is that so?")
	hitretResult := Hitret(10839)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030052")
	Mess("Yeah, this hasn't been replaced for a long time now, soÅ^we would be changing the water. We mostly do some majorÅ^cleanup before opening the pool classes.")
	hitretResult := Hitret(10840)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030053")
	Mess("There are so many garbages that we need to gather soÅ^it's still dirty, right?")
	hitretResult := Hitret(10841)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If you certainly look in closer, there are growing mossÅ^and bags of garbages that's stucked at the bottom vinylÅ^linings, and they're so deep under.")
	hitretResult := Hitret(10842)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You're right, I thought that you could swim already.")
	hitretResult := Hitret(10843)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO030054")
	Mess("Everyone in the club is having their own training thisÅ^season, it's also considered as a season where everyoneÅ^is taking a break, so no one would be swimming.")
	hitretResult := Hitret(10844)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030055")
	Mess("Besides, the club's budget is low, so we can't changeÅ^the water of the pool everytime.")
	hitretResult := Hitret(10845)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Certainly, it's not like changing water when you'reÅ^going to take a bath in the tub.")
	hitretResult := Hitret(10846)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just draining the water, and replacing it would be niceÅ^but it surely would take a lot of time.")
	hitretResult := Hitret(10847)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO030056")
	Mess("That's why, we clean it most of the time we have freeÅ^time.")
	hitretResult := Hitret(10848)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see.")
	hitretResult := Hitret(10849)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030057")
	Mess("For the time being, before we open the pool, maybe theÅ^P.E. class can bond together while cleaning it.")
	hitretResult := Hitret(10850)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030058")
	Mess("So, we'll be the ones doing the finishing touches.")
	hitretResult := Hitret(10851)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO030059")
	Mess("Last year was really terrible. There were larvaes andÅ^water striders in the pool, and then we would catch themÅ^and return them to the fields.")
	hitretResult := Hitret(10852)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh... In the pool? How did they come here?")
	hitretResult := Hitret(10853)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030060")
	Mess("The larvaes came from the eggs that the dragonfly lays,Å^the water striders are caught by the strong winds andÅ^gets here, some decides to fly away.")
	hitretResult := Hitret(10854)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Th-They fly? Water striders can fly?")
	hitretResult := Hitret(10855)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_03M", 0, 0)
	Talk("Nao", "NO030061")
	Mess("Yeah, almost every insect on the farm fields can fly youÅ^know? Didn't you know?")
	hitretResult := Hitret(10856)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Th-They fly?")
	hitretResult := Hitret(10857)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030062")
	Mess("Did you know that even diving beetles fly?")
	hitretResult := Hitret(10858)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030063")
	Mess("So we apply antiseptic solutions in the pool, just asÅ^soon as it wears off.")
	hitretResult := Hitret(10859)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I never knew that...")
	hitretResult := Hitret(10860)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("From where I came from, we wouldn't be talking aboutÅ^these things.")
	hitretResult := Hitret(10861)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO030064")
	Mess("In the city, most pools are indoor, and there are noÅ^ponds and farm fields for the insects to fly from.")
	hitretResult := Hitret(10862)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's quite amusing, a lot of things happen in differentÅ^places.")
	hitretResult := Hitret(10863)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_11M", 0, 0)
	Talk("Nao", "NO030065")
	Mess("There's a lot more.")
	hitretResult := Hitret(10864)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030066")
	Mess("When I was young, there was a lot of things that wasn'tÅ^taught to me...")
	hitretResult := Hitret(10865)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO030067")
	Mess("That's why, you need to take it easy since you just moveÅ^in, okay Haru-chan?")
	hitretResult := Hitret(10866)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan...")
	hitretResult := Hitret(10867)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_11M", 0, 0)
	Talk("Nao", "NO030068")
	Mess("Oh yeah... when summer ends again, don't just suddenlyÅ^go, okay?")
	hitretResult := Hitret(10868)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Sure.")
	hitretResult := Hitret(10869)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO030069")
	Mess("Ah, also... once the pool is open, come over and hangÅ^out okay?")
	hitretResult := Hitret(10870)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030070")
	Mess("We only have a few members, you can swim here as much asÅ^you like.")
	hitretResult := Hitret(10871)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh, is it really fine?")
	hitretResult := Hitret(10872)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030071")
	Mess("Yeah, as long as you bring your own swimsuit, then youÅ^do as you like.")
	hitretResult := Hitret(10873)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... Th-Thank you... Ryouhei wants to swim as well.")
	hitretResult := Hitret(10874)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_06M", 0, 0)
	Talk("Nao", "NO030072")
	Mess("That guy? He also said that last year, he constantlyÅ^swim when he's here.")
	hitretResult := Hitret(10875)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ryouhei even said that he loves the pool.")
	hitretResult := Hitret(10876)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030073")
	Mess("Eh... Aah, most likely he also say that he loves theÅ^environment around the pool right? That pervert...")
	hitretResult := Hitret(10877)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan, you and Ryouhei really get along well huh?")
	hitretResult := Hitret(10878)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_13M", 0, 0)
	Shake(3)
	Talk("Nao", "NO030074")
	Mess("Uugh... L-Like I said, stop saying things like that.")
	hitretResult := Hitret(10879)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("S-Sorry...")
	hitretResult := Hitret(10880)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO030075")
	Mess("That guy, he's just intrusive.")
	hitretResult := Hitret(10881)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030076")
	Mess("That's why Haru-chan, you should watch out from him.")
	hitretResult := Hitret(10882)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO030077")
	Mess("But it looks like he already liked you, it must beÅ^difficult on you.")
	hitretResult := Hitret(10883)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah but, he was the only friend I had on the class fromÅ^the start, he really helped me a lot.")
	hitretResult := Hitret(10884)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030078")
	Mess("That goes the same for him as well.")
	hitretResult := Hitret(10885)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030079")
	Mess("He repeated for a year, taking classes lightly. He wasÅ^out of place by then, so he barely had anyone to talkÅ^to.")
	hitretResult := Hitret(10886)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see...")
	hitretResult := Hitret(10887)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I didn't know anything about him before, but we justÅ^normally became friends, also, you would usually beÅ^nervous if there's a senior in your class...")
	hitretResult := Hitret(10888)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, Amatsume is also there, so he's not completelyÅ^alone.")
	hitretResult := Hitret(10889)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO030080")
	Mess("Well, aside from being noisy, he's also a helpful and aÅ^kind guy, so please get along with him.")
	hitretResult := Hitret(10890)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, I got that.")
	hitretResult := Hitret(10891)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At that time, the facial expression of Nao-chan that IÅ^saw earlier came into my mind.")
	hitretResult := Hitret(10892)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Looking from behind, everyone in the class was having aÅ^nice conversation compared to Nao-chan who was lonelyÅ^counting her steps.")
	hitretResult := Hitret(10893)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wonder what's wrong with Nao-chan?")
	hitretResult := Hitret(10894)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Could something be... bothering Nao-chan?")
	hitretResult := Hitret(10895)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Way back before, she never ever showed that kind ofÅ^face... or was that only before?")
	hitretResult := Hitret(10896)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I thought Nao-chan never changed, but we didn't meetÅ^after a while, I guess something might have happened?")
	hitretResult := Hitret(10897)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, it would be bad if I ask her straight on what'sÅ^bothering her, that's what's been hitting me inside thatÅ^I want to put to an end.")
	hitretResult := Hitret(10898)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	Change("00_b002")
	Return
	Change("EXIT_SCENARIO")
EndFunc

