Include"yosuga.ch"

Function sc00_Z014()
	ScenarioEnter()
	ScPlaySe("SE352", 0)
	SetCg("B19a", 0, 0)
	ScPlayBgm("BGM05", 0)
	Talk("Akira", "AK000133")
	Mess("It's over! Good work todayÅ`")
	hitretResult := Hitret(1967)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	SetBustup("CC01_02M", 0, 0)
	Talk("Ryouhei", "RH000165")
	Mess("Geez, the moment classes end, your face lights up.")
	hitretResult := Hitret(1968)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You say that, but you're happy too, aren't you?")
	hitretResult := Hitret(1969)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH000166")
	Mess("Hehe, that's true.")
	hitretResult := Hitret(1970)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(7)
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA000051")
	Mess("Sorry Akira, but I'm going home first today.")
	hitretResult := Hitret(1971)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK000134")
	Mess("Ahh, today's your lesson, right?")
	hitretResult := Hitret(1972)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA000052")
	Mess("Yep, that's right.")
	hitretResult := Hitret(1973)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Beside Migiwa-san's desk was what looked like a musicalÅ^instrument case.")
	hitretResult := Hitret(1974)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("For a violin...that's a bit too big, isn't it?")
	hitretResult := Hitret(1975)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA000053")
	Mess("Yeah, this is a viola.")
	hitretResult := Hitret(1976)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Viola...I've heard the name, but sorry, I don't knowÅ^much about it.")
	hitretResult := Hitret(1977)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA000054")
	Mess("I see. It's basically a sibling of the violin.")
	hitretResult := Hitret(1978)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA000055")
	Mess("If you actually heard it you might be able to tell, butÅ^the viola has a slightly deeper sound.")
	hitretResult := Hitret(1979)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm...I don't know much about musical instruments, but IÅ^see...")
	hitretResult := Hitret(1980)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_03M", 0, 0)
	Talk("Kazuha", "KA000056")
	Mess("Haha. If you don't really listen to classical, I don'tÅ^think you need to worry about it that much.")
	hitretResult := Hitret(1981)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA000057")
	Mess("I learned this instrument because I liked it, butÅ^there's basically no reason to choose it over another.")
	hitretResult := Hitret(1982)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH000167")
	Mess("But that's an expensive Stradivarius, isn't it?")
	hitretResult := Hitret(1983)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA000058")
	Mess("There's so few of those famous things that there's noÅ^way I'd have one! This was given to me by my mother, whoÅ^used to play it.")
	hitretResult := Hitret(1984)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD01_11M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san stroked the case gently.")
	hitretResult := Hitret(1985)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ahh, she really treasures it... From her actions andÅ^expression, it's hard not to think that.")
	hitretResult := Hitret(1986)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, then was the reason you started playing the violaÅ^was because you were influenced by your mother?")
	hitretResult := Hitret(1987)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA000059")
	Mess("Yes, that's right. My mother used to play it when I wasÅ^young, so I was always listening to this instrument'sÅ^tone.")
	hitretResult := Hitret(1988)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA000060")
	Mess("After that, I started liking that tone...and at someÅ^point I started to have an urge to be able to play itÅ^myself.")
	hitretResult := Hitret(1989)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_05M", 0, 0)
	Talk("Ryouhei", "RH000168")
	Mess("Hmmmm....")
	hitretResult := Hitret(1990)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_14M", 0, 0)
	Talk("Akira", "AK000135")
	Mess("Heeeeh....")
	hitretResult := Hitret(1991)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_10M", 0, 0)
	Talk("Kazuha", "KA000061")
	Mess("W-What's up with you all?")
	hitretResult := Hitret(1992)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH000169")
	Mess("No, nothing. It's just that instead of learning itÅ^because it's stylish for an Ojou-sama, it's because of aÅ^rather normal reason.")
	hitretResult := Hitret(1993)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK000136")
	Mess("When Kazu-chan plays she makes a muuuuuch nicer faceÅ^than usual. And when she's done, she looks so peacefulÅ^that I could fall in love!")
	hitretResult := Hitret(1994)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_13M", 0, 0)
	Talk("Kazuha", "KA000062")
	Mess("W-What do you mean, "than usual?" Geez...")
	hitretResult := Hitret(1995)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......Yeah, I kinda wanna see how Migiwa-san looks whenÅ^she's playing. Especially that peaceful face.")
	hitretResult := Hitret(1996)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_10M", 0, 0)
	Talk("Kazuha", "KA000063")
	Mess("Y-You too, Kasugano-kun? S-Stop teasing me!")
	hitretResult := Hitret(1997)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I think this is the most red I've ever seen someone'sÅ^face get.")
	hitretResult := Hitret(1998)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I wasn't trying to tease you. But...anyway, the partÅ^about wanting to hear you play was true.")
	hitretResult := Hitret(1999)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But taking lessons really is amazing.Å^Do you participate in any club activities as well?")
	hitretResult := Hitret(2000)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD01_11M", 0, 0)
	Talk("Kazuha", "KA000064")
	Mess("...There's no orchestra in this school.Å^There's a wind instrument band, but...")
	hitretResult := Hitret(2001)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Really? ...Since you're playing it anyway, what a waste,Å^huh?")
	hitretResult := Hitret(2002)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA000065")
	Mess("I don't really mind. I'd be lying if I said I wasn'tÅ^interested, but I also don't feel like I have to belongÅ^to a club.")
	hitretResult := Hitret(2003)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_03M", 0, 0)
	Talk("Kazuha", "KA000066")
	Mess("...As long as I can play for the people I want to playÅ^for, that's good enough.")
	hitretResult := Hitret(2004)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("For a moment, Migiwa-san's gaze went distant...andÅ^turned toward Amatsume.")
	hitretResult := Hitret(2005)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's nice, playing for the people you want to.")
	hitretResult := Hitret(2006)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA000067")
	Mess("...That's true. But it always feels too soothing toÅ^Akira, and she ends up falling asleep halfway...")
	hitretResult := Hitret(2007)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_03M", 0, 0)
	Talk("Akira", "AK000137")
	Mess("That's because it really does feel soothing.")
	hitretResult := Hitret(2008)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see. The tone is that soothing.Å^If there's a chance, could you let me hear it too...?")
	hitretResult := Hitret(2009)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_10M", 0, 0)
	Talk("Kazuha", "KA000068")
	Mess("Eh? Ah, w-well...if there's a chance...")
	hitretResult := Hitret(2010)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK000138")
	Mess("Ahahaha, Kazu-chan's gone all shy!")
	hitretResult := Hitret(2011)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? Is letting me hear it really that embarrassing?")
	hitretResult := Hitret(2012)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA000069")
	Mess("O-Of course not! It's just that normally the only oneÅ^who listens is Akira, so...um...")
	hitretResult := Hitret(2013)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH000170")
	Mess("Ahahaha, Kazu-chan's gone all shy!")
	hitretResult := Hitret(2014)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_05M", 0, 0)
	Talk("Kazuha", "KA000070")
	Mess("There's no way I'm letting Nakazato-senpai listen!")
	hitretResult := Hitret(2015)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK000139")
	Mess("Hm? Eh, Kazu-chan, your ride's here.")
	hitretResult := Hitret(2016)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH000171")
	Mess("She's right, it's the usual car. I can even seeÅ^Motoka-san.")
	hitretResult := Hitret(2017)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Looking toward the gate through the window, I could seeÅ^that at some point in time, the black car I saw someÅ^time ago had stopped there.")
	hitretResult := Hitret(2018)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA000071")
	Mess("Oh no...Akira, could you go ahead and tell them to waitÅ^a bit?")
	hitretResult := Hitret(2019)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK000140")
	Mess("Yeah, I don't mind. Ah, you need to go to the toilet?")
	hitretResult := Hitret(2020)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_06M", 0, 0)
	Jump(5, 0)
	Talk("Kazuha", "KA000072")
	Mess("Idiot!! Don't say that out loud!!")
	hitretResult := Hitret(2021)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se011", 0)
	Flush("WHITE", 100, "")
	SetBustup("CC01_09M", 0, 0)
	Shake(2)
	Talk("Akira", "AK000141")
	Mess("Owww!!")
	hitretResult := Hitret(2022)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Like a flash, Migiwa-san's strike exploded on Amatsume.")
	hitretResult := Hitret(2023)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B20a", 150, 0)
	SetBustup("CE01_01M", 0, 0)
	Transition("WIP_MOZV", 500)
	Talk("Motoka", "MT000004")
	Mess("Eh? Akira-chan? You're not with the Ojou-sama?")
	hitretResult := Hitret(2024)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK000142")
	Mess("Kazu-chan said she'd be here soon, so please wait aÅ^moment.")
	hitretResult := Hitret(2025)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT000005")
	Mess("Ah, I see. Roger thatÅ`")
	hitretResult := Hitret(2026)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	SetBustup("CC01_01M", 0, 0)
	Talk("Ryouhei", "RH000172")
	Mess("Motoka-san, chaperoning again today?")
	hitretResult := Hitret(2027)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT000006")
	Mess("No, we were just out shopping today, so I figured IÅ^might as well walk Ojou-sama here too.")
	hitretResult := Hitret(2028)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While playing with her hands, the maid chatted with themÅ^as if they were friends of the same age.")
	hitretResult := Hitret(2029)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT000007")
	Mess("How've you been lately? Studying hard?")
	hitretResult := Hitret(2030)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH000173")
	Mess("No need to ask, we're already perfect in that area!Å^Right, Akira?")
	hitretResult := Hitret(2031)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_03M", 0, 0)
	Talk("Akira", "AK000143")
	Mess("Yeah, yeah. I can take naps at any time, and theÅ^conversation about last night's TV was fun tooÅ`")
	hitretResult := Hitret(2032)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_04M", 0, 0)
	Talk("Motoka", "MT000008")
	Mess("Ahaha, same as ever. But if you don't work hard inÅ^school, it's going to be harder when you go intoÅ^society, you knowÅ`? Can't always be playing around.")
	hitretResult := Hitret(2033)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPauseBgm()
	BustupClear()
	SetBustup("CJ01_01M", 0, 0)
	Talk("Man in the SuitÅsåéå©éRÅt", "YM000001")
	Mess("......")
	hitretResult := Hitret(2034)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Standing beside the car, a man wearing sunglassesÅ^lightly cleared his throat.")
	hitretResult := Hitret(2035)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScRestartBgm()
	BustupClear(10)
	SetBustup("CE01_06M", 0, 0)
	Talk("Motoka", "MT000009")
	Mess("A-Anyway! I'm working today!! Yeah!")
	hitretResult := Hitret(2036)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The Oji-san stood so still and silent, without anyÅ^wasteful movement, that I felt oddly intimidated. TheÅ^fact that he's tall just makes it worse......")
	hitretResult := Hitret(2037)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But Amatsume and Ryouhei didn't seem to have noticed,Å^and only the maid felt in trouble and quivered.")
	hitretResult := Hitret(2038)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(6)
	SetBustup("CC01_01M", 0, 0)
	SetBustup("CJ01_01M", 0, 0)
	Talk("Akira", "AK000144")
	Mess("Hello, Yamanashi-san.")
	hitretResult := Hitret(2039)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CJ01_02M", 0, 0)
	Talk("Yamanashi", "YM000002")
	Mess("Hello. You're going home now, aren't you? Please beÅ^careful on your way home.")
	hitretResult := Hitret(2040)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_04M", 0, 0)
	Talk("Akira", "AK000145")
	Mess("Okay!")
	hitretResult := Hitret(2041)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wonder if they're close, but Amatsume's talkingÅ^without a hint of timidity.")
	hitretResult := Hitret(2042)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Migiwa-san's family....butler?")
	hitretResult := Hitret(2043)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But would a butler wear sunglasses...?")
	hitretResult := Hitret(2044)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH000174")
	Mess("Bzzzt. Secretary of the Ojou's father.")
	hitretResult := Hitret(2045)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Secretary...eh?")
	hitretResult := Hitret(2046)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I see......come to think of it he's a driver too, soÅ^when he wears sunglasses, he looks like a Secret ServiceÅ^agent or something.")
	hitretResult := Hitret(2047)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Actually, he looks like he'll be in a movie. Maybe as aÅ^highly-skilled secretary...?")
	hitretResult := Hitret(2048)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA000073")
	Mess("I'm sorry for making you wait.")
	hitretResult := Hitret(2049)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CJ01_01M", 0, 0)
	Talk("Yamanashi", "YM000003")
	Mess("I've come to pick you up, Ojou-sama.")
	hitretResult := Hitret(2050)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_11M", 0, 0)
	Talk("Motoka", "MT000010")
	Mess("W-Welcome back, Ojou-sama...")
	hitretResult := Hitret(2051)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA000074")
	Mess("Thank you. Sorry for making you come all this way.")
	hitretResult := Hitret(2052)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The atmosphere around Migiwa-san had changed completelyÅ^from when she was in the classroom a little while ago.")
	hitretResult := Hitret(2053)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But even then, the gap between her angry and embarrassedÅ^face was huge...")
	hitretResult := Hitret(2054)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	AddSelect("But that's what's interesting", 0)
	AddSelect("Too much teasing would be bad", 0)
	hitretResult := StartSelect()
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	If (ChkSelect(1))
		Talk("êSÇÃê∫", "")
		Mess("But on the other hand, I think it's for that very reasonÅ^that I find her interesting.")
		hitretResult := Hitret(2055)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("êSÇÃê∫", "")
		Mess("Migiwa-san is essentially a serious person, but herÅ^sudden changes from angry to timid...are something I seeÅ^as cute.")
		hitretResult := Hitret(2056)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("êSÇÃê∫", "")
		Mess("If I start teasing her too, she's going to have a hardÅ^time, but this is going to be fun.")
		hitretResult := Hitret(2057)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		OnFlag(4)
	ElseIf (ChkSelect(2))
		Talk("êSÇÃê∫", "")
		Mess("Well, Ryouhei and Amatsume are going to be teasing herÅ^regularly.")
		hitretResult := Hitret(2058)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("êSÇÃê∫", "")
		Mess("So at the very least, if I don't become an ally of sortsÅ^and try to calm them down, I'd feel sorry for her.")
		hitretResult := Hitret(2059)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("êSÇÃê∫", "")
		Mess("I want to get along with my classmates, after all.")
		hitretResult := Hitret(2060)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
	EndIf
	BustupClear()
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK000146")
	Mess("Okay then, good luck with your lessons.")
	hitretResult := Hitret(2061)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA000075")
	Mess("Yep. See you tomorrow.")
	hitretResult := Hitret(2062)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("See ya, Migiwa-san.")
	hitretResult := Hitret(2063)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH000175")
	Mess("See ya, Ojou.")
	hitretResult := Hitret(2064)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA000076")
	Mess("Excuse me.")
	hitretResult := Hitret(2065)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After Migiwa-san bowed to us once, the maid behind herÅ^opened the door and Migiwa-san hopped in. She justÅ^looked fairly good then, actually.")
	hitretResult := Hitret(2066)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE263", 0)
	BustupClear(5)
	SetBustup("CC01_01M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("The maid and secretary nodded to us lightly andÅ^disappeared through different doors.")
	hitretResult := Hitret(2067)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE260", 0)
	Talk("êSÇÃê∫", "")
	Mess("And leaving the soft noise of the engine, the car slowlyÅ^drove away.")
	hitretResult := Hitret(2068)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH000176")
	Mess("Okay, let's get going ourselves.")
	hitretResult := Hitret(2069)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK000147")
	Mess("Yep.")
	hitretResult := Hitret(2070)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if following the car, we headed toward the schoolÅ^gate.")
	hitretResult := Hitret(2071)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B17a", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Haruka", "")
	Mess("At any rate, an Ojou-sama like Migiwa-san really doesÅ^exist...")
	hitretResult := Hitret(2072)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH000177")
	Mess("Hm? Were you worried about that? It's not like it's yourÅ^first time seeing one, right?")
	hitretResult := Hitret(2073)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, that's true, but..where I used to live, I neverÅ^saw anyone getting picked up by a car. It's definitelyÅ^rare.")
	hitretResult := Hitret(2074)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH000178")
	Mess("Hmmm, we're always seeing this, so I guess we just thinkÅ^it's normal.")
	hitretResult := Hitret(2075)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_14M", 0, 0)
	Talk("Akira", "AK000148")
	Mess("Yup. But if it's just getting picked up by car, thatÅ^happens to Ryou-niichan as well sometimes. Right?")
	hitretResult := Hitret(2076)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH000179")
	Mess("I guess so!")
	hitretResult := Hitret(2077)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh, Ryouhei too? Is he like the son of a good family?")
	hitretResult := Hitret(2078)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH000180")
	Mess("Hahaha! My parents tell me to help out on the farm andÅ^come pick me up in a light truck.")
	hitretResult := Hitret(2079)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Yeah, just as I thought.")
	hitretResult := Hitret(2080)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH000181")
	Mess("Ah, Haruka, you..! Don't look down on farming. When itÅ^comes to harvest time, I'll call you over so you canÅ^witness the significance of AGRICULTURE!!")
	hitretResult := Hitret(2081)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh, I'll pass...I'm a know-nothing amateur, remember?")
	hitretResult := Hitret(2082)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH000182")
	Mess("That's okay. Eventually, I'll teach you how to drive aÅ^combine harvester with one hand!")
	hitretResult := Hitret(2083)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_03M", 0, 0)
	Talk("Akira", "AK000149")
	Mess("And also, the rice you harvest yourself tastes reaalllyyÅ^good!")
	hitretResult := Hitret(2084)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y-Yeah...It's not like I can't understand that,Å^but...have you tried it too, Amatsume?")
	hitretResult := Hitret(2085)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	SetBustup("CF01_01M", 0, 0)
	Talk("Akira", "AK000150")
	Mess("Yup, even Kazu-chan has gone rice planting barefooted inÅ^the rice fieldsÅ`")
	hitretResult := Hitret(2086)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What...?!")
	hitretResult := Hitret(2087)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That Ojou-sama, Migiwa-san, going barefoot into the riceÅ^fields...somewhat hard to imagine...")
	hitretResult := Hitret(2088)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hey, in that case, Nao-chan's done it too, right?")
	hitretResult := Hitret(2089)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH000183")
	Mess("Yup. You've gotten accustomed to this at a good time, soÅ^it's just about time to dye you with the colors of theÅ^countryside.")
	hitretResult := Hitret(2090)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK000151")
	Mess("Yeah, yeah! It's time to make you a child of this town!")
	hitretResult := Hitret(2091)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("N-nah, I think I'm already a child of this town...")
	hitretResult := Hitret(2092)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH000184")
	Mess("Geez. Without touching nature, how could you be a childÅ^of this town!Å^You crazy city kid!!")
	hitretResult := Hitret(2093)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE009", 0)
	Quake(16, 0, 1, 2, 30)
	Talk("Haruka", "")
	Mess("Whoaaa!? I-I'm joking!!")
	hitretResult := Hitret(2094)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei firmly grabbed my shoulders and started shakingÅ^me.")
	hitretResult := Hitret(2095)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK000152")
	Mess("Ahahahaha!")
	hitretResult := Hitret(2096)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In the midst of my shaking vision, the blue sky cameÅ^flooding in.")
	hitretResult := Hitret(2097)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Being able to make friends like this, being able to talkÅ^little by little, being able to fit into this town...")
	hitretResult := Hitret(2098)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It felt like this was the first time that I'd reallyÅ^accepted that I'd moved into this town.")
	hitretResult := Hitret(2099)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	If (IsUNK1)
		adv.enableKeyUpdateFlush(0)
		Hide()
		BlackOut(0, 0)
		ScWait(2000, 1)
		SetCg("pub1", 0, 0)
		Update()
		ScWait(1000, 1)
		ScPlayVoice("AK080003")
		ScWaitVoice(1)
		ScWait(5000, 1)
		ScPlayEnvSe("SE406", "", 0)
		SetCg("ImageBoard", 0, 800)
		Scroll(0, 0, 40000, 0, 0)
		Transition("", 3000)
		Update()
		ScPlayVoice("AK080006")
		ScWaitVoice(1)
		ScWait(1000, 1)
		ScPlayVoice("AK080007")
		ScWaitVoice(1)
		ScWait(1000, 1)
		ScPlayVoice("AK080008")
		ScWaitVoice(1)
		ScWait(1000, 1)
		ScPlayVoice("AK080009")
		ScWaitVoice(1)
		ScWait(1000, 1)
		ScPlayVoice("AK080010")
		ScWaitVoice(1)
		ScWait(1000, 1)
		WaitScroll()
		ScWait(5000, 0)
		WhiteOut(0, 0)
		ScWait(2000, 0)
		Change("EXIT_SCENARIO")
		Return
	ElseIf(ChkFlagOn(1))
		EyeCatchEnter("", 0)
		Change("00_a001")
		Return
	ElseIf (ChkFlagOn(2))
		EyeCatchEnter("DATE", 0)
		Change("00_b001")
		Return
	ElseIf (ChkFlagOn(3))
		EyeCatchEnter("DATE", 0)
		Change("00_c001")
		Return
	ElseIf (ChkFlagOn(4))
		EyeCatchEnter("", 0)
		Change("00_d001")
		Return
	Else
		EyeCatchEnter("", 0)
		Change("00_z015")
		Return
		Change("EXIT_SCENARIO")
		Return
	EndIf
EndFunc

