Include"yosuga.ch"

Function sc00_C015B()
	ScenarioEnter()
	SetCg("B28d", 0, 600)
	ScPlaySe("se014", 0)
	Talk("Haruka", "")
	Mess("Hoh... Heave-ho.....")
	hitretResult := Hitret(21382)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume is light, so I thought it will be easy. ButÅ^when I started climbing the stairs, my knees begin toÅ^feel tired.")
	hitretResult := Hitret(21383)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If I lose my balance and fall backwards, Amatsume and IÅ^will be in danger.")
	hitretResult := Hitret(21384)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I fix Amatsume's position, and slowly begin to climb upÅ^the stairs with no other choice.")
	hitretResult := Hitret(21385)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020721")
	Mess("Mmm...... Hmmm?")
	hitretResult := Hitret(21386)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... Amatsume, you're awake?")
	hitretResult := Hitret(21387)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020722")
	Mess("Nyaa? We already reached?")
	hitretResult := Hitret(21388)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Just a little more...... Right now, we are at the stairsÅ^to the shrine.")
	hitretResult := Hitret(21389)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020723")
	Mess("Ahh..... I'm sorry, I feel asleep......")
	hitretResult := Hitret(21390)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, I think it's natural to feel tired after playing soÅ^much.")
	hitretResult := Hitret(21391)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020724")
	Mess("Ah, I should walk on my own, can you put me down?")
	hitretResult := Hitret(21392)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Wait until we reached flat ground? If I put you down nowÅ^it will be dangerous.")
	hitretResult := Hitret(21393)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020725")
	Mess("Mmmm, I understand~ Haru-kun's back is really big.")
	hitretResult := Hitret(21394)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm sorry, it must feel uncomfortable from myÅ^perspiration.")
	hitretResult := Hitret(21395)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020726")
	Mess("No...... not at all. Haru-kun smells a little like aÅ^girl.")
	hitretResult := Hitret(21396)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-Is that so? I can't smell my ownself though......")
	hitretResult := Hitret(21397)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If I have the same smell as Sora, Ryouhei might comeÅ^sniffing at me agressively, how scary.")
	hitretResult := Hitret(21398)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopSe()
	SetCg("B07c", 0, 0)
	SetBustup("CC02_02M", 0, 0)
	Transition("WIP_BT", 500)
	ScPlayBgm("BGM09", 0)
	Talk("Akira", "AK020727")
	Mess("Mmmph! Thank you!")
	hitretResult := Hitret(21399)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, I'm thankful for a lot of things.")
	hitretResult := Hitret(21400)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020728")
	Mess("Ah, this is a rare moment, come in and have tea! AsÅ^thanks for carrying me up.")
	hitretResult := Hitret(21401)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh...... but......")
	hitretResult := Hitret(21402)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK020729")
	Mess("There's no one else, you don't need to think so much!")
	hitretResult := Hitret(21403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm...... then, I'll come in for a bit.")
	hitretResult := Hitret(21404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume pulls my hand and leads me to the house behindÅ^the shrine.")
	hitretResult := Hitret(21405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("Akira", "AK020730")
	Mess("I'm home!!")
	hitretResult := Hitret(21406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Excuse me for disturbing~")
	hitretResult := Hitret(21407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B08c", 0, 0)
	SetBustup("CC02_02M", 0, 0)
	Transition("WIP_LR", 500)
	Talk("Akira", "AK020731")
	Mess("Just help yourself and sit anywhere, I'll bring out theÅ^tea. Ah! I need to prepare snacks too.")
	hitretResult := Hitret(21408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Amatsume energetically went into the kitchen.")
	hitretResult := Hitret(21409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... She's really, staying here all by herself......")
	hitretResult := Hitret(21410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After Amatsume left, the surroundings suddenly becameÅ^quiet.")
	hitretResult := Hitret(21411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There are only a few very small lit lamps outside, andÅ^the other areas are pitch black out there.")
	hitretResult := Hitret(21412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume turns on the light, and the room turns bright,Å^but there's no presence of anyone else.")
	hitretResult := Hitret(21413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("No other family to greet her.")
	hitretResult := Hitret(21414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume is living in this place all by herself.")
	hitretResult := Hitret(21415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Although she had dinner with Yaruhi-san, but after thatÅ^from the second morning onwards, Amatsume is all byÅ^herself.")
	hitretResult := Hitret(21416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Is this the ramification after getting close to someoneÅ^else?")
	hitretResult := Hitret(21417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I have never seen her show a lonely expression evenÅ^once, so I felt inclined to know what expression she hasÅ^living here.")
	hitretResult := Hitret(21418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK020732")
	Mess("Sorry for the long wait! Here, barley tea is fine right?Å^If you like it warm, you can pour from here.")
	hitretResult := Hitret(21419)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks, this is good enough.")
	hitretResult := Hitret(21420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020733")
	Mess("What's wrong? Here, take a seat.")
	hitretResult := Hitret(21421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Mmm. Ah, then itadakimasu.")
	hitretResult := Hitret(21422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020734")
	Mess("Here, these are the remaining snacks.")
	hitretResult := Hitret(21423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hey......")
	hitretResult := Hitret(21424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK020735")
	Mess("Hmm? What is it?")
	hitretResult := Hitret(21425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There's only Amatsume here and no one else, and it'sÅ^really dark here, and it's really quiet. Aren't youÅ^scared?")
	hitretResult := Hitret(21426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020736")
	Mess("Mmm~ It has always been like that, and I got used to it.Å^There's no ghost here so I don't need to be scared.")
	hitretResult := Hitret(21427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020737")
	Mess("Haru-kun and Sora-chan together, so you wouldn't feelÅ^lonely right?")
	hitretResult := Hitret(21428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sometimes I don't really understand it too......")
	hitretResult := Hitret(21429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The time during back then in the mansion, even though weÅ^had the funeral, I still feel it sometimes \"Are Dad andÅ^the others really gone\", something like this.")
	hitretResult := Hitret(21430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("And sometimes I feel, did they really got into anÅ^accident, maybe they are just working overseas rightÅ^now, and they will still come back...")
	hitretResult := Hitret(21431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Therefore, I feel they are just around me, so I don'tÅ^really understand......")
	hitretResult := Hitret(21432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK020738")
	Mess("Your parents are by your side...... even in the future.")
	hitretResult := Hitret(21433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh.....")
	hitretResult := Hitret(21434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020739")
	Mess("Feeling pained, feeling sad, it's because Haruka-kun'sÅ^parents are in Haruka's heart.")
	hitretResult := Hitret(21435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020740")
	Mess("That is why...... you are feeling their presence, andÅ^why you felt everything isn't real.")
	hitretResult := Hitret(21436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK020741")
	Mess("You're really kind, Haru-kun.")
	hitretResult := Hitret(21437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020742")
	Mess("You don't need to force yourself to accept it, isn't itÅ^better not to suffer over it?")
	hitretResult := Hitret(21438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020743")
	Mess("It's because if Haru-lun doesn't lose that feeling, theyÅ^will always be your side.")
	hitretResult := Hitret(21439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume gazed at my face and said gently.")
	hitretResult := Hitret(21440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm sorry...... I said some strange things. You evenÅ^comforted me.... thank you.")
	hitretResult := Hitret(21441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_10M", 0, 0)
	Talk("Akira", "AK020744")
	Mess("No, I'm also...... saying some whimsical things likeÅ^that.")
	hitretResult := Hitret(21442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, no...... hearing things like that, I feel sort ofÅ^refreshed.")
	hitretResult := Hitret(21443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_11M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("This might be the first time I told my feelings toÅ^another person.")
	hitretResult := Hitret(21444)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I have carried this incomprehensible feelings up to now.")
	hitretResult := Hitret(21445)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume, is probably trying to bring my emotions to aÅ^better direction.")
	hitretResult := Hitret(21446)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("How about Amatsume? Staying here all by yourself......Å^Aren't you lonely?")
	hitretResult := Hitret(21447)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK020745")
	Mess("No...... I'm not lonely.")
	hitretResult := Hitret(21448)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020746")
	Mess("It's true that I'm alone during the night, but sometimesÅ^Teacher is around, and Kazu-chan and Ryou-onii chan willÅ^call sometimes.")
	hitretResult := Hitret(21449)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK020747")
	Mess("Also, I can see everyone the next day.")
	hitretResult := Hitret(21450)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("Amatsume is really positive. Is it something likeÅ^leaving home, and living by yourself?")
	hitretResult := Hitret(21451)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM16", 0)
	SetBustup("CC02_07M", 0, 0)
	Talk("Akira", "AK020748")
	Mess("No..... I'm a little strange.")
	hitretResult := Hitret(21452)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume stuck out her tongue, slightly shy.")
	hitretResult := Hitret(21453)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020749")
	Mess("It's because everyone said I don't have any father andÅ^mother, so I just thought it that way.")
	hitretResult := Hitret(21454)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020750")
	Mess("Since I didn't have anything from the start, so I don'tÅ^feel lonely at all.")
	hitretResult := Hitret(21455)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("ÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(21456)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020751")
	Mess("When grandpa died, I felt really sad...... but I didn'tÅ^feel lonely.")
	hitretResult := Hitret(21457)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_05M", 0, 0)
	Talk("Akira", "AK020752")
	Mess("It must...... really be strange......")
	hitretResult := Hitret(21458)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_07M", 0, 0)
	Talk("Akira", "AK020753")
	Mess("Maybe it's because, I'm the last person left, so I don'tÅ^feel lonely?")
	hitretResult := Hitret(21459)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I thought, because Amatsume had everyone surroundingÅ^her, giving her a lot of love, and so she wouldn't feelÅ^lonely")
	hitretResult := Hitret(21460)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But...... Amatsume believes that she's alone from theÅ^start, and views it as a natual thing, so she wouldn'tÅ^feel lonely?")
	hitretResult := Hitret(21461)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And so, even from now...... no matter how she has funÅ^and smile, that loneliness from her back?")
	hitretResult := Hitret(21462)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("....... No.")
	hitretResult := Hitret(21463)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Amatsume knows it.")
	hitretResult := Hitret(21464)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020754")
	Mess("Eh.....")
	hitretResult := Hitret(21465)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Because you understand loneliness, so you treasure theÅ^relationship from everyone.")
	hitretResult := Hitret(21466)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Everyone likes Amatsume. Because their feelings areÅ^transmitted over, you treasure them.")
	hitretResult := Hitret(21467)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Amatsume isn't strange. Because you are in touch withÅ^everyone, you don't feel lonely.")
	hitretResult := Hitret(21468)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_11M", 0, 0)
	Talk("Akira", "AK020755")
	Mess("I-Is that so......")
	hitretResult := Hitret(21469)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes, everyone likes Amatsume, and because of that youÅ^are always smiling. With everyone by your side, youÅ^won't feel lonely.")
	hitretResult := Hitret(21470)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_05M", 0, 0)
	Talk("Akira", "AK020756")
	Mess("Ehe... Hehe... Stop that..... Haru-kan...... Don'tÅ^praise me like that......")
	hitretResult := Hitret(21471)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020757")
	Mess("Everyone likes me.... I feel really happy......")
	hitretResult := Hitret(21472)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_11M", 0, 0)
	Talk("Akira", "AK020758")
	Mess("Ah, soo, Haru-kun. Haru-kun do you like me?")
	hitretResult := Hitret(21473)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh!?")
	hitretResult := Hitret(21474)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt confused at this instant. This should beÅ^something in the manner of good will, and not reallyÅ^about romantic love......")
	hitretResult := Hitret(21475)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume gazes at me, smiling at me.")
	hitretResult := Hitret(21476)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It probably..... doesn't mean something else......")
	hitretResult := Hitret(21477)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_08M", 0, 0)
	Talk("Akira", "AK020759")
	Mess("Ah...... dislike?")
	hitretResult := Hitret(21478)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No... It's not like that.")
	hitretResult := Hitret(21479)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just saying it straight should be okay......")
	hitretResult := Hitret(21480)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("L..... Like.")
	hitretResult := Hitret(21481)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_11M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("If it flows in this direction, it probably is the sameÅ^for everyone else? Something like that.....")
	hitretResult := Hitret(21482)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My own feelings, are probably a bigger plus than theÅ^others.")
	hitretResult := Hitret(21483)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_03M", 0, 0)
	Talk("Akira", "AK020760")
	Mess("Aha, I also really like Haru-kun!")
	hitretResult := Hitret(21484)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm glad..... I'm not hated by Amatsume.")
	hitretResult := Hitret(21485)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK020761")
	Mess("Eh..... Why's that?")
	hitretResult := Hitret(21486)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume looks intently at me in curiosity.")
	hitretResult := Hitret(21487)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK020762")
	Mess("Haru-kun...... is special.")
	hitretResult := Hitret(21488)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Special?")
	hitretResult := Hitret(21489)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK020763")
	Mess("Yes...... because, you returned back here once more.")
	hitretResult := Hitret(21490)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Now that she mentioned it, that time was also......")
	hitretResult := Hitret(21491)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("SP08", 0, 0)
	Flush("WHITE", 2000, "")
	Talk("Akira", "AK020764")
	Mess("....Sayohime-sama..... Haru-kun came back.....")
	hitretResult := Hitret(21492)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She became happy over our reunion.")
	hitretResult := Hitret(21493)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B08c", 0, 0)
	SetBustup("CC02_01M", 0, 0)
	Transition("CLOUD_A", 1000)
	Talk("Akira", "AK020765")
	Mess("I'm really happy. It is the like the first time I metÅ^Haru-kun..... That summer break continuation, is aboutÅ^to begin once more.")
	hitretResult := Hitret(21494)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK020766")
	Mess("Just like a miracle, we can play together a whole lotÅ^again.")
	hitretResult := Hitret(21495)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020767")
	Mess("So, to me, Haru-kun is really special.")
	hitretResult := Hitret(21496)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm not feeling bad on hearing something like \"I'mÅ^special\". I'm overjoyed, and my face feels a littleÅ^ticklish.")
	hitretResult := Hitret(21497)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Therefore even more from now, I will like her. TheÅ^things about Amatsume, I will cherish it even more.")
	hitretResult := Hitret(21498)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Amatsume is also really special, to me.")
	hitretResult := Hitret(21499)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_10M", 0, 0)
	Talk("Akira", "AK020768")
	Mess("Ehh......")
	hitretResult := Hitret(21500)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah..... erm....... the feelings Amatsume have for me, isÅ^the same.......")
	hitretResult := Hitret(21501)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020769")
	Mess("The same as me......?")
	hitretResult := Hitret(21502)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I said it out spontaneously, and regretted it slightly.")
	hitretResult := Hitret(21503)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("\"We are the same!\", that's what I thought Amatsume willÅ^happily say. But Amatsume's surprised reaction wasÅ^contrary to my expectations.")
	hitretResult := Hitret(21504)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ehh...... Did I say something unnecessary?")
	hitretResult := Hitret(21505)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_08M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Amatsume's face turned crimson red and lowered her head.")
	hitretResult := Hitret(21506)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("....... Ehhh...... Amatsume's meaning of \"special\" isÅ^......")
	hitretResult := Hitret(21507)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("......S-surely..... the same...... as me?")
	hitretResult := Hitret(21508)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_11M", 0, 0)
	Talk("Akira", "AK020770")
	Mess("...... Haru-kun.......")
	hitretResult := Hitret(21509)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("ÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(21510)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020771")
	Mess("Haru-kun feels...... the same as me?")
	hitretResult := Hitret(21511)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsme looked up at me with teary eyes. I wasÅ^mesmerized by her eyes and unable to move away fromÅ^them.")
	hitretResult := Hitret(21512)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her meaning and my meaning of \"special\"...... is itÅ^really the same thing?")
	hitretResult := Hitret(21513)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The question that she wanted to verify was abruptlyÅ^thrown at me, and I'm at a loss on how to reply.")
	hitretResult := Hitret(21514)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If...... I am wrong......")
	hitretResult := Hitret(21515)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What would...... Amatsume think of me.....?")
	hitretResult := Hitret(21516)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... Amatsume.......")
	hitretResult := Hitret(21517)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I can't lie to her about the feelings that grew from myÅ^heart,.")
	hitretResult := Hitret(21518)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It can't be helped if they are wronglyÅ^misrepresented......")
	hitretResult := Hitret(21519)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even if Amatsume's meaning of \"special\" is different, myÅ^feelings won't change.")
	hitretResult := Hitret(21520)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("So......")
	hitretResult := Hitret(21521)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	ScPlaySe("SE156", 0)
	SetBustup("CC02_12M", 0, 0)
	Jump(2, 0)
	Talk("Akira", "AK020772")
	Mess("Uuu!?")
	hitretResult := Hitret(21522)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!!!!!")
	hitretResult := Hitret(21523)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I was determined to answer in that instant, theÅ^melody from the mobile phone began to rang.")
	hitretResult := Hitret(21524)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_05M", 0, 0)
	Talk("Akira", "AK020773")
	Mess("Ahahaha...... Go ahead, Haru-kun.")
	hitretResult := Hitret(21525)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, okay......")
	hitretResult := Hitret(21526)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I took the mobile phone out of my pocket, and looked atÅ^the display screen.")
	hitretResult := Hitret(21527)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah...... It's from Sora.... Uuuu.......")
	hitretResult := Hitret(21528)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020774")
	Mess("What is it, what is it?")
	hitretResult := Hitret(21529)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The mobile phone's message only has a \"Slow\" written onÅ^it.")
	hitretResult := Hitret(21530)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM09", 0)
	SetBustup("CC02_05M", 0, 0)
	Talk("Akira", "AK020775")
	Mess("Ah, I'm sorry, I kept you for so long.")
	hitretResult := Hitret(21531)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No...... I")
	hitretResult := Hitret(21532)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We smiled wryly while walking out, as if to brush awayÅ^the awkward atmosphere.")
	hitretResult := Hitret(21533)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B07c", 0, 0)
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK020776")
	Mess("Haru-kun, thanks for sending me back.")
	hitretResult := Hitret(21534)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No problem, Amatsume, you look really tired, you shouldÅ^take a bath and have a good rest.")
	hitretResult := Hitret(21535)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK020777")
	Mess("Yeah. Haru-kun too, be careful when taking a bath andÅ^drown, okay?")
	hitretResult := Hitret(21536)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes, I'll be careful.")
	hitretResult := Hitret(21537)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though there are lights are outside Amatsume'sÅ^house, but the grounds leading to the descending stairsÅ^is in complete darkness.")
	hitretResult := Hitret(21538)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I take out my mobile phone again, and use the back lightÅ^to substitute it as a torchlight.")
	hitretResult := Hitret(21539)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020778")
	Mess("Ah, it's dangerous in the darkness, I'll go down withÅ^you.")
	hitretResult := Hitret(21540)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... But if that's the case, sending you back wouldn'tÅ^mean anything......")
	hitretResult := Hitret(21541)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_03M", 0, 0)
	Talk("Akira", "AK020779")
	Mess("It's fine, it's fine! Come on, Haru-kun let's go now!?")
	hitretResult := Hitret(21542)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... Ah..... Okay......")
	hitretResult := Hitret(21543)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	SetCg("B28d", 0, 0)
	Talk("Haruka", "")
	Mess("Amatsume...... are you fine even if there's no light?")
	hitretResult := Hitret(21544)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020780")
	Mess("Yes, I pass through here every day, so even if it's aÅ^little dark it's fine. It's completely dark every time IÅ^come back from Nee-chan's home.")
	hitretResult := Hitret(21545)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume seems to be really able to see her way,Å^progressing quickly.")
	hitretResult := Hitret(21546)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just to safe, I continue to use the mobile phone as aÅ^substitude for light, and under that weak light,Å^Amatsume is smiling.")
	hitretResult := Hitret(21547)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Is this really okay......")
	hitretResult := Hitret(21548)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora sent a mail, and our conversation was disrupted.")
	hitretResult := Hitret(21549)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Perhaps I will find out Amatsume's real feelings ......Å^Right at this instant, I felt slightly afraid.")
	hitretResult := Hitret(21550)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That moment...... What did Amatsume think about? Was theÅ^meaning of my \"special\" and her \"special\"...... same.")
	hitretResult := Hitret(21551)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020781")
	Mess("Haru-kun...... It's dangerous, you know?")
	hitretResult := Hitret(21552)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh...... Ah......")
	hitretResult := Hitret(21553)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She suddenly held my hand.")
	hitretResult := Hitret(21554)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Then Amatsume pulled my hand, and descended down theÅ^stairs.")
	hitretResult := Hitret(21555)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A really small and warm hand leading me. The certainÅ^footsteps in the darkness made me I can rely on her.")
	hitretResult := Hitret(21556)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I firmly held her hand. And Amatsume did the same.")
	hitretResult := Hitret(21557)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We are almost like lovers, holding hands and descendingÅ^down the stairs quickly.")
	hitretResult := Hitret(21558)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020782")
	Mess("We are almost there, watch you feet okay? There!!")
	hitretResult := Hitret(21559)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Woah!?")
	hitretResult := Hitret(21560)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume jumped down from the last step.")
	hitretResult := Hitret(21561)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B34c", 0, 0)
	SetBustup("CC02_01M", 0, 0)
	Transition("WIP_TB", 500)
	Talk("Akira", "AK020783")
	Mess("All right, we reached!")
	hitretResult := Hitret(21562)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With her pull, I too jumped beside Amatsume.")
	hitretResult := Hitret(21563)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahaha..... Thank you for sending me.")
	hitretResult := Hitret(21564)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK020784")
	Mess("It's also dark on the roads, be careful when you goÅ^home, okay?")
	hitretResult := Hitret(21565)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes, understood.")
	hitretResult := Hitret(21566)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I let go Amatsume's connected hand. In that second, myÅ^hand turns cold.")
	hitretResult := Hitret(21567)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, it's like I had lost Amatsume's warmth, and I feltÅ^slightly disappointed.")
	hitretResult := Hitret(21568)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK020785")
	Mess("Bye bye, see you again.")
	hitretResult := Hitret(21569)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes, good night.")
	hitretResult := Hitret(21570)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though I want stay with her more, I wave my handÅ^and walk towards my home.")
	hitretResult := Hitret(21571)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("I walk a distance before turning my head back to look.Å^Amatsume's facing me, skipping and waving her hand.")
	hitretResult := Hitret(21572)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("DATE", 0)
	Change("00_c016")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

