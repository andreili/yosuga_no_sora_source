Include"yosuga.ch"

Function sc00_D026()
	ScenarioEnter()
	ScPlayBgm("BGM05", 0)
	SetCg("B08a", 0, 0)
	SetBustup("CB02_01S", 0, 0)
	SetBustup("CC06_03S", 0, 0)
	SetBustup("CD02_03S", 0, 0)
	SetBustup("CF03_01S", 0, 0)
	Talk("Haruka", "")
	Mess("We're back, everyone.")
	hitretResult := Hitret(31096)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The next day, I called everyone together, saying I hadÅ^souvenirs for them.")
	hitretResult := Hitret(31097)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The location was Amatsume's place, of course.")
	hitretResult := Hitret(31098)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040305")
	Mess("Come on, let's leave the introductory remarks there andÅ^start eating!")
	hitretResult := Hitret(31099)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO040081")
	Mess("You...Well, I do understand that feeling. I am lookingÅ^forward to Haru-chan's souvenir.")
	hitretResult := Hitret(31100)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040388")
	Mess("Sweets? Sweets? What kind? What kind?")
	hitretResult := Hitret(31101)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040923")
	Mess("Akira, clam down a bit, you're getting a bit too hyper.")
	hitretResult := Hitret(31102)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040389")
	Mess("You weren't looking forward to Haru-kun's souvenir?")
	hitretResult := Hitret(31103)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040924")
	Mess("Of course I was. But before that, you have something toÅ^say, right?")
	hitretResult := Hitret(31104)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_02S", 0, 0)
	Talk("Nao", "NO040082")
	Mess("Oh, right....Welcome back, Haru-chan, Sora-chan.")
	hitretResult := Hitret(31105)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02S", 0, 0)
	Talk("Akira", "AK040390")
	Mess("Welcome back, Haru-kun, Sora-chan.")
	hitretResult := Hitret(31106)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040306")
	Mess("We appreicate your hard work! ...Eh, do you have to sayÅ^it too, Ojou?")
	hitretResult := Hitret(31107)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_10S", 0, 0)
	Talk("Kazuha", "KA040925")
	Mess("Eh?...O-Of course...W-Welcome back...Kasugano-kun...")
	hitretResult := Hitret(31108)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y-Yeah...thanks...")
	hitretResult := Hitret(31109)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Saying it twice...But I did get a bit shy, since it wasÅ^my first time saying it since we became a couple.")
	hitretResult := Hitret(31110)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_02S", 0, 0)
	Talk("Ryouhei", "RH040307")
	Mess("And now all that's been done! Haruka, bring out theÅ^goods!")
	hitretResult := Hitret(31111)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, yeah... Please accept my humble gift, yourÅ^majesty.")
	hitretResult := Hitret(31112)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("I respectfully placed the box of sweets on the table.")
	hitretResult := Hitret(31113)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("They were the same ones I showed Kazuha yesterday: 2Å^boxes of 12.")
	hitretResult := Hitret(31114)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I thought Ryouhei and Amatsume would eat a lot, so IÅ^bought more.")
	hitretResult := Hitret(31115)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_04M", 0, 0)
	Talk("Akira", "AK040391")
	Mess("Wah, it's a chicken! Cuuute!")
	hitretResult := Hitret(31116)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040308")
	Mess("Ooh, so this is that famous thing! Ohoo, it's my firstÅ^time seeing it, hurry up and open it!")
	hitretResult := Hitret(31117)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040392")
	Mess("Can I open them?")
	hitretResult := Hitret(31118)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yup, go ahead.")
	hitretResult := Hitret(31119)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	ScPlaySe("se064", 0)
	Talk("êSÇÃê∫", "")
	Mess("I passed her a box, which she quickly dug out from theÅ^wrapping.")
	hitretResult := Hitret(31120)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_01M", 0, 0)
	Talk("Nao", "NO040083")
	Mess("...Can't you do it a bit more politely?")
	hitretResult := Hitret(31121)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan sounded extremely surprised.")
	hitretResult := Hitret(31122)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA040926")
	Mess("Akira... I'll go get some tea.")
	hitretResult := Hitret(31123)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO040084")
	Mess("Then, I'll go help. Akira-chan, we'll be borrowing yourÅ^kitchen.")
	hitretResult := Hitret(31124)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(5)
	BustupClear(3)
	SetBustup("CC06_02M", 0, 0)
	SetBustup("CF03_02M", 0, 0)
	Talk("Akira", "AK040393")
	Mess("Yep...Waaah, the chicken's inside toooo.")
	hitretResult := Hitret(31125)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040309")
	Mess("Hohoo, looks rather tasty... Now, which one should IÅ^take...")
	hitretResult := Hitret(31126)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("At least wait until the tea comes.")
	hitretResult := Hitret(31127)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040394")
	Mess("Me too, me too!")
	hitretResult := Hitret(31128)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Not even being able to wait the short time for the teaÅ^to come, both of them took a piece in both hands andÅ^started eating.")
	hitretResult := Hitret(31129)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm glad that they're happy, but I do wonder...")
	hitretResult := Hitret(31130)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	BustupClear(7)
	SetBustup("CA02_01M", 0, 0)
	Talk("Haruka", "")
	Mess("We'll wait until the tea comes, okay?")
	hitretResult := Hitret(31131)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040133")
	Mess("...Huh?")
	hitretResult := Hitret(31132)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I looked toward Sora, but she'd already started eating.")
	hitretResult := Hitret(31133)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even if it's just me, I'll wait for Kazuha...")
	hitretResult := Hitret(31134)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B08a", 0, 0)
	SetBustup("CB02_06M", 0, 0)
	SetBustup("CD02_02M", 0, 0)
	Talk("Nao", "NO040085")
	Mess("Tch, Double Akira, you can't eat before us!")
	hitretResult := Hitret(31135)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040927")
	Mess("Let them off just this once, Ha...Kasugano-kun's sweetsÅ^look nice, so they probably couldn't wait.")
	hitretResult := Hitret(31136)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040928")
	Mess("Akira... Geez, you're getting it everywhere becauseÅ^you're holding two.")
	hitretResult := Hitret(31137)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(5)
	Talk("êSÇÃê∫", "")
	Mess("Kazuha took out her handkerchief and wiped the fragmentsÅ^off the side of her mouth, as well as the ones on theÅ^table.")
	hitretResult := Hitret(31138)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While she was doing that, Akira childishly stuck out herÅ^lips with an "Mmmmmm," making Kazuha seem like a motherÅ^cleaning up after a child.")
	hitretResult := Hitret(31139)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But even seeing the two like that, I wasn't jealous atÅ^all.")
	hitretResult := Hitret(31140)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Because we were a couple, even though no one else knew.")
	hitretResult := Hitret(31141)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_02M", 0, 0)
	Talk("Nao", "NO040086")
	Mess("What's up, Haru-chan? You seem happy for some reason.")
	hitretResult := Hitret(31142)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? R-Really?")
	hitretResult := Hitret(31143)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Crap... It shows on my face, huh....?")
	hitretResult := Hitret(31144)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("M-Maybe it's because the tea's arrived?")
	hitretResult := Hitret(31145)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_13M", 0, 0)
	Talk("Nao", "NO040087")
	Mess("You were actually waiting!? Sorry, I'll pour it now.")
	hitretResult := Hitret(31146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA040929")
	Mess("Here... Your cup, Kasugano-kun.")
	hitretResult := Hitret(31147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Seeing Nao-chan start handing out the tea, KazuhaÅ^quickly folded up her handkerchief and passed out theÅ^cups.")
	hitretResult := Hitret(31148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("T-Thanks...")
	hitretResult := Hitret(31149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040930")
	Mess("No............... You're welcome.")
	hitretResult := Hitret(31150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I took the cup, our fingers touched.")
	hitretResult := Hitret(31151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Yesterday, those fingers were entwined as we joinedÅ^hands...")
	hitretResult := Hitret(31152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_11M", 0, 0)
	Talk("Kazuha", "KA040931")
	Mess("............")
	hitretResult := Hitret(31153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Our eyes met, ad we looked at each other.")
	hitretResult := Hitret(31154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Forgetting about the heat of the teacup, we...")
	hitretResult := Hitret(31155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF03_06M", 0, 0)
	SetBustup("CC06_12M", 0, 0)
	Talk("Ryouhei", "RH040310")
	Mess("WHOAAA!? HOT PASSION LOVE LOVE FLARREEE!!!")
	hitretResult := Hitret(31156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040395")
	Mess("WHOOOOOAAAA!!!")
	hitretResult := Hitret(31157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD02_12M", 0, 0)
	Talk("Kazuha", "KA040932")
	Mess("Kya!...W-What...?")
	hitretResult := Hitret(31158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With the sudden onslaught of loud voices, we jerked ourÅ^hands away in surprise.")
	hitretResult := Hitret(31159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_02M", 0, 0)
	SetBustup("CC06_03M", 0, 0)
	Talk("Ryouhei", "RH040311")
	Mess("Nothing, just noticing some awful amount of intimacy inÅ^the air.")
	hitretResult := Hitret(31160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040396")
	Mess("And I was just doing it because Ryou-niichan made aÅ^weird noiseÅ`")
	hitretResult := Hitret(31161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("W-We weren't really being...intimate...")
	hitretResult := Hitret(31162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040312")
	Mess("No, no, I'm happy for you! That late bloomer Haruka hasÅ^finally awakened to LOVE!")
	hitretResult := Hitret(31163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040313")
	Mess("And the target was that Ojou! Way to go for the money.")
	hitretResult := Hitret(31164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040397")
	Mess("For the money! For the money!")
	hitretResult := Hitret(31165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_04M", 0, 0)
	Talk("Kazuha", "KA040933")
	Mess("...Akira, do you even know what you're saying?")
	hitretResult := Hitret(31166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040398")
	Mess("Eeeeh?")
	hitretResult := Hitret(31167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Doesn't look like it...")
	hitretResult := Hitret(31168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_05M", 0, 0)
	Talk("Ryouhei", "RH040314")
	Mess("So, how is it from the Ojou's side? Don't tell me thatÅ^it's mutual?")
	hitretResult := Hitret(31169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei was wearing an indecent but extremely broadÅ^grin... He was definitely teasing us.")
	hitretResult := Hitret(31170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040934")
	Mess("...Yeah yeah, who cares about that? So please, grab yourÅ^tea.")
	hitretResult := Hitret(31171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA040935")
	Mess("You too, Akira... But don't drink it right away, it'sÅ^hot.")
	hitretResult := Hitret(31172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK040399")
	Mess("Yeees, thanks, Kazu-chan.")
	hitretResult := Hitret(31173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_02M", 0, 0)
	Talk("Ryouhei", "RH040315")
	Mess("Thanks, Kazu-chan!Åô")
	hitretResult := Hitret(31174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA02_01M", 0, 0)
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA040936")
	Mess("You too, Sora-san.")
	hitretResult := Hitret(31175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040134")
	Mess("...Thanks.")
	hitretResult := Hitret(31176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF03_10M", 0, 0)
	Talk("Ryouhei", "RH040316")
	Mess("I-Ignoring me, huh?....That Ojou, when did she learnÅ^such a high-level technique...")
	hitretResult := Hitret(31177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Would you really call it high-level?")
	hitretResult := Hitret(31178)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040317")
	Mess("But it's the Ojou!Å^I thought I'd get definitely get attacked for sayingÅ^that.")
	hitretResult := Hitret(31179)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You...really think so?")
	hitretResult := Hitret(31180)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't know about now, but I think the old Kazuha wouldÅ^have at least kicked him, just as he said.")
	hitretResult := Hitret(31181)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Maybe she has changed a bit...")
	hitretResult := Hitret(31182)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD02_03M", 0, 0)
	Talk("Kazuha", "KA040937")
	Mess("...Ah...........Eheh.")
	hitretResult := Hitret(31183)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Okay, she has. Yesterday's events... Starting to go outÅ^with me was probably the trigger.")
	hitretResult := Hitret(31184)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK040400")
	Mess("Whaa, this chicken tastes nice! Can I have another one?")
	hitretResult := Hitret(31185)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There's a lot, so eat as much as you want.")
	hitretResult := Hitret(31186)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040401")
	Mess("Thanks....! *gobble**gobble**gobble*")
	hitretResult := Hitret(31187)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA040938")
	Mess("Ah geez, Akira, it's going everywhere again... ThereÅ^really is no helping you...")
	hitretResult := Hitret(31188)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF03_06M", 0, 0)
	Talk("Ryouhei", "RH040318")
	Mess("Umumu... Is this the Ojou? The fashionable Ojou 2.0!?")
	hitretResult := Hitret(31189)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("2.0 would be...rather old, no?")
	hitretResult := Hitret(31190)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040319")
	Mess("Eh, really? I heard it was a recent Paradise Lost?")
	hitretResult := Hitret(31191)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_08M", 0, 0)
	Talk("Nao", "NO040088")
	Mess("You mean "Paradigm Shift." Besides, do you even knowÅ^what "2.0" means?")
	hitretResult := Hitret(31192)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_03M", 0, 0)
	Talk("Ryouhei", "RH040320")
	Mess("For now, Nao would be 0.5!")
	hitretResult := Hitret(31193)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_03M", 0, 0)
	Talk("Nao", "NO040089")
	Mess("Hohoo, would you happen to know what that means as well?")
	hitretResult := Hitret(31194)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040321")
	Mess("Hahaha! Of course it refers to feminini-...Omph!")
	hitretResult := Hitret(31195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se007", 0)
	Shake(7)
	Update()
	ScWait(300, 0)
	BustupLeave(7, 0, 200, 1, 300, 4)
	ScPlaySe("se018", 0)
	Talk("êSÇÃê∫", "")
	Mess("Without even a blink, the smiling Nao-chan gave him aÅ^low kick.")
	hitretResult := Hitret(31196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_01M", 6, 0)
	Talk("Nao", "NO040090")
	Mess("Always saying stupid things...")
	hitretResult := Hitret(31197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Do you know what that means, Nao-chan? "2.0?"")
	hitretResult := Hitret(31198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB02_13M", 0, 0)
	Talk("Nao", "NO040091")
	Mess("Okay okay, time for me to eat one of these chickens,Å^toooo.")
	hitretResult := Hitret(31199)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Ah, she fled.")
	hitretResult := Hitret(31200)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA040939")
	Mess("Kazugano-kun, you haven't eaten any yet, right? If youÅ^don't eat one soon, Akira's going to down them all.")
	hitretResult := Hitret(31201)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Guess we can't have that. That'd be wasting the tea youÅ^gave me.")
	hitretResult := Hitret(31202)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040940")
	Mess("Yep, yep. So, let's eat together.")
	hitretResult := Hitret(31203)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Mmm, okay... Let's eat.")
	hitretResult := Hitret(31204)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040941")
	Mess("Yeah.... Hehe...")
	hitretResult := Hitret(31205)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha.... Mmm, it's the first time I've eaten this, butÅ^it's not bad at all.")
	hitretResult := Hitret(31206)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_12M", 0, 0)
	Talk("Kazuha", "KA040942")
	Mess("You haven't eaten any before!?")
	hitretResult := Hitret(31207)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It seemed to be exclusive for souvenir usage.")
	hitretResult := Hitret(31208)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_03M", 0, 0)
	Talk("Kazuha", "KA040943")
	Mess("Hehe... It's a good thing you moved here, huh?")
	hitretResult := Hitret(31209)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, really.")
	hitretResult := Hitret(31210)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It really is...if I think that moving here meant gettingÅ^this lifestyle that's so happy it's ticklish.")
	hitretResult := Hitret(31211)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I haven't really done anything up until now, but I canÅ^at least believe that this was the right choice.")
	hitretResult := Hitret(31212)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, that's because....")
	hitretResult := Hitret(31213)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I hadn't yet realized that it didn't mean something hadÅ^changed fundamentally.")
	hitretResult := Hitret(31214)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	Change("00_d027")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

