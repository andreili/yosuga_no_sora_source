Include"yosuga.ch"

Function sc00_D006()
	ScenarioEnter()
	SetCg("B17a", 0, 0)
	Update()
	ScPlaySe("SE352", 0)
	ScWait(3000, 0)
	SetCg("B19a", 0, 0)
	SetBustup("CF01_02M", 0, 0)
	Transition("WIP_TB", 500)
	ScPlayBgm("BGM03", 0)
	Talk("Ryouhei", "RH040051")
	Mess("Today's finally over!! Let's go home, Akira!")
	hitretResult := Hitret(27362)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_03M", 0, 0)
	Talk("Akira", "AK040060")
	Mess("Yup, yup! Kazu-chan, Haru-kun, you too!")
	hitretResult := Hitret(27363)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA040109")
	Mess("Me too? Why...?")
	hitretResult := Hitret(27364)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, wait a moment, I'll call Sora over.")
	hitretResult := Hitret(27365)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF01_04M", 0, 0)
	Talk("Ryouhei", "RH040052")
	Mess("Oh! Time to change to my irresistibly cool and reliableÅ^self!")
	hitretResult := Hitret(27366)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B17a", 0, 0)
	Transition("WIP_LR", 500)
	Update()
	ScWait(1000, 0)
	BlackOut(0, 0)
	SetCg("B12a", 0, 0)
	SetBustup("CA01_01S", 0, 0)
	SetBustup("CC01_01S", 0, 0)
	SetBustup("CD01_01S", 0, 0)
	SetBustup("CF01_01S", 0, 0)
	Transition("WIP_LR", 500)
	Talk("êSÇÃê∫", "")
	Mess("It's been a week since Sora started going to school.")
	hitretResult := Hitret(27367)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She's been able to cope with her classes on her ownÅ^without any problems.")
	hitretResult := Hitret(27368)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's just that we're together on the way to school, ofÅ^course. But she also comes to our classroom for lunch,Å^so it's a bit delicate when you ask about her makingÅ^friends.")
	hitretResult := Hitret(27369)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Well, just going to school is a step forward for her, soÅ^moving at her own pace would probably benefit her asÅ^well, I think.")
	hitretResult := Hitret(27370)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_10S", 0, 0)
	Talk("Ryouhei", "RH040053")
	Mess("It's reaally hot...and it's only the beginning ofÅ^summer.")
	hitretResult := Hitret(27371)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_07S", 0, 0)
	Talk("Sora", "SR040029")
	Mess("......")
	hitretResult := Hitret(27372)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Watching Ryouhei fanning himself with his hand, SoraÅ^nods silently.")
	hitretResult := Hitret(27373)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_08S", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Perhaps because everyone else thought the same, they allÅ^droped their shoulders as if they were dead tired.")
	hitretResult := Hitret(27374)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("This place is closer to the mountains, so I personallyÅ^think it's much cooler than in the city, but...")
	hitretResult := Hitret(27375)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040054")
	Mess("Maybe this year's special. The temperature is muchÅ^higher compared to last year.")
	hitretResult := Hitret(27376)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02S", 0, 0)
	Talk("Akira", "AK040061")
	Mess("Well thenÅ`Let's all go have some ice cream!")
	hitretResult := Hitret(27377)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02S", 0, 0)
	Talk("Ryouhei", "RH040055")
	Mess("Oh? Good idea there, Akira!")
	hitretResult := Hitret(27378)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040110")
	Mess("That's fine, but I don't think that Takano is still inÅ^town...")
	hitretResult := Hitret(27379)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040062")
	Mess("Not thatÅ` I'm talking about Hiro-neechan's place.")
	hitretResult := Hitret(27380)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Come to think of it, there were some ice cream sticksÅ^sold at that candy stand, right?")
	hitretResult := Hitret(27381)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01S", 0, 0)
	Talk("Sora", "SR040030")
	Mess("...Candy?")
	hitretResult := Hitret(27382)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040056")
	Mess("Ohh? Sora-chan hasn't gone before, eh? Well then, let meÅ^show you the way!")
	hitretResult := Hitret(27383)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_13S", 0, 0)
	Talk("Sora", "SR040031")
	Mess("Hii...!")
	hitretResult := Hitret(27384)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	Talk("Haruka", "")
	Mess("...You're frightening her!")
	hitretResult := Hitret(27385)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_03S", 0, 0)
	Talk("Kazuha", "KA040111")
	Mess("Hah...")
	hitretResult := Hitret(27386)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_04S", 0, 0)
	Talk("Ryouhei", "RH040057")
	Mess("Looks like I've done it now, my irresistible looks haveÅ^made all the girls timid.")
	hitretResult := Hitret(27387)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040063")
	Mess("Sora-chaaan, Ryou-niichan isn't that scary!")
	hitretResult := Hitret(27388)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA01_01S", 0, 0)
	Talk("Sora", "SR040032")
	Mess("...Gross.")
	hitretResult := Hitret(27389)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC01_05S", 0, 0)
	SetBustup("CD01_01S", 0, 0)
	SetBustup("CF01_01S", 0, 0)
	Talk("Akira", "AK040064")
	Mess("Ahahaha! That's probably trueÅ`")
	hitretResult := Hitret(27390)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040058")
	Mess("My looks do have opposite effects as well, I suppose!")
	hitretResult := Hitret(27391)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040112")
	Mess("Being involved like this, I really do feel sorry forÅ^Sora-san...")
	hitretResult := Hitret(27392)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("W-Well, it's not like she doesn't find it fun.")
	hitretResult := Hitret(27393)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I think it's much better than closing off her heart andÅ^shutting herself inside.")
	hitretResult := Hitret(27394)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR040033")
	Mess("...Haru, ice cream...")
	hitretResult := Hitret(27395)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("All right then, since today's weather is quite hot.")
	hitretResult := Hitret(27396)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040034")
	Mess("...Mm.")
	hitretResult := Hitret(27397)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC01_01M", 0, 0)
	SetBustup("CD01_01M", 0, 0)
	SetBustup("CF01_01M", 0, 0)
	Talk("Haruka", "")
	Mess("Let's hurry up, everyone. If we go too slowly we mightÅ^get heat stroke.")
	hitretResult := Hitret(27398)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040059")
	Mess("In that case, I'll race you to the shop!")
	hitretResult := Hitret(27399)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040065")
	Mess("You're on! ...Ready, go!")
	hitretResult := Hitret(27400)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02S", 0, 0)
	Update()
	ScWait(500, 0)
	BustupClear(2)
	Talk("Kazuha", "KA040113")
	Mess("Ah, wait a moment, Akira....")
	hitretResult := Hitret(27401)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040060")
	Mess("Dammit, There's no way I'm gonna lose!!")
	hitretResult := Hitret(27402)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(7)
	Talk("êSÇÃê∫", "")
	Mess("Hurry...but I didn't say to run...")
	hitretResult := Hitret(27403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("How about you, Migiwa-san?")
	hitretResult := Hitret(27404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_07M", 0, 0)
	Talk("Kazuha", "KA040114")
	Mess("...I'm not running in this weather.")
	hitretResult := Hitret(27405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("R-Right? ...Sora's here as well, so maybe we should justÅ^walk faster.")
	hitretResult := Hitret(27406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("But sprinting under this scorching heat was definitelyÅ^gruelling.")
	hitretResult := Hitret(27407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As we finally approached the candy shop, the fatiguedÅ^Amatsume and Ryouhei were already on the ground, pantingÅ^wildly.")
	hitretResult := Hitret(27408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What in the world are they doing...")
	hitretResult := Hitret(27409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B15a", 0, 0)
	SetBustup("CA01_01S", 0, 0)
	SetBustup("CC01_07S", 0, 0)
	SetBustup("CD01_02S", 0, 0)
	SetBustup("CF01_10S", 0, 0)
	Transition("WIP_LR", 500)
	Talk("êSÇÃê∫", "")
	Mess("In the end, Migiwa-san and I had to drag Amatsume andÅ^Ryouhei for the rest of the journey until we finallyÅ^reached Yahiro-san's shop.")
	hitretResult := Hitret(27410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040061")
	Mess("Sorry, Haruka... I'm so glad to have a friend likeÅ^you...")
	hitretResult := Hitret(27411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's hot and tiring, so I'm going to let you down here.")
	hitretResult := Hitret(27412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040066")
	Mess("Ahaha... Sorry about that, Kazu-chan...")
	hitretResult := Hitret(27413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040115")
	Mess("I'll let this one slide, but please don't overdo itÅ^again, okay?")
	hitretResult := Hitret(27414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040067")
	Mess("Okay!")
	hitretResult := Hitret(27415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040116")
	Mess("If you'd only just answer me that way all of the time...")
	hitretResult := Hitret(27416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE118", 0)
	BustupClear()
	Talk("VoiceÅsÇ‚Ç–ÇÎÅt", "YH040011")
	Mess("...Oi, stop making such a big fuss in front of myÅ^store...")
	hitretResult := Hitret(27417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just as we arrived at the store, we could hear a lazyÅ^moan coming from inside the shop.")
	hitretResult := Hitret(27418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH040012")
	Mess("What, it's you guys? Geez, what's with this crowd ofÅ^people... Well, good timing. Akira.")
	hitretResult := Hitret(27419)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH040013")
	Mess("I'm going to sleep at the back, so tend the shop for me.")
	hitretResult := Hitret(27420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040068")
	Mess("Yup, okay!")
	hitretResult := Hitret(27421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_08M", 0, 0)
	Talk("Yahiro", "YH040014")
	Mess("And you guys, if you make too much noise and interruptÅ^my beauty sleep, I'll kill you. Understand?")
	hitretResult := Hitret(27422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Having said that, Yahiro-san lazily dragged her feet toÅ^the back of the shop and slammed the wooden door behindÅ^her.")
	hitretResult := Hitret(27423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("You could feel an ominous aura leaking out from thatÅ^huge door...")
	hitretResult := Hitret(27424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01S", 0, 0)
	SetBustup("CC01_01S", 0, 0)
	SetBustup("CD01_07S", 0, 0)
	SetBustup("CF01_01S", 0, 0)
	Talk("Haruka", "")
	Mess("It's best we remain quiet.")
	hitretResult := Hitret(27425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040062")
	Mess("Ahh...I think she'll really kill us if we're noisy.")
	hitretResult := Hitret(27426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040117")
	Mess("Geez, just what does she see Akira as...")
	hitretResult := Hitret(27427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040069")
	Mess("Hiro-neechan is the guardianÅ`")
	hitretResult := Hitret(27428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040035")
	Mess("...Guardian? Whose?")
	hitretResult := Hitret(27429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040070")
	Mess("Me, me! Ever since my grandpa died, Hiro-neechan's beenÅ^taking care of me!")
	hitretResult := Hitret(27430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040036")
	Mess("Hmmm....")
	hitretResult := Hitret(27431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	Talk("Kazuha", "KA040118")
	Mess("I know that! ...But she treats you like a handyman....")
	hitretResult := Hitret(27432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040071")
	Mess("Hey, hey, Kazu-chan! Vanilla or chocolate?")
	hitretResult := Hitret(27433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_05S", 0, 0)
	Talk("Kazuha", "KA040119")
	Mess("Wait a moment! I'm talking about you, so why are youÅ^already choosing the ice cream?")
	hitretResult := Hitret(27434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC01_06M", 0, 0)
	Talk("Akira", "AK040072")
	Mess("Hmm, which should I choose? Vanilla is good, but I alsoÅ^want to eat chocolate...")
	hitretResult := Hitret(27435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_04M", 0, 0)
	Talk("Kazuha", "KA040120")
	Mess("Both are bad. You'll just ruin your stomach.")
	hitretResult := Hitret(27436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK040073")
	Mess("It's okayÅ` Just the other day, Hiro-neechan and IÅ^survived the whole day with just ice cream.")
	hitretResult := Hitret(27437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040121")
	Mess("W-What, are you stupid!? Doing such crazy andÅ^meaningless things, geez...")
	hitretResult := Hitret(27438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA01_01M", 0, 0)
	SetBustup("CF01_01M", 0, 0)
	Talk("Haruka", "")
	Mess("As intimate as usual, those two.")
	hitretResult := Hitret(27439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040063")
	Mess("They're the icon in this area, after all. Well, weÅ^really should start choosing too.Å^Ah, I'll choose one for you. Okay, Sora?")
	hitretResult := Hitret(27440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR040037")
	Mess("......")
	hitretResult := Hitret(27441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	Talk("Haruka", "")
	Mess("She really hates you.")
	hitretResult := Hitret(27442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040064")
	Mess("No, no, she's just embarrassed. Don't worry, she'll beÅ^calling me \"Onii-sama\" soon.")
	hitretResult := Hitret(27443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...She doesn't even call me Onii-sama.")
	hitretResult := Hitret(27444)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But what are we going to do about payment? Yahiro-san'sÅ^not here.")
	hitretResult := Hitret(27445)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK040074")
	Mess("Payment? Oh! There's a \"Please pay here box\" over there,Å^so just put the money in there.")
	hitretResult := Hitret(27446)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume, who just happened to pass by, gestured towardÅ^the counter.")
	hitretResult := Hitret(27447)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("On the counter, there was a box-shaped can thing there.Å^I can't really describe its exact shape.")
	hitretResult := Hitret(27448)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("So you just put the money in there... This method ofÅ^payment is really screwed up.")
	hitretResult := Hitret(27449)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What happens if you need change...and how does oneÅ^actually keep track of everything that the customersÅ^have purchased...?")
	hitretResult := Hitret(27450)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040065")
	Mess("Guess I'll have a popsicle... What about you Haruka?")
	hitretResult := Hitret(27451)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'll have that too.")
	hitretResult := Hitret(27452)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Man, it's been a while since I had a popsicle. ThisÅ^really brings back memories.... I picked up a popsicleÅ^and dropped the coins in the \"Please pay here box.\"")
	hitretResult := Hitret(27453)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Luckily, I had just the right amount.")
	hitretResult := Hitret(27454)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK040075")
	Mess("Thank you, please come againÅ`")
	hitretResult := Hitret(27455)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF01_02M", 0, 0)
	SetBustup("CA01_01M", 0, 0)
	Talk("Ryouhei", "RH040066")
	Mess("What do you want, Sora-chan? Sweeet strawberry?Å^Or maybe cool mint? Or what about some shaved ice?")
	hitretResult := Hitret(27456)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040038")
	Mess("...Vanilla.")
	hitretResult := Hitret(27457)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ignoring all the cupped ice cream Ryouhei took out, SoraÅ^went for the simple cup.")
	hitretResult := Hitret(27458)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(7)
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR040039")
	Mess("Haru, money.")
	hitretResult := Hitret(27459)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, yeah.")
	hitretResult := Hitret(27460)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But when I looked at the price of the ice cream she tookÅ^out, I was stunned.")
	hitretResult := Hitret(27461)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It definitely was a normal vanilla ice cream, but she'dÅ^picked the most expensive brand.")
	hitretResult := Hitret(27462)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Why is there Haagen-Dazs in the candy shop...?")
	hitretResult := Hitret(27463)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA040122")
	Mess("Maybe I'll get vanilla too...")
	hitretResult := Hitret(27464)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san also picked up a vanilla, but she picked up aÅ^normal one, unlike Sora.")
	hitretResult := Hitret(27465)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK040076")
	Mess("Well then, I'll have chocolate.")
	hitretResult := Hitret(27466)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Everyone had picked their own share, so we all startedÅ^eating.")
	hitretResult := Hitret(27467)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	SetBustup("CF01_02M", 0, 0)
	Talk("Sora", "SR040040")
	Mess("...*gulp* *shiver*...")
	hitretResult := Hitret(27468)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Trying to cool herself from the intense heat, SoraÅ^quickly placed a large mouthful of ice cream in herÅ^mouth.")
	hitretResult := Hitret(27469)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040067")
	Mess("...*bite* *bite* *swallow*... Aah... That hit the spot!Å^That's the stuff!")
	hitretResult := Hitret(27470)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei was also digging into his popsicle, occasionallyÅ^slapping himself on his forehead as he exclaimed \"SOOÅ^REFRESHING!\"")
	hitretResult := Hitret(27471)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...I'm going to eat mine calmly.")
	hitretResult := Hitret(27472)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC01_03M", 0, 0)
	SetBustup("CD01_01M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Amatsume was eating hers happily, while Migiwa-san wasÅ^slowly savouring hers, swallowing first before havingÅ^the next bite.")
	hitretResult := Hitret(27473)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040077")
	Mess("Kazu-chan, is it good?")
	hitretResult := Hitret(27474)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040123")
	Mess("...Yeah...well...")
	hitretResult := Hitret(27475)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm not sure if she's embarrassed, but she sure seemsÅ^hesitant...")
	hitretResult := Hitret(27476)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH040068")
	Mess("You don't really eat such plebeian things do you? So itÅ^must not suit your taste.")
	hitretResult := Hitret(27477)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_07M", 0, 0)
	Talk("Kazuha", "KA040124")
	Mess("I don't really eat many snacks in the first place. AndÅ^don't make it sound like I only enjoy eating exquisiteÅ^food.")
	hitretResult := Hitret(27478)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei was only teasing, but she does sound offended.")
	hitretResult := Hitret(27479)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(7)
	SetBustup("CC01_03M", 0, 0)
	Talk("Akira", "AK040078")
	Mess("Mmm, mmm, ahaa, this is great! Do you want someÅ^chocolate too, Kazu-chan? Why don't we exchange iceÅ^creams?")
	hitretResult := Hitret(27480)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA040125")
	Mess("Eh? That's okay, but..")
	hitretResult := Hitret(27481)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But when Amatsume talked to her, her expressionÅ^loosened.")
	hitretResult := Hitret(27482)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040079")
	Mess("Okay! Open wide!")
	hitretResult := Hitret(27483)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_10M", 0, 0)
	Talk("Kazuha", "KA040126")
	Mess("I-I can help myself... We're not children anymore...")
	hitretResult := Hitret(27484)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Brushing away the spoon that Amatsume had outstreched,Å^Migiwa-san scooped out some chocolate ice cream with herÅ^own spoon.")
	hitretResult := Hitret(27485)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's even more childish, I sat there thinking.")
	hitretResult := Hitret(27486)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK040080")
	Mess("Is it tasty?")
	hitretResult := Hitret(27487)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040127")
	Mess("Yes it is, this is actually pretty good.")
	hitretResult := Hitret(27488)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040081")
	Mess("Okay, give me some of that? Aaaaa.")
	hitretResult := Hitret(27489)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED06a", 0, 0)
	Talk("Kazuha", "KA040128")
	Mess("Geez, stop acting like a child...")
	hitretResult := Hitret(27490)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ignoring her, Amatsume childishly opened her mouthÅ^toward Migiwa-san.")
	hitretResult := Hitret(27491)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Scooping a spoonful of her vanilla ice cream, she placedÅ^it in Amatsume's mouth.")
	hitretResult := Hitret(27492)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("So she's fine with feeding others...")
	hitretResult := Hitret(27493)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040082")
	Mess("Mm! Waa, delicious!!")
	hitretResult := Hitret(27494)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED06b", 0, 0)
	Talk("Kazuha", "KA040129")
	Mess("Geez, stop exaggerating.")
	hitretResult := Hitret(27495)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In contrast to her chiding tone, Migiwa-san's expressionÅ^was soft.")
	hitretResult := Hitret(27496)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She was wearing a warm and comforting smile. For someÅ^reason, she was enjoying herself.")
	hitretResult := Hitret(27497)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Maybe it's a little too much to say that she looked likeÅ^a mother, but she seemed really happy to be lookingÅ^after Amatsume.")
	hitretResult := Hitret(27498)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040083")
	Mess("More please?")
	hitretResult := Hitret(27499)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040130")
	Mess("I can't say no, can I...? Okay, open wide.")
	hitretResult := Hitret(27500)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040084")
	Mess("Aaaa! ...Um... Ah! It's a little cold but it's reallyÅ^delicious...")
	hitretResult := Hitret(27501)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume continued mumbling with Migiwa-san's spoonÅ^still in her mouth.")
	hitretResult := Hitret(27502)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With that, Migiwa-san smiled softly. I could see theÅ^happiness in her eyes.")
	hitretResult := Hitret(27503)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040085")
	Mess("I want moreÅ`")
	hitretResult := Hitret(27504)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED06a", 0, 0)
	Talk("Kazuha", "KA040131")
	Mess("No way. If you eat that much, then there'd be none leftÅ^for me.")
	hitretResult := Hitret(27505)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040086")
	Mess("In that case, I'll give you mine.")
	hitretResult := Hitret(27506)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040132")
	Mess("No way, you've eaten too much.")
	hitretResult := Hitret(27507)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED06b", 0, 0)
	Talk("Kazuha", "KA040133")
	Mess("....But, please just a little bit more...")
	hitretResult := Hitret(27508)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040087")
	Mess("Ahaaa, I love you Kazu-chan!")
	hitretResult := Hitret(27509)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040134")
	Mess("What are you saying...")
	hitretResult := Hitret(27510)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("They really are close.")
	hitretResult := Hitret(27511)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED06c", 0, 0)
	Talk("Kazuha", "KA040135")
	Mess("Eh!?")
	hitretResult := Hitret(27512)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It seems like you say a lot of things to her, but youÅ^don't mind at all.")
	hitretResult := Hitret(27513)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040136")
	Mess("I-I'm not really... W-What!? Don't start teasing me too!")
	hitretResult := Hitret(27514)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040069")
	Mess("Hey, hey, show me some love as wellÅ` Let me have someÅ^ice cream too!")
	hitretResult := Hitret(27515)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040137")
	Mess("No! Definitely, no!")
	hitretResult := Hitret(27516)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040070")
	Mess("Eh! Akira's fine, but I'm not - That's not fair!")
	hitretResult := Hitret(27517)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040138")
	Mess("You're completely different from Akira!")
	hitretResult := Hitret(27518)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B15a", 0, 0)
	SetBustup("CC01_01M", 0, 0)
	SetBustup("CD01_13M", 0, 0)
	SetBustup("CF01_01M", 0, 0)
	Talk("Akira", "AK040088")
	Mess("Kazu-chan, you should spoon-feed Ryou-niichan andÅ^Haru-kun too!")
	hitretResult := Hitret(27519)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040139")
	Mess("No way! That's too embarrassing...")
	hitretResult := Hitret(27520)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even though you did it for Akira?")
	hitretResult := Hitret(27521)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_10M", 0, 0)
	Talk("Kazuha", "KA040140")
	Mess("T-That's because...Akira was acting like a spoiledÅ^child, and....")
	hitretResult := Hitret(27522)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_05M", 0, 0)
	Talk("Ryouhei", "RH040071")
	Mess("Kazu-chaaaan~ let me have some too, pleaaaasee?")
	hitretResult := Hitret(27523)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Whoa...")
	hitretResult := Hitret(27524)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_13M", 0, 0)
	Talk("Kazuha", "KA040141")
	Mess("Hiii......")
	hitretResult := Hitret(27525)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_14M", 0, 0)
	Talk("Akira", "AK040089")
	Mess("Ahahahaa, that really sounded like me!")
	hitretResult := Hitret(27526)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040142")
	Mess("T-That's nothing like it! That doesn't sound one bitÅ^like Akira!!")
	hitretResult := Hitret(27527)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I agree... That was totally different...")
	hitretResult := Hitret(27528)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH040072")
	Mess("There! I don't care how much, but spoon-feed me, please.")
	hitretResult := Hitret(27529)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_10M", 0, 0)
	Talk("Kazuha", "KA040143")
	Mess("Stop that already!!Å^...Geez, you're not a child anymore either, Akira, soÅ^eat it yourself.")
	hitretResult := Hitret(27530)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(5)
	Talk("êSÇÃê∫", "")
	Mess("As soon as she said that, she stomped off toward theÅ^opposite side of the shop to eat alone.")
	hitretResult := Hitret(27531)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_10M", 0, 0)
	Talk("Ryouhei", "RH040073")
	Mess("Tch, if only I could have some of Kazu-chan's ice cream.")
	hitretResult := Hitret(27532)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040090")
	Mess("Don't feel bad, she'll definitely give you some nextÅ^time! ....Omnom...mmmm, delicious!")
	hitretResult := Hitret(27533)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Personally, I don't think Ryouhei will ever get anythingÅ^from Migiwa-san...")
	hitretResult := Hitret(27534)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("In any case, Migiwa-san...")
	hitretResult := Hitret(27535)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_04S", 0, 0)
	Talk("Kazuha", "KA040144")
	Mess("..Geez, stop poking into other people's business...")
	hitretResult := Hitret(27536)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She says a lot of things about it, but she really doesÅ^spoil Amatsume.")
	hitretResult := Hitret(27537)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Glancing toward us every now and then, Migiwa-san'sÅ^expression softened after seeing that Amatsume wasÅ^enjoying herself.")
	hitretResult := Hitret(27538)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When it comes to Amatsume, Migiwa-san is hopeless.")
	hitretResult := Hitret(27539)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA01_01M", 0, 0)
	Talk("Haruka", "")
	Mess("Hey, Sora.")
	hitretResult := Hitret(27540)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040041")
	Mess("...What?")
	hitretResult := Hitret(27541)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Do you want to switch too? I'll give you some of myÅ^popsicle.")
	hitretResult := Hitret(27542)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040042")
	Mess("I don't want it.")
	hitretResult := Hitret(27543)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("R-Really...")
	hitretResult := Hitret(27544)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Feeling a bit jealous of Migiwa-san, I tried toÅ^negotiate with Sora, but I got shot down immediately.")
	hitretResult := Hitret(27545)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora having a bite of my ice cream... Yeah right,Å^delusions are only delusions...")
	hitretResult := Hitret(27546)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_d007")
	Return
	Change("EXIT_SCENARIO")
EndFunc

