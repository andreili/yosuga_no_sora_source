Include"yosuga.ch"

Function sc00_C017()
	ScenarioEnter()
	ScPlayBgm("BGM03", 0)
	SetCg("B33b", 0, 0)
	Transition("WIP_LR", 500)
	Talk("Haruka", "")
	Mess("Uu..... What's this......")
	hitretResult := Hitret(21872)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF02_06M", 0, 0)
	Talk("Ryouhei", "RH020379")
	Mess("Uuoooo, hot........")
	hitretResult := Hitret(21873)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The moment when we stepped down from the train, we were^enveloped by the humid air.")
	hitretResult := Hitret(21874)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The exhaust heat from the train and the sultry air^surround us. Our bodies that were in the air-conditioned^room earlier, is now perspiring.")
	hitretResult := Hitret(21875)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF02_10M", 0, 0)
	Talk("Ryouhei", "RH020380")
	Mess("Haruu, should we drink something?")
	hitretResult := Hitret(21876)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I concur. If this continues, I'll die before I reach^home.")
	hitretResult := Hitret(21877)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF02_01M", 0, 0)
	Talk("Ryouhei", "RH020381")
	Mess("Hey, do you know? The vending machine facing the sun is^cooler.")
	hitretResult := Hitret(21878)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? Is that so? I had the image of a hot machine^wouldn't be able to make something cold.")
	hitretResult := Hitret(21879)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH020382")
	Mess("If it's too hot, the sensor will make the temperature^drop down quickly.")
	hitretResult := Hitret(21880)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH020383")
	Mess("Therefore, during hot days you buy from the vending^machine that faces the sun. This is trivia knowledge.")
	hitretResult := Hitret(21881)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haa~ I heard something useful, I'll remember that.")
	hitretResult := Hitret(21882)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B12b", 0, 0)
	Transition("WIP_RL", 500)
	Talk("S‚Ìº", "")
	Mess("Ryouhei and I buy juice from the vending machine, and^drink it on the way back.")
	hitretResult := Hitret(21883)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("From the setting sun, I thought that it would be cooler,^but walking till now has drenched us in perspiration.")
	hitretResult := Hitret(21884)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF02_02M", 0, 0)
	Talk("Ryouhei", "RH020384")
	Mess("Today is really interesting.")
	hitretResult := Hitret(21885)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes, I hope that everyone can go together.")
	hitretResult := Hitret(21886)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("We walked to the neighbouring town as a rarely seen^combination of two guys.")
	hitretResult := Hitret(21887)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Even though it's smaller than the area that I lived in^the past, but there's a recent increase of shops here,^and looking around here is interesting.")
	hitretResult := Hitret(21888)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The reason why we didn't go with everyone, is because^Ryouhei and I are buying birthday presents.")
	hitretResult := Hitret(21889)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Actually, we invited sora, but she said she will^purchase from the internet, and took out a fan and^stayed in her room.")
	hitretResult := Hitret(21890)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The presents have been prepared, but what about the^other things?")
	hitretResult := Hitret(21891)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF02_01M", 0, 0)
	Talk("Ryouhei", "RH020385")
	Mess("The drinks will most likely be prepared by Hayahiro-san.")
	hitretResult := Hitret(21892)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The location will be at Amatsume's home, right?")
	hitretResult := Hitret(21893)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF02_03M", 0, 0)
	Talk("Ryouhei", "RH020386")
	Mess("Aah, everyone will intrude there, and begin a guerrilla^warfare.")
	hitretResult := Hitret(21894)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's interesting.")
	hitretResult := Hitret(21895)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH020387")
	Mess("Aah, but we can't let her see through, okay? That girl^has sharp senses, and if she finds out she will start^preparing with everyone.")
	hitretResult := Hitret(21896)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's...... sounds very much like Amatsume's style.")
	hitretResult := Hitret(21897)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF02_01M", 0, 0)
	Talk("Ryouhei", "RH020388")
	Mess("The time to the decisive day isn't far away. We must^quickly do it.")
	hitretResult := Hitret(21898)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay, I got it.")
	hitretResult := Hitret(21899)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH020389")
	Mess("Well then, good work!!")
	hitretResult := Hitret(21900)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Aah, let's talk again over the phone--")
	hitretResult := Hitret(21901)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("All right...... I should go back since sora is^waiting......")
	hitretResult := Hitret(21902)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("S‚Ìº", "")
	Mess("I pick up my things, and walk back on the path leading^to my home.")
	hitretResult := Hitret(21904)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B34b", 400, 0)
	ScPlayEnvSe("SE256", "", 0)
	Talk("Haruka", "")
	Mess("cccccccccccccccc")
	hitretResult := Hitret(21905)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Come to think of it, what present did Ryouhei prepare?^He didn't tell me in the end.")
	hitretResult := Hitret(21906)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Two guys entering a fancy shop, even had a hard time^there, and the result was to abandon the special things^there.")
	hitretResult := Hitret(21907)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("If Sora comes along, perhaps we could have gotten^something. Perhaps Ryouhei went there and she was^reluctant to go?")
	hitretResult := Hitret(21908)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("cccccccccccccccc")
	hitretResult := Hitret(21909)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Hmmm?")
	hitretResult := Hitret(21910)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE256", 0)
	Talk("S‚Ìº", "")
	Mess("I got a strange feeling, and I stopped there.")
	hitretResult := Hitret(21911)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... What is it?")
	hitretResult := Hitret(21912)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Even though something was bothering me, I didn't^understand why and thus I started walking again.")
	hitretResult := Hitret(21913)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayEnvSe("SE256", "", 0)
	Talk("S‚Ìº", "")
	Mess("Well then, what should I make for dinner....... Even^though I want to have soumen, but I made it at lunch for^Sora. If she continues to have that, will she get^angry...?")
	hitretResult := Hitret(21914)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("If I make warm soumen, perhaps it will be safe......^Hmm~...... What's left in the fridge......")
	hitretResult := Hitret(21915)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("cccccccccccccccc")
	hitretResult := Hitret(21916)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... Hmm, Amatsume right?")
	hitretResult := Hitret(21917)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE256", 0)
	ScPlayBgm("BGM05", 0)
	ScPlaySe("SE016", 0)
	SetCg("B34b", 966, 0)
	SetBustup("CC06_02M", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Akira", "AK020853")
	Mess("Haa--, I have been discovered.")
	hitretResult := Hitret(21918)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("This time I knew it.")
	hitretResult := Hitret(21919)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020854")
	Mess("I wanted to scare you, but I got too close and I got^exposed.")
	hitretResult := Hitret(21920)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("From the guardrail's shadows, Amatsume came out pushing^her bicycle.")
	hitretResult := Hitret(21921)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B34b", 400, 0)
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020855")
	Mess("Haru-kun welcome back, today you also came back^properly.")
	hitretResult := Hitret(21922)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Of course I returned...... Ehh, you saw us going out?")
	hitretResult := Hitret(21923)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020856")
	Mess("Mmm, this morning, I went to Honomi on an errand and saw^you.")
	hitretResult := Hitret(21924)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("...... I really need to be on my guard at all times,^what kind of coincidence is this.....")
	hitretResult := Hitret(21925)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020857")
	Mess("Was it fun? With Ryouhei-neechan's date.")
	hitretResult := Hitret(21926)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes, we went to various places and we had fun.")
	hitretResult := Hitret(21927)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Come to think of it, Amatsume, have you been helping out^the entire day?")
	hitretResult := Hitret(21928)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020858")
	Mess("Yup, I did my homework in the morning, and I went to^Honomi in the evening to buy things.")
	hitretResult := Hitret(21929)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, Amatsume also bought a lot of things.")
	hitretResult := Hitret(21930)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I placed the bags from the supermarket I used, and^placed it into the basket and on the handles.")
	hitretResult := Hitret(21931)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020859")
	Mess("Haru-kun things can also be placed inside, if you want^to?")
	hitretResult := Hitret(21932)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I can? Thanks.......")
	hitretResult := Hitret(21933)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020860")
	Mess("That's a really big bag, huh. What's inside?")
	hitretResult := Hitret(21934)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Amatsume looks very interested in the bags, from the^department store "TK hand".")
	hitretResult := Hitret(21935)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("This is bad..... Amatsume's present is about to get^exposed.......")
	hitretResult := Hitret(21936)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh, Amatsume!!! What do you think this is~?")
	hitretResult := Hitret(21937)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_14M", 0, 0)
	Talk("Akira", "AK020861")
	Mess("Nyaa?")
	hitretResult := Hitret(21938)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I held up a small yellow box in front of Amatsume.")
	hitretResult := Hitret(21939)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020862")
	Mess("*Sniff* There's a really sweet smell hmmm.")
	hitretResult := Hitret(21940)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Shall we eat it together? I bought it as a souvenir.")
	hitretResult := Hitret(21941)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020863")
	Mess("...... Are you sure? Giving it to me?")
	hitretResult := Hitret(21942)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Because there's a lot, you don't need to hold back it^and eat it.")
	hitretResult := Hitret(21943)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020864")
	Mess("Thanks-! What is it, cake?")
	hitretResult := Hitret(21944)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uuu, n-no. It's actually choux 