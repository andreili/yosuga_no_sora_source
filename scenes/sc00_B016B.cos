Include"yosuga.ch"

Function sc00_B016B()
	ScenarioEnter()
	ScPlayEnvSe("SE301", "", 0)
	SetCg("B17a", 0, 0)
	Transition("WIP_RL", 500)
	Talk("êSÇÃê∫", "")
	Mess("I expected this until summer vacation, there were only aÅ^few of us students who will go to school.")
	hitretResult := Hitret(13996)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There's a club that's wearing shorts and their jerseysÅ^during their activity, I saw them through the windows ofÅ^the school building though they closed the curtainsÅ^right away.")
	hitretResult := Hitret(13997)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There are only a few students here at school since it'sÅ^on vacation, though it really made it looked peacefulÅ^and it felt like weird at some point.")
	hitretResult := Hitret(13998)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B21a", 0, 0)
	Transition("WIP_RL", 500)
	Talk("êSÇÃê∫", "")
	Mess("Even in this heat, there might be no one else using theÅ^pool.")
	hitretResult := Hitret(13999)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The pool manager as of today is(?) reading at the cornerÅ^of the pool side again.")
	hitretResult := Hitret(14000)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EB02a", 0, 0)
	Talk("Haruka", "")
	Mess("Nao-chan.")
	hitretResult := Hitret(14001)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EB02b", 0, 0)
	Talk("Nao", "NO030765")
	Mess("Ah, welcome, Haru-chan.")
	hitretResult := Hitret(14002)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I came here to hang out, though it's really quite hotÅ^today rightÅ`?")
	hitretResult := Hitret(14003)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030766")
	Mess("YeaahÅ`, the baseball and basket ball club just cameÅ^here a while ago.")
	hitretResult := Hitret(14004)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heh, so you had some guests today.")
	hitretResult := Hitret(14005)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Looking at it carefully, there were water from theÅ^poolside indicating someone jumped in and out.")
	hitretResult := Hitret(14006)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It seems that they played so much when they got here.")
	hitretResult := Hitret(14007)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030767")
	Mess("Well, they made a ruckus here since it's already in theÅ^afternoon. Though it looks like every club came hereÅ^already ever since this morning.")
	hitretResult := Hitret(14008)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan, were you here throughout the day?")
	hitretResult := Hitret(14009)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030768")
	Mess("I really didn't think about doing that. Haru-chan, let'sÅ^swim since you came here.")
	hitretResult := Hitret(14010)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan closed her book and stood up.")
	hitretResult := Hitret(14011)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B21a", 0, 0)
	SetBustup("CB04_02M", 0, 0)
	Down(3, -25, 0, 0)
	Talk("Nao", "NO030769")
	Mess("Ngh... What do want to do Haru-chan? Did you come hereÅ^to soak yourself in?")
	hitretResult := Hitret(14012)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Down(3, -1, 0, 0)
	Talk("Haruka", "")
	Mess("Yeah, I came here after all, so I think I'll get in orÅ^something at least.")
	hitretResult := Hitret(14013)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030770")
	Mess("Yeah, you should change your clothes then? It's fine ifÅ^you use the locker room since they're open.")
	hitretResult := Hitret(14014)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay, later then.")
	hitretResult := Hitret(14015)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030771")
	Mess("Just hurry up there.")
	hitretResult := Hitret(14016)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE301", 1)
	BlackOut(0, 1)
	ScWait(1000, 0)
	SetCg("B21a", 0, 0)
	Transition("WIP_BT", 500)
	ScPlaySe("SE202", 0)
	Talk("êSÇÃê∫", "")
	Mess("As I came out of the shower, I quickly went to theÅ^poolside, and Nao-chan was just swimming on theÅ^twenty-five meter course.")
	hitretResult := Hitret(14017)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan did a beautiful turn as she got to the wall,Å^she charged once more on the water as she swim.")
	hitretResult := Hitret(14018)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just like at the beach, Nao-chan's form is really great,Å^her speed is also wonderful.")
	hitretResult := Hitret(14019)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I wasn't here, maybe Nao-chan was always giving herÅ^best in her club activities.")
	hitretResult := Hitret(14020)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I thought of her as nothing but a traditional girl who'sÅ^good in cooking as well, though her appearance is quiteÅ^the opposite to her actions, I'm really impressed withÅ^her.")
	hitretResult := Hitret(14021)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I was thinking of those things, Nao-chan had alreadyÅ^a fifty-meter swim.")
	hitretResult := Hitret(14022)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Then she faced up as she kicked the wall, then sheÅ^slowly floated like that and she drifted around aboveÅ^the water.")
	hitretResult := Hitret(14023)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM08", 0)
	Talk("êSÇÃê∫", "")
	Mess("She stared at the sky above her...")
	hitretResult := Hitret(14024)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What could she be thinking?")
	hitretResult := Hitret(14025)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Somehow, I felt this ambiance that hang around her as IÅ^drew near her.")
	hitretResult := Hitret(14026)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, I recognized her facial expression.")
	hitretResult := Hitret(14027)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At school... When we weren't there with Nao-chan.")
	hitretResult := Hitret(14028)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And... I already saw this, she had this face when I sawÅ^her one time...")
	hitretResult := Hitret(14029)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She looked bored... And, a bit lonely...")
	hitretResult := Hitret(14030)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There maybe something that I didn't notice...")
	hitretResult := Hitret(14031)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Always, it's like Nao-chan's hiding something behind herÅ^gentle face...")
	hitretResult := Hitret(14032)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("That dark and gloomy face, I slowly approached herÅ^without making any sound as I walked.")
	hitretResult := Hitret(14033)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030772")
	Mess("Ah, Haru-chan, that was fast.")
	hitretResult := Hitret(14034)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan was swimming while I got to her, then she wentÅ^out of the poolside and wiped her face with a towel.")
	hitretResult := Hitret(14035)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM06", 0)
	SetBustup("CB06_02M", 0, 0)
	Talk("Nao", "NO030773")
	Mess("Did you do some stretching?")
	hitretResult := Hitret(14036)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Just about now.")
	hitretResult := Hitret(14037)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030774")
	Mess("Be careful on stretching your feet. It's also deep inÅ^the middle of the pool so please be cautious about it.")
	hitretResult := Hitret(14038)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I won't go near it, it's quite scary there.")
	hitretResult := Hitret(14039)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_03M", 0, 0)
	Talk("Nao", "NO030775")
	Mess("Ahahaha, it'll be fine. I'm already familiar with theÅ^surroundings here since I've been doing most of myÅ^training here.")
	hitretResult := Hitret(14040)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Will I be able to swim like you Nao-chan?")
	hitretResult := Hitret(14041)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_01M", 0, 0)
	Talk("Nao", "NO030776")
	Mess("Well, I think about after five years? I also strictlyÅ^practiced always.")
	hitretResult := Hitret(14042)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That long?")
	hitretResult := Hitret(14043)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_03M", 0, 0)
	Talk("Nao", "NO030777")
	Mess("Ahahaha, I'm just kidding. I think I didn't even reach aÅ^whole year.")
	hitretResult := Hitret(14044)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_01M", 0, 0)
	Talk("Nao", "NO030778")
	Mess("I started swimming ever since I got here.")
	hitretResult := Hitret(14045)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see... Though, I think you're good enough alreadyÅ^right away?")
	hitretResult := Hitret(14046)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_02M", 0, 0)
	Talk("Nao", "NO030779")
	Mess("Thanks. But you see, I also had bad times you know?")
	hitretResult := Hitret(14047)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But, for someone who doesn't know how to swim like me,Å^you look amazing.")
	hitretResult := Hitret(14048)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_05M", 0, 0)
	Talk("Nao", "NO030780")
	Mess("Ah, sorry...")
	hitretResult := Hitret(14049)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's nothing... You don't have to worry about it. Also,Å^I came here to get some special lessons from you right?")
	hitretResult := Hitret(14050)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_02M", 0, 0)
	Talk("Nao", "NO030781")
	Mess("Yeah... I know. Well then, we should practice from theÅ^basics.")
	hitretResult := Hitret(14051)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("I feel like I unlocked my body as I stretched.")
	hitretResult := Hitret(14052)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("During our P.E. class, we were doing it by the book,Å^though I somehow didn't do it that way as always, IÅ^never liked it.")
	hitretResult := Hitret(14053)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan's teaching me despite everything else, this isÅ^also an opportunity for me to lose my fear on the water.")
	hitretResult := Hitret(14054)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_01M", 0, 0)
	Talk("Nao", "NO030782")
	Mess("Well, shall we get familiar with it first?")
	hitretResult := Hitret(14055)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh...?")
	hitretResult := Hitret(14056)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_02M", 0, 0)
	Talk("Nao", "NO030783")
	Mess("The basics? Like washing your face and showering and notÅ^losing your composure, since you need to put your faceÅ^against the water when you do it.")
	hitretResult := Hitret(14057)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030784")
	Mess("And, if you're not familiar with it, you won't be ableÅ^to have a nice stance above the water and it'll make youÅ^more tired right away.")
	hitretResult := Hitret(14058)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030785")
	Mess("That's why, you shouldn't panick if you're not familiarÅ^with the surroundings, just swim slowly around as youÅ^get used to it, that way you can float much longer too.")
	hitretResult := Hitret(14059)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see...")
	hitretResult := Hitret(14060)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_01M", 0, 0)
	Talk("Nao", "NO030786")
	Mess("Someone in a hurry will always be out of air right away,Å^so you should practice first.")
	hitretResult := Hitret(14061)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030787")
	Mess("And also, you should get used to it so you won't beÅ^flapping around.")
	hitretResult := Hitret(14062)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_03M", 0, 0)
	Talk("Nao", "NO030788")
	Mess("It's not too late for you to learn how to swim.")
	hitretResult := Hitret(14063)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, I got that. As you were saying Nao-chan, I feelÅ^like I can do it now since you're here you know?")
	hitretResult := Hitret(14064)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_02M", 0, 0)
	Talk("Nao", "NO030789")
	Mess("Hehehehe, for your sake Haru-chan, let's do it realÅ^quick okay? First, let's get used to it for one minute.Å^Ah, you can also close your eyes.")
	hitretResult := Hitret(14065)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? For 1 minute?")
	hitretResult := Hitret(14066)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_03M", 0, 0)
	Talk("Nao", "NO030790")
	Mess("It's like taking a bath, I think around 100 secondsÅ^would be fine too?")
	hitretResult := Hitret(14067)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Look after me for one minute please.")
	hitretResult := Hitret(14068)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_04M", 0, 0)
	Talk("Nao", "NO030791")
	Mess("Alright! Good luck! After the first minute, can you openÅ^your eyes on the next minute?")
	hitretResult := Hitret(14069)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uh... S-Sure...")
	hitretResult := Hitret(14070)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("We entered the pool as getting used it is part of theÅ^special training.")
	hitretResult := Hitret(14071)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The water level is by my chest.")
	hitretResult := Hitret(14072)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Honestly, my heart started beating so fast as myÅ^adreline shot up with just that.")
	hitretResult := Hitret(14073)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030792")
	Mess("If you're scared, you can look up right away.")
	hitretResult := Hitret(14074)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("Yeah... I understand.")
	hitretResult := Hitret(14075)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	ScPlaySe("SE205", 0)
	Talk("êSÇÃê∫", "")
	Mess("As I get ready, I placed a serious expression on myÅ^face.")
	hitretResult := Hitret(14076)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("To simply think about it, I don't want to look like anÅ^idiot, there's no way I'd drown here since it'll beÅ^strange if it happens.")
	hitretResult := Hitret(14077)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Also, I can just look up if I ever lose my breath.")
	hitretResult := Hitret(14078)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Also, I can just hold on tight by the edge of theÅ^poolside. I won't be staggering and losing myÅ^consciousness again.")
	hitretResult := Hitret(14079)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I also feel a little bit comfortable, though I hope IÅ^won't choke later on.")
	hitretResult := Hitret(14080)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When we were still kids, we used to play in the water byÅ^holding our breaths for one minute.")
	hitretResult := Hitret(14081)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, it's different when you're in the water. If youÅ^ever take a breath, you'll end up drinking water.")
	hitretResult := Hitret(14082)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It may be the cause... I'm afraid whenever I feel thatÅ^sensation, it's just like drowning.")
	hitretResult := Hitret(14083)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM05", 0)
	ScPlaySe("SE208", 0)
	SetCg("B21a", 0, 0)
	Transition("WIP_TB", 250)
	Talk("Haruka", "")
	Mess("Puaah--!")
	hitretResult := Hitret(14084)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_09M", 0, 0)
	Talk("Nao", "NO030793")
	Mess("Are you all right?")
	hitretResult := Hitret(14085)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y-Yeah... It's not even 1 minute right?")
	hitretResult := Hitret(14086)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_01M", 0, 0)
	Talk("Nao", "NO030794")
	Mess("Yeah, but you won't be able to do that right away sinceÅ^you just started.")
	hitretResult := Hitret(14087)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030795")
	Mess("As I thought, afraid?")
	hitretResult := Hitret(14088)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'll close my eyes.")
	hitretResult := Hitret(14089)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030796")
	Mess("I have goggles with me, if you want...")
	hitretResult := Hitret(14090)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's fine, I can try it one more time.")
	hitretResult := Hitret(14091)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	ScPlaySe("SE205", 0)
	Talk("êSÇÃê∫", "")
	Mess("I placed my face on the water one more time.")
	hitretResult := Hitret(14092)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My ears are also dipped in the water, I can barely hearÅ^anything except the water.")
	hitretResult := Hitret(14093)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan has started counting slowly from 1.")
	hitretResult := Hitret(14094)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Earlier, I made it till 30 seconds.")
	hitretResult := Hitret(14095)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("This time too...")
	hitretResult := Hitret(14096)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I also felt it, my heart is throbbing violently asÅ^usual, I feel so anxious about choking, my body feels soÅ^weak.")
	hitretResult := Hitret(14097)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's fine... This is nothing... I need to feel and keepÅ^at it, it looks like my lungs still have some air, IÅ^just need to control my mouth so it wouldn't happen.")
	hitretResult := Hitret(14098)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE208", 0)
	SetCg("B21a", 0, 0)
	Transition("WIP_TB", 250)
	Talk("Haruka", "")
	Mess("Kuh! Haa, haa...")
	hitretResult := Hitret(14099)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_01M", 0, 0)
	Talk("Nao", "NO030797")
	Mess("Take a rest for a bit.")
	hitretResult := Hitret(14100)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, I'm fine...")
	hitretResult := Hitret(14101)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030798")
	Mess("Don't work yourself too much, you might not be able toÅ^last there you know? Also, it's dangerous if you keep onÅ^doing it while your head hurts.")
	hitretResult := Hitret(14102)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030799")
	Mess("Even if you look at it in a good way, you should reallyÅ^practice first and feel comfortable with it.")
	hitretResult := Hitret(14103)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah...")
	hitretResult := Hitret(14104)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("It's actually shameful... Not being able to swim at thisÅ^kind of age.")
	hitretResult := Hitret(14105)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though I just got in the pool, I feel like I haveÅ^no energy to swim anymore, though Nao-chan seemed to beÅ^enjoying it...")
	hitretResult := Hitret(14106)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, after all these years, I really didn't think ofÅ^wiping off this fear I have.")
	hitretResult := Hitret(14107)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I know that this is just psychological, it's somethingÅ^that I've been keeping inside my heart...")
	hitretResult := Hitret(14108)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Let's do it one more time.")
	hitretResult := Hitret(14109)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_02M", 0, 0)
	Talk("Nao", "NO030800")
	Mess("Do your best, Haru-chan.")
	hitretResult := Hitret(14110)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah.")
	hitretResult := Hitret(14111)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	ScPlaySe("SE205", 0)
	Talk("êSÇÃê∫", "")
	Mess("I closed my eyes, then I carefully placed my face in theÅ^water.")
	hitretResult := Hitret(14112)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030801")
	Mess("One, two, three...")
	hitretResult := Hitret(14113)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan continued counting.")
	hitretResult := Hitret(14114)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030802")
	Mess("Seven, eight, nine...")
	hitretResult := Hitret(14115)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't know why it's difficult for me when it comes toÅ^this.")
	hitretResult := Hitret(14116)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even if I say it for many times, my body keeps on havingÅ^a hard time.")
	hitretResult := Hitret(14117)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE361", 0)
	Talk("êSÇÃê∫", "")
	Mess("*Blop*Blop*Blop*, bubbles came out of my mouth as theÅ^air from my cheeks bursted out.")
	hitretResult := Hitret(14118)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I can't do it... But if I can just close my mouth moreÅ^tightly, I might keep the air inside.")
	hitretResult := Hitret(14119)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!?")
	hitretResult := Hitret(14120)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I suddenly felt something touched my hands.")
	hitretResult := Hitret(14121)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030803")
	Mess("You can do it, Haru-chan...")
	hitretResult := Hitret(14122)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan placed her hands above mine. She gentlyÅ^encouraged me.")
	hitretResult := Hitret(14123)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030804")
	Mess("Seventeen, eighteen...")
	hitretResult := Hitret(14124)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, I felt a little bit better thanks to that.")
	hitretResult := Hitret(14125)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030805")
	Mess("Twenty! Alright! Haru-chan!")
	hitretResult := Hitret(14126)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE203", 0)
	Talk("êSÇÃê∫", "")
	Mess("I shook my head as I looked up.")
	hitretResult := Hitret(14127)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Thanks to Nao-chan, I can put my face in the water forÅ^more than twenty seconds now.")
	hitretResult := Hitret(14128)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Alright! Even though I was low on air, even though IÅ^couldn't endure it.")
	hitretResult := Hitret(14129)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I catiously open my eyes in the water, to see what's inÅ^the water.")
	hitretResult := Hitret(14130)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCgRGB(0, 174, 239)
	Transition("MOZCIR_", 800)
	Talk("êSÇÃê∫", "")
	Mess("It's like a big light blue world... I'm beside Nao-chanÅ^who's jumping with joy.")
	hitretResult := Hitret(14131)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I just need to keep on practicing slowly like this...Å^Because I...")
	hitretResult := Hitret(14132)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I feel that this pain inside me would slowly fade away.")
	hitretResult := Hitret(14133)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan suddenly went near me, then I touched her back.")
	hitretResult := Hitret(14134)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_04S", 0, 0)
	Zoom(400, 500, 250, 188, 0, 0, 0)
	ScStopBgm(1)
	Talk("êSÇÃê∫", "")
	Mess("!?")
	hitretResult := Hitret(14135)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I quickly looked away as Nao-chan's thigh drew near me.")
	hitretResult := Hitret(14136)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE201", 0)
	SetCg("B21a", 0, 0)
	Transition("WIP_TB", 50)
	Update()
	Quake(0, 16, 1, 2, 15)
	Talk("Haruka", "")
	Mess("Buwaaaaa!!!")
	hitretResult := Hitret(14137)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM14", 0)
	SetBustup("CB04_13L", 0, 0)
	Shake(3)
	Talk("Nao", "NO030806")
	Mess("Hyaaaa!?")
	hitretResult := Hitret(14138)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was like a fish who dynamically thrusted out of theÅ^water, I quickly lifted my head from the water.")
	hitretResult := Hitret(14139)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030807")
	Mess("Wha-Ah... Wh-what's the matter... Haru-chan?")
	hitretResult := Hitret(14140)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("*cough*cough* N-Nothing... Really... Suddenly... YouÅ^suddenly went near me, Nao-chan...")
	hitretResult := Hitret(14141)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_11L", 0, 0)
	Talk("Nao", "NO030808")
	Mess("Eh... Sorry, did I surprise you?")
	hitretResult := Hitret(14142)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No... I-It's not that...")
	hitretResult := Hitret(14143)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_02L", 0, 0)
	Talk("Nao", "NO030809")
	Mess("What? So what is it?")
	hitretResult := Hitret(14144)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("N-No... That's... It's not a big deal... I-I really wasÅ^just surprised...")
	hitretResult := Hitret(14145)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB04_04L", 0, 0)
	Talk("Nao", "NO030810")
	Mess("Fufufuun, what are you hiding? Come on, why don't tellÅ^it to your 'Onee-san'?")
	hitretResult := Hitret(14146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I said, it's nothing...")
	hitretResult := Hitret(14147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Shake(3)
	Talk("Nao", "NO030811")
	Mess("Hey, hey, it's fine since I'm your Nee-san right?")
	hitretResult := Hitret(14148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Quake(16, 0, 1, 2, 15)
	Talk("Haruka", "")
	Mess("Whooaaa!? S-Stop it!!")
	hitretResult := Hitret(14149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan hugged me from my back, then the weight of herÅ^body forced me stagger.")
	hitretResult := Hitret(14150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE012", 0)
	Talk("êSÇÃê∫", "")
	Mess("Her soft breasts was pushed against my back, I alreadyÅ^expected that it would be this soft if it's pushed.")
	hitretResult := Hitret(14151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("H-Hey, give me a break here?")
	hitretResult := Hitret(14152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030812")
	Mess("Hey, trying to run away huh? WaaiiiitÅ`!!")
	hitretResult := Hitret(14153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("As I got Nao-chan's hands off of me, I walked across theÅ^pool as I ran away.")
	hitretResult := Hitret(14154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But the water feels so heavy, I won't be able to goÅ^further than this.")
	hitretResult := Hitret(14155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_03M", 0, 0)
	Talk("Nao", "NO030813")
	Mess("You're too naive to run away from me on the pool, thisÅ^is easy!")
	hitretResult := Hitret(14156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She took off her glasses, then Nao-chan looks like sheÅ^went on to her serious mode.")
	hitretResult := Hitret(14157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030814")
	Mess("Take that!!")
	hitretResult := Hitret(14158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	ScPlaySe("SE204", 0)
	Talk("êSÇÃê∫", "")
	Mess("With a dash, it looks like she's going to catch up on meÅ^as she swim through the water like a fish.")
	hitretResult := Hitret(14159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE201", 0)
	SetBustup("CB06_04L", 0, 0)
	Quake(16, 0, 1, 2, 15)
	Talk("Nao", "NO030815")
	Mess("Puaaa! Got ya Åô")
	hitretResult := Hitret(14160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan cut me up in front, but then I hugged her as weÅ^bumped into each other.")
	hitretResult := Hitret(14161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah!")
	hitretResult := Hitret(14162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_03L", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("With a soft feeling, I think I won't be able to runÅ^anymore as I'm hugging Nao-chan.")
	hitretResult := Hitret(14163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE012", 0)
	Talk("êSÇÃê∫", "")
	Mess("As I'm almost naked, Nao-chan's body really has maturedÅ^now.")
	hitretResult := Hitret(14164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE012", 0)
	Talk("êSÇÃê∫", "")
	Mess("I can feel her breasts as it's pushed up against her,Å^one of my legs are in between her legs and it's quiteÅ^close to her crotch, I'm left speechless and defenselessÅ^from this kind of attack.")
	hitretResult := Hitret(14165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Then Nao-chan slowly untangled herself from me, I lostÅ^my balanced and I got dragged in the water.")
	hitretResult := Hitret(14166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I know that Nao-chan was only fooling around... But toÅ^me...")
	hitretResult := Hitret(14167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I give up... N-Nao-cha...")
	hitretResult := Hitret(14168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("This is more than what I expected... She was foolingÅ^around in a different way.")
	hitretResult := Hitret(14169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_01L", 0, 0)
	Talk("Nao", "NO030816")
	Mess("Tch! It was just a bit more though you resisted in anÅ^amusing way.")
	hitretResult := Hitret(14170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("... Sorry... But I'm not yet good right.")
	hitretResult := Hitret(14171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_02L", 0, 0)
	Talk("Nao", "NO030817")
	Mess("Hehehe, yeah. But, I'm sure you will be Haru-chan...")
	hitretResult := Hitret(14172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_11L", 0, 0)
	Talk("Nao", "NO030818")
	Mess("Ah... I'm sorry... If it's me...")
	hitretResult := Hitret(14173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan, I realized my feelings for you during thatÅ^time, as we clinged on while our hands slowly let go ofÅ^each other.")
	hitretResult := Hitret(14174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030819")
	Mess("N-No... If it was me... I'm sorry, Haru-chan...")
	hitretResult := Hitret(14175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, but... it's because I also fell... I'm really gladÅ^you were there for me.")
	hitretResult := Hitret(14176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030820")
	Mess("...Y-Yeah...")
	hitretResult := Hitret(14177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even it's still awkward, Nao-chan and I hugged onceÅ^more.")
	hitretResult := Hitret(14178)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We may be just fooling around, but my chest earlier wasÅ^pounding so hard.")
	hitretResult := Hitret(14179)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if we're glued together, Nao-chan's probably playingÅ^along with me.")
	hitretResult := Hitret(14180)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't feel anything strange about... But I'm tremblingÅ^so much...")
	hitretResult := Hitret(14181)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030821")
	Mess("Ah...")
	hitretResult := Hitret(14182)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, even if I try to suppress it, I can feel it beatÅ^much faster every time.")
	hitretResult := Hitret(14183)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Feeling Nao-chan's body up this close... Her lips areÅ^also close to mine as well..")
	hitretResult := Hitret(14184)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030822")
	Mess("...Is it fine if I... go?")
	hitretResult := Hitret(14185)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It somehow feels embarrassing, though it felt lonesomeÅ^as we got away from each other.")
	hitretResult := Hitret(14186)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Y-Yeah... Thanks...")
	hitretResult := Hitret(14187)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I didn't trip as Nao-chan supported me as we slowly wentÅ^away from each other.")
	hitretResult := Hitret(14188)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The warmth faded away as her thin swimsuit was away fromÅ^my body.")
	hitretResult := Hitret(14189)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Feeling Nao-chan that close...")
	hitretResult := Hitret(14190)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM17", 0)
	SetBustup("CB06_13L", 0, 0)
	Quake(16, 0, 1, 2, 15)
	Talk("Nao", "NO030823")
	Mess("!!")
	hitretResult := Hitret(14191)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_11L", 0, 0)
	Talk("Nao", "NO030824")
	Mess("...Haru-chan...")
	hitretResult := Hitret(14192)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan embraced me from my back as I went away fromÅ^her.")
	hitretResult := Hitret(14193)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030825")
	Mess("...Haru-chan...")
	hitretResult := Hitret(14194)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I took a quick look on Nao-chan as she looked up to me.")
	hitretResult := Hitret(14195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't like to show her that I'm taking advantage ofÅ^the situation, so I just continued to stare at her.")
	hitretResult := Hitret(14196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I want to feel... More of Nao-chan...")
	hitretResult := Hitret(14197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan has always been the elder sister who looksÅ^after us.")
	hitretResult := Hitret(14198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As we depended on her, she kept on supporting us.")
	hitretResult := Hitret(14199)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That kind of girl... is the one... I like.")
	hitretResult := Hitret(14200)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It doesn't feel like she takes my hand as a youngerÅ^brother, it's probably different on how she sees it.")
	hitretResult := Hitret(14201)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Perhaps we were depending on each others' kindness.")
	hitretResult := Hitret(14202)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, even if she really misunderstood... My feelings isÅ^still...")
	hitretResult := Hitret(14203)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After all, we haven't been talking for a short timeÅ^while we're facing each other like this.")
	hitretResult := Hitret(14204)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(0, 1)
	ScWait(2000, 0)
	Change("00_b017")
	Return
	Change("EXIT_SCENARIO")
EndFunc

