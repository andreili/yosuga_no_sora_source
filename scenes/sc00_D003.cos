Include"yosuga.ch"

Function sc00_D003()
	ScenarioEnter()
	ScPlayBgm("BGM05", 0)
	ScPlayEnvSe("SE156", "", 0)
	SetCg("EZ06a", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("During that day's second period, my heart leapt out inÅ^surprise at an unexpected electronic noise.")
	hitretResult := Hitret(26953)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040001")
	Mess("Whoa, what's that? What's that sound?")
	hitretResult := Hitret(26954)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And the source was...me.Å^And the mobile phone in my pocket.")
	hitretResult := Hitret(26955)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ryouhei turned around and the lesson was stopped, butÅ^because no one knew what that sound was, they didn'tÅ^realize that I was the cause.")
	hitretResult := Hitret(26956)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE156", 0)
	Talk("êSÇÃê∫", "")
	Mess("I hurriedly switched it to silent mode, and then theÅ^ringtone cut off.")
	hitretResult := Hitret(26957)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The LCD displayed \"Sora\" and indicated that an email hadÅ^arrived.")
	hitretResult := Hitret(26958)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The contents of it were short. It said, \"I'm hungry.\"")
	hitretResult := Hitret(26959)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...She does realize that I'm in school, right....?")
	hitretResult := Hitret(26960)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Typing in only \"There's rice in the fridge,\" I sent theÅ^reply and closed my phone.")
	hitretResult := Hitret(26961)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("So she really wasn't awake yet this morning...I sighed.")
	hitretResult := Hitret(26962)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06d", 0, 0)
	Talk("Ryouhei", "RH040002")
	Mess("Hehehehe...feels tiring, huh?")
	hitretResult := Hitret(26963)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Wh...What does?")
	hitretResult := Hitret(26964)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After lifting my head, I was thrown back.")
	hitretResult := Hitret(26965)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Right in front of me was a Ryouhei's face, with a faintÅ^smile.")
	hitretResult := Hitret(26966)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Coming in so close...Yuck.")
	hitretResult := Hitret(26967)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What made my heart race was not whether he saw it orÅ^not, but because I was surprised.")
	hitretResult := Hitret(26968)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06a", 0, 0)
	Talk("Ryouhei", "RH040003")
	Mess("What did you just hide? I don't suppose it has somethingÅ^to do with that noise just then?")
	hitretResult := Hitret(26969)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("T-That's...")
	hitretResult := Hitret(26970)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06d", 0, 0)
	Talk("Ryouhei", "RH040004")
	Mess("Don't hide itÅ` We're friends, right?")
	hitretResult := Hitret(26971)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even if you say that...")
	hitretResult := Hitret(26972)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The classroom had settled down and returned to normal,Å^and the teacher had resumed the lesson.")
	hitretResult := Hitret(26973)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Right now, there was no one paying attention to us.")
	hitretResult := Hitret(26974)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah...well, I forgot to put it into silent mode.")
	hitretResult := Hitret(26975)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While feeling guilty, I took out my phone.")
	hitretResult := Hitret(26976)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I've never forgotten to do that before, but perhapsÅ^because I'm not used to this lifestyle yet, I carelesslyÅ^forgot.")
	hitretResult := Hitret(26977)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06a", 0, 0)
	Talk("Ryouhei", "RH040005")
	Mess("Silent mode? What's that....D-Don't tell me that's oneÅ^of those \"mobile phone\" things!?")
	hitretResult := Hitret(26978)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040006")
	Mess("Whoa, you're amazing... You were carrying such a thing?")
	hitretResult := Hitret(26979)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's a normal mobile phone, isn't it? It can't evenÅ^connect to 1seg.")
	hitretResult := Hitret(26980)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040007")
	Mess("Huh? OneSeg? What's that?")
	hitretResult := Hitret(26981)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040026")
	Mess("Wan wan is...like a doggie?")
	hitretResult := Hitret(26982)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Not a dog, TV! The new models have tv built in, right?")
	hitretResult := Hitret(26983)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040008")
	Mess("What! You serious? You can watch TV on such a smallÅ^thing?")
	hitretResult := Hitret(26984)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040027")
	Mess("That's amazing! Lemme see, lemme see!")
	hitretResult := Hitret(26985)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Like I said, this model can't...")
	hitretResult := Hitret(26986)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06c", 0, 0)
	Talk("Kazuha", "KA040041")
	Mess("...hold on, you three, Do you realize what time it isÅ^now?")
	hitretResult := Hitret(26987)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06f", 0, 0)
	Talk("Ryouhei", "RH040009")
	Mess("Oh, Ojou-sama, you have a look too! It can watch TV!Å^It's even better than yours, right?")
	hitretResult := Hitret(26988)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040042")
	Mess("I don't care about that. How about focusing on class forÅ^at least a bit...?")
	hitretResult := Hitret(26989)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sorry, I'll put it away immediately.")
	hitretResult := Hitret(26990)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06a", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("After scolding us, Migiwa-san silently looked backÅ^toward the blackboard.")
	hitretResult := Hitret(26991)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san's diligent. I have to try and not be aÅ^distraction.")
	hitretResult := Hitret(26992)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040010")
	Mess("...Migiwa of all people is jealous.")
	hitretResult := Hitret(26993)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What'd you say, Ryouhei?")
	hitretResult := Hitret(26994)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040011")
	Mess("Well, yours is newer than the one that the Ojou'sÅ^carrying, so she's sulking.")
	hitretResult := Hitret(26995)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...I don't think so.")
	hitretResult := Hitret(26996)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040012")
	Mess("You still don't understand the Ojou. She really hates toÅ^lose, you know? I got to know that from experience.")
	hitretResult := Hitret(26997)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040028")
	Mess("She often gets angry at Ryou-niichan, huh.")
	hitretResult := Hitret(26998)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's different than saying that she hates to lose,Å^isn't it?")
	hitretResult := Hitret(26999)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040013")
	Mess("Oho, desperately covering for the Ojou, huh?Å^Hahahaaa? Besides you...")
	hitretResult := Hitret(27000)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06c", 0, 0)
	Talk("Kazuha", "KA040043")
	Mess("You three...")
	hitretResult := Hitret(27001)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just as Ryouhei was going to say something, Migiwa-sanÅ^interrupts again.")
	hitretResult := Hitret(27002)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It looks like her head's twitching.")
	hitretResult := Hitret(27003)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, Sorr...")
	hitretResult := Hitret(27004)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06f", 0, 0)
	Talk("Ryouhei", "RH040014")
	Mess("What can the Ojou's do? Only calls? Too bad, huuh. Can'tÅ^beat my Haruka's with just that, right?")
	hitretResult := Hitret(27005)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06e", 0, 0)
	Talk("Kazuha", "KA040044")
	Mess("I don't care who wins what! Stop being such an idiot!")
	hitretResult := Hitret(27006)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...She exploded...")
	hitretResult := Hitret(27007)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06f", 0, 0)
	Talk("Kazuha", "KA040045")
	Mess("Ah...")
	hitretResult := Hitret(27008)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san quickly covered her mouth, but everyone inÅ^the class had already turned this way.")
	hitretResult := Hitret(27009)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Maths Teacher", "NP120005")
	Mess("Migiwa, Nakazato, Kasugano, Amatsume! Do you realizeÅ^what time it is?")
	hitretResult := Hitret(27010)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ06c", 0, 0)
	Talk("EveryoneÅsâlÅïàÍótÅïó∫ïΩÅt", "SYN00011")
	Mess("Sorry...")
	hitretResult := Hitret(27011)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(2000, 1)
	SetCg("B19a", 0, 0)
	SetBustup("CD01_07M", 0, 0)
	Transition("WIP_LR", 500)
	Talk("Kazuha", "KA040047")
	Mess("Geez, I had to go through something so humiliatingÅ^thanks to you.")
	hitretResult := Hitret(27012)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even when we reached a break, Migiwa-san's anger stillÅ^hadn't subsided.")
	hitretResult := Hitret(27013)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sorry, once I said it, they got all fired up...")
	hitretResult := Hitret(27014)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	SetBustup("CF01_01M", 0, 0)
	Talk("Akira", "AK040030")
	Mess("Sorry, Kazu-chan.")
	hitretResult := Hitret(27015)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040016")
	Mess("Ahh, sorry, Ojou. I didn't think you hated losing thatÅ^much.")
	hitretResult := Hitret(27016)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_06M", 0, 0)
	Talk("Kazuha", "KA040048")
	Mess("Who hates losing? I was just scolding you over your lackÅ^of diligence!")
	hitretResult := Hitret(27017)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's probably true... It even seems that way to me.")
	hitretResult := Hitret(27018)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH040017")
	Mess("Now now, calm down. Show us your phone for a bit longer,Å^Haruka.")
	hitretResult := Hitret(27019)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The only one who doesn't realize is the perpetrator.")
	hitretResult := Hitret(27020)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA040049")
	Mess("I don't really care.")
	hitretResult := Hitret(27021)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040018")
	Mess("So Haruka, what else can yours do?")
	hitretResult := Hitret(27022)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_05M", 0, 0)
	Talk("Kazuha", "KA040050")
	Mess("Listen to what I'm saying!")
	hitretResult := Hitret(27023)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH040019")
	Mess("Surely the Ojou is interested as well?")
	hitretResult := Hitret(27024)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040051")
	Mess("Tch...Interpreting however you like...")
	hitretResult := Hitret(27025)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("M-Migiwa-san, calm down...I haven't believed a word he'sÅ^saying.")
	hitretResult := Hitret(27026)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_07M", 0, 0)
	Talk("Kazuha", "KA040052")
	Mess("Kasugano-kun, I don't really care what you think of me.")
	hitretResult := Hitret(27027)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, I see.")
	hitretResult := Hitret(27028)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's probably true, but that did hurt a bit...")
	hitretResult := Hitret(27029)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040031")
	Mess("Kazu-chan, Haru-kun, calm down, calm down! Let's watchÅ^TV together!")
	hitretResult := Hitret(27030)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Like I said, mine can't connect to 1seg.")
	hitretResult := Hitret(27031)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA040053")
	Mess("Wan...as in a dog?")
	hitretResult := Hitret(27032)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even you said the same silly line as Amatsume...")
	hitretResult := Hitret(27033)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_10M", 0, 0)
	Talk("Kazuha", "KA040054")
	Mess("Eh, what's that? Did I just say something weird?")
	hitretResult := Hitret(27034)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH040020")
	Mess("Wahahaha, even the Ojou came up with a doggie! At thisÅ^rate, Akira won't be the loser!")
	hitretResult := Hitret(27035)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_03M", 0, 0)
	Talk("Akira", "AK040032")
	Mess("Ahaa, I'm the same as Kazu-chan, right?")
	hitretResult := Hitret(27036)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040055")
	Mess("Huh...you too, Akira?")
	hitretResult := Hitret(27037)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040021")
	Mess("Iyaa, the Ojou has her cute side too? Confusing 1seg forÅ^a doggie.")
	hitretResult := Hitret(27038)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040022")
	Mess("Come on, come on, show us that cute doggie!")
	hitretResult := Hitret(27039)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Like I said, mine...")
	hitretResult := Hitret(27040)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just as I was going to say that mine couldn't do thatÅ^again...")
	hitretResult := Hitret(27041)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_05M", 0, 0)
	Talk("Kazuha", "KA040056")
	Mess("...Grr...")
	hitretResult := Hitret(27042)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It felt like Migiwa-san's temple was twitching.")
	hitretResult := Hitret(27043)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_06M", 0, 0)
	Talk("Kazuha", "KA040057")
	Mess("Hih!")
	hitretResult := Hitret(27044)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se011", 0)
	Flush("WHITE", 100, "")
	SetBustup("CF01_06M", 0, 0)
	SetBustup("CC01_12M", 0, 0)
	Shake(7)
	Talk("Ryouhei", "RH040023")
	Mess("Umph!")
	hitretResult := Hitret(27045)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Down(7, 150, 1000, 3)
	Talk("êSÇÃê∫", "")
	Mess("Then, Migiwa-san's right hand flashed.")
	hitretResult := Hitret(27046)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't know where she pulled it out of, but inÅ^Migiwa-san's hand was a silver.....What is that? A tray?Å^A lid?")
	hitretResult := Hitret(27047)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She smacked that rectangular silver board over Ryouhei'sÅ^head.")
	hitretResult := Hitret(27048)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_09M", 0, 0)
	Talk("Akira", "AK040033")
	Mess("R-Ryou-niichan, you okay?")
	hitretResult := Hitret(27049)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_10M", 0, 0)
	Shake(7)
	Talk("Ryouhei", "RH040024")
	Mess("Nu...ooooooo....")
	hitretResult := Hitret(27050)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I think the corner hit, because Ryouhei was stillÅ^clutching his head.")
	hitretResult := Hitret(27051)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA040058")
	Mess("......")
	hitretResult := Hitret(27052)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And the one who hit him had a smile that seemed to say,Å^\"He deserved it.\"")
	hitretResult := Hitret(27053)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Now that I have a good look at the tray, it was dentedÅ^here and there, was dull and had traces of a worn outÅ^shine. For something so worn out, it really matched itsÅ^shape.")
	hitretResult := Hitret(27054)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("This sort of thing undoubtedly happens a lot, so sheÅ^keeps it in reserve...")
	hitretResult := Hitret(27055)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_10M", 0, 0)
	Talk("Kazuha", "KA040059")
	Mess("Ah...A-Anyway! Behave yourself, okay?")
	hitretResult := Hitret(27056)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san waved her hand...and then the silver trayÅ^disappeared like magic.")
	hitretResult := Hitret(27057)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040060")
	Mess("...I've told you about going crazy with them before,Å^haven't I, Kasugano-kun?")
	hitretResult := Hitret(27058)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y-yeah...I understand. I'll be careful.")
	hitretResult := Hitret(27059)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA040061")
	Mess("If that's the case, then okay.")
	hitretResult := Hitret(27060)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san...for an honor student, she's certainlyÅ^graceful, but...unexpectedly quick to resort toÅ^violence...")
	hitretResult := Hitret(27061)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's probably best to be careful and not make herÅ^angry...")
	hitretResult := Hitret(27062)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	Change("00_d004")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

