Include"yosuga.ch"

Function sc00_A018A()
	ScenarioEnter()
	ScPlaySe("SE357", 0)
	SetCg("B01a", 0, 0)
	Update()
	ScWait(2000, 0)
	BlackOut(0, 1)
	SetCg("B03a", 0, 0)
	Transition("WIP_BT", 500)
	Talk("êSÇÃê∫", "")
	Mess("Dawn, Sora left my room without saying anything.")
	hitretResult := Hitret(6107)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After that... I could hear Sora's sleeping breath, andÅ^my paralysis was finally lifted.")
	hitretResult := Hitret(6108)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And, with my head in disorder, before I noticed it I hadÅ^fallen asleep too, and I vaguely remember Sora slippingÅ^out of the futon.")
	hitretResult := Hitret(6109)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's scent was still left on the futon.")
	hitretResult := Hitret(6110)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I have the feeling that... Sora's feelings are stillÅ^left there.")
	hitretResult := Hitret(6111)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(6112)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I feel heavy.")
	hitretResult := Hitret(6113)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's sudden... confession......")
	hitretResult := Hitret(6114)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The feelings within Sora's heart.")
	hitretResult := Hitret(6115)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("For Sora to keep harboring those feelings...")
	hitretResult := Hitret(6116)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That omen... was there.")
	hitretResult := Hitret(6117)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora revealed words, about what I did with Nao.")
	hitretResult := Hitret(6118)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010683")
	Mess("If I did the same thing, I wonder if he'd come to loveÅ^me?")
	hitretResult := Hitret(6119)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM16", 0)
	Talk("êSÇÃê∫", "")
	Mess("Her words meant just that.")
	hitretResult := Hitret(6120)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Above and beyond the affection of family...")
	hitretResult := Hitret(6121)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Binding my connection with her, she wants me to come toÅ^love her move...")
	hitretResult := Hitret(6122)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora, stepped onto that boundary line.")
	hitretResult := Hitret(6123)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And, without rejecting her, or accepting her... I...Å^couldn't do anything...")
	hitretResult := Hitret(6124)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("No, it's not that I couldn't. I just didn't do anything.")
	hitretResult := Hitret(6125)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was confused,and... it was like my self-defenseÅ^mechanism wanted to pretend nothing had happened.")
	hitretResult := Hitret(6126)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I had a feeling that at that time... from just a singleÅ^action... no, a single word... everything would haveÅ^collapsed.")
	hitretResult := Hitret(6127)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If that had happened... it would never have been able toÅ^be undone.")
	hitretResult := Hitret(6128)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora...")
	hitretResult := Hitret(6129)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When she said she loved me, I was honestly... happy.")
	hitretResult := Hitret(6130)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even when we are occassionally mistaken as lovers, itÅ^doesn't feel unpleasant.")
	hitretResult := Hitret(6131)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There's no way I would hate Sora... In fact, I love her.Å^I can honestly say she is an important person to me.")
	hitretResult := Hitret(6132)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, that's of course, as an older brother... It'sÅ^affection for family.")
	hitretResult := Hitret(6133)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If we cross over that line, we can't go forward...")
	hitretResult := Hitret(6134)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Of course we can't... can we...?")
	hitretResult := Hitret(6135)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wonder, why I muttered that a second time...")
	hitretResult := Hitret(6136)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though it should usually be such a clear answer...")
	hitretResult := Hitret(6137)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wonder if... a part of inside me is divided ......")
	hitretResult := Hitret(6138)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That fact... I, myself, couldn't believe.")
	hitretResult := Hitret(6139)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B02a", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("As I was heading to the bathroom to wash my face, IÅ^could smell a good scent coming from the kitchen.")
	hitretResult := Hitret(6140)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_02M", 0, 0)
	Talk("Sora", "SR010684")
	Mess("Ah, good morning, Haru.")
	hitretResult := Hitret(6141)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Breakfast was already lined up on the table.")
	hitretResult := Hitret(6142)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010685")
	Mess("The sunny-side-up fried eggs are almost done, so go washÅ^your face.")
	hitretResult := Hitret(6143)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Yeah.")
	hitretResult := Hitret(6144)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's long hair fluttering as she prepared...Å^captivated me for a moment.")
	hitretResult := Hitret(6145)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora seemed, happy for some reason.")
	hitretResult := Hitret(6146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Usually, she would sluggishly prepare absentmindedly...Å^No, her preparing something at all was rare...")
	hitretResult := Hitret(6147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("After that, the two of us ate breakfast as usual.")
	hitretResult := Hitret(6148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We both silently put food into our mouths as usual...Å^Although not everything was as usual, for some reason IÅ^was a little uncomfortable.")
	hitretResult := Hitret(6149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Without paying mind to Sora, I dipped the bread crustÅ^into the tea and ate it.")
	hitretResult := Hitret(6150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I peeked at Sora's facial expression a few times, butÅ^she looked the same as she always does.")
	hitretResult := Hitret(6151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("The atmosphere of something that couldn't be seen beingÅ^unbound, was probably just my imagination......")
	hitretResult := Hitret(6152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B34a", 400, 0)
	SetBustup("CA02_02M", 0, 0)
	ScPlayBgm("BGM03", 0)
	Talk("Sora", "SR010686")
	Mess("...Hey, Haru, it's not heavy?")
	hitretResult := Hitret(6153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nn...? No, not really...")
	hitretResult := Hitret(6154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010687")
	Mess("...So you're that strong...")
	hitretResult := Hitret(6155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Strong, huh... Well, I'm a man, so I think anyone couldÅ^do this much...")
	hitretResult := Hitret(6156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We had went to go buy stuff from the farmer that NaoÅ^introduced us to before.")
	hitretResult := Hitret(6157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was holding a large watermelon and a heavy sack withÅ^vegetables and fruits.")
	hitretResult := Hitret(6158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Since I went together with Sora, I ended up buying moreÅ^than I had planned.")
	hitretResult := Hitret(6159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Never showing a desire to go up until now, today forÅ^some reason Sora suddenly told me she wanted to come.")
	hitretResult := Hitret(6160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The farmers were surprised to see Sora for the firstÅ^time, and, as a result, they threw in some extras forÅ^free.")
	hitretResult := Hitret(6161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At first, Sora tried to carry the sack fully loaded withÅ^vegetables with both hands, but it still made herÅ^stagger. So, I ended up carrying everything.")
	hitretResult := Hitret(6162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Although, that was definitely heavier than what sheÅ^usually carries.")
	hitretResult := Hitret(6163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010688")
	Mess("Umm... Afterwards, rice too?")
	hitretResult := Hitret(6164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sure, but we can't carry all that in one load, so weÅ^need to make a trip home first.")
	hitretResult := Hitret(6165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010689")
	Mess("...I want to eat the watermelon before it gets cold.")
	hitretResult := Hitret(6166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's true... But, if we don't eat half of it, itÅ^probably can't fit in the refrigerator.")
	hitretResult := Hitret(6167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_03M", 0, 0)
	Talk("Sora", "SR010690")
	Mess("Fufu... Then you should do your best to help me eat it.")
	hitretResult := Hitret(6168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't ask the impossible...")
	hitretResult := Hitret(6169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_02M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("A never-ending conversation had begun.")
	hitretResult := Hitret(6170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The middle-aged farmer just now said that we looked likeÅ^we were lovers.")
	hitretResult := Hitret(6171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wonder if it was only because we got along so well.")
	hitretResult := Hitret(6172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Our bickering, while not minding anyone else, didn'tÅ^look like we were really bickering.")
	hitretResult := Hitret(6173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's in no way a bad thing, or an unpleasant thing. InÅ^fact, it's what I... should have wanted.")
	hitretResult := Hitret(6174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But... I couldn't hide my anxiety....")
	hitretResult := Hitret(6175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...that Sora suddenly changed her personality.")
	hitretResult := Hitret(6176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And, that I couldn't catch hold of Sora's feelings...")
	hitretResult := Hitret(6177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH010302")
	Mess("Ohhh, well if it isn't Haruka! So you came back, right?")
	hitretResult := Hitret(6178)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, just yesterday.")
	hitretResult := Hitret(6179)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010303")
	Mess("Sora-chan too...! Long time no see...! You're as cute asÅ^always today.")
	hitretResult := Hitret(6180)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_11M", 0, 0)
	Talk("Sora", "SR010691")
	Mess("......")
	hitretResult := Hitret(6181)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	Talk("Ryouhei", "RH010304")
	Mess("So the two of you are getting along well and shopping?Å^Need some help with it?")
	hitretResult := Hitret(6182)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks, that'd be a big help.")
	hitretResult := Hitret(6183)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010305")
	Mess("This much is no problem...... ...Uuohh, heavy...!?")
	hitretResult := Hitret(6184)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_06M", 0, 0)
	Shake(7)
	Talk("Haruka", "")
	Mess("I know, right? Ryouhei, you can hold the cabbageÅ^instead.")
	hitretResult := Hitret(6185)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH010306")
	Mess("...You know, you're... pretty strong, huh...")
	hitretResult := Hitret(6186)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I don't think it's because I'm strong... I'm justÅ^holding it because no one else can.")
	hitretResult := Hitret(6187)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010307")
	Mess("Hmm, well it's true that you can't let Sora hold it. ByÅ^the way, are you done with your summer homework?")
	hitretResult := Hitret(6188)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Eh? Yeah, pretty much.")
	hitretResult := Hitret(6189)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_05M", 0, 0)
	Talk("Ryouhei", "RH010308")
	Mess("Wha...!? ...Want to compare answers?")
	hitretResult := Hitret(6190)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ohh, so you're done too? That's amazing...")
	hitretResult := Hitret(6191)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_03M", 0, 0)
	Talk("Ryouhei", "RH010309")
	Mess("Heh, when you become a sophomore, it's too easy.")
	hitretResult := Hitret(6192)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, then if I have a problem I don't understand, wouldÅ^you be able to help me?")
	hitretResult := Hitret(6193)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_05M", 0, 0)
	Talk("Ryouhei", "RH010310")
	Mess("Y--You should solve it yourself. It's for your own good,Å^after all.")
	hitretResult := Hitret(6194)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...For some reason it seems like you're someoneÅ^completely different.")
	hitretResult := Hitret(6195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010311")
	Mess("H--Here, you've heard it a lot, right? 'If you don't seeÅ^a man for a whole day, their eyes will change.'")
	hitretResult := Hitret(6196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("'If you don't see a man for three days, observeÅ^carefully.' ...Right?")
	hitretResult := Hitret(6197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH010312")
	Mess("Y--Yep, yep. That's it, that's the one.")
	hitretResult := Hitret(6198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_06M", 0, 0)
	Talk("Sora", "SR010692")
	Mess("......")
	hitretResult := Hitret(6199)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B01a", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Haruka", "")
	Mess("Thanks, you were a big help.")
	hitretResult := Hitret(6200)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH010313")
	Mess("Eh, it was nothing.")
	hitretResult := Hitret(6201)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Want to come in? Though, I really only have tea toÅ^offer...")
	hitretResult := Hitret(6202)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_03M", 0, 0)
	Talk("Ryouhei", "RH010314")
	Mess("Nah, you don't have to do that. But, you insist...")
	hitretResult := Hitret(6203)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	SetBustup("CA02_02M", 0, 0)
	Talk("Sora", "SR010693")
	Mess("Thanks for helping. Goodbye.")
	hitretResult := Hitret(6204)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se107", 0)
	SetCg("BLACK", 0, 0)
	Transition("WIP_LR", 250)
	Talk("Ryouhei", "RH010315")
	Mess("Err.... Huh!? Hey...!?")
	hitretResult := Hitret(6205)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B02a", 0, 0)
	Talk("Haruka", "")
	Mess("H--Hey... Sora, why'd you do that?")
	hitretResult := Hitret(6206)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_02M", 0, 0)
	Talk("Sora", "SR010694")
	Mess("Next we're going to buy rice, right? Want to get it overÅ^with before it gets hot?")
	hitretResult := Hitret(6207)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("W--Well, that's true, but... We shouldn't blow offÅ^Ryousuke when he helped us.")
	hitretResult := Hitret(6208)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010695")
	Mess("...Let's put some watermelon in water in the bath. ThenÅ^let's eat it afterwards?")
	hitretResult := Hitret(6209)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	ScPlaySe("SE015", 0)
	Talk("êSÇÃê∫", "")
	Mess("Sora went to carry off the watermelon.")
	hitretResult := Hitret(6210)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(6211)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopSe()
	Talk("êSÇÃê∫", "")
	Mess("...I don't think Ryouhei will get angry over somethingÅ^like this, but I should apologize later.")
	hitretResult := Hitret(6212)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But still... what in the world is up with Sora...")
	hitretResult := Hitret(6213)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Honestly, I can't hide my confusion.")
	hitretResult := Hitret(6214)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What happened last night was, of course, the start ofÅ^what happened this morning... and, what happened justÅ^now.")
	hitretResult := Hitret(6215)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She's somehow different from the Sora up until now.")
	hitretResult := Hitret(6216)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm happy that she's always smiling now.")
	hitretResult := Hitret(6217)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, with that attitude just now... it was as if she wasÅ^denying Ryouhei that smile...")
	hitretResult := Hitret(6218)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wonder if... something changed, inside Sora.")
	hitretResult := Hitret(6219)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("An undescribable anxiety just crossed my heart.")
	hitretResult := Hitret(6220)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("", 0)
	Change("00_a018b")
	Return
	Change("EXIT_SCENARIO")
EndFunc

