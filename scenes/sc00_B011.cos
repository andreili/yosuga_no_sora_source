Include"yosuga.ch"

Function sc00_B011()
	ScenarioEnter()
	ScPlaySe("SE357", 0)
	SetCg("B12a", 0, 0)
	Talk("Haruka", "")
	Mess("...Hmmm? Huh... Amatsume, what is she doing?")
	hitretResult := Hitret(12780)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR030087")
	Mess("......")
	hitretResult := Hitret(12781)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora and I usually walk in this street when we're goingÅ^to school, but then we saw Amatsume.")
	hitretResult := Hitret(12782)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Amatsumee! What are you doing?")
	hitretResult := Hitret(12783)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume is somehow squatting by the corner of the road.")
	hitretResult := Hitret(12784)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM05", 0)
	SetBustup("CC01_02M", 0, 0)
	Jump(2, 0)
	Talk("Akira", "AK030070")
	Mess("Ah, Haru-kun, Sora-chan, good morning!")
	hitretResult := Hitret(12785)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She's really energetic, and it looked like she jumped upÅ^as she stood up.")
	hitretResult := Hitret(12786)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What were you doing at that kind of place?")
	hitretResult := Hitret(12787)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK030071")
	Mess("Ah, you see, decorating.")
	hitretResult := Hitret(12788)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Decorating?")
	hitretResult := Hitret(12789)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 8, 0)
	SetBustup("CC01_01M", 5, 0)
	Talk("êSÇÃê∫", "")
	Mess("Amatsume took a step, and we just watched her on whatÅ^she was doing.")
	hitretResult := Hitret(12790)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("By the corner of the road, there seem to look like smallÅ^shrine.")
	hitretResult := Hitret(12791)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's around sixty centimeters in height and it's moldedÅ^into a tear drop monolithic shape.")
	hitretResult := Hitret(12792)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was clean, and it looked new with the decorations onÅ^it.")
	hitretResult := Hitret(12793)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's appearance looked like something is carved in it,Å^though I can't read it at al.")
	hitretResult := Hitret(12794)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("This is...?")
	hitretResult := Hitret(12795)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK030072")
	Mess("A God.")
	hitretResult := Hitret(12796)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 8, 0)
	Talk("Sora", "SR030088")
	Mess("...A traveler's guardian deity?")
	hitretResult := Hitret(12797)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK030073")
	Mess("That's right, you got it correctly Sora-chan.")
	hitretResult := Hitret(12798)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_11M", 0, 0)
	Talk("Sora", "SR030089")
	Mess("...N-not really...")
	hitretResult := Hitret(12799)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see... Amatsume's a shrine maiden after all...")
	hitretResult := Hitret(12800)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK030074")
	Mess("Yeah, summer festival is getting near, I need to sweepÅ^it fairly and I need to decorate it too!")
	hitretResult := Hitret(12801)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heh, summer festival.")
	hitretResult := Hitret(12802)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK030075")
	Mess("Yes, yes, it's really fun, especially if the both of youÅ^come and play.")
	hitretResult := Hitret(12803)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yep, we'll definitely go.")
	hitretResult := Hitret(12804)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH030188")
	Mess("Yoh, what are you guys doing? Ah, oh yeah, it's almostÅ^time for that.")
	hitretResult := Hitret(12805)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK030076")
	Mess("Morning, Ryou-niichan. That's right, I'll be in yourÅ^care in this year too.")
	hitretResult := Hitret(12806)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_02M", 0, 0)
	Talk("Ryouhei", "RH030189")
	Mess("Ah, leave it to me. Oh, Haruka, of course you would helpÅ^too right?")
	hitretResult := Hitret(12807)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heh? Help?")
	hitretResult := Hitret(12808)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030190")
	Mess("Yes, yes, \"help\".")
	hitretResult := Hitret(12809)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("About what?")
	hitretResult := Hitret(12810)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH030191")
	Mess("That's because you've decided to go to the festivalÅ^right? They need someone to manage the tools to shiftÅ^the scene in the ceremony, something like that.")
	hitretResult := Hitret(12811)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030192")
	Mess("Well, if you help, there's lunch by the afternoon, weÅ^get paid by that.")
	hitretResult := Hitret(12812)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heeh, so that kind of job exist? That's my first timeÅ^hearing a job like that.")
	hitretResult := Hitret(12813)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030193")
	Mess("Of course, better come along okay?")
	hitretResult := Hitret(12814)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, that's great. Somehow, it sounds interesting, it'sÅ^like helping.")
	hitretResult := Hitret(12815)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	SetBustup("CF01_05M", 0, 0)
	Talk("Ryouhei", "RH030194")
	Mess("Of course, Sora-chan would come to right?")
	hitretResult := Hitret(12816)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR030090")
	Mess("Eh... I don't like.")
	hitretResult := Hitret(12817)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH030195")
	Mess("Don't say such things, please? It would be fun.")
	hitretResult := Hitret(12818)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK030077")
	Mess("Well, aside from helping, we also welcome those who justÅ^come and play you know?")
	hitretResult := Hitret(12819)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("With all trouble of coming, let's go and see it okay?Å^Even if you don't feel like helping.")
	hitretResult := Hitret(12820)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_11M", 0, 0)
	Talk("Sora", "SR030091")
	Mess("...I'm fine with that, but...")
	hitretResult := Hitret(12821)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK030078")
	Mess("Yehey! Thank you, Sora-chan.")
	hitretResult := Hitret(12822)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030196")
	Mess("I shall recommend and guide you to the nice stalls, lookÅ^forward to it okay?")
	hitretResult := Hitret(12823)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK030079")
	Mess("Sora-chan, if you want to be a Shrine maiden just say soÅ^okay? I can lend you some clothes for the Shrine maiden.")
	hitretResult := Hitret(12824)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR030092")
	Mess("N-not really... It's fine...")
	hitretResult := Hitret(12825)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK030080")
	Mess("It looks like it's going to interesting this year too!")
	hitretResult := Hitret(12826)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_03M", 0, 0)
	Talk("Ryouhei", "RH030197")
	Mess("I think I'll give you a present so you won't forget suchÅ^memories.")
	hitretResult := Hitret(12827)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR030093")
	Mess("...I don't need it.")
	hitretResult := Hitret(12828)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	SetBustup("CF01_01M", 0, 0)
	Talk("Haruka", "")
	Mess("...Festival huh.")
	hitretResult := Hitret(12829)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Until now, I never got to go to a festival, it's also myÅ^first time helping in their preparations.")
	hitretResult := Hitret(12830)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("By the way, when will the festival be?")
	hitretResult := Hitret(12831)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK030081")
	Mess("When we finished our final tests.")
	hitretResult := Hitret(12832)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_09M", 0, 0)
	Talk("Ryouhei", "RH030198")
	Mess("Hey, better remember it okay? It'll be a waste if youÅ^forget about it.")
	hitretResult := Hitret(12833)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK030082")
	Mess("It's all right, I'll do something about it.")
	hitretResult := Hitret(12834)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, that's right... there's also the final exams...")
	hitretResult := Hitret(12835)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH030199")
	Mess("Be careful okay? Supplementary lessons are quiteÅ^blunderous you know? Summer vacation is a precious thingÅ^after all.")
	hitretResult := Hitret(12836)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So the summer vacation here is shorter than the winterÅ^vacation... Or not?")
	hitretResult := Hitret(12837)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030200")
	Mess("Because we're not in a northern province, it would beÅ^bad to push it in.")
	hitretResult := Hitret(12838)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, so it's not.")
	hitretResult := Hitret(12839)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The atmosphere here is quite harmonious, I also haven'tÅ^seen a television here in the country side since itÅ^looks like no one has one.")
	hitretResult := Hitret(12840)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK030083")
	Mess("Ahahaha, you're doing something really interesting.Å^Throwing beans at something to knock it down seemsÅ^really interesting.")
	hitretResult := Hitret(12841)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030201")
	Mess("The bean throwing night, it's my second time to goÅ^there.")
	hitretResult := Hitret(12842)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I wasn't here during summer, so I don't really know whatÅ^to do, so maybe I'll ask around the town about it.")
	hitretResult := Hitret(12843)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030202")
	Mess("It can't be helped right, well, are you coming on theÅ^next rice reaping festival?")
	hitretResult := Hitret(12844)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("R-rice reaping? Everyone's harvesting?")
	hitretResult := Hitret(12845)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_05M", 0, 0)
	Talk("Ryouhei", "RH030203")
	Mess("Don't you dare cheat on us huh.")
	hitretResult := Hitret(12846)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_14M", 0, 0)
	Talk("Akira", "AK030084")
	Mess("Yeah, though I don't think he won't be able to doÅ^anything if you invite him.")
	hitretResult := Hitret(12847)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CB01_03M", 0, 0)
	Talk("Nao", "NO030505")
	Mess("Heeey, aren't you guys teasing him too much?")
	hitretResult := Hitret(12848)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, Nao-chan, good morning.")
	hitretResult := Hitret(12849)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030506")
	Mess("Good morning, everyone. It's almost time for festival...Å^time surely flies by.")
	hitretResult := Hitret(12850)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's right...")
	hitretResult := Hitret(12851)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's been a month ever since we came back to this town,Å^and it passed by just like that.")
	hitretResult := Hitret(12852)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We're so desperate to get used to it, though we feelÅ^much better just like that.")
	hitretResult := Hitret(12853)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO030507")
	Mess("Well, we should go to school now, or do you guys plan onÅ^being late?")
	hitretResult := Hitret(12854)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah.")
	hitretResult := Hitret(12855)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The summer vacation's not the only thing I want to seeÅ^in this town, there's so many more.")
	hitretResult := Hitret(12856)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Still, I think it would really be fun.")
	hitretResult := Hitret(12857)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("", 0)
	Change("00_b012")
	Return
	Change("EXIT_SCENARIO")
EndFunc

