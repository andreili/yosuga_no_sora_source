Include"yosuga.ch"

Function sc00_B027B()
	ScenarioEnter()
	SetCg("B18a", 0, 0)
	ScPlayEnvSe("SE403", "0", 0)
	Talk("Haruka", "")
	Mess("………………………………")
	hitretResult := Hitret(16535)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("The opening ceremony just ended, though it ended quite／early today.")
	hitretResult := Hitret(16536)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("With a quick pace, I went to the room of my seniors.")
	hitretResult := Hitret(16537)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Did Nao-chan go home already?")
	hitretResult := Hitret(16538)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("A lot of people went out of the room as their homeroom／ended, I took a peek at Nao-chan's room and checked who／was left behind.")
	hitretResult := Hitret(16539)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("……………………………")
	hitretResult := Hitret(16540)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Everyone on the classroom left in groups as they／gathered and talked to each other.")
	hitretResult := Hitret(16541)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Which reminds me, I know Nao-chan's classmates, but they／haven't seen me yet.")
	hitretResult := Hitret(16542)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Where could her seat be...")
	hitretResult := Hitret(16543)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao's friend", "NP260020")
	Mess("Huh? You are...?")
	hitretResult := Hitret(16544)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, hello...")
	hitretResult := Hitret(16545)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Some of her classmates were looking at me, then one of／Nao-chan's friends called out to me.")
	hitretResult := Hitret(16546)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Umm... Nao-cha--... Wh-where is Yorihime-senpai?")
	hitretResult := Hitret(16547)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao's friend", "NP260021")
	Mess("Huh, you just missed her. She just came out of the／classroom.")
	hitretResult := Hitret(16548)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Is that so? Thank you.")
	hitretResult := Hitret(16549)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE403", 0)
	Talk("Nao's friend", "NP260022")
	Mess("Say, what is about?")
	hitretResult := Hitret(16550)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh...")
	hitretResult := Hitret(16551)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM16", 0)
	Talk("Nao's friend", "NP260023")
	Mess("Ah, no, never mind, just forget it.")
	hitretResult := Hitret(16552)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes...")
	hitretResult := Hitret(16553)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao's friend", "NP260024")
	Mess("I think you can still catch up to her? You guys live in／the same area after all.")
	hitretResult := Hitret(16554)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes, thank you very much.")
	hitretResult := Hitret(16555)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I bowed to her and left right after.")
	hitretResult := Hitret(16556)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Nao-chan was here in school today.")
	hitretResult := Hitret(16557)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("If I can just only... Talk to Nao-chan for just a／little.")
	hitretResult := Hitret(16558)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I ran faster as I made my through the shoe lockers.")
	hitretResult := Hitret(16559)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Just to talk with Nao-chan.")
	hitretResult := Hitret(16560)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1000, 0)
	WindowView(1)
	SetCg("B20a", 0, 0)
	SetBustup("CF01_01M", 0, 0)
	Transition("WIP_BT", 500)
	Talk("Ryouhei", "RH030371")
	Mess("Hmm? Huh... Oii!! Nao!!!")
	hitretResult := Hitret(16561)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_05M", 0, 0)
	Talk("Nao", "NO031725")
	Mess("………………………………")
	hitretResult := Hitret(16562)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_06M", 0, 0)
	Talk("Ryouhei", "RH030372")
	Mess("Tch... Wh-what's the matter with you?")
	hitretResult := Hitret(16563)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_09M", 0, 0)
	Talk("Nao", "NO031726")
	Mess("...Nothing, nothing in particular...")
	hitretResult := Hitret(16564)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_06M", 0, 0)
	Talk("Nao", "NO031727")
	Mess("... What about it?")
	hitretResult := Hitret(16565)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF01_01M", 0, 0)
	Talk("Ryouhei", "RH030373")
	Mess("Ah, no... I was wondering where Haruka went?")
	hitretResult := Hitret(16566)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_10M", 0, 0)
	Talk("Nao", "NO031728")
	Mess("...Oh yeah... I just though of going home earlier today.")
	hitretResult := Hitret(16567)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030374")
	Mess("...Aaah...")
	hitretResult := Hitret(16568)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	SetBustup("CF01_08M", 0, 0)
	Talk("Ryouhei", "RH030375")
	Mess("...That rascal...")
	hitretResult := Hitret(16569)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide()
	SetCg("BLACK", 0, 0)
	Transition("WIP_LR", 500)
	WindowView(0)
	Update()
	SetCg("B20a", 0, 0)
	SetBustup("CF01_08M", 0, 0)
	Transition("WIP_LR", 500)
	Talk("Haruka", "")
	Mess("Ah, Ryouhei!!")
	hitretResult := Hitret(16570)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030376")
	Mess("...Nao said that she's going home early.")
	hitretResult := Hitret(16571)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Is that so?")
	hitretResult := Hitret(16572)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH030377")
	Mess("Come on, let's run and catch up to her.")
	hitretResult := Hitret(16573)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Got it, thanks.")
	hitretResult := Hitret(16574)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("……………………………………")
	hitretResult := Hitret(16575)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Ryouhei", "RH030378")
	Mess("... Damn, you're such a sight to see... Nao...")
	hitretResult := Hitret(16576)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(2000, 0)
	Change("00_b027c")
	Return
	Change("EXIT_SCENARIO")
EndFunc

