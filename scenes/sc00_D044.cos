Include"yosuga.ch"

Function sc00_D044()
	ScenarioEnter()
	SetCg("B07a", 0, 0)
	ScPlayBgm("BGM04", 0)
	Talk("êSÇÃê∫", "")
	Mess("On the shrine grounds, a nostalgic tune was echoing.")
	hitretResult := Hitret(33297)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It brought a soothing feeling with its gentle tone.")
	hitretResult := Hitret(33298)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If I close my eyes, it feels like I'm being hugged by myÅ^mother...")
	hitretResult := Hitret(33299)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED18a", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("It feels good... If I stay like this, then I might justÅ^fall asleep....")
	hitretResult := Hitret(33300)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040500")
	Mess("KuuÅ`Å`Å`")
	hitretResult := Hitret(33301)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED18b", 0, 0)
	Talk("Kazuha", "KA041537")
	Mess("Hold on! You just said \"KuÅ`!\" You're sleeping!")
	hitretResult := Hitret(33302)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040501")
	Mess("Hwaaa....I'm not! I'm not asleep, okay?")
	hitretResult := Hitret(33303)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I'm not sleeping either, okay?")
	hitretResult := Hitret(33304)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041538")
	Mess("Then... how was that performance?")
	hitretResult := Hitret(33305)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("H-How can I put it? It had a gentle, pleasant feel toÅ^it! Like a lullaby!")
	hitretResult := Hitret(33306)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041539")
	Mess("Geez, say something more tangible! There's no way I canÅ^correct it with something like that!")
	hitretResult := Hitret(33307)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even if you say that... I'm not a viola teacher....")
	hitretResult := Hitret(33308)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041540")
	Mess("And Akira was fast asleep!")
	hitretResult := Hitret(33309)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040502")
	Mess("That's not true. I really did listen!")
	hitretResult := Hitret(33310)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED18a", 0, 0)
	Talk("Kazuha", "KA041541")
	Mess("Oh, whatever. It's not like I can ask you for yourÅ^honest impressions.")
	hitretResult := Hitret(33311)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041542")
	Mess("...Same goes for Haruka too, though.")
	hitretResult := Hitret(33312)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahahaha.")
	hitretResult := Hitret(33313)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041543")
	Mess("For some reason, Haruka, you're becoming more and moreÅ^like Akira...")
	hitretResult := Hitret(33314)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh, really!?")
	hitretResult := Hitret(33315)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041544")
	Mess("Like using those innocent lines!")
	hitretResult := Hitret(33316)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040503")
	Mess("But, but... She was really great before, right?")
	hitretResult := Hitret(33317)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh. Yeah, yeah. At the contest.")
	hitretResult := Hitret(33318)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041545")
	Mess("Even though you don't understand a thing.")
	hitretResult := Hitret(33319)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040504")
	Mess("I do understand....here.")
	hitretResult := Hitret(33320)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After what sounded like Amatsume rummaging around, sheÅ^took a scrap of paper from her chest.")
	hitretResult := Hitret(33321)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was a snippet from the local newspaper, so it was inÅ^black and white, but there was a large picture ofÅ^Kazuha.")
	hitretResult := Hitret(33322)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED18c", 0, 0)
	Talk("Kazuha", "KA041546")
	Mess("Ah... Geez...")
	hitretResult := Hitret(33323)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040505")
	Mess("You got an award at the contest, so you must be amazing!")
	hitretResult := Hitret(33324)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was an article about the local music contest whereÅ^she won a prize a few days ago.")
	hitretResult := Hitret(33325)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It wasn't a contest that you could call big, but as thisÅ^was only her debut, it was amazing enough.")
	hitretResult := Hitret(33326)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The school club seems to be doing well, too.")
	hitretResult := Hitret(33327)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041547")
	Mess("Yeah, well... People who are interested have come, so...")
	hitretResult := Hitret(33328)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040506")
	Mess("Over here it reads in huge letters \"Representing theÅ^Japanese classical society, the next generationÅ^violist.\"")
	hitretResult := Hitret(33329)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041548")
	Mess("T-That's just an exaggeration! It was just localÅ^favoritism!")
	hitretResult := Hitret(33330)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040507")
	Mess("Even in this newspaper, see!")
	hitretResult := Hitret(33331)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Saying so, Amatsume took out one newspaper clippingÅ^after another.")
	hitretResult := Hitret(33332)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041549")
	Mess("Just how many of those are you carrying?")
	hitretResult := Hitret(33333)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Well, even in my house, we've got a book withÅ^newspaper clippings pasted in it...")
	hitretResult := Hitret(33334)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040508")
	Mess("Good luck next time, too! I'm coming again to cheer forÅ^you!")
	hitretResult := Hitret(33335)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041550")
	Mess("You don't have to, geez... You two fell asleep at theÅ^last one. Do you know how embarrassing that was?")
	hitretResult := Hitret(33336)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041551")
	Mess("I've got enough to be embarrassed about!")
	hitretResult := Hitret(33337)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED18a", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("With a displeased look, she started playing again.")
	hitretResult := Hitret(33338)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her usual tune, the one that her mother taught her.")
	hitretResult := Hitret(33339)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A peaceful, gentle tune. So much that I wonder if she'sÅ^purposely trying to put us to sleep.")
	hitretResult := Hitret(33340)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She wore the gentle smile of a mother singing a lullabyÅ^to her child.")
	hitretResult := Hitret(33341)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With our shoulders huddled together, we'll listen toÅ^that tune forever.")
	hitretResult := Hitret(33342)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	OnGameClear()
	Change("EXIT_SCENARIO")
	Return
	Change("EXIT_SCENARIO")
EndFunc

