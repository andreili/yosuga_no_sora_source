Include"yosuga.ch"

Function sc00_E013B()
	ScenarioEnter()
	ScPlayEnvSe("SE300", "", 0)
	ScWait(2000, 0)
	SetCg("B34c", 400, 0)
	Talk("心の声", "")
	Mess("The total lack fo streetlights would give the impression／of total darkness during our journey, but the stars／provided a surprising amount of reassuring light.")
	hitretResult := Hitret(35373)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Although it seems contradictory, the night feels dark,／yet very bright at the same time.")
	hitretResult := Hitret(35374)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("It was even possible to feel a certain warmth comeing／from somewhere...")
	hitretResult := Hitret(35375)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050483")
	Mess("Isn't it hot?")
	hitretResult := Hitret(35376)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM19", 0)
	SetCg("EE02a", 0, 0)
	ScStopEnvSe("SE300", 0)
	Talk("Haruka", "")
	Mess("Not really...")
	hitretResult := Hitret(35377)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("...The warmth was coming from not only the hot weather,／but also from the fact that I was carrying Motoka-san.")
	hitretResult := Hitret(35378)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050484")
	Mess("Is that so? But isn't there sweat on your neck? ...Hehe／haaaah.")
	hitretResult := Hitret(35379)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah!? ... Don't blow me!")
	hitretResult := Hitret(35380)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050485")
	Mess("Ahahaha, Haruka-kun's cute~! You're blushing~!")
	hitretResult := Hitret(35381)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't move around so much... and could you also stop／teasing me.")
	hitretResult := Hitret(35382)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I centered Motoka-san on my back again, reaffirmed my／footing and continued onwards.")
	hitretResult := Hitret(35383)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Motoka-san's legs had no power left in them, and while I／had originally intended on lending a supporting／shoulder, it became simply carring her.")
	hitretResult := Hitret(35384)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("For some reason that I did not know, Motoka-san seemed／strangley happy.")
	hitretResult := Hitret(35385)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050486")
	Mess("Mm Haruka-kun, your back is much igger than I remember.")
	hitretResult := Hitret(35386)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Wh-what are you saying... and stop moving around, how／many times do I have to say this...")
	hitretResult := Hitret(35387)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Every time she moves, she slides down a little and I／have to correct it.")
	hitretResult := Hitret(35388)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("...Every time, I'll also feel something soft on my／back... ah, what am I thinking!")
	hitretResult := Hitret(35389)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I'm sure my face is totally red... thank goodness the／only light is from the stars...")
	hitretResult := Hitret(35390)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE02b", 0, 0)
	Talk("Motoka", "MT050487")
	Mess("You're heartless, Haruka-kun.")
	hitretResult := Hitret(35391)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What are you saying...")
	hitretResult := Hitret(35392)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050488")
	Mess("That \"desuka\" that you always do. Am I a burden or a／woman to you?")
	hitretResult := Hitret(35393)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I have no opinion. You seem to care so much.")
	hitretResult := Hitret(35394)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050489")
	Mess("I do care. I've caused you so much trouble.")
	hitretResult := Hitret(35395)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("As long as you don't vomit on my back, it's no trouble.")
	hitretResult := Hitret(35396)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050490")
	Mess("Ah~...over there, turn left.")
	hitretResult := Hitret(35397)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE02a", 0, 0)
	Talk("心の声", "")
	Mess("I walk abiding by the directions Motoka-san gives me.")
	hitretResult := Hitret(35398)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("All around us were frogs, loudly chorusing without／restraint, making it difficult to make out Motoka-san's／murmurs.")
	hitretResult := Hitret(35399)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Even so, her voice was really quiet...it's not like／she's doing this on purpose, right...?")
	hitretResult := Hitret(35400)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Well, there's no reason to be suspicious.")
	hitretResult := Hitret(35401)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("About how much longer until we get there?")
	hitretResult := Hitret(35402)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE02c", 0, 0)
	Talk("Motoka", "MT050491")
	Mess("........")
	hitretResult := Hitret(35403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Motoka-san?")
	hitretResult := Hitret(35404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050492")
	Mess("zzzzzzz...")
	hitretResult := Hitret(35405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, she fell asleep.")
	hitretResult := Hitret(35406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050493")
	Mess("....mhmmmmhmm...I can't eat any more...")
	hitretResult := Hitret(35407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("She's mumbling like a kid.")
	hitretResult := Hitret(35408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Wait, I don't have time for this mumbling!!")
	hitretResult := Hitret(35409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE02b", 0, 0)
	Talk("Motoka", "MT050494")
	Mess("Kya...geez, why can't you quietly let me sleep.")
	hitretResult := Hitret(35410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If you fall asleep, I won't be able to take you home...")
	hitretResult := Hitret(35411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("What an act...I don't know what she's thinking.")
	hitretResult := Hitret(35412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050495")
	Mess("If you go straight, we'll get there, so it's fine for me／to sleep.")
	hitretResult := Hitret(35413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...I don't know which house it is.")
	hitretResult := Hitret(35414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050496")
	Mess("What a selfish kid!")
	hitretResult := Hitret(35415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("...She's drunk, right? If she wasn't, then she wouldn't／be saying such absurd things...")
	hitretResult := Hitret(35416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Isn't it about time you stopped getting drunk?")
	hitretResult := Hitret(35417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050497")
	Mess("...Is my drunken behavior really so bad?")
	hitretResult := Hitret(35418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Wow, I have a feeling she's drilling a hole in my nape／with her stare. Not that I can turn to check since I'm／the one carrying her.")
	hitretResult := Hitret(35419)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050498")
	Mess("Even Haruka-kun, are you saying it's hard to keep up／with me?")
	hitretResult := Hitret(35420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's not what I meant... I mean, you don't behave like／your usual self, Motoka-san.")
	hitretResult := Hitret(35421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050499")
	Mess("Not like my usual self? Then, how do I behave usually?")
	hitretResult := Hitret(35422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well... That is... Hmm...")
	hitretResult := Hitret(35423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Adult-like? Person of firm character? Calm, composed and／intellectual?")
	hitretResult := Hitret(35424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Well, maybe that's not quite it...")
	hitretResult := Hitret(35425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Aggressive drinker, tactless statterbrain... I can't／tell her such a negative evaluation.")
	hitretResult := Hitret(35426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, kind... a nice person, basically?")
	hitretResult := Hitret(35427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("初佳", "MT050500")
	Mess("…………………………………………………………………………")
	Mess("…………………………………………………………………………")
	Mess("……………………………………んん！")
	hitretResult := Hitret(35428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE02d", 0, 0)
	Update(1)
	Quake(16, 0, 1, 2, 30)
	Talk("悠", "")
	Mess("ぐぇっ！")
	hitretResult := Hitret(35429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("ちょっ……まっ……く、くびっ……")
	hitretResult := Hitret(35430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050501")
	Mess("......Who is... a good person... huh?")
	hitretResult := Hitret(35431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("悠", "")
	Mess("く、くるっ……くび……しまっ……")
	hitretResult := Hitret(35432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("悠", "")
	Mess("ぎ、ぎぶっ……ぐ、か……")
	hitretResult := Hitret(35433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Quake(16, 0, 1, 2, 30)
	Talk("Motoka", "MT050502")
	Mess("You don't get it. Haruka-kun, you don't understand／anything... about meee!!!")
	hitretResult := Hitret(35434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("悠", "")
	Mess("た、たす……あ、が……")
	hitretResult := Hitret(35435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(1500, 1)
	ScWait(1500, 0)
	SetCg("EE02b", 0, 0)
	Talk("初佳", "MT050503")
	Mess("…………………………………………………………………………")
	Mess("……………………………………")
	hitretResult := Hitret(35436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("悠", "")
	Mess("はぁっ、はぁっ、はぁっ……た、助かった……")
	hitretResult := Hitret(35437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("For an instant, everything went dark before my eyes...")
	hitretResult := Hitret(35438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Why would you suddenly attempt to strangle me?")
	hitretResult := Hitret(35439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050504")
	Mess("No particular reason.")
	hitretResult := Hitret(35440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, so...... If I said something unsettling, I can／apologize.")
	hitretResult := Hitret(35441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050505")
	Mess("What would you apologize about... You don't know／anything about me anyway...")
	hitretResult := Hitret(35442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("She's precisely right, and I have nothing to say.")
	hitretResult := Hitret(35443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I've known Motoka-san for a month, and we've had some／conversations, but did I really learn anything through／them?")
	hitretResult := Hitret(35444)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I don't even know why she keeps going to those parties.")
	hitretResult := Hitret(35445)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Maybe there's not any particular reason at all, but I／would not know even that.")
	hitretResult := Hitret(35446)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Motoka-san, you seem to be weak with sake, huh?")
	hitretResult := Hitret(35447)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050506")
	Mess("Uu... don't ask.")
	hitretResult := Hitret(35448)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If you feel bad about it, why do you keep getting drunk?")
	hitretResult := Hitret(35449)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "")
	Mess("………………………")
	hitretResult := Hitret(35450)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("………………………")
	hitretResult := Hitret(35451)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("初佳", "")
	Mess("………………………………………")
	hitretResult := Hitret(35452)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("悠", "")
	Mess("………………………………………初佳さん？")
	hitretResult := Hitret(35453)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EE02c", 0, 0)
	Talk("初佳", "MT050509")
	Mess("…………………………………………すぅ。")
	hitretResult := Hitret(35454)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Is she pretending to fall asleep again? ... No, that's／not it.")
	hitretResult := Hitret(35455)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("初佳", "MT050510")
	Mess("……すぅ…………すぅ…………すぅ…………")
	hitretResult := Hitret(35456)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Regular breath... This time, she probably fell asleep／for real.")
	hitretResult := Hitret(35457)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("It would be bad to wake her up. Let her rest for a／while.")
	hitretResult := Hitret(35458)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("初佳", "MT050511")
	Mess("んぅ…………すぅ…………すぅ…………")
	hitretResult := Hitret(35459)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("...however, I did not ask where she lives precisely...")
	hitretResult := Hitret(35460)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B28d", 0, 600)
	Scroll(0, 0, 1000, 3, 0)
	Talk("Haruka", "")
	Mess("W- What am I supposed to do...?")
	hitretResult := Hitret(35461)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	Talk("心の声", "")
	Mess("After that, having only starlight to rely on I somehow／managed to locate the nameplate of her house...")
	hitretResult := Hitret(35462)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's pitch dark... She is supposedly living with her／parents, but did they go to bed already?")
	hitretResult := Hitret(35463)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Considering the lights were off in the majority of the／other houses as well, that's a likely possibility.")
	hitretResult := Hitret(35464)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I would not want to wake them up, but we can't just stay／here like this.")
	hitretResult := Hitret(35465)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("...those were my thoughts.")
	hitretResult := Hitret(35466)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B28d", 0, 0)
	Talk("Haruka", "")
	Mess("No response, huh.")
	hitretResult := Hitret(35467)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I've tried using intercom and knocking, all to no avail.／As if there's no one home.")
	hitretResult := Hitret(35468)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050512")
	Mess("…………Hm……What's uupp……？")
	hitretResult := Hitret(35469)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sorry, I must have woken you up.")
	hitretResult := Hitret(35470)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But, it seems there is no one home.")
	hitretResult := Hitret(35471)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050513")
	Mess("That's to be expected, both my parents are away.")
	hitretResult := Hitret(35472)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050514")
	Mess("No one's home tonight, that's why I went to the party.")
	hitretResult := Hitret(35473)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's not something to boast about... So, where's the／key?")
	hitretResult := Hitret(35474)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050515")
	Mess("Ahー……yeah、waito a bitto……nhere。")
	hitretResult := Hitret(35475)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If you try to descend suddenly...")
	hitretResult := Hitret(35476)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050516")
	Mess("Kyah!")
	hitretResult := Hitret(35477)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Quake(0, 16, 1, 2, 30)
	ScPlaySe("se018", 0)
	Talk("心の声", "")
	Mess("Before I had a chance to catch her, Motoka falls down.")
	hitretResult := Hitret(35478)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Motoka-san!")
	hitretResult := Hitret(35479)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050517")
	Mess("Uuu、that was painful……But yeah、handbag……")
	hitretResult := Hitret(35480)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("It seems she's not hurt... It's hard to see well in this／darkness, she keeps fumbling with her hand inside her／bag in search of the key.")
	hitretResult := Hitret(35481)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Now that was close...")
	hitretResult := Hitret(35482)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050518")
	Mess("Ah!")
	hitretResult := Hitret(35483)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se018", 0)
	Talk("心の声", "")
	Mess("When her hand seemed to get stuck on something, handbag／drops down to the ground scattering its contents around.")
	hitretResult := Hitret(35484)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050519")
	Mess("うぅ……What am I doing……")
	hitretResult := Hitret(35485)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't be upset, let's pick this up. I'll help you.")
	hitretResult := Hitret(35486)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Purse, notebook, cosmetics and apparently a compact,／mobile phone...")
	hitretResult := Hitret(35487)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Motoka-san, she had a mobile phone... I don't recall／ever seeing anyone with a phone since we moved here／beside me and Sora.")
	hitretResult := Hitret(35488)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, here's the key.")
	hitretResult := Hitret(35489)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050520")
	Mess("Reallyー？　Thank youー、Haruka-kun。")
	hitretResult := Hitret(35490)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(0, 0)
	SetCg("B11c", 0, 0)
	ScPlayBgm("BGM16", 0)
	Talk("心の声", "")
	Mess("Still carrying Motoka-san who's unable to walk, I take／her to her room.")
	hitretResult := Hitret(35491)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Now, I just have to leave her sleeping in her bed.")
	hitretResult := Hitret(35492)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050521")
	Mess("I've bothered you so much today, neー.")
	hitretResult := Hitret(35493)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't mind. It's not like I can leave you in your／current state.")
	hitretResult := Hitret(35494)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'll bring you water. Where is the kitchen?")
	hitretResult := Hitret(35495)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(2000, 0)
	SetCg("B11c", 0, 0)
	SetBustup("CE03_02M", 0, 0)
	Talk("初佳", "MT050522")
	Mess("んくっ、んくっ…………ぷは、あははははっ。")
	hitretResult := Hitret(35496)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE03_04M", 0, 0)
	Talk("Motoka", "MT050523")
	Mess("It's just a water, but why does it feel so tasty? Is it／because you poured it, Haruka-kun?")
	hitretResult := Hitret(35497)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Wh- What are you saying, suddenly!")
	hitretResult := Hitret(35498)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050524")
	Mess("あー、You became flusteredー、kyahahahaha。")
	hitretResult := Hitret(35499)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If you're in the mood to tease me, I'm going home!")
	hitretResult := Hitret(35500)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE03_02M", 0, 0)
	Talk("Motoka", "MT050525")
	Mess("……So if I'm not、you'll stay？")
	hitretResult := Hitret(35501)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh?")
	hitretResult := Hitret(35502)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Why the sudden serious look... She isn't joking, is she?")
	hitretResult := Hitret(35503)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050526")
	Mess("Even if I pretend to be a responsible person, even if I／pretend to be a cutie and put all this make up on... No／one's paying any attention to me.")
	hitretResult := Hitret(35504)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050527")
	Mess("So, who should I be so that people look my way? What／kind of girl is worthy of someone's affection?")
	hitretResult := Hitret(35505)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Is she... asking me?")
	hitretResult := Hitret(35506)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, it's not that I'm an expert on the matters of／love, but... I don't think love has anything to do with／such criteria.")
	hitretResult := Hitret(35507)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's when you can't get someone out of your head, it's／when your interest in someone is beyond your control,／it's when just seeing someone makes you happy...")
	hitretResult := Hitret(35508)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Isn't it that tingling feeling...")
	hitretResult := Hitret(35509)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE03_01M", 0, 0)
	Talk("Motoka", "MT050528")
	Mess("Haruka-kun, you haven't really been in love, have you?")
	hitretResult := Hitret(35510)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well...")
	hitretResult := Hitret(35511)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050529")
	Mess("Or rather, you've only experienced a fleeting affection?")
	hitretResult := Hitret(35512)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("……………………")
	hitretResult := Hitret(35513)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE03_07M", 0, 0)
	Talk("Motoka", "MT050530")
	Mess("Those feelings……I've long forgotten them……to fall in／love with someone……is impossible……")
	hitretResult := Hitret(35514)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050531")
	Mess("But……failed at finding employment……failed at leaving／this village、failed at my job as a maid……")
	hitretResult := Hitret(35515)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050532")
	Mess("And yet... not even having a lover, isn't that truly／miserable? For such a pathetic woman to exist...")
	hitretResult := Hitret(35516)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Motoka-san......")
	hitretResult := Hitret(35517)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Motoka-san's voice, angry, tearful, a voice of a person／at her wits' end, that's how it sounded to me.")
	hitretResult := Hitret(35518)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("That's why, for sure, these are her real feelings.")
	hitretResult := Hitret(35519)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Motoka-san, she was just looking to be recognized by／someone.")
	hitretResult := Hitret(35520)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("That's what I thought.")
	hitretResult := Hitret(35521)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I think, Motoka-san is a great person.")
	hitretResult := Hitret(35522)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050533")
	Mess("……………………Really？")
	hitretResult := Hitret(35523)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm serious. That's why, someone will surely find such a／lovely person and fall in love with you.")
	hitretResult := Hitret(35524)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("初佳", "MT050534")
	Mess("………………………………………………")
	hitretResult := Hitret(35525)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There's definitely someone special for you out there,／don't worry about it!")
	hitretResult := Hitret(35526)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050535")
	Mess("When?")
	hitretResult := Hitret(35527)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh?")
	hitretResult := Hitret(35528)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE03_07L", 0, 0)
	Talk("Motoka", "MT050536")
	Mess("When will that \"someone special\" make his grand／entrance? When will a good-hearted prince capable of／loving me appear before my very eyes?")
	hitretResult := Hitret(35529)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah……")
	hitretResult := Hitret(35530)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050537")
	Mess("I prefer you not try to console me with white lies and／makeshift excuses.")
	hitretResult := Hitret(35531)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Motoka", "MT050538")
	Mess("Or maybe……")
	hitretResult := Hitret(35532)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Hide()
	SetCg("BLACK", 0, 0)
	Transition("WIP_MOZV", 1000)
	Update()
	Change("00_e013c")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

