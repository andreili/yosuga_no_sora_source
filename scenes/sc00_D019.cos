Include"yosuga.ch"

Function sc00_D019()
	ScenarioEnter()
	ScPlaySe("SE356", 0)
	SetCg("B34a", 0, 0)
	ScPlayBgm("BGM15", 0)
	Talk("êSÇÃê∫", "")
	Mess("Oh...the signs that the fireworks are starting.")
	hitretResult := Hitret(29108)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Let's hurry, Sora. Everyone might already be there.")
	hitretResult := Hitret(29109)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_01M", 0, 0)
	Talk("Sora", "SR040091")
	Mess("...Childish.")
	hitretResult := Hitret(29110)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh, but I've been looking forward to this, you know?")
	hitretResult := Hitret(29111)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040092")
	Mess("Sleepy.")
	hitretResult := Hitret(29112)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With a cute yawn, Sora rubbed her eyes.")
	hitretResult := Hitret(29113)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What's this? You were too exicted about today andÅ^couldn't get to sleep?")
	hitretResult := Hitret(29114)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_05M", 0, 0)
	Talk("Sora", "SR040093")
	Mess("...Was too hot.")
	hitretResult := Hitret(29115)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I see...")
	hitretResult := Hitret(29116)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She wasn't too enthusiastic about this from the start,Å^but even today, it doesn't look like she feels likeÅ^doing anything.")
	hitretResult := Hitret(29117)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Anyway, we should hurry. Amatsume and Migiwa-san areÅ^waiting too.")
	hitretResult := Hitret(29118)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040094")
	Mess("...Yeah, yeah.")
	hitretResult := Hitret(29119)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Today was the day of the festival that we had beenÅ^preparing for.")
	hitretResult := Hitret(29120)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Normally we'd just go pray and visit the street standsÅ^at night, but we're helping out a bit today.")
	hitretResult := Hitret(29121)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I haven't heard what we'll be helping out with, butÅ^there's worth in being able to experience backstageÅ^things.")
	hitretResult := Hitret(29122)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Ah...that's...")
	hitretResult := Hitret(29123)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Running along a road I used everyday, I caught sight ofÅ^a nearby stone monument, nicely decorated.")
	hitretResult := Hitret(29124)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt a bit proud that those decorations were somethingÅ^I helped to make.")
	hitretResult := Hitret(29125)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE358", 0)
	Talk("Haruka", "")
	Mess("If I clap my hands...*clap* *clap* ...")
	hitretResult := Hitret(29126)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_01M", 0, 0)
	Talk("Sora", "SR040095")
	Mess("...What?")
	hitretResult := Hitret(29127)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Just praying a bit...I'm finished anyway.")
	hitretResult := Hitret(29128)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040096")
	Mess("Hmmm?")
	hitretResult := Hitret(29129)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay. If we don't hurry, we'll be late, Sora!")
	hitretResult := Hitret(29130)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040097")
	Mess("Ah, Haru! ...Despite standing still yourself.")
	hitretResult := Hitret(29131)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide()
	SetCg("BLACK", 0, 0)
	Transition("WIP_RL", 500)
	Update()
	SetCg("B07d", 0, 0)
	Transition("WIP_LR", 500)
	Update()
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH040178")
	Mess("Oh, you're here.")
	hitretResult := Hitret(29132)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO040028")
	Mess("Morning, Haru-kun, Sora-chan...")
	hitretResult := Hitret(29133)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	SetBustup("CF03_02M", 0, 0)
	Shake(7)
	Talk("Ryouhei", "RH040179")
	Mess("Uhyooo! Sora-chaaan!!!")
	hitretResult := Hitret(29134)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040098")
	Mess("Hii!?")
	hitretResult := Hitret(29135)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se011", 0)
	Flush("WHITE", 100, "")
	SetBustup("CF03_06M", 0, 0)
	Down(7, 0, 0, 0)
	Talk("Ryouhei", "RH040180")
	Mess("@#%#%$!?")
	hitretResult := Hitret(29136)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(7)
	ScPlaySe("se018", 0)
	Talk("êSÇÃê∫", "")
	Mess("That smacking sound...")
	hitretResult := Hitret(29137)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_04M", 0, 0)
	SetBustup("CC06_02M", 0, 0)
	Talk("Kazuha", "KA040465")
	Mess("What do you think you're doing even at a time like this?")
	hitretResult := Hitret(29138)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040283")
	Mess("Welcome, everyone. Thanks for todayÅ`")
	hitretResult := Hitret(29139)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Migiwa-san...and Amatsume, good morning. I thought weÅ^were a bit late...")
	hitretResult := Hitret(29140)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san's in Japanese clothes... Maybe it's because IÅ^rarely see that, but...")
	hitretResult := Hitret(29141)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She definitely has an atmosphere about her, she'sÅ^beautiful...")
	hitretResult := Hitret(29142)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	SetBustup("CD03_10M", 0, 0)
	Talk("Kazuha", "KA040466")
	Mess("W-What? ...Don't look at me that much...")
	hitretResult := Hitret(29143)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("S-Sorry, I was a bet mesmerized.")
	hitretResult := Hitret(29144)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040467")
	Mess("Mesmerized...")
	hitretResult := Hitret(29145)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, no...")
	hitretResult := Hitret(29146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Maybe it's because of the festive mood, but I think I'mÅ^feeling a bit strange.")
	hitretResult := Hitret(29147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The shops on the shrine road had started preparing, andÅ^the smell of yakisoba being cooked and crepes filled theÅ^air.")
	hitretResult := Hitret(29148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I can't help being fired up that quickly, right?")
	hitretResult := Hitret(29149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("O-Oh right! What can I do to help?")
	hitretResult := Hitret(29150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_02M", 0, 0)
	Talk("Kazuha", "KA040468")
	Mess("Ah, that's right. You people are...")
	hitretResult := Hitret(29151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH040181")
	Mess("Us men are doing the odd jobs. There's lots of manualÅ^labor to do.")
	hitretResult := Hitret(29152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What about Sora?")
	hitretResult := Hitret(29153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040182")
	Mess("My personal s-...")
	hitretResult := Hitret(29154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se011", 0)
	Flush("WHITE", 100, "")
	BustupClear(7)
	SetBustup("CD03_02M", 0, 0)
	Talk("Kazuha", "KA040469")
	Mess("Sora-san will be helping me and Yorihime-senpai directÅ^the visitors.")
	hitretResult := Hitret(29155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_01M", 0, 0)
	Talk("Sora", "SR040099")
	Mess("...Mm.")
	hitretResult := Hitret(29156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(5)
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK040284")
	Mess("Ah, do you want to wear the spare shrine maiden outfit?Å^Come over here if you want itÅ`")
	hitretResult := Hitret(29157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA03_13M", 0, 0)
	Talk("Sora", "SR040100")
	Mess("...T-That's okay...")
	hitretResult := Hitret(29158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("Speaking of shrine maidens, Amatsume's not the only oneÅ^today, right?")
	hitretResult := Hitret(29159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At the shrine office's stand, some unfamiliar girlsÅ^wearing the same shrine maiden outfit were seated.")
	hitretResult := Hitret(29160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Looks like there are a few in the back, too.")
	hitretResult := Hitret(29161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_02L", 0, 0)
	Talk("Ryouhei", "RH040183")
	Mess("Ahhh, Master, eyeing the shrine maidens already, youÅ^have an expert eye.")
	hitretResult := Hitret(29162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040184")
	Mess("The contrast of red and white! That beautiful black hairÅ^and graceful movement! Neat and cute!")
	hitretResult := Hitret(29163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040185")
	Mess("Kuuuu, irresistable, right? This must be "moe!"")
	hitretResult := Hitret(29164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...It must be hot and sticky in that, so I can't thinkÅ^of them like that.")
	hitretResult := Hitret(29165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD03_04M", 0, 0)
	Talk("Kazuha", "KA040470")
	Mess("How can you think like that about the Shinto priesthood?Å^...Those people there are from the city's shrine, sentÅ^here to help out.")
	hitretResult := Hitret(29166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_02M", 0, 0)
	Talk("Kazuha", "KA040471")
	Mess("The others are the Chief Priest and the ones from theÅ^neighborhood, also here to help. There's no way we couldÅ^run the festival with ouly Akira, after all.")
	hitretResult := Hitret(29167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("This Shrine normally only has Akira, right?")
	hitretResult := Hitret(29168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040472")
	Mess("Yes. The priest was a friend of Akira's grandfather.Å^He's also from the city's shrine, and drops by once aÅ^week.")
	hitretResult := Hitret(29169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040473")
	Mess("Because Akira does the cleaning and managing of theÅ^shrine, they let her stay there.")
	hitretResult := Hitret(29170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmmm, is that so?")
	hitretResult := Hitret(29171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Looking around again, it's clear that there are quite aÅ^lot of people moving around.")
	hitretResult := Hitret(29172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With a festival of this scale, helping out must be hardÅ^work.")
	hitretResult := Hitret(29173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO040029")
	Mess("Okay, okay, that's enough chatter. It's time to work.")
	hitretResult := Hitret(29174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO040030")
	Mess("Especially Migiwa-san. The guests will be coming soon,Å^so we have to hurry.")
	hitretResult := Hitret(29175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040474")
	Mess("Ah...sorry about that. Let's go, Sora-san.")
	hitretResult := Hitret(29176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK040285")
	Mess("Hey everyone, after our jobs are done, let's go aroundÅ^the standsÅ`")
	hitretResult := Hitret(29177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B07a", 0, 0)
	Talk("Haruka", "")
	Mess("Now then, I wonder if this is okay...")
	hitretResult := Hitret(29178)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040186")
	Mess("Ahh, that should be okay.")
	hitretResult := Hitret(29179)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We were carrying cardboard boxes full of written oraclesÅ^and charms to be sold at the shrine office's goodsÅ^corner into the shrine grounds.")
	hitretResult := Hitret(29180)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Picking up one box at a time, and then carrying it intoÅ^the shrine office.")
	hitretResult := Hitret(29181)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Carrying something I'd normally be buying... It's a lotÅ^of work, and it puts things into perspective.")
	hitretResult := Hitret(29182)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The cardboard boxes we carry in are then opened and theÅ^contents lined up neatly by the city's shrine maidens.")
	hitretResult := Hitret(29183)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While we were carrying things in and out, people wereÅ^gathering in the reception room.")
	hitretResult := Hitret(29184)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("People I've seen before, people I haven't. Anyway, thereÅ^were a lot of people. Migiwa-san and Amatsume wereÅ^greeting them and handing out tea.")
	hitretResult := Hitret(29185)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("They were probably elite people from the city. It lookedÅ^like they were being very attentive.")
	hitretResult := Hitret(29186)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Compared to that, this endless manual labor is probablyÅ^easier.")
	hitretResult := Hitret(29187)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From the shrine office, some old men came out and stoodÅ^at the doorway.")
	hitretResult := Hitret(29188)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("They're people I've seen before.")
	hitretResult := Hitret(29189)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_02M", 0, 0)
	Talk("Kazuha", "KA040475")
	Mess("Those are the village head and his deputy. The other oneÅ^is the head of our ward.")
	hitretResult := Hitret(29190)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Coming up next to me, Migiwa-san explained to me with aÅ^whisper.")
	hitretResult := Hitret(29191)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040476")
	Mess("They were close friends of Kasugano-sensei, so how aboutÅ^talking to them afterwards?")
	hitretResult := Hitret(29192)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No way, I'd get too nervous...")
	hitretResult := Hitret(29193)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_03M", 0, 0)
	Talk("Kazuha", "KA040477")
	Mess("Hehe, even though they're just average old men?")
	hitretResult := Hitret(29194)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("They probably seem that way to you, but...")
	hitretResult := Hitret(29195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("I looked over at the old men again, and it seems thatÅ^they're headed toward the stairs.")
	hitretResult := Hitret(29196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetBustup("CK01_01S", 6, 0)
	SetBustup("CJ01_01S", 3, 0)
	Talk("êSÇÃê∫", "")
	Mess("At the stairs stood a well built man in JapaneseÅ^clothes.")
	hitretResult := Hitret(29197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Behind him, but maintaining a respectful distance, was aÅ^tall but slender man wearing a suit.")
	hitretResult := Hitret(29198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The one in Japanese clothes was middle aged, the suitedÅ^one was aging, but they were easily younger than theÅ^village head.")
	hitretResult := Hitret(29199)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But compared to them, the village head bowed lower whenÅ^greeting.")
	hitretResult := Hitret(29200)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She said he was the village head, so I would've thoughtÅ^him to be the highest, but it seems that there's aÅ^higher ranked person here.")
	hitretResult := Hitret(29201)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD03_07M", 0, 0)
	ScPlayBgm("BGM16", 0)
	Talk("êSÇÃê∫", "")
	Mess("Even Migiwa-san looked a bit nervous.")
	hitretResult := Hitret(29202)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That person is...")
	hitretResult := Hitret(29203)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Right after I asked, I remembered.")
	hitretResult := Hitret(29204)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I recognized the one in the back. He was the driver ofÅ^Migiwa-san's family's car.")
	hitretResult := Hitret(29205)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Secretary or something, Which means...")
	hitretResult := Hitret(29206)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040478")
	Mess("My father.")
	hitretResult := Hitret(29207)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san said, with a small groan.")
	hitretResult := Hitret(29208)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CK01_01S", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("In other words, Amatsume's father as well.")
	hitretResult := Hitret(29209)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Perhaps being influenced by Migiwa-san's feelings, IÅ^looked at him with complex thoughts.")
	hitretResult := Hitret(29210)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Something like a mix of hatred and respect.")
	hitretResult := Hitret(29211)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01S", 0, 0)
	Talk("Akira", "AK040286")
	Mess("Thank you for making time in your busy schedule today.")
	hitretResult := Hitret(29212)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After his greeting with the village head had completelyÅ^greeted, this time he was greeted by Amatsume.")
	hitretResult := Hitret(29213)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CK01_04S", 0, 0)
	Talk("Kazuha's Father", "NP040007")
	Mess("I am counting on you for a magnificent job this yearÅ^too.")
	hitretResult := Hitret(29214)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040287")
	Mess("Yes.")
	hitretResult := Hitret(29215)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It felt like an exchange between a Shinto shrineÅ^delegate and an influential person in his hometown.")
	hitretResult := Hitret(29216)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("You would not think that it was a talk between twoÅ^people related by blood.")
	hitretResult := Hitret(29217)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	SetBustup("CK01_01S", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("After his greeting with Amatsume had been completed, heÅ^became aware of Migiwa-san's presence.")
	hitretResult := Hitret(29218)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040480")
	Mess("......")
	hitretResult := Hitret(29219)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san bowed her head with a neutral expression.")
	hitretResult := Hitret(29220)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her father just greeted her with only his eyes beforeÅ^going into the shrine office with Amatsume and theÅ^village head.")
	hitretResult := Hitret(29221)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD03_07M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Following him with her eyes, Migiwa-san's face becameÅ^stern.")
	hitretResult := Hitret(29222)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You okay?")
	hitretResult := Hitret(29223)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040481")
	Mess("Eh?")
	hitretResult := Hitret(29224)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You're making a scary face.")
	hitretResult := Hitret(29225)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetBustup("CD03_10M", 0, 0)
	Talk("Kazuha", "KA040482")
	Mess("R-Really...? Oh no...what am...")
	hitretResult := Hitret(29226)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You'll scare the guests if you make that face.")
	hitretResult := Hitret(29227)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040483")
	Mess("I-I'll be careful...Come on, you have work to do too,Å^don't you? No slacking off now.")
	hitretResult := Hitret(29228)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay, I'm off thenÅ`")
	hitretResult := Hitret(29229)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_03M", 0, 0)
	Talk("Kazuha", "KA040484")
	Mess("Okay. Work hard, all right?")
	hitretResult := Hitret(29230)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And she sent me off with a voice that had returned toÅ^normal.")
	hitretResult := Hitret(29231)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_d019b")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

