Include"yosuga.ch"

Function sc00_C033B()
	ScenarioEnter()
	ScPlayBgm("BGM11", 0)
	SetCg("B34a", 400, 0)
	SetBustup("CD01_08M", 0, 0)
	Transition("WIP_BT", 500)
	Talk("Kazuha", "KA020596")
	Mess("Haaa.... Haaaa...... Wait.... Kasugano..... Kun....")
	hitretResult := Hitret(25797)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Are you alright? Migiwa-san...... Hm?")
	hitretResult := Hitret(25798)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I can faintly smell something burning.")
	hitretResult := Hitret(25799)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If they are just here......")
	hitretResult := Hitret(25800)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Besides here, I am unable to guess where they are......")
	hitretResult := Hitret(25801)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san and I gathered our breaths, and climb up theÅ^shrine stairs.")
	hitretResult := Hitret(25802)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(0, 0)
	Talk("Kazuha", "KA020597")
	Mess(".... What's that smell?")
	hitretResult := Hitret(25803)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... Something seems to be burning.")
	hitretResult := Hitret(25804)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020598")
	Mess("Hey..... Kasugano-kun......")
	hitretResult := Hitret(25805)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Let's hurry!!")
	hitretResult := Hitret(25806)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I didn't wait for Migiwa-san, and climb up the stairsÅ^two at a time.")
	hitretResult := Hitret(25807)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The white smoke is floating over...... Something seemsÅ^to be burning.")
	hitretResult := Hitret(25808)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It couldn't be possible...... That Sora......")
	hitretResult := Hitret(25809)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B07a", 0, 0)
	Transition("WIP_MOZH", 500)
	ScPlayBgm("BGM12", 0)
	ScPlaySe("SE013", 0)
	Talk("Haruka", "")
	Mess("ÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(25810)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The smoke is coming from the corner of the shrineÅ^grounds.")
	hitretResult := Hitret(25811)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There's the sound of twigs shrinking and breaking due toÅ^the fire's temperature.")
	hitretResult := Hitret(25812)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopSe()
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK021900")
	Mess("Ahh..... Haru-kun? Why are you in a panic?")
	hitretResult := Hitret(25813)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("........ Sora!!!")
	hitretResult := Hitret(25814)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_06M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Akira greets me leisurely as usual, with Sora by herÅ^side.")
	hitretResult := Hitret(25815)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora isn't looking at me. She's looking over at theÅ^other side.")
	hitretResult := Hitret(25816)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora..... Where is the envelope that you received?")
	hitretResult := Hitret(25817)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR020259")
	Mess("...... I don't know.")
	hitretResult := Hitret(25818)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's Sora who received it right? The post office'sÅ^personnel said that it was someone from the family.")
	hitretResult := Hitret(25819)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_05M", 0, 0)
	Talk("Sora", "SR020260")
	Mess("ÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(25820)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The twigs are making breaking sounds again.")
	hitretResult := Hitret(25821)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The weather is so hot and yet there is a fireÅ^burning....... If it's as usual, there shouldn't beÅ^leaves burning.....")
	hitretResult := Hitret(25822)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!? Hey, Sora!!!")
	hitretResult := Hitret(25823)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("I approach the fire and use my hand to dispel the smoke,Å^and check what is burning.")
	hitretResult := Hitret(25824)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!!! Aaa...... Akira...... This is.....")
	hitretResult := Hitret(25825)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Besides the leaves and twigs burning, the notebook isÅ^also burning in the midst of it.")
	hitretResult := Hitret(25826)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The white notebook spews out flames, quickly changingÅ^into a sepia color, turning into ashes and disappearing.")
	hitretResult := Hitret(25827)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... Oba-san's...... notebook...... Why.....")
	hitretResult := Hitret(25828)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The old notebook's fragment dances in the wind.")
	hitretResult := Hitret(25829)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The fragments are like black leaves flying in the wind,Å^disappearing into dust.")
	hitretResult := Hitret(25830)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The notebook is consumed by the fire, and the words thatÅ^were written on it are gradually lost.")
	hitretResult := Hitret(25831)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("W...... Why.....")
	hitretResult := Hitret(25832)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Grandmother's notebook is a clue that is meant to beÅ^used for finding Akira's mother.")
	hitretResult := Hitret(25833)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But the things that are written on it also contain theÅ^truth that Akira doesn't want to know.")
	hitretResult := Hitret(25834)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira has always been agonizing over it. Yet she hasÅ^never told anyone about it, intending to keep thisÅ^matter to herself......")
	hitretResult := Hitret(25835)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's right..... until I saw that notebook.")
	hitretResult := Hitret(25836)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_07M", 0, 0)
	Talk("Kazuha", "KA020599")
	Mess("...... What are you doing..... Akira?")
	hitretResult := Hitret(25837)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK021901")
	Mess("Ahh, Kazu-chan!! You're just in time. You know what, IÅ^just received sweet potatoes Åô How about everyoneÅ^having it together?")
	hitretResult := Hitret(25838)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!......")
	hitretResult := Hitret(25839)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira..... Why, are you still able to act normal?")
	hitretResult := Hitret(25840)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I shudder for the first time.")
	hitretResult := Hitret(25841)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Why are you still smiling? There's something importantÅ^that got burned.....")
	hitretResult := Hitret(25842)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021902")
	Mess("There's a lot, so there's no need to restrain yourselfÅ^so go ahead and eat.")
	hitretResult := Hitret(25843)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020600")
	Mess("!...... A, Akira?")
	hitretResult := Hitret(25844)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01L", 0, 0)
	Talk("Akira", "AK021903")
	Mess("It's interesting to eat roasted sweet potatoes duringÅ^the summer right? It's like having ice-cream in theÅ^winter!")
	hitretResult := Hitret(25845)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD01_07M", 0, 0)
	Talk("Kazuha", "KA020601")
	Mess("...... Kasugano-kun..... Could it be......")
	hitretResult := Hitret(25846)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("..... Akira......")
	hitretResult := Hitret(25847)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK021904")
	Mess("Ah, could it be that, Haru-kun is bad at eating sweetÅ^potatoes?")
	hitretResult := Hitret(25848)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Font(36, "")
	Quake(0, 16, 1, 2, 30)
	Talk("Haruka", "")
	Mess("THAT ISN'T IT!!")
	hitretResult := Hitret(25849)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021905")
	Mess("Hmm?")
	hitretResult := Hitret(25850)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I yelled without being able to control myself, yet AkiraÅ^does not even give the slightest quiver.")
	hitretResult := Hitret(25851)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She is constantly smiling...... And my own agitation,Å^appears to be silly.")
	hitretResult := Hitret(25852)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...... This is...... Akira?")
	hitretResult := Hitret(25853)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If she gets affected, her smile will get stiff, and herÅ^voice should have tremors as well.")
	hitretResult := Hitret(25854)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But right now, the Akira in front of me, is completelyÅ^the usual Akira.")
	hitretResult := Hitret(25855)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK021906")
	Mess("Haru-kun and the rest, how do you want to eat the sweetÅ^potatoes? Salt? Or butter? And then, you know what, ifÅ^you put vanilla ice-cream it's really delicious.")
	hitretResult := Hitret(25856)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...... The armor of smiling.")
	hitretResult := Hitret(25857)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira has been wearing up until now, a mask of iron.")
	hitretResult := Hitret(25858)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("SP07", 0, 0)
	Transition("CLOUD_A", 1000)
	Talk("Yahiro", "YH020172")
	Mess("When her grandfather passed away, everyone treated AkiraÅ^like a troublesome thing, pushing the responsibility toÅ^each other.")
	hitretResult := Hitret(25859)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020173")
	Mess("And yet, she...... Even when her life is at the mercy ofÅ^fate, she can still keep on smiling....")
	hitretResult := Hitret(25860)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020174")
	Mess("I really got a shock...... I merely thought she wasÅ^still small and unable to understand things, but thatÅ^isn't the case.")
	hitretResult := Hitret(25861)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020175")
	Mess("At that age, she already knows...... that she can't doÅ^anything about it.....")
	hitretResult := Hitret(25862)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B07a", 0, 0)
	SetBustup("CC01_02L", 0, 0)
	Flush("WHITE", 2000, "")
	Talk("êSÇÃê∫", "")
	Mess("Akira displays a smile that makes it seem like her tearsÅ^before me in the past is a lie.")
	hitretResult := Hitret(25863)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If you look at it from the sidelines, it seems we areÅ^the strange ones for panicking.")
	hitretResult := Hitret(25864)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira discovered what we wanted to do.")
	hitretResult := Hitret(25865)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That is why, she has decided to use her smile to dismissÅ^everything.")
	hitretResult := Hitret(25866)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In order not to have any other problems...... She hasÅ^burned the notebook evidence, and regardless of what weÅ^say, she will answer with a smile.")
	hitretResult := Hitret(25867)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("To Akira, the past doesn't matter anymore...... As longÅ^as the present is protected.....")
	hitretResult := Hitret(25868)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Forgetting all the hated things, and ending with aÅ^sentence [Everyone, let's eat together]......")
	hitretResult := Hitret(25869)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Akira.")
	hitretResult := Hitret(25870)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021907")
	Mess("Hm? What is it?")
	hitretResult := Hitret(25871)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... Where's the letter?")
	hitretResult := Hitret(25872)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK021908")
	Mess("...... Eh, this?")
	hitretResult := Hitret(25873)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That thing is very important, can you return it to me?")
	hitretResult := Hitret(25874)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I carefully reach out my hand.")
	hitretResult := Hitret(25875)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira is standing beside the fire. If the letter dropsÅ^now, it will be burned by the fire.")
	hitretResult := Hitret(25876)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA01_06M", 0, 0)
	Talk("Sora", "SR020261")
	Mess("That sort of thing..... Should be burned.")
	hitretResult := Hitret(25877)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess(".....!?")
	hitretResult := Hitret(25878)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("....... Sora...... What do you mean by that?")
	hitretResult := Hitret(25879)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK021909")
	Mess("Haru-kun what is this? Sora-chan just passed this to meÅ^earlier.")
	hitretResult := Hitret(25880)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's the test results. Akira and Migiwa-san's.")
	hitretResult := Hitret(25881)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK021910")
	Mess("Test results? Did I do something like that?")
	hitretResult := Hitret(25882)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I did the investigation arbitarily on my own. To checkÅ^who is Migiwa-san's mother's child.")
	hitretResult := Hitret(25883)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021911")
	Mess("You don't even need to even to check, it's Kazu-chan.")
	hitretResult := Hitret(25884)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Just in case. So that there are no mistakes.")
	hitretResult := Hitret(25885)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021912")
	Mess("My mother, isn't around anymore.")
	hitretResult := Hitret(25886)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then, even if you don't look at the results it's fineÅ^right?")
	hitretResult := Hitret(25887)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_08M", 0, 0)
	Talk("Akira", "AK021913")
	Mess(".........!")
	hitretResult := Hitret(25888)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira's shoulders tremble once.")
	hitretResult := Hitret(25889)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm sorry for keeping things a secret. But...... I can'tÅ^forget about it.")
	hitretResult := Hitret(25890)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK021914")
	Mess("I'm already very happy already you know? Haru-chan,Å^Kazu-chan and Sora-chan are so kind to me.")
	hitretResult := Hitret(25891)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If you continue to lie to yourself, will you still beÅ^happy? If you burn the important clue away, Akira, willÅ^you not feel anything?")
	hitretResult := Hitret(25892)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... How long more do you wish to hurt yourself Akira?")
	hitretResult := Hitret(25893)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In order not to make the problem bigger....... Akira isÅ^always like this.")
	hitretResult := Hitret(25894)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In order to protect Migiwa-san.")
	hitretResult := Hitret(25895)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR020262")
	Mess("Haru...... Stop. In truth you are also hestitating.")
	hitretResult := Hitret(25896)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("ÅcÅcÅcÅcÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(25897)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020263")
	Mess("Nobody will be happy...... Haru, do you want to doÅ^something like this?")
	hitretResult := Hitret(25898)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("..... I......")
	hitretResult := Hitret(25899)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("If the contents inside the envelop...... denies theÅ^relationship between Migiwa-san and her mother, theÅ^situation will change.")
	hitretResult := Hitret(25900)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even if Migiwa-san says she is okay, the mother that sheÅ^thought until now will become a stranger.")
	hitretResult := Hitret(25901)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And at the same time, she will also know about herÅ^mother's sinful deeds.")
	hitretResult := Hitret(25902)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Yahiro-san mentioned about cuckoos...... that they won'tÅ^raise their own children and lay them in other nests,Å^while dropping the real eggs elsewhere to kill them.")
	hitretResult := Hitret(25903)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Without being able to recognize the event, their ownÅ^children will be substituted")
	hitretResult := Hitret(25904)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And then, the real children will treated with scorn.")
	hitretResult := Hitret(25905)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When they realize the truth, the things they believe inÅ^will crumble into pieces, and their smiles will neverÅ^come back.")
	hitretResult := Hitret(25906)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The things I did, perhaps aided in that mother's plan.")
	hitretResult := Hitret(25907)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Akira, give me the letter.")
	hitretResult := Hitret(25908)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_07M", 0, 0)
	Talk("Akira", "AK021915")
	Mess("ÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(25909)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Akira......")
	hitretResult := Hitret(25910)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_09M", 0, 0)
	Talk("Kazuha", "KA020602")
	Mess("Akira, please!! No matter what happens to me......")
	hitretResult := Hitret(25911)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	BustupClear()
	SetBustup("CC01_07L", 0, 0)
	Talk("Akira", "AK021916")
	Mess("...... You won't be fine.")
	hitretResult := Hitret(25912)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020603")
	Mess("Akira!!!")
	hitretResult := Hitret(25913)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I promptly reach out, but I am unable to grasp theÅ^letter that slips from Akira's hand, and the fireÅ^gradually consumes it.")
	hitretResult := Hitret(25914)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide()
	SetCg("Black", 0, 0)
	Update(1)
	Flush("RED", 1000, "")
	ScPlaySe("SE010", 0)
	Talk("Haruka", "")
	Mess("Urgh!!")
	hitretResult := Hitret(25915)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020264")
	Mess("Haru!!!")
	hitretResult := Hitret(25916)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM16", 0)
	SetCg("B07a", 0, 0)
	SetBustup("CC01_08M", 0, 0)
	Talk("Akira", "AK021917")
	Mess("W..... Why..... Haru-kun?")
	hitretResult := Hitret(25917)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("..... Because it's important.")
	hitretResult := Hitret(25918)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_08M", 0, 0)
	Talk("Sora", "SR020265")
	Mess("...... Haru!!")
	hitretResult := Hitret(25919)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm okay......")
	hitretResult := Hitret(25920)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I had picked up the shrinking letter from the fire.")
	hitretResult := Hitret(25921)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I feel a throbbing pain in my right hand which hadÅ^gotten burned, but it doesn't matter.")
	hitretResult := Hitret(25922)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	Talk("êSÇÃê∫", "")
	Mess("I picked up the letter and thrust it towards Akira.")
	hitretResult := Hitret(25923)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Akira..... This is something that concerns you right?Å^Are you going to continue to keep this secret where youÅ^won't hurt anyone?")
	hitretResult := Hitret(25924)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_13M", 0, 0)
	Talk("Akira", "AK021918")
	Mess("...... I'm fine with it..... If everyone can be happyÅ^from it.")
	hitretResult := Hitret(25925)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But Akira isn't in it. If you wish for happiness whileÅ^you're just looking from afar, isn't it the same thatÅ^you're not here!")
	hitretResult := Hitret(25926)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021919")
	Mess("I...... Even so......")
	hitretResult := Hitret(25927)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Didn't Akira say that you don't want to leave here?Å^Because there are many important things here.")
	hitretResult := Hitret(25928)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If you lose everything....... What else do you have?")
	hitretResult := Hitret(25929)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You are only left regret..... If you lose yourself toÅ^protect them, the next thing that will happen isÅ^everyone getting hurt by you.")
	hitretResult := Hitret(25930)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Losing yourself in order not to hurt anyone, that sortÅ^of answer doesn't exist at all.")
	hitretResult := Hitret(25931)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021920")
	Mess("But..... It's only me......")
	hitretResult := Hitret(25932)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Am I unable to shoulder this too? Who am I to Akira?")
	hitretResult := Hitret(25933)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_08M", 0, 0)
	Talk("Akira", "AK021921")
	Mess("ÅcÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(25934)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_07M", 0, 0)
	Talk("Kazuha", "KA020604")
	Mess("Akira..... Don't forget about me too?")
	hitretResult := Hitret(25935)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021922")
	Mess("But I can't....... trouble you too.....")
	hitretResult := Hitret(25936)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_07M", 0, 0)
	Talk("Kazuha", "KA020605")
	Mess("Am I really so unreliable? If I can't help out now, whatÅ^kind of sister would I be!")
	hitretResult := Hitret(25937)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_03M", 0, 0)
	Talk("Kazuha", "KA020606")
	Mess("And furthermore, if the older sister protecting theÅ^younger sister is a natural thing, then this time ourÅ^positions should be reversed.")
	hitretResult := Hitret(25938)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020607")
	Mess("Because I might the older sister.")
	hitretResult := Hitret(25939)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021923")
	Mess("...... Kazu-chan......")
	hitretResult := Hitret(25940)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("This time, it's up to everyone to help you.")
	hitretResult := Hitret(25941)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No matter what sort of results is in front of you, AkiraÅ^can still do whatever you wish to.")
	hitretResult := Hitret(25942)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("We are here for this reason.")
	hitretResult := Hitret(25943)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(5)
	SetBustup("CC01_08L", 0, 0)
	Talk("Akira", "AK021924")
	Mess("Uu...... Uuuuh..... Haru-kun......")
	hitretResult := Hitret(25944)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You held on a long time. Akira can be more selfish,Å^okay?")
	hitretResult := Hitret(25945)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira tears keep flowing. Migiwa-san gently strokes herÅ^head.")
	hitretResult := Hitret(25946)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She looks like an older sister who is praising herÅ^sister for trying her best to endure.")
	hitretResult := Hitret(25947)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021925")
	Mess("You know...... I made Grandfather really angry once.")
	hitretResult := Hitret(25948)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021926")
	Mess("The people surrounding us raised huge objections whenÅ^Grandfather adopted me, and I said to him [Will it beÅ^better if I disappeared from here?].")
	hitretResult := Hitret(25949)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021927")
	Mess("In the end, Grandfather became terribly angry and showedÅ^a hurt expression.")
	hitretResult := Hitret(25950)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021928")
	Mess("At that time, I really don't understand why GrandfatherÅ^got angry.....")
	hitretResult := Hitret(25951)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021929")
	Mess("Because I thought bottling my feelings inside andÅ^concentrate on enduring..... is the best way.")
	hitretResult := Hitret(25952)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021930")
	Mess("That is why, I don't know when it started, I am unableÅ^to say my true feelings out...... Even though it'sÅ^something so easy.")
	hitretResult := Hitret(25953)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	SetCg("B07a_", 0, 0)
	Transition("CLOUD_A", 500)
	Talk("Akira's grandfather", "NP070017")
	Mess("I know that you're kind so you said these words.")
	hitretResult := Hitret(25954)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira's grandfather", "NP070018")
	Mess("But, that isn't the right thing to do.")
	hitretResult := Hitret(25955)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira's grandfather", "NP070019")
	Mess("It's not true kindness if you hurt yourself overÅ^everyone's feelings.")
	hitretResult := Hitret(25956)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021934")
	Mess("....... I'm sorry..... Grandfather.")
	hitretResult := Hitret(25957)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira's grandfather", "NP070020")
	Mess("No..... Even if I don't teach you, you're aÅ^straightforward and gentle child.")
	hitretResult := Hitret(25958)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira's grandfather", "NP070021")
	Mess("Let me hear it..... Akira's small feelings that areÅ^hidden away in Akira's corner.")
	hitretResult := Hitret(25959)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1000, 0)
	SetCg("B07a", 0, 0)
	SetBustup("CC01_08L", 0, 0)
	Talk("Akira", "AK021937")
	Mess("....... At that time, I.....")
	hitretResult := Hitret(25960)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021938")
	Mess("...... I said, \"I want to stay here\".")
	hitretResult := Hitret(25962)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021939")
	Mess("I want to stay with everyone..... So, I..... chose toÅ^protect everyone here.")
	hitretResult := Hitret(25963)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021940")
	Mess("Haru-chan...... You understand right?")
	hitretResult := Hitret(25964)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021941")
	Mess("No matter what happens, I can still stay here.")
	hitretResult := Hitret(25965)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes. There isn't any reason for Akira to leave here.")
	hitretResult := Hitret(25966)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020608")
	Mess("Yes, I won't forgive you if you leave everyone behind toÅ^go somewhere else.")
	hitretResult := Hitret(25967)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_07L", 0, 0)
	Talk("Akira", "AK021942")
	Mess("........ Thank you...... But I was really scared...... IÅ^don't know if others can understand like the two of youÅ^can .....")
	hitretResult := Hitret(25968)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021943")
	Mess("So...... I still...... chose to lie...... a lie thatÅ^everyone can be happy......")
	hitretResult := Hitret(25969)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But you don't need to lie anymore. Once Akira becomesÅ^happy, you just need to share it with everyone else.")
	hitretResult := Hitret(25970)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021944")
	Mess("Thank you...... Haru-kun...... Kazu-chan.....")
	hitretResult := Hitret(25971)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021945")
	Mess("And Sora-chan...... Thank you for worrying about me.")
	hitretResult := Hitret(25972)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA01_08M", 0, 0)
	Talk("Sora", "SR020266")
	Mess("..... I thought Haru is trying to hurt you.")
	hitretResult := Hitret(25973)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020267")
	Mess("He thought that everything is his fault, and will beÅ^unable to endure anymore and run away again.....")
	hitretResult := Hitret(25974)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_06M", 0, 0)
	Talk("Sora", "SR020268")
	Mess("Akira is also the same..... I thought if things turn outÅ^that way, it would be better for things to continue.....")
	hitretResult := Hitret(25975)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It won't, Akira is stronger than I am. That's becauseÅ^she didn't run away.")
	hitretResult := Hitret(25976)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_02M", 0, 0)
	Talk("Sora", "SR020269")
	Mess("......... The Haru now is also the same.")
	hitretResult := Hitret(25977)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... Eh......")
	hitretResult := Hitret(25978)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR020270")
	Mess("...... Idiot.")
	hitretResult := Hitret(25979)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess(".........!")
	hitretResult := Hitret(25980)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020271")
	Mess("..... Your burns. How about having Akira to take care ofÅ^it?")
	hitretResult := Hitret(25981)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(4)
	Talk("êSÇÃê∫", "")
	Mess("Sora leaves the shrine's grounds without turning herÅ^head back even once.")
	hitretResult := Hitret(25982)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora is....... worried about me, afraid that I wouldÅ^regret things later on....")
	hitretResult := Hitret(25983)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But...... I won't run away anymore. I'll take upÅ^responsibility for my own actions.")
	hitretResult := Hitret(25984)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And then, I can always be with Akira.")
	hitretResult := Hitret(25985)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_11M", 0, 0)
	Talk("Kazuha", "KA020609")
	Mess("In any case...... Akira, treat the hand first.")
	hitretResult := Hitret(25986)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah..... Migiwa-san.....")
	hitretResult := Hitret(25987)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_02M", 0, 0)
	Talk("Kazuha", "KA020610")
	Mess("I'll leave things to the both of you.....")
	hitretResult := Hitret(25988)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(5)
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san finishes her words and leaves the shrine'sÅ^grounds like Sora.")
	hitretResult := Hitret(25989)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... Ah..... Erm.....")
	hitretResult := Hitret(25990)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san lowers her head and quickly leaves, and I doÅ^not know what to say to her.")
	hitretResult := Hitret(25991)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_11M", 0, 0)
	Talk("Akira", "AK021946")
	Mess("Haru-kun, come over here. There's no aloe here at home,Å^so ice water should be used.")
	hitretResult := Hitret(25992)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... Okay.")
	hitretResult := Hitret(25993)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(0, 1)
	ScWait(2000, 0)
	Change("00_c033c")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

