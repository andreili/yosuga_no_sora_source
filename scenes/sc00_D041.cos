Include"yosuga.ch"

Function sc00_D041()
	ScenarioEnter()
	ScPlayBgm("BGM19", 0)
	SetCg("B09b", 0, 0)
	SetBustup("CD01_11M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("We returned to Kazuha's mansion.")
	hitretResult := Hitret(32748)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We couldn't do anything without changing Kazuha out ofÅ^her uniform, and that was stained with dirt and dust.")
	hitretResult := Hitret(32749)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041355")
	Mess("...Thanks, Haruka...")
	hitretResult := Hitret(32750)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm your boyfriend, I should at least take you home.")
	hitretResult := Hitret(32751)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_10M", 0, 0)
	Talk("Kazuha", "KA041356")
	Mess("Stop that....... Do you want to come in?")
	hitretResult := Hitret(32752)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh...?")
	hitretResult := Hitret(32753)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I remembered what happened the last time.")
	hitretResult := Hitret(32754)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At that time, we ran into her father...")
	hitretResult := Hitret(32755)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("........Kazuha's father, huh?")
	hitretResult := Hitret(32756)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Isn't your family in there?")
	hitretResult := Hitret(32757)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041357")
	Mess("I don't...know...")
	hitretResult := Hitret(32758)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kazuha started thinking carefully.")
	hitretResult := Hitret(32759)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("VoiceÅsàÍótÇÃïÉÅt", "NP040018")
	Mess("Kazuha, guests?")
	hitretResult := Hitret(32760)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CK01_01S", 0, 0)
	Talk("Kazuha", "KA041359")
	Mess("Father...")
	hitretResult := Hitret(32761)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As we turned to face that the direction the voice cameÅ^from, Kazuha's father came walking down from the gate.")
	hitretResult := Hitret(32762)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The moment she saw her father, Kazuha's face went tense.")
	hitretResult := Hitret(32763)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CK01_04S", 0, 0)
	Talk("Kazuha's Father", "NP040019")
	Mess("...Ah, Sensei's... Haruka-kun, wasn't it?Å^Welcome.")
	hitretResult := Hitret(32764)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While giving me a generously large bow, he smiled in aÅ^dignified way.")
	hitretResult := Hitret(32765)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's probably normal for a boyfriend to start panickingÅ^when suddenly meeting his girlfriend's father.")
	hitretResult := Hitret(32766)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But this was a chance.")
	hitretResult := Hitret(32767)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I instantly remembered what Yahiro had said. To take herÅ^away.")
	hitretResult := Hitret(32768)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Leaving aside whether or not I can take her away, IÅ^can't release Kazuha and Amatsume from their curse if IÅ^don't talk to the man who started all of this.")
	hitretResult := Hitret(32769)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CK01_04M", 0, 0)
	Talk("Haruka", "")
	Mess("Migiwa-san. I have something important to say. Is this aÅ^bad time?")
	hitretResult := Hitret(32770)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After bowing politely once to him, I stepped in front ofÅ^him.")
	hitretResult := Hitret(32771)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041361")
	Mess("......Haruka?")
	hitretResult := Hitret(32772)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kazuha called out in a shocked and uneasy tone.")
	hitretResult := Hitret(32773)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha's Father", "NP040020")
	Mess("Hm? Quite serious indeed. It almost feels like you'reÅ^asking for my permission to marry her.")
	hitretResult := Hitret(32774)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes. I'm asking you for permission to enter aÅ^relationship with Kazuha-san.")
	hitretResult := Hitret(32775)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CK01_01M", 0, 0)
	Talk("Kazuha's Father", "NP040021")
	Mess("Wha...?")
	hitretResult := Hitret(32776)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041364")
	Mess("H-Haruka!?")
	hitretResult := Hitret(32777)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My unquestionable reply put Kazuha's father at a lossÅ^for words.")
	hitretResult := Hitret(32778)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Because we're still students, I haven't thought aboutÅ^marriage yet, but I intend to take this relationshipÅ^seriously.")
	hitretResult := Hitret(32779)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha's Father", "NP040022")
	Mess("U- Hmm................... Kazuha, is this true?")
	hitretResult := Hitret(32780)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD01_08M", 0, 0)
	Talk("Kazuha", "KA041366")
	Mess("Yes... Father.")
	hitretResult := Hitret(32781)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CK01_01M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Would he yell, turn me away, or perhaps even hit her?")
	hitretResult := Hitret(32782)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But if I want him to seriously accept our relationship,Å^then I have to talk to him properly.")
	hitretResult := Hitret(32783)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha's Father", "NP040023")
	Mess("...That serious talk you mentioned, is this it?")
	hitretResult := Hitret(32784)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("He couldn't hide his uneasiness... But at least heÅ^hasn't gotten angry.")
	hitretResult := Hitret(32785)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Believing that he'd listen to me, I continued.")
	hitretResult := Hitret(32786)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No. It's about Amatsume.")
	hitretResult := Hitret(32787)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha's Father", "NP040024")
	Mess("......How much do you know?")
	hitretResult := Hitret(32788)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("All of it. I heard about it from both Kazuha andÅ^Amatsume.")
	hitretResult := Hitret(32789)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CK01_03M", 0, 0)
	Talk("Kazuha's Father", "NP040025")
	Mess("I see...")
	hitretResult := Hitret(32790)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Upon hearing Amatsume's name, a cold rage entered himÅ^for a moment, before disappearing into a quiet whimper.")
	hitretResult := Hitret(32791)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I couldn't help but think that the expression matchedÅ^Kazuha's perfectly.")
	hitretResult := Hitret(32792)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CK01_01M", 0, 0)
	Talk("Kazuha's Father", "NP040026")
	Mess("The things that they have told you are all true. I doÅ^feel very sorry for her, though.")
	hitretResult := Hitret(32793)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's why I knew for sure that this man loved bothÅ^Kazuha and Amatsume.")
	hitretResult := Hitret(32794)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I don't think Amatsume would want an apology.")
	hitretResult := Hitret(32795)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CK01_02M", 0, 0)
	Talk("Kazuha's Father", "NP040027")
	Mess("That's probably true...")
	hitretResult := Hitret(32796)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Amatsume's told me before that she's happiest as long asÅ^there's someone there, standing by her and supportingÅ^her.")
	hitretResult := Hitret(32797)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even if she can't live in the same house, just being byÅ^her side would be enough.")
	hitretResult := Hitret(32798)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kazuha has spent all her time thinking about what sheÅ^could do for Amatsume. She's done this as a subsituteÅ^for you, who has done nothing.")
	hitretResult := Hitret(32799)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even after going out with me, she even tried to end ourÅ^relationship, not wanting to abandon Amatsume even if itÅ^means sacrificing her own happiness.")
	hitretResult := Hitret(32800)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I didn't know if my words could reach his heart.")
	hitretResult := Hitret(32801)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But I at least wanted him to realize...")
	hitretResult := Hitret(32802)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What Kazuha has done. What Amatsume has done. And whatÅ^the two of them have been thinking.")
	hitretResult := Hitret(32803)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wanted him to know that everyone was trying to beÅ^happy.")
	hitretResult := Hitret(32804)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Amatsume hated the idea of someone losing theirÅ^happiness because of her.")
	hitretResult := Hitret(32805)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Rather than spitting the Migiwa family apart by livingÅ^with them, she thought it would be better to live at theÅ^shrine alone.")
	hitretResult := Hitret(32806)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("When Kazuha tried to break up with me, she even tried toÅ^make Kazuha hate her, and tried to isolate herself fromÅ^Kazuha so that she'd come back to me.")
	hitretResult := Hitret(32807)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume's capacity for sympathy is vast. She's tooÅ^kind.")
	hitretResult := Hitret(32808)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I think it's because Yahiro-san understands that thatÅ^she takes the role that makes her look like she's onlyÅ^using her.")
	hitretResult := Hitret(32809)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("So that Amatsume doesn't feel like she's a burden forÅ^her.")
	hitretResult := Hitret(32810)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That was Yahiro-san's own solution that she herself cameÅ^up with on how to treat Amatsume.")
	hitretResult := Hitret(32811)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even Kazuha is the same.")
	hitretResult := Hitret(32812)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("She loves you, her mother, and Amatsume... So sheÅ^thought of a way to not lose anyone, so that everythingÅ^wouldn't fall apart.")
	hitretResult := Hitret(32813)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Everyone was thinking their hardest about what theyÅ^could do.")
	hitretResult := Hitret(32814)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kazuha's father listened to me quietly.")
	hitretResult := Hitret(32815)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Kazuha didn't cut in, either.")
	hitretResult := Hitret(32816)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't know if all of this would reach his heart.")
	hitretResult := Hitret(32817)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But the only thing I could do was tell him about theÅ^things that I've heard and the things that I've seen.")
	hitretResult := Hitret(32818)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Please... watch over the both of them. Please... faceÅ^them without looking away.")
	hitretResult := Hitret(32819)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I know it must be very hard on you, too. But if youÅ^continue avoiding the situation, it will continue to beÅ^difficult. Kazuha and the people around her will keepÅ^suffering.")
	hitretResult := Hitret(32820)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("This isn't about duty or responsibility. Please... justÅ^think about them, and watch over them.")
	hitretResult := Hitret(32821)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I think this is the only way for them all to be happy..")
	hitretResult := Hitret(32822)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I had said everything that I could.")
	hitretResult := Hitret(32823)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CK01_01M", 0, 0)
	Talk("Kazuha's Father", "NP040028")
	Mess("...I see.")
	hitretResult := Hitret(32824)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After muttering that one phrase, Kazuha's father stoodÅ^deep in thought.")
	hitretResult := Hitret(32825)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After quite some time, he opened his eyes.")
	hitretResult := Hitret(32826)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CK01_04M", 0, 0)
	Talk("Kazuha's Father", "NP040029")
	Mess("You really are Sensei's grandson.")
	hitretResult := Hitret(32827)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("He finally spoke, as if he was really touched by what IÅ^said.")
	hitretResult := Hitret(32828)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha's Father", "NP040030")
	Mess("A long time ago, I was told the same thing. By yourÅ^grandmother.")
	hitretResult := Hitret(32829)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha's Father", "NP040031")
	Mess("\"You can think about the difficult parts later. ButÅ^first, face the problem.\" That's what she said.")
	hitretResult := Hitret(32830)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CK01_02M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("As if saying something he couldn't say before, he gave aÅ^warm smile.")
	hitretResult := Hitret(32831)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CK01_04M", 0, 0)
	Talk("Kazuha's Father", "NP040032")
	Mess("I would never have thought that I'd hear the same thingÅ^again... Thank you.")
	hitretResult := Hitret(32832)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha's Father", "NP040033")
	Mess("Words that everyone say... they certainly must be theÅ^truth...")
	hitretResult := Hitret(32833)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After saying those words, he extended his hand towardÅ^me.")
	hitretResult := Hitret(32834)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Although I was surprised, I took his hand.")
	hitretResult := Hitret(32835)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha's Father", "NP040034")
	Mess("The reason Kazuha has changed was because of you, huh?")
	hitretResult := Hitret(32836)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh..N-No, that's...")
	hitretResult := Hitret(32837)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha's Father", "NP040035")
	Mess("Look after them.")
	hitretResult := Hitret(32838)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh.... EH!?")
	hitretResult := Hitret(32839)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was so sudden. I stood there, stunned by what he hadÅ^just said.")
	hitretResult := Hitret(32840)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Embarrassing...")
	hitretResult := Hitret(32841)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_10M", 0, 0)
	Talk("Kazuha", "KA041380")
	Mess("F-Father!!")
	hitretResult := Hitret(32842)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha's Father", "NP040036")
	Mess("Hahaha...Å^Kazuha, treat him nicely, okay?")
	hitretResult := Hitret(32843)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_11M", 0, 0)
	Talk("Kazuha", "KA041382")
	Mess("Father... Yes.")
	hitretResult := Hitret(32844)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Finally, all three of us smiled.")
	hitretResult := Hitret(32845)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From the back, a car pulled in slowly. Exiting from theÅ^driver's seat, the secretary opened the door forÅ^Kazuha's father.")
	hitretResult := Hitret(32846)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While his hand was on the open car door,")
	hitretResult := Hitret(32847)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CK01_03M", 0, 0)
	Talk("Kazuha's Father", "NP040037")
	Mess("...When you realize you've made a mistake, you shouldÅ^fix it... But I wonder when it was that I made thatÅ^mistake...")
	hitretResult := Hitret(32848)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I heard his faint murmur.")
	hitretResult := Hitret(32849)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_d042")
	Return
	Change("EXIT_SCENARIO")
EndFunc

