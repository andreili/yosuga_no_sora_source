Include"yosuga.ch"

Function sc00_A020()
	ScenarioEnter()
	If (IsRecollectMode())
	Else
		SetCg("BLACK", 0, 0)
		Talk("Haruka", "")
		Mess("...............")
		hitretResult := Hitret(6591)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		SetCg("B03c", 0, 0)
		Talk("心の声", "")
		Mess("No matter how many times I tossed or turned, my／restlessness only grew, so I didn't sleep at all.")
		hitretResult := Hitret(6592)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("心の声", "")
		Mess("Without anything cold passing through my throat, I only／became more restless.")
		hitretResult := Hitret(6593)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("心の声", "")
		Mess("There's not really school tomorrow, so even if I'm／sluggish, it's fine.")
		hitretResult := Hitret(6594)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("心の声", "")
		Mess("I thought I might as well stay shut inside my room.")
		hitretResult := Hitret(6595)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("心の声", "")
		Mess("...Now it's like I'm Sora.")
		hitretResult := Hitret(6596)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("心の声", "")
		Mess("We've completely changed situations.")
		hitretResult := Hitret(6597)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("心の声", "")
		Mess("But, our reasons are completely different.")
		hitretResult := Hitret(6598)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("心の声", "")
		Mess("Because I'm... running away from Sora.")
		hitretResult := Hitret(6599)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("心の声", "")
		Mess(".........I'm despicable.")
		hitretResult := Hitret(6600)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		BlackOut(0, 0)
		Talk("心の声", "")
		Mess("I headed to the restroom.")
		hitretResult := Hitret(6601)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("心の声", "")
		Mess("If I sneak around like this in the middle of the night,／Sora won't notice.")
		hitretResult := Hitret(6602)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("心の声", "")
		Mess("But, why did I go in the opposite direction...?")
		hitretResult := Hitret(6603)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
	EndIf
	Talk("Voice《穹》", "SR010743")
	Mess(".........Nnn.........")
	hitretResult := Hitret(6604)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("With the lights off, I walked as silent as possible.")
	hitretResult := Hitret(6605)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("This is my own home, yet I have to sneak around.")
	hitretResult := Hitret(6606)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Voice《穹》", "SR010744")
	Mess("...Ahh...... Nnn......")
	hitretResult := Hitret(6607)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Looking at Sora's room, there's a gap in her door with／light leaking out.")
	hitretResult := Hitret(6608)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("...She's still awake? Or maybe I woke her up...")
	hitretResult := Hitret(6609)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I wonder if Sora will come to my room again?")
	hitretResult := Hitret(6610)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("If I pretend to be asleep again, I wonder if I'll hurt／Sora.")
	hitretResult := Hitret(6611)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Voice《穹》", "SR010745")
	Mess("Ahh... Ahhhh......")
	hitretResult := Hitret(6612)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......?")
	hitretResult := Hitret(6613)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I have a feeling I can hear Sora's voice.")
	hitretResult := Hitret(6614)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Voice《穹》", "SR010746")
	Mess("...Nnn, nnn... Haa haa haa......")
	hitretResult := Hitret(6615)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Voice《穹》", "SR010747")
	Mess("...Ahh... ahhhh... nn...")
	hitretResult := Hitret(6616)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess(".........")
	hitretResult := Hitret(6617)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I headed in the direction the sound came from.")
	hitretResult := Hitret(6618)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("What is Sora doing this late?")
	hitretResult := Hitret(6619)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Without making a sound I approached Sora's room. And／then...")
	hitretResult := Hitret(6620)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010748")
	Mess("...Nn, nnku... Haruuu......")
	hitretResult := Hitret(6621)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!!")
	hitretResult := Hitret(6622)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("She's calling my name.")
	hitretResult := Hitret(6623)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I secretly peered into Sora's room.")
	hitretResult := Hitret(6624)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM18", 0)
	SetCg("EA12a", 0, 0)
	Transition("WIP_RL", 2000)
	Talk("Sora", "SR010749")
	Mess("...Nn... Haruu... Haruuu......")
	hitretResult := Hitret(6625)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("At first I thought she was having a nightmare.")
	hitretResult := Hitret(6626)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("But, with an earnest face, Sora was breathing heavily／while groping her breasts.")
	hitretResult := Hitret(6627)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess(".........")
	hitretResult := Hitret(6628)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("It bugged me to be seeing what I wasn't supposed to see.")
	hitretResult := Hitret(6629)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("But, I couldn't look away.")
	hitretResult := Hitret(6630)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010750")
	Mess("Ahh... Haa haa... Nn... Kuu...")
	hitretResult := Hitret(6631)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("With one hand already reaching downwards, she played／with the inside of her spread out legs.")
	hitretResult := Hitret(6632)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010751")
	Mess("Ahh, auuu... Ahnn... Haa haa haaa......")
	hitretResult := Hitret(6633)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("While moaning in a sweet voice, her whole body trembled.")
	hitretResult := Hitret(6634)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Her entire body, now dyed pink, is covered in sweat.")
	hitretResult := Hitret(6635)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Her cuteness and lewdness intermingled, and the way the／cute girl degenerated was shocking.")
	hitretResult := Hitret(6636)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010752")
	Mess("*npuu*... *chuu*... Nnhuu... *chuu*... *rero*...")
	hitretResult := Hitret(6637)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010753")
	Mess("*chuu*... *chupu*... *shabu*... *chubu*......")
	hitretResult := Hitret(6638)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010754")
	Mess("Nnuu... *chuu*... *npuu*... *rero*...")
	hitretResult := Hitret(6639)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("With her own finger in her mouth, sucking it, it then／returned to her lewd place.")
	hitretResult := Hitret(6640)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA12b", 0, 0)
	Talk("Sora", "SR010755")
	Mess("...Uuu... yes... nnn......")
	hitretResult := Hitret(6641)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010756")
	Mess("Ahhaaa... Nnn......!!")
	hitretResult := Hitret(6642)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Her leg caught in her shorts began twitching.")
	hitretResult := Hitret(6643)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("But even still, in a trance, Sora continued stroking／down there.")
	hitretResult := Hitret(6644)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("The lewd sounds coming from her mouth increased.")
	hitretResult := Hitret(6645)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010757")
	Mess("...Haruu... Haruuu... Nnn!!")
	hitretResult := Hitret(6646)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("My name being called, startled me.")
	hitretResult := Hitret(6647)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I wonder if she discovered me peeking on her.")
	hitretResult := Hitret(6648)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("With her calling my name out without thinking, I just／kept silent, fixated on her actions")
	hitretResult := Hitret(6649)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010758")
	Mess("...I'm lonely... Ahh... Auu... Nnn......")
	hitretResult := Hitret(6650)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010759")
	Mess("...Why... ahh... do you... hate me?")
	hitretResult := Hitret(6651)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("While asking me that, Sora continued masturating.")
	hitretResult := Hitret(6652)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010760")
	Mess("...Uuu... Nnn... Even though I... love you... this／much...")
	hitretResult := Hitret(6653)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010761")
	Mess("... Haru... Haru... respond to me... Ahhh...!!")
	hitretResult := Hitret(6654)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Tightly pinching her nipple, her hips became unsteady.")
	hitretResult := Hitret(6655)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Sora couldn't help but bite her lip, and like that she／put her finger inside her mouth.")
	hitretResult := Hitret(6656)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010762")
	Mess("*nchuu*... *kucha*... *chu nnchuuu*...")
	hitretResult := Hitret(6657)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010763")
	Mess("*chupu chupu*... Nnnuuu... *chupo*... Haa, haa...")
	hitretResult := Hitret(6658)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010764")
	Mess("Haruu, Haruu...! Nnn... *chuuu*... *nnbuu*...!")
	hitretResult := Hitret(6659)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Breathing wild, heavy breaths, her hair danced about.")
	hitretResult := Hitret(6660)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010765")
	Mess("Haa haa... more... more... ahhh...")
	hitretResult := Hitret(6661)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010766")
	Mess("...Touch me... ahh...! Nnn.... haa haa......")
	hitretResult := Hitret(6662)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010767")
	Mess("...If it's Haru... I'll do whatever he... Ahhh...")
	hitretResult := Hitret(6663)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010768")
	Mess("...Nnuuu... nnhuu, nnfuuu......")
	hitretResult := Hitret(6664)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Her hips jumped over and over as Sora's crotch became／wet.")
	hitretResult := Hitret(6665)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Sora stimulated the area under where her pubic hair had／not fully grown.")
	hitretResult := Hitret(6666)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010769")
	Mess("Aguu... Nkuu... Ahh... Ahhhhh!!")
	hitretResult := Hitret(6667)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Sora's hips jumped as her back bent like a bridge.")
	hitretResult := Hitret(6668)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010770")
	Mess("...It... It feels... good... Haru......")
	hitretResult := Hitret(6669)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010771")
	Mess("...I... If it's Haru... nnn... anything...")
	hitretResult := Hitret(6670)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010772")
	Mess("I'll do anything... for... you, ahhhhuu...!!")
	hitretResult := Hitret(6671)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Stimulating herself with both hands, her moans become／loud.")
	hitretResult := Hitret(6672)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010773")
	Mess("...Ahh... No... ahh... ahhh... not yet...")
	hitretResult := Hitret(6673)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010774")
	Mess("Make me... feel better... Haru... more...")
	hitretResult := Hitret(6674)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010775")
	Mess("Do... whatever... you want... to... me...")
	hitretResult := Hitret(6675)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010776")
	Mess("...Auuu... Don't leave... me alone... Ahhh...")
	hitretResult := Hitret(6676)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010777")
	Mess("...Please...... Ahhh... Haruu.........")
	hitretResult := Hitret(6677)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Tears flooded her eyes as she called for me.")
	hitretResult := Hitret(6678)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Thinking of me, it's as if she's accepting me.")
	hitretResult := Hitret(6679)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("She loves me, this much......")
	hitretResult := Hitret(6680)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I became wrapped up in a strange arousal.")
	hitretResult := Hitret(6681)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("My crotch boiling, my heart intensely throbbing, my／sexual desire for her...")
	hitretResult := Hitret(6682)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I was impelled to bust into her room at any moment, and／pin her down in lust.")
	hitretResult := Hitret(6683)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010778")
	Mess("Akuu... Haru... Haruuu... Nnmm...!!")
	hitretResult := Hitret(6684)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Sora's calling... to accept me.")
	hitretResult := Hitret(6685)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("In embarrassment, my face was dyed bright red.")
	hitretResult := Hitret(6686)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Even though she's my sister... Even though she's my／precious family...")
	hitretResult := Hitret(6687)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I was entranced by Sora's body.")
	hitretResult := Hitret(6688)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I want to answer Sora's request.")
	hitretResult := Hitret(6689)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I want to reciprocate her love......")
	hitretResult := Hitret(6690)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I want us both to be sunk in desire.")
	hitretResult := Hitret(6691)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Common sense... Decency... I started to not care for／either them.")
	hitretResult := Hitret(6692)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Even though I'm loved this much... Even though I'm／wanted this much... I......")
	hitretResult := Hitret(6693)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("...want to respond......")
	hitretResult := Hitret(6694)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010779")
	Mess("Ahh, ahhhh..!! Haruu... Haruu...!! I... I'm...")
	hitretResult := Hitret(6695)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010780")
	Mess("I'm... cumming... Aauuu......")
	hitretResult := Hitret(6696)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("While trembling from pleasure with her eyes closed, she／indulges in pleasuring her vagina until the end.")
	hitretResult := Hitret(6697)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("When Sora spreads down there with her finger, cum／dribbles out and scatters on the bed.")
	hitretResult := Hitret(6698)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("With a twitch, cum spills out, and trickles down her／thigh.")
	hitretResult := Hitret(6699)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("As if rubbing it, Sora moves her finger.")
	hitretResult := Hitret(6700)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010781")
	Mess("Ah...Ahhh...!! Ha... Haru... I'm sorry... I...")
	hitretResult := Hitret(6701)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010782")
	Mess("I'm cumming... I'm cumming... Aguuu... Nnnnn...")
	hitretResult := Hitret(6702)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Sora closes her legs while enduring it.")
	hitretResult := Hitret(6703)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Her clitoris is rising as it's being stimulated.")
	hitretResult := Hitret(6704)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010783")
	Mess("Ah... Haa haa haa... Watch... watch me... when I cum...")
	hitretResult := Hitret(6705)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010784")
	Mess("...Don't... hate me... Ahhh...")
	hitretResult := Hitret(6706)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010785")
	Mess("...Haruu... Haruu... I love you......")
	hitretResult := Hitret(6707)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010786")
	Mess("...I love you... Ahhh... Haa haa haa...")
	hitretResult := Hitret(6708)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Getting up, she opens her crotch widely as if to show／me.")
	hitretResult := Hitret(6709)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010787")
	Mess("Ahh... Ahhhh...!! Ahhhannn...!! Nnkkuu...")
	hitretResult := Hitret(6710)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Her hips jumped up as her whole body began to convulse.")
	hitretResult := Hitret(6711)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EA12c", 0, 0)
	Flush("WHITE", 1000, "")
	Talk("Sora", "SR010788")
	Mess("I'm... cumming... I'm cumminggggg!! Ahhaaaaahh!!")
	hitretResult := Hitret(6712)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010789")
	Mess("Ahhhhhhhhh... Ahh... Haa haa haa...")
	hitretResult := Hitret(6713)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("While her body trembled, Sora continued cumming.")
	hitretResult := Hitret(6714)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("As her drool becomes mixed with her tears, her／expression becomes absentminded.")
	hitretResult := Hitret(6715)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR010790")
	Mess("...Ahh... Ah... Haruu......")
	hitretResult := Hitret(6716)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("With her hips raised, thick cum pours out.")
	hitretResult := Hitret(6717)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....................")
	hitretResult := Hitret(6718)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("When I first saw Sora doing this, I couldn't keep my／eyes off her.")
	hitretResult := Hitret(6719)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Craving for my sister, I tried to satisfy my own sexual／desire.")
	hitretResult := Hitret(6720)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("It was embarrassing.")
	hitretResult := Hitret(6721)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Sora spilling out her true feelings for me... It only／made me crave her more.")
	hitretResult := Hitret(6722)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("With my dick still hard, my arousal hasn't yet calmed／down.")
	hitretResult := Hitret(6723)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("How pitiful...")
	hitretResult := Hitret(6724)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Sora's feelings... as siblings... as family......")
	hitretResult := Hitret(6725)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("It made me realize that I, myself, was the one／responsible for Sora's feelings that I had not／reciprocated.")
	hitretResult := Hitret(6726)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	OnRecollectFlag(1)
	If (IsRecollectMode())
		Change("EXIT_SCENARIO")
		Return
	EndIf
	EyeCatchEnter("DATE", 0)
	Change("00_a021")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

