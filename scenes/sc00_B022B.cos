Include"yosuga.ch"

Function sc00_B022B()
	ScenarioEnter()
	SetCg("BLACK", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Several days later.")
	hitretResult := Hitret(15280)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM06", 0)
	SetCg("B33a", 0, 0)
	Talk("Nao", "NO031256")
	Mess("Haa-Haru-chan! S-Sorry, sorry!")
	hitretResult := Hitret(15281)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chaan! Over here, over here!")
	hitretResult := Hitret(15282)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan stretched her hand as she ran towards me.")
	hitretResult := Hitret(15283)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's not yet time, I was fine you didn't need to comeÅ^here right away.")
	hitretResult := Hitret(15284)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO031257")
	Mess("*Panting*... S-Sorry for making you wait, I didn'tÅ^notice the time while I was preparingÅ`.")
	hitretResult := Hitret(15285)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Just calm down for now. I came here way too early, soÅ^you don't need to worry about it.")
	hitretResult := Hitret(15286)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO031258")
	Mess("Really? ... Geez~, is my make-up ruined?")
	hitretResult := Hitret(15287)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan took out a mirror from her hand bag as she saidÅ^that, then she checked her face.")
	hitretResult := Hitret(15288)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031259")
	Mess("Haru-chan, my face... doesn't look weird right?")
	hitretResult := Hitret(15289)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmmm? Your make-up looks natural. It looks good on you.")
	hitretResult := Hitret(15290)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_11M", 0, 0)
	Talk("Nao", "NO031260")
	Mess("Thank you... I'm just being careful of getting anyÅ^sunburns, it hurts on the skin if I don't put my makeÅ^up, also it would be bad if I miss some spot.")
	hitretResult := Hitret(15291)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan fixed her bangs as she lifted it up while sheÅ^was using her mirror.")
	hitretResult := Hitret(15292)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_02M", 0, 0)
	Talk("Nao", "NO031261")
	Mess("But, I'm glad you understand me Haru-chan. Ah, I thinkÅ^my dress is a little bit too much?")
	hitretResult := Hitret(15293)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, no... I can see a your hair also has some. JustÅ^don't put too much make up.")
	hitretResult := Hitret(15294)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031262")
	Mess("It's because my natural look is much more better thanÅ^the one with the make-up right? As expected from aÅ^city-boy!")
	hitretResult := Hitret(15295)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Stop that... Geez, originally -- I WAS a city boy.")
	hitretResult := Hitret(15296)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031263")
	Mess("And again, aren't you being too humble? I think that'sÅ^one of the things that I like about you, Haru-chan.")
	hitretResult := Hitret(15297)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thank you, I've always liked your natural look Nao-chan.")
	hitretResult := Hitret(15298)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_04M", 0, 0)
	Talk("Nao", "NO031264")
	Mess("Geez! You're really good at flattering me. But, I don'tÅ^have make-up always right? It's because we shouldn'tÅ^swim with it.")
	hitretResult := Hitret(15299)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Because it might make the pool dirty?")
	hitretResult := Hitret(15300)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031265")
	Mess("YesÅ`, \"Let the chlorine stick to your skin\"... or soÅ^they say! Right?")
	hitretResult := Hitret(15301)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahahahaha!")
	hitretResult := Hitret(15302)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan grabbed me as she ran, I felt a bit excited asÅ^well as we don't usually do this.")
	hitretResult := Hitret(15303)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It feels different as we have been always dating by theÅ^pool, it's been a while since we got out too, so I feelÅ^a bit excited about this.")
	hitretResult := Hitret(15304)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I looked into my phone for a nice spot, and sinceÅ^Nao-chan had told me about the things she like before, IÅ^was able to prepare ahead of her.")
	hitretResult := Hitret(15305)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO031266")
	Mess("Let's have fun todaaay!")
	hitretResult := Hitret(15306)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Do you want to go somewhere?")
	hitretResult := Hitret(15307)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031267")
	Mess("Well, I have a few. Later, I think it would be fine ifÅ^we look into this one place I've been interested to.")
	hitretResult := Hitret(15308)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO031268")
	Mess("Ah, how about you Haru-chan, do you have any plans to goÅ^to?")
	hitretResult := Hitret(15309)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("HmmmÅ`, I'm really not familiar with the streets here,Å^but there's also this certain place I want to see.")
	hitretResult := Hitret(15310)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031269")
	Mess("Well, I'll be your escort then. And Haru-chan is theÅ^producer for this date, I'll look forward to it then?")
	hitretResult := Hitret(15311)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y-Yeah, I'll do my best!!")
	hitretResult := Hitret(15312)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_04M", 0, 0)
	Talk("Nao", "NO031270")
	Mess("Fufufu, I'm really looking forward to it.")
	hitretResult := Hitret(15313)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("D-Don't pressure me like that...")
	hitretResult := Hitret(15314)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO031271")
	Mess("Ahahaha, it's fine don't be too nervous.")
	hitretResult := Hitret(15315)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She always cooks for us, and she's been always doing theÅ^lead, that's why I'll be doing more than my best forÅ^Nao-chan to enjoy our date today.")
	hitretResult := Hitret(15316)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I also have a lot of date-plans inside my head, theÅ^train came horning while I was also thinking about myÅ^plans.")
	hitretResult := Hitret(15317)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Here's your ticket. I've bought it for you.")
	hitretResult := Hitret(15318)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO031272")
	Mess("Thank you. Today, let's hang out as much as you want!")
	hitretResult := Hitret(15319)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah!")
	hitretResult := Hitret(15320)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1500, 0)
	SetCg("B39a", 0, 0)
	Transition("WIP_LR", 500)
	ScPlaySe("SE405", 0)
	Talk("Haruka", "")
	Mess("Ooh, that arcade looks great.")
	hitretResult := Hitret(15321)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_04M", 0, 0)
	Talk("Nao", "NO031273")
	Mess("Right, right? Teehee.")
	hitretResult := Hitret(15322)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You don't really have to know this Nao-chan?")
	hitretResult := Hitret(15323)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	ScStopSe()
	Talk("Nao", "NO031274")
	Mess("Hehe, I guess so. You see, it's been a long time everÅ^since I went here. It used to be a shopping districtÅ^before.")
	hitretResult := Hitret(15324)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031275")
	Mess("That's why, you won't be able to see anything if you goÅ^here before, though they've really changed the wholeÅ^spot.")
	hitretResult := Hitret(15325)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031276")
	Mess("How can you compare this from the city Haru-chan?")
	hitretResult := Hitret(15326)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Here, it's so clean and the atmosphere is so peaceful,Å^they also have the branded store that I love, it'sÅ^really pleasing.")
	hitretResult := Hitret(15327)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031277")
	Mess("Alright, I won!")
	hitretResult := Hitret(15328)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahahaha, it's my loss then. Now then, let's go see someÅ^stores.")
	hitretResult := Hitret(15329)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031278")
	Mess("Sure sure! In what store do you want to go?")
	hitretResult := Hitret(15330)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Which is the best here, the novelty stores or theÅ^ordinary ones?")
	hitretResult := Hitret(15331)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031279")
	Mess("Let's just go in any of them.")
	hitretResult := Hitret(15332)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay then, let's start in novelty stores then.")
	hitretResult := Hitret(15333)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031280")
	Mess("Alright! If it's boring, there's a punishment.")
	hitretResult := Hitret(15334)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh!? Seriously?")
	hitretResult := Hitret(15335)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031281")
	Mess("Fufufu, I'm serious. You'll have to treat me with aÅ^juice then.")
	hitretResult := Hitret(15336)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("W-Will this be all right...")
	hitretResult := Hitret(15337)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As soon as we arrived in the arcade section, it wasÅ^crowded with people.")
	hitretResult := Hitret(15338)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We got sucked in by the wave of the people, and withÅ^that we were able to get inside the building.")
	hitretResult := Hitret(15339)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B39a", 0, 0)
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO031282")
	Mess("Ahahahahahaha!! This is fuun!")
	hitretResult := Hitret(15340)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan, you're laughing too much!")
	hitretResult := Hitret(15341)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031283")
	Mess("It's because, it was really fun!")
	hitretResult := Hitret(15342)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031284")
	Mess("They got hats with umbrellas on top, automaticÅ^stockings, they've really thought about it.")
	hitretResult := Hitret(15343)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031285")
	Mess("They took the chance of using everything, a line up ofÅ^books with 6 kitchen knifes, it's way over myÅ^imagination.")
	hitretResult := Hitret(15344)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO031286")
	Mess("Their sales idea is so amazing. My hands has beenÅ^itching since I want to buy everything that I wasÅ^seeing.")
	hitretResult := Hitret(15345)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It really was fun. I'm really glad with it, I think IÅ^just got saved from the punishment too.")
	hitretResult := Hitret(15346)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_02M", 0, 0)
	Talk("Nao", "NO031287")
	Mess("Fufufu, but I feel so thirsty since I've been laughingÅ^so hard. Can you buy me some?")
	hitretResult := Hitret(15347)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan saw the \"Seattle's Coffee\" drink stand in theÅ^middle of the street by the arcade section, then sheÅ^pointed at it.")
	hitretResult := Hitret(15348)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Aah, shall I get some of that?")
	hitretResult := Hitret(15349)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO031288")
	Mess("That looks healthy, I'll go with you so I can treat youÅ^while you treat me.")
	hitretResult := Hitret(15350)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031289")
	Mess("So, what do you like to drink Haru-chan? I want aÅ^Tropical Mix Juice!")
	hitretResult := Hitret(15351)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay then, I'll go with the Frozen Mocha Chocolate.")
	hitretResult := Hitret(15352)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031290")
	Mess("Ooh, this place is so luxurious to go to, you're prettyÅ^good.")
	hitretResult := Hitret(15353)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("N-No, I didn't plan anything...")
	hitretResult := Hitret(15354)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031291")
	Mess("Fufufu, let me drink some of yours later. Then you canÅ^also have some of mine.")
	hitretResult := Hitret(15355)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Nao", "NO031292")
	Mess("Ah, Haru-chaaaan, what toppings do you like?")
	hitretResult := Hitret(15356)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmmm, let me line up with you, that way I can chooseÅ^properly.")
	hitretResult := Hitret(15357)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO031293")
	Mess("Tch, just when I was feeling like your real Onee-chan.Å^Haru-chan, this place is so stylish.")
	hitretResult := Hitret(15358)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, they always do it that way.")
	hitretResult := Hitret(15359)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_09M", 0, 0)
	Talk("Nao", "NO031294")
	Mess("I knew it, you were raised around these things.")
	hitretResult := Hitret(15360)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm just really familiar with it. Nao-chan too, once youÅ^drink it you'll never forget it.")
	hitretResult := Hitret(15361)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO031295")
	Mess("Ugh, \"once you drink it...\" you're sounding more of aÅ^city-boy again! Haru-chan's usual things!")
	hitretResult := Hitret(15362)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ugh... That's kinda harsh...")
	hitretResult := Hitret(15363)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_02M", 0, 0)
	Talk("Nao", "NO031296")
	Mess("Fufu, look, it's our turn. Um, a regular sized tropicalÅ^mix juice please.")
	hitretResult := Hitret(15364)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Frozen mocha chocolate for me, with an additional creamÅ^on top please.")
	hitretResult := Hitret(15365)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031297")
	Mess("It looks delicious.")
	hitretResult := Hitret(15366)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan, your's looks so interesting as well.")
	hitretResult := Hitret(15367)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031298")
	Mess("Aah, the money... Okay... Here, thank you.")
	hitretResult := Hitret(15368)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We paid the sales person, then we went to get ourÅ^reciept by the booth, each drink has its own reciept.")
	hitretResult := Hitret(15369)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031299")
	Mess("Okay then, thank you for the drink...*gulp*gulp*...Å^nhaa~, it's sweet and delicious!")
	hitretResult := Hitret(15370)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Here, Nao-chan, would you want to have some of my drinkÅ^as well?")
	hitretResult := Hitret(15371)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031300")
	Mess("Th-Thank you. Okay then, take mine too.")
	hitretResult := Hitret(15372)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan and I exchanged drinks, and we drank on eachÅ^other's straw.")
	hitretResult := Hitret(15373)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_13M", 0, 0)
	Jump(3, 0)
	Talk("Nao", "NO031301")
	Mess("*gulp*gulp* Whoa! Delicious! What's this!? Haru-chan, IÅ^thought it only looked good!")
	hitretResult := Hitret(15374)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("L-Looked good!? Y-You're quite wrong there...")
	hitretResult := Hitret(15375)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO031302")
	Mess("Sorry, I think I habitually say that... Though thisÅ^coffee shop uses powder, I wonder if the taste is stillÅ^the same if we bring some home.")
	hitretResult := Hitret(15376)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("On your next birthday, I wonder if I can get you a giftÅ^set? Something like an expresso machine.")
	hitretResult := Hitret(15377)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO031303")
	Mess("Kuh... Don't tempt me. *Gulp*... ngh...")
	hitretResult := Hitret(15378)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("This tastes great too. I get a feeling I'm by the beachÅ^when I drink it.")
	hitretResult := Hitret(15379)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031304")
	Mess("I know right? It feels like summer, right? Here, thanks.")
	hitretResult := Hitret(15380)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Can I drink more?")
	hitretResult := Hitret(15381)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO031305")
	Mess("If you drink a lot, then I won't be drinking too much.Å^We can enjoy it more when we come back next time!")
	hitretResult := Hitret(15382)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahaha.")
	hitretResult := Hitret(15383)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO031306")
	Mess("Well, we don't have much time anymore, let's get moving.")
	hitretResult := Hitret(15384)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, now, let's go and see the places you want to seeÅ^Nao-chan.")
	hitretResult := Hitret(15385)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031307")
	Mess("I'm fine with doing some window shopping. You'll be ableÅ^to see a lot of things.")
	hitretResult := Hitret(15386)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("Okay, I got it.")
	hitretResult := Hitret(15387)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B39a", 0, 0)
	ScPlayBgm("BGM05", 0)
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan and walked through the arcade section.")
	hitretResult := Hitret(15388)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan looked so interested in one of the clothes thatÅ^are displayed oustide, she even went near it.")
	hitretResult := Hitret(15389)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_02M", 0, 0)
	Talk("Nao", "NO031308")
	Mess("It feels like it's already autumn here. It's still tooÅ^early for that... Speaking of which, the summer goodsÅ^are on sale too.")
	hitretResult := Hitret(15390)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan, these clothes would look good on you.")
	hitretResult := Hitret(15391)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031309")
	Mess("Really? Fufufu, they're fluttering too, do you like themÅ^Haru-chan? Maybe I can take off my clothes and try themÅ^on.")
	hitretResult := Hitret(15392)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("T-Take your clothes off!? Eh, no... I wasn't thinking ofÅ^that... Eh? Eeeeeeh!?")
	hitretResult := Hitret(15393)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO031310")
	Mess("Geez, I was just kidding! Aah, it looks like there areÅ^no more stocks for it, I want to buuuuy itÅ`.")
	hitretResult := Hitret(15394)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_11M", 0, 0)
	Talk("Nao", "NO031311")
	Mess("If I could just work and save some money, I wouldÅ^probably have bought this luxurious dress.")
	hitretResult := Hitret(15395)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If I could also work, I can give it to you as a present.")
	hitretResult := Hitret(15396)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031312")
	Mess("Thank you Haru-chan, I feel good with just you thinkingÅ^of my well-being.")
	hitretResult := Hitret(15397)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031313")
	Mess("I'll be able to wear stylish clothes someday, then I'dÅ^be able to walk beside you Haru-chan with suchÅ^confidence.")
	hitretResult := Hitret(15398)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Still, I'm really happy that I'm right here walkingÅ^beside you.")
	hitretResult := Hitret(15399)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO031314")
	Mess("Ahaha... Saying that with a serious face, it'sÅ^embarrassing... Haru-chan, you really know how to treatÅ^your girlfriend in a nice way.")
	hitretResult := Hitret(15400)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's not like that... I-It's because I was saying it toÅ^the person I love.")
	hitretResult := Hitret(15401)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_11M", 0, 0)
	Talk("Nao", "NO031315")
	Mess("Haru-chan...")
	hitretResult := Hitret(15402)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("'Kay, let's go Nao-can. Don't you want to hang out more,Å^it will be a waste you know?")
	hitretResult := Hitret(15403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03L", 0, 0)
	Talk("Nao", "NO031316")
	Mess("Yeah! Let's go!")
	hitretResult := Hitret(15404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I took Nao-chan's hands as I encouragely pulled her, sheÅ^smiled as she was embarrassed making her face red.")
	hitretResult := Hitret(15405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When our skins touch, when we kiss each other; it's likeÅ^there's a breeze that's pushing us to hold onto eachÅ^other which makes it a bit embarrassing.")
	hitretResult := Hitret(15406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, something hot is spreading deep inside my chest, itÅ^has this light feeling.")
	hitretResult := Hitret(15407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's slowly itching, it wants to burst out of me, but IÅ^feel like I need to continue to keep it inside me as IÅ^don't want it to make any conflicts.")
	hitretResult := Hitret(15408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("After that, we went around to see some stores, we ateÅ^pasta and pizza for our lunch.")
	hitretResult := Hitret(15409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though our lunch was so many, it really isn't thatÅ^expensive, Nao-chan seems to like it a lot, it looks soÅ^delicious as well.")
	hitretResult := Hitret(15410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B39a", 0, 0)
	SetBustup("CB03_01M", 0, 0)
	Transition("WIP_RL", 500)
	ScPlayBgm("BGM15", 0)
	Talk("Nao", "NO031317")
	Mess("Fuuuu! Let's eat, let's eat! This looks fun. It's alsoÅ^my first time to try a pasta with basil sauce on it.")
	hitretResult := Hitret(15411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("This Neapolitan meat sauce is also my first, I'm quiteÅ^surprised that they've got a lot of selections here.")
	hitretResult := Hitret(15412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031318")
	Mess("I want to come here and eat some againÅ`. But, let's buyÅ^other seasonings next time, so I can also try to make itÅ^like this.")
	hitretResult := Hitret(15413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, that's great. Okay then, let's do it next time.")
	hitretResult := Hitret(15414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO031319")
	Mess("It's fine! But, I need a bit more of practice. It'sÅ^quite sudden too, maybe I'll make a lot of mistakes too.")
	hitretResult := Hitret(15415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031320")
	Mess("Aah, but, the coffee the we just had, isn't it a bitÅ^personalized?")
	hitretResult := Hitret(15416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, I also thought that it was our usual coffee, butÅ^then it turned out to be bitter when I drank it.")
	hitretResult := Hitret(15417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031321")
	Mess("I wonder why, you could say that it's a waste, but weÅ^paid for the consequence.")
	hitretResult := Hitret(15418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031322")
	Mess("It was oily, can you still feel it in your mouth? YouÅ^can say that it's like some pickled vegtables?")
	hitretResult := Hitret(15419)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Pickled vegtables... You really think about weird thingsÅ^Nao-chan.")
	hitretResult := Hitret(15420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO031323")
	Mess("Eh!? But doesn't it smell old fashioned?")
	hitretResult := Hitret(15421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If Ryouhei was here, he'll probably be a little bit wildÅ^too.")
	hitretResult := Hitret(15422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_08M", 0, 0)
	Talk("Nao", "NO031324")
	Mess("I get it! It's because we ate some pickled vegtablesÅ^earlier! Ah, come on Haru-chan, laugh with me!!")
	hitretResult := Hitret(15423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahahaha! Sorry, sorry!!")
	hitretResult := Hitret(15424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_06M", 0, 0)
	Talk("Nao", "NO031325")
	Mess("*Sigh* I really do sound like a total bumpkin.")
	hitretResult := Hitret(15425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Really, I'm sorry. Look, look... Let's go to the gameÅ^center after our meal so that we could exercise a bit.")
	hitretResult := Hitret(15426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_08M", 0, 0)
	Talk("Nao", "NO031326")
	Mess("Muu--Victory! Victory! If you lose, there'll be aÅ^punishment!!")
	hitretResult := Hitret(15427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, I got it. You think I'll let you win easily?")
	hitretResult := Hitret(15428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_04M", 0, 0)
	Talk("Nao", "NO031327")
	Mess("Let's go see that place! All right! Let's go, let's go!")
	hitretResult := Hitret(15429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The punishment game should be fun then.")
	hitretResult := Hitret(15430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO031328")
	Mess("Geez, if I win, Haru-chan will wear the clothes of hisÅ^cute girlfriend!")
	hitretResult := Hitret(15431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eehhhhhhhhhhhh!? A-As I thought, this is your usual wayÅ^of having fun...")
	hitretResult := Hitret(15432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031329")
	Mess("No, no! If I win, I'll let you borrow my maid and mikoÅ^clothes!")
	hitretResult := Hitret(15433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Th-That's not fair at all!!")
	hitretResult := Hitret(15434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_04M", 0, 0)
	Talk("Nao", "NO031330")
	Mess("Hahaha, you can't get away from this! Look, look, overÅ^here~! Your kind OneeÅ`san will still let you have itÅ^either you win or lose, uhahaha.")
	hitretResult := Hitret(15435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Aaughhh!!!")
	hitretResult := Hitret(15436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_02M", 0, 0)
	Talk("Nao", "NO031331")
	Mess("Haru-chan, if you're gonna wear my clothes I'll be sureÅ^to take a picture of you to commemorate it. Hahaha, itÅ^would be something that I would treasure for a lifeÅ^time!")
	hitretResult := Hitret(15437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No! If that would happen, my whole life would beÅ^oveeeer!")
	hitretResult := Hitret(15438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_04L", 0, 0)
	Talk("Nao", "NO031332")
	Mess("Uhahahaha!")
	hitretResult := Hitret(15439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Quake(16, 0, 1, 2, 30)
	Talk("Haruka", "")
	Mess("Uaaaaaagh!!")
	hitretResult := Hitret(15440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan had a suspiscious smile as she placed her armsÅ^around my neck, then she dragged me up to the gameÅ^center.")
	hitretResult := Hitret(15441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide(1)
	ScWait(1500, 0)
	SetCg("B39b", 0, 0)
	SetBustup("CB03_03M", 0, 0)
	ScPlayBgm("BGM07", 0)
	Talk("Nao", "NO031333")
	Mess("Ngyaaaa!! Let's play!!!")
	hitretResult := Hitret(15442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y-Yeah...")
	hitretResult := Hitret(15443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO031334")
	Mess("Hmmm? What's the matter Haru-chan. Are you tired?")
	hitretResult := Hitret(15444)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y-Yeah... I somehow felt a lot better when I won.")
	hitretResult := Hitret(15445)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO031335")
	Mess("I regret it... Really.")
	hitretResult := Hitret(15446)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Somehow I was glad to bring it to a draw. But you knowÅ^Nao-chan... It's your first time to play on the gameÅ^center, yet you were so good?")
	hitretResult := Hitret(15447)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031336")
	Mess("Hahaha, it's just your imagination. Really, I want toÅ^see Haru-chan wearing a girl's clothes.")
	hitretResult := Hitret(15448)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I won't let that happen!!")
	hitretResult := Hitret(15449)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031337")
	Mess("Haha, that would make more infatuated with youÅ^Haru-chan.")
	hitretResult := Hitret(15450)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ugh, uuughh... I'm glad that it's a draw...")
	hitretResult := Hitret(15451)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_02M", 0, 0)
	Talk("Nao", "NO031338")
	Mess("Huh? What about my punishment?")
	hitretResult := Hitret(15452)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh...")
	hitretResult := Hitret(15453)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_11M", 0, 0)
	Talk("Nao", "NO031339")
	Mess("If it was you Haru-chan, I'd do anything that you'd askÅ^me to.")
	hitretResult := Hitret(15454)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...I'm kind of tired today, we can have fun again nextÅ^time.")
	hitretResult := Hitret(15455)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO031340")
	Mess("Yeah, whoever wins next time. Ah, how about betting on aÅ^50-meter freestyle swimming on the pool?")
	hitretResult := Hitret(15456)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I would absolutely lose there!")
	hitretResult := Hitret(15457)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031341")
	Mess("Hahaha, a volleyball then.")
	hitretResult := Hitret(15458)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Geez, Nao-chan...")
	hitretResult := Hitret(15459)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO031342")
	Mess("Okay then, even though it's quite regretful, it's almostÅ^time for us to go home. I also have a lot of souvenirs.")
	hitretResult := Hitret(15460)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan lifted her bag that she filled up from the gameÅ^center.")
	hitretResult := Hitret(15461)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She stuffed it with the things from the item catcher andÅ^the mini-game gifts.")
	hitretResult := Hitret(15462)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was a result from our so-called war, and we got a lotÅ^of spoils from it.")
	hitretResult := Hitret(15463)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Looks like we can give everyone a souvenir.")
	hitretResult := Hitret(15464)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031343")
	Mess("You can get what you want right now, someone might getÅ^it later.")
	hitretResult := Hitret(15465)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, Nao-chan too.")
	hitretResult := Hitret(15466)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_02M", 0, 0)
	Talk("Nao", "NO031344")
	Mess("Well, I wonder which stuffed toy I should get. This guyÅ^looks like a dice.")
	hitretResult := Hitret(15467)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The one you gave me looks like a radio... Does it work?")
	hitretResult := Hitret(15468)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031345")
	Mess("We can call everyone next time, so we can just buy theÅ^things they want on our way.")
	hitretResult := Hitret(15469)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm pretty sure they'll dig onto something, sayingÅ^things like \"So you guys went out\".")
	hitretResult := Hitret(15470)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO031346")
	Mess("Haha, that may be right.")
	hitretResult := Hitret(15471)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If we tell everyone that the both of us went to a dateÅ^by the game center, I don't know how I'll even tell themÅ^that.")
	hitretResult := Hitret(15472)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I want to see a bit of their reaction, though it wouldÅ^probably be embarrassing.")
	hitretResult := Hitret(15473)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO031347")
	Mess("Are we going to make it for the train?")
	hitretResult := Hitret(15474)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, we're in time for it.")
	hitretResult := Hitret(15475)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031348")
	Mess("Aah, I'm really regretful about it... I even wanted toÅ^eat our dinner here.")
	hitretResult := Hitret(15476)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... But, it's quite late already, and maybe yourÅ^mother is worried about you already Nao-chan.")
	hitretResult := Hitret(15477)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	SetBustup("CB03_10M", 0, 0)
	Talk("Nao", "NO031349")
	Mess("Ah...")
	hitretResult := Hitret(15478)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was sudden but subtle, Nao-chan shrugged herÅ^shoulders a bit.")
	hitretResult := Hitret(15479)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At that moment, it's like I didn't notice that I hadÅ^said something bad.")
	hitretResult := Hitret(15480)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... N-Nao-chan...")
	hitretResult := Hitret(15481)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM16", 0)
	SetBustup("CB03_11M", 0, 0)
	Talk("Nao", "NO031350")
	Mess("Y-Yeah... Me too, Sora-chan might be hungry waiting forÅ^you... It's late, I feel sorry for her...")
	hitretResult := Hitret(15482)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y-Yeah...")
	hitretResult := Hitret(15483)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's been sulking these past few days, locking herselfÅ^inside her room most of the time.")
	hitretResult := Hitret(15484)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Whenever I'm not around, I always leave some food in theÅ^kitchen but she brings them inside her room.")
	hitretResult := Hitret(15485)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Whenever she sees me, it would only mean she needsÅ^something or she won't talk at all.")
	hitretResult := Hitret(15486)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I told her that I'll be going out today... but sheÅ^didn't even answer back. Perhaps if I didn't go homeÅ^late, she wouldn't be mad again.")
	hitretResult := Hitret(15487)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I took out my cell phone and looked at it, there wasn'tÅ^even a single mail from Sora. I felt scared as ifÅ^something flipped inside me.")
	hitretResult := Hitret(15488)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But... I want to be with Nao-chan even for just a littleÅ^bit more... so I placed those eerie feelings behind me.")
	hitretResult := Hitret(15489)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO031351")
	Mess("I really had fun, I'll look forward next time.")
	hitretResult := Hitret(15490)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031352")
	Mess("Let's go home, Haru-chan.")
	hitretResult := Hitret(15491)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... you're right.")
	hitretResult := Hitret(15492)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("We were mixed along with the other passengers by theÅ^station as we waited for the train, we're heading backÅ^home to Hozumi station.")
	hitretResult := Hitret(15493)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I really enjoyed it, saying the wrong things rightÅ^before when our date just started.")
	hitretResult := Hitret(15494)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At that time, if I hadn't said anything unnecessary,Å^we'll we be talking about the same things in thoseÅ^restaurants?")
	hitretResult := Hitret(15495)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But... Even though our date ended in a simple way, I'mÅ^glad to see Nao-chan smiling as we had fun, saying thetÅ^she really enjoyed.")
	hitretResult := Hitret(15496)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We got off the train... Then, we held hands all the timeÅ^until we parted by my house, I will never forget theÅ^warmth of her hands.")
	hitretResult := Hitret(15497)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That's because I didn't want to create an awkward auraÅ^between us, I'm quite worried because she was kind to meÅ^until the end.")
	hitretResult := Hitret(15498)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("DATE", 0)
	Change("00_b023")
	Return
	Change("EXIT_SCENARIO")
EndFunc

