Include"yosuga.ch"

Function sc00_C012()
	ScenarioEnter()
	SetCg("B01a", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Haruka", "")
	Mess("...... That's about it.")
	hitretResult := Hitret(19940)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM05", 0)
	Talk("êSÇÃê∫", "")
	Mess("I was stuffing gathered garbage into a vinyl bag.")
	hitretResult := Hitret(19941)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was the first time I was doing cleaning aroundÅ^entranceway.")
	hitretResult := Hitret(19942)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Differently to as it was in apartment, it's not like itÅ^will be done for me, unnoticed. If I won't do cleaningÅ^here, then, before I realize it, dirt and fallen leavesÅ^will quickly pile up.")
	hitretResult := Hitret(19943)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I didn't know how far should I go with cleaning, butÅ^once I started cleaning, I steadily had more and more toÅ^do, so it took me a considerable amount of time.")
	hitretResult := Hitret(19944)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's fun to do it once in a while, but I'd like to beÅ^spared from doing it everyday.")
	hitretResult := Hitret(19945)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume, who's cleaning everyday such wide groundsÅ^alone, is really amazing... She might be already used toÅ^it, but I found new respect for her.")
	hitretResult := Hitret(19946)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I picked up the broom left in the corner of the garden.")
	hitretResult := Hitret(19947)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...That Sora, she ran away...")
	hitretResult := Hitret(19948)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Well, there's also the thing that I became engrossed inÅ^cleaning up, but I'd like her to help me from now on...")
	hitretResult := Hitret(19949)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EC02c", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Akira", "AK020437")
	Mess("Ahaa, Haru-kun!! Cleaning up?")
	hitretResult := Hitret(19950)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yo, Amatsume. I finished just now.")
	hitretResult := Hitret(19951)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020438")
	Mess("It became pretty, really. Haru-kun, you're good atÅ^cleaning.")
	hitretResult := Hitret(19952)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You think so? It's the first cleaning I've done sinceÅ^moving here. It sure is tiring, once you put your mindÅ^into it, huh.")
	hitretResult := Hitret(19953)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EC02b", 0, 0)
	Talk("Akira", "AK020439")
	Mess("Me too, especially before tests, I get that overwhelmingÅ^desire to do some cleaning.")
	hitretResult := Hitret(19954)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's true, I got the will to do some cleaning too.")
	hitretResult := Hitret(19955)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("By the way, are you going somewhere to help out again?")
	hitretResult := Hitret(19956)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EC02a", 0, 0)
	Talk("Akira", "AK020440")
	Mess("Today, in order to make shrine ornaments, I'm on my wayÅ^to get some straws.")
	hitretResult := Hitret(19957)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Straws? Shrine ornaments...?")
	hitretResult := Hitret(19958)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020441")
	Mess("Yeah. In time of festival, we put out decorations, soÅ^I'm going to make some.")
	hitretResult := Hitret(19959)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heh, so it's hand-made. It's somehow family-like, right?")
	hitretResult := Hitret(19960)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020442")
	Mess("Haru-kun, won't you try it too? It's pretty fun.")
	hitretResult := Hitret(19961)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Is it okay? For an amateur like me to help.")
	hitretResult := Hitret(19962)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EC02c", 0, 0)
	Talk("Akira", "AK020443")
	Mess("I'm just about as much of an amateur like you, so it'sÅ^okay!")
	hitretResult := Hitret(19963)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh!? You're not used to it... Well, I'll let myselfÅ^watch how to do it a little.")
	hitretResult := Hitret(19964)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020444")
	Mess("Yeah, then, shall we go together to get straws?")
	hitretResult := Hitret(19965)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sure.")
	hitretResult := Hitret(19966)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(2000, 0)
	SetCg("B15a", 0, 0)
	Transition("WIP_LR", 500)
	Talk("Haruka", "")
	Mess("Well, is here okay?")
	hitretResult := Hitret(19967)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020445")
	Mess("Yeah, it's okay. Thank you, put it down somewhere here.")
	hitretResult := Hitret(19968)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heave... ho... Uh...")
	hitretResult := Hitret(19969)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Fastened to Amatsume's bicycle's luggage carrier, aÅ^bundle of packages, looking like rice straw bags, beingÅ^paid as annual tribute in historical dramas, was slowlyÅ^going down.")
	hitretResult := Hitret(19970)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As expected of Amatsume, it looked pretty heavy, so IÅ^came all the way here, pushing the bicycle.")
	hitretResult := Hitret(19971)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Local Ojii-san", "NP360001")
	Mess("Uouo, Akira-chan, I was waiting for you.")
	hitretResult := Hitret(19972)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020446")
	Mess("Grandpa, thank's for waiting! I look forward to yourÅ^help this year too.")
	hitretResult := Hitret(19973)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There were many elder people waiting in front of cheapÅ^candy store.")
	hitretResult := Hitret(19974)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Is everyone here going to help with making decorations?")
	hitretResult := Hitret(19975)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020447")
	Mess("Yeah, everyone here is a shrine parishioner.")
	hitretResult := Hitret(19976)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I greeted everyone, slowly bowing my head.")
	hitretResult := Hitret(19977)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Local Ojii-san", "NP360002")
	Mess("We have to make splendid decorations this year too,Å^right?")
	hitretResult := Hitret(19978)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Local Old Lady", "NP370001")
	Mess("Akira-chan, who is that person? Could it be a boyfriend?")
	hitretResult := Hitret(19979)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020448")
	Mess("Umm... Haru-kun is sensei's grandson. He moved inÅ^recently.")
	hitretResult := Hitret(19980)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm sorry for the late introduction... I'm KasuganoÅ^Haruka... Grandp... Grandfather taking care of me.")
	hitretResult := Hitret(19981)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Local Ojii-san", "NP360003")
	Mess("So is it? Sensei's grandson, right? A refined young manÅ^we have here.")
	hitretResult := Hitret(19982)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Local Old Lady", "NP370002")
	Mess("He and Akira-chan suit together, right? He's so prettyÅ^he could be mistaken for a girl.")
	hitretResult := Hitret(19983)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Pr-Pretty? No, it's...")
	hitretResult := Hitret(19984)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03M", 0, 0)
	Talk("Akira", "AK020449")
	Mess("Come on, Haru-kun, shall we make them together?")
	hitretResult := Hitret(19985)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Everyone gathered here was scattered before the store,Å^as if on a gossip meeting, and started working.")
	hitretResult := Hitret(19986)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Skillfully, slowly taking share of straws, carefullyÅ^getting the length and weaving.")
	hitretResult := Hitret(19987)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Practically no cars passed by here, so one could sitÅ^here on the ground, really feeling the tranquility. ItÅ^was the very image of people from countryside that canÅ^be seen in television.")
	hitretResult := Hitret(19988)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020450")
	Mess("Haru-kun, I'll teach you too, okay?")
	hitretResult := Hitret(19989)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Urged by Amatsume, I sat down on the store's pipe chairÅ^too.")
	hitretResult := Hitret(19990)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020451")
	Mess("Firstly, get the proper length, then make a smallÅ^bundle. Later, hold the lower part between your thighsÅ^and... start knitting from the top.")
	hitretResult := Hitret(19991)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020452")
	Mess("Haru-kun, did you ever knit Sora-chan's hair into aÅ^braid?")
	hitretResult := Hitret(19992)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No... I didn't.")
	hitretResult := Hitret(19993)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020453")
	Mess("Is that so? Then, give me your hand...")
	hitretResult := Hitret(19994)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah...")
	hitretResult := Hitret(19995)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01L", 0, 0)
	Talk("Akira", "AK020454")
	Mess("Do it like that, divide in three similar parts, and nowÅ^take it and alternate...")
	hitretResult := Hitret(19996)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A small and soft hand was holding my hand. And, while IÅ^was being surprised with having my hand grabbedÅ^suddenly, I clumsily moved my hand as I was being shown.")
	hitretResult := Hitret(19997)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I could feel a nice, sweet smell from Amatsume, whileÅ^her body was pulled to mine.")
	hitretResult := Hitret(19998)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Could it be the fragnance of shampoo that Amatsume used?Å^While thinking that dazedly, I was taking glances atÅ^Amatsume's face.")
	hitretResult := Hitret(19999)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02L", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Having fun, kindly smiling Amatsume.")
	hitretResult := Hitret(20000)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sociable, able to befriend anyone, maybe not having anyÅ^wariness at all, approaching to my side like that.")
	hitretResult := Hitret(20001)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She's easy to talk to, with her tender atmosphere, andÅ^always seems to be having fun. I understood many timesÅ^that Amatsume is being percieved as adorable byÅ^everyone.")
	hitretResult := Hitret(20002)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her honest side, and her side when she was sometimesÅ^feigning innocence too, are both amusing.")
	hitretResult := Hitret(20003)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I think that Amatsume just casually came into contactÅ^with me, but this distance between us, lower than usual,Å^was sure getting into my awareness.")
	hitretResult := Hitret(20004)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The other day, she touched me with her breasts. ThereÅ^were situations where she ended up causing meÅ^misunderstand.")
	hitretResult := Hitret(20005)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And... Right now, too...")
	hitretResult := Hitret(20006)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even if Amatsume doesn't have such intention... She'sÅ^gotten me embarrassed.")
	hitretResult := Hitret(20007)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03L", 0, 0)
	Talk("Akira", "AK020455")
	Mess("Did you get it?")
	hitretResult := Hitret(20008)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uwa!? Ah, no...")
	hitretResult := Hitret(20009)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume looked into my eyes, smiling. Even though we'reÅ^so close, Amatsume isn't any different from usual.")
	hitretResult := Hitret(20010)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Could it be that I'm outside of her awareness? BecauseÅ^I'm not a dangerous animal? To sum it up, she doesn'tÅ^percieve me as a man?")
	hitretResult := Hitret(20011)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...... That itself makes me feel down.")
	hitretResult := Hitret(20012)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020456")
	Mess("Let's do it one more time, okay?")
	hitretResult := Hitret(20013)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume guided my hand again, slowly weaving straws.")
	hitretResult := Hitret(20014)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Like that, we created one shrine rope.")
	hitretResult := Hitret(20015)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020457")
	Mess("Here, it's done. If we'll make a lot of them like thatÅ^and knit them together, we'll get one big shrine ropeÅ^ornament.")
	hitretResult := Hitret(20016)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y- Yes, right...")
	hitretResult := Hitret(20017)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_05M", 0, 0)
	Talk("Akira", "AK020458")
	Mess("Ah... Sorry, was it boring?")
	hitretResult := Hitret(20018)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Not at all, it's not that! Your explanation method wasÅ^good, so it was very easy to understand.")
	hitretResult := Hitret(20019)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While I was dazed by the lingering feeling of softnessÅ^of Amatsume's hand, I conversely made her worry... NotÅ^good, not good...")
	hitretResult := Hitret(20020)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_11M", 0, 0)
	Talk("Akira", "AK020459")
	Mess("It that so? I'm by no means good at it, so I'mÅ^practicing on Kazu-chan by knitting braids.")
	hitretResult := Hitret(20021)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then, maybe I'll get Sora to let me practice braiding onÅ^her.")
	hitretResult := Hitret(20022)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Although, staying motionlessly seems like a pain, so sheÅ^would end up running away quickly...")
	hitretResult := Hitret(20023)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020460")
	Mess("Well then, shall we make the next one together? Here,Å^next straw.")
	hitretResult := Hitret(20024)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... Thanks.")
	hitretResult := Hitret(20025)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Amatsume sat beside me again and began knitting, in theÅ^way she was explaining before.")
	hitretResult := Hitret(20026)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She showed me how to do it twice, so I intend to do myÅ^best on not knitting clumsily.")
	hitretResult := Hitret(20027)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("First, measure the length... Then, divide it into threeÅ^parts... And braid it...")
	hitretResult := Hitret(20028)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I started knitting steadily, while keeping the order...Å^So, it should be like forcing it through the hole inÅ^shoelace, huh?")
	hitretResult := Hitret(20029)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Anyway, if women knitting braids like that, untying themÅ^must be hard, right...?")
	hitretResult := Hitret(20030)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Thinking things like that, me and Amatsume were slowlyÅ^knitting straws. The straws we were knitting, onceÅ^combined with the ones knitted by elder people, made theÅ^final item even bigger, once weaved.")
	hitretResult := Hitret(20031)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Amazing, you're not using any string or glue?")
	hitretResult := Hitret(20032)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020461")
	Mess("This straw replaces string and, once weaved, it sticksÅ^together fairly well.")
	hitretResult := Hitret(20033)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020462")
	Mess("Well then, shall we make a decoration for traveler'sÅ^guardian deity too?")
	hitretResult := Hitret(20034)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh, is it okay for me to make it?")
	hitretResult := Hitret(20035)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020463")
	Mess("It's all right, it's very welcome! Then, shall we do itÅ^together?")
	hitretResult := Hitret(20036)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I was a honor shrine parishioner, right? That said, IÅ^need to put out some effort here and cooperate, huh.")
	hitretResult := Hitret(20037)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020464")
	Mess("Ahahaha, right.")
	hitretResult := Hitret(20038)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020465")
	Mess("Here, Haru-kun. Hold the bottom of it, okay?")
	hitretResult := Hitret(20039)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While saying that, Amatsume started twisting it,Å^knitting it different than before.")
	hitretResult := Hitret(20040)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020466")
	Mess("Here, next is Haru-kun's turn. Twist it, pressing andÅ^turning around, okay?")
	hitretResult := Hitret(20041)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Here we go... It seems to need quite a bit of force...")
	hitretResult := Hitret(20042)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Twisting it clockwisely, it was knitted as if it wasÅ^screwed in. A small shrine rope-like thing came out as aÅ^result.")
	hitretResult := Hitret(20043)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03M", 0, 0)
	Talk("Akira", "AK020467")
	Mess("Oooh, Haru-kun, you're skillful! Then, shall we attachÅ^it to mine?")
	hitretResult := Hitret(20044)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We knitted it mine and Amatsume's, creating a loop,Å^but...")
	hitretResult := Hitret(20045)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("H- Huh... I- Is it okay like that?")
	hitretResult := Hitret(20046)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Shape of the decoration we knitted together was somehowÅ^resembling a heart mark.")
	hitretResult := Hitret(20047)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Somehow, it in the end it seems like we have putÅ^together two parts of a heart mark. I had a strangeÅ^feeling about it.")
	hitretResult := Hitret(20048)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_04M", 0, 0)
	Talk("Akira", "AK020468")
	Mess("Ahaha, it turned up to be cute, like a heart markÅô")
	hitretResult := Hitret(20049)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y- Yeah...")
	hitretResult := Hitret(20050)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Is it okay for it not to be round?")
	hitretResult := Hitret(20051)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020469")
	Mess("It's okay, it's okay. After all, we made it together andÅ^it looks interesting, so lets leave it like that.")
	hitretResult := Hitret(20052)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If you say so, then it's okay... Though...")
	hitretResult := Hitret(20053)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume doesn't seem to think much of it, huh? ThinkingÅ^so, was I the one that was overthinking things startingÅ^from a while ago?")
	hitretResult := Hitret(20054)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020470")
	Mess("Next, we need to make the part for lower gods and it'sÅ^complete.")
	hitretResult := Hitret(20055)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume put her hand into the sleeve of her mikoÅ^clothes, took out a pair of scizzors and cut the excessÅ^straws with a snipping sound.")
	hitretResult := Hitret(20056)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("A... Amatsume, are you always carrying scizzors withÅ^you?")
	hitretResult := Hitret(20057)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020471")
	Mess("Huh? Ah, yes, those are helpful in many situations, so IÅ^always have them inside.")
	hitretResult := Hitret(20058)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heeh, so you're well prepared, you say. Do you haveÅ^something else there too?")
	hitretResult := Hitret(20059)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetBustup("CC06_14M", 0, 0)
	Talk("Akira", "AK020472")
	Mess("Yeah, umm... This one and that one...")
	hitretResult := Hitret(20060)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM13", 0)
	Talk("êSÇÃê∫", "")
	Mess("While saying so, she began taking out stuff from herÅ^cuffs. Ball-point pen, packing tape, hammer, sweets,Å^sewing set, screwdriver, pliers and some device lookingÅ^like a mobile phone.")
	hitretResult := Hitret(20061)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I could let off the hammer in a rural area, but if she'dÅ^pop it out in town like that, she'd get arrested!")
	hitretResult := Hitret(20062)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("A- Amazing. It's like a handy pocket I saw in a mangaÅ^long time ago...")
	hitretResult := Hitret(20063)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020473")
	Mess("If I'd have a saw and nails there it would be complete!")
	hitretResult := Hitret(20064)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Preparing them like that, is she planning a robbery orÅ^what!?")
	hitretResult := Hitret(20065)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, umm... That's right, Amatsume, have you got a mobileÅ^phone?")
	hitretResult := Hitret(20066)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020474")
	Mess("Ah, this is a telephone extension. When I'm doingÅ^cleaning in the garden, even if I get a call, I don'tÅ^need to go back.")
	hitretResult := Hitret(20067)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Still, it won't connect all the way down here, right?")
	hitretResult := Hitret(20068)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03M", 0, 0)
	Talk("Akira", "AK020475")
	Mess("If you'll put a bit of effort, it will connect.")
	hitretResult := Hitret(20069)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("R- Right...")
	hitretResult := Hitret(20070)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020476")
	Mess("Ah, Haru-kun, shall we exchange phone numbers? I won'tÅ^send any e-mail, but I'll call you from time to time.")
	hitretResult := Hitret(20071)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, it's okay.")
	hitretResult := Hitret(20072)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	Talk("êSÇÃê∫", "")
	Mess("I had my mobile register Amatsume's phone number. SayingÅ^so, since the time I came here, I didn't call or sendÅ^e-mail to anyone besides Sora.")
	hitretResult := Hitret(20073)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I almost stopped sending any mails at all, so I didn'tÅ^use it much... It's been a while since I last exchangedÅ^phone numbers.")
	hitretResult := Hitret(20074)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("She's always besides me, but with that, I felt that withÅ^this became one more thing that bonded me with Amatsume.")
	hitretResult := Hitret(20075)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM03", 0)
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020024")
	Mess("...What the? It's that time already?... Yawn...")
	hitretResult := Hitret(20076)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020477")
	Mess("Ah, Hiro-nee-chan, good morning!")
	hitretResult := Hitret(20077)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020025")
	Mess("Akira, serve the eldery some tea.")
	hitretResult := Hitret(20078)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020478")
	Mess("Yeah, got it!")
	hitretResult := Hitret(20079)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020026")
	Mess("Ah? And what's up with you?")
	hitretResult := Hitret(20080)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I- I was helping with making shrine ornaments.")
	hitretResult := Hitret(20081)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020027")
	Mess("Hmph... Well, I don't care anyway.")
	hitretResult := Hitret(20082)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("............")
	hitretResult := Hitret(20083)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020028")
	Mess("Are you stiiil clinging to Akira even though you learnedÅ^all that?")
	hitretResult := Hitret(20084)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Learning all that you say... Akira is my friend, soÅ^there's no reason for me to avoid her.")
	hitretResult := Hitret(20085)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020479")
	Mess("Ready! A tea, as a refreshment for everyone fromÅ^Hiro-nee-chan!")
	hitretResult := Hitret(20086)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Local Ojii-san", "NP360004")
	Mess("Ooh, thank you, Yahiro-chan.")
	hitretResult := Hitret(20087)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_04M", 0, 0)
	Talk("Yahiro", "YH020029")
	Mess("Thank you for your hard work as always... And forÅ^looking over this one too.")
	hitretResult := Hitret(20088)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_12M", 5, 0)
	SetBustup("CG01_04M", 7, 0)
	Talk("Akira", "AK020480")
	Mess("Auu!")
	hitretResult := Hitret(20089)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_14M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Yahiro-san grabbed Amatsume's head and pulled it towardsÅ^herself.")
	hitretResult := Hitret(20090)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Local Old Lady", "NP370003")
	Mess("By no means, since you, Yahiro-chan, took responsibilityÅ^for her, Akira-chan can stay here from now on too.")
	hitretResult := Hitret(20091)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Local Ojii-san", "NP360005")
	Mess("You two are similar to my grandchilds. No matter how oldÅ^they are, they are still adorable.")
	hitretResult := Hitret(20092)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_03M", 0, 0)
	Talk("Yahiro", "YH020030")
	Mess("No way, I'm not young enough to be a grandson. Akira,Å^open appropriately wrappings with tea cakes from theÅ^store.")
	hitretResult := Hitret(20093)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020481")
	Mess("Okaay!")
	hitretResult := Hitret(20094)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CG01_04M", 0, 0)
	Talk("Local Ojii-san", "NP360006")
	Mess("Even though that child's relatives have passed away, sheÅ^grew up so bright and honest. All thanks to Hiro-chan.")
	hitretResult := Hitret(20095)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020031")
	Mess("I just adopted her midway, it's really all thanks to theÅ^eldery that took care of her.")
	hitretResult := Hitret(20096)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Local Ojii-san", "NP360007")
	Mess("No, no, when that kid had hard times, I couldn't doÅ^anything.")
	hitretResult := Hitret(20097)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020032")
	Mess("I do it only from time to time too. I was unemployed,Å^with a lot of free time on my hands, and grandma thoughÅ^of her as adorable, so...")
	hitretResult := Hitret(20098)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020482")
	Mess("Here, grandpa. Crackers and doughnuts with red beanÅ^paste. Each one is tasty, so I recommend them!")
	hitretResult := Hitret(20099)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Local Old Lady", "NP370004")
	Mess("Thank you, Hiro-chan, Akira-chan.")
	hitretResult := Hitret(20100)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Local Ojii-san", "NP360008")
	Mess("Whew, I'll have a rest here for a while.")
	hitretResult := Hitret(20101)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	Talk("êSÇÃê∫", "")
	Mess("Eldery has crowded around Amatsume and started a break.")
	hitretResult := Hitret(20102)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume, at the time she played baseball together withÅ^the kids, and while being together with us, has alwaysÅ^been in the center of attention.")
	hitretResult := Hitret(20103)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With just that, she's loved by everyone, huh...")
	hitretResult := Hitret(20104)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020033")
	Mess("That fellow, with her good sociability, she could liveÅ^anywhere, huh.")
	hitretResult := Hitret(20105)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But, if she had nobody to take care of her, you'd worryÅ^about her, right?")
	hitretResult := Hitret(20106)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020034")
	Mess("Tch, shaddup...")
	hitretResult := Hitret(20107)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020035")
	Mess("That idiot, even if she was driven out of here, sheÅ^would still laugh dubiously, it's frightening...")
	hitretResult := Hitret(20108)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("Eh......")
	hitretResult := Hitret(20109)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM16", 0)
	Talk("Yahiro", "YH020036")
	Mess("When her grandfather died, each and every one treatedÅ^Akira as a burden. They refused to take her in and wereÅ^pushing her one onto another.")
	hitretResult := Hitret(20110)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020037")
	Mess("Even so, she... Even though discussions about her ownÅ^fate were fluttering about above her head, she just keptÅ^smiling...")
	hitretResult := Hitret(20111)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020038")
	Mess("She always did it... One would think \"Because she's aÅ^kid, she didn't understand it\", but she wasn't likeÅ^that...")
	hitretResult := Hitret(20112)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020039")
	Mess("At that age, she comprehended it... That she couldn't doÅ^anything herself...")
	hitretResult := Hitret(20113)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH020040")
	Mess("...To tell the truth, I couldn't watch... at either ofÅ^them.")
	hitretResult := Hitret(20114)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_07M", 0, 0)
	Talk("Yahiro", "YH020041")
	Mess("I was unemployed and didn't have the will to work, soÅ^after receiving grandma's store, I though about livingÅ^on noncommitially, I though that just one person moreÅ^wouldn't do much of a difference and you see...")
	hitretResult := Hitret(20115)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You like Amatsume too, don't you, Yahiro-san?")
	hitretResult := Hitret(20116)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	ScPlayBgm("BGM13", 0)
	SetBustup("CG01_05M", 0, 0)
	Shake(8)
	Talk("Yahiro", "YH020042")
	Mess("Id- Idiot! Don't you ever say it again! Such embarrasingÅ^words!")
	hitretResult := Hitret(20117)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020043")
	Mess("If I drove her out, grandma's spirit might get out ofÅ^her grave to haunt me, so that's why!!")
	hitretResult := Hitret(20118)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020483")
	Mess("Hey, hey, what happened, Hiro-nee-chan?")
	hitretResult := Hitret(20119)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_08M", 0, 0)
	Talk("Yahiro", "YH020044")
	Mess("Shaddup! I'll give you some ice cream, so go away!!")
	hitretResult := Hitret(20120)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020484")
	Mess("Thank you! Haru-kun, shall we split it in halves?")
	hitretResult := Hitret(20121)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_06M", 0, 0)
	Talk("Yahiro", "YH020045")
	Mess("Argh, geez... You, take the one you like too!")
	hitretResult := Hitret(20122)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Is it okay? Thank you.")
	hitretResult := Hitret(20123)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_04M", 0, 0)
	Talk("Akira", "AK020485")
	Mess("Yaaay! Ice cream, ice cream!")
	hitretResult := Hitret(20124)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	Talk("êSÇÃê∫", "")
	Mess("Amatsume, looking all happy, picked out ice cream fromÅ^the refrigator. Well then, which one should I have...?")
	hitretResult := Hitret(20125)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH020046")
	Mess("You pay 100 yen.")
	hitretResult := Hitret(20126)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh!? Isn't it a treat?")
	hitretResult := Hitret(20127)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020047")
	Mess("Of course. You're a customer after all.")
	hitretResult := Hitret(20128)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("Uuu......")
	hitretResult := Hitret(20129)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(2000, 0)
	SetCg("B15b", 0, 0)
	Transition("WIP_LR", 500)
	ScPlayBgm("BGM07", 0)
	Talk("êSÇÃê∫", "")
	Mess("In between the break, creating shrine ornaments wasÅ^steadily progressing.")
	hitretResult := Hitret(20130)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While I was in dire distress trying to prepare ornamentÅ^for traveler's guardian deity, a large one wasÅ^completed, for decorating the shrine.")
	hitretResult := Hitret(20131)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("As expected, they are fast... If I was making that, itÅ^would take me around a month. Are you making that eachÅ^year?")
	hitretResult := Hitret(20132)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020486")
	Mess("Yeah, every year we make decorations out of rice plantÅ^straws that are left after harvest. On a side note, weÅ^harvested a lot this year.")
	hitretResult := Hitret(20133)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020487")
	Mess("And, on the day of festival we pray \"May we harvest aÅ^lot next year\".")
	hitretResult := Hitret(20134)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heh, at Amatsume's place is a festival to wish for largeÅ^harvest, huh.")
	hitretResult := Hitret(20135)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020488")
	Mess("There are various other wishes too. Everyone conveys aÅ^lot of their troubles or wishes.")
	hitretResult := Hitret(20136)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020489")
	Mess("Haru-kun, if you're worrying about something, maybe tryÅ^to complain to Sayo-hime-sama?")
	hitretResult := Hitret(20137)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eeh!? If I'd complain like that to a god, I would get aÅ^divine retribution.")
	hitretResult := Hitret(20138)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020490")
	Mess("It's all right! A good thing might happen too.")
	hitretResult := Hitret(20139)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020491")
	Mess("Me too, I'm wishing for various things every year, andÅ^I'm fine.")
	hitretResult := Hitret(20140)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y- You say so? If that's the case, maybe I'll tryÅ^wishing for what seems impossible.")
	hitretResult := Hitret(20141)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Local Ojii-san", "NP360009")
	Mess("Akira-chan, We'll carry the shrine ornament to theÅ^storehouse.")
	hitretResult := Hitret(20142)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020492")
	Mess("Okay! Thank you, grandpa. I'll come too!")
	hitretResult := Hitret(20143)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("After putting the completed shrine ornament to theÅ^storehouse, Amatsume and I were cleaning remainingÅ^straws and garbage made in the process.")
	hitretResult := Hitret(20144)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B15b", 0, 0)
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020493")
	Mess("Haru-kun, good work.")
	hitretResult := Hitret(20145)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Amatsume too. Making shrine ornaments was pretty fun.Å^But, won't they get angry, I wonder? At that thingÅ^created by such an amateur like me.")
	hitretResult := Hitret(20146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03M", 0, 0)
	Talk("Akira", "AK020494")
	Mess("It's okay. Festival is about enlivening things up withÅ^everyone after all. I'm happy that I was able to make itÅ^together with you, Haru-kun.")
	hitretResult := Hitret(20147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah...")
	hitretResult := Hitret(20148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Since Amatsume said so, I stopped feeling bad. Though,Å^it was a little embarrassing for shrine to be decoratedÅ^with a thing that I made.")
	hitretResult := Hitret(20149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020495")
	Mess("Well then, shall we go to put decorations on right away?")
	hitretResult := Hitret(20150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Putting them on already? Isn't it too early?")
	hitretResult := Hitret(20151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020496")
	Mess("Not at all, for everyone to realize that there will aÅ^festival soon, let's put on decorations early.")
	hitretResult := Hitret(20152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Huh, indeed. Similar to a poster with an advance notice.")
	hitretResult := Hitret(20153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020497")
	Mess("That's right. Well, shall we go?")
	hitretResult := Hitret(20154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah.")
	hitretResult := Hitret(20155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Change("00_c012b")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

