Include"yosuga.ch"

Function sc00_C025B()
	ScenarioEnter()
	SetCg("B34a", 400, 0)
	Transition("WIP_BT", 500)
	ScPlayEnvSe("SE302", "", 0)
	Talk("Haruka", "")
	Mess("Al-right.")
	hitretResult := Hitret(24050)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I put the string I got from Sora into my pocket. (TL:^Make up your mind author, string or ribbon -_-)")
	hitretResult := Hitret(24051)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("To talk about using it for what, it will be used as a^string for the one I saw yesterday.")
	hitretResult := Hitret(24052)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Back in the past when I lost it, the string seemed to^have broken, leaving only the gem, so I want to find a^string to replace it.")
	hitretResult := Hitret(24053)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021531")
	Mess("Ha-ru-kunnnnn!!")
	hitretResult := Hitret(24054)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("A, Akira!! Owaaaa!?")
	hitretResult := Hitret(24055)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE302", 0)
	ScPlaySe("SE254", 0)
	ScPlayBgm("BGM06", 0)
	SetCg("EC02b", 0, 0)
	Transition("WIP_LR", 500)
	Talk("Akira", "AK021532")
	Mess("Ah, sorry, sorry! Are you alright?")
	hitretResult := Hitret(24056)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That s-startled me.......")
	hitretResult := Hitret(24057)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021533")
	Mess("I carelessly, forgot to break.")
	hitretResult := Hitret(24058)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's dangerous! I thought I will fall into the fields^when I was avoiding you.")
	hitretResult := Hitret(24059)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Akira did not brake and charged towards me, and I nearly^jumped towards the side.")
	hitretResult := Hitret(24060)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("But after doing a drift, she stops in front of me, that^was really dangerous.")
	hitretResult := Hitret(24061)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Could it be that you injured yourself when you slipped^on the hill yesterday?")
	hitretResult := Hitret(24062)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EC02c", 0, 0)
	Talk("Akira", "AK021534")
	Mess("Nope, because Haru-kun protected me so I'm totally fine.")
	hitretResult := Hitret(24063)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then that's good. Hmm, today you are bustling everywhere^as well.")
	hitretResult := Hitret(24064)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EC02a", 0, 0)
	Talk("Akira", "AK021535")
	Mess("Mmm, everything is about to be done though, after this^I'll be going to Hiro-oneechan's place and deliver the^rice.")
	hitretResult := Hitret(24065)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yahiro-san is still the same, using people this^roughly.......")
	hitretResult := Hitret(24066)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021536")
	Mess("Nope, since it's because I eat half of it.")
	hitretResult := Hitret(24067)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021537")
	Mess("Ah, if Haru-kun's home rice is about to be finished, how^about buying it together?")
	hitretResult := Hitret(24068)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hm, that's true. To be safe, it will be good to buy 10^kilograms.")
	hitretResult := Hitret(24069)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021538")
	Mess("The summer's too hot so we can eat stuff like soumen.^Although it's quite seldom to eat rice, if it's Onigiri^it's surprisingly acceptable to eat it. (TL: Soumen,^thin wheat noodles that are usually served cold)")
	hitretResult := Hitret(24070)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Next is curry.")
	hitretResult := Hitret(24071)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EC02c", 0, 0)
	Talk("Akira", "AK021539")
	Mess("Fufuu, indeed.")
	hitretResult := Hitret(24072)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You can make it the next time, it's really delicious.")
	hitretResult := Hitret(24073)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021540")
	Mess("Okay, I'll receive some curry powder during the Bon^Lantern festival, I'll make it using that the next time.")
	hitretResult := Hitret(24074)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks, I'll be waiting for that. Well, let's go.")
	hitretResult := Hitret(24075)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021541")
	Mess("Yeah!")
	hitretResult := Hitret(24076)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B27a", 0, 0)
	Transition("WIP_MOZV", 500)
	Talk("S‚Ìº", "")
	Mess("Akira and I went to the usual farmer's place, and as^usual bought high grade rice at a cheap price.")
	hitretResult := Hitret(24077)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("2 people with 20 kilograms. I secure the rice onto the^bicycle, and I push it towards our home.")
	hitretResult := Hitret(24078)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Akira carries the freebie bag containing pumpkins and^eggplants, happily walking beside me.")
	hitretResult := Hitret(24079)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B01a", 0, 0)
	SetBustup("CC06_02M", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Akira", "AK021542")
	Mess("Are you sure? Letting me get all the freebies.")
	hitretResult := Hitret(24080)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I have never cooked before using pumpkin and eggplants,^I'll leave it to you who's more skillful.")
	hitretResult := Hitret(24081)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK021543")
	Mess("Is that so? Well I'll cook the pumpkin and I'll marinade^the eggplant and give it to Haru-kun.")
	hitretResult := Hitret(24082)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks. I'll take a look and learn from you when you do^it. It might be possible for me to make it the next^time.")
	hitretResult := Hitret(24083)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021544")
	Mess("Marinating is easy. Place the eggplant and marinating^souce together into the plastic bag, rub it a little and^it will be done.")
	hitretResult := Hitret(24084)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I always receive Takano-ojisan's pickled vegetables, so^I look forward to making it.")
	hitretResult := Hitret(24085)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021545")
	Mess("Even though learning things is a good thing, it is^better for boys to wait excitedly for it")
	hitretResult := Hitret(24086)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then, I'll take my and put my portion down first.")
	hitretResult := Hitret(24087)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK021546")
	Mess("Ok! Let me help.")
	hitretResult := Hitret(24088)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heave-ho! Guuu...... 10 kilograms is heavier than what I^imagined...")
	hitretResult := Hitret(24089)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I staggered towards the front door. After Akira opens^the door, I leave it temporarily at the hallway's^corner.")
	hitretResult := Hitret(24090)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, next will be Yahiro-san's place. Let's go.")
	hitretResult := Hitret(24091)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021547")
	Mess("Thanks, that's really a great help.")
	hitretResult := Hitret(24092)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I once again push the bicycle along, heading towards^Yahiro-san's home.")
	hitretResult := Hitret(24093)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("", 0)
	SetCg("B08a", 0, 0)
	Talk("Haruka", "")
	Mess("Excuse me for intruding~ (TL: Uhoh, bedroom intruder^detected.)")
	hitretResult := Hitret(24094)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK021548")
	Mess("I'll make some tea. Then I'll get some fruits.")
	hitretResult := Hitret(24095)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks.")
	hitretResult := Hitret(24096)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	ScPlayBgm("BGM03", 0)
	Talk("S‚Ìº", "")
	Mess("We delivered the rice to Yahiro-san's home, Akira said^she wants to make me some tea, so I said take advantage^of it and intrude.")
	hitretResult := Hitret(24097)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Akira's room is different from mine, and it's a wonder^that without an air-conditioner it still feels cooling^and comfortable.")
	hitretResult := Hitret(24098)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("During the waiting time, the bored me looked around^restlessly to find a place to sit down.")
	hitretResult := Hitret(24099)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehhh......")
	hitretResult := Hitret(24100)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("On top of Akira's table, is the gemstone that was picked^up yesterday.")
	hitretResult := Hitret(24101)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The gemstone has been wiped clean of dirt and looks^really pretty, but as expected it has not been placed^into a string yet.")
	hitretResult := Hitret(24102)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("This is just right. I can try it later with the string^that I brought.")
	hitretResult := Hitret(24103)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But come to think of it, there isn't a lot of things in^Akira's room.")
	hitretResult := Hitret(24104)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Besides the dictionary and textbooks, there are no game^machines, no manga and no magazines. Personal computers,^mobile phones and no television too.")
	hitretResult := Hitret(24105)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("It can't be possibly be like the people from the past^times, playing with origami and beanbags...... Is there^a room that places all the other things that I haven't^been to?")
	hitretResult := Hitret(24106)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("How does she passes her time alone here.......")
	hitretResult := Hitret(24107)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Looking from her class attitude, I don't really^think...... she very keen in her studies.")
	hitretResult := Hitret(24108)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("..... Hm? But, is she doing her summer's homework...?")
	hitretResult := Hitret(24109)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("On top of the desk where the tatami chairs are, was an^old opened notebook.")
	hitretResult := Hitret(24110)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("It's about time to finish doing the homework...... It's^good to get Ryouhei too and everyone finishing it^together.")
	hitretResult := Hitret(24111)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Even though it's not good to peak at it, but I am^interested in what homework she was doing, and take a^glimpse at the notebook.")
	hitretResult := Hitret(24112)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("ccccccH")
	hitretResult := Hitret(24113)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The notebook contains letters that are skillfully^written, and I think they are not written by Akira.^There are written dates, and it looks more like a diary.")
	hitretResult := Hitret(24114)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What is written inside here......")
	hitretResult := Hitret(24115)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060001")
	Mess("wI wanted to tell the adults. The children did not have^any sins and they are innocent. I really hope that they^will not do any things that will make them unhappy.x")
	hitretResult := Hitret(24116)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm? What is this......")
	hitretResult := Hitret(24117)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM08", 0)
	Talk("S‚Ìº", "")
	Mess("I turn over to the notebook's cover. Written on the^cover is Kasugano's medical hospital's logbook.")
	hitretResult := Hitret(24118)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!! This notebook...... is Grandfather's^hospital's....... (TL: Grandfather = Oji-san)")
	hitretResult := Hitret(24119)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Why does Akira have a diary from the hospital, I don't^understand.")
	hitretResult := Hitret(24120)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021549")
	Mess("Haru-kunnn!!")
	hitretResult := Hitret(24121)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("II")
	hitretResult := Hitret(24122)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I turn my head in a hurry.")
	hitretResult := Hitret(24123)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021550")
	Mess("Sorry-, I don't know where I put the can opener, I'm^searching for it-. Just wait a little longer-.")
	hitretResult := Hitret(24124)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, s, sure! Even if you take your time it's okay!!")
	hitretResult := Hitret(24125)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("My heart skips a beat and I feel I can't read it any^longer, and place the noteback onto the desk.")
	hitretResult := Hitret(24126)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("But, I'm really bothered by the contents.....")
	hitretResult := Hitret(24127)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I should wait for Akira to come back and ask her^precisely about the details, but I am too curious about^it.")
	hitretResult := Hitret(24128)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Akira has not come back. Just a little...! Yes, just a^bit, and I continue to flip the book.")
	hitretResult := Hitret(24129)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The place where I stop at had \"August 3rd\" written in^it.")
	hitretResult := Hitret(24130)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060002")
	Mess("wAugust 3rd: There was an emergency case in the middle^of the night. Mrs Migiwa's water broke early and had^labor pains before the anticipated date.")
	hitretResult := Hitret(24131)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060003")
	Mess("Even though it's safer at a fully equipped hospital,^there is danger during transportation, so I decided to^deal with this case.")
	hitretResult := Hitret(24132)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("This is written by...... Grandmother.")
	hitretResult := Hitret(24133)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Grandmother is like Grandfather's assistant nurse, her^job is also a birth attendent, and I heard Sora and I^were also delivered by Grandmother.")
	hitretResult := Hitret(24134)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Grandmother was also present when Migiwa-san was born.")
	hitretResult := Hitret(24135)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B01d_", 0, 0)
	Transition("CLOUD_A", 1000)
	Talk("Journals—I‚Ì‘c•êt", "NP060004")
	Mess("wI asked the neighbour hor help to boil water. Mrs^Migiwa was very cal, and was concerned when Mr Migiwa is^able to come from work.")
	hitretResult := Hitret(24136)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060005")
	Mess("wThe initial stage of the birth took a long time. After^the water breaks, the contraction will be difficult to^bear, but she did not pass out. Her enduring and firm^countenance made others feel that she displayed the^strength of a mother.")
	hitretResult := Hitret(24137)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060006")
	Mess("wI said words of encouragement and wiped away her^perspiration, holding her hands tightly, but there was^nothing else I could have done.x")
	hitretResult := Hitret(24138)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060007")
	Mess("wAt that time, there was a commotion at the entrance. I^thought it was Mr Migiwa, but it was actually another^emergency patient that was sent here.x")
	hitretResult := Hitret(24139)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060008")
	Mess("wThe patient that was sent in was also a pregnant lady^just like Mrs Migiwa. I had the impression that it was a^beautiful lady that wasn't near this area.x")
	hitretResult := Hitret(24140)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060009")
	Mess("wThis pregnant lady also started to have contractions,^and she was in the late stages of giving birth. Later on^I heard that she was found on the road and seemed to be^in great pain.x")
	hitretResult := Hitret(24141)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060010")
	Mess("wI don't know why she went out in the middle of the^night under such a late stage of pregnancy, but she was^also in a dangerous state.x")
	hitretResult := Hitret(24142)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060011")
	Mess("wThe futon was laid out in the living room, and we^moved them there for observation. Up until now, I have^never took care of two pregnant women at the same time,^but I hoped they gave birth safely.")
	hitretResult := Hitret(24143)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060012")
	Mess("wAnd then, 30 minutes before the next morning came,^there was the sound of the baby's first cry. The lady^who was sent in later gave birth safely to a baby^girl.x")
	hitretResult := Hitret(24144)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060013")
	Mess("wAnd the baby's first cry seemed to be a stimulation,^soon after that, Mrs Migiwa also gave birth to a cute^girl.x")
	hitretResult := Hitret(24145)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060014")
	Mess("wThis child was very docile, and even made us suspect^whether her trachea was in danger of being blocked, but^she soon made sleeping sounds.x")
	hitretResult := Hitret(24146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Their births are on the same day, so it means the first^one born was Akira? And that after that is^Migiwa-san......")
	hitretResult := Hitret(24147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The lady that she has never seen is Akira's mother?")
	hitretResult := Hitret(24148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I felt the same way as Grandmother did, finding it^strange to walk in the middle of the night.")
	hitretResult := Hitret(24149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060015")
	Mess("wAfter the childbirths, the mothers finally got to meet^with their children, but as they had exhausted their^strength, they lost consciousness and finally got a^chance to rest.x")
	hitretResult := Hitret(24150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060016")
	Mess("wIn order to have a checkup on the babies, I^temporarily placed them in another room. Even though I^wanted to leave them beside their mothers, there were^many things that needed to be done.x")
	hitretResult := Hitret(24151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Journals—I‚Ì‘c•êt", "NP060017")
	Mess("wMrs Migiwa seemed to have a cold before she was sent^here, in order to prevent the child from getting^infected, and it is unfortunate that she was unable to^temporarily meet with the child.x")
	hitretResult := Hitret(24152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060018")
	Mess("wAt this time, Mr Migiwa arrived, but after he heard^the children's births his face turned glum. When I asked^him for the reason, he said that the things that^happened here was to be kept a secret.x")
	hitretResult := Hitret(24153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM11", 0)
	SetCg("B09c_", 0, 0)
	Transition("CLOUD_A", 1000)
	Talk("Journals—I‚Ì‘c•êt", "NP060019")
	Mess("wThe lady that was sent here was originally working at^Mr Migiwa's office. Even though Mr Migiwa had a wife, he^had a shameful affair with her for a short while.x")
	hitretResult := Hitret(24154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060020")
	Mess("wThen one day she suddenly quit, and after informing^that she was pregnant, she went missing for a while. It^seems that Yama-san said he suddenly saw her coming back^here.x")
	hitretResult := Hitret(24155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060021")
	Mess("wThe ironic thing is, Mr Migiwa got both his wife and^lover pregnant at the same time. Migiwa-san did not know^what motive that lady has, but he hopes to keep it a^secret from his wife and bring things to a close.x")
	hitretResult := Hitret(24156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060022")
	Mess("wMr Migiwa was preparing to walk on the political stage^at that time. He could not let his enemy find out about^this, right when he was about to reach the critical^juncture. He could not lost everything due to this,^otherwise his wife and child will end up on the^streets.x")
	hitretResult := Hitret(24157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060023")
	Mess("wI requested Mr Migiwa to let the mothers rest before^talking to them. He also bowed his head to plead with me^not to let his wife and lover meet.x")
	hitretResult := Hitret(24158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060024")
	Mess("wCompared to my uneasiness, the lady in the midst of^this problem treated as if nothing has happened to look^after her child, gently embracing and cuddling the^baby.x")
	hitretResult := Hitret(24159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060025")
	Mess("wMigiwa-san's child, completely didn't have the^meekness when she was just born. She was very energetic^and even broke her mother's necklace.x")
	hitretResult := Hitret(24160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060026")
	Mess("wI wanted to tell the adults. The children did not have^any sins and they are innocent. I really hope that they^will not do any things that will make them unhappy.x")
	hitretResult := Hitret(24161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh......")
	hitretResult := Hitret(24162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I read that portion once again.")
	hitretResult := Hitret(24163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("gShe was very energetic, and broke her mother's^necklace.h")
	hitretResult := Hitret(24164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("S‚Ìº", "")
	Mess("Her mother's necklace..... Akira also talked about this^matter right!?")
	hitretResult := Hitret(24165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B07a_", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Haruka", "")
	Mess("Heyyy..... Did you make this necklace yourself?")
	hitretResult := Hitret(24166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021551")
	Mess("Eh, this? Yeah...... When I was born, I broke it my^mother's who was wearing it.......")
	hitretResult := Hitret(24167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B08a", 0, 0)
	Transition("WIP_MOZV", 500)
	Talk("S‚Ìº", "")
	Mess("At that time, Akira said she broke her mother's^necklace.")
	hitretResult := Hitret(24168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......But ...... if it's like that, doesn't it^contradict what is written here......")
	hitretResult := Hitret(24169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Did Grandmother write wrongly? Mistaking things about^Akira's mother for Migiwa-san's mother...")
	hitretResult := Hitret(24170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I turn over the diary's page.")
	hitretResult := Hitret(24171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B01d_", 0, 0)
	Transition("CLOUD_A", 1000)
	Talk("Journals—I‚Ì‘c•êt", "NP060027")
	Mess("wA few days later, Mrs Migiwa's condition worsened. It^was good to check have the child have a complete^checkup. So I wrote a letter of introduction to a big^hospital to request them to check up.x")
	hitretResult := Hitret(24172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060028")
	Mess("wThe ambulance came. Mrs Migiwa was simply laid onto a^stretcher and moved into the ambulance. At that moment^the other lady also took her child and entered as^well.x")
	hitretResult := Hitret(24173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060029")
	Mess("wShe also said that her condition is worsening, and^forcibly went together on the same ambulance.x")
	hitretResult := Hitret(24174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060030")
	Mess("wThe clinic suddenly turned quiet. The endearing sounds^of the children voices were gone. My husband and I^thought back on the past busy days, and felt that we^really were powerless.x")
	hitretResult := Hitret(24175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060031")
	Mess("wWe only hope that Mr Migiwa can bring up the children^well into adults.x")
	hitretResult := Hitret(24176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060032")
	Mess("wWhen we almost done with the cleaning, one of the^helpers came to me and reported a tag has been picked^up.x")
	hitretResult := Hitret(24177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060033")
	Mess("wIt was a name tag placed on the wrist when the^children are born. It was passed to me when it fell.x")
	hitretResult := Hitret(24178)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("cccccccccccccc")
	hitretResult := Hitret(24179)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("Journals—I‚Ì‘c•êt", "NP060034")
	Mess("wWhen I took it, my whole body had goosebumps.x")
	hitretResult := Hitret(24180)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060035")
	Mess("wBecause the tags are made of paper, so it's easy to^break them. Was it due to the transporting that broke^it, or because of the mischievious children...... The^tag had Migiwa written on it.x")
	hitretResult := Hitret(24181)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060036")
	Mess("wI recall the scene when I sent Mrs Migiwa off. First^of all it was sending off the ill Mrs Migiwa, and then^that lady carred her own child aboard the vehicle.x")
	hitretResult := Hitret(24182)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060037")
	Mess("wAnd finally the ambulance staff carried Mrs Migiwa's^child up the vehicle. Could the tag have dropped during^that moment?x")
	hitretResult := Hitret(24183)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060038")
	Mess("wJust in case, I contacted the nurse-in-charge that I^know from the hospital. From her words \"Both of them\"^dropped the tags, and they were placed back up.x")
	hitretResult := Hitret(24184)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060039")
	Mess("wShe laughed and reassured me, the mothers did not put^down their child until they reached the hospital, so it^was impossible to be mistaken and I was overthinking.x")
	hitretResult := Hitret(24185)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B08a", 0, 0)
	Transition("WIP_MOZV", 500)
	Talk("Haruka", "")
	Mess("ccccccccccc")
	hitretResult := Hitret(24186)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060040")
	Mess("wAfter a few days later. This time the nurse contacted^me. She said the lady that came in together with^Migiwa-san had left the baby in the hospital and was^gone.x")
	hitretResult := Hitret(24187)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060041")
	Mess("wAs a father, Mr Migiwa tried tried to discuss with Mrs^Migiwa to place the baby under his care until the mother^is found, but she strongly objected to it.x")
	hitretResult := Hitret(24188)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060042")
	Mess("wIt seems that during the period when Mrs Migiwa was^sent to the hospital, the secrets about the children^were exposed. If that lady was aiming for that moment,^she's really a frightening woman.x")
	hitretResult := Hitret(24189)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060043")
	Mess("wThe husband's infidelity cannot be forgiven, but Mrs^Migiwa also understands her husband is at a critical^period, and she exercised patience in consideration for^her child. In exchange for for compromising, she laid^down some conditions.x")
	hitretResult := Hitret(24190)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060044")
	Mess("wShe wants that child to never appear in front of her^eyes. Also, even if they are tied by blood, he cannot^provide anything at all to help...... She expressed a^strong rejection.x")
	hitretResult := Hitret(24191)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Journals—I‚Ì‘c•êt", "NP060045")
	Mess("wSince her wife made such a compromise, Mr Migiwa was^unable to oppose it, and gave up the idea of adopting,^and the child was sent to an orphan's home.x")
	hitretResult := Hitret(24192)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM16", 0)
	Talk("Journals—I‚Ì‘c•êt", "NP060046")
	Mess("wThe child has no crime. I look at the growing child^without a mother in front of me , ashamed of being^powerless. At least if she's somewhere near, I can lend^some aid.x")
	hitretResult := Hitret(24193)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060047")
	Mess("wThe future of the child is decided because of the^adults actions on that day. An unexpected person came to^us.x")
	hitretResult := Hitret(24194)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B29b_", 0, 0)
	Transition("CLOUD_A", 1000)
	Talk("Journals—I‚Ì‘c•êt", "NP060048")
	Mess("wMy husband's friend in the shrine, Amatsume-san, who's^also an advisor to Migiwa-san, brought up a request to^adopt the child.x")
	hitretResult := Hitret(24195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060049")
	Mess("wWe were surprised with the sudden request, but he said^he felt lonely as he had no grandchildren, and since he^received assistance from Migiwa-san from the past, he^wished to lend a hand.x")
	hitretResult := Hitret(24196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060050")
	Mess("wAfter Amatsume-san's wife had passed away, he's now^living alone. His son is married but he has no children^yet.x")
	hitretResult := Hitret(24197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060051")
	Mess("wHe has the experience in raising children, and his^income is also stable and should not have too much of a^problem, but the only concern is he is the same as in^terms of old age.x")
	hitretResult := Hitret(24198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060052")
	Mess("wMister Amatsume and Mister Migiwa talked to each other^several times, and made a promise not to bother each^other, and thus the motherless child became Mister^Amatsume's adopted child.x")
	hitretResult := Hitret(24199)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B07b_", 0, 0)
	Talk("Journals—I‚Ì‘c•êt", "NP060053")
	Mess("wAfter observing in the hospital for a period, Mister^Amatsume bought milk bottles and diapers once again^after decades, and took the child away.x")
	hitretResult := Hitret(24200)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060054")
	Mess("wAt that time, I heard a name was decided for the^child. \"Akira\". He thought it was a boy when he saw the^child for the first time, but since he like the sound of^it he changed the kanji to a girl's.x")
	hitretResult := Hitret(24201)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060055")
	Mess("wThe two child living in the same town, didn't make Mrs^Akira very happy, but she was originally a sympathetic^and kind person, so she didn't say anything.x")
	hitretResult := Hitret(24202)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060056")
	Mess("wI prayed sincerely in my heart, that the uneasy things^that I felt wouldn't become true when the children grow^up. My only worry that's left, is the issue where I did^not carefully look after the children, when Mrs Migiwa^was sent to the hospital.x")
	hitretResult := Hitret(24203)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060057")
	Mess("wThe children really takes after Mr Migiwa. The girls^resemble each other so much that I make mistakes^sometime.x")
	hitretResult := Hitret(24204)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B08a", 0, 0)
	Transition("WIP_MOZV", 500)
	Talk("Haruka", "")
	Mess("cccccccccccc (TL: About the kanji stated^earlier on, my guess is male \"Akira kanji\" are different^from female \"Akira kanji\")")
	hitretResult := Hitret(24205)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I slowly put the notebook back onto the table. The page^that Akira turned to had creases on it, as though she^had read it many times.")
	hitretResult := Hitret(24206)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("And it was written right that.")
	hitretResult := Hitret(24207)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060058")
	Mess("wMigiwa-san's child, completely didn't have the^meekness when she was just born. She was very energetic^and even broke her mother's necklace.x")
	hitretResult := Hitret(24208)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I look at the necklace's gem on the table.")
	hitretResult := Hitret(24209)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Is this the thing that was pulled down from Migiwa-san's^mother when she was young?")
	hitretResult := Hitret(24210)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("But, Akira said it before. That it was her that broke it^when she was young.")
	hitretResult := Hitret(24211)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("If this really belongs to Migiwa-san's mother, then it^would be strange for anyone else other than Migiwa-san^to have it. It couldn't have been Akira taking care of^it.")
	hitretResult := Hitret(24212)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... What is going on?")
	hitretResult := Hitret(24213)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Grandmother had felt uneasy...... Did Akira's mother do^something.")
	hitretResult := Hitret(24214)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060059")
	Mess("To be safe, I contacted the nurse that I came to know^when I saw her off. From what I heard from her \"they^both\" dropped the tags, and has been put on once again.")
	hitretResult := Hitret(24215)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060060")
	Mess("She smiled at me and reassured me, the mother did not^put down the child until they reached the hospital, so^it was impossible to make a mistake and I worried too^much.")
	hitretResult := Hitret(24216)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("But, the latter half of the journal was written on this^way.")
	hitretResult := Hitret(24217)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060061")
	Mess("I prayed from my heart, the uneasy things that I felt^will not come true when the children grow up. The worry^that I had, was from the part where I did not carefully^look after the children, only seeing Migiwa-san and the^rest off to the hospital.")
	hitretResult := Hitret(24218)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Journals—I‚Ì‘c•êt", "NP060062")
	Mess("The children resemble Mr Migiwa greatly. They are so^alike that I sometimes will be mistaken.")
	hitretResult := Hitret(24219)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("...... What... did Akira's mother do?")
	hitretResult := Hitret(24220)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("She gave birth to Akira, divulged Migiwa's san adultery,^didn't take any compensation and left. What was her^goal?")
	hitretResult := Hitret(24221)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Is her motive only to squeeze the child into this^family, and let the family grow apart?")
	hitretResult := Hitret(24222)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Is it really only something like this......?")
	hitretResult := Hitret(24223)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("In that instant, in the corner of my mind arise the^worst way to take revenge.")
	hitretResult := Hitret(24224)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!..... This...... is impossible......")
	hitretResult := Hitret(24225)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("If it's not just a coincidence the tags didn't fall off,^then who did it on purpose? What's the motive?")
	hitretResult := Hitret(24226)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("If the tags drop off, only the one who is observing will^know whose child belongs to the mother.")
	hitretResult := Hitret(24227)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The hospital personnel are not there during the birth.^If there are no tags, you can only believe what the^mother says.")
	hitretResult := Hitret(24228)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... The children has been exchanged.")
	hitretResult := Hitret(24229)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Her own child brought to Migiwa's family, even if she^doesn't care, her child will be brought up under loving^care.")
	hitretResult := Hitret(24230)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("At the same time, the motherless child, if there's no^one to adopt her, she will be sent to the orphange.")
	hitretResult := Hitret(24231)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("If they were brought together to the hospital and the^affair was revealed, that child will not be adopted.")
	hitretResult := Hitret(24232)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Is this calculated? Is this revenge? I shudder. To^easily cause mayhem to other people's life, was there no^hesitation in it?")
	hitretResult := Hitret(24233)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("And according to Grandmother's journal, Akira is^carrying the pendant torn from her mother.")
	hitretResult := Hitret(24234)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("S‚Ìº", "")
	Mess("Then.......")
	hitretResult := Hitret(24235)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Migiwa's family realy child is...... Akira.")
	hitretResult := Hitret(24236)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021552")
	Mess("Haru-kun.")
	hitretResult := Hitret(24237)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("!?")
	hitretResult := Hitret(24238)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_13M", 0, 0)
	Talk("Akira", "AK021553")
	Mess(".......Haru-kun......... What are you looking at?")
	hitretResult := Hitret(24239)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se018", 0)
	Talk("S‚Ìº", "")
	Mess("As a result of being surprised, I drop the notebook. The^notebook knocks against something on the table and send^it flying.")
	hitretResult := Hitret(24240)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se062", 0)
	Talk("S‚Ìº", "")
	Mess("The jewel from the the table fell onto the floor and^rolled next to Akira's feet.")
	hitretResult := Hitret(24241)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("S-Sorry.....")
	hitretResult := Hitret(24242)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I go over and pick up the scattered things in a hurry.")
	hitretResult := Hitret(24243)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("During the moment when I turn my head around, I look at^Akira's expression and started to feel scared. I avert^my eyes away and pick up the things in an exaggerated^manner.")
	hitretResult := Hitret(24244)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Here..... I'm sorry for dropping them, especially when^it's so important.")
	hitretResult := Hitret(24245)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK021554")
	Mess("Thank you, Haru-kun.")
	hitretResult := Hitret(24246)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh...... Sure.......")
	hitretResult := Hitret(24247)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I raise my head up, and it is the usual Akira. Her eyes^did not have that piercing gaze earlier, and it is the^usual faint smile that appear.")
	hitretResult := Hitret(24248)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM07", 0)
	Talk("Akira", "AK021555")
	Mess("I'm sorry to keep you waiting. I actually wanted to make^the white cream treacle.. hmm? There's something wrong.^Mmm, Black treacle white cream Anmitsu? I made that, but^I took a long time to find the can. (Anmitsu: A type of^Japanese dessert.")
	hitretResult := Hitret(24249)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Wow, it looks delicious. There are lots of ingredients^inside.")
	hitretResult := Hitret(24250)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK021556")
	Mess("Right? Here, sit sit. Let's eat, let's eat!")
	hitretResult := Hitret(24251)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay..... Itadakimasu.")
	hitretResult := Hitret(24252)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The transparent glassware is filled with white Dango and^Anmitsu, and on the top there are treacle and fresh^cream. (TL: Dango = Jap. rice flour dumpling)")
	hitretResult := Hitret(24253)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I took a bite using the spoon, and the sweetness spread^throughout my mouth. My cheeks involuntarily slack, and^even my mind felt like it could melt altogether.")
	hitretResult := Hitret(24254)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03M", 0, 0)
	Talk("Akira", "AK021557")
	Mess("So good-!! It's delicious right? They showed this on the^television once. I did really well, even though it's my^first try!")
	hitretResult := Hitret(24255)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahaha, this is really well done. It's really sweet and^quite well suited for girls. How about doing it next^time for everyone?")
	hitretResult := Hitret(24256)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_14M", 0, 0)
	Talk("Akira", "AK021558")
	Mess("Yeah! I bet this can be sold too` Hamuu hamuu, so^good-!!")
	hitretResult := Hitret(24257)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Akira put a lot of cream on top of the Dango, and sends^it to her mouth, smiling from ear to ear.")
	hitretResult := Hitret(24258)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_05M", 0, 0)
	Talk("Akira", "AK021559")
	Mess("Ah, this is bad. I forgot about the tea! I poured out^the barley tea, but I didn't carry it over --!")
	hitretResult := Hitret(24259)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("S‚Ìº", "")
	Mess("Akira runs back to the kitchen again. I look at side of^the table. The notebook that I peeked sneakily earlier^has already been closed, and I could only see the words^that Grandmother wrote on the cover.")
	hitretResult := Hitret(24260)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("What did Akira think after reading that.")
	hitretResult := Hitret(24261)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("...... Will she think of the same things that I did?")
	hitretResult := Hitret(24262)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021560")
	Mess("That notebook, is given to me before Haru-kun's^grandmother passed away.")
	hitretResult := Hitret(24263)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("She placed the iced barley tea in front of me.")
	hitretResult := Hitret(24264)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK021561")
	Mess("She said, this is a possible clue to find my mother.")
	hitretResult := Hitret(24265)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Is that so...... My grandmother.")
	hitretResult := Hitret(24266)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021562")
	Mess("My home's grandfather, Hiro-neechan's grandmother,^Haru-kun's grandparents...... even though they are gone,^but everyone still loved me very much.")
	hitretResult := Hitret(24267)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021563")
	Mess("Therefore, I think it's fine right now. I'm still being^loved, as a grandfather's granddaughter.")
	hitretResult := Hitret(24268)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then, Then....... All that is written here.........")
	hitretResult := Hitret(24269)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_07M", 0, 0)
	Talk("Akira", "AK021564")
	Mess("No, that's something that cannot be ascertain. They are^only things that are a possibility.")
	hitretResult := Hitret(24270)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021565")
	Mess("Furthermore, my mother wouldn't make a mistake in her^own children.")
	hitretResult := Hitret(24271)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well...... Even though it's put that way.......")
	hitretResult := Hitret(24272)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("As expected, Akira has thought about this. But, she^didn't believe this?")
	hitretResult := Hitret(24273)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021566")
	Mess("There must have been too many coincidences, and the^situation was too chaotic, and led Haru-kun's^grandmother to be confused.")
	hitretResult := Hitret(24274)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That is true, even though it was chaotic at the time^when the two mothers were sent into grandfather's^hospital. the timing when the children were born cannot^be chosen.")
	hitretResult := Hitret(24275)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Therefore, even if there was such a plan, it's probably^useless.")
	hitretResult := Hitret(24276)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Akira's...... Mother...... probably wanted to let the^child get recognized.")
	hitretResult := Hitret(24277)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("No matter what, the father cannot be replaced.")
	hitretResult := Hitret(24278)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("But, even if it's like that, there's still something^that cannot be comprehended.")
	hitretResult := Hitret(24279)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But, if that's the case, why is the necklace that's^pulled down here with Akira?")
	hitretResult := Hitret(24280)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	SetBustup("CC06_08M", 0, 0)
	Talk("Akira", "AK021567")
	Mess("I don't know.")
	hitretResult := Hitret(24281)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh......")
	hitretResult := Hitret(24282)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM10", 0)
	SetBustup("CC06_07M", 0, 0)
	Talk("Akira", "AK021568")
	Mess("I heard from Grandfather. When I was born, I carried^this like a very important thing.")
	hitretResult := Hitret(24283)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021569")
	Mess("So, I thought that this is something from my mother who^disappeared.")
	hitretResult := Hitret(24284)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then, show this to Migiwa's mother......")
	hitretResult := Hitret(24285)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_08M", 0, 0)
	Talk("Akira", "AK021570")
	Mess(".......No.")
	hitretResult := Hitret(24286)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Why's that? Even though you can know the truth?")
	hitretResult := Hitret(24287)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021571")
	Mess("Haru-kun..... This cannot do.")
	hitretResult := Hitret(24288)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh......")
	hitretResult := Hitret(24289)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_07M", 0, 0)
	Talk("Akira", "AK021572")
	Mess("I, don't want to know the truth.")
	hitretResult := Hitret(24290)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("W, why?")
	hitretResult := Hitret(24291)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_08M", 0, 0)
	Talk("Akira", "AK021573")
	Mess("You can't..... please..... Haru-kun.")
	hitretResult := Hitret(24292)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("........Akira.........")
	hitretResult := Hitret(24293)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_07M", 0, 0)
	Talk("Akira", "AK021574")
	Mess("Even if the truth is revealed, there won't be anyone who^will become happy.")
	hitretResult := Hitret(24294)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021575")
	Mess("If it only concerns me it will be fine...... but.......^Kazu-chan......")
	hitretResult := Hitret(24295)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Urgh.....")
	hitretResult := Hitret(24296)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("I felt as if my head was being bashed upon.")
	hitretResult := Hitret(24297)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Confirming the truth, will bring Migiwa-san into it as^well..... Akira's truth and Migiwa's truth are the same.")
	hitretResult := Hitret(24298)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021576")
	Mess("Kazu-chan wearing a Miko's dress, sweeping the floor^alone here, listening to the Oji-chan tachi wishes, and^preparing dinner every day for Hiro-oneechan... (TL:^Tachi is used to make plurals to pronouns and nouns^referring to people. Oji-chan tachi = Elderly males)")
	hitretResult := Hitret(24299)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021577")
	Mess("And I will be sitting in a call sent to practice playing^the violin, and Motoka-oneechan will call me Ojou-sama.^Can you imagine that?")
	hitretResult := Hitret(24300)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("cccccccccccc")
	hitretResult := Hitret(24301)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("That is a world that might have originally happened.")
	hitretResult := Hitret(24302)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("If there's a definite answer, then it's possible that^their positions will be reversed.")
	hitretResult := Hitret(24303)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("But, even if the truth is revealed, the positions for^the two are reversed, it will not become what Akira has^imagined.")
	hitretResult := Hitret(24304)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The situation will become more complicated. Everyone^will not be able to accept it easily. If the problem is^exacerbated, it might not end at just hurting everyone^else.")
	hitretResult := Hitret(24305)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Akira has even thought of things to this point. Ever^since Grandmother has passed the notebook to her, she^must have been agonizing over things....... by herself.")
	hitretResult := Hitret(24306)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("The suffering that Akira has faced, must have been much^more that I personally felt. But, even if she says that^it is fine for things to remain as it is...... I am^unable to find any words for that.")
	hitretResult := Hitret(24307)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK021578")
	Mess("Haru-kun, I'm really thankful for your grandmother's who^in heaven, to care so much about me.")
	hitretResult := Hitret(24308)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021579")
	Mess("But, I'm really fine. I can continue the way I am now.^Haru-kun, you don't need to look at me with such a^worried gaze.")
	hitretResult := Hitret(24309)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK021580")
	Mess("Hereee, the white cream molasses Anmitsu? Eat it up? If^it's warm it won't be nice anymore.")
	hitretResult := Hitret(24310)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Did I do something unnecessary? Have I made Akira care^about me instead?")
	hitretResult := Hitret(24311)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Akira seemingly ate the Anmitsu with relish, smiling at^me faintly.")
	hitretResult := Hitret(24312)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("Bringing out the truth into the light, will it ruin the^relation between us?")
	hitretResult := Hitret(24313)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("If I did things to this extent...... What we will get in^return.")
	hitretResult := Hitret(24314)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("But even if that's the case..... is it really fine?")
	hitretResult := Hitret(24315)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("S‚Ìº", "")
	Mess("My thoughts are too shallow..... The me right now,^doesn't enough resolution nor the strength to face this^situation.")
	hitretResult := Hitret(24316)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("", 0)
	Change("00_c026")
	Return
	Change("EXIT_SCENARIO")
EndFunc

