Include"yosuga.ch"

Function sc00_B025()
	ScenarioEnter()
	If (IsRecollectMode())
	Else
		ScPlayBgm("BGM18", 0)
		Talk("Nao", "NO031477")
		Mess("Haru-chan... want to do it?")
		hitretResult := Hitret(15786)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("Yeah...")
		hitretResult := Hitret(15787)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Nao", "NO031478")
		Mess("Ah, sit in your desk Haru-chan. I know something thatÅ^you'd like...")
		hitretResult := Hitret(15788)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("êSÇÃê∫", "")
		Mess("She urged me to sit on my desk. Nao-chan stood in frontÅ^of me as she unbutton my shirt.")
		hitretResult := Hitret(15789)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("Ahh, I can take off my own shirt...")
		hitretResult := Hitret(15790)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Nao", "NO031479")
		Mess("*Shakes her head*... I want to take them off. Hehe, areÅ^you getting embarassed Haru-chan?")
		hitretResult := Hitret(15791)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("Y-Yeah... It somehow feels weird...")
		hitretResult := Hitret(15792)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("êSÇÃê∫", "")
		Mess("The situation where I want to undress a girl myself justÅ^flipped, that pattern made it so awkward.")
		hitretResult := Hitret(15793)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Nao", "NO031480")
		Mess("Haru-chan, you don't have a big belly too, your body isÅ^really nice. It's become more stiff than before.")
		hitretResult := Hitret(15794)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("It was because of your special training, it's quite hardÅ^for me after all.")
		hitretResult := Hitret(15795)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Nao", "NO031481")
		Mess("Eh? Was it that intense?")
		hitretResult := Hitret(15796)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("Just kidding. Thank you, you've done a lot for myÅ^sake...")
		hitretResult := Hitret(15797)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Nao", "NO031482")
		Mess("It's nothing, after all, it's you Haru-chan.")
		hitretResult := Hitret(15798)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("Nao-chan... A-Ah, let me take them off next time.")
		hitretResult := Hitret(15799)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("êSÇÃê∫", "")
		Mess("\"Because it's me.\" She gently stared at me while sayingÅ^those words, I feel like I'm really good which made meÅ^feel a bit shy and my cheeks flushed red.")
		hitretResult := Hitret(15800)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("êSÇÃê∫", "")
		Mess("I quickly tricked her back, my hands quickly went toÅ^unbutton Nao-chan's clothes.")
		hitretResult := Hitret(15801)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Nao", "NO031483")
		Mess("Ahn...")
		hitretResult := Hitret(15802)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("Ah, sorry.")
		hitretResult := Hitret(15803)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Nao", "NO031484")
		Mess("No need to apologize. So you want to feel more? TheÅ^buttons are here... Yes... It's hidden behind this thinÅ^strip of cloth.")
		hitretResult := Hitret(15804)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("Over here right? Okay, here I go...")
		hitretResult := Hitret(15805)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("êSÇÃê∫", "")
		Mess("While I was feeling the tenderness of her breasts, I wasÅ^taking off the button of Nao-chan's clothes.")
		hitretResult := Hitret(15806)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("êSÇÃê∫", "")
		Mess("She's wearing a beautiful padded bra.")
		hitretResult := Hitret(15807)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("Nao-chan, your bra is so cute.")
		hitretResult := Hitret(15808)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Nao", "NO031485")
		Mess("Thanks... Did you know that I've always thought ofÅ^showing them to you Haru-chan?")
		hitretResult := Hitret(15809)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("I-Is that so?")
		hitretResult := Hitret(15810)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("êSÇÃê∫", "")
		Mess("I-Is this the matching underwears guys usually talkÅ^about?")
		hitretResult := Hitret(15811)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Nao", "NO031486")
		Mess("Hehe, just kidding. Today, I'm just really glad that IÅ^wore something cute.")
		hitretResult := Hitret(15812)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Nao", "NO031487")
		Mess("Now then Haru-chan, I'll be taking off your pants.")
		hitretResult := Hitret(15813)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
		Talk("Haruka", "")
		Mess("Ah...")
		hitretResult := Hitret(15814)
		If ((hitretResult == 30) | (hitretResult == 20))
			Change("EXIT_SCENARIO")
			Return
		ElseIf (hitretResult == 40)
			Return
		EndIf
	EndIf
	ScPlayBgm("BGM18", 0)
	Talk("êSÇÃê∫", "")
	Mess("She slowly unfastened my belt as she pull them downÅ^along with my trunks under it.")
	hitretResult := Hitret(15815)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031488")
	Mess("How should I tease you here?... Well...*Kiss*...")
	hitretResult := Hitret(15816)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan still doesn't know what to do with my thingÅ^that's stretch out like a stiff rod, so she kissed itÅ^and licked it from its sides.")
	hitretResult := Hitret(15817)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nghh... Ughh... Aaghh... Nao-chan...")
	hitretResult := Hitret(15818)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt embarrassed as she stared at it for a long time,Å^I feel like it might droop or something, but as sheÅ^licked under it, my thing sprung up.")
	hitretResult := Hitret(15819)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031489")
	Mess("Ahah... It's getting more energetic again... Okay then,Å^I'll make you feel even more better.")
	hitretResult := Hitret(15820)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EB12a", 0, 0)
	Transition("WIP_MOZV", 500)
	Talk("Nao", "NO031490")
	Mess("Nghh... Shoo... Ahh... Haru-chan's hot thing...")
	hitretResult := Hitret(15821)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt surprised as I felt Nao-chan's breasts. As usual,Å^it feels so soft as I felt it, she placed my thing inÅ^between them.")
	hitretResult := Hitret(15822)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031491")
	Mess("Nghh... Ohh... With this, I can do you with myÅ^breasts... nghhh...")
	hitretResult := Hitret(15823)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031492")
	Mess("It's not yet wet, does it hurt? *Suck*... *Kiss*...")
	hitretResult := Hitret(15824)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031493")
	Mess("*Kiss*... nghh... *Kiss*... Aahh... *Suck*...")
	hitretResult := Hitret(15825)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She held my thing in her mouth, then I felt her thickÅ^saliva wrapping it.")
	hitretResult := Hitret(15826)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As she opened her mouth, I saw a string of saliva fromÅ^her tongue tied to my thing.")
	hitretResult := Hitret(15827)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031494")
	Mess("Aheh... *kiss*... I guess this feels good now... Okay,Å^here I go...")
	hitretResult := Hitret(15828)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031495")
	Mess("Nghhh... nghhh... How is it... Does it feel much betterÅ^with my breasts?")
	hitretResult := Hitret(15829)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031496")
	Mess("It's soft, and I'm confident with it... I'll jack youÅ^off with this.")
	hitretResult := Hitret(15830)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My thing slowly towered up perpendicularly, thenÅ^Nao-chan caressed it more with her breasts.")
	hitretResult := Hitret(15831)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt chilly around my thing as she rubbed it with herÅ^sweaty skin, then she continued to tease it as sheÅ^pressed hard against it.")
	hitretResult := Hitret(15832)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031497")
	Mess("Look... nghhh... Haru-chan... nghhhh...")
	hitretResult := Hitret(15833)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031498")
	Mess("Aah... It's becoming more stiffer... nghh... *Kiss*...Å^Does it feel good? Haru-chan?")
	hitretResult := Hitret(15834)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeaah... So soft... It's like you're stroking meÅ^gently...")
	hitretResult := Hitret(15835)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's not like she's being rough, but the sensation I'mÅ^feeling right now is spreading throughout my body, as ifÅ^I want this feeling to go on forever.")
	hitretResult := Hitret(15836)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031499")
	Mess("Nghh... I'll try to be more rough okay. *Kiss*... *Lick*Å^*Lick*...")
	hitretResult := Hitret(15837)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031500")
	Mess("I'll use my tongue too... nghh... nghhh... nghhh...Å^nghhh!")
	hitretResult := Hitret(15838)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031501")
	Mess("*Kiss*... *Suck*... *Lick*... nghaa... nghh!!")
	hitretResult := Hitret(15839)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031502")
	Mess("*Kiss*... *Lick*Lick*... *Pop*Pop*... nghh...")
	hitretResult := Hitret(15840)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031503")
	Mess("*Panting*... *Kiss*... ngh... H-Hrow ish it? Dosh ithÅ^feel good?")
	hitretResult := Hitret(15841)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031504")
	Mess("...Nfuu... *Pop* *Kiss*... Unnghhh... nghh!!")
	hitretResult := Hitret(15842)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031505")
	Mess("Nnghh... ngghhh... ngghhuuu... *Kiss* *Kiss*...")
	hitretResult := Hitret(15843)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She rubbed the lower part of my rod roughly with herÅ^breast along as she sticks the tip in her mouth.")
	hitretResult := Hitret(15844)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan kept on sucking as if she wants me to spill outÅ^my cum already, at that moment, she held on a weirdÅ^expression on her face.")
	hitretResult := Hitret(15845)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That face only means one thing, I knew it at thatÅ^instant, she'll be more rougher.")
	hitretResult := Hitret(15846)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ughh... Aah... N-No way... If you keep on doinf that--Å^Guuh...")
	hitretResult := Hitret(15847)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031506")
	Mess("Hehehe... You liked that huh... nghhh... *Lick*... YourÅ^head looks like it's talking to me... *Kiss*...")
	hitretResult := Hitret(15848)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031507")
	Mess("*Panting*... I think I'll tease you more with myÅ^tongue... nghhhh...*Kiss*... *Lick*...")
	hitretResult := Hitret(15849)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031508")
	Mess("Nghh... Aah, Haru-chan, you're getting wetter as well...Å^*Kiss*...")
	hitretResult := Hitret(15850)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031509")
	Mess("Because it's here right... nghuuu... *Pop*... ith feelshÅ^ghood... nghhh... Here too... nghhh... *Lick*... *Pop*!")
	hitretResult := Hitret(15851)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan licked and sucked my thing without even takingÅ^interest in anything as she smiled.")
	hitretResult := Hitret(15852)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While her drool and sweat dripped from her face to herÅ^breasts, she kept on giving me a paizuri which made itÅ^even more stickier.")
	hitretResult := Hitret(15853)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I also placed my hands on Nao-chan's hands, then I movedÅ^along with her.")
	hitretResult := Hitret(15854)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As Nao-chan used her breasts, it feels like it'sÅ^something immoral though it feels so good that I evenÅ^thrusted along with her.")
	hitretResult := Hitret(15855)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031510")
	Mess("Nghh... Aahh... Ahaah... How is it? How's my breasts?")
	hitretResult := Hitret(15856)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ngghh... Aahh... Aaahhh... Uuughhh... This feels soÅ^great...")
	hitretResult := Hitret(15857)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She didn't only used her hands to keep on making me feelÅ^it... She also used her mouth as she continued to suckÅ^on the tip of my thing as she tease me.")
	hitretResult := Hitret(15858)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her breasts felt like they're glued to my thing, it keptÅ^on changing its shape as it stimulates my thing.")
	hitretResult := Hitret(15859)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("*Chuku-Chuku-Chuku*, the sound as I thrusted my thing,Å^it's like something's piling up inside it.")
	hitretResult := Hitret(15860)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("This kind of feeling, made my body shiver. If I let itÅ^out like this, it would really feel so good.")
	hitretResult := Hitret(15861)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, I want to feel it for just a little more...")
	hitretResult := Hitret(15862)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031511")
	Mess("Ngghh... Aahh... H-Haru-chan... nggaahh... Aaahh... MyÅ^nipples... Pinch them...")
	hitretResult := Hitret(15863)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As Nao-chan continued to give me a paizuri, I gentlyÅ^pinched her erected nipples that were touching eachÅ^other in the middle of it.")
	hitretResult := Hitret(15864)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031512")
	Mess("Nggaaa... Aahh... Aaaah... Th-That's... T-Too muuchhh!!Å^Aahhii... I-If you pull it that hard... It wouldÅ^stretch!!! Ngggaaaahhh!!")
	hitretResult := Hitret(15865)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031513")
	Mess("Aagghh... nnghhh... Aaahnnn... Even, even if you pull itÅ^just like that... Aaahh... Aa-aaahhh!")
	hitretResult := Hitret(15866)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I turned her nipples around, and then I pull them likeÅ^that, Nao-chan continued on giving me a paizuri.")
	hitretResult := Hitret(15867)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I feel sorry for her, since she's been doing great atÅ^making me feel good, and I'm just bullying her.")
	hitretResult := Hitret(15868)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031514")
	Mess("A-Aaghh... My nipples... Aahhh... It's a special placeÅ^where my milk will come out... b-but, i-if you touchÅ^them like that...")
	hitretResult := Hitret(15869)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031515")
	Mess("Nggghhh... I-It feels good... Aaahh... Ahaahh!")
	hitretResult := Hitret(15870)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan... I'm just rubbing your nipples...")
	hitretResult := Hitret(15871)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031516")
	Mess("Ehh... *Pant* *Pant*... You're just rubbing my nipples?Å^Haru-chan...")
	hitretResult := Hitret(15872)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031517")
	Mess("Y-Yeah... H-How about this... ngghh... Uughh...Å^Aaahhh...")
	hitretResult := Hitret(15873)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She pointed her nipples against my thing.")
	hitretResult := Hitret(15874)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's like her nipples holds a new kind of pleasure as itÅ^graze the tip of my thing.")
	hitretResult := Hitret(15875)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ngghh... This feels good too... Ughh...")
	hitretResult := Hitret(15876)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031518")
	Mess("Ngghh... ngghh... Okay then, I'll do it more... nghh...")
	hitretResult := Hitret(15877)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031519")
	Mess("N-Nghhuu... A-Aaahhh... Ah, does my nipples feel muchÅ^better like this? Nghuuuu... Aaahhh...")
	hitretResult := Hitret(15878)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031520")
	Mess("Nghuuu... Ahah... Aaahh... I-I also feel a bit weird...Å^I feel more ecchi... This somehow feels abnormal...Å^nghahaaan!")
	hitretResult := Hitret(15879)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031521")
	Mess("Aaahh... Aaahh... Rub it more... ngghh... nggghh!!")
	hitretResult := Hitret(15880)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031522")
	Mess("When you rub my nipples... It feels good... Aha... EvenÅ^if--aahh!! Ngghaaa... AhaaÅ`... nghh...!!")
	hitretResult := Hitret(15881)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ughh... N-Nao-chan...")
	hitretResult := Hitret(15882)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031523")
	Mess("Ngh, *pant* *pant*... Haru-chan, are you about to come?Å^Nghh... Because, just now some juice were leaking out...Å^Did you came?")
	hitretResult := Hitret(15883)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I'm coming... Auughh... Ughh...")
	hitretResult := Hitret(15884)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031524")
	Mess("It's fine... Just come... nghh... nghh... *Kiss*...Å^Hehe, what a cute face you have trying to hold it in...")
	hitretResult := Hitret(15885)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031525")
	Mess("Aah... *kiss*... Hey now... nghh... Wouldn't it be badÅ^to hold it in? Nghh... *Kiss*... nghhaa...")
	hitretResult := Hitret(15886)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... Aahh... Nao-chan...")
	hitretResult := Hitret(15887)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan's moving along her breast as I thrust my rodÅ^into it.")
	hitretResult := Hitret(15888)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She moved her tongue around the neck of my rod, and thenÅ^she drank the sap that sagged over it.")
	hitretResult := Hitret(15889)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her breasts were dripping with sweat as she massaged meÅ^with her mouth, it made me even love Nao-chan more thanÅ^ever. I smiled as I watched her.")
	hitretResult := Hitret(15890)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EB12b", 0, 0)
	Talk("Nao", "NO031526")
	Mess("Nghh... nghhh... Aahh... You couldn't hold it anymoreÅ^right? Your hips are trembling... nghh... nghhh...Å^*Kiss*...")
	hitretResult := Hitret(15891)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031527")
	Mess("H-Here it comes... Haru-chan... nghh*Kiss*... More...Å^More... Feel it... nghhaa...*Pant* *Pant*...")
	hitretResult := Hitret(15892)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nghhh... Ughh... Aahh...")
	hitretResult := Hitret(15893)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I grabbed Nao-chan's breasts as I violently thrusted myÅ^rod in and out from it.")
	hitretResult := Hitret(15894)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031528")
	Mess("Nghh!? Nghh... nghh... Aa-Aahh!!!")
	hitretResult := Hitret(15895)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031529")
	Mess("*Suck*... *Pop*... ngghhhh!! *Pant* *Pant*... nghhh!")
	hitretResult := Hitret(15896)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031530")
	Mess("H-Haru-chan... S-So rough... nghhh! Nghhh!!")
	hitretResult := Hitret(15897)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I thrust my hip violently, Nao-chan was forced toÅ^blow me as she gave me a paizuri, I pushed my thing upÅ^to her mouth.")
	hitretResult := Hitret(15898)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan's lips became tighter as she used it toÅ^stimulate the tip of my thing.")
	hitretResult := Hitret(15899)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My rough and violent act... Feels like I'm violatingÅ^her.")
	hitretResult := Hitret(15900)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, I can't hold in my desperation for Nao-chan, as IÅ^sticked it inside her, she wrapped it with her tongue asÅ^she suck it.")
	hitretResult := Hitret(15901)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031531")
	Mess("Nghh... *Kiss* *Kiss*... Aahnn... Aaahh...")
	hitretResult := Hitret(15902)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031532")
	Mess("Nghghh... Kkhh... nghhh... H-Haru-chan... nghhh!")
	hitretResult := Hitret(15903)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031533")
	Mess("I--your hot sperm is coming out!! Ngghhhaa... Aahh...")
	hitretResult := Hitret(15904)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031534")
	Mess("Let it all... ouuuut!! Nghhaaaaa!!")
	hitretResult := Hitret(15905)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031535")
	Mess("Nghh, nghh, nghh, ngh!! *Pant* *Pant*... ngh, nghhh!")
	hitretResult := Hitret(15906)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Ughh... Guhh... I-I can't hold it... I-I'm aboutÅ^to...")
	hitretResult := Hitret(15907)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I firmly drew my hips back, then my back bended. Then, IÅ^felt something hot around my crotch.")
	hitretResult := Hitret(15908)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031536")
	Mess("Ahaa... Aaahh... Aah! Aaah! Aaahnn!!")
	hitretResult := Hitret(15909)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031537")
	Mess("Nghh... nghh... *Kiss*! *Kiss* *Kiss*!!")
	hitretResult := Hitret(15910)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031538")
	Mess("Nghh... Moore!! Nghh... Cooome now!!")
	hitretResult := Hitret(15911)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nghh!! Ughh...!!!")
	hitretResult := Hitret(15912)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031539")
	Mess("Ahh... Aah, aah, aaaaah!! Nghhh!!!")
	hitretResult := Hitret(15913)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031540")
	Mess("Aah... nghhh!! Nghhhh nghhhh!!!")
	hitretResult := Hitret(15914)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EB12c", 0, 0)
	Flush("WHITE", 1000, "")
	Talk("Nao", "NO031541")
	Mess("Nghhh... Haaaaaaaaaaaaaaahnn!!")
	hitretResult := Hitret(15915)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Gguh... Ughh...")
	hitretResult := Hitret(15916)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031542")
	Mess("Aahh... Ahaa... Aaaahhh... Aahh...")
	hitretResult := Hitret(15917)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031543")
	Mess("It's coming out... Aaah... nghhh... *Pant* *Pant*...")
	hitretResult := Hitret(15918)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031544")
	Mess("Nghh... A-Amazing... It's so thick... nghh... You reallyÅ^came a lot... nghhh... *Kiss*...")
	hitretResult := Hitret(15919)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ughh... Nao-chaaan... *Pant* *Pant* *Pant*...")
	hitretResult := Hitret(15920)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At that moment, I scattered a lot of my thick whiteÅ^sperm that I just ejaculated.")
	hitretResult := Hitret(15921)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I even felt a little bit ticklish as I squeezed out theÅ^last drop of it, I scattered them throughout Nao-chan'sÅ^breasts.")
	hitretResult := Hitret(15922)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I also poured some of my semen on Nao-chan's face, myÅ^body was shaking as I felt so great.")
	hitretResult := Hitret(15923)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031545")
	Mess("Nghh... It's getting sticky by the moment... nghh...")
	hitretResult := Hitret(15924)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031546")
	Mess("*Lick*... nghh... *Slurp*... *Lick*... *Kiss*...")
	hitretResult := Hitret(15925)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan politely licked the swollen semen off of myÅ^thing as if she was eating an ice cream.")
	hitretResult := Hitret(15926)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031547")
	Mess("Hehe, Haru-chan... Your face looks like you're reallyÅ^satisfied.")
	hitretResult := Hitret(15927)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's all because of you Nao-chan... I'm really glad.")
	hitretResult := Hitret(15928)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031548")
	Mess("I... I'm not yet finished with you... *Kiss*... nghh...")
	hitretResult := Hitret(15929)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan disagreed with me as she licked the semen offÅ^of her finger.")
	hitretResult := Hitret(15930)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031549")
	Mess("Say, Haru-chan...")
	hitretResult := Hitret(15931)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Mm? What?")
	hitretResult := Hitret(15932)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031550")
	Mess("... Can you... do it again?")
	hitretResult := Hitret(15933)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan looked at me with embarassing eyes.")
	hitretResult := Hitret(15934)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... Of course... Nao-chan...")
	hitretResult := Hitret(15935)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan placed her hand on my cheek and gently kissedÅ^me.")
	hitretResult := Hitret(15936)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031551")
	Mess("Nghh... *Kiss*... Thank you, Haru-chan...")
	hitretResult := Hitret(15937)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan wobbled as she stand. I thought it would beÅ^better if we changed places this time... Since I don'tÅ^want to see her wobble.")
	hitretResult := Hitret(15938)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, be careful...")
	hitretResult := Hitret(15939)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031552")
	Mess("S-Sorry... It's because I stood with my knees, my feetÅ^feels a little numb...")
	hitretResult := Hitret(15940)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Are you all right? ... Hmm?")
	hitretResult := Hitret(15941)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I supported Nao-chan through her arms. At that time, myÅ^thing slightly grazed Nao-chan's crotch.")
	hitretResult := Hitret(15942)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031553")
	Mess("Ah... Haru-chan... It hit me...")
	hitretResult := Hitret(15943)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan's crotch felt hot, and it slowly became wet.")
	hitretResult := Hitret(15944)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Are you getting wet... Nao-chan?")
	hitretResult := Hitret(15945)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031554")
	Mess("Yeah... My shorts felt like it's damped already... It'sÅ^quite embarrassing...")
	hitretResult := Hitret(15946)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't be... Nao-chan, it's because you felt real goodÅ^just now.")
	hitretResult := Hitret(15947)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031555")
	Mess("That's because, you kept on harrassing my boobs...Å^Aaghnn! Hii... Aah... Y-Yes there, touch me...")
	hitretResult := Hitret(15948)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I gently touched her above her shorts, it felt so warm,Å^and it seems sticky around Nao-chan's crotch.")
	hitretResult := Hitret(15949)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031556")
	Mess("Nghh... Aahh... nghhhh... Aahh, w-wait... My shorts...Å^Take them off...")
	hitretResult := Hitret(15950)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan took it off by raising one of her legs.")
	hitretResult := Hitret(15951)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031557")
	Mess("Ookay... For now, I'll stay in the desk?")
	hitretResult := Hitret(15952)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, come here just like that.")
	hitretResult := Hitret(15953)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031558")
	Mess("Eh... On top of you, Haru-chan?")
	hitretResult := Hitret(15954)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... I want to make you feel good Nao-chan.")
	hitretResult := Hitret(15955)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031559")
	Mess("Geez... That's kind of embarrassing... Ah, but, doesÅ^that mean, I get to see your cute face again, Haru-chan.")
	hitretResult := Hitret(15956)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("N-No way... A-As I thought, we shouldn't do thisÅ^position...")
	hitretResult := Hitret(15957)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031560")
	Mess("Noooope... Okaay... Excuse me...")
	hitretResult := Hitret(15958)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nghh... Aahh... Guuhh...")
	hitretResult := Hitret(15959)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031561")
	Mess("Over here Haru-chan... It looks like it's stillÅ^recovering...")
	hitretResult := Hitret(15960)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh... Aughh... I-It's a bit ticklish... nghh...")
	hitretResult := Hitret(15961)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031562")
	Mess("Put it in Haru-chan...")
	hitretResult := Hitret(15962)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay...")
	hitretResult := Hitret(15963)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EB13a", 0, 0)
	Transition("WIP_LR", 500)
	Talk("Nao", "NO031563")
	Mess("Ah... nghh... I-It's touching me... nghh...")
	hitretResult := Hitret(15964)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan grabbed me at my shoulders as she lowered herÅ^hips.")
	hitretResult := Hitret(15965)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I supported my thing with my hands as it hit Nao-chan'sÅ^place, and just like that, it slowly pierced her vaginaÅ^and it got sucked in.")
	hitretResult := Hitret(15966)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031564")
	Mess("Ah... Aaaah... nghh... I-It's in...")
	hitretResult := Hitret(15967)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan and I are connected again as we faced eachÅ^other.")
	hitretResult := Hitret(15968)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan...")
	hitretResult := Hitret(15969)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031565")
	Mess("Hehe, Haru-chan... *Kiss*... nghhh...")
	hitretResult := Hitret(15970)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We both looked at each other. Somehow I feltÅ^embarrassed, and we laughed a little.")
	hitretResult := Hitret(15971)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I tried my best balancing as we were on a small desk,Å^Nao-chan held on the curtain by the window.")
	hitretResult := Hitret(15972)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031566")
	Mess("Aah... I feel amazing... Haru-chan's thing is twitchingÅ^inside of me...")
	hitretResult := Hitret(15973)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031567")
	Mess("It totally reached inside me... And it feels like it'sÅ^by my stomach already...")
	hitretResult := Hitret(15974)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nghh... Nao-chan too, you're amazingly tightening up...")
	hitretResult := Hitret(15975)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031568")
	Mess("B-Because... It's on my inner most part... I feel likeÅ^it's on my womb already...")
	hitretResult := Hitret(15976)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031569")
	Mess("Ngh... Aghh... Just like this , if I push it downÅ^deeper, I think I'll go crazy...")
	hitretResult := Hitret(15977)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'll make you feel so good... Nao-chan... Just push itÅ^down on me.")
	hitretResult := Hitret(15978)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031570")
	Mess("Haru-chan...")
	hitretResult := Hitret(15979)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I placed my hands around Nao-chan's hips to support herÅ^a bit, then I pulled her to me.")
	hitretResult := Hitret(15980)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As we were firmly connected, she felt so stimulated as IÅ^thrust my thing inside her.")
	hitretResult := Hitret(15981)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031571")
	Mess("Ahn! A-Ah! Aahn! Haahaa...")
	hitretResult := Hitret(15982)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031572")
	Mess("So deep... nghhh... Aaahh... A-Aaah! It's so deep...Å^I-Haru-chan let's do it together... nghhh!!")
	hitretResult := Hitret(15983)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan's breasts shook as her she shivered withÅ^pleasure, then I slowly slid my hands to rub her bigÅ^juggs.")
	hitretResult := Hitret(15984)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031573")
	Mess("Ahaah... Aahh... Do me more... nghhh... Aah!")
	hitretResult := Hitret(15985)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031574")
	Mess("Nghh... Yes... Aahh... More--rougher!! Ngghhh!! I feelÅ^so lewd that my nipples are soo erected!! Touch meÅ^more!!")
	hitretResult := Hitret(15986)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031575")
	Mess("Nghh... nghhh... Aah... Ahh! Aaahnn!")
	hitretResult := Hitret(15987)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan shoved her breasts on me as she move her hips.")
	hitretResult := Hitret(15988)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She kept on grazing the sides of my thing as she moveÅ^up, then I also moved my hips slowly.")
	hitretResult := Hitret(15989)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I enjoyed the pleasure as I suck on Nao-chan's breasts.")
	hitretResult := Hitret(15990)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nghh... *Kiss*... *Suck*... Your nipples are stiffÅ^again...")
	hitretResult := Hitret(15991)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031576")
	Mess("Nghh... Aahh... Y-Yeah, I can feel it... Aahh... More...Å^Lick it more... nghhh... Aaahnn!")
	hitretResult := Hitret(15992)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If I rub it this much, maybe some milk would come outÅ^huh... *suck*...")
	hitretResult := Hitret(15993)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031577")
	Mess("Nghh... Aah... Aaah... Haahaa... I-If something wouldÅ^come out of my breasts, Haru-chan should-- Drink all ofÅ^it... nghhh...")
	hitretResult := Hitret(15994)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There would be a lot then... Your breasts are very bigÅ^after all... *Suck*...Nghhh...")
	hitretResult := Hitret(15995)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I licked her from the breasts to her nipples, there wereÅ^tracks of my saliva. I licked her with the tip of myÅ^tongue that swirled around the ring of her nipples up toÅ^its tip.")
	hitretResult := Hitret(15996)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031578")
	Mess("Nghh... I-Is it delicious? My breasts... ? Haahaa...")
	hitretResult := Hitret(15997)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... Nao-chan, would you like to try too?")
	hitretResult := Hitret(15998)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031579")
	Mess("Eeh? ... S-Sure... Let me try... nghhh... l-like this?Å^Aahh... *suck*... *suck*... nghhuuu...")
	hitretResult := Hitret(15999)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan lifted up her own breasts so that she can alsoÅ^suck it, then she licked her nipples that was erected soÅ^much.")
	hitretResult := Hitret(16000)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031580")
	Mess("Nghh... nghuuh... I-I feel strange... I'm lickingÅ^myself... nghhh... Aahh... H-Haru-chan?")
	hitretResult := Hitret(16001)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Let's do it together... nghhh... *Kiss*...")
	hitretResult := Hitret(16002)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031581")
	Mess("Nghhh... Aah... *Kiss* *Kiss* *Lick* *Suck*...")
	hitretResult := Hitret(16003)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031582")
	Mess("A-As I thought, it's weird... nghhh...Å^*lick*...*kiss*...Haahaahh... Aahh... Ahaaa....")
	hitretResult := Hitret(16004)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hey... Here too... Suck them too...")
	hitretResult := Hitret(16005)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I lifted both of her breasts up, then Nao-chan placedÅ^them in her mouth.")
	hitretResult := Hitret(16006)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Along with her other breast, the both of us licked herÅ^breasts together.")
	hitretResult := Hitret(16007)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031583")
	Mess("Aahh... Gghh... *Suck*Kiss*... ngghh... Haru-hyan...Å^nghhh... *Suck*... nghhh... Haaahaaa...")
	hitretResult := Hitret(16008)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nghh... *kiss* *suck*...")
	hitretResult := Hitret(16009)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031584")
	Mess("Nghh... Aahh... *Suck* *Slurp*... I-It's becomingÅ^sweet... Licking my own breasts... I had never thoughtÅ^of this...")
	hitretResult := Hitret(16010)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Will you also drink your own milk if it comes out?")
	hitretResult := Hitret(16011)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031585")
	Mess("Geez... Haru-chan...")
	hitretResult := Hitret(16012)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan scolded me as she gently headbutted me with herÅ^forehead.")
	hitretResult := Hitret(16013)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031586")
	Mess("...If it comes out, okay? Hehehe... *Kiss*... ngghh...")
	hitretResult := Hitret(16014)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As we lick along her nipples, our tongues touched eachÅ^other and twirled around each other.")
	hitretResult := Hitret(16015)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031587")
	Mess("Nghh... nghhh... *Kiss*... Haahaahaa... I'm tired, myÅ^breasts are heavy and big...")
	hitretResult := Hitret(16016)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan dropped her breasts which she was lifting.")
	hitretResult := Hitret(16017)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You swim a lot, yet they're not thinned out? Ah, that'sÅ^right... It's because you were always reading by theÅ^pool side...")
	hitretResult := Hitret(16018)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031588")
	Mess("Th-That's true, but... Thanks to that, you're having funÅ^with my breasts, right Haru-chan?")
	hitretResult := Hitret(16019)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Guuhh... nghh... C-Certainly...")
	hitretResult := Hitret(16020)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031589")
	Mess("Come now... If I was always doing my best swimming, youÅ^won't be able to savor them that way, right?")
	hitretResult := Hitret(16021)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I know... I give up... nghhh... *Kiss*...")
	hitretResult := Hitret(16022)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031590")
	Mess("Aahh... ngghhh... Again... You're attacking me in myÅ^nipples again...")
	hitretResult := Hitret(16023)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Because, it's your weak spot Nao-chan... nghhh...")
	hitretResult := Hitret(16024)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031591")
	Mess("Nghhhh! Aah... Hii... Ughhh...")
	hitretResult := Hitret(16025)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031592")
	Mess("Y-You're pinching it again... ngghghhhh... ngghhh...Å^Aahh...")
	hitretResult := Hitret(16026)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Aah, don't move like that... It's dangerous, we mightÅ^fall from the desk...")
	hitretResult := Hitret(16027)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031593")
	Mess("Gghh... B-But... ngghh... Aaahh... Hii...")
	hitretResult := Hitret(16028)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031594")
	Mess("Aaaaaah... nghhh... Y-You meanie... I know that I can'tÅ^escape so you're being rough on me... nngghhaaaaahn!!")
	hitretResult := Hitret(16029)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031595")
	Mess("Nggghhhhhhh... Aaa... Aaaahnn... Aah! Aaah!")
	hitretResult := Hitret(16030)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I placed some support on the back of Nao-chan so that ifÅ^she tries to move, I could pull her back and continue.")
	hitretResult := Hitret(16031)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031596")
	Mess("Nghh... A-Aaaahh... Haahaahaah... Geez... Haru-chan...Å^Here's my revenge...")
	hitretResult := Hitret(16032)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eeh...? Aah... Wh-what?")
	hitretResult := Hitret(16033)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031597")
	Mess("Nghuu... nghh... Haahaah... Because Haru-chan, your rodÅ^was twitching like it's nervous or something, that'sÅ^why...")
	hitretResult := Hitret(16034)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031598")
	Mess("Take... thiiis... nghhh... gnhhh... A-Aah...!")
	hitretResult := Hitret(16035)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ugghh... Y-You're strangling it... Ughhh...")
	hitretResult := Hitret(16036)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan moved her hips in an intense way as she was onÅ^top of me.")
	hitretResult := Hitret(16037)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I took advantage of pushing my hips up as she's on topÅ^of me, as Nao-chan felt more, she tightened up around myÅ^thing as it graze her walls.")
	hitretResult := Hitret(16038)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Aah... Gghh... ngghh... N-Nao-chan...")
	hitretResult := Hitret(16039)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031599")
	Mess("Nggngghh! Nghhh... H-How is it? It feels good right?Å^Turning it like this... It's rubbing right... nghhh...")
	hitretResult := Hitret(16040)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031600")
	Mess("I'll move slowly... N-Nghhhhh... Aah, aah, aahh, aaahh!")
	hitretResult := Hitret(16041)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031601")
	Mess("Just relax Haru-chan, let me do all the moving... nghhh!Å^Nghuuu... Ahh-AAah!!")
	hitretResult := Hitret(16042)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Gghh... Na... o... chan...")
	hitretResult := Hitret(16043)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Everytime she rubs it, I feel ticklish, though I thinkÅ^she also do feel it as she looks enticed.")
	hitretResult := Hitret(16044)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031602")
	Mess("Ngghh... nghhh! Hey, a-are you feeling it?")
	hitretResult := Hitret(16045)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Aaughhh... Ghhh !?")
	hitretResult := Hitret(16046)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A sudden wave of pleasure made me bend my back, I feltÅ^like I was about to fall, though I reached out my handsÅ^and grabbed on Nao-chan's breasts.")
	hitretResult := Hitret(16047)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031603")
	Mess("Nghhi? Aa-aaaahh!! Ow... O-Ouch...")
	hitretResult := Hitret(16048)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Aahh, sorry... Ah, now's my chance... Come here...")
	hitretResult := Hitret(16049)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031604")
	Mess("Aaahh... nghhh... S-Stop that, again? A-Aaahnn!!Å^Nghhh... Aaahh!! Aahh... Nooot my nipplesss!!")
	hitretResult := Hitret(16050)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031605")
	Mess("Nghhhh... I-I won't lose to youu... nghhh... ngghhh!!")
	hitretResult := Hitret(16051)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031606")
	Mess("AAhh... Hii!? N-No... Ahhn... Aah... nnghh! Th-That's...")
	hitretResult := Hitret(16052)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Not only her breasts, I reached out to Nao-chan's crotchÅ^and gently stroke her clitoris up and down.")
	hitretResult := Hitret(16053)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I attacked her weak spots at the same time, thenÅ^Nao-chan curved backward as if she had cramps.")
	hitretResult := Hitret(16054)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031607")
	Mess("Ngghhaaa... Haahaahahh... A-Amazing... It feels like aÅ^wave of electricity is running around my back...Å^nghhh... Aahhnn...")
	hitretResult := Hitret(16055)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It feels so good inside you... Nao-chan... The way youÅ^move your hips, and the way you squeeze me.")
	hitretResult := Hitret(16056)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031608")
	Mess("Haahaah... I have my way of doing you after all...")
	hitretResult := Hitret(16057)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan... Let's come together... the both of us...")
	hitretResult := Hitret(16058)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031609")
	Mess("Okay... Let's do it more...")
	hitretResult := Hitret(16059)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We firmly embraced each other, then we kept on teasingÅ^each other as we kissed.")
	hitretResult := Hitret(16060)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031610")
	Mess("Nghhh, nghhh...*Kiss*... ngghh... H-Haru-chan...")
	hitretResult := Hitret(16061)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("N-Nao-chan... nghhh... Ghhh...")
	hitretResult := Hitret(16062)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031611")
	Mess("Hot... My body feels hot... Ahhnn... Ahh... Haa!")
	hitretResult := Hitret(16063)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031612")
	Mess("If I come like this, I feel like I'll melt with youÅ^Haru-chan... nggghh!")
	hitretResult := Hitret(16064)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031613")
	Mess("...I feel so good... nghhh! Ngghhhhhh!! Aaaah! Aaaaaahn!")
	hitretResult := Hitret(16065)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031614")
	Mess("I-It's coming!! A lot of it is inside me... nghh,Å^nghhhh...!")
	hitretResult := Hitret(16066)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031615")
	Mess("Nghhh... Y-Yes!! Aah... Rub it all the way in up to myÅ^stomach... nghhh... nghhh.!!")
	hitretResult := Hitret(16067)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031616")
	Mess("Aa-aahh!! M-My strength... Aaahh! It's going away,Å^Haru-chan! Nhhhaaa!!!")
	hitretResult := Hitret(16068)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031617")
	Mess("Haahaah... nghhh... Aa-aahhhhhn! Aah...")
	hitretResult := Hitret(16069)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan is almost at her limit, as she move her hipsÅ^around, I also move my hips along her so we could feelÅ^it until the end.")
	hitretResult := Hitret(16070)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As Nao-chan and I were embracing each other, my thingÅ^felt something inside her vagina.")
	hitretResult := Hitret(16071)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was also near my limit... I burried my face onÅ^Nao-chan's breasts as I tried to hold it in.")
	hitretResult := Hitret(16072)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031618")
	Mess("Aah, aah, aahhh!! Ngh, nghhh... Aaah!!")
	hitretResult := Hitret(16073)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031619")
	Mess("H-Haru-chan... let's kiss... nghhh... Mnhghgh...Å^*Kiss*...")
	hitretResult := Hitret(16074)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031620")
	Mess("Nghhh... nghhh... *Kiss*... *Kiss*...")
	hitretResult := Hitret(16075)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031621")
	Mess("Nghhaa... Aah... Aaaah, nghhhh... Ahaaah !")
	hitretResult := Hitret(16076)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031622")
	Mess("Y-You're teasing me again!! Aa-aaahn! Nghhh!")
	hitretResult := Hitret(16077)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031623")
	Mess("I-It's hitting me inside... nghhh!! M-More..!!")
	hitretResult := Hitret(16078)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031624")
	Mess("Th-That's the spot... Ahh, ahh, aaahhh, aahh!!")
	hitretResult := Hitret(16079)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan moved her hips as if she was drawing a circle,Å^then my thing felt like it was rubbed once more.")
	hitretResult := Hitret(16080)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As she swallowed up my thing, it felt sticky as ourÅ^fluids covered it, it also looked like our pubic hairÅ^are entangled at each other.")
	hitretResult := Hitret(16081)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan slowly bended backward as she felt theÅ^pleasure.")
	hitretResult := Hitret(16082)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031625")
	Mess("Aahh... nghhhhh. It's hitting me up to my stomach!")
	hitretResult := Hitret(16083)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031626")
	Mess("Nghhh... Aah... D-Don't... H-Haru-chan... I-I'm...Å^coming!! Aaaghh... Aaahh!!")
	hitretResult := Hitret(16084)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kuh... N-Nao-chan... Y-You're too rough... Kuh...")
	hitretResult := Hitret(16085)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031627")
	Mess("Aaah... nghhh... nghhh... Aaahh... Aaahnn!!Å^H-Haru-chan... P-Pleeaaase... nghhh!")
	hitretResult := Hitret(16086)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031628")
	Mess("Are you also about to? Nghhhaaa... Aaah... Haahaahaah!!")
	hitretResult := Hitret(16087)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031629")
	Mess("Nghhh... Aah... Do my breasts more! Nghhh!")
	hitretResult := Hitret(16088)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031630")
	Mess("Hyii... I-I'm coming... I'm coming! I'm coming!Å^Ngghhhaaa!")
	hitretResult := Hitret(16089)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031631")
	Mess("Nghhh... A-A-A-Ahaaah... nghaaaaaaa!!")
	hitretResult := Hitret(16090)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("N-Nao-chan... Gguuh!!")
	hitretResult := Hitret(16091)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031632")
	Mess("Aah... nghhh... Aaah... A-Aaaaaaaaaah!!")
	hitretResult := Hitret(16092)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EB13b", 0, 0)
	Flush("WHITE", 1000, "")
	Talk("Nao", "NO031633")
	Mess("Nghhh... A-Aaaaaaaaaaaaaaaaaaaaaaaaaah!!!")
	hitretResult := Hitret(16093)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031634")
	Mess("Nghh... A-A-A-Ahhhh... nghhhh...")
	hitretResult := Hitret(16094)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031635")
	Mess("Haahaahaahaah... Aaah... Ughh... Kkuh...")
	hitretResult := Hitret(16095)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I pulled my hips from Nao-chan, then our hot fluidsÅ^dripped out of her as I impregnated her.")
	hitretResult := Hitret(16096)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("*Biku* *Biku* *Biku*, Nao-chan's vagina trembled as IÅ^removed my thing that it swallowed up to its deepestÅ^parts.")
	hitretResult := Hitret(16097)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Aah... Ughh... Haahaahaah...")
	hitretResult := Hitret(16098)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031636")
	Mess("Aa... Aaahh...")
	hitretResult := Hitret(16099)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan... nghhh...")
	hitretResult := Hitret(16100)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	SetCg("B19a", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("As her strength left her, Nao-chan crumbled down andÅ^embraced me.")
	hitretResult := Hitret(16101)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We smiled at each other as our foreheads met.")
	hitretResult := Hitret(16102)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031637")
	Mess("Nghh... It was amazing... what you did earlier...")
	hitretResult := Hitret(16103)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... Nao-chan, you were so rough...")
	hitretResult := Hitret(16104)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031638")
	Mess("Because... I was really feeling it... But, I just reallyÅ^felt like being in the lead you know?")
	hitretResult := Hitret(16105)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... I understand... Though I really did my best toÅ^move along with you.")
	hitretResult := Hitret(16106)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031639")
	Mess("But you know... You were really good Haru-chan... YouÅ^made me feel it so hard too...")
	hitretResult := Hitret(16107)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You were so cute... *Kiss*...")
	hitretResult := Hitret(16108)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031640")
	Mess("Nghhh... *kiss*... Thanks...")
	hitretResult := Hitret(16109)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031641")
	Mess("Aaah... We've got to much sweat, it's becoming stickyÅ^already...")
	hitretResult := Hitret(16110)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Let's take a shower...")
	hitretResult := Hitret(16111)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031642")
	Mess("You're right... Ah, but I'm out for recess, they mightÅ^suspect something if we go back now since we're coveredÅ^in sweat...")
	hitretResult := Hitret(16112)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's all right... Probably.")
	hitretResult := Hitret(16113)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031643")
	Mess("Geez... Hahaha.")
	hitretResult := Hitret(16114)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The curtain from the window swayed as a long breeze ofÅ^wind came in.")
	hitretResult := Hitret(16115)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From the pool, we heard voices that were having fun.Å^This doesn't look like a nice opportunity to take aÅ^shower.")
	hitretResult := Hitret(16116)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, for a little more while... Nao-chan and I embracedÅ^each other.")
	hitretResult := Hitret(16117)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	OnRecollectFlag(8)
	If (IsRecollectMode())
		Change("EXIT_SCENARIO")
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("", 0)
	Change("00_b026")
	Return
	Change("EXIT_SCENARIO")
EndFunc

