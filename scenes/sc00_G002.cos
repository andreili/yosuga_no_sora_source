Include"yosuga.ch"

Function sc00_G002()
	ScenarioEnter()
	SetCg("B34a", 400, 0)
	ScPlayBgm("BGM05", 0)
	Talk("êSÇÃê∫", "")
	Mess("That might not have been the primary reason, but IÅ^decided to take a walk around town after our lunch.")
	hitretResult := Hitret(39214)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Along the way, I was thinking if the area I'm passingÅ^through was depicted in the photos I saw.")
	hitretResult := Hitret(39215)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B13a", 0, 0)
	Transition("WIP_LR", 500)
	Update()
	ScWait(1500, 0)
	SetCg("B05a", 0, 0)
	Transition("WIP_LR", 500)
	Update()
	ScWait(1500, 0)
	SetCg("B15a", 0, 0)
	Transition("WIP_LR", 500)
	Update()
	ScWait(1500, 0)
	SetCg("B07a", 0, 0)
	Transition("WIP_LR", 500)
	Update()
	ScPlaySe("SE302", 0)
	Talk("êSÇÃê∫", "")
	Mess("And as I was wandering around like that, I reached theÅ^shrine before I knew it.")
	hitretResult := Hitret(39216)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Then, I remembered seeing something that looked like aÅ^forest surrounding shrine in the photos.")
	hitretResult := Hitret(39217)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm... Now that I think about it, I don't remember anyÅ^festival photos in that blog...")
	hitretResult := Hitret(39218)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If she lived in this town, I figure she would want toÅ^feature some festival photos at least.")
	hitretResult := Hitret(39219)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While reminiscing about the festival, I walked aroundÅ^the shrine grounds.")
	hitretResult := Hitret(39220)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Right now, nothing could be heard aside from the criesÅ^of cicadas and my footsteps, but at the time of theÅ^festival the place was packed to the brim with people.")
	hitretResult := Hitret(39221)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And amidst that, we walked around with Motoka-san...")
	hitretResult := Hitret(39222)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...I wonder if that could be considered a date...")
	hitretResult := Hitret(39223)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Wait a minute, am I the only one concerned about suchÅ^things...")
	hitretResult := Hitret(39224)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Hmm, ah, that's it. There is a probability thatÅ^Motoka-san is the one who writes that blog...")
	hitretResult := Hitret(39225)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("No, no. The length of her hair does not match at all.")
	hitretResult := Hitret(39226)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And it does not seem plausible that Ryouhei would notÅ^recognize her, considering he meets her on a regularÅ^basis.")
	hitretResult := Hitret(39227)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But the thing is that everyone in this town recognizesÅ^each other. In that case, I realize the fact that itÅ^would be unlikely that we don't recognize her.")
	hitretResult := Hitret(39228)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Verily, a mysterious woman.")
	hitretResult := Hitret(39229)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(0, 1)
	SetCg("B26a", 0, 0)
	Update()
	ScPlaySe("SE301", 0)
	Talk("êSÇÃê∫", "")
	Mess("I ventured deeper behind the shrine.")
	hitretResult := Hitret(39230)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There, a sort of forest started, and cries of cicadasÅ^became increasingly loud.")
	hitretResult := Hitret(39231)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Here, I could not find anything man-made, the place wasÅ^the embodiment of nature.")
	hitretResult := Hitret(39232)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se055", 0)
	Talk("êSÇÃê∫", "")
	Mess("Because of that, an out-of-place electronic sound seemedÅ^to resound especially strong.")
	hitretResult := Hitret(39233)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Camera...?")
	hitretResult := Hitret(39234)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What was that? As I thought that, I peeked in theÅ^direction of sound.")
	hitretResult := Hitret(39235)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ04a", 0, 0)
	Transition("WIP_TB", 500)
	ScPlayBgm("BGM03", 0)
	Talk("êSÇÃê∫", "")
	Mess("...For a coincidence, this is something really over theÅ^top...")
	hitretResult := Hitret(39236)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Or rather, "speak of the devil" as is...")
	hitretResult := Hitret(39237)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There, just the person I was looking for.")
	hitretResult := Hitret(39238)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her figure was beautiful as she placed her hand on aÅ^tree as if trying to talk to it and looked up.")
	hitretResult := Hitret(39239)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se055", 0)
	Talk("êSÇÃê∫", "")
	Mess("Once more, I heard a shutter sound.")
	hitretResult := Hitret(39240)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A tripod stood in the vicinity, so it seemed she wasÅ^taking photos of herself.")
	hitretResult := Hitret(39241)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Onee-san changed the pose, this time pretending to combÅ^her hair.")
	hitretResult := Hitret(39242)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That gesture, and her expression were so natural. Truly,Å^what a beautiful onee-san...")
	hitretResult := Hitret(39243)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Hmm, wait a minute. I've seen that face beforeÅ^somewhere...")
	hitretResult := Hitret(39244)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eeeeh!!")
	hitretResult := Hitret(39245)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Mysterious BeautyÅsÇ‚Ç–ÇÎÅt", "YH050158")
	Mess("Huh? Who is it!!")
	hitretResult := Hitret(39246)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Upon noticing me, she let out a threatening andÅ^overbearing call, markedly unfitting for her refinedÅ^appearance.")
	hitretResult := Hitret(39247)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...That was the voice I remembered clearly.")
	hitretResult := Hitret(39248)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ya- Yahiro-san...!?")
	hitretResult := Hitret(39249)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EZ04b", 0, 0)
	Talk("Yahiro", "YH050159")
	Mess("Geh- ! Ha- Haruka!")
	hitretResult := Hitret(39250)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("After all, it was really Yahiro-san...?")
	hitretResult := Hitret(39251)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050160")
	Mess("Uwa- y- you, what are you doing here?")
	hitretResult := Hitret(39252)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Just taking a hike... Yahiro-san, what are YOU doing?")
	hitretResult := Hitret(39253)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050161")
	Mess("No- Nothing!! It does not matter, so just go away!")
	hitretResult := Hitret(39254)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Flush("WHITE", 1000, "")
	ScPlaySe("se055", 0)
	Talk("êSÇÃê∫", "")
	Mess("And just as she was scowling back at me, the cameraÅ^shutter sounded once more.")
	hitretResult := Hitret(39255)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Now, that was a ruined shot, I thought to myself.")
	hitretResult := Hitret(39256)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050162")
	Mess("Uh...")
	hitretResult := Hitret(39257)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see, taking a commemorative photo? Want me to lend aÅ^helping hand?")
	hitretResult := Hitret(39258)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050163")
	Mess("Do not want! Beat it! For real!")
	hitretResult := Hitret(39259)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The one Ryouhei poetically referred to as "MysteriousÅ^Beauty" turned out to be Yahiro-san...")
	hitretResult := Hitret(39260)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Yahiro", "YH050164")
	Mess("What's with that saccharine epithet? ...Ah, wait aÅ^minute.")
	hitretResult := Hitret(39261)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B26a", 0, 0)
	SetBustup("CG02_08L", 0, 0)
	Talk("Haruka", "")
	Mess("W- What...?")
	hitretResult := Hitret(39262)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM13", 0)
	Talk("êSÇÃê∫", "")
	Mess("Yahiro-san abruptly approached me and grabbed the collarÅ^of my shirt.")
	hitretResult := Hitret(39263)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("For all her beauty, Yahiro-san's angered face reallyÅ^packed a punch... Truth be told, it was rather scary.")
	hitretResult := Hitret(39264)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050165")
	Mess("I assume you understand already, but just to be clear...")
	hitretResult := Hitret(39265)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I- I won't tell anyone about this, really!")
	hitretResult := Hitret(39266)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050166")
	Mess("If a word of this gets out, you are the one to takeÅ^responsibility!")
	hitretResult := Hitret(39267)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("N- Now, isn't that rather unreasonabl... eh...")
	hitretResult := Hitret(39268)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As she strangled my neck, I choked for a moment.")
	hitretResult := Hitret(39269)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG02_06L", 0, 0)
	Talk("Yahiro", "YH050167")
	Mess("I would not mind getting rid of you right here and now,Å^but since you are an outsider, that might attractÅ^unnecessary attention.")
	hitretResult := Hitret(39270)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050168")
	Mess("Since I'm all for peaceful conflict resolution, you willÅ^have to pretend this never happened.")
	hitretResult := Hitret(39271)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Wow, threatening me, that does not seem peaceful atÅ^all...")
	hitretResult := Hitret(39272)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050169")
	Mess("...I also dislike those who dodge the question...")
	hitretResult := Hitret(39273)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("O- Of course that was a joke! Very peaceful indeed! IÅ^won't tell anyone anything about what I did not see hereÅ^today.")
	hitretResult := Hitret(39274)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050170")
	Mess("You'd do good to remember that.")
	hitretResult := Hitret(39275)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG02_06M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("At last, Yahiro-san let go of her grip and I steppedÅ^back from her a bit.")
	hitretResult := Hitret(39276)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Coughing a bit, I took another glance at her.")
	hitretResult := Hitret(39277)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In a stark contrast to her usual unkempt self, herÅ^current appearance was fascinating.")
	hitretResult := Hitret(39278)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Well, what's inside did not change one bit though...")
	hitretResult := Hitret(39279)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050171")
	Mess("Geez, you're always annoying me, but it's so hard to getÅ^rid of you.")
	hitretResult := Hitret(39280)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Who would have thought that Yahiro-san is the oneÅ^writing that blog...")
	hitretResult := Hitret(39281)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG02_08M", 0, 0)
	Talk("Yahiro", "YH050172")
	Mess("...It seems someone here knows way too much for his ownÅ^good.")
	hitretResult := Hitret(39282)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Quake(0, 16, 1, 2, 30)
	Talk("Haruka", "")
	Mess("Hii!?")
	hitretResult := Hitret(39283)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Damn, I've let something unnecessary slip out of myÅ^mouth.")
	hitretResult := Hitret(39284)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("W- Wait... It's not like I told anyone! There is no needÅ^at all to crack your knuckles in an intimidating manner!")
	hitretResult := Hitret(39285)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG02_08L", 0, 0)
	Talk("Yahiro", "YH050173")
	Mess("Ok, you will just have to die here.")
	hitretResult := Hitret(39286)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hii... Someone help me!")
	hitretResult := Hitret(39287)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050143")
	Mess("Aah! Found Hiro-neechan!")
	hitretResult := Hitret(39288)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just as she heard this voice, Yahiro-san stopped herÅ^raised fists mid-air.")
	hitretResult := Hitret(39289)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01S", 0, 0)
	SetBustup("CG02_05L", 0, 0)
	Talk("Yahiro", "YH050174")
	Mess("Geh! Akira...")
	hitretResult := Hitret(39290)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("A- Amatsume... Nice timing...")
	hitretResult := Hitret(39291)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03M", 0, 0)
	SetBustup("CG02_05M", 0, 0)
	Talk("Akira", "AK050144")
	Mess("Whoah... Hiro-neechan so cute~!")
	hitretResult := Hitret(39292)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050175")
	Mess("S- Stop that! Don't look at me like that!")
	hitretResult := Hitret(39293)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050145")
	Mess("Ne~Ne~, what were you two doing here? Could it be, IÅ^interrupted something interesting?")
	hitretResult := Hitret(39294)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050176")
	Mess("Exactly! So just leave!")
	hitretResult := Hitret(39295)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050146")
	Mess("I see~ Sorry for interrupting!")
	hitretResult := Hitret(39296)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	Talk("êSÇÃê∫", "")
	Mess("Amatsume, how gullible can you be!?")
	hitretResult := Hitret(39297)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("Aah! Amatsume! Wait a bit!!")
	hitretResult := Hitret(39298)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050177")
	Mess("Oi, you bastard! Where do you think you are going!!")
	hitretResult := Hitret(39299)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B07a", 0, 0)
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK050147")
	Mess("Oh? Haru-kun?")
	hitretResult := Hitret(39300)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haa, haa... good, I managed to catch you...")
	hitretResult := Hitret(39301)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050148")
	Mess("What about Hiro-neechan? I won't interrupt you two, youÅ^know?")
	hitretResult := Hitret(39302)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uh... You really saved me by appearing there...")
	hitretResult := Hitret(39303)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK050149")
	Mess("Huuh... I wonder what was Hiro-neechan up to?")
	hitretResult := Hitret(39304)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, well... I would not even like to think about that...")
	hitretResult := Hitret(39305)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even if it's Amatsume, I have a feeling discussing suchÅ^things would bring unfortunate consequences.")
	hitretResult := Hitret(39306)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_14M", 0, 0)
	Talk("Akira", "AK050150")
	Mess("Wh... What?")
	hitretResult := Hitret(39307)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume expressed a face of concern as she propped upÅ^her chin with her finger.")
	hitretResult := Hitret(39308)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("However, I'm sorry. There is no way I can satisfy yourÅ^curiosity.")
	hitretResult := Hitret(39309)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050178")
	Mess("Tch, Akira too now...")
	hitretResult := Hitret(39310)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG02_02M", 0, 0)
	SetBustup("CC06_12M", 0, 0)
	Talk("Akira", "AK050151")
	Mess("Ah, Hiro-neechan! Wow, so much luggage!")
	hitretResult := Hitret(39311)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From behind, Yahiro-san approached carrying a camera andÅ^a tripod.")
	hitretResult := Hitret(39312)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When you look at her like that, she seems pretty heavilyÅ^equipped.")
	hitretResult := Hitret(39313)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Not really an elegant and refined image.")
	hitretResult := Hitret(39314)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050179")
	Mess("Damn... Oi, Haruka.")
	hitretResult := Hitret(39315)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("H- Hai!")
	hitretResult := Hitret(39316)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG02_06M", 0, 0)
	Talk("Yahiro", "YH050180")
	Mess("Don't you forget! If you go around blabbering aboutÅ^this, pain and misfortune awaits!")
	hitretResult := Hitret(39317)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("O- Of course I'm fully aware of that!")
	hitretResult := Hitret(39318)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH050181")
	Mess("And you, Akira! You should not let your mouth run tooÅ^much, especially about the events of today.")
	hitretResult := Hitret(39319)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03M", 0, 0)
	Talk("Akira", "AK050152")
	Mess("Yesyes! I understand!")
	hitretResult := Hitret(39320)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG02_05M", 0, 0)
	Talk("Yahiro", "YH050182")
	Mess("...Damn... Why do I have to experience such embarrassingÅ^happenings...")
	hitretResult := Hitret(39321)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	ScPlaySe("SE302", 0)
	BustupClear(8)
	Talk("êSÇÃê∫", "")
	Mess("As her dreadful threatening attitude disappearedÅ^somewhere, Yahiro-san left the shrine grounds whileÅ^grumbling to herself.")
	hitretResult := Hitret(39322)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What was all that about...?")
	hitretResult := Hitret(39323)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though I learned the identity of the "MysteriousÅ^Beauty", I still don't have the faintest idea about theÅ^whole deal with the blog.")
	hitretResult := Hitret(39324)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And it's not like I can share my findings withÅ^Ryouhei...")
	hitretResult := Hitret(39325)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Now that's some secret I never wanted to be carryingÅ^around...")
	hitretResult := Hitret(39326)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK050153")
	Mess("Hiro-neechan, bye-bye~!")
	hitretResult := Hitret(39327)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I stood near to Amatsume as she waved her handÅ^nonchalantly, and could not help but feel cold sweatÅ^running down my back despite this summer heat.")
	hitretResult := Hitret(39328)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopSe()
	EyeCatchEnter("DATE", 0)
	Change("00_e013a")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

