Include"yosuga.ch"

Function sc00_C004()
	ScenarioEnter()
	SetCg("B12a", 0, 0)
	ScPlayBgm("BGM05", 0)
	Talk("Akira", "AK020111")
	Mess("Sora-chan, how was the first day in school?")
	hitretResult := Hitret(18407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR020041")
	Mess("...I'm tired.")
	hitretResult := Hitret(18408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If you're tired from that, will it be hard from now on?")
	hitretResult := Hitret(18409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora's first day in school ended peacefully.")
	hitretResult := Hitret(18410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora was in bad mood, but since she didn't escape in theÅ^middle of the day, maybe she could keep it up.")
	hitretResult := Hitret(18411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I tried to go to Sora's classroom and kind of asked aÅ^nice classmate of hers to help me with some things, so IÅ^gave my regards in advance.")
	hitretResult := Hitret(18412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_06M", 0, 0)
	Talk("Sora", "SR020042")
	Mess("Nn..... Uuu.....")
	hitretResult := Hitret(18413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020112")
	Mess("Huh?")
	hitretResult := Hitret(18414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What happened, Sora?")
	hitretResult := Hitret(18415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020043")
	Mess("........ Mosquito.")
	hitretResult := Hitret(18416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Before I noticed it, Sora has been bitten and the backÅ^of her hand became swollen.")
	hitretResult := Hitret(18417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Aah... Don't scratch it too much, it will leave a scar.")
	hitretResult := Hitret(18418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_04M", 0, 0)
	Talk("Sora", "SR020044")
	Mess("Uuuu... It's itchy...")
	hitretResult := Hitret(18419)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("However, not being able to resist it, she ended upÅ^scratching the bitten area.")
	hitretResult := Hitret(18420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK020113")
	Mess("Aah, season for mosquitos is coming, huh...")
	hitretResult := Hitret(18421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, before you even notice it, they get in yourÅ^room.Iif you leave your window open, a lot of them comeÅ^in and it's terrible later.")
	hitretResult := Hitret(18422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_06M", 0, 0)
	Talk("Sora", "SR020045")
	Mess("..... Hmph.")
	hitretResult := Hitret(18423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora, the one trying to repel them with hairspray,Å^turned away her eyes.")
	hitretResult := Hitret(18424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK020114")
	Mess("I my case, I don't notice it when I go sleep, but when IÅ^wake up, I'm bitten by a lot of mosquitos and it'sÅ^itching all over.")
	hitretResult := Hitret(18425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I have to go buy a mosquito coil, an anti-bug spray orÅ^something like that.")
	hitretResult := Hitret(18426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK020115")
	Mess("Haru-kun, do you have an air conditioner? If you had oneÅ^and cooled off the room, the mosquitos would go away.")
	hitretResult := Hitret(18427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh? You serious? But, we don't have air conditioner atÅ^home.")
	hitretResult := Hitret(18428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020116")
	Mess("Ahh, same in my home. Then, maybe an incense stick or aÅ^mosquito net?")
	hitretResult := Hitret(18429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("A mosquito net, huh? Well, no, I haven't searched theÅ^storehouse yet.")
	hitretResult := Hitret(18430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020117")
	Mess("Nnn, I have some left over at home so - do you want toÅ^use them?")
	hitretResult := Hitret(18431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh, is it okay?")
	hitretResult := Hitret(18432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK020118")
	Mess("Yeah, it's old so if it's okay, I'll give it to you.Å^Well then, come for it later? I'll prepare them for you.")
	hitretResult := Hitret(18433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thank you, that saved me. Isn't that great, Sora?")
	hitretResult := Hitret(18434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA01_01M", 0, 0)
	Talk("Sora", "SR020046")
	Mess("..... If Haru will put it up.....")
	hitretResult := Hitret(18435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Somehow, during summer it always feels like we're goingÅ^to lay around.")
	hitretResult := Hitret(18436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_03M", 0, 0)
	Talk("Akira", "AK020119")
	Mess("Yes, it happens! Sometimes I feel like staying at homeÅ^and lazing around too!")
	hitretResult := Hitret(18437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well then, I'll drop by later.")
	hitretResult := Hitret(18438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK020120")
	Mess("Yeah, see you soon then.")
	hitretResult := Hitret(18439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(0, 1)
	ScWait(1500, 0)
	Talk("êSÇÃê∫", "")
	Mess("After leaving my bag and changing clothes, I headed toÅ^Amatsume's home right away.")
	hitretResult := Hitret(18440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I need an electric fan or something, if I don't make anyÅ^preparations, it'll gradually get hot and it'll be hardÅ^to fall asleep.")
	hitretResult := Hitret(18441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's not like just because it's the countryside, it'llÅ^be cool here...")
	hitretResult := Hitret(18442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B07a", 0, 0)
	Transition("WIP_RL", 500)
	ScPlayEnvSe("SE406", "", 0)
	Talk("Haruka", "")
	Mess("................")
	hitretResult := Hitret(18443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The shrine grounds were neatly cleaned and there wereÅ^still traces of someone sweeping the area with a broom.")
	hitretResult := Hitret(18444)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Being so spacious, this place must take a lot of effortÅ^to clean.")
	hitretResult := Hitret(18445)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020121")
	Mess("Ah, Haru-kun! Welcome back!!")
	hitretResult := Hitret(18446)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EC05a", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Akira", "AK020122")
	Mess("You were surprisingly fast.")
	hitretResult := Hitret(18447)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, well, was I?")
	hitretResult := Hitret(18448)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE406", 1)
	ScPlayBgm("BGM13", 0)
	Talk("êSÇÃê∫", "")
	Mess("There was some kind of a bulge on Amatsume's head.")
	hitretResult := Hitret(18449)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020123")
	Mess("I'll prepare everything, so wait a minute, okay?")
	hitretResult := Hitret(18450)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("O- Okay........")
	hitretResult := Hitret(18451)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A... cat??")
	hitretResult := Hitret(18452)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There was a cat placed on top of Amatsume's head, butÅ^the person in question didn't mind it at all.")
	hitretResult := Hitret(18453)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The cat wasn't concerned with me or was just gazingÅ^blankly into mid-air.")
	hitretResult := Hitret(18454)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020124")
	Mess("Oh yes, are you sleeping together with Sora-chan?")
	hitretResult := Hitret(18455)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...No, separately. She likes to stay up late, so itÅ^would be troublesome to sleep together...")
	hitretResult := Hitret(18456)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020125")
	Mess("Is it so? What a waste, not being able to seeÅ^Sora-chan's sleeping face.")
	hitretResult := Hitret(18457)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Certainly, while sleeping she's most obedient, but...Å^She's the worst at getting up, so I don't really want toÅ^sleep together...")
	hitretResult := Hitret(18458)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("........it... it bugs me........")
	hitretResult := Hitret(18459)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My sight was fixed on Amatsume's head.")
	hitretResult := Hitret(18460)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A new hat or something... no way. It was flapping it'sÅ^tail, but Amatsume wasn't reacting at all.")
	hitretResult := Hitret(18461)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah!")
	hitretResult := Hitret(18462)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The cat glanced at me, but then again slowly averted hisÅ^eyes.")
	hitretResult := Hitret(18463)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020126")
	Mess("Then, I'd better prepare two mosquito nets, right? IsÅ^size of six tatami mats enough? If I'm not mistaken,Å^there was one...")
	hitretResult := Hitret(18464)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y- yeah... I think so...")
	hitretResult := Hitret(18465)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020127")
	Mess("Huh? Something's wrong?")
	hitretResult := Hitret(18466)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh!? Ah, well, that is... Amatsume... on your he-")
	hitretResult := Hitret(18467)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020128")
	Mess("Ah, yeah. Sorry for not serving you a cup of tea. I'llÅ^go bring it, so please wait a while.")
	hitretResult := Hitret(18468)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, it's not that!!")
	hitretResult := Hitret(18469)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B07a", 0, 0)
	Transition("WIP_MOZV", 500)
	Talk("Haruka", "")
	Mess("................................")
	hitretResult := Hitret(18470)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume with a cat placed on her head had gone away.")
	hitretResult := Hitret(18471)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("........ Is there something wrong with my eyes?")
	hitretResult := Hitret(18472)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Its as though there wasn't anything there, was I the oneÅ^acting strangely?")
	hitretResult := Hitret(18473)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("That idea alone is scary...")
	hitretResult := Hitret(18474)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EC05a", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Akira", "AK020129")
	Mess("Sorry to have kept you waiting. Here you are.")
	hitretResult := Hitret(18475)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Brought on a tray, barley tea was readily served.")
	hitretResult := Hitret(18476)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Th- Thanks...")
	hitretResult := Hitret(18477)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She's still carrying it...")
	hitretResult := Hitret(18478)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The cat, that was relaxing himself, looked like it wasÅ^feeling at home, as if that place was claimed as hisÅ^own.")
	hitretResult := Hitret(18479)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Right from the start, he was totally ignoring me and hadÅ^the nerve to just sit there.")
	hitretResult := Hitret(18480)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Maybe it's just me who doesn't know, but is alwaysÅ^carrying a cat around like that a normal thing to do?")
	hitretResult := Hitret(18481)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Mmm... Tastes good, doesn't it? Barley tea.")
	hitretResult := Hitret(18482)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Holding some barley tea in my mouth, I was observingÅ^Amatsume and the cat.")
	hitretResult := Hitret(18483)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020130")
	Mess("I never get tired of it, even though I drink it all yearÅ^round. Chilled in summer and hot in winter.")
	hitretResult := Hitret(18484)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahaha, just like a line taken from a commercial.")
	hitretResult := Hitret(18485)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020131")
	Mess("There's plenty more of it, so don't hold back, okay?")
	hitretResult := Hitret(18486)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, thanks.")
	hitretResult := Hitret(18487)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("By the way, it was bothering me for some time, but...Å^The thing on your head...")
	hitretResult := Hitret(18488)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020132")
	Mess("Uwaa! I forgot! Haru-kun came to pick up a mosquito net,Å^didn't he? I can't just lay around yet.")
	hitretResult := Hitret(18489)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B07a", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Haruka", "")
	Mess("Ah... Th- That too, but...")
	hitretResult := Hitret(18490)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("Amatsume ran once more to the back of the shrine office.")
	hitretResult := Hitret(18491)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(2000, 0)
	SetCg("B07a", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Akira", "AK020133")
	Mess("I'm back!!")
	hitretResult := Hitret(18492)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM03", 0)
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK020134")
	Mess("Sorry to have kept you waiting, I prepared two forÅ^Haru-kun and Sora-chan. If it's torn, please tell me.Å^I'll come to sew it.")
	hitretResult := Hitret(18493)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay, you really helped me, thank you. With that, we canÅ^comfortably lie down in the summer.")
	hitretResult := Hitret(18494)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020135")
	Mess("Having taken that out, I though \"Aah, it's summer time\".")
	hitretResult := Hitret(18495)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("................ It's gone.")
	hitretResult := Hitret(18496)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020136")
	Mess("Eh? What is?")
	hitretResult := Hitret(18497)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, nothing... Amatsume, isn't there something odd?")
	hitretResult := Hitret(18498)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_14M", 0, 0)
	Talk("Akira", "AK020137")
	Mess("Huh? Eh? Eeh?")
	hitretResult := Hitret(18499)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Aren't you missing something?")
	hitretResult := Hitret(18500)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_03M", 0, 0)
	Talk("Akira", "AK020138")
	Mess("Uh? Aah! You're talking about metal fittings?")
	hitretResult := Hitret(18501)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Th- Those are here, but... Hey, up until now...")
	hitretResult := Hitret(18502)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020139")
	Mess("Huuh~? What could it be? Was I acting strange?")
	hitretResult := Hitret(18503)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Is she doing this on purpose? No, assuming she isn't,Å^Amatsume's a natural airhead after all!?")
	hitretResult := Hitret(18504)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hey, on your head before...")
	hitretResult := Hitret(18505)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020140")
	Mess("Huh? ... On my head?")
	hitretResult := Hitret(18506)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	ScPlaySe("SE311", 0)
	Talk("êSÇÃê∫", "")
	Mess("In that instant, something walked between me andÅ^Amatsume.")
	hitretResult := Hitret(18507)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020141")
	Mess("Kya!")
	hitretResult := Hitret(18508)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM06", 0)
	SetCg("EC05b", 0, 0)
	Transition("WIP_LR", 500)
	Quake(0, 16, 0, 2, 30)
	Talk("Akira", "AK020142")
	Mess("Hyaaan, Master, it tickles!")
	hitretResult := Hitret(18509)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ma- Master!?")
	hitretResult := Hitret(18510)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020143")
	Mess("Uaaah, my neck is my weak spot, aah! Ahnn!")
	hitretResult := Hitret(18511)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The cat went on top of Amatsume's head and wasÅ^struggling. He was brushing against the nape of her neckÅ^with his tail and hind legs and it looked like he wasÅ^ticking her.")
	hitretResult := Hitret(18512)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020144")
	Mess("Master, behave yourself. It's enough, you're such aÅ^pampered one...")
	hitretResult := Hitret(18513)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020145")
	Mess("Kyaa, yaan, ahh! Nfuu, aaahn!!")
	hitretResult := Hitret(18514)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Are- Are you okay... Amatsume??")
	hitretResult := Hitret(18515)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume was in a strange kind of agony, so it caught myÅ^interest too.")
	hitretResult := Hitret(18516)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020146")
	Mess("Uuuh! Ah, hyaa... time out, time out, Maste~r!")
	hitretResult := Hitret(18517)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020147")
	Mess("Ah, ah, ah... Uh... Even if you beg for it, it isn'tÅ^dinner time yet.")
	hitretResult := Hitret(18518)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE311", 0)
	SetCg("EC05a", 0, 0)
	Talk("Akira", "AK020148")
	Mess("It was out of nowhere, it surprised you, huh?")
	hitretResult := Hitret(18519)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("N- No... It surprised me that he was placed on your headÅ^the whole time.")
	hitretResult := Hitret(18520)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020149")
	Mess("Aah, Master's a spoiled one, so he gets on whenever heÅ^pleases.")
	hitretResult := Hitret(18521)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020150")
	Mess("Maybe he played a lot today? He has been pleading forÅ^food for some time now.")
	hitretResult := Hitret(18522)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So, it's a usual thing, huh........")
	hitretResult := Hitret(18523)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020151")
	Mess("Ah, Haru-kun, was it first time you saw him? Well, thenÅ^it's a first meeting for Master too, right? Master, thisÅ^is my friend, Haru-kun.")
	hitretResult := Hitret(18524)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("................................")
	hitretResult := Hitret(18525)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("........ P- Pleased to meet you........")
	hitretResult := Hitret(18526)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("................................")
	hitretResult := Hitret(18527)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was ignored........")
	hitretResult := Hitret(18528)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Somehow or other, it seems that either I wasn't presentÅ^in Master Cat's field of vision or I was in around theÅ^last place in ranking of his concerns.")
	hitretResult := Hitret(18529)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("For how long have you keep him now? When I came here aÅ^long time ago, he didn't seem to be around.")
	hitretResult := Hitret(18530)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020152")
	Mess("Hmm, for how long was it? It feels like he was here asÅ^long as I remember.")
	hitretResult := Hitret(18531)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020153")
	Mess("But, after eating food, he vanishes right away.Å^Patrolling the neighbourhood is Master's hobby.")
	hitretResult := Hitret(18532)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020154")
	Mess("When Haru-kun moved here too, it was Master who let meÅ^know about it.")
	hitretResult := Hitret(18533)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see........ Tsk tsk tsk tsk tsk........ Ah!?")
	hitretResult := Hitret(18534)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When I was about to stroke Master's throat, he repelledÅ^me with his paws.")
	hitretResult := Hitret(18535)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020155")
	Mess("Hey, Master, you can't do that! Haru-kun, sorry, it'sÅ^almost dinner time, so he's antsy.")
	hitretResult := Hitret(18536)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020156")
	Mess("Next time, bring some snacks with you. He likes breadÅ^crusts, so if you feed him with them, I think he willÅ^remember you.")
	hitretResult := Hitret(18537)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Bread cursts? Not fish but that, he is unusual, huh?Å^Well then, I'll bring those next time. By the way, whyÅ^\"Master\"?")
	hitretResult := Hitret(18538)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020157")
	Mess("I wonder why... Grandpa was calling him that, so somehowÅ^I ended up copying him, though...")
	hitretResult := Hitret(18539)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020158")
	Mess("When I was little, I often carried around Master on myÅ^back, maybe because he was kind of like an adoptedÅ^child.")
	hitretResult := Hitret(18540)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's why he grew up to be so proud of himself, huh...")
	hitretResult := Hitret(18541)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Master took a glance at me and once again turned awayÅ^his face.")
	hitretResult := Hitret(18542)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There will be many hardships ahead before he recognisesÅ^me.")
	hitretResult := Hitret(18543)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020159")
	Mess("Don't worry, you'll get along great. Right, Master?")
	hitretResult := Hitret(18544)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE312", 0)
	SetCg("EC05b", 0, 0)
	Talk("Akira", "AK020160")
	Mess("Hya!? Ahaha, I told you, you're tickling me! Aha,Å^ahahah! Ahahahahaha!!")
	hitretResult := Hitret(18545)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020161")
	Mess("I get it, I get it, so!! Aahn! Ahahahaha! Master, I'llÅ^give you food, so...")
	hitretResult := Hitret(18546)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020162")
	Mess("Well then, Master, let's go.")
	hitretResult := Hitret(18547)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE311", 0)
	SetCg("B07a", 0, 0)
	SetBustup("CC06_02M", 0, 0)
	Transition("WIP_MOZV", 500)
	Talk("êSÇÃê∫", "")
	Mess("Master jumped from top of her head, landed and nuzzledÅ^his head and body against Amatsume's legs.")
	hitretResult := Hitret(18548)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well then, I'll be on my way. Thank you for treating meÅ^with tea.")
	hitretResult := Hitret(18549)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020163")
	Mess("You are welcome. If you're okay with only that much, youÅ^can come to talk anytime.")
	hitretResult := Hitret(18550)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, that helps.")
	hitretResult := Hitret(18551)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK020164")
	Mess("Well then, Haru-kun, shall we go shopping together?")
	hitretResult := Hitret(18552)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Erm... I have to make dinner...")
	hitretResult := Hitret(18553)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020165")
	Mess("Ah, okay.")
	hitretResult := Hitret(18554)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("??")
	hitretResult := Hitret(18555)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Amatsume took Master along and went down the shrine'sÅ^stairs.")
	hitretResult := Hitret(18556)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Shall we go shopping? But, today's not mobileÅ^supermarket's day, so...")
	hitretResult := Hitret(18557)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("........Where are we going?")
	hitretResult := Hitret(18558)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(2000, 0)
	Change("00_c005")
	Return
	Change("EXIT_SCENARIO")
EndFunc

