Include"yosuga.ch"

Function sc00_C014D()
	ScenarioEnter()
	ScPlayEnvSe("SE400", "", 0)
	SetCg("B23a", 0, 0)
	SetBustup("CC07_02M", 0, 0)
	Transition("MOZCIR", 500)
	Talk("Akira", "AK020669")
	Mess("Ahahaha! Haru-kun come here-!")
	hitretResult := Hitret(21097)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("W-wait a second...")
	hitretResult := Hitret(21098)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I chase after Amatsume who energetically holding theÅ^floater was running.")
	hitretResult := Hitret(21099)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After eating lunch, leaving Sora who normally fellÅ^asleep to Nao-chan, Ryouhei and the others with ModokaÅ^went out to swim.")
	hitretResult := Hitret(21100)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I thought about staying back with Nao-chan, but I wasÅ^invited to play by the water from Amatsume.")
	hitretResult := Hitret(21101)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Wouldn't it have been better for Amatsume to go swimmingÅ^with everyone else?")
	hitretResult := Hitret(21102)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020670")
	Mess("Aha, I'm not really good at swimming, so like this...")
	hitretResult := Hitret(21103)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Saying that, Amatsume puts her bottom on the ringÅ^portion of the floating ring.")
	hitretResult := Hitret(21104)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020671")
	Mess("Ei!!")
	hitretResult := Hitret(21105)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE400", 0)
	ScPlayBgm("BGM06", 0)
	BustupClear()
	ScPlaySe("SE204", 0)
	Talk("êSÇÃê∫", "")
	Mess("And jumping into the ocean with her bottom, she floatsÅ^on top the of waves.")
	hitretResult := Hitret(21106)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020672")
	Mess("I like taking it easy floating around like this.")
	hitretResult := Hitret(21107)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Being rocked by the waves, while being spun around likeÅ^a flower on top of the water she is tossed about.")
	hitretResult := Hitret(21108)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That looks like it'd feel good just from looking at it.")
	hitretResult := Hitret(21109)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020673")
	Mess("Yeah, at this rate I just might be interested in leavingÅ^myself to the waves and see where I'd end up going. Ah,Å^Haru-kun you want to try as well?")
	hitretResult := Hitret(21110)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... Thanks, Amatsume.")
	hitretResult := Hitret(21111)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_01M", 0, 0)
	Talk("Akira", "AK020674")
	Mess("Huh? What's wrong all of the sudden?")
	hitretResult := Hitret(21112)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hanging out with me who can't swim.")
	hitretResult := Hitret(21113)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_03M", 0, 0)
	Talk("Akira", "AK020675")
	Mess("No, the ocean isn't just for swimming, if you want toÅ^play doing all sorts of things that's fine.")
	hitretResult := Hitret(21114)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK020676")
	Mess("And even if you can't swim, if you are going to enterÅ^the tub that's enough. Take this!")
	hitretResult := Hitret(21115)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE205", 0)
	Talk("Haruka", "")
	Mess("Uwa!? Now you've done it!")
	hitretResult := Hitret(21116)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE205", 0)
	SetBustup("CC07_04M", 0, 0)
	Talk("Akira", "AK020677")
	Mess("Ahahaha!!")
	hitretResult := Hitret(21117)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Both of us who were in the water up to our knees, foolsÅ^around splashing water all eachother.")
	hitretResult := Hitret(21118)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_03M", 0, 0)
	Talk("Akira", "AK020678")
	Mess("Haru-kun, here I go!! Ei!!")
	hitretResult := Hitret(21119)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oo!?")
	hitretResult := Hitret(21120)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE009", 0)
	Talk("êSÇÃê∫", "")
	Mess("Amatsume all of the sudden came at me throwing a ball,Å^and at that rate I receive it and throw it back.")
	hitretResult := Hitret(21121)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_02M", 0, 0)
	Talk("Akira", "AK020679")
	Mess("Want to play volleyball? If you can't catch it, it's aÅ^punishment game!")
	hitretResult := Hitret(21122)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Alright! Is the place fine being here? It's not good forÅ^the footing but-")
	hitretResult := Hitret(21123)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_04M", 0, 0)
	Talk("Akira", "AK020680")
	Mess("That makes it interesting! Here we go!")
	hitretResult := Hitret(21124)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Alright!")
	hitretResult := Hitret(21125)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE009", 0)
	Talk("êSÇÃê∫", "")
	Mess("I lightly receive the ball, and pass the ball high.")
	hitretResult := Hitret(21126)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_10M", 0, 0)
	Shake(2)
	Talk("Akira", "AK020681")
	Mess("Anya!? Awawawawa!!")
	hitretResult := Hitret(21127)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume who was surprised at the high ball panicklyÅ^moves over to the falling location.")
	hitretResult := Hitret(21128)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_03M", 0, 0)
	Jump(2, 0)
	ScPlaySe("SE009", 0)
	Talk("Akira", "AK020682")
	Mess("(Sore)!! (TL Note: Just think of more creative ways ofÅ^TLing all these "Sore's"... this is getting annoying toÅ^localize in English.")
	hitretResult := Hitret(21129)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nice catch! Alright, my turn...")
	hitretResult := Hitret(21130)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE009", 0)
	Talk("êSÇÃê∫", "")
	Mess("I somewhat teasingly return the ball which was wellÅ^tossed back towards Amatsume's right side.")
	hitretResult := Hitret(21131)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_06M", 9, 0)
	Talk("Akira", "AK020683")
	Mess("Ah, hyan, that's not fair! Sore!!")
	hitretResult := Hitret(21132)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE009", 0)
	Talk("êSÇÃê∫", "")
	Mess("Amatsume somewhat catches up to it, and skillfullyÅ^receives it and returns it back.")
	hitretResult := Hitret(21133)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("How about this time? Sore!!!")
	hitretResult := Hitret(21134)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE009", 0)
	Talk("êSÇÃê∫", "")
	Mess("This time I toss the boll up towards Amatsume's leftÅ^side.")
	hitretResult := Hitret(21135)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_05M", 3, 0)
	Talk("Akira", "AK020684")
	Mess("Ahn! Haru-kun you are such a bully!!")
	hitretResult := Hitret(21136)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Shaking from left to right Amatsume is tossed about. IÅ^wonder if this was a bit wrong of me... is what IÅ^thought, but Amatsume trying hard caught up to all ofÅ^them.")
	hitretResult := Hitret(21137)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_02M", 0, 0)
	Talk("Akira", "AK020685")
	Mess("Hahah... nsho!!")
	hitretResult := Hitret(21138)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE009", 0)
	Jump(2, 0)
	Talk("Haruka", "")
	Mess("Oo!?")
	hitretResult := Hitret(21139)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The returned ball without any vigor, seemingly was goingÅ^to drop in the area in front.")
	hitretResult := Hitret(21140)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE009", 0)
	Quake(0, 16, 0, 2, 30)
	Talk("Haruka", "")
	Mess("Ei!!")
	hitretResult := Hitret(21141)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I panickly dashed forward, and somehow receive andÅ^return it.")
	hitretResult := Hitret(21142)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The ball slowly draws a parabola, and flies towards inÅ^front of Amatsume.")
	hitretResult := Hitret(21143)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_14M", 0, 0)
	Talk("Akira", "AK020686")
	Mess("Fufufu-")
	hitretResult := Hitret(21144)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Eh...")
	hitretResult := Hitret(21145)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_04M", 0, 0)
	ScPlaySe("SE003", 0)
	Talk("Akira", "AK020687")
	Mess("Ei!!!")
	hitretResult := Hitret(21146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume, shows that grin on her face, and spikes theÅ^ball as hard as she could.")
	hitretResult := Hitret(21147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oo!? Not good!!!")
	hitretResult := Hitret(21148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("The ball goes beyond my head and flies off.")
	hitretResult := Hitret(21149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ottototototo!!!")
	hitretResult := Hitret(21150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("While having my legs taken by the ocean, I panickly backÅ^up and reach the go for the ball.")
	hitretResult := Hitret(21151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ku!! Sore!!! Owa...!?")
	hitretResult := Hitret(21152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE208", 0)
	Quake(0, 16, 1, 2, 30)
	Talk("êSÇÃê∫", "")
	Mess("I somehow catch the ball and return it to Amatsume, butÅ^my balance is broken and I fall on my butt.")
	hitretResult := Hitret(21153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The ball slowly flies towards Amatsume.")
	hitretResult := Hitret(21154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_04S", 0, 0)
	Talk("Akira", "AK020688")
	Mess("Haru-kun, sorry.")
	hitretResult := Hitret(21155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...!!")
	hitretResult := Hitret(21156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume extends a hand...")
	hitretResult := Hitret(21157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE017", 0)
	Talk("Akira", "AK020689")
	Mess("Choi!")
	hitretResult := Hitret(21158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And lightly strikes the ball.")
	hitretResult := Hitret(21159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("Ku!!! Uooo!!!")
	hitretResult := Hitret(21160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I get up, and head for the ball ferociously dashing.")
	hitretResult := Hitret(21161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The ball again slowly falls where I was before.")
	hitretResult := Hitret(21162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I had been teasing Amatsume front left to right, thisÅ^time I was being countered from forward to back.")
	hitretResult := Hitret(21163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Make it in time!!")
	hitretResult := Hitret(21164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Like I had been headsliding I jump forward, and extendÅ^my hand towards the ball.")
	hitretResult := Hitret(21165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	ScPlayEnvSe("SE400", "", 0)
	ScPlaySe("SE204", 0)
	Quake(0, 16, 1, 2, 30)
	SetBustup("CC07_12M", 0, 0)
	Talk("Akira", "AK020690")
	Mess("Anya--!?")
	hitretResult := Hitret(21166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("I give it all I have and jump into the ocean.")
	hitretResult := Hitret(21167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B23a", 0, 0)
	Transition("WIP_MOZH", 500)
	Talk("Haruka", "")
	Mess("Buha!?")
	hitretResult := Hitret(21168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Suddenly jumping and aiming for the ball, even though itÅ^was shallow, I who was panicking about having dived in,Å^hurriedly raises my face.")
	hitretResult := Hitret(21169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was shallow to the point that you could push yourselfÅ^up and your face would come out, but my heart poundsÅ^from a little fear.")
	hitretResult := Hitret(21170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Ah...um... Amatsume...")
	hitretResult := Hitret(21171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se012", 0)
	Talk("êSÇÃê∫", "")
	Mess("At the time as I was getting up, I bump into somethingÅ^soft.")
	hitretResult := Hitret(21172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_11L", 0, 0)
	Talk("Akira", "AK020691")
	Mess("Haru-kun, are you okay?")
	hitretResult := Hitret(21173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE400", 0)
	ScPlayBgm("BGM17", 0)
	Talk("êSÇÃê∫", "")
	Mess("Amatsume and I sitting down were looking at eachother.")
	hitretResult := Hitret(21174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_05L", 0, 0)
	Talk("Akira", "AK020692")
	Mess("Haru-kun was running so fast, I was surprised, andÅ^(you/Haru-kun) came flying in, I'm really wet~.")
	hitretResult := Hitret(21175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Little droplets fall from the front of Amatsume's hair.")
	hitretResult := Hitret(21176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Th time I had been vigorously jumping foward, it seemsÅ^that I splashed water onto Amatsume.")
	hitretResult := Hitret(21177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sorry... Did I (not) bump into you?")
	hitretResult := Hitret(21178)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_11L", 0, 0)
	Talk("Akira", "AK020693")
	Mess("It's okay, I was just surprised from the water splashÅ^that I fell down.")
	hitretResult := Hitret(21179)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("and Amatsume who in one stride was astonished, stickilyÅ^sat back down and smiled.")
	hitretResult := Hitret(21180)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see... huh?")
	hitretResult := Hitret(21181)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A ball floating along the waves came flowing to us.")
	hitretResult := Hitret(21182)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("In the end, I made it in time?")
	hitretResult := Hitret(21183)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_01L", 0, 0)
	Talk("Akira", "AK020694")
	Mess("I also was too shocked and don't really know, that's whyÅ^we can call this a draw.")
	hitretResult := Hitret(21184)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I see... was it wrong for me to do something so petty toÅ^try and win I wonder...?")
	hitretResult := Hitret(21185)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_02L", 0, 0)
	Talk("Akira", "AK020695")
	Mess("You have to play fair and square. Haru-kun you are a badÅ^person aren't you?")
	hitretResult := Hitret(21186)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE205", 0)
	Talk("Haruka", "")
	Mess("Ah... S-sorry... it's cold.")
	hitretResult := Hitret(21187)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume scooping water with her two hands throws itÅ^onto my head.")
	hitretResult := Hitret(21188)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_03L", 0, 0)
	Talk("Akira", "AK020696")
	Mess("Hehe, but it was fun.")
	hitretResult := Hitret(21189)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...I was looking forward to the punishment game though.")
	hitretResult := Hitret(21190)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC07_11L", 0, 0)
	Talk("Akira", "AK020697")
	Mess("Haha, me as well, it's a bit of a shame.")
	hitretResult := Hitret(21191)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We somewhat become a bit odd, and while sitting on topÅ^of the water laugh at eachother.")
	hitretResult := Hitret(21192)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Like children, forgetting about ourselves and playingÅ^was fun.")
	hitretResult := Hitret(21193)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume is this close to I who am a guy, yet she alwaysÅ^defensivelessly smiles.")
	hitretResult := Hitret(21194)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Hanging out with me without staggering, (Editor insertÅ^phrase along the lines of (That's good, I wish I couldÅ^do that, etc.))...")
	hitretResult := Hitret(21195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The natural atmosphere that Amatsume holds, I began toÅ^like.")
	hitretResult := Hitret(21196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, it's just that she is so defenseless, that I beginÅ^to become concerned and my heart pounds.")
	hitretResult := Hitret(21197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("Even now, that figure of hers having her body drenchedÅ^in water with her swimming atire attached, my eyes areÅ^stolen by it.")
	hitretResult := Hitret(21198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Change("00_c014e")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

