Include"yosuga.ch"

Function sc00_Z003()
	ScenarioEnter()
	ScPlayBgm("BGM07", 0)
	SetCg("B34b", 400, 0)
	Talk("Haruka", "")
	Mess("As I thought, first comes a bike...")
	hitretResult := Hitret(373)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I had thought it'd be good to buy even more thanÅ^yesterday for the future, but I'd completely forgottenÅ^that I had to carry all this home.")
	hitretResult := Hitret(374)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The plastic bags I was holding in both hands containedÅ^rice, meat, drinks, as well as sweets and jam for Sora.")
	hitretResult := Hitret(375)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Their weight most likely exceeded 10kg easily.")
	hitretResult := Hitret(376)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Unable to support their weight, the plastic bags wereÅ^starting to stretch. If I don't move carefully...WellÅ^even now, it seems like they'll tear soon.")
	hitretResult := Hitret(377)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("For Sora's sake I have to hurry up and get home...But ifÅ^I hurry, the bags will break...")
	hitretResult := Hitret(378)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Guuuu.")
	hitretResult := Hitret(379)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The bags were digging into my finger joints, and theyÅ^were starting to get numb...Maybe I should rest a bit?")
	hitretResult := Hitret(380)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...HaaaaÅ`")
	hitretResult := Hitret(381)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After gently putting the bags down, I could finally feelÅ^the blood flowing in my freed fingers.")
	hitretResult := Hitret(382)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("How much longer is it to the house...")
	hitretResult := Hitret(383)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It feels as if I've walked a long way, but judging fromÅ^the scenery which I'd started to remember, I'm probablyÅ^around halfway.")
	hitretResult := Hitret(384)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE156", 0)
	Talk("Haruka", "")
	Mess("Hmm? Email?")
	hitretResult := Hitret(385)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE153", 0)
	Talk("êSÇÃê∫", "")
	Mess("Subject: Bike")
	hitretResult := Hitret(386)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A mail with no text, just a hyperlink....It must be fromÅ^Sora.")
	hitretResult := Hitret(387)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Bike? To do with our conversation just before?")
	hitretResult := Hitret(388)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Let's see what we have here...")
	hitretResult := Hitret(389)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After opening the hyperlink, a picture of an electricÅ^bike was displayed.")
	hitretResult := Hitret(390)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("She thinks I should get one of these?")
	hitretResult := Hitret(391)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'd be great if I had a bike right at this moment...ThatÅ^said, I'm in the middle of realizing that I can't liveÅ^without one.")
	hitretResult := Hitret(392)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("To send a picture of a bike at this exact moment...makesÅ^me really admire the fact that we're twins.")
	hitretResult := Hitret(393)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The price is...8...800,000?! What?")
	hitretResult := Hitret(394)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("You could easily buy a bike for 10000 yen theseÅ^days...for this amount you could get a second-handÅ^motorbike.")
	hitretResult := Hitret(395)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Hmm...There's no way Sora's thinking of riding itÅ^herself, but on the other hand, I doubt she's doing thisÅ^for me...")
	hitretResult := Hitret(396)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Picked it because she could ride behind me withoutÅ^slowing me down...No doubt about it. I'd bet on it.")
	hitretResult := Hitret(397)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Geez...That's so like her.")
	hitretResult := Hitret(398)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I couldn't help having a bitter smile, but.... I guess IÅ^can be thankful for the change of pace during the break.")
	hitretResult := Hitret(399)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well then, time to get going...*grunt*")
	hitretResult := Hitret(400)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The fact that she sent an email must be a sign thatÅ^she's probably starting to get bored and ill-tempered.")
	hitretResult := Hitret(401)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	Talk("êSÇÃê∫", "")
	Mess("Thinking that, I tried to raise the heavy bags, when...")
	hitretResult := Hitret(402)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se018", 0)
	Quake(0, 16, 1, 2, 30)
	Talk("Haruka", "")
	Mess("...HUH?!")
	hitretResult := Hitret(403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A bag I was holding broke with a snap, and the contentsÅ^of the destroyed bag flew out.")
	hitretResult := Hitret(404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("*Sigh*...")
	hitretResult := Hitret(405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Now what am I supposed to do...")
	hitretResult := Hitret(406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just as I was looking absolutely dumbfounded at theÅ^remains of the plastic bag in my hand...")
	hitretResult := Hitret(407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("VoiceÅsìﬁèèÅt", "NO000001")
	Mess("...You okay?")
	hitretResult := Hitret(408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Eh?")
	hitretResult := Hitret(409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A hand reached out from behind me and gently picked upÅ^the things that had fallen from the tattered bag.")
	hitretResult := Hitret(410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_05M", 0, 0)
	Talk("Haruka", "")
	Mess("Eh? Ah....Thanks...")
	hitretResult := Hitret(411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("GirlÅsìﬁèèÅt", "NO000002")
	Mess("Seems quite packed.")
	hitretResult := Hitret(412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah...Well...")
	hitretResult := Hitret(413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Forgetting to start picking it up myself, I tried toÅ^chase that outstretched arm with my eyes...when our eyesÅ^met.")
	hitretResult := Hitret(414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("GirlÅsìﬁèèÅt", "NO000003")
	Mess("......")
	hitretResult := Hitret(415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide()
	SetCg("EB01b", 0, 0)
	Zoom(550, 180, 400, 300, 1000, 0, 0)
	Update(1)
	ScWait(500, 0)
	SetCg("B34b", 400, 0)
	SetBustup("CB01_01M", 0, 0)
	Flush("WHITE", 2000, "")
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....Eh?")
	hitretResult := Hitret(417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01L", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("That face...")
	hitretResult := Hitret(418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In my head, a faint, fleeting image suddenly becameÅ^clear and definite.")
	hitretResult := Hitret(420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide()
	SetCgRGB(255, 255, 255)
	Transition("", 1000)
	Update()
	SetCg("EB01b", 0, 0)
	Update(1)
	ScWait(1000, 0)
	SetCg("B34b", 400, 0)
	SetBustup("CB01_13L", 0, 0)
	Flush("WHITE", 2000, "")
	Talk("GirlÅsìﬁèèÅt", "NO000004")
	Mess("*Gasp*")
	hitretResult := Hitret(421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Hey...")
	hitretResult := Hitret(422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The girl's eyes opened wide with an expression of shock.Å^Her face then quickly because flushed, changing into anÅ^expression of happiness.")
	hitretResult := Hitret(423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01L", 0, 0)
	ScPlayBgm("BGM08", 0)
	Talk("Girl & HarukaÅsìﬁèèÅt", "NO000005")
	Mess("Haru-chan?Å^Nao-chan?")
	hitretResult := Hitret(424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The two words came out at the same time.")
	hitretResult := Hitret(425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Long time n-...Whaa?!")
	hitretResult := Hitret(426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EB08a", 0, 0)
	Update()
	Quake(0, 32, 1, 1, 30)
	Talk("Nao", "NO000006")
	Mess("Haru-....CHAN!!!")
	hitretResult := Hitret(427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Suddenly, I was violently hugged.")
	hitretResult := Hitret(428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The sensation of her slender arms and soft body...IÅ^could feel it strongly.")
	hitretResult := Hitret(429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A faint, girly smell drifted into my nose, as her tenderÅ^warmth was steadily passed to me.")
	hitretResult := Hitret(430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EB08c", 0, 0)
	Talk("Nao", "NO000007")
	Mess("Haru-chan! ....You're Haru-chan, right?")
	hitretResult := Hitret(431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, long time no see...")
	hitretResult := Hitret(432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000008")
	Mess("...Why...?")
	hitretResult := Hitret(433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her fast throbbing heart began to echo through my body.")
	hitretResult := Hitret(434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000009")
	Mess("If you were going to come over here, you should've saidÅ^something...")
	hitretResult := Hitret(435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A pleasant voice flowed into my ears...it was nostalgic,Å^but a bit different to what I remembered, probablyÅ^because she's grown up.")
	hitretResult := Hitret(436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I just got here yesterday.")
	hitretResult := Hitret(437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The sensation of her embrace still remained.")
	hitretResult := Hitret(438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000010")
	Mess("Where?")
	hitretResult := Hitret(439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Grandpa's place.")
	hitretResult := Hitret(440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000011")
	Mess("Ah, is that right...?")
	hitretResult := Hitret(441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("During the time she was hugging and talking to me,Å^Nao-chan's...huge chest...was moving up and down.")
	hitretResult := Hitret(442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EB08b", 0, 0)
	Talk("Nao", "NO000012")
	Mess("You've really grown up...")
	hitretResult := Hitret(443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Zoom(450, 310, 150, 113, 1000, 0, 0)
	ScPlaySe("SE012", 0)
	Talk("Haruka", "")
	Mess("You too, Nao-chan...")
	hitretResult := Hitret(444)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Without thinking, I replied in an admiring tone.Å^I don't think they had this much volume before, but...")
	hitretResult := Hitret(445)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("No, let's not think about that for now.")
	hitretResult := Hitret(446)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Zoom(400, 300, 800, 600, 1000, 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("...Nao Yorihime is a girl who lives close to us, thoughÅ^she's a year older.")
	hitretResult := Hitret(447)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When we used to come here for summer break, she wouldÅ^always enthusiastically look after us.")
	hitretResult := Hitret(448)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's been how many years since we last met...but itÅ^seems her bright and kind personality hasn't changed atÅ^all, and for that I'm really happy.")
	hitretResult := Hitret(449)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though we only met by accident.")
	hitretResult := Hitret(450)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(451)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000013")
	Mess("........")
	hitretResult := Hitret(452)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("..........")
	hitretResult := Hitret(453)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("However, this is troubling...It doesn't seem likeÅ^Nao-chan's going to let go.")
	hitretResult := Hitret(454)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Umm...Nao-chan?")
	hitretResult := Hitret(455)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000014")
	Mess("Hmm?...What's wrong?")
	hitretResult := Hitret(456)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("S-Sorry...do you think you could let go?")
	hitretResult := Hitret(457)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000015")
	Mess("Oh...Was it painful?")
	hitretResult := Hitret(458)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, no, that's not it, but...")
	hitretResult := Hitret(459)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000016")
	Mess("Sorry...it's just that it's been so long..")
	hitretResult := Hitret(460)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("After squeezing once more as if reluctant, Nao-chan letÅ^go.")
	hitretResult := Hitret(461)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM03", 0)
	SetCg("B34b", 400, 0)
	SetBustup("CB01_02L", 0, 0)
	Talk("Nao", "NO000017")
	Mess("I was watching an unfamiliar figure from behindÅ^thinking, \"Who is that?\" Especially since he wasÅ^staggering under those heavy looking bags.")
	hitretResult := Hitret(462)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y-Yeah. Well, I guess I did kind of overdo it.")
	hitretResult := Hitret(463)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000018")
	Mess("...Looks like it to me. Wait a moment, I'll help you.")
	hitretResult := Hitret(464)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("T-Thanks...")
	hitretResult := Hitret(465)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan picked up the rest of the things that hadÅ^fallen out.")
	hitretResult := Hitret(466)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO000019")
	Mess("Don't worry about it. I was surprised, you moved in soÅ^suddenly.")
	hitretResult := Hitret(467)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000020")
	Mess("I've seen several tradesman going in and out of thatÅ^house lately, so I thought the house was falling down.")
	hitretResult := Hitret(468)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Oh, that was probably the cleaners and renovators.")
	hitretResult := Hitret(469)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000021")
	Mess("Ehhh? So, what're you doing here at this time?Å^Getting it ready for another summer vacation retreat?")
	hitretResult := Hitret(470)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh...Well...")
	hitretResult := Hitret(471)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I fumbled against an extremely natural question.")
	hitretResult := Hitret(472)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...But this topic will undoubtedly come up again. And ifÅ^it's Nao-chan, I can't avoid telling her.")
	hitretResult := Hitret(473)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO000022")
	Mess("Oh...If it's too difficult to talk about, you don't haveÅ^to tell me.")
	hitretResult := Hitret(474)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nah...Rather, it's something I have to make clear to youÅ^anyhow.")
	hitretResult := Hitret(475)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I couldn't possibly hide it and move on. In the firstÅ^place, I had no reason to hide it.")
	hitretResult := Hitret(476)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Recently...my parents...Both of them...died in anÅ^accident.")
	hitretResult := Hitret(477)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_09M", 0, 0)
	Talk("Nao", "NO000023")
	Mess("Eh?")
	hitretResult := Hitret(478)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan's face stiffened for a moment.")
	hitretResult := Hitret(479)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'll leave out the details for now...but Sora and I haveÅ^come here to stay.")
	hitretResult := Hitret(480)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000024")
	Mess("Is that right...Å^I'm sorry, making you talk about such painful things.Å^Um...I...")
	hitretResult := Hitret(481)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nah, it's okay. It's in the past..and we've alreadyÅ^committed to see this through, so don't worry about it.")
	hitretResult := Hitret(482)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Rather, you've made it easier for me to say by askingÅ^first.")
	hitretResult := Hitret(483)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO000025")
	Mess("Yeah...if that's the case, it's okay, but...")
	hitretResult := Hitret(484)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000026")
	Mess("But, if you have any problems, come see me right away,Å^okay? We're going to be living close by in any case, andÅ^no holding back between us, right?")
	hitretResult := Hitret(485)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah...Thanks.")
	hitretResult := Hitret(486)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO000027")
	Mess("Aha, you can count on me! But for now, it's moving theseÅ^bags, right?")
	hitretResult := Hitret(487)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh...Yeah.")
	hitretResult := Hitret(488)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Finally, the strange atmosphere between us had clearedÅ^and we'd returned to the same sort of conversation as inÅ^the old days.")
	hitretResult := Hitret(489)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000028")
	Mess("Well now, I'll take these then.")
	hitretResult := Hitret(490)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan held the things I'd dropped before.")
	hitretResult := Hitret(491)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, yeah. Thanks for that.")
	hitretResult := Hitret(492)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B01b", 0, 0)
	SetBustup("CB01_01M", 0, 0)
	Transition("WIP_LR", 500)
	Talk("Nao", "NO000029")
	Mess("We've arrivedÅ`. Hehe, my arms are a bit tired.")
	hitretResult := Hitret(493)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("S-Sorry...but you were a big help. I honestly didn'tÅ^know what to do when the plastic bag broke, and I wasÅ^just standing there confused.")
	hitretResult := Hitret(494)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_03M", 0, 0)
	Talk("Nao", "NO000030")
	Mess("Ahaha, the expression on your face at that moment wasÅ^cuteÅ` I wonder if you've had that expression when youÅ^were here before?")
	hitretResult := Hitret(495)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eeeh? I was making such a face?")
	hitretResult := Hitret(496)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO000031")
	Mess("I didn't realize it was you at first. But when I wasÅ^picking those things up, I thought, \"Huh?\" After that, IÅ^suddenly recognized you...")
	hitretResult := Hitret(497)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's a good thing I recognized you right away.")
	hitretResult := Hitret(498)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_10M", 0, 0)
	Talk("Nao", "NO000032")
	Mess("Yeah. Both of our memories of the past...")
	hitretResult := Hitret(499)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO000033")
	Mess("A-Ah! That's right, do you need help with anything else?Å^If so, I'll help out with anything.")
	hitretResult := Hitret(500)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nah, I'm okay for now. I'll invite you over again onceÅ^I've unpacked everything.")
	hitretResult := Hitret(501)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO000034")
	Mess("Then there's no point in my offer to help. Ah well, IÅ^guess that kind of stuff is something you guys need toÅ^do yourselves.")
	hitretResult := Hitret(502)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, and we've already finished unpacking all the bigÅ^things, anyway.")
	hitretResult := Hitret(503)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000035")
	Mess("Is that so? Ummm...you're going to the school over hereÅ^too, right?")
	hitretResult := Hitret(504)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's right. We can't go yet because of our situation,Å^but we will once we're done settling in.")
	hitretResult := Hitret(505)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000036")
	Mess("Roger that. In that case, I'll tell you about the schoolÅ^next time.")
	hitretResult := Hitret(506)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay, I'll be counting on you.")
	hitretResult := Hitret(507)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000037")
	Mess("Well, I'll be going then. See ya.")
	hitretResult := Hitret(508)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... Thanks a lot.")
	hitretResult := Hitret(509)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_11S", 3, 0)
	Talk("Nao", "NO000038")
	Mess("...Ah....")
	hitretResult := Hitret(510)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("?")
	hitretResult := Hitret(511)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000039")
	Mess("...I'm glad..We were able to meet again.Å^Hey... N-nothing happened after that?")
	hitretResult := Hitret(512)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Eh...?")
	hitretResult := Hitret(513)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO000040")
	Mess("I-It's nothing. See ya.")
	hitretResult := Hitret(514)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupLeave(3, -50, 0, 1, 1000, 1)
	Talk("Haruka", "")
	Mess("Nao-chan...")
	hitretResult := Hitret(515)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan waved back once and left.")
	hitretResult := Hitret(516)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("\"After that...\"")
	hitretResult := Hitret(517)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("\"After that...\" I came here last for summer vacation.")
	hitretResult := Hitret(518)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("When Nao-chan said \"memories of the past,\" was sheÅ^thinking about...")
	hitretResult := Hitret(519)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("........")
	hitretResult := Hitret(520)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se050", 0)
	Talk("Haruka", "")
	Mess("......?")
	hitretResult := Hitret(521)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After hearing some noise, I turned around and saw theÅ^curtains swaying in the medical examination room.")
	hitretResult := Hitret(522)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("DATE", 0)
	Change("00_z004")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

