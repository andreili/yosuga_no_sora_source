Include"yosuga.ch"

Function sc00_A029A()
	ScenarioEnter()
	SetCg("B27b", 0, 600)
	Scroll(0, 0, 100000, 1, 0)
	WindowView(1)
	ScPlayBgm("BGM16", 0)
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011374")
	Mess("The feeling of stepping firmly in the sand... changed to^pain with every step I took.")
	hitretResult := Hitret(9146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011375")
	Mess("I gradually became unable to feel anything. Even the^pain from the pricking in the soles of my feet, even the^warmth of the surface, and even the rising heat...")
	hitretResult := Hitret(9147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011376")
	Mess("I just put my feet forward, and continued on.")
	hitretResult := Hitret(9148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011377")
	Mess("I wonder where this is.")
	hitretResult := Hitret(9149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011378")
	Mess("I don't know where I am, nor where I'm trying to go.")
	hitretResult := Hitret(9150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011379")
	Mess("For what reason I'm doing this... even that has become^unimportant.")
	hitretResult := Hitret(9151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011380")
	Mess("The scenery is becoming dim.")
	hitretResult := Hitret(9152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011381")
	Mess("I want my head to be empty... Thinking that, I went^outside.")
	hitretResult := Hitret(9153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011382")
	Mess("But, no matter where I go... Haru's face appears.")
	hitretResult := Hitret(9154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011383")
	Mess("Haru...")
	hitretResult := Hitret(9155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011384")
	Mess("Haru left and went away.")
	hitretResult := Hitret(9156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011385")
	Mess("He left me behind.")
	hitretResult := Hitret(9157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011386")
	Mess("We can't continue on like this... are the words he left^me with.")
	hitretResult := Hitret(9158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR011387")
	Mess("Why...")
	hitretResult := Hitret(9159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011388")
	Mess("That day, when Haru revealed his feelings... I was truly^happy.")
	hitretResult := Hitret(9160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011389")
	Mess("We tenderly embraced... We loved each other intensely...^His heart was also satisfied.")
	hitretResult := Hitret(9161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011390")
	Mess("With the bliss of communicating our feelings... just^being next to each other we felt tranquility.")
	hitretResult := Hitret(9162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011391")
	Mess("That alone would have been enough.")
	hitretResult := Hitret(9163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011392")
	Mess("That way, it would have lasted forever.")
	hitretResult := Hitret(9164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011393")
	Mess("But, Haru...")
	hitretResult := Hitret(9165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011394")
	Mess("He said we can't continue on like this.")
	hitretResult := Hitret(9166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011395")
	Mess("He said let's end it.")
	hitretResult := Hitret(9167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011396")
	Mess("......Let's end... this relationship....")
	hitretResult := Hitret(9168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B30a", 0, 0)
	Transition("", 2000)
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011397")
	Mess("...Ever since I was small, I've held feelings for him.")
	hitretResult := Hitret(9169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011398")
	Mess("It came true... only for a short time.")
	hitretResult := Hitret(9170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011399")
	Mess("I can still recall it.")
	hitretResult := Hitret(9171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011400")
	Mess("The feeling of Haru touching me a lot. As long as this^throbbing, and this warmth are here... it's not a lie.")
	hitretResult := Hitret(9172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011401")
	Mess("I wonder if it should have stayed that way.")
	hitretResult := Hitret(9173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011402")
	Mess("Because he conveyed the desire to give up.")
	hitretResult := Hitret(9174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011403")
	Mess("I wonder since when... have I been in love with Haru.")
	hitretResult := Hitret(9175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011404")
	Mess("When the only thing I knew about was the sky I saw^outside the hospital window, he told me about the^outside world.")
	hitretResult := Hitret(9176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B27a", 0, 0)
	Transition("", 2000)
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011405")
	Mess("The real sky... is vast and connected everywhere.")
	hitretResult := Hitret(9177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011406")
	Mess("And so, even if we were separated, we would still be^under the same sky.")
	hitretResult := Hitret(9178)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011407")
	Mess("And before I realized it, I was attracted to the Haru^who would smile as he looked at me.")
	hitretResult := Hitret(9179)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011408")
	Mess("Underneath this sky, we are connected.")
	hitretResult := Hitret(9180)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011409")
	Mess("Sora... I came to love this name.")
	hitretResult := Hitret(9181)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011410")
	Mess("My name, and Haru's name.")
	hitretResult := Hitret(9182)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011411")
	Mess("A distant sky.")
	hitretResult := Hitret(9183)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011412")
	Mess("So that I can always be connected to Haru.")
	hitretResult := Hitret(9184)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011413")
	Mess("However, I soon understood.")
	hitretResult := Hitret(9185)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011414")
	Mess("My window wasn't connected to anything but Haru.")
	hitretResult := Hitret(9186)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011415")
	Mess("But, just like Haru's name... he was connected to a^world even vaster.")
	hitretResult := Hitret(9187)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011416")
	Mess("We were supposed to look up at the same sky, but Haru^saw the world I was in too.")
	hitretResult := Hitret(9188)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011417")
	Mess("To me, I couldn't see... anything other than Haru.")
	hitretResult := Hitret(9189)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011418")
	Mess("That's why, Haru's words didn't reach me.")
	hitretResult := Hitret(9190)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011419")
	Mess("What kind of situation we are left with...")
	hitretResult := Hitret(9191)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011420")
	Mess("What kind of hardships Haru took in my place...")
	hitretResult := Hitret(9192)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011421")
	Mess("How much Haru suffered...")
	hitretResult := Hitret(9193)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011422")
	Mess("Even though I watched Haru that much... I never tried to^see those things, relying only on his kindness.")
	hitretResult := Hitret(9194)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011423")
	Mess("Haru has no reason to save... someone like me.")
	hitretResult := Hitret(9195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011424")
	Mess("It was only that, the two of us were together.")
	hitretResult := Hitret(9196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011425")
	Mess("We only experience peaceful times together.")
	hitretResult := Hitret(9197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011426")
	Mess("Even though I loved him...... I still knew nothing about^Haru.")
	hitretResult := Hitret(9198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011427")
	Mess("Someone like me...")
	hitretResult := Hitret(9199)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011428")
	Mess("...to Haru, could only be a burden.")
	hitretResult := Hitret(9200)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ƒ‚ƒmƒ[ƒOsâut", "SR011429")
	Mess("If I caused him trouble again, to smooth over his^relationship with me...")
	hitretResult := Hitret(9201)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Sora", "SR011430")
	Mess("Ha...ru......")
	hitretResult := Hitret(9202)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Hide()
	WindowView(0)
	ScWait(2000, 0)
	Change("00_a029b")
	Return
	Change("EXIT_SCENARIO")
EndFunc

