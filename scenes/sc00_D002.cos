Include"yosuga.ch"

Function sc00_D002()
	ScenarioEnter()
	ScPlayBgm("BGM05", 0)
	SetCg("B32a", 0, 0)
	Talk("Haruka", "")
	Mess("I'm off to school.")
	hitretResult := Hitret(26816)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("......")
	hitretResult := Hitret(26817)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The uniforms are arriving today. Once they do, we can goÅ^to school together, right?")
	hitretResult := Hitret(26818)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("......")
	hitretResult := Hitret(26819)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Tch, no reply?")
	hitretResult := Hitret(26820)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Not even responding to a knock...I wonder if she's stillÅ^sleeping?")
	hitretResult := Hitret(26821)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I think I'll start preparing early tomorrow...")
	hitretResult := Hitret(26822)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I've put the rice in the fridge, so make sure you eatÅ^some, okay? All right, I'm off.")
	hitretResult := Hitret(26823)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B01a", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Just in case, I locked the door after I left the house.")
	hitretResult := Hitret(26824)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B34a", 0, 0)
	Scroll(400, 0, 10000, 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("It's a town full of things that I'm not used to, butÅ^I've definitely remembered the way to school.")
	hitretResult := Hitret(26825)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm not as reliable as Migiwa-san says, but I've becomeÅ^better at dealing with things in my daily life.")
	hitretResult := Hitret(26826)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Once the uniforms arrive, she'll probably go toÅ^school...and once that happens, that's one less burdenÅ^for me to carry.")
	hitretResult := Hitret(26827)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("How long will we have to keep doing things here beforeÅ^it becomes our natural lifestyle...?")
	hitretResult := Hitret(26828)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040001")
	Mess("Ah! It's Haru-kun! Morniiiinngggg!!!")
	hitretResult := Hitret(26829)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040024")
	Mess("Wait! Akira, don't use such a loud voice...")
	hitretResult := Hitret(26830)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B34a", 400, 0)
	SetBustup("CC01_02M", 0, 0)
	SetBustup("CD01_12M", 0, 0)
	Talk("Haruka", "")
	Mess("Amatsume, Migiwa-san...Morning.")
	hitretResult := Hitret(26831)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040025")
	Mess("G-Good Morning...")
	hitretResult := Hitret(26832)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(5)
	SetBustup("CC01_01L", 0, 0)
	Talk("Akira", "AK040002")
	Mess("Hmmm.....?")
	hitretResult := Hitret(26833)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("W-What?")
	hitretResult := Hitret(26834)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	SetBustup("CD01_12M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("As she looked into my face while I was greeting them, IÅ^bent backwards without thinking.")
	hitretResult := Hitret(26835)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even Migiwa-san looked as if she were looking atÅ^something strange.")
	hitretResult := Hitret(26836)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040003")
	Mess("For some reason you've got a worn out face. You okay?")
	hitretResult := Hitret(26837)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh?")
	hitretResult := Hitret(26838)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...After hearing something slightly unexpected, I made aÅ^weird noise.")
	hitretResult := Hitret(26839)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I was making such a face...?")
	hitretResult := Hitret(26840)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_08M", 0, 0)
	Talk("Kazuha", "KA040026")
	Mess("I-I don't really know....")
	hitretResult := Hitret(26841)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even when I ask Migiwa-san, she made a troubled face.")
	hitretResult := Hitret(26842)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Since I didn't notice myself, I have no idea what sortÅ^of expression I should be making now.")
	hitretResult := Hitret(26843)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's not your imagination is it, Amatsume?")
	hitretResult := Hitret(26844)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Once I asked Amatsume that, she-")
	hitretResult := Hitret(26845)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK040004")
	Mess("Hmm...maybe.")
	hitretResult := Hitret(26846)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("readily withdrew it.")
	hitretResult := Hitret(26847)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA040027")
	Mess("What's up with you, geez...")
	hitretResult := Hitret(26848)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040005")
	Mess("Ahahaha, maybe I'm just happy to meet Haru-kun.")
	hitretResult := Hitret(26849)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even if that's the case, I don't understand you.")
	hitretResult := Hitret(26850)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK040006")
	Mess("Really?")
	hitretResult := Hitret(26851)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040028")
	Mess("Let's hurry up and go. If we relax here, that personÅ^will show up.")
	hitretResult := Hitret(26852)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That person?")
	hitretResult := Hitret(26853)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040029")
	Mess("It's nothing. Kasugano-kun if you're going to wait here,Å^then we're going to go first.")
	hitretResult := Hitret(26854)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("Wait, wait. I'll go with you too.")
	hitretResult := Hitret(26855)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	SetCg("B12a", 0, 0)
	SetBustup("CD01_01M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("When I caught up to Migiwa-san, I realized that \"thatÅ^person\" was probably Ryouhei.")
	hitretResult := Hitret(26856)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It seems she sees him as an enemy.")
	hitretResult := Hitret(26857)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't know the reason, but...")
	hitretResult := Hitret(26858)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040030")
	Mess("...Umm, I could see it too just then, so do you want toÅ^talk...?")
	hitretResult := Hitret(26859)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, no, that's not it.")
	hitretResult := Hitret(26860)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("...Well, if you're going to look hard at me, of courseÅ^you'll think I'm weird...")
	hitretResult := Hitret(26861)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA040031")
	Mess("Huh? Is there something over there?")
	hitretResult := Hitret(26862)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, not really.")
	hitretResult := Hitret(26863)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Probably thinking that my actions were unnatural, sheÅ^looked away from me and followed my gaze.")
	hitretResult := Hitret(26864)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_04M", 0, 0)
	Talk("Kazuha", "KA040032")
	Mess("Ah....")
	hitretResult := Hitret(26865)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh, is there really something there?")
	hitretResult := Hitret(26866)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Hearing Migiwa-san unexpectedly cry out, I looked overÅ^there again.")
	hitretResult := Hitret(26867)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CG01_02S", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("A woman...?")
	hitretResult := Hitret(26868)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From the direction of the school, an extremely roughÅ^figure wearing a tank top and shorts came walking towardÅ^us while dragging her sandals on the ground.")
	hitretResult := Hitret(26869)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She was scratching her head while wearing aÅ^frown.....She was a bit scary, but had a sharp look.Å^Perhaps, she just might be....a beautiful woman.")
	hitretResult := Hitret(26870)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But for someone to be coming from the city this early inÅ^the morning... It feels weird.")
	hitretResult := Hitret(26871)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's mostly a time when people would be going into theÅ^city, like us, or going into the fields.")
	hitretResult := Hitret(26872)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC01_04M", 0, 0)
	Talk("Akira", "AK040007")
	Mess("Ah! It's Hiro-neechan! Oiiiiii!!!")
	hitretResult := Hitret(26873)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040033")
	Mess("A-Akira!")
	hitretResult := Hitret(26874)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh, Amatsume?")
	hitretResult := Hitret(26875)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("This time it was Amatsume who called out, and ran towardÅ^the woman.")
	hitretResult := Hitret(26876)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I wonder if they're acquaintances.")
	hitretResult := Hitret(26877)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02S", 0, 0)
	SetBustup("CG01_06S", 0, 0)
	Talk("Woman With Mean LookÅsÇ‚Ç–ÇÎÅt", "YH040001")
	Mess("Shut up!!! Don't shout so loudly! Urgh...")
	hitretResult := Hitret(26878)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...For some reason, I think her words were a bitÅ^rough...")
	hitretResult := Hitret(26879)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In a sense, you could say it fits her appearance...")
	hitretResult := Hitret(26880)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	SetBustup("CG01_02M", 0, 0)
	Talk("Akira", "AK040008")
	Mess("Hey, are you just returning now? Just now?")
	hitretResult := Hitret(26881)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Woman With Mean LookÅsÇ‚Ç–ÇÎÅt", "YH040002")
	Mess("Don't say it twice!...Geez, yes, fromÅ^Motoka's...pl...ace...")
	hitretResult := Hitret(26882)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Oh, this is my first time seeing a woman come back afterÅ^staying out all night...Eh? But isn't \"Motoka\" a girl'sÅ^name?")
	hitretResult := Hitret(26883)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("For some reason, I think I've heard that nameÅ^somewhere...")
	hitretResult := Hitret(26884)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_06M", 0, 0)
	Talk("Woman With Mean LookÅsÇ‚Ç–ÇÎÅt", "YH040003")
	Mess("Huh? What do you want? You've been staring at me for aÅ^while now. Trying to pick a fight?")
	hitretResult := Hitret(26885)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What, no way!")
	hitretResult := Hitret(26886)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Don't tell me she really wants to fight? Why...")
	hitretResult := Hitret(26887)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040009")
	Mess("Ah, you haven't been introduced to Haru-kun yet, huh?Å^This is Hiro-neechan!")
	hitretResult := Hitret(26888)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hiro...neechan?")
	hitretResult := Hitret(26889)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_04M", 0, 0)
	Talk("Kazuha", "KA040034")
	Mess("Akira, that's not much of an introduction.")
	hitretResult := Hitret(26890)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_05M", 0, 0)
	Talk("Akira", "AK040010")
	Mess("I see, I see. Hiro-neechan's the one who works at myÅ^shrine's cheap candy shop.")
	hitretResult := Hitret(26891)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_03M", 0, 0)
	Talk("Akira", "AK040011")
	Mess("A shop manager.")
	hitretResult := Hitret(26892)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_02M", 0, 0)
	Talk("Woman With Mean LookÅsÇ‚Ç–ÇÎÅt", "YH040004")
	Mess("...It's not just cheap candy, you know?")
	hitretResult := Hitret(26893)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_01M", 0, 0)
	Talk("Kazuha", "KA040035")
	Mess("...Ifukube Yahiro-san.")
	hitretResult := Hitret(26894)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if seeing no other choice, Migiwa-san gives me herÅ^full name.")
	hitretResult := Hitret(26895)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Yahiro, therefore Hiro-neechan, makes sense.")
	hitretResult := Hitret(26896)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Pleased to meet you...I'm Kasugano Haruka.")
	hitretResult := Hitret(26897)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_01M", 0, 0)
	Talk("Yahiro", "YH040005")
	Mess("Kasugano...? You're the one at Sensei's place?")
	hitretResult := Hitret(26898)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes, I'm his grandson.")
	hitretResult := Hitret(26899)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH040006")
	Mess("Hmmm....")
	hitretResult := Hitret(26900)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ifukube-san...actually, it doesn't feel right to useÅ^such a formal name here...Yahiro-san just closed herÅ^eyes for a second and snuffled, holding her head as ifÅ^all meaning was lost to her.")
	hitretResult := Hitret(26901)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Umm...Are you not feeling well?")
	hitretResult := Hitret(26902)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_06M", 0, 0)
	Talk("Yahiro", "YH040007")
	Mess("Shut up. It's a hangover, so stop talking.")
	hitretResult := Hitret(26903)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("O-Okay...")
	hitretResult := Hitret(26904)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I thought, probably a scary person.")
	hitretResult := Hitret(26905)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK040012")
	Mess("Hiro-neechan, I made some onogiri at home, so when youÅ^get hungry, eat those, okay?")
	hitretResult := Hitret(26906)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH040008")
	Mess("Ahh, thank you...Well, at this rate I'll just be fastÅ^asleep...")
	hitretResult := Hitret(26907)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK040013")
	Mess("I see, then good night!!")
	hitretResult := Hitret(26908)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_08M", 0, 0)
	Talk("Yahiro", "YH040009")
	Mess("Tch...like I said, don't shout!!!! It echoes in my head!")
	hitretResult := Hitret(26909)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	BustupClear(5)
	SetBustup("CG01_02M", 0, 0)
	Talk("Yahiro", "YH040010")
	Mess("Geez, and that's why you can't drink sake yet...")
	hitretResult := Hitret(26910)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(8)
	Talk("êSÇÃê∫", "")
	Mess("Leaving behind that sharp remark, she left on the roadÅ^we came from, with her sandals dragging on the groundÅ^like before.")
	hitretResult := Hitret(26911)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I would never have believed that such an untidy personÅ^was an acquaintance of Amatsume and Migiwa-san.")
	hitretResult := Hitret(26912)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Though Ryouhei probably has quite a few areas where he'sÅ^similar.")
	hitretResult := Hitret(26913)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("They seem like they know each other but...they're notÅ^sisters are they?")
	hitretResult := Hitret(26914)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Ifukube is different to both Amatsume and Migiwa, afterÅ^all.")
	hitretResult := Hitret(26915)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	SetBustup("CD01_01M", 0, 0)
	Talk("Akira", "AK040014")
	Mess("Hmm...Guardian, I suppose?")
	hitretResult := Hitret(26916)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Guardian?")
	hitretResult := Hitret(26917)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040015")
	Mess("Yeah, I don't have anyone in my family right now, soÅ^Hiro-neechan looks after me.")
	hitretResult := Hitret(26918)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No one...in your family?")
	hitretResult := Hitret(26919)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK040016")
	Mess("Yup!")
	hitretResult := Hitret(26920)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume had a grandpa, I think...he was old and frail,Å^but he was the Shinto priest and...")
	hitretResult := Hitret(26921)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Don't tell me he died like my grandparents...")
	hitretResult := Hitret(26922)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("So Amatsume's like me and Sora, having no relatives...")
	hitretResult := Hitret(26923)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sorry...I probably asked something strange...")
	hitretResult := Hitret(26924)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040017")
	Mess("Hmm? Why is Haru-kun apologizing?")
	hitretResult := Hitret(26925)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, that's...")
	hitretResult := Hitret(26926)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Upon looking at Amatsume, who had a bright smile on herÅ^face, you'd think I was the one who had the wrongÅ^reaction.")
	hitretResult := Hitret(26927)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...If someone asked me that too, I'd probably give theÅ^same response...")
	hitretResult := Hitret(26928)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_07M", 0, 0)
	Talk("Kazuha", "KA040036")
	Mess("She says guardian, but if you look at who's lookingÅ^after who...")
	hitretResult := Hitret(26929)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Interrupting our conversation, Migiwa-san spat outÅ^something full of thorns.")
	hitretResult := Hitret(26930)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if she was someone like Ryouhei, she shot a hostileÅ^glare at the retreating Yahiro-san.")
	hitretResult := Hitret(26931)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...So Migiwa-san has people other than Ryouhei that sheÅ^takes that attitude with...")
	hitretResult := Hitret(26932)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK040018")
	Mess("Something like 6:4, Hiro-neechan's way?")
	hitretResult := Hitret(26933)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040037")
	Mess("Isn't it 8:2, your way?")
	hitretResult := Hitret(26934)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040019")
	Mess("You really think so?")
	hitretResult := Hitret(26935)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040038")
	Mess("Always making you tend to the candy shop and making youÅ^cook dinner, just how is she guarding you?")
	hitretResult := Hitret(26936)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040020")
	Mess("By letting me stay in this town?")
	hitretResult := Hitret(26937)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_05M", 0, 0)
	Talk("Kazuha", "KA040039")
	Mess("Kuh....there is that, but....")
	hitretResult := Hitret(26938)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_07M", 0, 0)
	Talk("Akira", "AK040021")
	Mess("Thanks for always worrying about me, Kazu-chan. ButÅ^although she may look that way, Hiro-neechan's actuallyÅ^a kind person.")
	hitretResult := Hitret(26939)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK040022")
	Mess("And besides, being able to be here with everyone likeÅ^this...I'm happy.")
	hitretResult := Hitret(26940)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD01_08M", 0, 0)
	Talk("Kazuha", "KA040040")
	Mess("That's.......")
	hitretResult := Hitret(26941)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume is smiling sweetly, and in contrast, Migiwa-sanÅ^is looking depressed.")
	hitretResult := Hitret(26942)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Looking to cheer up Migiwa-san, Amatsume took her hand.")
	hitretResult := Hitret(26943)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just friends...no, even if they were close friends...IÅ^get the feeling that they're something more than that.")
	hitretResult := Hitret(26944)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But it was something that was impossible to understandÅ^for someone who had just met them, like me.")
	hitretResult := Hitret(26945)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK040023")
	Mess("Ahaha, it turned into a weird conversation, huh? Sorry,Å^Haru-kun.")
	hitretResult := Hitret(26946)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040024")
	Mess("I'm completely okay, so don't worry about it too much,Å^okay?")
	hitretResult := Hitret(26947)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh, yeah...")
	hitretResult := Hitret(26948)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_04M", 0, 0)
	Talk("Akira", "AK040025")
	Mess("Hey, come on! Let's go to school! We're going to beÅ^late!")
	hitretResult := Hitret(26949)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Smiling again, Amatsume grabbed Migiwa-san and me by ourÅ^hands and ran.")
	hitretResult := Hitret(26950)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if falling forward, with Migiwa-san and I following.")
	hitretResult := Hitret(26951)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But Migiwa-san's expression was still downcast.")
	hitretResult := Hitret(26952)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_d003")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

