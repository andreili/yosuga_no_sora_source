Include"yosuga.ch"

Function sc00_C032B()
	ScenarioEnter()
	SetCg("B15a", 0, 0)
	Talk("Yahiro", "YH020162")
	Mess("Akira~ Come here~! Quickly quickly~!!")
	hitretResult := Hitret(25495)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM05", 0)
	SetBustup("CG01_04M", 0, 0)
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK021886")
	Mess("Sniff! Woah, there's something smelling really good!")
	hitretResult := Hitret(25496)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Yahiro-oneesan thrusts the bottle before Akira's noseÅ^and lets her smell it.")
	hitretResult := Hitret(25497)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020163")
	Mess("Right over here, is something called propolis, and youÅ^know what, it's a really special type of honey--.")
	hitretResult := Hitret(25498)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_14M", 0, 0)
	Jump(2, 0)
	Talk("Akira", "AK021887")
	Mess("Woaah! Honey-! Honey-! Mapleee-!")
	hitretResult := Hitret(25499)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("T, that last one shouldn't have anything to do withÅ^anything, I would think......")
	hitretResult := Hitret(25500)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_03M", 0, 0)
	Talk("Yahiro", "YH020164")
	Mess("I usually drink it with Hyaluronic acid, so my skin isÅ^maintained very nicely. You're at an age where youÅ^should take care of menopause~.")
	hitretResult := Hitret(25501)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020165")
	Mess("So, this propolis is something realll good to preventÅ^menopause-! I'll specially share it with you today,Å^taste it carefully okay-?")
	hitretResult := Hitret(25502)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_04M", 0, 0)
	Talk("Akira", "AK021888")
	Mess("Yeah, I'll drink it I'll drink it!!")
	hitretResult := Hitret(25503)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020166")
	Mess("Idiot~, this is meant to be licked. Just hold on for aÅ^while, this is really expensive......")
	hitretResult := Hitret(25504)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Yahiro-san dabbles the propolis onto a cotton bud andÅ^moves it in front of Akira's eyes.")
	hitretResult := Hitret(25505)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_04M", 0, 0)
	Talk("Yahiro", "YH020167")
	Mess("Al~right, lick it carefully-! Don't lick my fingersÅ^okay-.")
	hitretResult := Hitret(25506)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC06_14L", 0, 0)
	Talk("Akira", "AK021889")
	Mess("Itadakimasu! Nmmmonom..... chuuu.... nom nom.....Å^mmm.....")
	hitretResult := Hitret(25507)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020168")
	Mess("How is it, delicious-?")
	hitretResult := Hitret(25508)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_11L", 0, 0)
	Talk("Akira", "AK021890")
	Mess("Nnom... sschu.... nom nom.....")
	hitretResult := Hitret(25509)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Somehow or rather, Yahiro-san is doing a piston exerciseÅ^in Akira's mouth, playing with it in and out.")
	hitretResult := Hitret(25510)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD02_05M", 0, 0)
	Talk("Kazuha", "KA020548")
	Mess("H, hey wait! Please stop playing with Akira!")
	hitretResult := Hitret(25511)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_03M", 0, 0)
	Talk("Yahiro", "YH020169")
	Mess("Huh? Ojou wants it too? Alright! Here!!")
	hitretResult := Hitret(25512)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD02_12L", 0, 0)
	Shake(5)
	Talk("Kazuha", "KA020549")
	Mess("Nhmph!? Numom...... mm.... Chuu.... Ahaa....")
	hitretResult := Hitret(25513)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Yahiro-san thrusts another cotton bud into theÅ^protesting Migiwa-san's mouth..... Why is she moving it?")
	hitretResult := Hitret(25514)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC06_11M", 0, 0)
	Talk("Akira", "AK021891")
	Mess("Yahiro-oneechan...... I still want more~.")
	hitretResult := Hitret(25515)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CG01_03M", 0, 0)
	Talk("Yahiro", "YH020170")
	Mess("It can't be helped haah, this is really expensive so IÅ^can only let you have it one more time alright?")
	hitretResult := Hitret(25516)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC06_11L", 0, 0)
	Talk("Akira", "AK021892")
	Mess("Aaaannn! Chuu.... Nom nom nom.... Ahh...")
	hitretResult := Hitret(25517)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Yahiro", "YH020171")
	Mess("You must suck it with your tongue there's no taste leftÅ^okay.")
	hitretResult := Hitret(25518)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021893")
	Mess("Nom nom chuuu..... Afuuu....")
	hitretResult := Hitret(25519)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("T, toying with her..... definitely.....")
	hitretResult := Hitret(25520)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, with Yahiro-san's idea, we can collect Akira'sÅ^saliva.")
	hitretResult := Hitret(25521)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As for her hair, we can get it when Migiwa-san isÅ^stroking Akira's head, that's also A-Okay.")
	hitretResult := Hitret(25522)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	BustupClear()
	SetBustup("CF03_06M", 0, 0)
	ScPlayBgm("BGM14", 0)
	Talk("Ryouhei", "RH020493")
	Mess("Damn it!! I'm late again!!")
	hitretResult := Hitret(25523)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_03M", 0, 0)
	Talk("Ryouhei", "RH020494")
	Mess("Please let me join in too-!! Yahiro-san!!")
	hitretResult := Hitret(25524)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se003", 0)
	SetBustup("CF03_10M", 0, 0)
	Shake(7)
	Talk("Ryouhei", "RH020495")
	Mess("Gyaha!?")
	hitretResult := Hitret(25525)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	ScPlaySe("se018", 0)
	SetBustup("CD02_05M", 0, 0)
	Talk("Kazuha", "KA020550")
	Mess("You're incorrigible, again it's you!!")
	hitretResult := Hitret(25526)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_10M", 0, 0)
	Talk("Ryouhei", "RH020496")
	Mess("I-isn't that mean right Haru..... I also want to have aÅ^memorable youth.")
	hitretResult := Hitret(25527)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uuu.... Mmm...")
	hitretResult := Hitret(25528)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("This time only, I'm really sorry Ryouhei...... I placedÅ^my palms together in my heart.")
	hitretResult := Hitret(25529)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1000, 0)
	SetCg("B35b", 0, 0)
	Transition("WIP_RL", 500)
	ScPlayBgm("BGM10", 0)
	Talk("Haruka", "")
	Mess("Well then......")
	hitretResult := Hitret(25530)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_07M", 0, 0)
	Talk("Kazuha", "KA020551")
	Mess("We cannot go back after this.")
	hitretResult := Hitret(25531)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If we throw this letter into the post box.")
	hitretResult := Hitret(25532)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020552")
	Mess("How long before we know the result?")
	hitretResult := Hitret(25533)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Around a week.")
	hitretResult := Hitret(25534)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020553")
	Mess("Is that so..... It seems very long and it seems veryÅ^short....")
	hitretResult := Hitret(25535)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("........ Indeed.")
	hitretResult := Hitret(25536)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA020554")
	Mess("...... Throw it in, Kasugano-kun.")
	hitretResult := Hitret(25537)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... Yeah, I know.")
	hitretResult := Hitret(25538)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("I throw the DNA test application into the letterbox.")
	hitretResult := Hitret(25539)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Things cannot be undone now. Unless the post gets intoÅ^an accident, we will have the results after a week.")
	hitretResult := Hitret(25540)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("When it comes to that, we might not be able to playÅ^happily like today.")
	hitretResult := Hitret(25541)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I feel some regret over this even till now over thisÅ^rash action.")
	hitretResult := Hitret(25542)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Because there wouldn't be people in pain right now overÅ^this matter.")
	hitretResult := Hitret(25543)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But........ if I didn't read that notebook, I would notÅ^find out about Akira's pain.")
	hitretResult := Hitret(25544)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...... Whichever is better, that opinion does not matterÅ^as it is too late. Already, we have done something thatÅ^has gone past the point of return.")
	hitretResult := Hitret(25545)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I even dragged Migiwa-san and Yahiro-san into thisÅ^together.")
	hitretResult := Hitret(25546)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("They have been with Akira longer than I have, and she isÅ^important to them as well, and they will keep onÅ^protecting her.")
	hitretResult := Hitret(25547)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I instigated them and made them accomplices.")
	hitretResult := Hitret(25548)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("To Migiwa-san, this conclusion might make her familyÅ^collapse. Even so, she still helped out for Akira'sÅ^sake.")
	hitretResult := Hitret(25549)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm such a terrible boy...... I really am. To alwaysÅ^force someone to sacrifice......")
	hitretResult := Hitret(25550)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("ÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(25551)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA020555")
	Mess("Kasugano-kun.")
	hitretResult := Hitret(25552)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm? What is it?")
	hitretResult := Hitret(25553)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_02M", 0, 0)
	Talk("Kazuha", "KA020556")
	Mess("It's not your fault.")
	hitretResult := Hitret(25554)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("B.... but..... If...... Akira and I keep things aÅ^secret.....")
	hitretResult := Hitret(25555)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020557")
	Mess("That sort of thing will not last for long. You're tooÅ^kind and you will not able to lie, and do somethingÅ^about it.")
	hitretResult := Hitret(25556)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020558")
	Mess("This is something that just happened.")
	hitretResult := Hitret(25557)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......... Migiwa-san.")
	hitretResult := Hitret(25558)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_08M", 0, 0)
	Talk("Kazuha", "KA020559")
	Mess("This ending resulted because of my father. Therefore theÅ^responsibility has to be taken by my family.")
	hitretResult := Hitret(25559)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD02_01M", 0, 0)
	Talk("Kazuha", "KA020560")
	Mess("Kasugano is only requested by your Grandmother to dispelÅ^her suspicions back then.")
	hitretResult := Hitret(25560)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020561")
	Mess("There must be someone who must find out about this. AndÅ^then, that is something that requires bravery.")
	hitretResult := Hitret(25561)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020562")
	Mess("Because Akira is more important than the other things inÅ^your heart, and so you are capable of doing somethingÅ^like this.")
	hitretResult := Hitret(25562)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA020563")
	Mess("No matter what kind of result it is, Akira and I willÅ^not hate you. Therefore Kasugano-kun, you have no needÅ^to blame yourself.")
	hitretResult := Hitret(25563)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("ÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(25564)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("And that is why...... it is difficult to bear.")
	hitretResult := Hitret(25565)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If they berate me, saying "Look what you have done!", IÅ^might feel better.")
	hitretResult := Hitret(25566)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But....... this is my reponsibility. I have to shoulderÅ^everything that is going to happen next.")
	hitretResult := Hitret(25567)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...... I'm not running away anymore.")
	hitretResult := Hitret(25568)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I only need to wait for the result, and the result willÅ^certainly arrive.")
	hitretResult := Hitret(25569)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Before that happens, I'm going to gaze upon Akira'sÅ^smiles till the point where I cannot forget them.")
	hitretResult := Hitret(25570)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("......... Urgh.............")
	hitretResult := Hitret(25571)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira's crying face suddenly appears in my mind. IÅ^quickly dismiss that worst conclusion by shaking myÅ^head.")
	hitretResult := Hitret(25572)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("DATE", 0)
	Change("00_c033")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

