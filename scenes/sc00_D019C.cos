Include"yosuga.ch"

Function sc00_D019C()
	ScenarioEnter()
	ScPlayBgm("BGM15", 0)
	SetCg("B07a", 0, 0)
	Talk("Ryouhei", "RH040187")
	Mess("Hahaha, I've been looking for you, partner!")
	hitretResult := Hitret(29340)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_03M", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("As I arrived, Ryouhei was waiting with a thumbs-upÅ^expression.")
	hitretResult := Hitret(29341)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Upon seeing the excited Ryouhei and hearing a cheerfulÅ^Japanese orchestra, the heat and noise made me feelÅ^strange.")
	hitretResult := Hitret(29342)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What now?")
	hitretResult := Hitret(29343)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH040188")
	Mess("The bentou, of course. Once the ceremony's over, we haveÅ^a lunch meeting, so we have to carry them in before itÅ^starts.")
	hitretResult := Hitret(29344)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040189")
	Mess("Ours are in there too, and they're really delicious, youÅ^know? It's the only reason I'm helping out!")
	hitretResult := Hitret(29345)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Are they really that good?")
	hitretResult := Hitret(29346)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040190")
	Mess("When people say that something's so delicious that it'llÅ^knock your socks off, they're talking about this!")
	hitretResult := Hitret(29347)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_02M", 0, 0)
	Talk("Ryouhei", "RH040191")
	Mess("If this is just a \"bentou,\" then what my mother makesÅ^would just be a \"be.\"")
	hitretResult := Hitret(29348)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Sorry. I didn't understand any of that.")
	hitretResult := Hitret(29349)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_04M", 0, 0)
	Talk("Ryouhei", "RH040192")
	Mess("Well, if I used two hiragana, it'd sound indecent,Å^right? [TL Note: Two hiragana would be ben, which meansÅ^feces.]")
	hitretResult := Hitret(29350)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Yet again, he confident said it with an arrogantÅ^thumbs-up.")
	hitretResult := Hitret(29351)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It really is hot here.")
	hitretResult := Hitret(29352)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("Well, where are we taking these bentou to?")
	hitretResult := Hitret(29353)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At the entrance to the shrine office was a pile ofÅ^boxes.")
	hitretResult := Hitret(29354)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We'll probably just have to carry them inside.")
	hitretResult := Hitret(29355)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH040193")
	Mess("There's a reception room, and we're taking it there.Å^Nao's organizing it now, so we have to help her too.")
	hitretResult := Hitret(29356)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Roger that. Okay, let's get moving.")
	hitretResult := Hitret(29357)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("We made several trips back and forth, placing the bentouÅ^on the trays lined up for us.")
	hitretResult := Hitret(29358)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The tatami room with all the trays lined up in a row wasÅ^quite a sight to behold.")
	hitretResult := Hitret(29359)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san's father will probably be sitting here... inÅ^the seat of honor, and everyone else in rank order.")
	hitretResult := Hitret(29360)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH040194")
	Mess("Us commoners will just get a bentou and sit outside.")
	hitretResult := Hitret(29361)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO040031")
	Mess("It's about time for the ceremony to end. The elites willÅ^be here soon, so let's get out.")
	hitretResult := Hitret(29362)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah, I guess we should...Oooiii Soraa, we're goingÅ`")
	hitretResult := Hitret(29363)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040101")
	Mess("Mm.")
	hitretResult := Hitret(29364)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	SetCg("B07a", 0, 0)
	SetBustup("CD03_01M", 0, 0)
	Talk("Kazuha", "KA040503")
	Mess("Huh, everyone? ...Time for lunch?")
	hitretResult := Hitret(29365)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO040032")
	Mess("We've got your share, but... Are you eating here?")
	hitretResult := Hitret(29366)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040504")
	Mess("Yes, thank you. Then, I'll go get some tea.")
	hitretResult := Hitret(29367)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH040195")
	Mess("Okay, we'll go find a place to sit. Next to the shrineÅ^office is fine, right?")
	hitretResult := Hitret(29368)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040505")
	Mess("...Try not to get in the way of those actually going toÅ^worship at the shrine.")
	hitretResult := Hitret(29369)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(5)
	Talk("Ryouhei", "RH040196")
	Mess("You don't have to say something that obvious, huh?")
	hitretResult := Hitret(29370)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO040033")
	Mess("In your case, no matter how much someone emphasizes it,Å^there's no \"emphasizing too much.\"")
	hitretResult := Hitret(29371)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It is Ryouhei, after all...")
	hitretResult := Hitret(29372)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040197")
	Mess("Oi, oi, I'm a man that works when he's needed, you know?")
	hitretResult := Hitret(29373)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(3)
	SetBustup("CA03_13M", 0, 0)
	SetBustup("CF03_03M", 0, 0)
	Talk("Ryouhei", "RH040198")
	Mess("Right, Sora-chan?")
	hitretResult := Hitret(29374)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040102")
	Mess("G-Get away!")
	hitretResult := Hitret(29375)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se006", 0)
	SetBustup("CF03_06M", 0, 0)
	Shake(7)
	Talk("Ryouhei", "RH040199")
	Mess("Gwah!? ...Hehe...that's quite a kick you haveÅ^there...Sora-chan...")
	hitretResult := Hitret(29376)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se018", 0)
	BustupLeave(7, 0, 200, 1, 500, 3)
	SetBustup("CA03_04M", 0, 0)
	Talk("Sora", "SR040103")
	Mess("*Pant**pant*...What...is this?")
	hitretResult := Hitret(29377)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I really do wonder...")
	hitretResult := Hitret(29378)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Well, at least he can't be a bother to the worshippersÅ^when he's like that.")
	hitretResult := Hitret(29379)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CB03_03M", 0, 0)
	Talk("Nao", "NO040034")
	Mess("Now that the evil has been defeated, let's go eat.")
	hitretResult := Hitret(29380)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CF03_02M", 0, 0)
	Talk("Ryouhei", "RH040200")
	Mess("Agreed!")
	hitretResult := Hitret(29381)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...It's not defeated at all.")
	hitretResult := Hitret(29382)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	SetCg("B07a", 0, 0)
	SetBustup("CA03_01S", 0, 0)
	SetBustup("CB03_02S", 0, 0)
	SetBustup("CD03_01S", 0, 0)
	SetBustup("CF03_02S", 0, 0)
	Transition("WIP_LR", 500)
	Talk("êSÇÃê∫", "")
	Mess("Like Ryouhei said, the lunch was really good, making meÅ^a bit depressed that what I was making every dayÅ^couldn't even be called food.")
	hitretResult := Hitret(29383)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It seemed like they're from a traditional JapaneseÅ^restaurant. Just eating this makes helping out todayÅ^worthwhile.")
	hitretResult := Hitret(29384)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("...Which makes me no different than Ryouhei.")
	hitretResult := Hitret(29385)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Amatsume's going without lunch?")
	hitretResult := Hitret(29386)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_02S", 0, 0)
	Talk("Kazuha", "KA040506")
	Mess("Akira's eating inside. She is a member of the shrineÅ^after all, so she has to accompany the guests.")
	hitretResult := Hitret(29387)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_04S", 0, 0)
	Talk("Kazuha", "KA040507")
	Mess("I'm a bit worried that she might do something weird,Å^but...")
	hitretResult := Hitret(29388)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO040035")
	Mess("You're just worrying too much. She does this everyÅ^year... Here, have some tea.")
	hitretResult := Hitret(29389)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_02S", 0, 0)
	Talk("Kazuha", "KA040508")
	Mess("Yes, please... Kasugano-kun, your cup's empty too.")
	hitretResult := Hitret(29390)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, yeah...thanks.")
	hitretResult := Hitret(29391)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("What followed was a weird sequence of Nao-chan fillingÅ^Migiwa-san's cup, then Migiwa-san filling mine.")
	hitretResult := Hitret(29392)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Which means I have to pour someone else's cup, but...")
	hitretResult := Hitret(29393)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR040104")
	Mess("...No need.")
	hitretResult := Hitret(29394)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Sora quickly hid her cup.")
	hitretResult := Hitret(29395)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Both Ryouhei and Nao-chan's cups had plenty of tea left.")
	hitretResult := Hitret(29396)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_01S", 0, 0)
	Talk("Kazuha", "KA040509")
	Mess("...But that kid... Every year, she messes somethingÅ^up...")
	hitretResult := Hitret(29397)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_01S", 0, 0)
	Talk("Ryouhei", "RH040201")
	Mess("Nah, I wouldn't say that. Akira's failures are what addsÅ^spice to the festival.")
	hitretResult := Hitret(29398)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB03_01S", 0, 0)
	Talk("Nao", "NO040036")
	Mess("Well, she wouldn't be Akira if she did it all perfectly.")
	hitretResult := Hitret(29399)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_07S", 0, 0)
	Talk("Kazuha", "KA040510")
	Mess("What are you saying? What if she screwed up during theÅ^sacred ceremony...?")
	hitretResult := Hitret(29400)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040202")
	Mess("She hasn't screwed up anything... And next is the musicÅ^and the Kagura dance, which makes one suspect that,Å^huh...?")
	hitretResult := Hitret(29401)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040511")
	Mess("Don't say that! Doing something so shameful in front ofÅ^all the spectators...")
	hitretResult := Hitret(29402)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO040037")
	Mess("Like I said, you're worrying too much. Akira's beenÅ^practicing too.")
	hitretResult := Hitret(29403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_01S", 0, 0)
	Talk("Kazuha", "KA040512")
	Mess("It would be nice if that were true, but...")
	hitretResult := Hitret(29404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO040038")
	Mess("It's just about time. Come on Ryouhei, move it, move it.")
	hitretResult := Hitret(29405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Ryouhei", "RH040203")
	Mess("Yeaaah... Haruka, let's go.")
	hitretResult := Hitret(29406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The next job? What're we doing now?")
	hitretResult := Hitret(29407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040513")
	Mess("The music and Kagura dance are soon, so please set upÅ^the stage.")
	hitretResult := Hitret(29408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO040039")
	Mess("Get all the props ready. Ryouhei knows where they are.")
	hitretResult := Hitret(29409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha, one job after the other.")
	hitretResult := Hitret(29410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_02S", 0, 0)
	Talk("Kazuha", "KA040514")
	Mess("After that, it'll basically be all over, so we can goÅ^around the street stands together.")
	hitretResult := Hitret(29411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Okay. All right, it's the last job, so I'll do my best.")
	hitretResult := Hitret(29412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	Talk("êSÇÃê∫", "")
	Mess("But before we could finish moving everything, theÅ^performers arrived, so we had to start getting off theÅ^stage.")
	hitretResult := Hitret(29413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We were told to calm down because not all of them wentÅ^up at once, but just the thought of not seeing it afterÅ^all this work, I panicked.")
	hitretResult := Hitret(29414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Then they told me that there were many acts beforeÅ^Amatsume's turn, so I finally calmed down.")
	hitretResult := Hitret(29415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B07a", 0, 0)
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("After finally moving everything, we rushed to the KaguraÅ^hall.")
	hitretResult := Hitret(29416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040515")
	Mess("Kasugano-kun! Over here!")
	hitretResult := Hitret(29417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san had saved seats for us near the front.")
	hitretResult := Hitret(29418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_03M", 0, 0)
	Talk("Kazuha", "KA040516")
	Mess("You made it.")
	hitretResult := Hitret(29419)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("On the stage, there were some men dancing wildly.")
	hitretResult := Hitret(29420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume's turn was supposed to be after this.")
	hitretResult := Hitret(29421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ryouhei should be here soon too.")
	hitretResult := Hitret(29422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CB03_01M", 0, 0)
	Talk("Nao", "NO040040")
	Mess("In his case, he probably wanted to watch the SevenÅ^Deities of Good Luck more than Akira.")
	hitretResult := Hitret(29423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CF03_01M", 0, 0)
	Talk("Ryouhei", "RH040204")
	Mess("Being able to eat something so nice, of course I'd beÅ^happy.")
	hitretResult := Hitret(29424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CD03_01M", 0, 0)
	Talk("Kazuha", "KA040517")
	Mess("At least say something like it's because you've beenÅ^blessed by the gods.Å^...It's starting.")
	hitretResult := Hitret(29425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	ScPlayBgm("BGM21", 0)
	Talk("êSÇÃê∫", "")
	Mess("The wild men were going backstage when the chime of aÅ^bell could be heard.")
	hitretResult := Hitret(29426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Each ringing of the bell was followed by another, andÅ^the rhythm grew faster.")
	hitretResult := Hitret(29427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The audience had also gone so quiet that you could hearÅ^a pin drop, and I think that even the atmosphere of thisÅ^place changed.")
	hitretResult := Hitret(29428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Finally, Amatsume came out from the side of the stage,Å^eyes closed and making a strange face.")
	hitretResult := Hitret(29429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Unlike her usual cheerful, bouncy movements, she wasÅ^moving with gentle, flowing footsteps, ringing the bellÅ^as she glided toward the center of the stage.")
	hitretResult := Hitret(29430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...What's this dance called?")
	hitretResult := Hitret(29431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I asked Migiwa-san in a whisper, so I wouldn't disturbÅ^the others.")
	hitretResult := Hitret(29432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040518")
	Mess("...\"Kaibyakushinchi.\"")
	hitretResult := Hitret(29433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It was a story from a long time ago, when Okukozume wasÅ^just a village.")
	hitretResult := Hitret(29434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A legend of a girl from this land, struggling to protectÅ^the village with her servants from a foreign enemy.")
	hitretResult := Hitret(29435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The name of the girl was \"Princess Sayori.\" This shrineÅ^was made in her honor.")
	hitretResult := Hitret(29436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Before the festival, the servants decorated all of theÅ^countless Dousojin statues.")
	hitretResult := Hitret(29437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Amatsume, who was playing Princess Sayori, wasn'tÅ^dressed in her normal shirne maiden outfit, but aÅ^prettier one with many ornaments.")
	hitretResult := Hitret(29438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She looked off into the distance with eyes that didn'tÅ^seem like her own, with an earnest expression on herÅ^face.")
	hitretResult := Hitret(29439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It fit the image of an unhappy girl so perfectly thatÅ^I'd actually believe it if someone said that she was theÅ^reincarnation of a god.")
	hitretResult := Hitret(29440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...This Amatsume is cool too.")
	hitretResult := Hitret(29441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040519")
	Mess("...Yeah...")
	hitretResult := Hitret(29442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...You look good in a kimono too, so it wouldn't be outÅ^of place if you were up there too.")
	hitretResult := Hitret(29443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040520")
	Mess("N-No way...besides, Princess Sayori-sama is one person,Å^so if I were up there, I'd be the bad guy.")
	hitretResult := Hitret(29444)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Yeah, that's true...but I do want to see you twoÅ^dancing together.")
	hitretResult := Hitret(29445)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040521")
	Mess("...Geez... I'd never do it...")
	hitretResult := Hitret(29446)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Migiwa-san's voice turned sulky.")
	hitretResult := Hitret(29447)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The dance was nearing the climax, so it was getting moreÅ^and more exciting.")
	hitretResult := Hitret(29448)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("A huge war had ended and a disaster had been averted.Å^But the girl who had used up all her power had to sleep.")
	hitretResult := Hitret(29449)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The people, sorry to see her go, sent her off with aÅ^final farewell.")
	hitretResult := Hitret(29450)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And the chimes of the bell rang out sadly.")
	hitretResult := Hitret(29451)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if reluctant to part with her until the veryÅ^end...and then...")
	hitretResult := Hitret(29452)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm(1)
	BlackOut(100, 0)
	Talk("Akira", "AK040303")
	Mess("Unya...!?")
	hitretResult := Hitret(29453)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah...")
	hitretResult := Hitret(29454)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040522")
	Mess("...Akira!?")
	hitretResult := Hitret(29455)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Trying to spin around, Amatsume stepped on her own robesÅ^and fell straight down.")
	hitretResult := Hitret(29456)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM05", 0)
	SetCg("B07a", 0, 0)
	Talk("Akira", "AK040304")
	Mess("Ehehehe.")
	hitretResult := Hitret(29457)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Everyone started laughing at Amatsume, who had stood upÅ^and cutely stuck her tongue out.")
	hitretResult := Hitret(29458)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The dignified atmosphere which had permeated the placeÅ^was dispelled in one moment.")
	hitretResult := Hitret(29459)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_04M", 0, 0)
	Talk("Kazuha", "KA040523")
	Mess("Geez... That wasn't a time for laughing...")
	hitretResult := Hitret(29460)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But it's just like her.")
	hitretResult := Hitret(29461)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_08M", 0, 0)
	Talk("Kazuha", "KA040524")
	Mess("...That's true, but...that scary kid...being able toÅ^laugh at that time...")
	hitretResult := Hitret(29462)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Being able to calmly laugh after such a big screw up onÅ^the big stage makes me think how much of an airhead sheÅ^really is.")
	hitretResult := Hitret(29463)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Looking onto the stage again, Amatsume had noticed us,Å^and waved.")
	hitretResult := Hitret(29464)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040525")
	Mess("...Take it seriously for once...")
	hitretResult := Hitret(29465)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With that act, the room tried to hide their smilesÅ^again.")
	hitretResult := Hitret(29466)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And with that, the atmosphere of the sacred ceremonyÅ^receded, and with the closing of stage curtains, theÅ^bustling festival mood returned.")
	hitretResult := Hitret(29467)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(3000, 1)
	SetCg("B07a", 0, 0)
	SetBustup("CC06_02M", 0, 0)
	Talk("Akira", "AK040305")
	Mess("Haaaaaa, I'm tireedd. I was sooo nervousssÅ`Å`Å`Å`")
	hitretResult := Hitret(29468)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Having finished changing and coming out of the KaguraÅ^hall, Amatsume had a relaxed smile, in complete contrastÅ^to her words.")
	hitretResult := Hitret(29469)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_05M", 0, 0)
	Talk("Kazuha", "KA040526")
	Mess("How so!? Are you sure you didn't screw up on purpose atÅ^the end!? You ruined it!")
	hitretResult := Hitret(29470)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040306")
	Mess("Eh? Really?")
	hitretResult := Hitret(29471)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040527")
	Mess("Even if you do screw up, you're meant to just continue,Å^not do frivolous things.")
	hitretResult := Hitret(29472)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK040307")
	Mess("I danced properly, you know?")
	hitretResult := Hitret(29473)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040528")
	Mess("You waved to us, didn't you?")
	hitretResult := Hitret(29474)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_04M", 0, 0)
	Talk("Akira", "AK040308")
	Mess("Ah, yeah. Because for some reason, there seemed to be aÅ^nice atmosphere around you twoÅ`")
	hitretResult := Hitret(29475)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_10M", 0, 0)
	Talk("Kazuha", "KA040529")
	Mess("Wha!? What do you mean! We were just watching!!")
	hitretResult := Hitret(29476)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040309")
	Mess("Really? What do you say, Haru-kun?")
	hitretResult := Hitret(29477)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Eh!?...Umm.... We were just watching together.")
	hitretResult := Hitret(29478)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_01M", 0, 0)
	Talk("Akira", "AK040310")
	Mess("What, that's it?")
	hitretResult := Hitret(29479)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA040530")
	Mess("Yeah, we weren't doing anything else!!")
	hitretResult := Hitret(29480)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Though it's probably best if she didn't stress it soÅ^much...")
	hitretResult := Hitret(29481)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040311")
	Mess("Okay then, I'm off to say hello to the otherÅ^performersÅ`")
	hitretResult := Hitret(29482)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	Talk("êSÇÃê∫", "")
	Mess("And she left with the pattering of her sandals.")
	hitretResult := Hitret(29483)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I can't help but feel that she had a smile on her faceÅ^until the end...")
	hitretResult := Hitret(29484)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CD03_06M", 0, 0)
	Talk("Kazuha", "KA040531")
	Mess("You still have some work to do, right?")
	hitretResult := Hitret(29485)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, yeah! I'm off!")
	hitretResult := Hitret(29486)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Why is she mad at me too....?")
	hitretResult := Hitret(29487)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_d020")
	Return
	Change("EXIT_SCENARIO")
EndFunc

