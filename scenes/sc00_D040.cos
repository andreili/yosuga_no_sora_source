Include"yosuga.ch"

Function sc00_D040()
	ScenarioEnter()
	ScPlayBgm("BGM10", 0)
	SetCg("B12b", 0, 0)
	SetBustup("CD01_08S", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Since Kazuha was growing weaker and her steps were muchÅ^slower, I found her as soon as I left the shrine.")
	hitretResult := Hitret(32662)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even from behind, it looked like she was wobblingÅ^dangerously.")
	hitretResult := Hitret(32663)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Just as I thought that I needed to help her, sheÅ^staggered.")
	hitretResult := Hitret(32664)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("I quickly broke into a sprint.")
	hitretResult := Hitret(32665)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But before I could reach her, Kazuha put her handsÅ^forward, as if she was going to fall.")
	hitretResult := Hitret(32666)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Kazuha...!")
	hitretResult := Hitret(32667)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041326")
	Mess("Get away...")
	hitretResult := Hitret(32668)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("ED05", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Although she probably had it cleaned in time for the newÅ^semester, her white uniform had already turned brownÅ^from the dust.")
	hitretResult := Hitret(32669)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Perhaps not wanting to show how vulnerable she was,Å^Kazuha dropped to the ground and hugged herself.")
	hitretResult := Hitret(32670)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As if she was rejecting my touch.")
	hitretResult := Hitret(32671)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041327")
	Mess("...Go away...")
	hitretResult := Hitret(32672)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She murmured that once again.")
	hitretResult := Hitret(32673)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("However, I sat down beside her, ignoring her pleas.")
	hitretResult := Hitret(32674)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was such a pathetic boyfriend. I couldn't even stay byÅ^her side at a time like this.")
	hitretResult := Hitret(32675)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041328")
	Mess("I'm...so horrible........pushing away something soÅ^important to me... Hurting everyone...")
	hitretResult := Hitret(32676)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As Kazuha's voice trembled, I put my hand on herÅ^shoulder.")
	hitretResult := Hitret(32677)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Although she was surprised, she couldn't run awayÅ^anymore.")
	hitretResult := Hitret(32678)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041329")
	Mess("..........You were...there?")
	hitretResult := Hitret(32679)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah....")
	hitretResult := Hitret(32680)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041330")
	Mess("Then you saw it, right? Me doing something so cruel.")
	hitretResult := Hitret(32681)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041331")
	Mess("I loved Akira, I'd do anything for her sake. I thoughtÅ^that I could accept her for who she was.....")
	hitretResult := Hitret(32682)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041332")
	Mess("All this time, I thought that I was the only one whoÅ^could accept Akira's selfishness and her quirks.")
	hitretResult := Hitret(32683)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041333")
	Mess("That I was the only one that girl had, that I was theÅ^only one who understood her! That was my pride!")
	hitretResult := Hitret(32684)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041334")
	Mess("...So... I thought...")
	hitretResult := Hitret(32685)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She clumsily blurting out everything that was on herÅ^mind.")
	hitretResult := Hitret(32686)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041335")
	Mess("And yet.... And yet....*sob*")
	hitretResult := Hitret(32687)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And then her body started to shake violently yet again.")
	hitretResult := Hitret(32688)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She had probably recalled....that kiss from before.")
	hitretResult := Hitret(32689)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041336")
	Mess("...I was so scared... I'd never seen...that side ofÅ^Akira before... I ran...")
	hitretResult := Hitret(32690)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041337")
	Mess("...That girl wasn't Akira... The Akira I knew wasn'tÅ^like that.....")
	hitretResult := Hitret(32691)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041338")
	Mess("Suddenly that girl...smiled... And I realized...")
	hitretResult := Hitret(32692)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041339")
	Mess("That I...didn't see Akira as anything. Even though IÅ^was...supposed to be the one...who understood her.")
	hitretResult := Hitret(32693)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041340")
	Mess("I couldn't believe myself...All of my resolve...was forÅ^nothing...")
	hitretResult := Hitret(32694)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041341")
	Mess("I couldn't do anything...")
	hitretResult := Hitret(32695)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Torturing herself for her own foolishness while cryingÅ^was heartwrenching.")
	hitretResult := Hitret(32696)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Is that so?")
	hitretResult := Hitret(32697)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I didn't think that Kazuha was anything like the personÅ^that she described herself as.")
	hitretResult := Hitret(32698)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I think your feelings for Amatsume were real.Å^After all, you're suffering so much right now.")
	hitretResult := Hitret(32699)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041342")
	Mess("...No matter how much you love someone, once you hurtÅ^them, it's over...")
	hitretResult := Hitret(32700)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If you really do love someone so much that you can'tÅ^help it, there would be times that you can't help butÅ^hurt them.")
	hitretResult := Hitret(32701)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even Amatsume knew that doing such a thing wouldÅ^probably have hurt Kazuha.")
	hitretResult := Hitret(32702)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But even then, she knew that it was something that sheÅ^had to do for the sake of her sister's happiness.")
	hitretResult := Hitret(32703)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But by thinking about Amatsume, Kazuha was losing sightÅ^of everything else.")
	hitretResult := Hitret(32704)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even you scold Amatsume a lot, don't you?")
	hitretResult := Hitret(32705)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041343")
	Mess("...That's...completely different...")
	hitretResult := Hitret(32706)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You...didn't want to sacrifice Amatsume for the sake ofÅ^your own happiness.")
	hitretResult := Hitret(32707)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("And Amatsume didn't want to rob you of your happinessÅ^because of her.")
	hitretResult := Hitret(32708)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041344")
	Mess("......")
	hitretResult := Hitret(32709)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even though you were both looking at the same thing.")
	hitretResult := Hitret(32710)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Because of their mutual kindness, they ended up hurtingÅ^each other.")
	hitretResult := Hitret(32711)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even so, I was a bit envious at how much they loved eachÅ^other.")
	hitretResult := Hitret(32712)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Lately, Sora's been waking up on her own.")
	hitretResult := Hitret(32713)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("She's even going to and coming back from school withoutÅ^me. Before, she'd be in a bad mood if I wasn't with her,Å^but today she went home with the rest of her classmates.")
	hitretResult := Hitret(32714)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041345")
	Mess("...?")
	hitretResult := Hitret(32715)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even I was worried about that at the start. But afterÅ^watching them, I finally realized...")
	hitretResult := Hitret(32716)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Wasn't this...a good thing?")
	hitretResult := Hitret(32717)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041346")
	Mess("....I don't get it... I don't get what you're trying toÅ^say.")
	hitretResult := Hitret(32718)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Haha. I don't really get it either.")
	hitretResult := Hitret(32719)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I just want you to understand that it's not because IÅ^started hating Sora, and it's not because Sora startedÅ^hating me.")
	hitretResult := Hitret(32720)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's because Sora and I are brother and sister.")
	hitretResult := Hitret(32721)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041347")
	Mess("Me and... Akira are...")
	hitretResult := Hitret(32722)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("I was starting to think that you two really are sisters.")
	hitretResult := Hitret(32723)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041348")
	Mess("Eh...")
	hitretResult := Hitret(32724)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Both of you are extremely similar. The way you think.Å^The things you do. Even though your personalities are soÅ^different.")
	hitretResult := Hitret(32725)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041349")
	Mess("........")
	hitretResult := Hitret(32726)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("As your boyfriend, I love you.Å^But since she's my girlfriend's sister, I also likeÅ^Amatsume.")
	hitretResult := Hitret(32727)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So I want the both of you to be happy. If you'reÅ^suffering, I want to able to share your suffering. IÅ^want to help, even for minor things.")
	hitretResult := Hitret(32728)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041350")
	Mess("Even though I...hurt you that much...")
	hitretResult := Hitret(32729)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But I definitely still love you. So much that I couldÅ^feel your pain more than I could feel my own.")
	hitretResult := Hitret(32730)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("So that's why I want you to share your burdens with me.Å^The things you're worried about. The things you'reÅ^suffering from.")
	hitretResult := Hitret(32731)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("And... We'll think about them together, okay?")
	hitretResult := Hitret(32732)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041351")
	Mess("Haru... Haruka...")
	hitretResult := Hitret(32733)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B27b", 0, 600)
	ScPlayBgm("BGM19", 0)
	Update()
	Scroll(0, 0, 1500, 3, 0)
	Talk("Kazuha", "KA041352")
	Mess("Haruka...!")
	hitretResult := Hitret(32734)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As Kazuha clung onto the back of my neck, I reachedÅ^around her back and gently hugged her.")
	hitretResult := Hitret(32735)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041353")
	Mess("I don't...deserve to...be treated so...kindly by you...")
	hitretResult := Hitret(32736)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("We treat you kindly because we love you - both me andÅ^Amatsume. The same way you love us.")
	hitretResult := Hitret(32737)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Because we love each other... Isn't that a good enoughÅ^reason?")
	hitretResult := Hitret(32738)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Because we love each other, we misunderstand.Å^Because we love each other, we end up going differentÅ^ways.")
	hitretResult := Hitret(32739)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Because we only think of each other, we end up thinkingÅ^that wrong choices are actually the right ones.")
	hitretResult := Hitret(32740)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The position that Kazuha and Amatsume were put in wasn'tÅ^simple, like how mine and Sora's was.")
	hitretResult := Hitret(32741)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm sure you and Amatsume will be fine together.Å^But... do you think it's okay if I slip in there withÅ^you two?")
	hitretResult := Hitret(32742)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Kazuha", "KA041354")
	Mess("I don't want to lose them... Your warmth. YourÅ^kindness... And Akira's smile...")
	hitretResult := Hitret(32743)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Mm... You'll keep them for sure... There will always beÅ^a way....")
	hitretResult := Hitret(32744)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There are people who love Kazuha, like Amatsume and me.")
	hitretResult := Hitret(32745)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("People that watch over us.")
	hitretResult := Hitret(32746)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If we open our eyes, we can find the right answer.")
	hitretResult := Hitret(32747)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(3000, 1)
	Change("00_d041")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

