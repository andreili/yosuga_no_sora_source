Include"yosuga.ch"

Function sc00_B031()
	ScenarioEnter()
	ScPlayEnvSe("SE308", "1", 0)
	WindowView(1)
	Talk("モノローグ《奈緒》", "NO031837")
	Mess("The rain slowly subsides.")
	hitretResult := Hitret(17068)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031838")
	Mess("From that, I just started moving once more.")
	hitretResult := Hitret(17069)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031839")
	Mess("I waited under the shade for the rain to settle as I／stared blankly.")
	hitretResult := Hitret(17070)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE308", 1)
	ScPlayBgm("BGM10", 0)
	Talk("モノローグ《奈緒》", "NO031840")
	Mess("Thinking why they were in the bus, Haru-chan and／Sora-chan...")
	hitretResult := Hitret(17071)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031841")
	Mess("I tried to catch up with Haru-chan as soon as I heard／something happened.")
	hitretResult := Hitret(17072)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031842")
	Mess("But... The both of them went to ride a bus...")
	hitretResult := Hitret(17073)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031843")
	Mess("As the bus pass through me, I noticed Sora-chan's cold／stare, she stared at me with those eyes which forced me／to look away.")
	hitretResult := Hitret(17074)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031844")
	Mess("At that time, I felt it...")
	hitretResult := Hitret(17075)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031845")
	Mess("Aah... I won't ever be forgiven.")
	hitretResult := Hitret(17076)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031846")
	Mess("I wasn't able to reach out to Sora-chan properly, then I／took Haru-chan from her.")
	hitretResult := Hitret(17077)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031847")
	Mess("And then again... I'm alone... Left alone...")
	hitretResult := Hitret(17078)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031848")
	Mess("I'm all alone again...")
	hitretResult := Hitret(17079)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031849")
	Mess("But that's... how it really was.")
	hitretResult := Hitret(17080)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031850")
	Mess("Ever since then, I was always alone.")
	hitretResult := Hitret(17081)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031851")
	Mess("It just went back to it...")
	hitretResult := Hitret(17082)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031852")
	Mess("These past several months... were merely a dream...")
	hitretResult := Hitret(17083)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031853")
	Mess("I want to be with Haru-chan... so bad...")
	hitretResult := Hitret(17084)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B35c", 0, 0)
	Transition("WIP_TB", 1000)
	Talk("モノローグ《奈緒》", "NO031854")
	Mess("The rain stopped....")
	hitretResult := Hitret(17085)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031855")
	Mess("The neighbourhood once again stood quiet.")
	hitretResult := Hitret(17086)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031856")
	Mess("As if, my heart doesn't have anything to feel inside.")
	hitretResult := Hitret(17087)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031857")
	Mess("... What should I do...")
	hitretResult := Hitret(17088)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031858")
	Mess("...I should go back...")
	hitretResult := Hitret(17089)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031859")
	Mess("... Where should I go?")
	hitretResult := Hitret(17090)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031860")
	Mess("Even if I go anywhere, it probably won't change／anything.")
	hitretResult := Hitret(17091)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("……………………………")
	hitretResult := Hitret(17092)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031861")
	Mess("I took one step with my foot as I move on.")
	hitretResult := Hitret(17093)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("BLACK", 0, 0)
	Transition("CLOUD_A", 1000)
	Talk("モノローグ《奈緒》", "NO031862")
	Mess("But... It gets darker as I move on.")
	hitretResult := Hitret(17094)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031863")
	Mess("There are no electric lamps on this road, which fits／perfectly for me.")
	hitretResult := Hitret(17095)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031864")
	Mess("Just like that... I melted into it and I was consumed by／it...")
	hitretResult := Hitret(17096)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B12c", 0, 0)
	Transition("WIP_LR", 500)
	Talk("モノローグ《奈緒》", "NO031865")
	Mess("The lights outside started to shine bit by bit as I saw／them.")
	hitretResult := Hitret(17097)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031866")
	Mess("A light from the darkness... Then darkness from light／again...")
	hitretResult := Hitret(17098)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031867")
	Mess("I'm like a person who's bobbing up and down as I walked／along the street.")
	hitretResult := Hitret(17099)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("モノローグ《奈緒》", "NO031868")
	Mess("The every day light that always shine... Won't be coming／back again, right...")
	hitretResult := Hitret(17100)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("モノローグ《奈緒》", "NO031869")
	Mess("...Haru-chan...")
	hitretResult := Hitret(17101)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("………………………………………")
	hitretResult := Hitret(17102)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("………………………………")
	hitretResult := Hitret(17103)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("………………………")
	hitretResult := Hitret(17104)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	WindowView(0)
	Talk("Haruka", "")
	Mess("Nao-chann!!!!")
	hitretResult := Hitret(17105)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031870")
	Mess(".......Eh?")
	hitretResult := Hitret(17106)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B34c", 400, 0)
	SetBustup("CB01_10M", 0, 0)
	Talk("Nao", "NO031871")
	Mess("...Wh-why...")
	hitretResult := Hitret(17107)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("…………………………")
	hitretResult := Hitret(17108)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("After that, we got off the bus, we went through the／trees as we searched for a way back.")
	hitretResult := Hitret(17109)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("During that time, we unexpectedly met Migiwa-san outside／who was in her car...")
	hitretResult := Hitret(17110)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("As soon as we got back to the town, I went down right／away.")
	hitretResult := Hitret(17111)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Then I saw Nao-chan right away.")
	hitretResult := Hitret(17112)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I told you that we would go home together, yet... I'm／sorry...")
	hitretResult := Hitret(17113)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031872")
	Mess("...Haru-chan...")
	hitretResult := Hitret(17114)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan...")
	hitretResult := Hitret(17115)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM08", 0)
	SetBustup("CB01_10S", 0, 0)
	Talk("Nao", "NO031873")
	Mess("………! ……………………")
	hitretResult := Hitret(17116)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I noticed it right away, Nao-chan slowly took a step／back.")
	hitretResult := Hitret(17117)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("?")
	hitretResult := Hitret(17118)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031874")
	Mess("... Sorry... We shouldn't...")
	hitretResult := Hitret(17119)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031875")
	Mess("Even if Sora-chan... said we're not supposed to talk...")
	hitretResult := Hitret(17120)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031876")
	Mess("Even so... I know it was wrong yet I talked to you...")
	hitretResult := Hitret(17121)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_12S", 0, 0)
	Talk("Nao", "NO031877")
	Mess("...... Even though I know... I'm not really the right／girl to accept your feelings right?")
	hitretResult := Hitret(17122)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031878")
	Mess("I felt it when you came to me again, Haru-chan...")
	hitretResult := Hitret(17123)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031879")
	Mess("I... didn't even notice it... Until I felt it.")
	hitretResult := Hitret(17124)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031880")
	Mess("But... I really can't accept your feelings...／Haru-chan... Even though I really felt them...")
	hitretResult := Hitret(17125)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031881")
	Mess("I was happy... I was really happy...")
	hitretResult := Hitret(17126)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031882")
	Mess("I felt lonely until now... Though it's so easy just to／blow those feelings away...")
	hitretResult := Hitret(17127)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031883")
	Mess("Perhaps, I was mistaken right from the start...")
	hitretResult := Hitret(17128)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("……………………………")
	hitretResult := Hitret(17129)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_10S", 0, 0)
	Talk("Nao", "NO031884")
	Mess("We really can't... Go back to what we use to be... Back／to that time...")
	hitretResult := Hitret(17130)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031885")
	Mess("I enjoyed all the fun moments, when we went out／together... We were always great together and seemed to／be connected all the time...")
	hitretResult := Hitret(17131)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No...")
	hitretResult := Hitret(17132)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031886")
	Mess("...Eh?")
	hitretResult := Hitret(17133)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("We'll be together always starting from today.")
	hitretResult := Hitret(17134)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_07S", 0, 0)
	Talk("Nao", "NO031887")
	Mess("There's nothing anymore...")
	hitretResult := Hitret(17135)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031888")
	Mess("Everyone... always said to stop...")
	hitretResult := Hitret(17136)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_09M", 0, 0)
	Talk("Nao", "NO031889")
	Mess("I was hated... I... again...")
	hitretResult := Hitret(17137)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031890")
	Mess("No one trusts me anymore...")
	hitretResult := Hitret(17138)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031891")
	Mess("Whatever it was... It can never be forgiven...")
	hitretResult := Hitret(17139)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's... Should I be angry or not?")
	hitretResult := Hitret(17140)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_10M", 0, 0)
	Talk("Nao", "NO031892")
	Mess("?")
	hitretResult := Hitret(17141)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It certainly was something terrible... But, you forgot／about it Nao-chan, I wasn't really angry back then.")
	hitretResult := Hitret(17142)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Everyone was troubled by it, because it's something／important to you Nao-chan.")
	hitretResult := Hitret(17144)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("They were worried... Nao-chan.")
	hitretResult := Hitret(17145)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_06M", 0, 0)
	Talk("Nao", "NO031893")
	Mess("Th-That's a lie...")
	hitretResult := Hitret(17146)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Because they were worried about you Nao-chan, just like／how you were worried by your parents divorce right?")
	hitretResult := Hitret(17147)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_09M", 0, 0)
	Talk("Nao", "NO031894")
	Mess("Th-That's... because I was always the problem... so they／kept on monitoring me...")
	hitretResult := Hitret(17148)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("They were worried... Right? Nao-chan...")
	hitretResult := Hitret(17149)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_10M", 0, 0)
	Talk("Nao", "NO031895")
	Mess("Uhh... No way...")
	hitretResult := Hitret(17150)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Probably, even my grandfather... My mother, and my／father...")
	hitretResult := Hitret(17151)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...They could have been worried about your well-being／Nao-chan.")
	hitretResult := Hitret(17152)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031896")
	Mess("……………………………")
	hitretResult := Hitret(17153)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even so... I was worried... Just like everyone, but that／feeling made me want to be closer to you more Nao-chan.")
	hitretResult := Hitret(17154)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("The only reason for me... to go back to what we used to／be.")
	hitretResult := Hitret(17155)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But, it's different now... I will take responsibility on／my actions.")
	hitretResult := Hitret(17156)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("And... I'll give my all.")
	hitretResult := Hitret(17157)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Because... I don't want you to feel that kind of pain／again, Nao-chan.")
	hitretResult := Hitret(17158)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...I love you after all... Nao-chan.")
	hitretResult := Hitret(17159)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_11M", 0, 0)
	Talk("Nao", "NO031897")
	Mess("Haru... cha...")
	hitretResult := Hitret(17160)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Also... Because of Sora... I made you worried again...／She even told you so many bad things... I'm sorry...")
	hitretResult := Hitret(17161)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even Sora... Sora has come to understand about it now...")
	hitretResult := Hitret(17162)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("To keep... This precious family......")
	hitretResult := Hitret(17163)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_09M", 0, 0)
	Talk("Nao", "NO031898")
	Mess("……………………………")
	hitretResult := Hitret(17164)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan too...")
	hitretResult := Hitret(17165)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_11M", 0, 0)
	Talk("Nao", "NO031899")
	Mess("... Me...... Too?")
	hitretResult := Hitret(17166)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... Nao-chan, you're our big sister, right...")
	hitretResult := Hitret(17167)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Even if summer was just a short time, we always come／here to hang out. We looked like we were real siblings,／you treated us kindly...")
	hitretResult := Hitret(17168)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That was... us...")
	hitretResult := Hitret(17169)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's why... Even with how Sora feels towards you...／Together...")
	hitretResult := Hitret(17170)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Nao-chan... From now on we will...")
	hitretResult := Hitret(17171)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM17", 0)
	Talk("Haruka", "")
	Mess("We will always be by your side...")
	hitretResult := Hitret(17172)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_12L", 0, 0)
	Talk("Nao", "NO031900")
	Mess("...Haru-chan...")
	hitretResult := Hitret(17173)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031901")
	Mess("Ugghhh... Thank you... Th-Thank youu...")
	hitretResult := Hitret(17174)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031902")
	Mess("Haru-chan... Haru-chan, you still haven't change ever／since...")
	hitretResult := Hitret(17175)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031903")
	Mess("...You're still kind and gentle, no matter what... As／always...")
	hitretResult := Hitret(17176)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...You too... Nao-chan.")
	hitretResult := Hitret(17177)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031904")
	Mess("...Haru-chan...")
	hitretResult := Hitret(17178)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("The reason I saw why Nao-chan is in pain because she's／always alone.")
	hitretResult := Hitret(17179)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Always, I was the cause of her pain.")
	hitretResult := Hitret(17180)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("But I also became worried about it when I felt that／Nao-chan was always in pain.")
	hitretResult := Hitret(17181)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I don't want her to cry like that again...")
	hitretResult := Hitret(17182)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("What we did back then, Nao-chan carried the／responsibility alone.")
	hitretResult := Hitret(17183)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("When we were young, I was already attracted to Nao-chan.")
	hitretResult := Hitret(17184)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("And then I noticed it, when I met Nao-chan again, I／remembered the sensation myself.")
	hitretResult := Hitret(17185)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("That's why... I don't want Nao-chan to feel deserted and／lonely again.")
	hitretResult := Hitret(17186)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Starting right now... I won't make it happen again...")
	hitretResult := Hitret(17187)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Nao-chan...")
	hitretResult := Hitret(17188)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I slowly draw my face to Nao-chan.")
	hitretResult := Hitret(17189)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("The tears that streamed down on her lonely face,／refracts a glittering light from it.")
	hitretResult := Hitret(17190)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("I wipe them with my fingers, as I brush my fingers it／along the side of her face.")
	hitretResult := Hitret(17191)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031905")
	Mess("...Nnghh...")
	hitretResult := Hitret(17192)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Nao-chan slowly opens her eyes.")
	hitretResult := Hitret(17193)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EB06a", 0, 258)
	Scroll(0, 0, 15000, 0, 0)
	Talk("Nao", "NO031906")
	Mess("... *Kiss*...")
	hitretResult := Hitret(17194)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("And then, her lips shivered as we kiss.")
	hitretResult := Hitret(17195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031907")
	Mess("...Nghh...")
	hitretResult := Hitret(17196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("We quietly kissed each other as we got tangle up in the／heat.")
	hitretResult := Hitret(17197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("And then, we hug each other tightly, in each other's／arms.")
	hitretResult := Hitret(17198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("We were in-sync, as if we're taking up a one-way road as／we let out all of our feelings.")
	hitretResult := Hitret(17199)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("From now on, I won't be making the same mistake in the／past that led to her sadness.")
	hitretResult := Hitret(17200)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("It's heart-breaking to know that Nao-chan... waited long／enough for me.")
	hitretResult := Hitret(17201)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031908")
	Mess("Nghh... nghhh...")
	hitretResult := Hitret(17202)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("We kept on sucking each other's lips.")
	hitretResult := Hitret(17203)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("To be the big sister we could always depend on, I felt／that small existence as it fleeted through as we／embraced each other tightly.")
	hitretResult := Hitret(17204)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("To endure everything... Up till now...")
	hitretResult := Hitret(17205)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...I'm really sorry, Nao-chan...")
	hitretResult := Hitret(17206)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031909")
	Mess("...No, don't talk about it anymore...")
	hitretResult := Hitret(17207)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("EB06b", 0, 0)
	Talk("Nao", "NO031910")
	Mess("...I love you, Haru-chan... nghhh...")
	hitretResult := Hitret(17208)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("And again, we kissed.")
	hitretResult := Hitret(17209)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Nao-chan's tears touched and streamed on my cheeks.")
	hitretResult := Hitret(17210)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("Those tears, made our lips warmer as they touched.")
	hitretResult := Hitret(17211)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(0, 1)
	ScWait(3000, 0)
	Change("00_b031b")
	Return
	Change("EXIT_SCENARIO")
EndFunc

