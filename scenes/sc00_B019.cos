Include"yosuga.ch"

Function sc00_B019()
	ScenarioEnter()
	ScPlaySe("SE357", 0)
	SetCg("B01a", 0, 0)
	Update()
	ScWait(1500, 0)
	BlackOut(0, 1)
	SetCg("B02a", 0, 0)
	Transition("WIP_LR", 500)
	ScPlayBgm("BGM03", 0)
	Talk("êSÇÃê∫", "")
	Mess("I was busy ever since this morning.")
	hitretResult := Hitret(14394)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I just finished the things that I had to do yesterday,Å^like cleaning and washing the laundry.")
	hitretResult := Hitret(14395)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As soon as I finished it, I went to make our meals.")
	hitretResult := Hitret(14396)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I woke up early today, and I washed the plates rightÅ^away, I also refilled the bottles of barley tea.")
	hitretResult := Hitret(14397)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I think Sora said she wants to eat something else.")
	hitretResult := Hitret(14398)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I won't do such thing again today, though it would beÅ^fine if I ever come back late since I've made somethingÅ^to eat already.")
	hitretResult := Hitret(14399)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Putting it simply, I poured hot water on the whiteÅ^noodles and molded the rice balls.")
	hitretResult := Hitret(14400)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And, I boiled more water so I can make more barley tea.")
	hitretResult := Hitret(14401)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After all, I might ran into Yahiro-san in her place ifÅ^I'd need to buy juice and candies.")
	hitretResult := Hitret(14402)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With this, Sora won't be hungry anymore.")
	hitretResult := Hitret(14403)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Though I should be wary of the time as well, she mightÅ^just eat some instant curry so I need to make sure sheÅ^shouldn't.")
	hitretResult := Hitret(14404)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... *Phew*... I guess I'm about to be done...")
	hitretResult := Hitret(14405)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I paused for a while and sat on my chair. Then I lookedÅ^at the door of Sora's room.")
	hitretResult := Hitret(14406)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("From cleaning and doing the laundry today, I've preparedÅ^so many food as well, but Sora hasn't come out of herÅ^room yet.")
	hitretResult := Hitret(14407)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I don't know why she doesn't wake up early in theÅ^morning, though it looked like she became moreÅ^unsociable during this summer vacation.")
	hitretResult := Hitret(14408)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If that's really the case, I really need to bring SoraÅ^outside...")
	hitretResult := Hitret(14409)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	ScPlaySe("SE101", 0)
	Talk("Haruka", "")
	Mess("... Sora?")
	hitretResult := Hitret(14410)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I tried calling her out of her room.")
	hitretResult := Hitret(14411)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Are you awake?")
	hitretResult := Hitret(14412)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("ÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(14413)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... Maybe she's still asleep...")
	hitretResult := Hitret(14414)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sora... Come out, the meal's ready, aren't you gonnaÅ^eat?")
	hitretResult := Hitret(14415)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Also... Don't you want to go out today?")
	hitretResult := Hitret(14416)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR030173")
	Mess("...I don't want to go.")
	hitretResult := Hitret(14417)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You're always holed up in there, don't you wanna haveÅ^some fresh air?")
	hitretResult := Hitret(14418)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR030174")
	Mess("...I'm fine.")
	hitretResult := Hitret(14419)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Okay then. I'll be going out again, I also preparedÅ^some food for the whole day, so just eat them okay?")
	hitretResult := Hitret(14420)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030942")
	Mess("Is anybody home?")
	hitretResult := Hitret(14421)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah... Coming!!")
	hitretResult := Hitret(14422)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B01a", 0, 0)
	SetBustup("CB01_11M", 0, 0)
	Talk("Nao", "NO030943")
	Mess("Hello...")
	hitretResult := Hitret(14423)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, yeah. Hello Nao-chan.")
	hitretResult := Hitret(14424)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I looked at Nao-chan's face, I remembered the thingsÅ^that happened yesterday.")
	hitretResult := Hitret(14425)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("And again, I felt embarrassed so I tried to look around.")
	hitretResult := Hitret(14426)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Confessing my feelings, I feel so relieved even thoughÅ^there was a sudden change in our lives, having saidÅ^those feelings feels so good as we understood eachÅ^other.")
	hitretResult := Hitret(14427)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I can still feel it until now, I think I love her moreÅ^than I used to since I feel like more of my feelings areÅ^piling up.")
	hitretResult := Hitret(14428)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030944")
	Mess("Ah, oh yeah... What shall we do today?")
	hitretResult := Hitret(14429)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah... I finished the things I need to do at home... SoÅ^for today...")
	hitretResult := Hitret(14430)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030945")
	Mess("Since you have finished making food... If it's fine withÅ^you, don't you want to go somewhere else?")
	hitretResult := Hitret(14431)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Yeah, oh... you're wearing your uniform today... Why?")
	hitretResult := Hitret(14432)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO030946")
	Mess("Ah, this? Just to make my parents quiet, I told themÅ^that I'll be doing some school activity.")
	hitretResult := Hitret(14433)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, I see...")
	hitretResult := Hitret(14434)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, how about continue where we left yesterday.")
	hitretResult := Hitret(14435)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_11L", 0, 0)
	Talk("Nao", "NO030947")
	Mess("Eh? Y-Yesterday...")
	hitretResult := Hitret(14436)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Aah, you know, the swimming practice...")
	hitretResult := Hitret(14437)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02L", 0, 0)
	Talk("Nao", "NO030948")
	Mess("Ah, ahahaha... I-I see...")
	hitretResult := Hitret(14438)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("What were you thinking about?")
	hitretResult := Hitret(14439)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_03L", 0, 0)
	Talk("Nao", "NO030949")
	Mess("Geez... It's nothing bad!")
	hitretResult := Hitret(14440)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("se009", 0)
	Quake(16, 0, 1, 2, 30)
	Talk("Haruka", "")
	Mess("Ouch... Aha-hahaha...")
	hitretResult := Hitret(14441)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It seems to be really hot. Why don't you wait inside.")
	hitretResult := Hitret(14442)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01L", 0, 0)
	Talk("Nao", "NO030950")
	Mess("I-It's fine... I'll be fine here...")
	hitretResult := Hitret(14443)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Got it, just wait for me a little, okay?")
	hitretResult := Hitret(14444)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B02a", 0, 0)
	Transition("WIP_LR", 500)
	Talk("Haruka", "")
	Mess("SoraÅ`, I'll be going outside now so... Look after theÅ^house for me, okay?")
	hitretResult := Hitret(14445)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I made the meal already, there are also sweets.")
	hitretResult := Hitret(14446)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She didn't answer at all, though she probably heardÅ^about what I'm going to do since we talked about it asÅ^well, I also prepared a lot of things.")
	hitretResult := Hitret(14447)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I took a towel and my swimming trunks, and then I pouredÅ^some green tea in my water bottle.")
	hitretResult := Hitret(14448)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I changed into my uniform and went out.")
	hitretResult := Hitret(14449)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B01a", 0, 0)
	SetBustup("CB01_01L", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Haruka", "")
	Mess("Sorry for the wait.")
	hitretResult := Hitret(14450)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030951")
	Mess("That was fast, as expected from guys.")
	hitretResult := Hitret(14451)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Well, shall we go, it looks like it's going to be muchÅ^hotter today.")
	hitretResult := Hitret(14452)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030952")
	Mess("That's right.")
	hitretResult := Hitret(14453)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("I closed the door by the main entrance, then Nao-chanÅ^and I went on our way to the school.")
	hitretResult := Hitret(14454)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(1000, 0)
	SetCg("B12a", 0, 0)
	Transition("WIP_RL", 500)
	ScPlayEnvSe("SE301", "", 0)
	SetBustup("CB01_02L", 0, 0)
	Talk("Nao", "NO030953")
	Mess("Ah, what's Sora-chan doing?")
	hitretResult := Hitret(14455)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("She locked herself in her room.")
	hitretResult := Hitret(14456)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("For the time being, I made a lot of food for her.")
	hitretResult := Hitret(14457)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01L", 0, 0)
	Talk("Nao", "NO030954")
	Mess("I see... It would be much better if the both of youÅ^would come...")
	hitretResult := Hitret(14458)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I already asked her to go out, but she said she doesn'tÅ^like to come, especially at this time when it's hot,Å^it's seems impossible for her to come out.")
	hitretResult := Hitret(14459)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO030955")
	Mess("I see...")
	hitretResult := Hitret(14460)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("ÅcÅcÅcÅc?")
	hitretResult := Hitret(14461)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_01M", 0, 0)
	Talk("Nao", "NO030956")
	Mess("... What's the matter?")
	hitretResult := Hitret(14462)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Hmm... Nothing...?")
	hitretResult := Hitret(14463)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I felt like she somehow glanced at me.")
	hitretResult := Hitret(14464)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I looked around in the neighborhood as I feel likeÅ^someone's watching us, though I saw no one.")
	hitretResult := Hitret(14465)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_02M", 0, 0)
	Talk("Nao", "NO030957")
	Mess("... Who are you looking for?")
	hitretResult := Hitret(14466)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I think I was mistaken, it must be my imagination...")
	hitretResult := Hitret(14467)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As we were going further away, I looked back the houseÅ^though I couldn't see Sora anymore.")
	hitretResult := Hitret(14468)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("... Must be imagination... I guess...")
	hitretResult := Hitret(14469)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sorry, it's nothing. Come on, let's go?")
	hitretResult := Hitret(14470)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB01_03M", 0, 0)
	Talk("Nao", "NO030958")
	Mess("Yeah.")
	hitretResult := Hitret(14471)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("I tried taking those thoughts out of my head, thenÅ^Nao-chan and I once more went on our way to school")
	hitretResult := Hitret(14472)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopEnvSe("SE301", 1)
	EyeCatchEnter("", 0)
	Change("00_b020")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

