Include"yosuga.ch"

Function sc00_C022B()
	ScenarioEnter()
	SetCg("B32b", 0, 0)
	ScPlayBgm("BGM07", 0)
	Transition("WIP_LR", 500)
	Talk("êSÇÃê∫", "")
	Mess("The first thing that I did when I returned home, was toÅ^go to the window screens and opened them.")
	hitretResult := Hitret(23192)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Letting the kitchen's ventilation spin up, and thenÅ^having the fan to point at the direction of the windowÅ^to get ventilation. In a short while, the temperatureÅ^should go down.")
	hitretResult := Hitret(23193)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Because we were absent for a few days, there were no iceÅ^or barley tea. Even the last cake was snatched by Sora,Å^so I can only pitifully drink tepid tea at the station.")
	hitretResult := Hitret(23194)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though I can go to Hiru-san's place to buy, but IÅ^don't want to go out already.")
	hitretResult := Hitret(23195)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Maybe I'm slightly tired...... I sit beside the window,Å^and then lie on the floor and look at the ceiling aboveÅ^me.")
	hitretResult := Hitret(23196)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("All the accumulated fatigue suddenly seem to come at me,Å^and my body is sluggish and heavy.")
	hitretResult := Hitret(23197)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though I don't want to think about anything, butÅ^starting from just now, there are many things that comeÅ^to my mind.")
	hitretResult := Hitret(23198)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The things about my aunt, the things about home, andÅ^what Sora said to me earlier...........")
	hitretResult := Hitret(23199)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("GO back? I didn't think about this at all recently.")
	hitretResult := Hitret(23200)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The idea of just going back itself is simple.")
	hitretResult := Hitret(23201)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But even if I do go back, my parents aren't there. ForÅ^me to abandon the place I stayed, is there anythingÅ^there at all?")
	hitretResult := Hitret(23202)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even if I can still remember the things about my parentsÅ^being close to me, then what?")
	hitretResult := Hitret(23203)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My aunt says that I will come to accept this naturally.Å^But I am unable to do so yet.")
	hitretResult := Hitret(23204)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("So I'm keeping my distance just like this.")
	hitretResult := Hitret(23205)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The place where I had pleasant memories when I was aÅ^child.")
	hitretResult := Hitret(23206)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My parents will be coming to fetch us if we waitÅ^here...... I indulge myself with this childlikeÅ^sentimentality.")
	hitretResult := Hitret(23207)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I haven't changed at all...... I'm still so weak.")
	hitretResult := Hitret(23208)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("êSÇÃê∫", "")
	Mess("And then, perhaps this time, I have another excuse withÅ^the name of Akira.")
	hitretResult := Hitret(23209)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("........ Akira.")
	hitretResult := Hitret(23210)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02L", 0, 0)
	Talk("Akira", "AK021217")
	Mess("What is it~?")
	hitretResult := Hitret(23211)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Quake(16, 0, 1, 2, 30)
	Talk("Haruka", "")
	Mess("UWAAaa!? Tottotto!?")
	hitretResult := Hitret(23212)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021218")
	Mess("Tottotto?")
	hitretResult := Hitret(23213)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira sidles up to me who was falling backwards becauseÅ^of the surprise.")
	hitretResult := Hitret(23214)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("W-when did you come!?")
	hitretResult := Hitret(23215)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM05", 0)
	SetBustup("CC02_01L", 0, 0)
	Talk("Akira", "AK021219")
	Mess("Welcome back, Haru-kun!")
	hitretResult := Hitret(23216)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I-I'm home.")
	hitretResult := Hitret(23217)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA02_04S", 0, 0)
	Talk("Sora", "SR020170")
	Mess("...... Nois......y!?")
	hitretResult := Hitret(23218)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02S", 4, 0)
	Talk("Akira", "AK021220")
	Mess("Welcome back too, Sora-chan-!")
	hitretResult := Hitret(23219)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Shake(4)
	Talk("Sora", "SR020171")
	Mess("Auuu!?")
	hitretResult := Hitret(23220)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira hugs the complaining Sora in a tackling manner.")
	hitretResult := Hitret(23221)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ah, Akira...... What's wrong?")
	hitretResult := Hitret(23222)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04S", 0, 0)
	Talk("Sora", "SR020172")
	Mess("...... Ak..... Akira?")
	hitretResult := Hitret(23223)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CA02_04M", 8, 0)
	SetBustup("CC02_02M", 4, 0)
	Talk("Akira", "AK021221")
	Mess("You told me that you're coming back today. So, I cameÅ^here to see whether you returned.")
	hitretResult := Hitret(23224)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021222")
	Mess("Also, if Haru-kun's sleeping I can get to see yourÅ^sleeping face.")
	hitretResult := Hitret(23225)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_06M", 0, 0)
	Talk("Sora", "SR020173")
	Mess("Illegal trespassing.......")
	hitretResult := Hitret(23226)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("H-hey!! Don't say something like that.")
	hitretResult := Hitret(23227)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	SetBustup("CC02_05M", 0, 0)
	Talk("Akira", "AK021223")
	Mess("Sorry, I just conveniently entered here withoutÅ^greeting. Should I go back to the entrance and come in?")
	hitretResult := Hitret(23228)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("N-no, that's okay. This is alright.")
	hitretResult := Hitret(23229)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK021224")
	Mess("Ah, that's right, have the two of you eaten dinner?")
	hitretResult := Hitret(23230)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, not yet. Today's really tiring, I intend to makeÅ^something simple.")
	hitretResult := Hitret(23231)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021225")
	Mess("Ah~ Then this is a good timing. Here, this is curry thatÅ^I made with the pot that you gave me! I'll share it withÅ^you!")
	hitretResult := Hitret(23232)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Is that okay? Giving it to us?")
	hitretResult := Hitret(23233)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_03M", 0, 0)
	Talk("Akira", "AK021226")
	Mess("Yeah, today Hiro-neechan went out for a gathering, andÅ^I'm the only one for today's dinner, curry spoils easilyÅ^in summer and I'll be glad if both of you eat it.")
	hitretResult := Hitret(23234)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then how about eating together?")
	hitretResult := Hitret(23235)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK021227")
	Mess("Ehh, is that alright?")
	hitretResult := Hitret(23236)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's okay right, Sora?")
	hitretResult := Hitret(23237)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021228")
	Mess("Yeah I got it! Then I'll bring it straight away.")
	hitretResult := Hitret(23238)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	Talk("Haruka", "")
	Mess("Really, Akira's still the same.......")
	hitretResult := Hitret(23239)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04M", 0, 0)
	Talk("Sora", "SR020174")
	Mess("........Akiraa?")
	hitretResult := Hitret(23240)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh, ahh, wh-what's the matter?")
	hitretResult := Hitret(23241)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_05M", 0, 0)
	Talk("Sora", "SR020175")
	Mess("When did it start...........")
	hitretResult := Hitret(23242)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Isn't Sora addressing her as Akira, right? So I'mÅ^also......")
	hitretResult := Hitret(23243)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04M", 0, 0)
	Talk("Sora", "SR020176")
	Mess("Liar.")
	hitretResult := Hitret(23244)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("J-Just kidding...... I'm sorry, I didn't inform you.")
	hitretResult := Hitret(23245)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Akira and I..... started earlier.......")
	hitretResult := Hitret(23246)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_11M", 0, 0)
	Talk("Sora", "SR020177")
	Mess("......It's..... f-fine. You don't need toÅ^explain.........")
	hitretResult := Hitret(23247)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... Sorry. Is it possible, Sora, you dislike Akira?")
	hitretResult := Hitret(23248)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR020178")
	Mess("........ Not particularly. Haru, is someone who broods,Å^perhaps finding someone like her is a good thing......")
	hitretResult := Hitret(23249)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Someone like her...... saying something like that, am IÅ^a brooding person?")
	hitretResult := Hitret(23250)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_06M", 0, 0)
	Talk("Sora", "SR020179")
	Mess("Fnnn........")
	hitretResult := Hitret(23251)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh-, laughing through your nose really makes me feelÅ^annoyed.")
	hitretResult := Hitret(23252)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021229")
	Mess("Haru-kuun! I brought it!")
	hitretResult := Hitret(23253)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("F-fast....... Are you sure you didn't bring it here fromÅ^the start...... The whole pot.")
	hitretResult := Hitret(23254)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_08M", 0, 0)
	Talk("Sora", "SR020180")
	Mess("Akira is kind..... But, sometimes scary.")
	hitretResult := Hitret(23255)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("B-But Akira's a cheerful person, even though she's aÅ^little childish, she isn't a reckless child.")
	hitretResult := Hitret(23256)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020181")
	Mess("There are times I don't know what she is thinking.......Å^Even though she's smiling.")
	hitretResult := Hitret(23257)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("ÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(23258)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("SOra probably senses something, and it surprises me thatÅ^she said the same thing as Yahiro-san.")
	hitretResult := Hitret(23259)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR020182")
	Mess("But, I don't hate her...... If Haru likes it, it's fineÅ^to just date her.")
	hitretResult := Hitret(23260)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Mmm...... Thank you...... Sora.")
	hitretResult := Hitret(23261)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("Haruka", "")
	Mess("Akira! You can come in now!")
	hitretResult := Hitret(23262)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021230")
	Mess("Yeah! Excuse me for entering!")
	hitretResult := Hitret(23263)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK021231")
	Mess("Sorry to keep you waiting! Can you let me use theÅ^kitchen? I'll prepare everything.")
	hitretResult := Hitret(23264)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("âlÇÕñlÇÁÇ™ÉvÉåÉ[ÉìÉgÇµÇΩìÁÇÃëºÇ…ÅAÉrÉjÅ[Éãë‹ÇéËÇ…íÒÇ∞Çƒ")
	Mess("Ç¢ÇΩÅB")
	hitretResult := Hitret(23265)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Inside the plastic bag...... Are ingredients and pears.Å^Could it be possible that even dessert is prepared asÅ^well?")
	hitretResult := Hitret(23266)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Let's help too. Right, Sora?")
	hitretResult := Hitret(23267)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_09M", 0, 0)
	Talk("Sora", "SR020183")
	Mess("...... Troublesome.")
	hitretResult := Hitret(23268)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hey!")
	hitretResult := Hitret(23269)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021232")
	Mess("It's okay, the two of you must be tired from the longÅ^trip, just leave it to me!")
	hitretResult := Hitret(23270)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But I can at least cook the rice.")
	hitretResult := Hitret(23271)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK021233")
	Mess("Hey, Haru-kun you all like curry that's spicy? OrÅ^normal? Right now I'm going to put in the curry spicesÅ^and plate it.")
	hitretResult := Hitret(23272)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm fine with either.")
	hitretResult := Hitret(23273)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR020184")
	Mess("....... Medium spicy.")
	hitretResult := Hitret(23274)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_14M", 0, 0)
	Talk("Akira", "AK021234")
	Mess("Okay, then I'll adjust it medium spicy.")
	hitretResult := Hitret(23275)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Before the rice is cooked, Akira puts in the spices toÅ^adjust the taste, and also makes a mini salad.")
	hitretResult := Hitret(23276)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Other than Sora and myself, there are very few peopleÅ^who will use the kitchen to make the food, and I feltÅ^that it's a slighty novel thing.")
	hitretResult := Hitret(23277)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira who's wearing the apron, looks like a bride whoÅ^just got married and trying very hard to make the food,Å^and she's really cute.")
	hitretResult := Hitret(23278)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After letting Sora try the taste and getting an OK, itÅ^was cooked slightly longer, and the curry is completed.")
	hitretResult := Hitret(23279)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B32c", 0, 0)
	SetBustup("CC02_03M", 0, 0)
	SetBustup("CA02_01M", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Akira", "AK021235")
	Mess("Uwooo, this aroma is really delicious.")
	hitretResult := Hitret(23280)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's for sure since it's done by Akira.")
	hitretResult := Hitret(23281)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK021236")
	Mess("Ehee, the rice is done by Haru-kun both of you, and it'sÅ^a collaboration.")
	hitretResult := Hitret(23282)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("On the top of the table is the steaming curry, arrangedÅ^with mini salad as a garnish.")
	hitretResult := Hitret(23283)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("In the end I only cooked the rice, everything else isÅ^prepared by Akira.")
	hitretResult := Hitret(23284)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As expected of the one in charge of Yahiro-san's dinner.Å^Her skill in cooking is splendid.")
	hitretResult := Hitret(23285)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then, let's quickly eat. Itadakimasu-!")
	hitretResult := Hitret(23286)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020185")
	Mess("....... Itadakimasu.")
	hitretResult := Hitret(23287)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Mmu....... Haa~ It's so richly flavored! Hmm? Akira, areÅ^you not eating?")
	hitretResult := Hitret(23288)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_14M", 0, 0)
	Talk("Akira", "AK021237")
	Mess("That's not it, itadakimasu! Nomnom! It's so tasty-!")
	hitretResult := Hitret(23289)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira stuffs her mouth fully with the curry rice, andÅ^she seems to be eating it happily.")
	hitretResult := Hitret(23290)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Watching her act this way makes me put on a glad smile.")
	hitretResult := Hitret(23291)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Is it good, Sora?")
	hitretResult := Hitret(23292)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_05M", 0, 0)
	Talk("Sora", "SR020186")
	Mess("....... Yeah, it tastes better than Haru.")
	hitretResult := Hitret(23293)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uu..... Even though that is true....")
	hitretResult := Hitret(23294)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK021238")
	Mess("I want to try the curry Haru-kun makes the next time.")
	hitretResult := Hitret(23295)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04M", 0, 0)
	Talk("Sora", "SR020187")
	Mess("It's best to stop there.")
	hitretResult := Hitret(23296)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uu..... Indeed, my cooking compared to Akira's is not asÅ^good.")
	hitretResult := Hitret(23297)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_02M", 0, 0)
	Talk("Akira", "AK021239")
	Mess("No. Even if the curry uses the same spices, the taste isÅ^different. So I'm especially interested in otherÅ^people's curry.")
	hitretResult := Hitret(23298)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Is that so? It's true that we bought the same spicesÅ^from Takano's supermarket, but the taste is different.")
	hitretResult := Hitret(23299)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then I'll teach Akira the next time I make curry.")
	hitretResult := Hitret(23300)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021240")
	Mess("Yeah, fufuu, I will look forwar to~ Haru-kun's curry!")
	hitretResult := Hitret(23301)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But I will put this first, I can't do it as good asÅ^Akira, okay?")
	hitretResult := Hitret(23302)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021241")
	Mess("Nope, I'm sure it's delicious. Your cooking will slowlyÅ^improve, if you cook for someone else.")
	hitretResult := Hitret(23303)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_01M", 0, 0)
	Talk("Sora", "SR020188")
	Mess("Compared to Haru, Akira doing it will be faster.")
	hitretResult := Hitret(23304)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("H-Hey!")
	hitretResult := Hitret(23305)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_01M", 0, 0)
	Talk("Akira", "AK021242")
	Mess("If it doesn't trouble you, I can come over to help youÅ^make meals.")
	hitretResult := Hitret(23306)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No, it is bad of us, if we trouble you even for this.")
	hitretResult := Hitret(23307)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020189")
	Mess("If that's the case, Akira can just stay here.")
	hitretResult := Hitret(23308)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021243")
	Mess("Eh, I can be Haru-kun and Sora-chan's kid?")
	hitretResult := Hitret(23309)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_11M", 0, 0)
	Talk("Sora", "SR020190")
	Mess("........ If it happens that way...... Haru will beÅ^happy.")
	hitretResult := Hitret(23310)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("S-Sora...... You......")
	hitretResult := Hitret(23311)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_10M", 0, 0)
	Talk("Akira", "AK021244")
	Mess("...... Sora-chan......")
	hitretResult := Hitret(23312)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_11M", 0, 0)
	Talk("Akira", "AK021245")
	Mess("..... To live with Haru you all..... I'm reallyÅ^anticpating that.")
	hitretResult := Hitret(23313)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021246")
	Mess("Ehehe, you know I'll take your word for it if you saidÅ^those words, right?")
	hitretResult := Hitret(23314)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020191")
	Mess("...... If it's Akira...... It's fine.")
	hitretResult := Hitret(23315)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021247")
	Mess("Ahaa, thank you, Sora-chan....... I'm really happy, thatÅ^you have this thought.....")
	hitretResult := Hitret(23316)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Akira...... If it doesn't trouble you, then feel free toÅ^come, I'll be happy too.")
	hitretResult := Hitret(23317)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021248")
	Mess("Haru-kun......")
	hitretResult := Hitret(23318)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_04M", 0, 0)
	Talk("Akira", "AK021249")
	Mess("Ah, Sora-chan, you don't have any more curry! I'll fillÅ^it up for you!")
	hitretResult := Hitret(23319)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_13M", 0, 0)
	Talk("Sora", "SR020192")
	Mess("Eh!? I can't eat that much........")
	hitretResult := Hitret(23320)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021250")
	Mess("Haru-kun too! I'll give you a lot!")
	hitretResult := Hitret(23321)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh? N-no need, I just ate earlier!?")
	hitretResult := Hitret(23322)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	Talk("êSÇÃê∫", "")
	Mess("Akira grabs my plate, and went into the kitchen in aÅ^jumping manner.")
	hitretResult := Hitret(23323)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_06M", 0, 0)
	Talk("Sora", "SR020193")
	Mess("....... Haru, take responsibility.")
	hitretResult := Hitret(23324)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ehh..... Which is it?")
	hitretResult := Hitret(23325)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Sora", "SR020194")
	Mess("....... Everything.")
	hitretResult := Hitret(23326)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Uuu...... If it's not curry......")
	hitretResult := Hitret(23327)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_04M", 0, 0)
	Talk("Sora", "SR020195")
	Mess("....... Is it too early.")
	hitretResult := Hitret(23328)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("No...... It's not that.")
	hitretResult := Hitret(23329)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("There's a repeating noise from the kitchen.")
	hitretResult := Hitret(23330)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("After being approved by Sora, looking at Akira'sÅ^embarassed face, I don't understand why but I feelÅ^myself getting embarrassed too.")
	hitretResult := Hitret(23331)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, from now onwards the three of us can eat happilyÅ^like this...")
	hitretResult := Hitret(23332)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I'm really happy that things turned out like this.")
	hitretResult := Hitret(23333)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC02_04M", 0, 0)
	Talk("Akira", "AK021251")
	Mess("Here~ I kept you waiting-! Here's a second helping!!")
	hitretResult := Hitret(23334)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CA02_13M", 0, 0)
	Font(36, "")
	Talk("Haruka & Sora", "SR020196")
	Mess("EHH!!")
	hitretResult := Hitret(23335)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Placed in front of us, is a extra large plate of curryÅ^rice due to Akira's change in her mood.")
	hitretResult := Hitret(23336)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	EyeCatchEnter("", 0)
	Change("00_c022c")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

