Include"yosuga.ch"

Function sc00_D039()
	ScenarioEnter()
	ScPlayBgm("BGM10", 0)
	SetCg("B07b", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("After Kazuha fled, the one who was left behind headedÅ^toward me and greeted me with a feeble smile.")
	hitretResult := Hitret(32635)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_08S", 0, 0)
	Talk("Haruka", "")
	Mess("So you did realize I was here.")
	hitretResult := Hitret(32636)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("As I came out from my hiding spot, I became convincedÅ^that Akira had wanted to show me that scene.")
	hitretResult := Hitret(32637)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC06_07M", 0, 0)
	Talk("Akira", "AK040480")
	Mess("Haru-kun...I'm so sorry.")
	hitretResult := Hitret(32638)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira bowed her head toward me, as deeply as Motoka-sanÅ^would bow to Kazuha.")
	hitretResult := Hitret(32639)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But I don't understand. Why would she do such a thing?")
	hitretResult := Hitret(32640)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040481")
	Mess("I'm the one who held Kazu-chan back... Because she knewÅ^we were sisters, it looks like I was a curse for her...")
	hitretResult := Hitret(32641)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Curse.")
	hitretResult := Hitret(32642)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira labelled Kazuha's sympathy toward her as somethingÅ^that was almost revolting.")
	hitretResult := Hitret(32643)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If that was a curse, then how sinful is kindness?")
	hitretResult := Hitret(32644)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040482")
	Mess("What should I do to dispel that curse? Maybe I shouldÅ^just disappear?")
	hitretResult := Hitret(32645)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That...wouldn't work...")
	hitretResult := Hitret(32646)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even if she didn't mean it, such self-destructiveÅ^thoughts were making me even more depressed.")
	hitretResult := Hitret(32647)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040483")
	Mess("Don't worry... I won't do anything weird. Kazu-chan...Å^It'd hurt her, too.")
	hitretResult := Hitret(32648)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I know you're not that kind of person.")
	hitretResult := Hitret(32649)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040484")
	Mess("Thanks...")
	hitretResult := Hitret(32650)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't thank me for such small things... I still haven'tÅ^done anything yet.")
	hitretResult := Hitret(32651)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("For the very reason that Kazuha had cared so much forÅ^her, for Akira to put on that act... Even going as farÅ^to kiss her and to make Kazuha pull herself away.")
	hitretResult := Hitret(32652)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess(""Even if I have to throw myself away, I don't want to beÅ^a burden to you."")
	hitretResult := Hitret(32653)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess(""Rather than being with me, you'll be happy withÅ^Haruka."")
	hitretResult := Hitret(32654)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The fact that she didn't resent anyone showed herÅ^kindness that was impossible to comprehend.")
	hitretResult := Hitret(32655)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK040485")
	Mess("Haru-kun... It might seem strange coming from me, but...Å^Take care of Kazu-chan...")
	hitretResult := Hitret(32656)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahh... Okay.")
	hitretResult := Hitret(32657)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B27b", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Giving her a forceful nod, I ran down the stairs thatÅ^Kazuha had just ran down.")
	hitretResult := Hitret(32658)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If kindness was a curse, then Kazuha was not the onlyÅ^one being cursed.")
	hitretResult := Hitret(32659)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even Amatsume bore the same curse as Kazuha.")
	hitretResult := Hitret(32660)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Both of them needed to be released from that curse.")
	hitretResult := Hitret(32661)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	Change("00_d040")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

