Include"yosuga.ch"

Function sc00_B020C()
	ScenarioEnter()
	SetCg("B21a", 0, 0)
	Transition("WIP_LR", 500)
	Talk("êSÇÃê∫", "")
	Mess("When I came back from the toilet, it looks like everyoneÅ^was tired of playing around, I went back to our place.")
	hitretResult := Hitret(14703)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The poolside was wet, it's like a storm went by andÅ^carried the waters out.")
	hitretResult := Hitret(14704)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...?")
	hitretResult := Hitret(14705)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan is in the middle of the pool.")
	hitretResult := Hitret(14706)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("She She started doing a backstroke just like before,Å^then she floated just like that, the water didn't evenÅ^tremble as she floated like that.")
	hitretResult := Hitret(14707)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Nao-chan...")
	hitretResult := Hitret(14708)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan was staring at the sky as she looked up.")
	hitretResult := Hitret(14709)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's like she entrusted her body to the water as sheÅ^looked so calm, then she stared at the sky innocently.")
	hitretResult := Hitret(14710)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan had the same face back then, she lookedÅ^lonely...")
	hitretResult := Hitret(14711)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Her face looks lonely.")
	hitretResult := Hitret(14712)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Alright.")
	hitretResult := Hitret(14713)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I certainly felt something as I entered the poolÅ^quietly, without making any sound and ripples.")
	hitretResult := Hitret(14714)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	SetCg("B27a", 0, 600)
	WindowView(1)
	ScPlayBgm("BGM19", 0)
	Talk("ÉÇÉmÉçÅ[ÉOÅsìﬁèèÅt", "NO031051")
	Mess("Whenever I'm wrapped up with water, I feel at ease whenÅ^I look at the sky.")
	hitretResult := Hitret(14715)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅsìﬁèèÅt", "NO031052")
	Mess("I can see it so widely, the blue world of the sky.")
	hitretResult := Hitret(14716)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅsìﬁèèÅt", "NO031053")
	Mess("Even if I drift above the water, I feel like I'm meltingÅ^into the sky.")
	hitretResult := Hitret(14717)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅsìﬁèèÅt", "NO031054")
	Mess("I forget everything for a while... With this feeling.")
	hitretResult := Hitret(14718)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅsìﬁèèÅt", "NO031055")
	Mess("I can just close my eyes and feel like I'm reallyÅ^melting into it, as if I'll disappear.")
	hitretResult := Hitret(14719)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅsìﬁèèÅt", "NO031056")
	Mess("That's how I want to feel.")
	hitretResult := Hitret(14720)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅsìﬁèèÅt", "NO031057")
	Mess("But it's different now...")
	hitretResult := Hitret(14721)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("ÉÇÉmÉçÅ[ÉOÅsìﬁèèÅt", "NO031058")
	Mess("That was the sky before... But now I'm beside with...")
	hitretResult := Hitret(14722)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE203", 0)
	Talk("Nao", "NO031059")
	Mess("?")
	hitretResult := Hitret(14723)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Whoa!? Ooff!!")
	hitretResult := Hitret(14724)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE203", 0)
	Talk("Nao", "NO031060")
	Mess("? Haru-chan!?")
	hitretResult := Hitret(14725)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ooof!! I-I got you, I'm glad!!")
	hitretResult := Hitret(14726)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031061")
	Mess("Wh-what are you doing? That's dangerous!!")
	hitretResult := Hitret(14727)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅsìﬁèèÅt", "NO031062")
	Mess("Before I notice it, Haru-chan already came beside me.")
	hitretResult := Hitret(14728)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("ÉÇÉmÉçÅ[ÉOÅsìﬁèèÅt", "NO031063")
	Mess("But then I was floating around the deep part, Haru-chanÅ^could have drowned there.")
	hitretResult := Hitret(14729)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	WindowView(0)
	ScWait(1000, 0)
	SetCg("B21a", 0, 0)
	SetBustup("CB06_06L", 0, 0)
	Transition("WIP_BT", 500)
	ScPlayBgm("BGM03", 0)
	Talk("Nao", "NO031064")
	Mess("Geez, that's dangerous right? And you're still not goodÅ^at swimming Haru-chan.")
	hitretResult := Hitret(14730)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Ahaha... Sorry, I felt like jumping back in, andÅ^suddenly it was deep, so I panicked.")
	hitretResult := Hitret(14731)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031065")
	Mess("I got scared, why did you do that?")
	hitretResult := Hitret(14732)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("N-Nothing... It was just... I felt like scaring youÅ^Nao-chan...")
	hitretResult := Hitret(14733)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_13L", 0, 0)
	Update()
	ScWait(1000, 0)
	SetBustup("CB06_06L", 0, 0)
	Talk("Nao", "NO031066")
	Mess("Even so, it's still dangerous, right?")
	hitretResult := Hitret(14734)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Yeah... Sorry...")
	hitretResult := Hitret(14735)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_11L", 0, 0)
	Talk("Nao", "NO031067")
	Mess("Ah... No, I'm sorry, I think I was being hard on youÅ^too...")
	hitretResult := Hitret(14736)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Me too, sorry if I surprised you.")
	hitretResult := Hitret(14737)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_01L", 0, 0)
	Talk("Nao", "NO031068")
	Mess("Yep, you're forgiven.")
	hitretResult := Hitret(14738)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks.")
	hitretResult := Hitret(14739)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_02L", 0, 0)
	Talk("Nao", "NO031069")
	Mess("Hehehe.")
	hitretResult := Hitret(14740)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan made sure to push me in my forehead, afterÅ^that, we laughed.")
	hitretResult := Hitret(14741)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("... What were you doing?")
	hitretResult := Hitret(14742)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_01L", 0, 0)
	Talk("Nao", "NO031070")
	Mess("The sky... I was looking at it. It looked so good andÅ^peaceful so I felt like floating around.")
	hitretResult := Hitret(14743)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Heh... That sounds fun.")
	hitretResult := Hitret(14744)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031071")
	Mess("Wanna try?")
	hitretResult := Hitret(14745)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I would love to, though I'm still scared of it...")
	hitretResult := Hitret(14746)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031072")
	Mess("I can support you like a beat boat below, want to try?")
	hitretResult := Hitret(14747)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If that's how it is, you should be as calm as a rubberÅ^boat.")
	hitretResult := Hitret(14748)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_02L", 0, 0)
	Talk("Nao", "NO031073")
	Mess("Hehe, kind of.")
	hitretResult := Hitret(14749)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031074")
	Mess("It feels so great, that's why I always wanted to float.")
	hitretResult := Hitret(14750)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031075")
	Mess("If there's a chance, I would also want to look at theÅ^moon that way.")
	hitretResult := Hitret(14751)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That sounds dramatic.")
	hitretResult := Hitret(14752)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031076")
	Mess("Before that, we might catch a cold.")
	hitretResult := Hitret(14753)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("......")
	hitretResult := Hitret(14754)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...I hope I can do the same thing with you Nao-chan...Å^Will I see it too?")
	hitretResult := Hitret(14755)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_01L", 0, 0)
	Talk("Nao", "NO031077")
	Mess("...Eh...")
	hitretResult := Hitret(14756)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("It's like you're watching...")
	hitretResult := Hitret(14757)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 0)
	Talk("Nao", "NO031078")
	Mess("I don't like to disappoint you, though with your feetÅ^like that... I think it'll take more time...")
	hitretResult := Hitret(14758)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...Yeah...")
	hitretResult := Hitret(14759)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It's not just my body but also my feelings, my limbsÅ^feel so light as always too.")
	hitretResult := Hitret(14760)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I feel like I always need to use more of my strength.")
	hitretResult := Hitret(14761)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If I also put it into my consciousness, I'd sink again.")
	hitretResult := Hitret(14762)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("If I could just remove all of my feelings, I'll be ableÅ^to float, though that would be a miracle.")
	hitretResult := Hitret(14763)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031079")
	Mess("I'll help you... I'll let go now...")
	hitretResult := Hitret(14764)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I turn my face up as I float, I felt how she slowlyÅ^removed her hands from my back.")
	hitretResult := Hitret(14765)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("At that moment, I felt like I was about to sink, but IÅ^released all of my stress and tried to stay calm as myÅ^body floated around.")
	hitretResult := Hitret(14766)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031080")
	Mess("Hey... It's simple right?")
	hitretResult := Hitret(14767)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We slowly held onto each other's hands.")
	hitretResult := Hitret(14768)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan and I were floating sideways, and smiling.")
	hitretResult := Hitret(14769)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I slowly opened my eyes... And there...")
	hitretResult := Hitret(14770)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetCg("B27a", 0, 600)
	Transition("MOZCIR_", 1500)
	ScPlayBgm("BGM16", 0)
	Talk("Haruka", "")
	Mess("Whooaaaa...")
	hitretResult := Hitret(14771)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("My face was slightly submerged in the water while I wasÅ^looking up the sky.")
	hitretResult := Hitret(14772)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We drifted above the water as we looked up in the sky,Å^it felt like it's a miracle.")
	hitretResult := Hitret(14773)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It feels so great as if I'm wrapped into something.")
	hitretResult := Hitret(14774)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Somehow, it's just like we're floating in the sky.")
	hitretResult := Hitret(14775)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031081")
	Mess("...Yeah.")
	hitretResult := Hitret(14776)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I have my doubts on my balance as I float, I also can'tÅ^see Nao-chan's face.")
	hitretResult := Hitret(14777)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I was nervous which made me move a bit, then I felt likeÅ^I'm sinking.")
	hitretResult := Hitret(14778)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But, Nao-chan caught me.")
	hitretResult := Hitret(14779)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Like always... Nao-chan's looking after me.")
	hitretResult := Hitret(14780)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("This feeling... Nao-chan is completely holding me in herÅ^arms.")
	hitretResult := Hitret(14781)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Probably, even Nao-chan likes this feeling.")
	hitretResult := Hitret(14782)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thanks...")
	hitretResult := Hitret(14783)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031082")
	Mess("Eh...")
	hitretResult := Hitret(14784)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It's because you're teaching me a lot of stuffsÅ^Nao-chan, I probably wouldn't be able to feel this ifÅ^you didn't.")
	hitretResult := Hitret(14785)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031083")
	Mess(".......")
	hitretResult := Hitret(14786)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("It feels gentle somehow... I feel like I always want toÅ^float.")
	hitretResult := Hitret(14787)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031084")
	Mess("...Haru-chan too?")
	hitretResult := Hitret(14788)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yeah...")
	hitretResult := Hitret(14789)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Scroll(0, 0, 40000, 0, 0)
	Talk("Nao", "NO031085")
	Mess("I always look at this alone... this sky...")
	hitretResult := Hitret(14790)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031086")
	Mess("I even thought sometimes that you might be somewhereÅ^looking at the same sky, Haru-chan...")
	hitretResult := Hitret(14791)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031087")
	Mess("But... right now there's two of us... Looking at itÅ^together...")
	hitretResult := Hitret(14792)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I squeezed her hand tightly as we held hands.")
	hitretResult := Hitret(14793)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I slowly looked at Nao-chan, Nao-chan also looked at meÅ^as she smiled in a gentle way.")
	hitretResult := Hitret(14794)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Nao", "NO031088")
	Mess("It's just like a dream... To be with you, Haru-chan...Å^Like this...")
	hitretResult := Hitret(14795)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With a small pull, we got closer together as we floated.")
	hitretResult := Hitret(14796)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("For a little while, we continued to float like that asÅ^we melted into the blue sky.")
	hitretResult := Hitret(14797)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetCg("B21a", 0, 0)
	Transition("WIP_BT", 500)
	Talk("Nao", "NO031089")
	Mess("...Haru-chan...")
	hitretResult := Hitret(14798)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CB06_11L", 0, 0)
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan embraced me, I started sinking in the middle ofÅ^the water that instant.")
	hitretResult := Hitret(14799)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE204", 0)
	BustupClear()
	Talk("êSÇÃê∫", "")
	Mess("Nao-chan suddenly drew her face near mine and kissed me.")
	hitretResult := Hitret(14800)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Then we went out of the water, though I didn't careÅ^about losing air as we kept on kissing each other.")
	hitretResult := Hitret(14801)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BlackOut(0, 1)
	ScWait(2000, 0)
	Change("00_b021")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

