Include"yosuga.ch"

Function sc00_E025()
	ScenarioEnter()
	ScPlayBgm("BGM07", 0)
	SetCg("B03a", 0, 0)
	SetBustup("CE01_03M", 0, 0)
	Talk("心の声", "")
	Mess("抱擁を解くと、なぜか二人とも苦笑いになった。")
	hitretResult := Hitret(37738)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("さっきの盛り上がった気持ちを覚えているだけに、素に戻った")
	Mess("自分達が気恥ずかしいのかもしれない。")
	hitretResult := Hitret(37739)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("お互いに衣服を整えて、もう一度苦笑いになった。")
	hitretResult := Hitret(37740)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_06M", 0, 0)
	Talk("初佳", "MT051222")
	Mess("わっ……服に精液付いちゃってる……どうしよ……")
	hitretResult := Hitret(37741)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("悠", "")
	Mess("す、すみません……拭いて取れるかな……？")
	hitretResult := Hitret(37742)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("僕が考えなしにあんなに出すから……")
	hitretResult := Hitret(37743)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("初佳", "MT051223")
	Mess("付いてるのは取れるけど……染みになってるのはどうかなあ？")
	hitretResult := Hitret(37744)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_08M", 0, 0)
	Talk("初佳", "MT051224")
	Mess("全部脱いで洗うワケにもいかないし、乾けば……匂いでバレ／")
	Mess("ちゃうよね？")
	hitretResult := Hitret(37745)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("悠", "")
	Mess("ど、どうかな……")
	hitretResult := Hitret(37746)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("かなり全面的に僕が悪いので、上手い言い訳も思いつかない。")
	hitretResult := Hitret(37747)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_01M", 0, 0)
	Talk("初佳", "MT051225")
	Mess("洗面所貸して貰える？　取れるだけ取ってみるから。")
	hitretResult := Hitret(37748)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("悠", "")
	Mess("案内します……あ。")
	hitretResult := Hitret(37749)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlaySe("SE158", 0)
	Talk("心の声", "")
	Mess("携帯の着信音に、つい自分の携帯を確認する。")
	hitretResult := Hitret(37750)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CE01_03M", 0, 0)
	Talk("初佳", "MT051226")
	Mess("あ、私のだわ……お屋敷からかな、結構時間経っちゃってるか")
	Mess("ら……")
	hitretResult := Hitret(37751)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("悠", "")
	Mess("出て良いですよ。僕、向こう行ってますから。")
	hitretResult := Hitret(37752)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("初佳", "MT051227")
	Mess("うん、ゴメンね……やばいなぁ、大丈夫かな……")
	hitretResult := Hitret(37753)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear()
	Talk("心の声", "")
	Mess("初佳さんはスカートの染みを気にしつつ、携帯を耳と肩とで挟")
	Mess("む。")
	hitretResult := Hitret(37754)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("心の声", "")
	Mess("僕はそんな初佳さんのために、タオルでも濡らしておこうと、")
	Mess("洗面所へと向かった。")
	hitretResult := Hitret(37755)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	BlackOut(0, 0)
	Talk("初佳", "MT051228")
	Mess("よしっ、出るか………………あれ、この番号……")
	hitretResult := Hitret(37756)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	EyeCatchEnter("", 0)
	Change("00_e026")
	Return
	Change("EXIT_SCENARIO")
EndFunc

