Include"yosuga.ch"

Function sc00_C033C()
	ScenarioEnter()
	ScPlayBgm("BGM03", 0)
	SetCg("B08a", 0, 0)
	SetBustup("CC01_08M", 0, 0)
	Transition("WIP_RL", 500)
	Talk("Akira", "AK021947")
	Mess("..... Does it hurt? Still hot?")
	hitretResult := Hitret(25994)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though I reached into the fire for just a moment,Å^my thumb and index finger hurts due to holding on to theÅ^letter.")
	hitretResult := Hitret(25995)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("But it's just a little red, I probably won't even getÅ^blisters if it's sufficiently cooled.")
	hitretResult := Hitret(25996)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Thank you, I'm fine now.")
	hitretResult := Hitret(25997)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021948")
	Mess("I'm sorry...... You did something like that because ofÅ^me.....")
	hitretResult := Hitret(25998)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("To tell the truth..... I'm really scared.")
	hitretResult := Hitret(25999)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021949")
	Mess("...... Uuu.......")
	hitretResult := Hitret(26000)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I'm afraid I won't be able to talk to Akira again.")
	hitretResult := Hitret(26001)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021950")
	Mess("Haru-kun......")
	hitretResult := Hitret(26002)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hey!!")
	hitretResult := Hitret(26003)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_12M", 0, 0)
	Shake(2)
	Talk("Akira", "AK021951")
	Mess("Nyaa!?")
	hitretResult := Hitret(26004)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I pull Akira's both cheeks.")
	hitretResult := Hitret(26005)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Really, you're making me worry until the end!")
	hitretResult := Hitret(26006)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_09M", 0, 0)
	Talk("Akira", "AK021952")
	Mess("Auuuu.... S, sorry....")
	hitretResult := Hitret(26007)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("I lightly poke her cheeks then gently stroke it.")
	hitretResult := Hitret(26008)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_08M", 0, 0)
	Talk("Haruka", "")
	Mess("I'm the one who should apologize, doing something likeÅ^that without asking. I'm sorry. I might have thoughtÅ^it's for the sake of Akira, but it might be possibleÅ^that I did it for myself.")
	hitretResult := Hitret(26009)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_13M", 0, 0)
	Talk("Akira", "AK021953")
	Mess("No...... I'm the one who should be sorry for hidingÅ^things..... So......")
	hitretResult := Hitret(26010)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Can we stop apologizing to each other over this matterÅ^again?")
	hitretResult := Hitret(26011)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_07M", 0, 0)
	Talk("Akira", "AK021954")
	Mess("...... Okay.")
	hitretResult := Hitret(26012)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Then....... what should we do next, about that?")
	hitretResult := Hitret(26013)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We look at the charred envelope.")
	hitretResult := Hitret(26014)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK021955")
	Mess("Hey, how much does it cost to do a test?")
	hitretResult := Hitret(26015)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm...... About 50000 yen.")
	hitretResult := Hitret(26016)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_12M", 0, 0)
	Jump(2, 0)
	Talk("Akira", "AK021956")
	Mess("5! 50000 yen!!")
	hitretResult := Hitret(26017)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira jumped and thrusts out both her hands. That'sÅ^100000 yen!!")
	hitretResult := Hitret(26018)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("I did not pay for the full amount. Yahiro=san said it'sÅ^the salary that hasn't been given to Akira...... andÅ^paid as well.")
	hitretResult := Hitret(26019)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_11M", 0, 0)
	Talk("Akira", "AK021957")
	Mess("Yahiro-oneechan.....")
	hitretResult := Hitret(26020)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("You can't be deceived by her okay? Your part-time jobÅ^money needs to be collected.")
	hitretResult := Hitret(26021)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021958")
	Mess("Ahaha, Haru-chan is really shrewd.")
	hitretResult := Hitret(26022)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yup, this and that are two different things.")
	hitretResult := Hitret(26023)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... Can it be read? Perhaps it can't be seen clearlyÅ^if it's burned.")
	hitretResult := Hitret(26024)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK021959")
	Mess("...... Mmm. This is something that everyone has done forÅ^me.")
	hitretResult := Hitret(26025)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Do you want to read it alone?")
	hitretResult := Hitret(26026)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_08M", 0, 0)
	Talk("Akira", "AK021960")
	Mess("........ No. With Haru-kun together....... please.....")
	hitretResult := Hitret(26027)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Talk("Haruka", "")
	Mess("Okay...... I understand.")
	hitretResult := Hitret(26028)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira picks up the envelop, and the burned partsÅ^crumbled away.")
	hitretResult := Hitret(26029)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM16", 0)
	SetBustup("CC01_05M", 0, 0)
	Talk("Akira", "AK021961")
	Mess("If, the things that are written on it states that I'mÅ^Haru-kun's family, what should I do?")
	hitretResult := Hitret(26030)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("At that time, Sora and I are the younger siblings.")
	hitretResult := Hitret(26031)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK021962")
	Mess("Ahaa, it's also good if I become the older sister.")
	hitretResult := Hitret(26032)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("If we get married, Sora will become the younger sister.")
	hitretResult := Hitret(26033)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021963")
	Mess("Younger sister?")
	hitretResult := Hitret(26034)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("We're twins, so Sora says we don't have any ranking.")
	hitretResult := Hitret(26035)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("But, a Hikkimori sister is just not fitting, so it'sÅ^just a convenience for me to become the older brother.")
	hitretResult := Hitret(26036)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021964")
	Mess("Woah~ Even though it's a bit late, this is an amazingÅ^bit of news. But, Haru-kun and I getting married, I'mÅ^really happy.")
	hitretResult := Hitret(26037)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Don't get disillusioned from now onwards.")
	hitretResult := Hitret(26038)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_11M", 0, 0)
	Talk("Akira", "AK021965")
	Mess("Nope...... I'll like you more and more.")
	hitretResult := Hitret(26039)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Th, that's really embarrassing.....")
	hitretResult := Hitret(26040)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_14M", 0, 0)
	Talk("Akira", "AK021966")
	Mess("Shame~ Shame~!!")
	hitretResult := Hitret(26041)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Stop that Akira!")
	hitretResult := Hitret(26042)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_13M", 0, 0)
	Talk("Akira", "AK021967")
	Mess("Well...... let's start reading.......")
	hitretResult := Hitret(26043)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("....... Okay.")
	hitretResult := Hitret(26044)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We opened the letter, and the fragments fly apart.")
	hitretResult := Hitret(26045)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The contents in the letter contain various graphs andÅ^tables, but we don't understand it.")
	hitretResult := Hitret(26046)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_10M", 0, 0)
	Talk("Akira", "AK021968")
	Mess("Hmmm, STR, DNA, genes, Parent-Child RelationshipÅ^Index???")
	hitretResult := Hitret(26047)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("There's even Strength index, this really resembles aÅ^game huh....... Let's skip the complicated sections.....")
	hitretResult := Hitret(26048)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021969")
	Mess("...... According to the DNA Locus, the Parent-ChildÅ^relationship is......")
	hitretResult := Hitret(26049)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("ÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(26050)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_05M", 0, 0)
	Talk("Akira", "AK021970")
	Mess("This part is burned off and it can't be read......")
	hitretResult := Hitret(26051)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Y, yes...... Indeed......")
	hitretResult := Hitret(26052)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("The explanation containing the biology section wasÅ^also......... The crucial section had been burned awayÅ^and it cannot be read.")
	hitretResult := Hitret(26053)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01M", 0, 0)
	Talk("Akira", "AK021971")
	Mess("....... There's one more sheet of paper, what is this?")
	hitretResult := Hitret(26054)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's Migiwa-san's test results.")
	hitretResult := Hitret(26055)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021972")
	Mess("...... Can we take a look at it?")
	hitretResult := Hitret(26056)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes...... Let's look at it together.")
	hitretResult := Hitret(26057)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	SetBustup("CC01_13M", 0, 0)
	Talk("Akira", "AK021973")
	Mess("....... Okay.")
	hitretResult := Hitret(26058)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("It wasn't just Akira. In order to be safe, we requestedÅ^Migiwa-san to take the test.")
	hitretResult := Hitret(26059)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("With two parties aligned together, it will becomeÅ^irrefutable proof.")
	hitretResult := Hitret(26060)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Hmm..... But it is the same as before, according to theÅ^DNA locus, the Parent-Child relationship is...... theÅ^latter part cannot be read.")
	hitretResult := Hitret(26061)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021974")
	Mess(".....ÅwBased on the child's DNA analysis...... theÅ^alleged mother, cannot be excluded.......")
	hitretResult := Hitret(26062)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("óI", "")
	Mess("ÅcÅcÅcÅcÅcÅcÅcÅcÅcÅcÅc")
	hitretResult := Hitret(26063)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_08M", 0, 0)
	Talk("Akira", "AK021975")
	Mess("as the biological mother of the child.Åx ???? What doesÅ^that mean?")
	hitretResult := Hitret(26064)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScPlayBgm("BGM17", 0)
	Talk("Haruka", "")
	Mess("Ahahaha....... It is exactly as what the words state!!!!")
	hitretResult := Hitret(26065)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021976")
	Mess("Eh?")
	hitretResult := Hitret(26066)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("This means, the test is unable to deny that she is theÅ^mother, it means that she is Migiwa-san's mother!!")
	hitretResult := Hitret(26067)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02M", 0, 0)
	Talk("Akira", "AK021977")
	Mess("Then, then! Erm!! Erm--!!")
	hitretResult := Hitret(26068)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That's right!")
	hitretResult := Hitret(26069)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_11M", 0, 0)
	Talk("Akira", "AK021978")
	Mess("Kazu-chan is...... Kazu-chan's mother's child right!!")
	hitretResult := Hitret(26070)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("ÅwÅcÅc The possibility is above 99.9%Åxwithout a doubt.")
	hitretResult := Hitret(26071)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_04M", 0, 0)
	Jump(2, 0)
	Talk("Akira", "AK021979")
	Mess("Yahoo-!! Kazu-chan is Kazu-chan!!")
	hitretResult := Hitret(26072)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("That means, Akira is also, Akira!!")
	hitretResult := Hitret(26073)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_04L", 0, 0)
	Talk("Akira", "AK021980")
	Mess("I am also, I!?")
	hitretResult := Hitret(26074)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Yes....... Akira is, Amatsume Akira.")
	hitretResult := Hitret(26075)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_11L", 0, 0)
	Talk("Akira", "AK021981")
	Mess("Ha~~~nyan........ That's a relief----.........")
	hitretResult := Hitret(26076)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("...... Yes. It's great that it was needless worry on myÅ^grandmother's part-!!")
	hitretResult := Hitret(26077)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We both held each other hands and got happy together.")
	hitretResult := Hitret(26078)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_05L", 0, 0)
	Talk("Akira", "AK021982")
	Mess("Ah----, I feel like my strength has escaped me.....")
	hitretResult := Hitret(26079)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Me too........")
	hitretResult := Hitret(26080)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_02L", 0, 0)
	Talk("Akira", "AK021983")
	Mess("But......... This is really great.......")
	hitretResult := Hitret(26081)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Akira........")
	hitretResult := Hitret(26082)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("We hugged each other tightly.")
	hitretResult := Hitret(26083)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Even though we went back to the starting point....... itÅ^was completely different from the worst result we wereÅ^worried about.")
	hitretResult := Hitret(26084)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Sheesh....... there should be a limit for scaringÅ^people........")
	hitretResult := Hitret(26085)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_11L", 0, 0)
	Talk("Akira", "AK021984")
	Mess("No....... Granny was still worried about me to the finalÅ^moment....... With this, she can finally rest in peace.")
	hitretResult := Hitret(26086)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("........ Thank you, Akira")
	hitretResult := Hitret(26087)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021985")
	Mess("No....... It should be me who needs to thank you......")
	hitretResult := Hitret(26088)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Haruka", "")
	Mess("Bu...... This has gone back to how it was before. In theÅ^end we still don't who Akira's mother is.....")
	hitretResult := Hitret(26089)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_01L", 0, 0)
	Talk("Akira", "AK021986")
	Mess("Nope...... Didn't Haru-kun said it eaerlier? From nowÅ^onwards, I can do whatever I like.....")
	hitretResult := Hitret(26090)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("Akira", "AK021987")
	Mess("I can search for my mother. I can also go out to theÅ^world.")
	hitretResult := Hitret(26091)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	SetBustup("CC01_11L", 0, 0)
	Talk("Akira", "AK021988")
	Mess("Haru-kun....... has made me free.")
	hitretResult := Hitret(26092)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	Talk("êSÇÃê∫", "")
	Mess("Akira gently smiled at me and came closer to my face.")
	hitretResult := Hitret(26093)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	BustupClear(2)
	Talk("Akira", "AK021989")
	Mess("Mmmm..... chuu..... Haru-kun.... I love you.")
	hitretResult := Hitret(26094)
	If ((hitretResult == 30) | (hitretResult == 20))
		Change("EXIT_SCENARIO")
		Return
	ElseIf (hitretResult == 40)
		Return
	EndIf
	ScStopBgm()
	Change("00_c034")
	Return
	Change("EXIT_SCENARIO")
	Return

EndFunc

